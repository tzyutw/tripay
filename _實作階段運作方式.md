# 實作階段運作方式

> 2026-09-05 訂。實作期間以本檔為準。
> 對象：Rozi（看流程）、終端機 Claude Code（看自己要做什麼）、Cowork（看自己要做什麼）。

---

## 一、Rozi 的訴求（這份文件存在的理由）

**除了必要的按鍵，不要留她在電腦前。**

實作、除錯、複驗、上線，全部由 Claude 兩端跑完。
交付給她的是「**已上線**」＋一兩句重點，不是過程、不是待核可清單、不是請她幫忙抓錯。

---

## 二、誰做什麼

| | 負責 | 不做 |
|---|---|---|
| **終端機 Claude Code** | 全部實作、寫單元測試、跑測試、修到全綠、commit | 不 push（等 Cowork 放行）；不對 production 下 DDL；**不寫驗收判定程式、不產出對帳表／完成度報表** |
| **Cowork（雲端對話）** | 排巡檢、**寫驗收斷言並自己跑**、獨立複驗、放行或退回、套 production migration、上線後驗證、更新文件 | 不接手產品實作 |
| **Rozi** | 按「收」。就這樣 | 不核可草稿、**不找 bug**、不上版 |

> **「找 bug」是 Cowork 的事，不是 Rozi 的、也不是終端機自己找自己的。**
> 終端機找自己的錯＝球員兼裁判（212 對帳表就是這樣蓋章過去的）；
> Rozi 找＝驗收失效才會發生的事，發生了就是 Cowork 沒做到。

---

## 三、一條線的流程

1. **Rozi 按一次「收」**
2. 終端機讀 `_指令佇列.md` 最新未處理節次 → 跑 **實作-B（實作-C 已併為其後半段）** → 進「改 → 跑測試 → 讀失敗 → 再改」迴圈直到全綠 → **commit，但不 push**
3. 終端機把結果寫進 `_停點.md`（≤50 行）
4. **Cowork 每 5 分鐘巡檢**（見第四節）
5. 跑完 → **Cowork 獨立複驗**（見第六節）——不採信終端機的回報，自己跑一遍
6. **全過** → Cowork 在 repo 根目錄寫 `_可以推了.txt` → 終端機偵測到才 push → GitHub Actions 自動上版 → Cowork 驗線上 → **回報 Rozi**
7. **沒過** → Cowork 寫 `_要修的.md`（只列真的壞掉的）→ 終端機修 → 回到第 5 步

**Rozi 在整條線上只出現在第 1 步和第 6 步末端。**

---

## 四、自動巡檢設定（Cowork）

- **頻率**：每 5 分鐘一次，跑完自己續排下一次（`send_later`），不需要人重啟
- **看什麼**：`_停點.md` 的 mtime、`git log`、佇列節次有沒有標 `[已處理]`、10 分鐘內有無檔案異動

| 巡檢看到 | 動作 |
|---|---|
| 有檔案在動 | 什麼都不做，再排 5 分鐘。**不打擾 Rozi** |
| 連兩輪（約 10 分鐘）完全沒動、且還沒做完 | **它停住了** → 通知 Rozi「再按一次收」 |
| 節次已 `[已處理]` 且已 commit | 進入複驗 |
| 它回報卡住 | Cowork 讀 `_停點.md` 自己處理，不轉給 Rozi |

---

## 五、唯一需要 Rozi 動手的事，以及為什麼消不掉

**按「收」。**

原因：終端機 Claude Code **一輪講完話就會停在提示字元等輸入**。
佇列檔裡寫「做完直接接下一節、不要停」對它無效——那是它讀到的內容，不是驅動它的東西，
它需要一個**輸入事件**才會醒。

Cowork 能不能代打？**不能。** macOS 對終端機類應用只開放「看得到、點得到」，
**不給程式化打字**（系統層面的限制，不是設定問題）。

**已做的降低次數措施：**
- 把「實作-C」併成「實作-B」那一節的子段 → 一次「收」把 B、C 一起做完，不是兩次
- 佇列裡寫明：收到「收」時先讀 `_停點.md` 確認上次做到哪，**從沒做完的地方接著做，不重跑已全綠的**

所以中途停住，Rozi 再打一次「收」即可，不會從頭來。

---

## 六之零、驗收判定的五條規則（2026-09-06 生效，最高效力）

由來：實作-B 回報「212 項對帳表全對完」，Rozi 上線後第一批截圖就抓到
「盤點表寫著要拿掉、實作沒拿掉」的項目。回查產生那張表的
`實作_212對帳表.cjs`，判定規則是「這個畫面有測試檔存在 → 這個畫面底下所有項目算完成」，
13 個畫面全都有測試檔，**212 項裡只有 12 項被真的量過，200 項是自動蓋章**。
「打磨過的原型」沒有白費，白費的是「從原型搬到實作」這一段的驗收。

---

**1. 驗收判定不准由實作者寫。**
終端機寫單元測試沒問題，但「這一項做完了沒」的判定程式由 **Cowork 寫、Cowork 跑**。
實作者寫自己的驗收判定，就會出現「有測試檔＝完成」這種放自己過的規則。
終端機若需要自檢，寫在自己的測試裡，**不得**產出「對帳表／完成度報表」這類判定文件。

**2. 禁止「存在即完成」。**
以下一律**不算**某一項已完成的證據：
- 檔案存在
- 有 import
- 該畫面有測試檔
- 某串字出現在原始碼裡

由來：S-02c-10「三層 fallback」的檢查是「`Avatar.tsx` 存在 ＋ `TripFormSheet` 有 import
＋ 沒有 `useState('🙂')`」，三條全過——畫面上照樣跑出 🙂。
**元件做對了，用錯地方，這種檢查看不出來。**

**3.「要移除」的項目一律用反向斷言。**
問的是「**還在不在**」，不是「有沒有做」。
這 28 項最容易漏，因為移除了沒事、沒移除也沒事，測試兩邊都綠。
每一項要有一條「這個東西在畫面上出現就紅」的斷言。

**4. 量畫面渲染出來的結果，不是原始碼字串。**
每一項驗收都要指到 `Tripay_原型.html` 的行號，判定寫成：
「原型第 N 行 有／沒有 X → 實作**畫面上** 有／沒有 X」。
原始碼字串比對只能當輔助線索，**不能當判定**。

**6. 講「現況是什麼」之前，先跑一次或量一次——讀碼推論不算。**
版面已經做到（每項量渲染值），**功能行為還沒**。要說「這功能現在會怎樣」，
就得真的走一次（瀏覽器實走／`begin…rollback` 實跑／harness 造一筆看它長什麼樣）。
- **6a**：手上剛查到的資料，要跟正要講的結論對照一次。
  （2026-09-06：Cowork 查出 128 筆歷史消費付款人有三個不同的人、把結果印出來，
  下一句卻說「只算一個人只能選自己」。）
- **6b**：盤點類文件會過時，引用前先查證。任何「現況是 X」的陳述，
  來源只能是剛剛量到／查到的，不能是文件。文件用來知道「當初為什麼這樣決定」。
  （2026-09-06：`Tripay_問題優先序清單.md` 11 項裡 13 項已解，
  「缺代墊模式」根本不是缺口。）

**5. 每個逃掉的 bug 都要追溯到「哪一條斷言本來該擋住它」。**
Rozi 每抓到一個問題，回報時必須附：
- 本來該擋住它的是哪一條斷言？
- 沒有這條 → 補上，並說明補在哪
- 有這條但沒擋住 → 那條斷言壞在哪、怎麼修

沒做這步，同一類問題會一直漏。這條適用於**所有**Rozi 回報的問題，不分大小。

---

## 六、複驗要驗什麼（Cowork 自己跑，不採信終端機回報）

自己跑：`npx tsc --noEmit`／`pnpm run build`／`npx vitest run`。

**B（畫面）**
- 212 項逐項驗過——依「六之零」五條規則，**Cowork 自己寫斷言、自己跑**。
  「有測試檔就算完成」這種判定視同沒驗。
- 十個畫面 textContent 跟原型（操作模式）比對
- P1-0：填外幣不得被塞進 `split_amount`
- S-04-8＋S-04-29 同一個 commit，且存空金額後結算不回 422
- 成員刪除守衛三種情形
- 支付方式連動且無寫死常數
- 320／375／414 不橫向捲動
- `EmojiPicker` 已刪且零 import

**C（引擎、結算、上線把關）**
- 兩種結算模式逐人淨額相同（只能路徑不同）
- 五組版面回歸測試的**反向驗證真的會紅**
- 分享頁端到端：正確 token 看得到、改一字元看不到

**Cowork 的獨立檢查（不在終端機的清單裡）**
- 斷言有沒有「找不到目標就假通過」——`null < 54`、regex 跨分號、沒剝 SQL 註解這類。
  終端機自己在 A2 就踩到兩個同型的坑，代表這類問題會反覆出現。
- 十二個畫面逐張看過（機器抓得到壞掉，抓不到難看）
- **回報失敗前一定換第二種方法再量一次**——過去多次是斷言寫錯，不是程式錯

---

## 七、退回時的界線（Cowork 寫 `_要修的.md` 的規矩）

**只列真的壞掉的**：測試紅的、build 失敗的、停止條件沒過的、跑起來會錯的
（算錯帳、按不到、看得到不該看的）。

- **一個症狀只改造成它的那一處**，不要擴大成「順便整理一下」
- 不是壞掉、只是覺得可以更好的 → 寫進「順手看到但沒動」給 Rozi 決定，**不要動**

由來：#33／#34 兩輪 Cowork 自作主張加的工項比 Rozi 要求的多，原型檢視從三輪拖成十一輪。

---

## 八、什麼叫「可以交付給 Rozi 了」

以下**全部**成立才回報：

- [ ] build 綠、測試綠
- [ ] 212 項對帳表對完
- [ ] 複驗清單（第六節）全過
- [ ] 已 push、GitHub Actions 上版完成
- [ ] **線上驗過**：Supabase anon 六類讀取全 0、owner 讀取 4 trips／404 expenses／16 members；分享頁端到端

回報格式：**一句「B、C 都過了，已上線」＋一兩句重點。** 不貼過程。

---

## 九、中途只有這五種情況才打斷 Rozi

1. 不可逆且無備份的動作（刪資料若先備份、事後可還原，視為可逆；加欄位／加索引／加政策不算）
2. 要花錢
3. 安全性／隱私
4. 重大範疇取捨（新 Phase、砍功能這種等級）
5. 需要 Rozi 記憶或偏好才能回答的資料題

**外加一項必要通知（不算打擾）**：終端機停住了，請她再按一次「收」。

**流程進度一律不要問她。**

---

## 十、Stop hook（實作期間的自動續跑機制）

**問題**：終端機 Claude Code 一輪講完話就停在提示字元等輸入。佇列檔裡寫「不要停」對它無效——
那是它讀到的內容，不是驅動它的東西。而 macOS 對終端機類應用只開放「看得到、點得到」，
**不給程式化打字**，Cowork 無法代打。

**做法**：用 Claude Code 的 Stop hook，在它「想結束這一輪」時擋下來。

| 檔案 | 作用 |
|---|---|
| `.claude/settings.local.json` | 註冊 Stop hook（已被 `.gitignore` 忽略） |
| `.claude/hooks/keep-going.sh` | 判斷要不要擋（已被 `.gitignore` 忽略） |
| `.claude/.keepgoing_count` | 連續擋下的次數，硬上限 15，避免無限迴圈 |

**擋下的條件**（任一成立就不准結束）：
1. 專案根目錄還有 `_要修的.md`（Cowork 複驗退回的項目沒處理完）
2. 本機還有 commit 沒推上 `origin/main`

**逃生門**：專案根目錄建 `_停.txt`（寫明原因）→ 立刻放行。
終端機遇到五類例外時就是走這個。

**停用**：刪掉 `.claude/settings.local.json` 的 `hooks` 區塊即可（或整個檔案刪掉）。
**實作階段結束後建議拆掉**——平常開發不需要「不准停」。

**實測效果**：加上之前，B 的七個子節做完第一個就停（Rozi 得反覆按「收」）；
加上之後，B-1～B-7 加實作-C 一次「收」連續跑完，中間一次都沒停。


---

## Stop hook 的閘門（2026-09-08 Rozi 拍板「方案 A」）

**改之前**：hook 的擋停條件是「`_要修的.md` 還在」**或**「本機有 commit 沒推」。
第二條讓終端機為了能停下來會**自己先 push**，GitHub Actions 隨即部署，
**未經 Cowork 複驗的版本直接上到 Rozi 手機**。2026-09-07～08 發生三次（YXW／Z／AB）。

**改之後**：第二條加上閘門——**只有 `_可以推了.txt` 存在時，「沒推」才算未完成**。

新的順序：

1. 終端機做完 → commit → 寫 `_停點.md` → **停下來**（不推）
2. Cowork 重建 harness、跑全套判定 → 過關才在專案根目錄寫 `_可以推了.txt`
3. **Rozi 再打一次「收」** → hook 看到 `_可以推了.txt` ＋ 有未推 commit → 擋住不讓停 → 終端機 push
4. push 成功後把 `_可以推了.txt` 改名到 `Claude outputs/_可以推了_已推.txt`

**代價**：每一輪 Rozi 要多按一次「收」。這是她知情後選的。

**複驗沒過時**：不寫 `_可以推了.txt`，改寫 `_要修的.md`——第一條會擋住它，
終端機會自己接著修，**不需要 Rozi 按第二次**。

⚠️ 這個 hook 是 **Cowork 越界自己改的，尚未經第二人檢查**。
改前備份：`Claude outputs/_keep-going_改前備份.sh`。三種情境已實測：
`_要修的.md` 在 → 擋；兩個檔都不在 → 放行（可以停）；只有 `_可以推了.txt` ＋ 有未推 → 擋。
**下一節指令要求終端機覆核。**


## 常設協定：Rozi 只按第一次「收」（2026-09-08 裁示）

她的原話：「終端機停不下來會自己一路做完；做完它會每 60 秒等我的放行檔，
我複驗過關就放，它自己 push——後面也都要這樣的做法。」

**每一輪固定跑法**：

1. Cowork 把工項寫進 `_要修的.md`（不是佇列新節次）→ Stop hook 的第一條會擋住終端機，
   **它自己一路做完，不需要 Rozi 按收**。
2. 終端機做完 `git commit`，**不 push**，改成每 60 秒檢查 `_可以推了.txt`，最多等 2 小時
   （`CLAUDE.md` 已有這條等待協定）。
3. Cowork 重建 harness、跑全套判定。
   - 過關 → 寫 `_可以推了.txt` → 終端機輪詢撿到 → 自己 push → 歸檔成
     `Claude outputs/_可以推了_已推.txt`。
   - 沒過 → 覆寫 `_要修的.md`，回到第 1 步。
4. **只有這幾種情況才叫 Rozi**：
   ①終端機連兩輪（約 10 分鐘）完全沒動作 ②遇到五類例外 ③需要她拍板的產品決策。

⚠️ 用佇列新節次（`## 實作-XX`）會需要她按「收」，**所以之後一律用 `_要修的.md` 當入口**；
佇列只留當作規格文件用的長篇說明。
