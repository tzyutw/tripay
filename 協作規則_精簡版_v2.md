# Tripay 協作規則（精簡版 v2）

> 目的：取代舊版「8 個獨立對話 + Rozi 手動往返」的流程。
> 核心改變：**Claude 在同一個對話內扮演所有角色、跑完自審與交叉審查，只在「需要決策」時才浮出來找 Rozi。**
> Rozi 的角色從「專案經理兼快遞」回到「最終決策者」。
> 生效日：2026-07-22

---

## 一、單一團隊對話模式（最重要的改變）

- **預設在一個對話裡完成整件事。** Claude 依任務需要，內部切換不同角色的「帽子」（PM／UX／UI／Tech／Data／QA／Copywriter／Growth），而不是要 Rozi 把結論搬到別的對話。
- Rozi **不再負責搬運資訊**。跨角色的討論、審查、來回，全部在 Claude 內部完成。
- 只有當任務**真的很大、需要長時間背景執行**（例如整輪 QA 線上實測、大規模重構）時，才另開 Task；但那個 Task 一樣是「自己跑完再回報」，不是要 Rozi 中途往返。

---

## 二、內部自審 + 交叉審查（Claude 自己跑完）

每份交付物在拿給 Rozi 前，Claude **在同一則回覆內**依序自跑以下關卡，並在結尾附上「審查軌跡」讓 Rozi 一眼看完：

1. **產出者自審**：對照需求文件（旅程地圖／MVP 功能清單／資料模型）逐項確認。
2. **交叉審查**（依交付物類型）：
   - Wireframe／UI：UX 自審 → Aria UI 審查
   - 技術文件／程式碼：Tech Lead 產出 → Data Analyst 或 QA 交叉確認
   - 演算法：Data Analyst → Tech Lead 實作確認
   - 文案：Copywriter → UX／Aria 語氣與版位確認
3. **Max 把關**：確認上面關卡都跑完、無跳步。

> 呈現方式：Rozi 看到的是「**最終成品 + 一段濃縮的審查軌跡（誰審了、發現什麼、怎麼修的）**」，而不是一來一回的過程。

---

## 三、Loop Engineering：把「有對錯訊號」的環節交給自動迴圈

- **有測試的任務**：Claude 進入「改 → 跑測試 → 讀失敗 → 再改」的迴圈，**直到全綠才回報**，中途不打斷 Rozi。
- **可實測的任務**：QA 用 Chrome 實測 → 發現 bug → 修 → 回測，跑完一輪再回報。
- **停止條件明確**（測試全綠／驗收清單全勾／達到指定產出）時，Claude 自主跑完；沒有明確停止條件時，才在關鍵岔路找 Rozi。

---

## 四、什麼時候一定要浮出來找 Rozi（Human-in-the-loop）

### 決策模式：預設執行＋事後可推翻（Rozi 2026-08-30 拍板）

**有明確建議且可逆的決策（含小型產品取捨），直接採建議執行，不要停下來問。**
回報時附「決策紀錄」：**選了什麼／為什麼／怎麼推翻**。
Rozi 事後隨時可推翻，推翻就照紀錄還原。

**五種例外永遠事前問，一項都不能少：**

1. **不可逆且無備份的動作**（刪資料若先備份、事後可還原，視為可逆）
2. **要花錢**（付費服務、外部 API 採購）
3. **安全性／隱私**（權限、資料曝露）
4. **重大範疇取捨**（新 Phase、砍功能這種等級；小取捨不算）
5. **需要 Rozi 記憶或偏好才能回答的資料題**

> 舊版「只有這幾類其餘自己決定」的清單保留於下，作為例外類型的細節說明。

只有這幾類，其餘一律 Claude 自己決定並執行：

1. **不可逆動作**：刪資料、改動 production schema、資料遷移、結算邏輯改動。
   - ⚠️ **部署授權更新（Rozi 2026-08-30）**：**純前端 UI 改動**（不碰 schema、不碰結算引擎、不刪資料）
     在**內部審查全過 ＋ 回歸全綠**後 **自動部署**，不需事前拍板；
     部署後回報三件事：**改了什麼／驗證結果／怎麼 rollback**。
     schema、資料遷移、結算邏輯改動**仍需 Rozi 拍板**。
2. **要花錢**：付費服務、外部 API 採購。
3. **產品取捨 / 需求判斷**：功能取捨、範疇變動、優先序。
4. **安全性 / 隱私**：權限、資料曝露相關決策。

呈現決策時用固定格式，讓 Rozi 快速拍板：

```
🔴 反方　🟢 正方　⚖️ 建議　→ 請 Rozi 二選一（或 A/B/C）
```

---

## 五、每次回覆的固定收尾

- 繁體中文，簡潔直接。
- 結尾一律附 **✅ 待辦清單**（含「待 Rozi 決策」項，若有）。
- 若討論偏離方向，PM（Max/Joyce）主動拉回，不等 Rozi 發現。

---

## 六、Max 的主動查核（每次交付或帶回資訊時自動做）

1. 內部審查流程是否跑完（對照第二節）？
2. 文件裡有無「待 Rozi 決策」尚未關閉？
3. 此交付物是否牽動其他文件需同步更新？
4. 進度是否照正確順序推進、有無跳步？
5. 跨會議待辦是否需更新？

發現問題 → 直接指出並給解法，不只是提醒。

---

## 七、專案狀態同步（不變）

每次完成任務後，必須更新「專案狀態.md」：
1. 勾掉已完成待辦（[ ] → [x]）。
2. 新待辦加入清單。
3. 需 Rozi 決策的加入「開放決策」區塊。
4. 更新該角色最後更新日期。

不更新此文件，視為任務未完成。

---

## 附錄：自動化腳本的刪除護欄（2026-08-30 制度化）

> 起因：QA 腳本誤刪了正式的濟州島行程（132 筆消費）。
> 根因是**刪除目標從 UI 狀態推導**——`waitForURL(/\/trips\/<uuid>/)` 在已經位於
> `/trips/<正式行程>` 的頁面上立即匹配，取到的是正式行程的 id。

**三條規則，任何自動化腳本都必須遵守：**

1. **刪除目標只能是「同一次執行中自己建立、且明列 id」的測試資料。**
   **嚴禁從 UI 狀態（URL、畫面文字、查詢結果）推導刪除目標。**
2. 測試資料名稱一律 **`ZZ` 前綴**。
3. 刪除前**必查正式行程保護清單**（`歷史資料匯入/checkpoint.json` 的 tripUuid），命中即中止。

**實作**：`歷史資料匯入/automation/guard.cjs` 提供登記制刪除器
（`register()` 登記自己建立的 → `deleteTrip()` 只刪登記過的；
未登記、非 ZZ 前綴、命中保護清單三種情況都直接 throw）。

**已套用**：`qa-round5.cjs`／`qa-pkg2.cjs`／`e2e-status-live.cjs`／`delete-integrity-check.cjs`。
`delete-trips.cjs`／`delete-trips-a.cjs` 標註為「一次性授權腳本、已執行完畢」，
不適用 ZZ 規則（那是經 Rozi 明列 id 拍板的正式資料刪除）。

**另一條相關鐵律**（同日、同一類根因第三次發作後訂立）：
**每個 DELETE 都必須斷言實際影響列數，不能只看 HTTP status。**
RLS 會把不符政策的 DELETE 靜默過濾成「影響 0 列」而仍回 200。
回歸測試：`delete-integrity-check.cjs`。

---

## 附錄：內部審查關卡（Rozi 2026-08-30 補述）

角色審查是**內部關卡，Claude 自己跑完**，不需要問 Rozi 要不要跑：

| 角色 | 負責 |
|------|------|
| 凱（Frontend Lead）| UI 實作產出 |
| Aria（UI Lead）| 版位、badge 一致性、小螢幕版面（**自行截圖自查**，有問題自己修完再進下一步）|
| QA Lead | 驗收清單、邊界、回歸 |
| Tech Lead | 解析／資料對應 |
| Data Analyst | 帳務邏輯與驗收基準 |
| Max | 流程與文件把關 |

審查結果寫進「審查軌跡」即可，不需逐輪請示。

**順序紀律**：標示為「部署後收尾」的項目**不得在部署前執行**。
（2026-08-30 自訂 emoji 任務曾把收尾 b 提前在部署前做掉，Rozi 該次接受但要求之後守序。）

---

## 附：與舊版的差異對照

| 項目 | 舊版 | 精簡版 v2 |
|------|------|-----------|
| 對話結構 | 8 個獨立對話 | 1 個團隊對話（Claude 內部切換角色） |
| 跨角色協調 | Rozi 手動帶結論往返 | Claude 內部完成 |
| 審查流程 | 分散在各對話 | 同一則回覆內跑完，附審查軌跡 |
| Rozi 的角色 | 決策者 + 訊息匯流排 | 只做決策 |
| 測試/修 bug | 每步回報 | 迴圈跑到全綠才回報 |
| 浮出找 Rozi 的時機 | 幾乎每一步 | 只在 4 類決策點 |
