# 可以推了　2026-09-10　實作-掛／文／協／私／補／規／讀／新／部

Cowork 複驗通過。**這一輪你唯一要做的三件事**（不要讀 `_指令佇列.md`，
那裡最新的一節是 9/8 的舊節，已經處理過了）：

1. `git push origin main`（要推的是 `8734aac`）
2. 把這個檔改名到 `Claude outputs/_可以推了_已推.txt`
3. 講一句「推好了」就結束這一輪，**不要再開等待迴圈**

⚠️ **協-1 的等待迴圈作廢，不要再用**：它是這個 Claude Code session 的背景輪詢，
已經被中止三次（不是你停的）。推完之後由 Cowork 這邊的巡檢接手量
`/tripay/privacy` 與 `/tripay/terms` 的狀態碼，不需要你等。

## 安全檢查（私-1）全過

| 停止條件 | 結果 |
|---|---|
| 私-1-1 工作區沒有那個 email | ✅ 0 筆 |
| 私-1-2 HEAD 不含 jpg／png | ✅ 0 個 |
| 私-1-3 `.gitignore` 有 `Claude outputs/screenshot-*` | ✅ 第 58 行 |
| 私-1-4 整個 git 歷史查不到那兩張截圖 | ✅ `git log --all` 0 筆 |

**你自己抓到的那件事很好**：第一次清完 `git grep` 還有 2 筆，
在**歸檔的退回單**與**你自己的報告**裡——「把 email 寫進說明它為什麼不該寫的文件」
也是洩漏。這一點 Cowork 沒想到，是你補上的。

已推上去的那三處照 Rozi 的裁示不重寫歷史，這一推之後就看不到了。

## 判定結果

**`verify_C12` 11／11 綠——C12 的四條路徑全部有人守了。**

- C12-3（離線）**不再 skip**：掛-4 的 `window.__FAIL__` 做好之後，
  Cowork 把這條改成照**真實情境**測——頁面正常載入 → 網路才斷 → 按存檔。
  （`?fail=offline` 從頭斷是連 App 都打不開，走不到存檔那一步，那不是使用者會遇到的樣子。）
- C12-6／6b（讀不到不准畫成「還沒有資料」）**轉綠**。

**回歸 26 支全跑、全綠**：AA 23／AB 8／AC 19／AD 13／AE 9／C12 11／LOGIN 17／O 14／
O_extra 3／P 9／Q 14／R 28／S 62／T 42／U 29／U7 28／V 37／W 25／X 21／Y 12／Z 13／
case_A9 15／case_A11_A12 5／hub 10／standing_C6／standing_C9 36。

`verify_Y` 三組破壞測試都擋得住（hero→7,9／topbar→10,11／split2→5,6）。
白名單：`src/lib/summary.ts` **這一輪沒有被動**。

## 新-1（拿掉 G-01）

- `AuthLayout` 不再引用 `usePostLoginRedirect`，hook 檔已刪除
- `src/` 裡剩下的 `/trips/new` 全部是「使用者自己按進來」的路徑
- `專案狀態.md` 第 1182 行已標記 **已推翻（Rozi 2026-09-10）**

⚠️ 這一項的判定是**原始碼層的反向斷言**，因為「自動導向」沒有畫面可以量
（量測靶是 MemoryRouter 單畫面掛載，沒有 AuthLayout）。
**真正的驗證是新帳號實際登入一次**——已列進「機器驗不到」。

## 補-1（migration）

- 覆核意見四點 Cowork 讀過，**同意**：尤其「search_path 是退路、寫死 schema 前綴才是
  不依賴環境」這句話講得比 Cowork 原本的說明準確。
- `confirm_settlement` 的主體 Cowork 拿 production 的 `pg_get_functiondef()`
  逐字比對過，**與 migration 檔一模一樣，只多 `SET search_path` 那一行**。
- **Cowork 已套用**（`apply_migration`，名稱 `confirm_settlement_search_path`）。

### 套用後的寫入路徑實跑（照規-1 的新規則，三件事都對齊）

`role=authenticated` ＋ `search_path=public, extensions` ＋ 該使用者的 JWT claims，
建行程 → 加成員 → 記一筆 → 分帳 → 建結算 → 結算明細 → **呼叫 `confirm_settlement`**：

| 量到什麼 | 結果 |
|---|---|
| 結算狀態 | `confirmed` |
| 有結算時間 | true |
| 行程狀態 | `settled` |
| 函式的 search_path | `public, pg_temp` |

跑完 `rollback`，沒有留下任何測試資料。

## 部-1（deploy 的 404）

`deploy.yml` 已改。**狀態碼要推上線之後 Cowork 才驗得到**——
推完 Cowork 會用真實瀏覽器量 `/tripay/privacy` 與 `/terms` 的 HTTP 狀態碼，
沒變成 200 會再退回來。

## 機器驗不到、只有 Rozi 在手機上才看得到

1. **新帳號登入後是不是停在空行程頁**（G-01 拿掉之後的實際樣子）
2. 「現在讀不到你的行程／資料還在，只是連不上」那兩段字在真機上讀不讀得懂、
   「重新整理」按下去有沒有反應
3. 「連不上網路，這筆還沒存起來。你打的還在」在收訊差的地方是不是真的會出現
4. 「登不進去」那段字、隱私權政策／服務條款兩頁的行距與段落
