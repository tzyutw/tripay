# 要修的（Cowork 寫，2026-09-08 傍晚）　實作-AE：延後清單的甲區三項 ＋ 丁區一項

> **先把實作-AD 做完，再做這一份。**
> Rozi 2026-09-08 裁示：這幾項一次做完，**中途不要停、也不要等她按「收」**。
>
> ⚠️ **做完 commit 之後不要自己 push。** 照 `CLAUDE.md` 的等待協定：
> 每 60 秒看一次 repo 根目錄有沒有 `_可以推了.txt`，最多等 2 小時，
> **出現才 `git push`**，推完把它改名到 `Claude outputs/_可以推了_已推.txt`。
> Cowork 複驗過關就會放那個檔，Rozi 不會再打「收」。

---

## 1　封存的行程，消費列不可以點進編輯

**出處（規則 6：「既有 bug」要引得出一行）**：`畫面地圖.md` 第 119 行——

> | **封存** | 顯示封存日期、總花費 | **列表唯讀** | 無底部操作按鈕；右上角工具列常駐「分享」＋「⚙ 設定」 |

⚠️ **對照第 118 行的「已結算」**：那一列寫的是「列表唯讀（**點擊仍可編輯，重新計算**）」。
**已結算可以點、封存不可以點**，兩者不同，不要順手把已結算也鎖掉。

- 封存狀態：消費列不可點（不是 `<button>`，或 `pointer-events:none` ＋ 沒有游標變化）
- 已結算狀態：維持可點（反向斷言）
- 「重新開啟行程」之後要恢復可點

## 2　點到已刪除／不存在的行程，畫面永遠轉圈圈

`/trips/:id` 用失效的 id 進入時，`.single()` 回 0 筆 → HTTP 406 → 沒有錯誤處理，spinner 不會停。
（Cowork 2026-09-06 複驗發現，Rozi 當時已裁示要做，一直沒排。）

- 查不到行程時停止載入，顯示一個說得清楚的畫面：
  標題逐字 `找不到這趟行程`，一句說明 `它可能已經被刪掉了。`，
  一顆「回首頁」按鈕（可點區符合 C6）。
- **不要**顯示技術錯誤碼。
- 載入中與「找不到」是兩個狀態，不得同時出現。

## 3　成員識別圓圈在編輯前後尺寸不一致

`<Avatar>` 在成員列未編輯時 28px、進入就地編輯時 32px，切換時那一列會微微跳動。

- 統一成 **28px**（類別 emoji 已經統一成 28px，照抄即可）。
- 沿用 `src/components/shared/Avatar.tsx`，**不准另寫第二套**（常設 C1）。
- 改完那一列的高度在編輯前後必須相同（±0.5px）。

## 4　成員移除鈕的「✕」換成圖示

`TripFormSheet.tsx:752` 用文字字元 `✕` 當 icon，違反 `Icon.tsx` 開頭那條
「不要用文字字元充當 icon（樣子由系統字體決定）」，
而且 `TripFormSheet.test.tsx:91` 有一條測試在禁這件事（目前沒抓到，因為它測的是另一個渲染狀態）。

- 換成 `<Icon name="close" size={16} />`。
- 換完要確認 44×44 的 `.tap44` 擴張還在（C6）。
- 順手把 `TripFormSheet.test.tsx:91` 那條測試補強到**這個渲染狀態也會抓到**。

---

## 停止條件

1. 封存狀態（`?screen=s03&state=archived`）：消費列 **0 個是 `<button>`**，
   且點下去不會出現編輯 sheet。
2. **反向**：已結算狀態（`?screen=s03&state=settled`）消費列**仍然可點**、點下去出現編輯 sheet。
3. 失效行程（`?trip=missing`）：**不得**出現載入中的動畫；
   畫面出現逐字 `找不到這趟行程` 與 `它可能已經被刪掉了。`，以及一顆可點的「回首頁」。
4. 反向：正常行程不得出現「找不到這趟行程」。
5. `?screen=s02b`（編輯行程・成員列）：`.avatar` 在未編輯與就地編輯兩種狀態下
   **寬高皆為 28px**，且該列高度兩種狀態相差 ≤ 0.5px。
6. 全站 `.avatar` 尺寸集合仍為 `{28, 20}`（獨立 28、chip 內 20），不得出現 32。
7. 成員移除鈕：`✕` 這個字元在 `TripFormSheet.tsx` 出現 **0 次**；
   該鈕是 `<Icon>`，有效可點區 ≥ 44×44。
8. 常設全綠（C1／C4／C6／C9），`pnpm build` 綠、`pnpm test` 全綠。
9. 白名單：只允許 `src/pages/TripPage.tsx`（或實際處理 `/trips/:id` 的那一個檔）、
   `src/components/TripFormSheet.tsx`、`src/components/ExpenseList*.tsx`（實際渲染消費列的檔）、
   `src/components/shared/Avatar.tsx`、`src/index.css`、`src/test/harness/fixtures.ts`
   ＋對應測試檔。⚠️ `src/lib/summary.ts` 不准出現。
   **實際動到哪些檔案寫進 `_停點.md`**，Cowork 會照這份比對。

---

## 這一輪**不做**（Cowork 的判斷，Rozi 可推翻）

Rozi 說「修甲跟丁」，但丁區還有兩項我建議**留著不動**，理由寫在這裡：

- **輸入框高度沒有統一階梯（41／25／32px 三種）**：要改就是全站輸入框一起動，
  牽動每一個表單畫面。現在的目標是**收斂 Phase 1**，動全站版面跟收斂相反。
- **矮但寬的按鈕（`.chip` 35px、分頁鈕 39px、`.rowb` 41px、`.btn` 40px、s01 新增行程 37px）**：
  低於 Apple 建議的 44pt，但寬度 52～322px，**實際點得到**，Rozi 從來沒回報過點不到。
  改高度會改整個版面比例。

兩項都留在 `_延後與不做清單.md`。**如果 Rozi 要做，會另外開一節。**
