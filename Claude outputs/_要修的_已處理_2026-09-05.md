# 要修的（Cowork 複驗　2026-09-05 22:35）

> **先講結論：不要 push。** 下面三項修完、測試綠，再跑一次 `_停點.md` 的自我檢查，
> 我會再驗一次，過了才會寫 `_可以推了.txt`。
>
> **界線（重要）**：這三項是**逐條指名**的。
> 一個症狀只改造成它的那一處，**不要順便整理別的東西、不要動沒點名的檔案**。
> 你自己列在「順手看到但沒動」的其它項目（`.ic2::after` 兩份定義、`expense_type:'personal'`、
> 寫死色碼）**這一輪一律不要碰**。

---

## ① `ExpenseListPage.tsx` 有**兩個刪除對話框**同時渲染（真的壞掉）

`grep -n "deleteOpen" src/pages/ExpenseListPage.tsx` →
`122`（state）、**`422`**、**`463`** ——兩個 `{deleteOpen && (` 區塊。

- **422–452**：新的、對的。打「刪除」兩個字才 enable，`.btn.dg`，`.dlg`／`.dlgs`／`.dlginput` 用原型 class。
- **463–501**：**舊的，忘了刪**。要打「行程名稱」才 enable，Tailwind 寫死樣式
  （`bg-[#F5F4F2]`、`border-[#E4DFD9]`、`bg-out`），上面那行註解還是
  「複製行程：以這趟為範本開新行程」——註解跟下面的 `{copyOpen && …}` 走散了。

**使用者實際會看到**：按「刪除行程」時**兩個對話框疊在一起**。
兩個共用同一個 `deleteConfirm` state，所以打「刪除」只會 enable 上面那顆、
打行程名只會 enable 下面那顆，怎麼打都有一顆是灰的。

**要做的**：**只刪掉 463–501 那一整個 `{deleteOpen && (…)}` 區塊**，
並把 462 行那句走散的註解移回去 `{copyOpen && …}` 上面（或直接刪掉）。
**422–452 不要動。**

**驗**：`grep -c "{deleteOpen && (" src/pages/ExpenseListPage.tsx` 必須是 **1**。
加一條測試：打開刪除對話框後，畫面上 `dlgt`（或「刪除「…」？」這句）**只能出現一次**，
而且「請輸入行程名稱」這句**不存在**。反向驗證：把舊區塊貼回去 → 這條要紅。

---

## ② S-03b 分享 sheet 三項沒對到原型（212 裡的「要改」，不是「順手看到」）

`src/pages/ExpenseListPage.tsx:80–84` 的 `opts` 陣列，對照
`Tripay_原型.html:2866–2868`（位階 1）：

| 編號 | 原型 | 現在的 App | 要改成 |
|---|---|---|---|
| S-03b-2 | `複製文字摘要`（**沒有灰字**） | `複製結算摘要` ＋灰字「貼到 LINE 群組，讓大家知道誰付誰」 | 標題改 `複製文字摘要`，**灰字整段拿掉** |
| S-03b-3 | `複製分享連結` ＋灰字 `不用登入就看得到消費明細` | 灰字是「任何人打開都能看消費明細，不用登入」 | 灰字改成原型那一句（逐字） |
| S-03b-4 | `預覽分享頁面`（**沒有灰字**） | ＋灰字「看看對方收到連結會看到什麼」 | **灰字整段拿掉** |

原型第 2989／2991 行的稿註也寫了理由：「#25-5 灰字砍掉（在講好處）」。

**只改這三筆文字與灰字的有無。ShareSheet 的版面、portal、動線都不要動**
（版面本來就列在延後清單）。

**驗**：把分享 sheet 的三個 `title`／`sub` 納入 B-3 的文字比對；
反向驗證：改回舊字串 → 要紅。

---

## ③ 停止條件 4 沒過：**212 項對帳表不存在**

佇列 `_指令佇列.md:5224` 與 `:5246` 要求 `_停點.md` 附
「212 項逐項標『已完成／不適用（＋理由）／未做』，**未做必須是 0**」。
現在 `_停點.md` 裡沒有這張表，`Claude outputs/` 也沒有。

**這不是形式問題**——上面第 ② 項就是因為沒有這張表才漏掉的：
B 的七個子節加起來只點名了 S-00／S-01／S-02／S-02b／S-03／S-03d／S-04／S-05／S-06／S-07，
而 `_盤點_實作缺口.md` 的 212 列還包含 **S-02c（13 項）／S-03b（6 項）／S-03c（8 項）**。
（S-02c 全部是「要移除」，刪掉 `EmojiPicker.tsx` 就等於做完，那 13 項標「已完成」即可；
S-03b／S-03c 是 `ExpenseListPage` 裡的兩個 sheet，就是 ①② 這兩個問題的所在。）

**要做的**：
1. 以 `_盤點_實作缺口.md`「第一段：逐項對照（212 列）」為母體，
   逐項標 `已完成` ／ `不適用（＋理由）` ／ `未做`，寫成
   `Claude outputs/_212對帳表.md`，並在 `_停點.md` 附統計（三個數字相加＝212）。
2. **表要用程式產生，不要手抄**：從盤點檔解析編號，再逐項比對，
   輸出「掃了幾列／已完成幾列／不適用幾列／未做幾列」的**數字**，
   不要只回布林值（會「查不到＝通過」）。
3. **「未做」必須是 0**。若對表過程再發現漏掉的項目，**先寫進表裡並回報**，
   修不修等這份表出來再說——**不要一邊對表一邊擴大改動範圍**。

---

## 已經由 Cowork 獨立驗過、**不用你再跑**的（供參考，不要重做）

| 項目 | 我實際跑的結果 |
|---|---|
| `tsc --noEmit` | exit 0 |
| `vitest run` | **196 通過 / 3 skip**，與你回報一致 |
| 型別對帳 | 29 通過 0 失敗，差異列數 0 |
| token 掃描 | 52 通過 0 失敗 |
| 文案唯一性 | 13 通過 0 失敗 |
| 版面回歸（真實 Chromium，10 畫面 × 320/375/414） | **172 通過 0 失敗**；金絲雀有效（塞 900px → 溢出 580px、超出右緣 1 個） |
| 分享頁端到端（對 production，anon 角色） | 正確 token → 「2025 北海道四寶團」74 筆；末字元改一碼 → null；前 8 碼 → null；空字串 → null |
| anon 直接讀六張表 | trips／expenses／expense_splits／trip_members／settlements／settlement_items **全部 0 列** |
| commit 衛生 | `.bak`／暫存檔**沒有**進 commit，`git ls-files` 乾淨 |
| `EmojiPicker` | 檔案已刪；`src/` 只剩 4 處提及，全在 `SettingsPage.test.tsx` 裡「斷言它已被刪」的那段 |

`vite build` 我這邊跑不起來，原因是我的執行環境不能刪 `dist/`（權限），不是程式問題；
`tsc` 與 vitest 都綠，你那邊 build 也通過，這條算過。

---

## 修完之後

1. 三項都修好、`vitest run` 全綠、新斷言各有反向驗證紀錄。
2. 更新 `_停點.md`（含 212 對帳表統計）與 `Claude outputs/_實作B進度.md`。
3. **commit，不要 push。** 把這個檔案改名或標記為已處理，我會再驗一次。
