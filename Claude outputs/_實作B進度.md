# 實作-B 進度（子節之間用這個檔，不要動 `_停點.md`）

> 中途被打斷就讀這裡接回去。**已標「完成全綠」的子節不要重跑。**

| 子節 | 項數 | 狀態 |
|---|---|---|
| B-1 建立／編輯行程表單 | 24 | ✅ **完成全綠 2026-09-05 12:54**（`TripFormSheet.test.tsx` 15 項綠） |
| B-2 首頁＋登入 | 12 | ✅ **完成全綠 2026-09-05 21:18**（`LoginPage.test.tsx` 6 項＋`TripListPage.test.tsx` 10 項） |
| B-3 行程頁＋未定案清單 | 37 | ✅ **完成全綠 2026-09-05 21:38**（`ExpenseListPage.test.tsx` 18 項） |
| B-4 記一筆 | 39 | ✅ **完成全綠 2026-09-05 21:52**（`ExpenseFormSheet.test.tsx` 18 項；S-04-8＋29 同一個 commit） |
| B-5 結算 | 30 | ✅ **完成全綠 2026-09-05 22:05**（`SettlementPage.test.tsx` 15 項） |
| B-6 分享頁 | 15 | ✅ **完成全綠 2026-09-05 22:14**（`SharePage.test.tsx` 8 項） |
| B-7 設定＋刪 EmojiPicker | 8 | ✅ **完成全綠 2026-09-05 22:16**（`SettingsPage.test.tsx` 8 項） |
| **實作-C** | — | ✅ **完成全綠 2026-09-05 22:25**（C-1 查證／C-2 hub／C-3 版面回歸／C-5 把關） |
| **實作-D** | 3 | ✅ **完成全綠 2026-09-05 22:50**（Cowork 複驗退回三項） |
| **實作-E** | 4 列 | ✅ **完成全綠 2026-09-05 23:05**（對帳表的 4 列「未做」收掉，未做＝0） |

## Cowork 已裁示的技術問題（不要再問，也不要拿去問 Rozi）

1. **寫死色碼（`#E4DFD9` 等）**：**這一輪不處理。** 已列在 `_延後與不做清單.md`／
   `Tripay_問題優先序清單.md` P4，不是 B 的停止條件。**不要順手改。**
2. **建立表單成員區仍是舊版面（白卡片＋虛線按鈕，非 `.rowb`）**：
   若 B 的文字比對已全綠，**這一輪不動**；等 Rozi 上線後看實機再決定。列進「順手看到但沒動」。
3. **B-1「刪除行程移進本頁最下方」與原型不符**：你照原型做（S-02b 沒有刪除行程，
   它在 S-03 的 ⋯ 選單）是**正確的**。原型是唯一真相，佇列文字有誤。**維持現狀，不要補。**

## B-1 完成內容摘要（供後續子節參考，不要重做）

- 成員 emoji 改就地編輯，`EmojiPicker` 引用已斷開（檔案等 B-7 刪）
- 成員刪除守衛補上第三種情形（`individual_member_id` 指向他）
- `tone_seq` 建立當下就算好存起來
- 全專案當時：vitest 92、型別對帳 29、token 掃描 52、文案唯一性 13、原型既有 871，全綠；
  `tsc` 零錯誤、`vite build` 通過
- `git diff` 只動 `src/`、`package.json`＋lock、`vitest.config.ts`，沒有碰原型

## 之後每做完一個子節，就在這裡更新一列（不要寫 `_停點.md`）

## B-2 完成內容摘要（供後續子節參考，不要重做）

**S-00 登入**（`LoginPage.tsx` 全檔重寫）
- App icon（S-00-1）與幽靈卡（S-00-6）移除；整頁只剩一顆按鈕
- 底色改工作色深夜版 `linear-gradient(158deg,#0B2233,#0F5E9E 48%,#1276C4)`
- 高度走 `100dvh`（`@supports` 退 `100vh`），**沒有任何寫死的 min-height**
- 標語與登入鈕共用同一個 `.s00col`（240px），標語 `white-space:nowrap`
- 文案「用 Google 繼續」；官方四色 G 內嵌，四個 `fill` 不套 currentColor
- 版面 class（`.s00wrap/.s00pad/.s00brand/.s00col/.s00act/.s00logo/.s00slogan/.s00tag/.gbtn`）
  整段搬進 `src/index.css`，數值逐字對原型

**S-01 首頁**（`TripListPage.tsx`）
- S-01-15 設定入口：`.ic2` ＋ `settings` 20px，排在「新增行程」**之後**（頻率較低）→ `/settings`
- S-01-2 全形「＋」→ `<Icon name="add" size={16} />`
- S-01-4 空狀態補主要按鈕「建立第一趟」（帶 add icon）
- S-01-13/14 卡片下緣移除成員 emoji（**只有首頁**，其他頁照常）
- S-01-9/10 G-06 分享橫幅與「複製連結」鈕整塊移除

**兩處為了通過原型文字比對而必須改的（不是順手改）**
1. `deriveStatus.ts` 的 `STATUS_LABEL.settled`：`'✅ 已結算'` → `'已結算'`。
   原型是「已結算」，且 ✅ 是「表示狀態的圖形」不是內容，走不了 emoji 的豁免。
2. `format.ts` 的 `md()` 改純字串切片（原本 `new Date()` 在 UTC 以西時區會少一天），
   並新增 `dateRange()`；`TripListPage` 內的 `formatDateRange` 刪除改用它。
   原型的 `md()`／`dateRange()` 也是字串切片，兩邊必須同一種算法。

**反向驗證（12 條，拿掉什麼 → 哪一條紅了）**
| 拿掉 | 結果 |
|---|---|
| S-01-15 設定入口 | 1 紅 |
| ＋ 改回全形字元 | 1 紅 |
| 空狀態 CTA 整顆刪掉 | 1 紅 |
| 卡片把成員 emoji 加回來 | 1 紅 |
| 已結算補回 ✅ | 1 紅 |
| 日期改回 `new Date()` | 2 紅 |
| S-00 App icon 加回來 | 2 紅 |
| S-00 文案改回長版 | 2 紅 |
| S-00 標語不共用 240px 欄 | 1 紅 |
| S-00 G 改吃 currentColor | 1 紅 |
| S-00 寫死 min-height | 1 紅 |

**全專案當時**：vitest 105 綠（B-2 前 92）、型別對帳 29、token 掃描 52、文案唯一性 13，
`tsc` 零錯誤、`vite build` 通過。`git diff` 只動 `src/`，沒有碰原型。

**順手看到但沒動**（等 Rozi 決定要不要進下一節）
- S-01 行程卡的**外框**與原型不同：原型是白卡片（`.tcard`，padding 14、border、內圖 150px），
  App 是整張圖直接鋪（184px、無白框）。B-2 只點名「移除成員 emoji」，卡片外觀沒點名，維持現狀。
- S-01-6 狀態徽章用 `STATUS_BADGE_CLASS` 四套樣式，原型只有一套 `.stamp`（白描邊白字）。沒點名。
- 停止條件第 9 條（320／375／414 橫向不捲動）需要真實瀏覽器，
  照節次分工屬於 **C-3「版面回歸測試移植進 src/」**，B-2 沒有自行搬。

## B-3 完成內容摘要（供後續子節參考，不要重做）

**新檔 `src/lib/summary.ts`**——`calc()`／`tripSummary()`／`tripRate()`
逐條移植自原型第 1767–1900 行。這是**呈現層**彙總，不是結算引擎（引擎是 C-1 的查證對象）。
共用元件的 `SharedSummary` 邊界就是為它留的。
移植正確性由測試釘住：同一組示範資料算出 $34,375／$32,620／$10,515／$20,620，
與原型 fixture 完全相同。

**新檔 `實作_原型CSS移植.cjs`**——把原型 `.ui` 的版面規則**程式抽出**進 `src/index.css`
（150 條，只拿掉 `.ui ` 前綴，內容一字未改）。**不手抄**：#31-5／#32-1 那輪 `.ic2`
的底色就是手抄抄反的。要改數值一律改原型再重跑這支。
> 抽取器踩過兩個坑，都已修並寫進註解：
> ① 註解與 `@media`／`@keyframes` 的巢狀大括號會把規則切歪，**不報錯**、只是少搬幾條
>   （少的正好是 `.btn`／`.seg`／`.hero`／`.exprow` 這種最基本的）。
> ② 只掃 `className="..."` 會漏掉把 class 放進變數的寫法
>   （`const cls = \`exprow${pend ? ' pend' : ''}\``）——`.exprow` 就是這樣漏的。
>   改成掃所有字串與樣板字面量。

**`ExpenseListPage.tsx`（S-03／S-03d 全檔重寫渲染段）**
- hero：`destinationOf()` 色調；**副標只有日期區間，沒有成員 emoji**
- 動作重組：hero 右上只剩「返回」「⋯」兩顆 `.ic2`；編輯／分享／複製／封存／刪除全進 ⋯ 選單
- S-03-33 消費／結算分段控制；「結算」分頁就是 S-05 整頁，
  它自己的導覽列用 CSS（`.settlepane > .bar`）收起來，**不動 S-05 的輸出**
- 統計卡改用共用元件（總花費可展開 → 每人分擔 → 註腳），預設收合
- 日期分組由新到舊、同日依記錄先後由新到舊（共用元件已含）
- G-05 分享橫幅五處全清，含 `sessionStorage` 的 `g05-dismissed`（測試會掃原始碼擋復發）
- S-03-29 未定案入口「有 N 筆還沒算清楚 ›」，N＝0 整條不顯示
- 底部主鈕依狀態三種（旅途中「記一筆」／已封存「重新開啟行程」／已結算沒有）
- S-03-25 刪除確認改成打「刪除」二字才 enable，只有這裡用 `--dg` 實心
- **既有 bug 已修**：`openEdit` 補狀態守衛，封存／已結算不得點進編輯
- ⚠️ `:553` 那個「複製行程」註解接刪除對話框的陷阱——整段已重寫，註解與內容一致
- S-03d：兩種進入方式（統計卡的人／列表上方入口）走同一畫面，只有標題與範圍不同

**測試環境補強（`src/test/utils.tsx`）**
- `Providers` 補上 `ToastProvider`——少了它 `useToast()` 直接 throw，整頁炸掉
- `render()` 新增 `path` 參數。**沒有 `<Route path>` 比對時 `useParams()` 一律回空物件，
  元件會停在載入中而不報錯**，測試只說「找不到那段文字」，看不出根因。

**反向驗證（16 條）**
| 拿掉 | 結果 |
|---|---|
| hero 加回成員 emoji | 1 紅 |
| hero 右上多一顆設定 | 1 紅 |
| ⋯ 選單的刪除不著色 | 1 紅 |
| 未定案入口拿掉 | 4 紅 |
| 分段控制的結算分頁拿掉 | 1 紅 |
| 日期分組改回由舊到新 | 1 紅 |
| 沒匯率時外幣可以切 | 1 紅 |
| S-03d 的列改成可點 | 1 紅 |
| 引擎：贊助改計入總額 | 2 紅 |
| 引擎：各自金額改讀錯欄位（P1-0） | 1 紅 |
| 引擎：未定案門檻改成 1 人 | 1 紅 |
| ＋ B-2 的 11 條照舊全紅 | — |

> **反向驗證抓到兩個真的漏洞**（第一輪這兩條改壞了卻全綠）：
> 引擎的「填寫幣別是外幣」分支與「1 人沒填不標記」分支，
> S-03 的示範資料剛好都沒走到。已補兩項專門測這兩條分支的斷言。
> 這正是 CLAUDE.md 那句「查了但沒查到與沒有問題長得一模一樣」。

**一條反向驗證維持綠，是預期的**：拿掉 `openEdit` 的狀態守衛不會變紅，
因為封存／已結算時 `S.readonly` 已經讓列渲染成 `<div>`，點不到。
守衛是第二層防線，留著；使用者看得到的契約（列不可點、底部主鈕）兩種狀態都有測試釘住。

**全專案當時**：vitest 123 綠（B-3 前 105）、型別對帳 29、token 掃描 52、文案唯一性 13，
`tsc` 零錯誤、`vite build` 通過。

**順手看到但沒動**
- `ShareSheet` 仍是舊版面（自寫的 sheet，不是共用元件）。B-3 沒點名它的版面，
  只點名「分享」要進 ⋯ 選單——已做。列進來等 Rozi 決定。
- `SettlementPage` 未改，B-5 才動；目前用 CSS 收掉它的導覽列。

## B-4 完成內容摘要（供後續子節參考，不要重做）

`ExpenseFormSheet.tsx` 全檔重寫（638 → 約 520 行）。金額與幣別行為一律照
`規格_金額未定案與幣別.md`，版面與文案逐字對齊原型的 `renderS04()`／`paintS04()`／`cmpRow()`。

**🔴 硬相依已在同一次改動內完成**
- S-04-8：拿掉「之後再填」toggle，空欄一律放行存檔（存檔鍵不 disable、不跳確認）
- S-04-29：空欄時自動寫 `foreign_pending`／`twd_pending`
- 測試直接釘住這一組：完全沒填金額存檔 → `twd_amount` 為 null **且** `twd_pending` 為 true。
  反向驗證把 pending 改成永遠 false → 該條立刻紅。

**其餘**
- 日期列（S-04-10）改成 `.fieldrow` 形狀、標籤與值同一列、**位置在標題正下方**；
  預設今天，今天超過回程日才夾到回程日，**早於出發日不夾**（行前支出要落在「出發前」）；
  編輯既有消費沿用該筆原本的日期
- 金額區：外幣／台幣兩欄；外幣總額自動補入標「自動」、可覆寫；台幣由匯率換算時同樣標「自動」
- 各自金額：填寫幣別切換（S-04-31）存 `split_fill_currency`；
  **填外幣時存進 `split_amount_foreign`，不塞 `split_amount`**（P1-0 的根因）；
  自動均分值走 placeholder（空白 ≠ 0）；逐人列 `<label for>` 配 `.amtrow`
- 差額三級（剛好／≤1% 淡色／>1% 橘色）**三種都可存檔**，移除所有加總阻擋
- 支付方式（S-04-9）讀 `trip.payment_methods`，順序跟著清單、預設第一項；
  **程式碼中已無任何支付方式常數**（舊的 `PAYMENT_OPTIONS` 整組刪除，測試會掃原始碼）
- 「只算一個人」可選人，存 `individual_member_id`
- 類別 emoji 就地編輯（共用 `useInlineEdit`），手動改過寫 `category_emoji_manual=true`，
  之後改標題不再覆蓋；**全檔無 `autoFocus`**
- 「當場就清了」與「贊助／回饋」用全站統一的 `.chip`
- 存檔四種 toast 抽成純函式 `saveToastFor()`，文案照規格 §4
- 刪除文案改誠實說法（是軟刪，不寫「無法復原」）
- 編輯時刪 splits 補上 `.select()` 並斷言影響列數（帳務鐵律，RLS 會靜默刪 0 列）

**測試環境**：`src/index.css` 的原型 CSS 重抽一次，150 → **189 條**（新增 S-04 用到的類別）。

**反向驗證（14 條）**：13 條當場變紅。
「只算一個人不存 `individual_member_id`」在批次腳本裡看似全綠，
原因是**我的替換字串在檔案裡出現兩次**（`calc()` 的參數也有一份），
腳本只換了第一處。單獨重跑該處確認**會紅**（`expected null to be 'm1'`）。
> 這正是 CLAUDE.md 那條「回報『沒做到』之前，一定要換第二種方法再量一次」。

**全專案當時**：vitest 141 綠（B-4 前 123）、型別對帳 29、token 掃描 52、文案唯一性 13，
`tsc` 零錯誤、`vite build` 通過。

**順手看到但沒動**
- `➕` 當「還沒選 emoji」的預設值仍保留。CLAUDE.md 的「佔位不是內容」那條說它該走 Feather，
  但**原型（位階 1）目前仍輸出 `➕`**，fixture 也是這樣抓的，B-4 沒點名要改。
  依文件效力順序以原型為準，維持現狀，列在這裡等 Rozi 裁示。
- `personal` 這個 `expense_type` 在原型的三種分帳方式裡沒有對應（原型是
  一起分／各付各的／只算一個人）。目前寫入時不會產生 `personal`，
  但既有資料若有 `personal` 的列，`calc()` 會當作零參與者。沒點名，未動。

## B-5 完成內容摘要

**`src/lib/summary.ts` 補上**：`settleTrip()`（淨額 ＋ 轉帳路徑）、`prepaidShare()`、
`ExpenseCalc.debts`。三個都逐條移植自原型。
> **hub 分支一併搬了**，雖然 B-5 寫「只做 direct」。理由：`settleTrip()` 與
> `TransferView` 兩邊本來就都含 hub，硬拆一半等於製造第二份路徑，
> 反而違反「同一件事不得兩處各寫一份」。C-2 只剩「驗 net 相同 ＋ 接 S-05-31 引導」。
> 測試已釘住 #22-6c：**hub 與 direct 的 net 完全相同，只有 tx 不同**。

**`SettlementPage.tsx`**
- 未結算態＝完整轉帳預覽（S-05-28），標題「現在的狀況」／「還會變 —— 之後記帳會影響這裡」，
  與 S-05-7 共用同一個 `TransferView`
- 結算前檢查層（S-05-4／30）：標題「有 N 筆還沒算清楚」＋逐筆**可點**清單；
  「就這樣結算」不 disable，**真的會結算**（提醒不是禁止）
- 人話淨額（S-05-11）補上對象，由多筆轉帳組成時全部列出
- 摺紙 signature（S-05-16）取代 popIn：`@media(prefers-reduced-motion:no-preference)`
  才套動畫，reduce 直接停在最終畫面；殘影 `.trail` 放在 `.plane` 之外
- G-08 回顧卡（S-05-17）數字帶符號、標籤純文字
- 分享 CTA（S-05-29）升為主要動作；建立新行程／封存行程降為 `.btn.qt`
- 移除 S-05-2 待填警示、S-05-12 Excel 正負號提醒
- S-05-31 引導**只給連結不給設定**（設定的唯一入口是 S-02b-13）
- `expenses` 查詢改拿整列（含 splits），預覽才算得出淨額

**抽取器再修一個坑**：原本把 `@media`／`@keyframes`／`@supports` **整段丟掉**，
於是 `.fold` 的三拍動畫與 `.spin` 的轉圈靜靜消失（它們刻意住在
`@media(prefers-reduced-motion:no-preference)` 裡）。改成切出來另外處理。
CSS 規則 189 → **202 條 ＋ 4 個 at-rule 區塊**。

**反向驗證（12 條）**：全部變紅。
其中三條第一輪是綠的，都補強了斷言才變紅：
- 「當場就清了改成進結算」→ 補一條「拿掉旗標後淨額**必須**改變」的對照
- 「hub 改走 minimum tx」→ 中心人改選**非最大債權人**，否則兩種模式算出來剛好一樣
- 「清單改成不可點」→ 第一次的替換讓 JSX 壞掉（vitest 連跑都跑不起來，
  輸出沒有 Tests 那一行）。**「跑不起來」不等於「紅了」**，重做成合法的替換才算數。

**全專案當時**：vitest 156 綠（B-5 前 141）。

## B-6／B-7 完成內容摘要

### B-6　S-06 分享頁
- **資料源改成 `get_shared_trip()` RPC**。指令說「實作-A 已經改成呼叫 RPC」，
  **實際上沒有**——A 只寫了 migration 013，`SharePage.tsx` 仍在直接查表。
  013 已經把 anon 對 trips／expenses 的直接讀取收掉，所以線上的分享頁
  對未登入者是**壞的**（拿到空資料、不報錯、整頁空白）。這次一併修好。
  測試把 `supabase.from` 換成會 throw 的樁，改回查表就直接紅。
- hero 用 `destinationOf()`；統計卡與消費列表改用共用元件
  → 外幣格（S-06-5）與人均（S-06-6）消失、消費明細改日期分組、列上不寫日期
- 總花費排除贊助，與行程頁同一套算法（測試比對兩頁同一個 $34,375）
- 全頁唯讀：消費列 `<div>`、每人分擔列不可點、註腳不寫「點名字看是哪幾筆」
- S-06-12 頁尾改文案；**S-06-13 安裝鈕整顆移除**，連 state 與 listener 一起

**🚩 原型與佇列指令有出入，這一項要 Rozi 裁示：**
> B-6 明寫「hero **拿掉成員 emoji**（與 S-03 一致，同一段 `join('')` 的問題）」，
> 但**原型的 `renderS06()` 仍然把成員 emoji 接在日期後面**，
> fixture 抓到的也是 `3/14 – 3/18 · 2026 🐵🐱🍋🐟`。
> 依文件效力順序原型是位階 1，但這條指令具體點名了它、還說明了理由，
> 而 S-03 已經照同一個理由拿掉了。
> **目前照佇列做（拿掉）**，並把這一項單獨釘在 `SharePage.test.tsx` 的
> `KNOWN_DIVERGENCE`，不藏進「差異為空集合」裡。
> Rozi 若裁示以原型為準，改回來只要動一行 ＋ 拿掉那個常數。

**順手修了一個 A-4 的 bug（B-6 的停止條件過不了）**：
`StatCardTotal`／`StatCardFoot` 的「約」判定原本看 `readonly` **prop**，
應該看 `S.readonly`（行程狀態）。兩者是不同的東西——
prop 管「這一頁能不能點」，`S.readonly` 管「已結算就不再標約」。
判錯的結果是分享頁的「約」與註腳整組消失，與原型對不上。
`shared.test.tsx` 的唯讀案例同步改成餵 `{...S, readonly:true}`——
fixture 本來就是這樣抓的（`實作_元件基準擷取.cjs:70`），是測試的輸入沒有照抄。

### B-7　S-07 設定
- 登出：`.btn.qt` 描邊 ＋ 文字 `var(--md)`（**不是 `--dg`**，登出可復原）；確認框 `.btn.gh`
- 版本 footer 照原型「Tripay · 每一趟，都記得」，**沒有版本號**
- **沒有**補做「我的資料」與「顯示設定」（測試逐字擋掉八個關鍵詞）
- `src/components/EmojiPicker.tsx` **整檔刪除**，全專案零引用
  （掃描會輸出「掃了幾個檔」，避免一個檔都沒掃到卻算通過）

**反向驗證（12 條）**：全部變紅。
兩條第一次因為替換字串不合法而沒跑起來（引號跳脫、JSX 破掉），重做後確認會紅。

**全專案當時**：vitest 172 綠（B-5 後 156）、型別對帳 29、token 掃描 52、文案唯一性 13。

**順手看到但沒動**
- `ExpenseListPage` 的 `ShareSheet` 仍是自寫版面（B-3 已列過）。

## 實作-D　Cowork 複驗退回的三項（2026-09-05 22:50）

### ① 兩個刪除對話框同時渲染 —— 真的壞掉，已修
根因是我自己造成的：B-3 重寫 `ExpenseListPage` 的 render 時，
我用「切掉中段、保留頭尾」的方式改，**尾巴那一段裡還有舊的 `{deleteOpen && (…)}`**。
兩個共用同一個 `deleteConfirm`，所以打「刪除」只 enable 上面那顆、打行程名只 enable 下面那顆，
使用者怎麼打都有一顆是灰的。已刪掉舊的 40 行，走散的註解移回 `{copyOpen && …}` 上面。
> **教訓**：大段重寫時「保留尾巴」不等於「尾巴是對的」。
> 這正是 212 對帳表存在的理由——沒有表就沒人發現 S-03c 有兩份。

### ② S-03b 分享 sheet 三筆文案（S-03b-2／3／4）
逐字對齊原型 2866–2868：標題改「複製文字摘要」，兩句**在講好處**的灰字砍掉
（#25-5；不符合「灰字只留兩種」），留下的那句改成「不用登入就看得到消費明細」。
`sub` 為空時不渲染空段落。

### ③ 212 對帳表
`實作_212對帳表.cjs`（新增）從 `_盤點_實作缺口.md` 第一段**解析** 212 列，不手抄。
判定分三層：證據可量的直接量、例外逐項寫理由、其餘看該畫面有沒有對原型的比對測試。
**宣告與證據衝突就直接紅**——第一次跑就抓到我自己標錯一列（S-03c-3 宣告「未做」，
交叉檢查卻要求證據為真），修正後 10 項交叉檢查全過。

**結果：已完成 207／不適用 1／未做 4（合計 212）**

### 反向驗證
| 拿掉 | 結果 |
|---|---|
| 把舊的刪除對話框貼回去 | 2 紅 |
| 分享 sheet 文案改回舊版 | 2 紅 |
| 212 表：把某列的判定標錯 | 交叉檢查紅（實際發生過一次） |

**全專案當時**：vitest 201 綠、版面回歸 172、token 52、型別對帳 29、文案唯一性 13、
端到端 2、212 交叉檢查 10，`tsc` 零錯誤、`vite build` 通過。

## 實作-E　把對帳表的 4 列「未做」收掉（2026-09-05 23:05）

### ① S-03c 刪除對話框逐字對齊原型 2873–2882
| 編號 | 改成 |
|---|---|
| S-03c-2 | `刪掉就<b>救不回來</b>：` |
| S-03c-3 | 拆回三個 `<li>`（`.dlglist`，數值照原型 inline style 收成 class），數字即時帶入 |
| S-03c-4 | `請輸入「刪除」兩個字` |
| S-03c-7 | 按鈕字「刪除行程」→「刪除」（忙碌態「刪除中…」保留） |

停止條件那條測試做成**雙向綁定**：先斷言這些字面量**還在原型裡**，再比對話框的
`textContent`。原型哪天改了字，這條會紅，而不是兩邊靜靜地各走各的。

### ② S-02c-10 三層 fallback
新增共用元件 `src/components/shared/Avatar.tsx`，逐條移植原型 `avatar()`（993–1002）：
有 emoji → emoji；沒 emoji 但名字非空 → **第一個 grapheme ＋ 填色圓底**
（`LETTER_COLORS[idx % 6]`，逐字搬自原型 947）；兩者皆無 → 🙂。
`TripFormSheet` 的成員列與新增列都改走它，**S-02／S-02b 同一份**。

> **關鍵是把成因也修掉**：原本 `newMemberEmoji` 預設 `'🙂'`，
> 所以第二層永遠走不到。預設改成空字串，「沒填 emoji」才會真的落在第二層。
> 測試直接擋住 `useState('🙂')` 這個寫法復發。

grapheme 用 `firstGrapheme()`，不是 `name[0]`——ZWJ 組合（👩‍👧）、國旗（🇹🇼）都不切斷，
測試逐一驗過。

### ③ 對帳表重跑：**已完成 212／不適用 0／未做 0**
- `S-06-3` 依 Rozi 2026-09-05 裁示改標「已完成」（不帶 emoji，與 S-03 一致）。
  `SharePage.test.tsx` 的 `KNOWN_DIVERGENCE` 常數**保留當守門的**，註解改成「已裁示」。
- `S-03c-7` 理由欄補一句（顏色本來就對，按鈕字這一輪一起改）。
- 交叉檢查從 10 條加到 **13 條**：新增的四項證據（三層 fallback、後果說明、確認提示、
  三個 `<li>`、按鈕字、hero 無 emoji）都由程式量，宣告與證據衝突就紅。

### 反向驗證（7 條，全部變紅）
| 拿掉 | 結果 |
|---|---|
| 後果說明改回舊句 | 2 紅 |
| 影響清單收回一句話 | 2 紅 |
| 確認提示改回舊句 | 2 紅 |
| 按鈕字改回「刪除行程」 | 1 紅 |
| 把第二層（填色圓底）拿掉 | 3 紅 |
| 第二層改用 `name[0]` | 1 紅 |
| 拿掉填色（只留字沒有底色） | 2 紅 |
| 新成員 emoji 預設塞回 🙂 | 1 紅 |

**全專案當時**：vitest **211 綠**、版面回歸 172、token 52、型別對帳 29、文案唯一性 13、
端到端 2、212 交叉檢查 13，`tsc` 零錯誤、`vite build` 通過。
