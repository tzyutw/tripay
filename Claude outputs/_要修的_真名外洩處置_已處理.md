# 要修的（Cowork 緊急處置覆核 · 實作-K 期間）　2026-09-06 06:22

## ⚠️ Cowork 越界了，這段沒有經過第二人檢查，請覆核

**發生什麼**：Cowork 在 `實作-K` 的指令裡寫「`Tripay_原型.html` 沒被 git 追蹤，
所以可以放 Rozi 的真實成員名」。**這句是錯的**——那個檔自 `750d857` 起就是 tracked，
而且已經在公開的 `origin/main` 上。

誤判原因：Cowork 用 `git ls-files | grep 原型` 判斷，但 git 會把中文檔名轉義成
`"Tripay_\345\216\237\345\236\213.html"`，grep 抓不到，於是誤判成「未追蹤」。

**你照那個錯誤指令做了**，工作區的原型第 994／998 行被寫進
`Ziyu`／`Ning`／`Mei`／`Xiu`——那是 Rozi 的真實成員名，repo 是公開的。

**Cowork 已於 06:22 直接改掉工作區那兩行**（→ `Alex`／`Robin`／`Sam`／`Kai`），
沒有經過你檢查。請覆核下面幾點。

## 要你做的

1. **確認 `grep -nw -e Ziyu -e Ning -e Mei -e Xiu Tripay_原型.html` 無輸出**。
   若你之後又寫回真名，改回中性英文短名（維持 4–5 字母的字寬特性即可，
   **要對齊的是狀態，不是名字**）。
2. **確認真名沒有被 commit 出去**：
   `git log -p --all -S Ziyu -- Tripay_原型.html` 應該只有工作區改動、無 commit。
   若已經 commit 但**尚未 push**，把那個 commit 改掉再推。
   若**已經 push 出去了，停下來寫 `_停.txt` 並回報**——
   改寫已推出的歷史是不可逆動作，要 Rozi 本人決定，不要自己 force push。
3. **`.gitignore` 裡新增的 `Tripay_原型.html` 那幾行請移除**。
   Rozi 2026-09-06 明確要原型有版控備份，而它本來就在版控裡；
   加了那行只會製造「它沒被追蹤」的誤解（何況 `.gitignore` 對已追蹤檔案無效）。
4. **只處理 `Tripay_原型.html` 這一個檔，其餘不要動。**
   Cowork 已查過：Rozi 的成員名（Ziyu／Ning／Mei／Xiu）**早就在公開 origin/main 上**，
   散在十幾個歷史文件裡（`Excel匯入對應規則_濟州島案例.md` 9 處、
   `D2_people層變更提案.md` 4 處、`CLAUDE.md`、多份文案稿等），
   最早可追到專案初期，**不是這一輪造成的**。
   要不要清理既有的屬於「改寫已推出的歷史」＝不可逆動作，**已交 Rozi 決定，你不要碰**。
   `_指令佇列.md`（31 處）與 `專案狀態.md`（13 處）同理，這一輪一律不動。

## 停止條件

1. `grep -nw -e Ziyu -e Ning -e Mei -e Xiu Tripay_原型.html` **無輸出**
   （只驗這一個檔，其餘依第 4 點不動）。
2. `git log --all -S Ziyu -- Tripay_原型.html` 無 commit（或已處理並說明）。
3. `.gitignore` 不含 `Tripay_原型.html`。
4. `npx tsc --noEmit` 0 錯；`npx vitest run` 全綠且**不含 `Unhandled Errors`**；
   `pnpm run build` 成功；已 commit 並 push。
5. 做完把本檔改名為 `Claude outputs/_要修的_真名外洩處置_已處理.md`，
   回報寫進 `_停點.md`。

## 五類例外

**第 2 點若命中「已經 push 出去」，屬於安全性／隱私 ＋ 不可逆動作，必須停下來問 Rozi。**
其餘可以一路做完。
