# 驗收案例（總驗收）

> **這份跟「停止條件」是兩件事。**
> 停止條件問的是「這一節指令做到了沒」——那是**變更驗證**。
> 這份問的是「使用者要完成的事，從頭到尾走得通嗎」——那是**驗收**。
> 兩件事都做才叫驗收完成。（2026-09-07 建立：Rozi 問「你有自己做過 test case
> 做總驗收嗎？PO 在哪？這不是 QA 的工作嗎？」——當時的答案是沒有。）
>
> **規則**
> - 每一輪新功能要往這裡加案例；**每一輪都要跑一次「核心」那一區**
> - **實作-S 這一輪之後要跑「全部」，不是只跑核心區**（Rozi 2026-09-07 指定）
> - 案例以**使用者要完成的事**為單位，不是以畫面或元件為單位
> - 狀態只有三種：`✅ 跑過通過`｜`❌ 跑過沒過`｜`⬜ 沒跑過`。
>   **不准寫「應該可以」。** 沒跑過就是 ⬜
> - 每個案例要寫清楚「怎麼算通過」，而且要是**機器或人都能照著走**的敘述

---

## 核心（每一輪都要跑）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 | 最後跑 |
|---|---|---|---|---|
| A1 | 開一趟新行程並出發 | 填「這趟叫什麼／誰一起去／當地幣別／出發」→ 按出發 → 行程建立成功且出現在清單 | ✅ | 2026-09-07 production `begin…rollback`：行程 1、成員 2（含一個沒 emoji） |
| A2 | 必填沒填不能出發 | 清空「這趟叫什麼」→ 按出發 → 焦點跳到該欄、**沒有送出寫入** | ✅ | 2026-09-06 實作-O |
| A3 | 設好匯率，只填外幣也算得出台幣 | 設匯率 → 記一筆只填外幣 → 台幣自動算出且**方向正確** | ✅ | 2026-09-07 production：10,000 JPY ＠0.21 → **2,100** 台幣（與畫面上的換算範例同一個數字） |
| A4 | 記一筆（一起分） | 填金額、選付款人 → 記下來 → 出現在清單、金額與千分位正確 | ✅ | 2026-09-07 production：expenses 1 筆＋expense_splits 2 筆，備註一併存入 |
| A5 | 記一筆（各自付各的，用外幣填） | 各自金額用外幣填 → 存進去的分擔金額是台幣、Σ = 該筆台幣總額 | ✅ | 2026-09-06 實作-O |
| A6 | 結算一趟 | 按結算行程 → 產生轉帳清單 → 金額加總等於各人差額 | ✅ | 2026-09-07 production：settlements 1＋settlement_items 1，寫入未被 RLS 擋 |
| A7 | 已結算之後還能改帳 | 已結算行程點消費列 → 開得了編輯表單 → 存檔後提示去按「重新計算」 | ✅ | 2026-09-07 實作-R |
| A8 | 封存之後不能改帳，但看得出來 | 封存行程點消費列 → **不開表單**，跳「先按重新開啟行程」 | ✅ | 2026-09-07 實作-R |
| A9 | 分享給沒帳號的人看 | 分享連結 → 唯讀頁看得到消費與結算，**沒有任何編輯入口** | ✅ | 2026-09-07（7 條：列是 div、11 個編輯字樣命中 0、無輸入欄、載入 0 寫入） |
| A10 | 每一顆按鈕都按得到 | 九畫面所有 button 可點區過門檻，且中心點命中自己 | ✅ | 2026-09-07 實作-S 複驗重跑 |
| A11 | 手機上不橫向捲動 | 390／320／414 三寬度都不橫向捲動 | ✅ | 2026-09-07（30 組：3 寬度 × 10 畫面） |
| A12 | 成員沒有 emoji 也認得出是誰 | `?members=noemoji` 全站顯示「名字第一個字＋填色圓底」，`🙂` 出現 0 次 | ✅ | 2026-09-07（10 畫面，裸字母 0、🙂 0、尺寸只有 28／20） |

## 這一輪新增（實作-S）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| S1 | 看得懂系統把匯率理解成哪個方向 | 填 `0.21` → 畫面出現「花 10,000 日圓，記成台幣 2,100」；按換個方向 → 變成 47,619,048，**兩者數量級明顯不同** | ✅ |
| S2 | 範例不會騙人 | 在記一筆輸入同一個示範金額，台幣欄算出來的數字**等於**範例裡寫的 | ✅ |
| S3 | 記一筆時補一句給自己看 | 記一筆填備註 → 存檔 → 重新打開那一筆看得到；**清單頁與分享頁看不到** | ✅ |
| S4 | 移除成員 | 移除鈕看得見的是圖示不是文字字元；可點區 44×44；有消費的成員不能移除 | ✅ |
| S5 | 事後補記一整趟舊行程 | 連續記三筆，第二、三筆的「記在」預設**跟著上一筆**，不是每次都跳回回程日 | ✅ |
| S6 | 記帳時看得懂金額欄要怎麼填 | 台幣未定案時，「填一邊就好」與「先記著了…」**同時**看得到；320px 不換行 | ✅ |
| S7 | 消費很多時還按得到「記一筆」 | 一進總行程頁不用捲就看得到按鈕；捲到底最後一筆不被蓋住 | ✅ |
| S8 | 一眼看出哪個金額在結算 | 金額兩欄並列時外幣是灰的、台幣是主色；外幣欄「有值」與「空的」看得出差別 | ✅ |

---

## 這一輪新增（實作-T）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| T1 | 從「還沒算清楚」直接去補那一筆 | 點清單裡的列 → 開得了編輯表單 | ✅ 2026-09-07（`verify_T.cjs` 2a–2d；封存態反向也驗過） |
| T2 | 看得懂結算是怎麼算出來的 | 展開計算依據 → 每個人看得到三段組成，三條等式都對得起來 | ✅ 2026-09-07（`verify_T.cjs` 5a–5f）**但正式資料有尾差，見下** |
| T3 | 切到外幣看自己填的原始金額 | 填過外幣的顯示原值不帶「約」；沒填的顯示回推值帶「約」 | ✅ 2026-09-07（`verify_T.cjs` 8a–8d；實測 ₩4,988 回推值 → ₩5,000 原值） |
| T4 | 返回時畫面不閃 | 進場動畫的每一幀都不得讓文件變寬 | ✅ 2026-09-07（`verify_T.cjs` 9，390/320/414 三寬度逐幀取樣 0/20/50/80/120/160/220ms） |
| T5 | 一眼分得出台幣與外幣 | 外幣對比度 4.4～4.7、台幣 ≥17 | ✅ 2026-09-07（`verify_T.cjs` 10a–10d；實測外幣 4.51、台幣 17.21、placeholder 2.54） |

---

## 沒有人在守的（誠實記下來，不要假裝有）

### 2026-09-07 實作-T 複驗新增的三筆（都不是這一輪造成的，但現在有人在看了）

- **「標記付清」與「開一趟自己的」只有 31px 高**（門檻是 44，或 34＋面積1600）。
  常設 C6 擴大到所有互動元素之後第一次掃到；`pre-T` 量到同樣的三顆，**是既有的**。
  這一輪沒改（不擴大工項），列在這裡等 Rozi 決定要不要排進下一節。
  判定程式：`standing_C6.cjs`，掃 11 個畫面 × 6 種元素，113 個互動元素。
- **結算頁展開的三欄，在正式資料上有 2～4 元的尾差**。
  用真的 `calc()` 跑過 Rozi 四趟正式行程（`acct_check.mjs`，不是 SQL 近似）：
  濟州島／北海道全員自洽；福岡 1 人差 2 元；東京 3 人差 2～4 元。
  來源是每筆均分各自 `Math.round` 的累積，**轉帳金額一塊都沒變、帳沒有算錯**。
  以前「應分攤」是用 `實際付出 − 差額` 反推的所以必然自洽，
  這一輪改成誠實計算，尾差就露出來了。**顯示方式要 Rozi 拍板。**
- **假資料涵蓋不到「多筆累積取整」**。`verify_T` 5d–5f 在假資料上全綠，
  是因為假資料的筆數少、剛好整除。要驗這一類，必須拿正式資料跑真程式。
  已把 `acct_check.mjs` 收進常設流程。


- **A11／A12 已於 2026-09-07 補跑並通過**（判定程式 `/home/claude/case_A11_A12.cjs`）。
  A11 第一版寫錯過一次：用「scrollWidth > clientWidth」掃所有元素，
  但 `overflow:visible` 的元素內容溢出時也會這樣回報卻**不會捲動**
  （tap44 的透明 `::after` 把 26px 頭像撐出 35px）→ 紅了一堆假的。
  改成只看 documentElement／body 與「overflow-x 不是 visible」的容器，
  另外一併回報「有沒有東西凸出視窗右緣」。
- **A9 已於 2026-09-07 補跑並通過**（`/home/claude/case_A9.cjs`）。
- **A1／A3／A4／A6 已於 2026-09-07 實作-S 複驗時補跑並通過**——
  用一筆 `begin…rollback` 交易，以 authenticated 身分把「建立行程 → 加成員 →
  只填外幣記一筆 → 分帳 → 結算 → 轉帳明細」整條走完再回滾。
  ⚠️ 過程中學到：這條路**不能塞進同一句 CTE**，
  `trip_members` 的 RLS 政策回頭查 `trips` 時看不到同一句剛 insert 的那一列，
  會被自己的權限擋下。要一句一句送，才是前端真正走的路徑。
- **核心區 12 條現在全部跑過。** 之後每輪照樣重跑核心區。
- ⚠️ **2026-09-07 發現核心區有兩個缺口**（Rozi 在手機上抓到的）：
  ① 畫面清單**沒有包含「還沒算清楚」那一頁**（`?unsettled=all`），所以那一頁的列
  點不進去從來沒被驗過；② A11 只在**動畫結束後**量橫向捲動，
  漏掉進場動畫那 200ms 文件被撐成 780px。兩個都已改進常設。
- E2E（TC-STATUS-18~20）需要 Docker，裝置 VM 上沒有，仍缺。
