# 驗收案例（總驗收）

> **這份跟「停止條件」是兩件事。**
> 停止條件問的是「這一節指令做到了沒」——那是**變更驗證**。
> 這份問的是「使用者要完成的事，從頭到尾走得通嗎」——那是**驗收**。
> 兩件事都做才叫驗收完成。（2026-09-07 建立：Rozi 問「你有自己做過 test case
> 做總驗收嗎？PO 在哪？這不是 QA 的工作嗎？」——當時的答案是沒有。）
>
> **規則**
> - 每一輪新功能要往這裡加案例；**每一輪都要跑一次「核心」那一區**
> - **實作-S 這一輪之後要跑「全部」，不是只跑核心區**（Rozi 2026-09-07 指定）
> - 案例以**使用者要完成的事**為單位，不是以畫面或元件為單位
> - 狀態只有三種：`✅ 跑過通過`｜`❌ 跑過沒過`｜`⬜ 沒跑過`。
>   **不准寫「應該可以」。** 沒跑過就是 ⬜
> - 每個案例要寫清楚「怎麼算通過」，而且要是**機器或人都能照著走**的敘述

---

## 核心（每一輪都要跑）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 | 最後跑 |
|---|---|---|---|---|
| A1 | 開一趟新行程並出發 | 填「這趟叫什麼／誰一起去／當地幣別／出發」→ 按出發 → 行程建立成功且出現在清單 | ✅ | 2026-09-07 production `begin…rollback`：行程 1、成員 2（含一個沒 emoji） |
| A2 | 必填沒填不能出發 | 清空「這趟叫什麼」→ 按出發 → 焦點跳到該欄、**沒有送出寫入** | ✅ | 2026-09-06 實作-O |
| A3 | 設好匯率，只填外幣也算得出台幣 | 設匯率 → 記一筆只填外幣 → 台幣自動算出且**方向正確** | ✅ | 2026-09-07 production：10,000 JPY ＠0.21 → **2,100** 台幣（與畫面上的換算範例同一個數字） |
| A4 | 記一筆（一起分） | 填金額、選付款人 → 記下來 → 出現在清單、金額與千分位正確 | ✅ | 2026-09-07 production：expenses 1 筆＋expense_splits 2 筆，備註一併存入 |
| A5 | 記一筆（各自付各的，用外幣填） | 各自金額用外幣填 → 存進去的分擔金額是台幣、Σ = 該筆台幣總額 | ✅ | 2026-09-06 實作-O |
| A6 | 結算一趟 | 按結算行程 → 產生轉帳清單 → 金額加總等於各人差額 | ✅ | 2026-09-07 production：settlements 1＋settlement_items 1，寫入未被 RLS 擋 |
| A7 | 已結算之後還能改帳 | 已結算行程點消費列 → 開得了編輯表單 → 存檔後提示去按「重新計算」 | ✅ | 2026-09-07 實作-R |
| A8 | 封存之後不能改帳，但看得出來 | 封存行程點消費列 → **不開表單**，跳「先按重新開啟行程」 | ✅ | 2026-09-07 實作-R |
| A9 | 分享給沒帳號的人看 | 分享連結 → 唯讀頁看得到消費與結算，**沒有任何編輯入口** | ✅ | 2026-09-07（7 條：列是 div、11 個編輯字樣命中 0、無輸入欄、載入 0 寫入） |
| A10 | 每一顆按鈕都按得到 | 九畫面所有 button 可點區過門檻，且中心點命中自己 | ✅ | 2026-09-07 實作-S 複驗重跑 |
| A11 | 手機上不橫向捲動 | 390／320／414 三寬度都不橫向捲動 | ✅ | 2026-09-07（30 組：3 寬度 × 10 畫面） |
| A12 | 成員沒有 emoji 也認得出是誰 | `?members=noemoji` 全站顯示「名字第一個字＋填色圓底」，`🙂` 出現 0 次 | ✅ | 2026-09-07（10 畫面，裸字母 0、🙂 0、尺寸只有 28／20） |

## 這一輪新增（實作-S）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| S1 | 看得懂系統把匯率理解成哪個方向 | 填 `0.21` → 畫面出現「花 10,000 日圓，記成台幣 2,100」；按換個方向 → 變成 47,619,048，**兩者數量級明顯不同** | ✅ |
| S2 | 範例不會騙人 | 在記一筆輸入同一個示範金額，台幣欄算出來的數字**等於**範例裡寫的 | ✅ |
| S3 | 記一筆時補一句給自己看 | 記一筆填備註 → 存檔 → 重新打開那一筆看得到；**清單頁與分享頁看不到** | ✅ |
| S4 | 移除成員 | 移除鈕看得見的是圖示不是文字字元；可點區 44×44；有消費的成員不能移除 | ✅ |
| S5 | 事後補記一整趟舊行程 | 連續記三筆，第二、三筆的「記在」預設**跟著上一筆**，不是每次都跳回回程日 | ✅ |
| S6 | 記帳時看得懂金額欄要怎麼填 | 台幣未定案時，「填一邊就好」與「先記著了…」**同時**看得到；320px 不換行 | ✅ |
| S7 | 消費很多時還按得到「記一筆」 | 一進總行程頁不用捲就看得到按鈕；捲到底最後一筆不被蓋住 | ✅ |
| S8 | 一眼看出哪個金額在結算 | 金額兩欄並列時外幣是灰的、台幣是主色；外幣欄「有值」與「空的」看得出差別 | ✅ |

---

## 這一輪新增（實作-T）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| T1 | 從「還沒算清楚」直接去補那一筆 | 點清單裡的列 → 開得了編輯表單 | ✅ 2026-09-07（`verify_T.cjs` 2a–2d；封存態反向也驗過） |
| T2 | 看得懂結算是怎麼算出來的 | 展開計算依據 → 每個人看得到三段組成，三條等式都對得起來 | ✅ 2026-09-07（`verify_T.cjs` 5a–5f）**但正式資料有尾差，見下** |
| T3 | 切到外幣看自己填的原始金額 | 填過外幣的顯示原值不帶「約」；沒填的顯示回推值帶「約」 | ✅ 2026-09-07（`verify_T.cjs` 8a–8d；實測 ₩4,988 回推值 → ₩5,000 原值） |
| T4 | 返回時畫面不閃 | 進場動畫的每一幀都不得讓文件變寬 | ✅ 2026-09-07（`verify_T.cjs` 9，390/320/414 三寬度逐幀取樣 0/20/50/80/120/160/220ms） |
| T5 | 一眼分得出台幣與外幣 | 外幣對比度 4.4～4.7、台幣 ≥17 | ✅ 2026-09-07（`verify_T.cjs` 10a–10d；實測外幣 4.51、台幣 17.21、placeholder 2.54） |

---

## 這一輪新增（實作-U，2026-09-07）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| U1 | 各自付各的、金額填外幣但外幣總額沒填時，帳不能算錯 | 該筆每個參與者的 `shares` 都是 `null`；全趟守恆式成立 | ✅ 2026-09-07（`conserve.mjs`，正式資料守恆差 50,860 → **0**） |
| U2 | 點成員金額看得到「這個人的帳」 | 三段（跟大家平分的／各自付各的／只算我的），小計加總＝列上金額，每列點得進編輯 | ✅ 2026-09-07（`verify_U.cjs` 5–10b） |
| U3 | 看得出來這一筆是誰付的 | 他是付款人的列有「你付的 $N」；不是的不得有（反向） | ✅ 2026-09-07（`verify_U.cjs` 8a/8b） |
| U4 | 「標記付清」按得到 | `.clearbtn` 可點區 ≥44×44，外觀仍 31px，不蓋住鄰居 | ✅ 2026-09-07（`verify_U.cjs` 14–16；`standing_C6` 113 元素全過） |
| U5 | 都轉給同一個人也算得對 | hub 模式下三條恆等式全過，每筆轉帳有一端是中心人 | ✅ 2026-09-07（`hub.cjs` 10 條） |
| U6 | 在一長串消費裡看得出哪幾筆還沒算清楚 | 算不出來的列左框≠正常灰；算得出來的維持正常灰（反向） | ✅ 2026-09-07（`verify_U.cjs` 19–19c） |
| U7 | 卡住的時候看得懂卡在哪、誰沒填、怎麼解 | 灰字情境切換、警示點名、toast 不說謊、紅框只標沒填的、按一次就存 | ✅ 2026-09-07（`verify_U7.cjs` 28 條） |

---

## 沒有人在守的（誠實記下來，不要假裝有）

### 2026-09-07 實作-U 複驗新增

- **已結算／已封存態仍顯示「約」標記**（`藥妝店 約 $1,035`）。
  `規格_金額未定案與幣別.md` §5.6 明寫「不顯示「約」標記、不顯示未定案入口、
  不做結算前檢查」——未定案入口已正確不顯示，只有「約」還在。
  pre-U（35f1bef）量到一模一樣 → **既有缺口，非實作-U 造成**。
  斷言 `verify_U.cjs` 19d **刻意留紅不放寬**，等下一節處理。
- **Cowork 自己的斷言在這一輪錯了六處**，逐條記在 `_可以推了.txt`。
  其中兩個最危險：①用錯 harness 參數（`?unsettled=mix` 是進未定案頁的路由參數）
  導致整組斷言在**錯的畫面上全綠**——假通過；②為了省時間把
  `scrollIntoView` 從無條件改成「看不到才捲」，元素被 sticky 底部按鈕列蓋住 → 7 個假紅。
  **教訓：斷言的「畫面／元素／時間點」三件事，改任何一件都要重新驗證它還在量對的東西。**


### 2026-09-07 實作-T 複驗新增的三筆（都不是這一輪造成的，但現在有人在看了）

- **「標記付清」與「開一趟自己的」只有 31px 高**（門檻是 44，或 34＋面積1600）。
  常設 C6 擴大到所有互動元素之後第一次掃到；`pre-T` 量到同樣的三顆，**是既有的**。
  這一輪沒改（不擴大工項），列在這裡等 Rozi 決定要不要排進下一節。
  判定程式：`standing_C6.cjs`，掃 11 個畫面 × 6 種元素，113 個互動元素。
- **結算頁展開的三欄，在正式資料上有 2～4 元的尾差**。
  用真的 `calc()` 跑過 Rozi 四趟正式行程（`acct_check.mjs`，不是 SQL 近似）：
  濟州島／北海道全員自洽；福岡 1 人差 2 元；東京 3 人差 2～4 元。
  來源是每筆均分各自 `Math.round` 的累積，**轉帳金額一塊都沒變、帳沒有算錯**。
  以前「應分攤」是用 `實際付出 − 差額` 反推的所以必然自洽，
  這一輪改成誠實計算，尾差就露出來了。**顯示方式要 Rozi 拍板。**
- **假資料涵蓋不到「多筆累積取整」**。`verify_T` 5d–5f 在假資料上全綠，
  是因為假資料的筆數少、剛好整除。要驗這一類，必須拿正式資料跑真程式。
  已把 `acct_check.mjs` 收進常設流程。


- **A11／A12 已於 2026-09-07 補跑並通過**（判定程式 `/home/claude/case_A11_A12.cjs`）。
  A11 第一版寫錯過一次：用「scrollWidth > clientWidth」掃所有元素，
  但 `overflow:visible` 的元素內容溢出時也會這樣回報卻**不會捲動**
  （tap44 的透明 `::after` 把 26px 頭像撐出 35px）→ 紅了一堆假的。
  改成只看 documentElement／body 與「overflow-x 不是 visible」的容器，
  另外一併回報「有沒有東西凸出視窗右緣」。
- **A9 已於 2026-09-07 補跑並通過**（`/home/claude/case_A9.cjs`）。
- **A1／A3／A4／A6 已於 2026-09-07 實作-S 複驗時補跑並通過**——
  用一筆 `begin…rollback` 交易，以 authenticated 身分把「建立行程 → 加成員 →
  只填外幣記一筆 → 分帳 → 結算 → 轉帳明細」整條走完再回滾。
  ⚠️ 過程中學到：這條路**不能塞進同一句 CTE**，
  `trip_members` 的 RLS 政策回頭查 `trips` 時看不到同一句剛 insert 的那一列，
  會被自己的權限擋下。要一句一句送，才是前端真正走的路徑。
- **核心區 12 條現在全部跑過。** 之後每輪照樣重跑核心區。
- ⚠️ **2026-09-07 發現核心區有兩個缺口**（Rozi 在手機上抓到的）：
  ① 畫面清單**沒有包含「還沒算清楚」那一頁**（`?unsettled=all`），所以那一頁的列
  點不進去從來沒被驗過；② A11 只在**動畫結束後**量橫向捲動，
  漏掉進場動畫那 200ms 文件被撐成 780px。兩個都已改進常設。
- E2E（TC-STATUS-18~20）需要 Docker，裝置 VM 上沒有，仍缺。

---

## 這一輪新增（實作-V，2026-09-07）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| V1 | 改完一筆立刻再打開，看到的是改過的 | 改標題 → 存 → 1 秒內重開同一筆 → 讀到新標題 | ✅ 2026-09-07（`verify_V.cjs` 1／2；**未修版金絲雀確實紅過**：清單已更新但單筆仍是舊的） |
| V2 | 新增一筆之後立刻打開它 | 記一筆 → 存 → 點那一列 → 1.2 秒內標題欄是剛打的字，且沒有 pageerror | ✅ 2026-09-07（`verify_V.cjs` 4，H-1 修好量測靶後轉綠） |
| V3 | 別人請客那一筆，在「他的帳」看得出是進帳不是花費 | S-03 與「{名字} 的帳」的金額都以 `−` 開頭、顏色都等於 `--in`；不顯示「你付的」 | ✅ 2026-09-07（`verify_V.cjs` 5a／5b／6） |
| V4 | 警示看得完整，不會圖示孤零零在上一行 | 圖示與文字第一行 top 差 ≤4；320px 下文字佔 2 行且每行左邊界對齊 | ✅ 2026-09-07（`verify_V.cjs` 8／9／9b／9c） |
| V5 | 捲到行程中段還找得到返回與更多 | 捲 400px 後頂部條 ≤64px 黏在頂端，兩顆鍵仍 44×44 且中心點命中自己，行程名還看得到 | ✅ 2026-09-07（`verify_V.cjs` 15／16／17／18） |
| V6 | 首頁捲動時還看得到「新增行程」 | 捲 300px 後頂部列 top ≤4、底色不透明；捲到最上面不蓋住第一張卡 | ✅ 2026-09-07（`verify_V.cjs` 12／12b／13） |
| V7 | 一進行程頁就看得到消費，不是只看到一片藍 | 捲動位置 0、390×844 時至少 2 筆 `.exprow` 完整落在視窗內 | ✅ 2026-09-07（`verify_V.cjs` 19，實測 9 筆） |

### 2026-09-07 實作-V 收尾：核心區與全部案例重跑

**規則是「實作-S 之後每輪跑全部」，這一輪照做了。**

- **A1／A3／A4／A5／A6 ＋ 編輯 ＋ 刪除 ＋ 標記付清**：對 production 用一筆
  `begin … rollback` 交易，以 `authenticated` 身分整條走完
  （建立行程 → 加成員（一個 `emoji=''`）→ 只填外幣記一筆 → 分帳 →
  各自付各的（外幣填）→ 編輯改標題 → 結算 → 轉帳明細 → 標記付清 →
  刪除分帳與消費）。回傳：行程 1、成員 2、消費剩 1、編輯後標題正確、
  分帳 2、結算 1、已付清 1。**回滾後查 `trips` 沒有任何 ZZ 開頭的列。** ✅
  ⚠️ 這一輪學到：`trip_members.emoji` 是 **NOT NULL**，
  「沒有 emoji」在資料庫裡是**空字串**不是 null；
  `payment_method` 是 enum（`cash|credit_card|stored_value`），
  `payment_methods` 是 **jsonb** 不是 `text[]`。
- **A2／A7／A8**：`verify_R`／`verify_T` 重跑全綠 ✅
- **A9（分享頁沒有編輯入口）**：`case_A9.cjs` 7 條全綠 ✅
- **A10（每顆按鈕按得到）**：`standing_C6.cjs` 113 個互動元素全綠 ✅
- **A11／A12**：`case_A11_A12.cjs` 重跑。**A12 全綠；A11 這次紅了一條**——
  不是回歸，是**我把畫面清單補上狀態變體之後第一次掃到**（見下）。
- **U1（守恆式）**：`conserve.mjs` 對 `驗收：濟州島` 111 筆跑過，差 **0** ✅

## 沒有人在守的（2026-09-07 實作-V 收尾新增）

- **`s05&state=settled` 在 320px 有 2px 橫向捲動**（`.clearbtn` 右緣 322 vs 視窗 320）。
  `9bc7269`（實作-V 之前）量起來一模一樣 → **既有缺口**。
  逃掉的原因：**A11／C4 的畫面清單只有「乾淨的十個畫面」，沒有任何狀態變體**。
  已把 `s05&state=settled`／`s05&state=archived`／`s03&state=settled`／
  `s03&unsettled=all` 加進 `case_A11_A12.cjs` 的 `SCREENS`。已寫成 `實作-W` W-2。
- **已結算／已封存的消費列仍標「約」**（違反 §5.6）。統計卡做對了、消費列沒有——
  **同一條規則有兩個實作點，斷言只守了一個**。已寫成 `實作-W` W-1。
- **分享頁在已結算行程上的行為目前量不到**：量測靶的 `rpc` 回的行程沒有套用
  `?state=settled`，所以分享頁永遠看到 `active`。已寫成 `實作-W` W-1b。

## 這一輪新增（實作-W，2026-09-07　指令已寫入，還沒跑）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| W1 | 已結算的行程看起來是定案的 | 已結算／已封存態 `.approx` 為 0，但橘紅左邊框維持不變 | ✅ 2026-09-07（`verify_W` 1–4） |
| W2 | 小螢幕（320px）也不會把畫面拉歪 | `s05&state=settled` 三寬度都不橫向捲動，`.clearbtn` 仍 ≥44×44 | ✅ 2026-09-07（`verify_W` 7–10，三寬度各驗） |
| W3 | 收到分享連結的人，點得進某個人的帳看他花了什麼 | 分享頁成員列點得進去，三段結構與自己那邊相同，金額對得起來 | ✅ 2026-09-07（`verify_W` 12／13／16／19／20；骨架並排比對過） |
| W4 | 收到分享連結的人**改不了**任何東西 | `s06&member=N` 的消費列全是 `div`、0 個輸入欄、不含五個編輯字樣、連點三列都不開表單 | ✅ 2026-09-07（`verify_W` 14／15 ＋ `case_A9` 已擴充掃這一頁） |

⚠️ **A9 的掃描範圍要擴充**：現在只掃分享頁本身（`s06`），
W-3 會生出一個新表面 `s06&member=N`。**新畫面沒加進清單就永遠不會被 A9 掃到**
——這正是 2026-09-07 連續踩到第三次的同一個錯（斷言的範圍圈錯）。

## 這一輪新增（實作-YXW 的批 Y 與批 X，2026-09-07）

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| Y1 | 頂部看起來是一整塊，不是拼接的兩塊 | `?sat=59` 下，安全區與底下那一條由同一個元素畫（收合與展開都要） | ✅（`verify_Y` 5／6） |
| Y2 | 捲動時頂部列不會透出底下的卡片 | 五個分數捲動位置 × 每 0.25px 取樣，全部命中頂部列；且不是靠負位移解的 | ✅（`verify_Y` 10／11）**真機的 1px 縫仍只有 Rozi 看得到** |
| Y3 | 每一頁的標題列跟內容看起來是同一個平面 | 四個畫面的 `.bar` 底色等於它下面那一塊 | ✅（`verify_Y` 13／14） |
| Y4 | 內容不會被瀏海壓到 | 十一個畫面在 `?sat=59` 下第一個可見文字沒被蓋住 | ✅（`verify_Y` 4） |
| X1 | 只想看要跟大家分的帳 | 開關打開後少的剛好是「自己買給自己的」，不誤傷「別人付、只算一個人」 | ✅（`verify_X` 1／2／3） |
| X2 | 錢不會憑空消失 | 總花費(關) − 共同的帳(開) ＝ 摘要行金額 | ✅（`verify_X` 5／6） |
| X3 | 開關不會把「還沒填完」的警告一起藏掉 | 兩個狀態的「還沒算清楚」筆數相同 | ✅（`verify_X` 9） |
| X4 | 開關不會改變誰欠誰 | `s05` 與 `s05&onlyshared=1` 的轉帳清單逐字相同 | ✅（`verify_X` 12） |
| X5 | 下次進來還是我上次選的樣子 | 按下開關 → 重載 → 仍開著 | ✅（`verify_X` 13） |
| X6 | 全部都是自己買的時候不會看到一片空白 | 清單為空時有一句話說明 | ✅（`verify_X` 16） |
| X7 | 換了引擎之後我的帳沒有變 | 用**重新編譯的引擎**對正式帳 132 筆重跑：總花費／每人／三筆轉帳／尾差／逐筆守恆逐字相同 | ✅ 2026-09-07 |

## 這一輪新增（修-5／修-6／修-7，2026-09-10）　commit `76dd035`

| # | 使用者要完成的事 | 怎麼算通過 | 狀態 |
|---|---|---|---|
| P1 | 想看看有沒有新資料，往下拉一下就重新整理 | 從頂端拉 80px 放開會重新抓；拉 40px 不會（誤觸保護） | ✅ 2026-09-10（`verify_AF_ptr` 4 條，CDP 送真的觸控事件） |
| P2 | 收訊不好的時候，不會一直看著轉圈不知道發生什麼事 | 10 秒沒回應 → 停轉圈、跳「現在連不上網路」、原本的資料留著不動 | ✅ 2026-09-10（`verify_AF_ptr_timeout` T0–T5b 共 8 條，金絲雀兩種都有效） |
| P3 | 網路正常的時候，下拉不會亂跳錯誤 | 反向：正常下拉 3 秒後不轉圈、不出現「連不上網路」 | ✅ 2026-09-10（T5／T5b） |
| P4 | 在飛航模式下打開 App，看得到上次的行程 | 斷網重抓後行程卡還在，同時有講「連不上／讀不到」 | ✅ 2026-09-10（`verify_AF` A9／A9b） |
| P5 | 斷網時點開一筆消費，不會被畫成一張空白表單害我覆蓋掉原本那筆 | 表單有內容或明說「連不上」；按「記下來」擋你的理由是網路不是付款人；不准真的寫入 | ✅ 2026-09-10（`verify_AF_blank` A12／A12b／A12c） |
| P6 | 第一次進來知道要按哪裡開新行程 | s01 上「開新行程」的入口**恰好 1 個**（右上按鈕已移除），幽靈卡排第一張、寫「記新的一趟」 | ✅ 2026-09-10（`verify_AF` A2／A7） |

### 沒有人在守的（誠實列出）

- ~~下拉的手感~~ —— **2026-09-11 Rozi 手機實拉，回報「手感 OK」。關閉。**
- **「連快取都沒有」時點開一筆消費**（直接開編輯連結又離線）——Cowork 的測法會把整頁渲染成空白，量到的紅是測法造成的，已拿掉該案例；終端機自己實測過那條路，但實作者的自驗不算數
- **`原型_*.cjs` 13 支**——回歸清單從來沒有涵蓋 `原型_*`，已列延後

## 2026-09-11　Phase 1 收線前的實測（Cowork 跑，用 76dd035 的建置）

Rozi 2026-09-11 問「Phase 1 有沒有要結束了」。收斂條件（2026-09-08 她同意的）是
**除非算錯錢，其餘一律進延後清單**，所以先把還掛著的缺口實際量一次、分成兩堆。

| # | 量什麼 | 量到 | 歸類 |
|---|---|---|---|
| Q1 | 已封存的行程，消費列點不點得進編輯 | 表單**不開** | ✅ 已解 |
| Q1b | 已結算的行程（對照組，規格說「可以編輯」） | 表單**會開** | ✅ 符合規格 |
| Q2 | 已結算／已封存的消費列還有沒有「約」標記 | 0 處 | ✅ 已解 |
| Q3 | `s05&state=settled` 在 320px 會不會橫向捲動 | `scrollWidth=clientWidth=320` | ✅ 已解 |
| Q4 | 結算頁有沒有壞掉的數字（NaN／undefined／負金額） | 都沒有 | ✅ |
| Q5 | 各自金額能不能指定幣別（P1-0，會算錯錢那條） | 有「$ 台幣填／₩ KRW 填」兩個分頁 | ✅ 已解 |

**守門（防假通過，2026-09-07 踩過一次）**：

- 一般狀態的 `s03`「約」有 **2 處**、有「還沒算清楚」字樣、11 筆消費
  → 已結算量到 0 是真的拿掉了，不是假資料裡本來就沒有
- `s05&state=settled` 上 `.clearbtn` **存在**、右緣 291 → 真的量到目標了
- `s03&state=archived` 有封存字樣、11 筆可點的消費列 → 「點不進去」是真的被擋住

**結論：機器這一側，會算錯錢的缺口是 0 項。**
剩下的終點線是收斂條件寫的那一條——Rozi 自己在手機上走完「驗收：濟州島」那一趟。
