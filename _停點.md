# 停點｜實作-R（一次「收」跑完，**已上線**）　2026-09-07 02:05
需求↔斷言對照在 `_需求台帳.md`；歷輪在 `Claude outputs/_實作B進度.md`。

## R-1　「按『換個方向』沒有反應」——那顆鈕沒壞，是 52×19，手指按不到
`.ratelink` 加內距、再用**等量負外距**把版面吃回去 → 45×76，看得見的字停在原位。
同一類的還有成員移除鈕（24×24）與當按鈕用的 avatar（28×28），用透明 `::after`
撐到 44×44——**擴可點區，不放大看得見的圖形，也不撐大 border box**（撐大會擠掉同列內容）。

> 🔴 **我自己的斷言先漏了一次**：`.avatar` 是 `overflow:hidden`（給 `<img>` 頭像裁圓角用），
> 會把 44×44 的擴張區**整個裁掉**——擴了等於沒擴，而量 computed style 完全看不出來。
> 是「橫向溢出」那條既有斷言先紅了才發現。修法：`.avatar.tap44 { overflow: visible }`，
> 並把量測改成「擴張矩形 **∩ 每一層裁切祖先的可視框**」，之後再有人被裁就會紅。

**B 類（矮但很寬、實際點得到）一顆都沒動**：`.chip` 34.5、分頁鈕 38.5、
`[data-payrow]` 46.5、`.rowb` 46——四項改動前後完全相同。

## R-3　App 圖示：T 太小又浮在上方
根因：`<text y="320">` 的 **`y` 是基線不是中心**，260px 的字墨水只佔 35.7%、
中心比正中央高 27.5px。**不用字體度量推算**（`system-ui` 在不同機器是不同的字），
寫了 `實作_圖示T置中.cjs`：把 SVG 畫進 canvas → 找出米白像素的邊界框 →
迭代調 `font-size` 與 `y`，三輪收斂到 **墨水高 52.0%、垂直偏 0.00%**。
三張 PNG 全部從改好的 SVG 重出，檔名維持 `-v2`（改檔名等於再製造一次快取問題）。
底色漸層、圓角、字色一個都沒動。

## R-4　已結算的消費列點不動——**是實作把規格做反了**
Rozi 問「已結算不代表有按封存吧？這兩個功能應該是分開的？」——她是對的。
`畫面地圖.md:118` 白紙黑字：已結算 → 列表唯讀（**點擊仍可編輯，重新計算**），
封存那一列才沒有那個括號；`規格_金額未定案與幣別.md` §5.6 對已結算只規定
「不顯示約、不顯示未定案入口、不做結算前檢查」，**一個字都沒提唯讀**。
而 `ExpenseListPage.tsx` 的註解寫「既有 bug：已結算仍點得進編輯」——那不是 bug。

- 已結算 → 列點得開編輯表單；存檔／刪除後跳「帳改了。記得到『結算』分頁按『重新計算』」
  （**不自動作廢、不自動重算**——那會抹掉已經標記付清的紀錄，而且不可逆）
- 封存 → 維持不可編輯，但點下去講一句「這趟封存了。要改的話，先按下面的『重新開啟行程』」
- **`summary.ts` 的 `readonly` 一個字都沒動**（它同時在管 §5.6，那對已結算是成立的）；
  只換 `ExpenseListPage` 傳給 `ExpenseGroups` 的值與 `openEdit()` 的守衛
- 分享頁不傳 `onReadonlyTap` → 列維持 `<div>`，行為一個字沒變（反向斷言守著）

## 驗證
`tsc` 0 錯｜`vitest` **306 綠**無 Unhandled｜`pnpm run build` 過。
可點區與圖示T **62**（掃 82 顆按鈕：低於門檻 0、中心點被吃 0、擴張被裁 0）｜
版面回歸 1163｜匯率與千分位 42｜寫入與更新 61｜高度階梯與圖示 50｜輸入框與選單 53｜
查不到與把手 58｜成員識別 79｜原型真實形狀 50｜資料形狀 9｜token 52｜型別 29｜文案 13。

**五組反向驗證全部紅過**：拿掉 `.ratelink` 擴張 → 4 紅｜拿掉 `.tap44::after` → 9 紅｜
`.avatar.tap44` 改回 `overflow:hidden` → 5 紅｜SVG 的 `y` 改回 320 → 3 紅｜
已結算改回不可編輯 → 單元 1 紅＋瀏覽器 3 紅。

> ⚠️ **基準值的差異已釐清（Cowork 2026-09-07 回覆）：是取樣條件不同，不是誰量錯。**
> s02 941／s02b 1201 是「匯率沒填」時量的，我的腳本量之前先填了 0.21，版面本來就不同；
> s01 我量到 1688、Cowork 量到 844，量法不同。
> **關鍵結論兩邊一致**：用 `569cf56`（實作-R 之前）現場建置的 pre-R harness 前後對照，
> 九個畫面的捲動高度**差 0**，`.chip` 與 `.rowb` 的高度集合完全相同——**版面沒有任何改變**。
> 我這一支腳本改用自己實測的 pre-R 值當基準，比對的是同一種量法，結論相同。

## 超出白名單的兩個檔（都是必要的）
- `src/lib/messages.ts`：R-4b／4c 的兩句文案。專案規則是「同一句文案只准寫一次」，
  所以放常數檔，不散在元件裡。
- `Tripay_原型.html`：`.ratelink` 的 CSS **來源就是原型**（`src/index.css` 那一段是
  `實作_原型CSS移植.cjs` 抽出來的，直接改會被下一次抽取覆蓋）。只改了那一條的內距／外距。

## 要 Rozi 在手機上看過才算數
1. 「換個方向」現在按不按得到（可點區 45×76，但真機手指落點只有她說了算）
2. 成員移除鈕與 emoji 圓圈——**擴大的是透明區，看起來應該一模一樣**；
   如果覺得「變得容易誤觸」要講
3. **App 圖示**：要先刪掉主畫面舊圖示、重新「加入主畫面」一次
4. 已結算的行程點消費列現在打得開；改完會跳「記得按重新計算」——那句話夠不夠清楚

## 順手看到但沒動
- **原型的 S-02／S-02b 欄位一致（實作-O-5）仍未補**；這一輪也只動原型的 `.ratelink` 一條。
- 成員移除鈕的 `✕` 是文字字元（違反「不要用文字字元充當 icon」）——
  指令明文寫「留到下一輪」，已寫進 `_延後與不做清單.md`。
- 刪除一筆消費也會讓已結算的數字過期，所以 `del` 那條路徑也帶了 `saved=true`
  （指令只寫「存檔成功之後」）。**這是我加的，覺得不對可以拿掉。**
- 既有的紅（與這一輪無關）：`原型_字級與真機測試` 3｜`原型_貼邊與結構測試` 5｜
  `原型_預設日期與欄位列測試` 1｜`原型_版面與拖曳測試` 2｜`原型_焦點與圓角測試` crash。

## 🚀 已上線　https://tzyutw.github.io/tripay/
Cowork 複驗 28/28 ＋ 圖示 15/15；金絲雀打在 `569cf56`（實作-R 之前）→ **7 紅**，
證明斷言會咬不是空跑。並對 production 實跑三條寫入路徑（已結算行程的
expenses UPDATE 2 筆／軟刪除 1 筆／expense_splits UPDATE 12 筆，全部 rollback）
——**R-4 讓已結算行程重新可編輯之後，RLS 沒有把這三條路徑擋掉。**
