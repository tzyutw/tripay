# 停點報告

> 2026-09-05　狀態：**#30 全部做完（30-1～30-7），等 Rozi 在手機上看**

## 手機打開這個網址就會自動變成真機模式
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**

## 你抓到的「結算會跳過這筆」——那句確實把因果講反了
它把一個**隨時可以解除的暫時狀態**，寫成了**永久的判決**。使用者讀到「會跳過」，
合理的反應就是懷疑記它有沒有意義；但事實正好相反——**這筆已經完整記下來了**。

三句都改了，並訂成常設規則（先說東西還在，再用可行動的語氣講解除條件）：
- 沒設匯率 →「**先記著了。設好這趟的現金匯率就會自動換算。**去設定 ›」
- 沒台幣金額 →「**先記著了。補上台幣金額就會算進結算。**」
- 有匯率、正常 → 不變

**同一句在兩處出現，現在引用同一個常數**（`MSG_TWD_PENDING`），原始碼只寫一次。

## 統計卡那句也是同一個毛病
「換算不了」講的是系統的處境。改成「**有 2 筆還沒換算成台幣，上面的總花費不含它們。
設現金匯率 ›**」——講出後果、規模、解法。補上匯率後整條消失。

**一處我按指令改了計數方式，說明理由**：指令的兩句是對稱的（台幣檢視／外幣檢視），
但實作上 `money()` 有匯率時兩邊都換算得出來，所以**外幣那句在任何狀態下都不成立**，
沒有做。台幣那句我只數「有外幣金額、缺台幣金額」的筆數——完全沒填金額的那些
補上匯率也救不回來，而且 S-03-29「有 N 筆還沒算清楚」已經在講它們了。
這樣「補上匯率 → 這條消失」才成立。

**外幣分頁不是壞掉**：示範資料本來就沒設匯率（要展示「還沒設匯率」那一態），
所以外幣鍵是灰的。已在 S-03 的原型控制加一顆「設一個示範匯率」，按了就切得過去，
總花費與每人分擔列都會變成 ₩。

## 兩處稿註真的漏在產品畫面裡
S-00 的「移除了 App icon 與幽靈卡」、S-01 的「列表到此結束」——**都在手機框內**，
產品上真的會顯示。兩句都刪了，並加了永久斷言：手機框內不得出現描述稿件本身的字眼
（掃 11 個關鍵字，三個寬度）。**沒有掃到第三、第四句。**

## 其餘
- 三個關閉鍵改用 `.ic2`（原本 24×24＋13px icon，可點區遠低於 44）；`.x` 的 CSS 已刪
- hero 拿掉成員 emoji。你的疑慮是對的：沒設 emoji 的人會退回名字第一個字，
  四個字串在一起沒間距沒圓底——**四人都沒設時是「小阿美魚」**
- S-07-4 整段拿掉。移除後設定頁只剩帳號卡與登出，**看起來不空**（登出是滿版按鈕）
- 金額列 39px → **51px**，改用 `<label>`，**點「台幣」兩個字就能輸入**；逐人金額列同樣
- S-00 字標 44px 列為字級階梯的唯一例外，測試裡寫明，不會被順手縮掉

## 測試（十組全過，新增一組 54 項）
60／132／54／34／18／45／79／65／114／**54**。原型 212 項，兩份盤點文件都對得起來。
