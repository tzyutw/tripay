# 停點報告

> 2026-09-04　狀態：**#16 完成，等 Rozi 看過再做批二**

## 交付
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**（同一連結）

## 做了什麼
`S-02b-12` 從 4 個輸入框收斂成 **2 個**，唯讀的「1 TWD ≈ 45.0 KRW」那一列整條移除——
畫面本身就是那個算式。你點出的那件事是對的：**換了多少錢對分帳完全沒有影響，
只有匯率有**，所以兩個金額欄本來就是多餘的。

    🇹🇼  [   1   ]  TWD      ← 預設 1
    🇰🇷  [  45   ]  KRW      ← 只填這個

- 標籤改成國旗＋幣別代碼，「台幣總額」這類文字標籤拿掉
- 金額字級放大、等寬字（這是區塊主角）
- 焦點中的欄位用主文字色，另一欄轉次要
- `inputmode="decimal"`，兩欄都吃小數；外幣小數位數依幣別（KRW／JPY 不給小數）
- 灰字只剩兩句：**在當地換錢後填一次就好** ／ **刷卡不用這個匯率，直接填台幣**

## 「1」擺哪一欄
照你給的規則實作：**1 單位外幣換算成台幣後 < 0.1 → 「1」放台幣欄，否則放外幣欄**。
`currencies.ts` 全部 18 種都判好了，落在台幣欄的是 **KRW／VND／IDR**，其餘都在外幣欄。
**「1」那一欄排在上面**，所以讀起來永遠是「1 X = N Y」；
兩列上下順序會隨幣別改變，測試裡明確斷言這件事，不會被誤當成 bug 改掉。

## 測試（三組全過，新增 13 項）
| 測試 | 項數 | 結果 |
|---|--:|---|
| `原型_批一回歸測試.cjs` | 45 → **58** | 全過 |
| `原型_輸入回歸測試.cjs` | 34 | 全過 |
| `原型_版面回歸測試.cjs`（真實 Chrome，320／375／414）| 12 | 全過 |

新增的 13 項逐條對應你的停止條件：輸入框數為 2、無唯讀匯率列、無「≈」算式、
KRW「1」在台幣欄且在上、填 45 → 內部匯率 45、JPY「1」在外幣欄且在上、
台幣欄填 0.22 → 等價於 1 JPY = 0.22 台幣、THB「1」在外幣欄、
**10000 / 450000 與 1 / 45 算出同一個匯率**、兩欄都接受小數、灰字只留兩句。

## 一個實作說明
內部匯率存成 **外幣 ÷ 台幣**（1 台幣換得到幾單位外幣），另提供 `twdPerForeign()` 反向表達，
因為 S-04 換算時要用的是「1 外幣值多少台幣」。兩者互為倒數，測試兩個方向都驗過。

## 沒做的
只動 `S-02b-12` 這個區塊，S-02b 其餘部分未碰。
未改 `src/`、未跑 migration、未刪檔。
