# 停點　2026-09-12 16:45　收尾-AI（文案四處 ＋ 圖示鈕改 IG 式）

## 十六條停止條件的實際輸出
| # | 量到什麼 |
|---|---|
| 1 | `還沒換算成台幣` **0**／`總花費還少了` **1** |
| 2 | `方向由系統判斷` 全 src **0**／`填完檢查一下對不對` **1** |
| 3 | `每個人只要轉一次` **0**／`現在要轉` **1** |
| 4 | ⚠️ **做不到，見下面「兩處與指令不符」** |
| 5 | 預設假資料 N==M → `.note.calm` 為 `null`、畫面不含「都轉給同一個人」✅ |
| 6 | ⚠️ `出發！` 量到 **2** 不是 1（見下），驚嘆號還在 |
| 7 | `移除 ` 帶空格 **0 行**／`移除${` **1 行** |
| 8 | `^.ic2 {` **1**／深底變體 class **0** |
| 9 | s05：bg `rgba(0, 0, 0, 0)`、border `0px`、blur `none` |
| 10 | s03 封面：bg `rgba(0, 0, 0, 0.22)`、border `0px`、blur `blur(14px)` |
| 11 | s03 捲 400px 後（已確認 `.herowrap.is-compact` 真的翻了）：同上三個值 |
| 12 | 三處都是「圖形 40×40／可點區 44×44」 |
| 13 | s03 封面 **9.6**（線 #fff／圓內實測 L=0.059）；s05 **6.0**（線 rgb(76,91,100)／底 L=0.850） |
| 14 | s03、s05 × 320／390／430 六組都不橫向捲動（含頁內會捲的容器） |
| 15 | 原型與 index.css 的 `.ic2` 外觀屬性、`.hero` 三個變數，程式抽出來比對後完全相同 |
| 16 | 單元測試 353 passed／3 skipped、`tsc` 0、`build` 0 |

回歸：新腳本 `實作_圖示鈕IG式測試.cjs` 34/0、token 掃描 54/0、可點區 61/0、安全區 74/0、
版面回歸 1163/0、失敗路徑 52/0、全部付清 21/0。

## 兩處與指令不符（用量到的值，沒有硬湊）

**條件 4 做不到**：假資料的三筆轉帳**本來就全部從同一個人出去**（Alex→Robin／Sam／Kai），
改成 hub 還是三筆，**甲案正確地把整句收掉了**——條件 4 與甲案互相矛盾。改在單元測試用
一組刻意不同的資料驗那句話：direct 2 次 → hub 3 次，畫面確實印出「現在要轉 2 次」「會變成 3 次」。
**條件 6 的基準是 2 不是 1**：862 行是按鈕，502 行是註解在引用那三個字。我沒動這個檔。

## 指令沒寫、但不做會壞的四件事

1. **原型要一起改**：`screens.json` 是從原型機器抽出來的（只改 json 下次重抽就打回原形），
   而且有測試在比對「原型有的字 App 有沒有」。AI-1／2／3 都同步進原型，
   `settleTrip()` 多兩個可選參數才算得出「會變成幾次」。
2. **s05 段數基準 104 → 102**（原型套甲案後那兩段不再渲染）；
   **s03 頁高基準 1632 → 1613**（AI-1 那句從兩行變一行，實測高 19px、行高 18.85px，對得起來）。
3. **token 掃描的兩條 `.ic2` 條款重寫**。其中一條原本守 `.ic2::after` 用 `var(--h-tap)`，
   但那條手寫規則**早就被移植區塊蓋掉**——守著一條沒有作用的死規則綠了一輪又一輪，
   合併成一份時才露出來。已把原型的 `::after` 也改用 token（數字仍是 44）。

## 🔴 要 Rozi 在手機上看過才算數
毛玻璃在 iOS 的實際觀感（`-webkit-backdrop-filter` 已加）：封面那三顆鈕應該「透透的、
看得到底下照片」不是灰片；純色列應該只剩線條、沒有圓底。

## 順手看到但沒動
`實作_版面回歸測試.cjs` 的 `SCREENS` 仍沒有狀態變體（AH-3 新規則說該補）；
移植輸出有 4 條規則不在 index.css（`.ghost` 兩條＋兩個 at-rule），改動之前就存在。
