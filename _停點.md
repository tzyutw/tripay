# 停點｜實作-B～J（**已上線**）　2026-09-06 11:30

## 實作-J　成員識別 S-02c-10 全站貫徹 ＋ 兩個版面缺陷

**1. 全站貫徹**　`shared/Avatar.tsx` 加 `size`（28／20），八處接上：
A 類 28px（統計卡、要排除誰逐人列、各自金額逐人列、S-05 人話淨額）；
B 類 20px（誰付的 chip、只算一個人 chip、hub 中心人 chip、S-05 對帳表）。
C 類（`memberLabel()` 回傳字串、複製用摘要、登入者頭像）**照裁示沒動**。

**2. 「加一個人」的頭像列整列刪掉**（原型 S-02-15 沒有頭像，盤點表也寫「emoji 選擇鈕要拿掉」）。
新成員 emoji 恆為空，加進來後由 Avatar 第二層顯示。🙂 因此不再出現在任何畫面。

**3. 裸元素選擇器壓過元件尺寸**　`input[type=text]{width:100%}` 搬過來時 `.ui` 前綴掉了，
變成全域 (0,1,1) 壓過 Tailwind 的 (0,1,0)，`w-8 h-8` 靜默失效、輸入框撐滿整列。
改用 `:where()` 降到 0。**抽取器一起修**，不然下次重跑又退回去。
`input,textarea,select{font-size:var(--fs-input)}` **維持全域**——那是 iOS 的 16px 硬性下限。

**4. 原型已同步**（八處改成識別圓底＋名字、S-06 hero 拿掉成員 emoji）。
`:1946`／`:2507` **沒動**——那兩處對應的是 C 類（`memberLabel`／`nm2` 回傳純文字），
實作沒改，動原型反而會製造新的落差。

**5. 假資料補 `?members=noemoji`**　原本四位成員清一色有 emoji，
**第二層從來沒被測到過**——Rozi 反覆回報「沒有填色圓底」而 263 條全綠。

### 量到的數字（`?members=noemoji`，390×844）
s02b／s03／s04×4／s05×2 每一區 `.avatar.letter` 各 4 顆，六色輪替
`rgb(45,106,138)`…；chip 最高 34.5px；就地編輯框 32.0px（原本撐滿整列）；
「加一個人」內頭像 0 個；全站無 🙂。

### 反向驗證
誰付的 chip 改回純文字 2 紅｜統計卡改回純文字 1 紅｜S-05 逐人列改回純文字 1 紅｜
拿掉 `:where()` 1 紅｜「加一個人」加回頭像 2 紅｜拿掉一條 `:where()`（CSS 掃描）2 紅｜
把 iOS 16px 那條也包起來 2 紅。

> **兩條第一版沒紅**：門檻是「整頁 `.avatar.letter` ≥4」，一頁有好幾區都有識別圖，
> 改壞其中一區總數仍過關。改成**分區量測**（`.perlist`／`.detailtable`／各 `.fld`）才紅。
> 另外我這一輪加的 `.chip:has(.avatar)` 規則，量過**有沒有它 chip 都是 34.5px、
> 中心差 0.0 vs 0.8px**——不是它在做事，我的註解一度寫錯。已移除（含原型端），
> 高度是 `.avatar.sm{display:inline-flex}` 修好的。

## 前幾輪（都已上線，細節在 `Claude outputs/_實作B進度.md`）
**B–C** 全站對齊原型＋引擎查證＋版面回歸測試｜**D／E** 複驗退回兩輪｜**F** 推上線｜
**G** 分享頁轉帳列重複 12 次｜**H** 日期欄位沒接上 `.datefield`、移除 G-09 預填成員｜
**I** 五個線上實測 bug：sheet 捲不動、沒選付款人靜默失敗、成員錯誤沒清、
名字沒按「加進來」就送出、`＋` 沒對齊（全域 `.ic` 沒被搬過來）。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`750d857` 實作-A｜`723466a` B＋C｜`8cba1ef` D｜`355a64e` E｜`3e01b02` 修 CI lockfile｜
`f8c00ca` G｜（本次）H。

**前兩次部署失敗是我造成的**：CI 走 `pnpm install --frozen-lockfile`，我卻整輪用 npm，
`pnpm-lock.yaml` 沒跟上。**往後這個 repo 一律用 pnpm，推之前先跑 CI 那兩道指令。**

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

## 212 對帳表
`實作_212對帳表.cjs` **已由 Cowork 於 2026-09-06 作廢**（判定規則會自動蓋章）。
依 `_實作階段運作方式.md`「六之零」，驗收判定改由 Cowork 寫、Cowork 跑，我不再產出對帳表。

## 原型與佇列指令的兩處出入 —— **Rozi 2026-09-05 都已裁示，這兩項已結案**
1. **S-06 hero 的成員 emoji**：裁示「不要 emoji，維持現狀」（與 S-03 一致）。原型待日後同步。
2. **`➕` 當「還沒選 emoji」的預設值**：裁示維持 `➕`（原型是位階 1）。

**留給原型的待辦**（`Tripay_原型.html` 由終端機維護，但這兩處要不要同步請 Cowork 指示）：
S-06 的 `renderS06()` hero 仍接著 `members.map(...).join('')`。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
