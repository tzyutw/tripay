# 停點｜實作-B（B-1～B-7）＋ 實作-C　全部跑完　2026-09-05 22:25

**一次「收」跑完 B-1～B-7 與實作-C，中途沒停。已 commit，未 push**（等 `_可以推了.txt`）。
逐節細節與**每節的反向驗證逐條記錄**在 `Claude outputs/_實作B進度.md`。

vitest **196 綠**（3 skip）｜版面回歸 172｜token 52｜型別對帳 29｜文案唯一性 13｜端到端 2｜
`tsc` 零錯誤｜`vite build` 通過。B 開始前是 92 綠。

## C-1　引擎查證：**沒有改引擎**
`calculate-settlement` 在**查詢階段**就用 `.eq("twd_pending", false)` 濾掉待填的筆，
排在型別驗證之前，所以有標記 pending 的筆不會 422。
422 只來自「`twd_amount is null` 但 `twd_pending = false`」——那正是 S-04-29 要擋的。
盤點表 `:151` 與 `:296` 講的是不同的東西，兩句都對。→ **C-4 四趟 verify 不需要跑。**

## C-2　hub：淨額相同，只有轉帳路徑不同
| 成員 | direct | hub | | 成員 | direct | hub |
|---|---|---|---|---|---|---|
| 🐵 Rozi | 46,180 | 46,180 ✅ | | 🍋 阿明 | −17,740 | −17,740 ✅ |
| 🐱 小美 | −8,220 | −8,220 ✅ | | 🐟 小魚 | −20,220 | −20,220 ✅ |

direct 3 筆都轉給 Rozi／hub（中心人＝小美）小美→Rozi 46,180、阿明→小美、小魚→小美。
`TransferView` 已由 `SettlementPage.tsx` 與 `SharePage.tsx` **兩個檔案**引用。

## C-3　版面回歸（真實 Chrome，10 畫面 × 320／375／414）
掃描前展開：s03 總花費／要排除誰、s03d 清單、s04 各付各的＋要排除誰、s05 計算依據、s06 總花費。
**抓到一個真的版面 bug 並修好**：S-02 日期列 `flex-1` 少 `min-w-0`，原生 date 欄位不肯縮，
320px 下撐出畫面 28px（桌機與 jsdom 都看不到）。斷言有金絲雀（塞 900px 元素 → 兩條都紅）。

## C-5　上線前把關
- **環境地雷全部複查過**，修好三處（`ShareSheet` 少 `createPortal`、幣別搜尋框有 `autoFocus`、
  兩個 DELETE 沒斷言影響列數），並寫成常設測試守著。
- **分享頁端到端（對 production，只讀不寫）**：anon 直接查 `trips` 拿到 **0 列**（013 生效）；
  token 改一個字元 → 回 `null`。**正確 token 那個方向缺一個真實 token**，
  跑 `SHARE_TOKEN=<token> node 實作_分享頁端到端.cjs` 即可補完。
- **`TC-STATUS-18～20` 仍是 skip**：本機沒有 Docker／Supabase CLI。
  同一批規則的 29 項單元測試全綠。**沒有自行安裝 Docker。**

## 兩件要 Rozi 裁示的（原型與佇列指令有出入）
1. **S-06 hero 的成員 emoji**：佇列說拿掉，**原型仍然有**，fixture 也是。目前照佇列做，
   並把這一項釘在 `SharePage.test.tsx` 的 `KNOWN_DIVERGENCE`，沒有藏進「差異為空集合」。
2. **`➕` 當「還沒選 emoji」的預設值**：CLAUDE.md 說佔位符該走 Feather，
   但原型（位階 1）仍輸出 `➕`。維持原型，沒動。

## 這一項要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32 踩過）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
