# 停點報告

> 2026-09-05　狀態：**#25 批三回饋 7 項＋1 項自查全部做完，等 Rozi 看過**

## 交付
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**（同一連結）

## 你問「那個紅色是不是系統配色就這樣」——是，而且比想像中直接
`--dg`（刪除）與 `--out`（提醒）本來是**同一個色碼 #B03A22**。
「刪掉整趟救不回來」跟「這筆金額還沒填」視覺上同一個重量級。
`--dg` 改 **#9B1B14**（白字對比實測 **8.20:1**）。`--out` 一個字沒動。

**跟著變色的畫面共三處**：S-03c-7 刪除鈕（滿版深紅塊）、
S-07-6 登出的文字色、S-03 底部 `.delrow`「刪除這趟行程」的文字色。
S-07-8 登出對話框的登出鈕本來就吃 `.btn.dg`，一併變深。

## 按鈕寬度：你看到兩顆，實際有四顆
`b()` 會在每項外包一層 `div.b`；按鈕列逐顆編號時 flex item 就變成那層 div，
而 `.btn{width:100%}` 是相對它算的，沒給 `flex:1` 就縮成內容寬。
加 `.ui .flexrow > .b{flex:1;min-width:0}` 修好，編號維持一顆鈕一個。
**逆向驗證**：把規則拿掉，測試立刻抓出四筆，數字與 Cowork 實測完全一致——
S-02b-3 / S-02b-4 各 **58px**、S-03c-6 **72px**、S-03c-7 **28px**（應為 129～150px）。

## 摺紙動畫（S-05-16）
原本兩個圖形交叉淡入淡出，**沒有「摺」這個動作**，而且無限迴圈。
改成三拍、總長 1.4s、**只播一次**停在最終畫面：
字線沿摺線收攏 → 帳單本體 `scaleX` 對摺 → 紙飛機描線飛出（加速離開）。
紅虛線本來沒有語意，改成飛行軌跡殘影留在原地淡到 .35。
`reduce` 時直接呈現最終畫面。逐格截圖確認過，不是只看程式碼——也因此
發現飛機飛出去會被 viewBox 切掉，順手把畫布放大到裝得下。
**`.empty-anim` 沒動**（護欄）。

## 其餘
- S-02-20 文案定稿，**全文「佔位／〔／〕」歸零**
- 空名字不得存檔：鈕 disabled ＋ Enter 路徑也擋；打字時只改 `disabled`，不重繪欄位（#13）
- S-02c-12／13 移出手機框，**編號保留、驗證邏輯一字未動**，說明搬到右側清單
- S-03b 三條灰字砍到剩一條（隱私告知），差別做進標題：「文字摘要」對「分享連結」
- S-03c-8 移除；**S-03c-3 的三項清單一字沒動**（那是唯一給你判斷依據的東西）
- 手機框全站不再出現「批一～批四」，S-07 改「即將推出」

## 測試（六組全過，新增一組 45 項）
批一 60｜批二 132｜批三 54｜輸入 34｜版面 18｜**批三回饋 45**（`原型_批三回饋測試.cjs`）
四項修正都做了逆向驗證：改回舊寫法，對應測試就紅。
批三有一條舊斷言寫死「刪除」二字，依 #25-6 更新為「兩個字」。

## 順手發現、沒有自行處理
盤點表的 **S-03c-8 是「失敗訊息」**（線上產品的 ExpenseListPage:203），
原型的 S-03c-8 卻是「不可按的說明」——**同號不同義**。
指令禁止重新編號，所以我只回報不動手。

## 沒做的
未改 `src/`、未跑 migration、未刪檔。
