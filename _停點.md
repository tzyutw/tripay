# 停點報告

> 2026-08-31 03:05（GMT+8）　狀態：**等第三批逐頁清單（S-03 起）**

## 一句話
#9 v3 全數落地並已部署：行程 emoji 全站 7 處清完、字體規則落地、登入頁幽靈卡移除。

## 一、部署（純前端，照部署授權自動處理）
**改了什麼**
- **A 行程 emoji 退場（7 處）**：顯示端 `TripListPage:119`／`SharePage:159`；
  寫入端 `TripFormSheet` 的初值／編輯 update／建立 insert；UI 端封面按鈕與 picker 傳值。
  EmojiPicker 改為只走 member 模式
- **E 幽靈卡**：`LoginPage` 移除；`TripListPage` 保留（P-2）
- **F 字體規則**：`index.html` 的 Google Fonts 載入整組移除（Cormorant Garamond＋Noto Sans TC）；
  `tailwind.config` 的 `sans` 改 PingFang TC 優先、`serif` 家族移除；`font-serif` 8 處全清；
  `TripListPage:134` 日期上的 `tabular-nums` 清除

**驗證結果**：tsc + vite build 通過；vitest 57 passed / 3 skipped；
`trip.emoji`／`coverEmoji`／`font-serif`／`fonts.googleapis` 全站各 **0 筆**；
`tabular-nums` 剩 **16 處全為金額**；「你的下一趟在哪」剩 **1 處**（首頁）。

**Rollback**：`git revert` 本次 commit 後推送，無 schema／資料異動。

## 二、順帶做的一件小事（可推翻）
serif 家族撤掉後，原本套在中文上的 `italic` 會變成**合成假斜體**——
這正是既有決策「中文不用假斜體」要避免的。所以把 slogan 等 3 處的 `italic` 一併清掉。
不想要就把 `italic` 加回去即可。

## 三、稿上同步（artifact 已更新）
清掉 S-01 三張卡與 S-06-3 的行程 emoji；新增 **S-01-14** 記錄 7 處清除點。
**明確保留且已確認未動**：行程內頁 hero 成員列、分享頁成員與付款人、
消費列的類別 emoji（`category_emoji`）、盤點表裡的移除紀錄敘述。

## 四、S-03E 矛盾已依裁示①收斂
決策 D 由行程頁 hero 滿足，空狀態不新增成員列。已寫進決策紀錄與 `_稿碼對照.md` §7。

## 五、一個要讓你知道的細節
`trips.emoji` 的 DB 預設是 `'✈️'`（NOT NULL DEFAULT）。前端已不再寫入，
所以**新行程仍會由 DB 預設填入 `'✈️'`**——不顯示、不影響任何功能。
要讓新資料存空字串需要一條 migration（作法同 009）。**這是 schema 變更，我沒自行做。**
要或不要，第三批一起說。

## 六、文件
`_稿碼對照.md` **轉正式**（7 處已全數標 ✅）｜`_盤點_畫面功能.md` 重出 246 列｜
`專案狀態.md` 補 6 條決策｜`決策符合性檢查表.md` v1.5 補 #54–59（**5 條可 grep**，累計 59 條）｜
`_實作批次工作清單.md` 補 G-05 全清項｜`CLAUDE.md` 守則補「移除顯示須連同寫入端一起清」
