# 停點報告

> 2026-09-05　狀態：**#26 現況盤點完成（唯讀），#27 留待下一次「收」**

先講為什麼只做一節：**#26 要求做完後 `git status` 只有一個新檔，
#27 要改 `Tripay_原型.html`——兩者的護欄互斥，先做唯讀的那個。**

## 產出
`_盤點_實作缺口.md`（327 行）。**沒有動任何程式。**

## 第五段：分類統計（母體從原型現算，不是寫死的 206）

| 分類 | 項數 |
|---|---:|
| 已如此（不用動）| **92** |
| 要改 | **57** |
| 全新 | **34** |
| 要移除 | **23** |
| 合計 | 206 ✓ |

schema 缺口 **6 項**（全部是加欄位，都不影響既有資料）。
工作清單：已完成 6 ／ 仍待做 21 ／ 已作廢 3。問題清單：已修 6 ／ 仍在 6 ／ 不適用 2。

## 最意外的三項

**一、你們懷疑的四處過期，實際上是五處，而且有一組互相打架。**
工作清單第 13 項要「複用既有 EmojiPicker」，同一份檔案的 v5 第二批卻裁示
**刪掉 `EmojiPicker.tsx`**。兩條同時執行不可能，實作前得先定哪一個。

**二、指令說「已知會落在已作廢的至少有四項」——那四項早就不在檔案裡了。**
切換成員視角、成員分佈 chips、匯入 UI、G-04 OG 圖，`_實作批次工作清單.md`
現在一個都找不到。不需要回填，反而是**工作清單第 9 項該作廢卻還留著**：
它要「沒標『這是我』時顯示提示」，但稿上 S-03-11「我的花費」整格已經移除。

**三、`src/types/database.ts` 落後資料庫四個 migration。**
`Trip` 少了 `cover_path`(008)、`kind`／`card_id`(007)、
`settlement_mode`／`hub_member_id`(010)，`TripMember` 少了 `person_id`(006)。
`TripListPage.tsx:37` **已經在查 `kind` 了**，靠的是型別沒擋住它。
不影響現在跑的東西，但實作 hub 結算時第一個會踩到。

## 一個要你知道的產品事實
**唯一會靜默算錯帳的仍然在**：各自金額欄沒有任何幣別標示
（`ExpenseFormSheet.tsx:451`）。在日幣行程照當地金額填，會被當台幣算進結算，
不報錯。修法要加一個欄位，已列在第二段第一列。

## 停止條件（機器判斷，九條全過）
列數＝母體、分類四選一無空白、現況位置無空白、「要改」都有寫差在哪、
統計與實際相符、工作區只有那一個新檔。**檢查腳本沒留在 repo**——
指令說唯一允許新增的檔案是盤點檔本身。既有六組測試全綠：60／132／54／34／18／45。

## 沒做的
未改 `src/`、未改 `supabase/`、未跑 migration、未部署、未刪檔，
也未改那兩份既有清單本身（指令要求只在盤點檔記錄結論）。
