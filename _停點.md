# 停點｜實作-B～G（**已上線**）　2026-09-05 23:32

## 實作-G　修掉上線後複驗抓到的 regression
**分享頁把「誰付給誰」重複列了 12 次**（線上北海道那趟有 12 筆結算：
10 superseded ＋ 1 confirmed ＋ 1 draft，各 3 個 item → 36 列）。

**根因是我做的**：改版前的 `SharePage` 有
`.eq('status','confirmed').order('created_at', desc)`，我把它重寫成走 RPC 時
**那層篩選弄丟了**——`get_shared_trip()` 回的是這趟**全部**結算，我照單全收。
`SettlementPage` 到現在都還有挑，所以兩頁行為不一致。

**修法（只動 `SharePage.tsx` 這一處）**：抽出 `pickConfirmed()`——只認 `confirmed`，
多筆取 `settled_at` 最新（null 視為最舊）；再只取該次的 items。挑不到就走前端預覽。
測試**比列數**不是比字串（重複時字串照樣在），三條反向驗證全紅。

## 🚀 已上線　`c23dcf2..HEAD` → https://tzyutw.github.io/tripay/

| commit | 內容 |
|---|---|
| `750d857` | 實作-A 型別對齊、全域 token、schema 012／013、分享權限漏洞 |
| `723466a` | 實作-B（B-1～B-7）＋ 實作-C |
| `8cba1ef` | 實作-D 複驗退回三項 |
| `355a64e` | 實作-E 對帳表 4 列「未做」收掉 |
| `ff2a8fc` `46fa331` | 決策紀錄與收尾文件 |
| `3e01b02` | **修 CI：pnpm-lock.yaml** |
| （本次） | **實作-G 分享頁只畫 confirmed 那一次** |

### ⚠️ 前兩次部署失敗——我自己造成的
CI 走 `pnpm install --frozen-lockfile`，我整輪卻用 **npm** 裝套件，
`pnpm-lock.yaml` 沒跟上（6 個新相依）。用與 CI 同版的 pnpm 11 重產 lockfile，
本機把 CI 兩道指令完整跑過才推。**往後這個 repo 一律用 pnpm。**
`Claude outputs/_cowork暫存_可刪/` 已刪（含 anon key 的 `_e2e.env` 未進 commit）。

vitest **215 綠**（3 skip）｜版面回歸 172｜token 52｜型別對帳 29｜文案唯一性 13｜
212 交叉檢查 13｜`tsc` 零錯誤｜`pnpm run build` 通過。B 開始前是 92 綠。

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

## ✅ 212 對帳表：**已完成 212／不適用 0／未做 0** —— 停止條件 4 過了

## 各節摘要（細節在 `Claude outputs/_實作B進度.md`）
**B-1～B-7** S-00～S-07 全站對齊原型，每頁逐字比對 fixture｜
**C** 引擎查證後沒改引擎（四趟 verify 不必跑）、hub 淨額與 direct 相同、
新增真實 Chrome 版面回歸（抓到 S-02 少 `min-w-0` 的 28px 溢出）、環境地雷修三處｜
**D** 兩個刪除對話框疊在一起、S-03b 三筆文案、212 對帳表改由程式產生｜
**E** S-03c-2／3／4／7 對齊原型、S-02c-10 三層 fallback 收進 `shared/Avatar.tsx`｜
**F** 推上線｜**G** 分享頁轉帳列重複。

## 原型與佇列指令的兩處出入 —— **Rozi 2026-09-05 都已裁示，這兩項已結案**
1. **S-06 hero 的成員 emoji**：裁示「不要 emoji，維持現狀」（與 S-03 一致）。原型待日後同步。
2. **`➕` 當「還沒選 emoji」的預設值**：裁示維持 `➕`（原型是位階 1）。

**留給原型的待辦**（`Tripay_原型.html` 由終端機維護，但這兩處要不要同步請 Cowork 指示）：
S-06 的 `renderS06()` hero 仍接著 `members.map(...).join('')`。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
