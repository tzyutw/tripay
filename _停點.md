# 停點報告

> 2026-08-31 01:20（GMT+8）　狀態：**等第二批逐頁清單（S-02 起）＋三個提案裁示＋一個確認**

## 一句話
佇列 #3/#4/#5 全數處理：hotfix 已上線驗證、v5 第一批調整已更新、協定與盤點落檔完成。

## 一、線上 hotfix（#2 #3）已部署驗證
commit `0d2fd34` → Actions success → **production bundle 實測**：
`origin}/tripay/share/` 已出現，舊死鏈樣態 0 筆，寫死「4 人」0 筆。
tsc + vite build 通過；vitest 57 passed / 3 skipped。
**Rollback**：`git revert 0d2fd34` 後推送，無 schema 或資料異動。

## 二、v5 第一批調整（同一個 artifact，URL 未變）
2facaecf-d131-41e2-8048-c58fe7a37e2c
- 移除 S-00-1（App icon）、S-00-6（幽靈卡）、S-01-9＋S-01-10（分享橫幅）
- **編號不重排**：移除項留在盤點表標刪除線，號碼不回收
- **S-01-12 的答案**：它是「看不見的行為」（列表過濾 `.eq('kind','trip')`），沒有視覺區塊。
  全稿共 18 列這類項目，已全部加註「不在稿上」記號，日後不會再對不上
- 新增 S-05-28「暫時算一下」唯讀預覽（#14 定案）
- S-01-11 維持不動

## 三、三個提案（P-1～P-3，全部待裁示，未落地）
- **P-1 登入頁識別**：移除 icon 後由字標＋slogan 承擔即可，**不補任何圖形**。
  日後要 logo，收據摺紙（S-05-16）是現成語彙
- **P-2 幽靈卡**：放 S-00 不合理（不可點卻長得可點、稀釋唯一動作、未登入者沒有「上一趟」）。
  **移除即可、不需搬家**——S-01-8 是同一元件且在那裡可點
- **P-3 分享動線**：問題不在「有沒有入口」而在「時機」。
  提案：① S-03-5 常駐 ↑ 不動 ② **S-05 結算完成頁加分享 CTA**（目前完全沒有，
  但那才是答案剛產生的一刻）③ G-05 觸發從「記了第 1 筆」改為「結算後未分享」或整個拿掉

## 四、⚠️ 一個要你確認才能動的
指令寫「S-01-9 與 **S-01-4** 移除（分享入口常駐但對象不明）」。
S-01-9／S-01-10 已移除，但 **S-01-4 是首頁空狀態**，與分享無關；移除會讓新使用者首頁全白，
且與讀碼發現 #11（空狀態該補 CTA）方向相反。**判斷是 S-01-10 的筆誤，先保留未動。**

## 五、協定與落檔（#3 #5A）
- `CLAUDE.md` 93 行：補佇列衛生（一節一事、整節替換、已核可）、提案不逕行執行、
  停點 ≤50 行、大型盤點必須落檔並記 commit
- `_盤點_畫面功能.md`（327 行）：234 列盤點索引，記錄盤點 commit `e976ffe`
  與 hotfix 造成的兩處差異；重新盤點先 `git diff` 只讀變動檔
- `_實作批次工作清單.md`：#4–#13 逐條建議＋對應編號、#14 定案、第二批範圍、待裁示清單

## 六、我沒有做的事
未動 S-02 起的稿；三個提案未落地；未改任何 src（hotfix 兩處除外）；未動 production 資料。
