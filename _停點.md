# 停點｜實作-Q（一次「收」跑完，**已上線**）　2026-09-06 23:55
需求↔斷言對照在 `_需求台帳.md`；歷輪在 `Claude outputs/_實作B進度.md`。

## migration 015（Cowork 已套 production）
`expenses.twd_from_rate` 已存在。空跑符合標記條件 **1 筆**（2155測試 實作OP／藥妝，
308500 → 1469048），實套後 `twd_from_rate = true` 共 **1 筆 / 411 筆**，
**沒有任何金額被改動**——真正的重算由 App 在她按下「儲存匯率」時走正常寫入路徑做。

## Q-1　匯率改成一個空格＋系統判方向（Rozi 拍板方案 C）
根因：實作-O 的「填一邊、另一邊自動帶 1」讓使用者可以組出**方向相反**的兩個數字，
系統無從分辨。她想的是「1 日幣 = 0.21 台幣」，系統讀成「1 台幣 = 0.21 日幣」，
308500 日圓存成 **1,469,048 台幣**（正確 64,785），`twd_pending=false`——
錯的值被當成確定值寫進資料庫，全程沒有警告。

- **一個輸入框**，placeholder 給該幣別的量級範例（由 `TWD_PER_UNIT` 推，沒有寫死表）
- **判方向跟幣別量級比，不是跟 1 比**：她的「小於 1 用乘、大於 1 用除」在日韓泰越印菲印度
  都對，但美元 32／歐元 35／港幣 4.1 會反。改成在對數尺度上比 `TWD_PER_UNIT`
- **白話那一行**把系統的理解攤開（「1 日圓 ＝ 0.21 台幣」），旁邊一顆「換個方向」——
  判錯要救得回來，只顯示不給救跟不顯示一樣糟
- 按過「換個方向」之後再改數字**不會被自動判定翻回去**（不然使用者救不回來）
- schema 不動、`tripRate()` 不動：方向組成 `trips` 既有的兩欄
- **Q-1d 重算**：新增 `twd_from_rate`。改匯率時「沒台幣的補算」＋「系統算的重算」，
  **使用者手打的一筆都不准動**。判斷只看這一欄，不用 `exchange_rate` 有沒有值來猜

## Q-2　金額千分位
`src/lib/amount.ts`：`parseAmount`／`formatAmount`／`caretAfterFormat`。
**存檔前剝逗號**是最容易漏的一步（`Number('308,500')` 回 NaN，帳直接壞掉且不警告）——
所以剝離與格式化寫在同一個檔、同一組測試守著。游標保位：算出左側有幾個數字再定位，
不然每打一個字就跳到最後。匯率那一欄**不加**千分位。
`AmountInput` 定義在**模組層級**——定義在元件內部會讓 React 每次都當成新型別、
重建 input、iOS 鍵盤收起來（`CashRate` 咬過一次）。

## Q-3　「先去看一下」去錯地方（不是壞掉）
它有反應，只是回到整個消費列表，而她本來就是從那裡過來的。
警示層那一列 → **直接開那一筆的編輯表單**；「先去看一下」→ **「還沒算清楚」的篩選清單**。
兩個都走 query（`?expense=`／`?unsettled=`），不用 state——理由與實作-O-8 相同：
跨 route 的 state 會被丟掉。

## Q-4　App 圖示換色沒生效
兩個問題：① manifest 的 `icons[].src` 寫死 `/pwa-icon.svg`，網站掛在 `/tripay/` 底下，
**一直都是 404**（舊有的錯，不是換色造成的）；② 沒有 `apple-touch-icon`，
而 iOS 不吃 SVG、又用 URL 當快取鍵，所以刪掉重加還是舊圖。
改：icon 路徑帶 base、加 PNG（192／512／180），檔名帶 `-v2` 逼 iOS 重抓。
PNG 用 `實作_圖示產生.cjs` 從同一張 SVG 渲染出來，**沒有手繪、沒有用線上工具**。

## 驗證
`tsc` 0 錯｜`vitest` **305 綠**無 Unhandled｜`pnpm run build` 過。
版面回歸 1163｜匯率與千分位 **42**｜寫入與更新 61｜高度階梯與圖示 50｜輸入框與選單 53｜
查不到與把手 58｜成員識別 79｜原型真實形狀 50｜資料形狀 9｜token 52｜型別對帳 29｜文案唯一性 13。
**六組反向驗證全部紅過**，逐條列在 `_需求台帳.md`。

> ⚠️ **修掉一個假資料造成的假通過**：`?rate=full` 原本是 `twd:1, for:0.21`，
> 那對 KRW 是對的、**對 JPY 是反的**——所以舊的端到端期望值 `$ 23,810` 其實是
> 方向反了才會出現的數字。假資料改成「照該幣別正確的方向」組出來，
> 期望值一律**由這一趟真正的兩欄算出來**，不寫死。

## 要 Rozi 在手機上看過才算數
1. 匯率只剩一格、下面那行白話讀起來對不對；「換個方向」按得到嗎
2. 金額打字時游標會不會亂跳（真機輸入法與 Chrome 不同）
3. **App 圖示要先刪掉主畫面舊圖示、重新「加入主畫面」一次**——檔名已經帶 `-v2`，
   這次應該會換到新的藍色
4. 千分位在 iOS 數字鍵盤下打起來順不順

## 順手看到但沒動
- **原型的 S-02／S-02b 欄位一致（實作-O-5）還沒補**——這一輪只同步了匯率那一塊
  （`rateRows` 是自足的函式，改動可控）。欄位一致要動五個有狀態的 handler ＋
  拖曳排序寫死 `#scr-s02b`，還有 15 支 `原型_*.cjs` 綁著那些 id。**請 Rozi 決定要不要開一節。**
- production 還有 19 筆「有外幣沒台幣」的消費散在各趟；migration 015 只做標記，
  依裁示不重算，等她在該趟按下儲存匯率時由 App 補。
- 既有的紅（與這一輪無關，已對改動前的原型複核過，數字相同）：
  `原型_字級與真機測試` 3｜`原型_貼邊與結構測試` 5｜`原型_預設日期與欄位列測試` 1｜
  `原型_版面與拖曳測試` 2｜`原型_焦點與圓角測試` crash。

## 寫入路徑對 production 實跑再 rollback（只動 `ZZ 測試行程`，0 殘留）
① 記一筆（只填外幣、有匯率）→ `twd_amount 64785`／`twd_pending false`／`twd_from_rate true`
② 儲存匯率時的重算 upsert…returning →
   「ZZ 系統算的」1,469,048 → **64,785**（重算）｜
   「ZZ 手打的」60,000 → **完全沒出現在結果裡**（一個欄位都沒動）｜
   藥妝店／午餐（沒台幣）→ 630／252（補算）
跑完 `ZZ %` 殘留 0 筆、`twd_from_rate` 仍是 1 筆 / 411 筆。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`8c26d94`，線上 bundle `index-CVpVfTJG.js` 含本次 commit 短碼與「換個方向」。
三張 PNG 與 manifest 線上都是 **200**（先前 manifest 的 icon 是 404）。
