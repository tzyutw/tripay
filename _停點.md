# 停點　2026-09-07　實作-Z（收合條沒真的變藍／收合後名字不見／首頁抖動／開關太大）

## Z-1　收合條畫出來真的是藍的
- 根因：Y-2 只改了 `background-color`，**漸層是 `background-image`、蓋在它上面**，
  使用者看到的還是綠漸層。Cowork 的斷言也只驗到被蓋住的那一層。
- 另一半是 `.sc` 那層暗色遮罩：它讓收合條量到 `rgb(16,103,171)` 而不是 `rgb(18,118,196)`。
  收合態關掉它——白字壓 `#1276C4` 的對比**實算 4.76:1**（過 AA 的 4.5），不需要遮罩。
- 斷言改成**讀像素**：扣掉三個控制項的矩形之後，3,262 個取樣點**沒有一個**不是 `#1276C4`。

## Z-2　行程名不見＋首頁抖動（同一個根因）
- **拿掉 `animation-timeline: scroll()`**（`src/index.css` 現在 0 處），改成
  `IntersectionObserver` 監看哨兵、越過門檻切 `.is-compact`，過渡交給 CSS `transition`。
- **安全區從 sticky 元素的 padding 搬出去**：`.topbarwrap` / `.herowrap` 承擔留白與塗色，
  `.topbar` / `.hero` 高度固定。實測 `--sat` 59→0→59 三次，兩者高度都是 **56 / 56 / 56**。
- 收合態的行程名不再靠 opacity 動畫：`opacity` 量到 **1**，147×26，中心點命中自己。
- ⚠️ 踩到兩個坑，都寫在測試檔的註解裡：
  ① **`useRef` ＋ `useEffect([])` 掛不上觀察器**——這一頁在資料回來前先 `return <spin>`，
     那時哨兵還沒進 DOM，而空依賴的 effect 不會再跑一次。改用 callback ref。
  ② **開第二個分頁解 PNG，被量的那一頁就變背景分頁**，Chrome 把 IO 節流掉，
     `is-compact` 永遠不切——量到的「沒收合」是分頁被凍住，不是程式壞掉。改在 Node 裡解 PNG。

## Z-3　開關瘦身移位
- `.swrow` 整列移除，開關進「消費紀錄 · N 筆」那一行的右邊（同一行，top 差 0）。
- 13px／600，與標頭同級同粗；底色 alpha 0（不填色）；開啟時只有文字變 `#1276C4`。
- 可點區靠透明 `::after` 撐到 **78×44**，看得見的字沒有放大。
- s03 頁高 1697 → **1632**，差 65 —— 剛好還原成加那一列之前的值。

## 驗證
- `實作_收合與開關瘦身測試.cjs` **38 項全綠**；金絲雀：關掉 `.sc` 遮罩紅 4、
  行程名 opacity 紅 1、安全區搬回 padding 紅 1、開關搬回獨立一列紅 4。
- ⚠️ **Z-1 有兩道各自都夠的防線**（CSS 的 `background-image:none !important` 與
  JSX 收合時不給 inline 漸層），所以單獨拿掉任一道**斷言都還是綠的**，
  兩道都拿掉才紅（紅 5）。留兩道是因為 inline 那一端最容易在改版時被順手加回來。
- 22 支回歸全綠、2,297 項：版面 1163、這個人的帳 118、時間差 88、安全區 70、
  約與分享頁 63、可點區 62、只看共同的帳 52…。`tsc` 0 錯、`vitest` 325 綠、`npm run build` 過。

## 我改了斷言的地方（都是結構換了，不是為了讓它綠）
1. `verify_V` 12～20 與 `verify_Y` 5～12：黏住／收合的元素從 `.topbar`／`.hero`
   變成 `.topbarwrap`／`.herowrap`，斷言跟著指過去，**驗的東西一個字沒改**
   （還黏在頂端、底色不透明、不蓋住內容、鍵仍 44×44、三個寬度都不橫向捲）。
   `verify_V` 原本還會**整支崩掉**（`bar.parentElement.children[1]` 變 undefined）。
2. `verify_Y` 拿掉「收合態 `::after` 遮罩」那兩條——遮罩是捲動驅動動畫的產物，已經沒有了；
   「畫出來是不是藍的」改由 Z 的讀像素那幾條驗（那才是原本漏掉的地方）。
3. `verify_X` 的可點區改成含 `::after`：Z-3 之後看得見的字只有 13px 高，
   只量 border box 會誤判成「按不到」。
4. 基準兩處：s03 高度 1697→1632、文字段數 95→96（`.navttl` 讓行程名多一個節點，內容沒新增）。

## 機器驗不到，只有 Rozi 在手機上才看得到
1. **抖動有沒有真的消失**——無頭 Chrome 沒有會伸縮的網址列，這一項只能真機看。
2. 收合／展開的過渡是不是順（改成 0.18s 的 CSS transition，不再跟著手指走）。
3. 收合後的純藍配白字讀不讀得舒服（實算 4.76:1，但那是數字不是感受）。

## 順手看到但沒動
1. **原型的 hero 沒有真的會收合**——原型是一頁十幾個畫面，沒有各自的捲動容器，
   `.is-compact` 在原型上切不起來。CSS 與結構已經同步，但要在原型看到收合態，
   得先幫原型做逐畫面的捲動容器。
2. 原型的「{名字} 的帳」仍停在改版前（S-03d 那個篩選頁），與上一節記的同一件。
3. `per[]` 對贊助的處理（那個固定 25,000 的落差）仍等她拍板。
