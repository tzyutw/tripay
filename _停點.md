# 停點｜實作-B～L（**已上線**）　2026-09-06 15:10

## 實作-L　Rozi 手機實測四項

### 1. 🔴 回歸：「新增一種支付方式」從 39px 塌成 26px
實作-J 把 `input[type=text]{width;background;padding;…}` **整條**包進 `:where()`，
特異度降到 (0,0,0) → **連 Tailwind preflight 的 `input{padding:0}`（0,0,1）都蓋得過它**。
拆成兩條：`width` 留在 `:where()`（0,0,0），外觀那半寫成
`input:where([type=…])`＝**(0,0,1)**。

> ⚠️ 不能照指令寫成 `input[type=…]`＝(0,1,1)——**那會讓匯率欄從 38px 變 44px**。
> 原型裡是 `.ui input[…]`(0,1,1) 對 `.ui .rateinput`(0,2,0)，class 本來就贏；
> 搬過來拿掉 `.ui` 之後大小關係反過來了。(0,0,1) 才同時滿足
> 「贏 preflight、輸 class」。
>
> 另外 preflight 給輸入框 `line-height: inherit`（1.5 → 內容 24px），
> 原型沒有 preflight、用瀏覽器預設的 `normal`（≈19px）——差這 5px 就是 44 vs 39。
> 補一條只給**沒有自己 class 且不在 `.fieldrow` 內**的輸入框。

量到：支付方式 **41px／padding 9px**、`.rateinput` **38**、S-02 **46**、S-04 **28**——
後三種依指令不得被動到，確認都沒動。

### 2. 🔴 匯率欄每打一字鍵盤就收起來
`CashRate` 把 `Row` 定義在**元件內部** → 每次 render 都是新的函式參考 →
React 當成不同元件型別 → 卸載重掛 → input 被銷毀重建 → 失焦。
搬到模組層級。量到：連打 `0`.`1``9` 四鍵**失焦 0 次**、值 `0.19`、
`isSameNode` 為 true（比節點不比 id——重建後 id 一樣）。
反向驗證搬回去 → **失焦 4 次**，完全重現「0.19 要點四次」。

### 3. S-02b-14 編輯行程可改名稱（設計變更）
與 S-02「去哪？」同一款欄位、同一套驗證，`h-[46px]`。**只加名稱**，
幣別與日期不加。原型同步（含編號清單與 input handler）。

### 4. ⋯ 改成獨立頁面（設計變更）
route `/trips/:id/more`，用 route 不用 state——裝置返回鍵自然回到 S-03。
左上返回鍵＋標題「這趟行程」，**底部不再有「取消」**。
四個項目的文字／樣式／編號沒動，刪除仍是唯一著色。原型同步並加 `s03more` 一格。

**CLAUDE.md 補兩條教訓**：`:where()` 降權重前先看 preflight／不要在元件內部定義函式元件。

### 反向驗證（五條全紅）
padding 整條包回 `:where()`（支付方式 **23px／padding 0**）｜`width` 移出 `:where()`｜
外觀寫成 (0,1,1)（匯率欄變 44）｜`RateRow` 搬回元件內部（失焦 4 次）｜留著 `.scrim`／`.sheet`。

### 測試
vitest **244 綠**、無 Unhandled Errors｜版面回歸 1163｜輸入框與選單 32｜成員識別 79｜
原型真實形狀 50｜資料形狀 9｜token 52｜型別對帳 29｜文案唯一性 13。

## 前幾輪（都已上線，細節在 `Claude outputs/_實作B進度.md`）
**B–C** 全站對齊原型＋引擎查證＋版面回歸測試｜**D／E** 複驗退回兩輪｜**F** 推上線｜
**G** 分享頁轉帳列重複 12 次｜**H** 日期欄位沒接上 `.datefield`、移除 G-09 預填成員｜
**I** 五個線上實測 bug｜**J** 成員識別全站貫徹（＋第一輪複驗退回）｜
**K** 假資料換成真實形狀（`?members=noemoji`／`?expenses=none`／`?rate=half`／`?pays=none`）。

**真名外洩處置**：查證 `Tripay_原型.html` 的真名**從未被 commit、也從未 push**
（`git log --all -S` 0 個 commit、`origin/main` 上是 `Alex/Robin/Sam/Kai`）。
`.gitignore` 那幾行已移除——原型本來就在版控裡。
散在其他歷史文件裡的真名屬於改寫已推出的歷史＝不可逆，**已交 Rozi 決定**。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`750d857` 實作-A｜`723466a` B＋C｜`8cba1ef` D｜`355a64e` E｜`3e01b02` 修 CI lockfile｜
`f8c00ca` G｜（本次）H。

**前兩次部署失敗是我造成的**：CI 走 `pnpm install --frozen-lockfile`，我卻整輪用 npm，
`pnpm-lock.yaml` 沒跟上。**往後這個 repo 一律用 pnpm，推之前先跑 CI 那兩道指令。**

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

## 已結案的舊事
`實作_212對帳表.cjs` 已由 Cowork 於 2026-09-06 作廢（判定規則會自動蓋章），
驗收判定改由 Cowork 寫、Cowork 跑（`_實作階段運作方式.md`「六之零」）。
S-06 hero 不帶成員 emoji、`➕` 維持原型——Rozi 2026-09-05 都已裁示。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
