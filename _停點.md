# 停點　2026-09-08　實作-AC（計算依據併進每人卡片；分享頁同步；差額不吃舊結算）

## AC-1／AC-2／AC-3　每人一張卡
- 四欄表（成員／實際付出／應分攤／差額）整張移除，展開後每位成員一張卡。
- 四行固定順序：**他幫大家先付的 → 一起分的 → 各付各的 →（分隔線）→ 差額 → 自己買給自己的**。
  三行**真的加得起來**（`fronted − shared − each = diff`，四人逐一驗過，差額加總 0）。
- 「他先付出去的」改名「他幫大家先付的」，而且**只算代墊**：
  Robin 從 1,895 變成 **1,035**（差的 860 是他自己買的紀念品）。
  `data-paid` 保留原意，恆等式 `fronted = paid − self` 也有斷言。
- 「自己買給自己的」放在差額下面、灰字縮排、右側標「不進結算」；
  原本那句「這些你自己付、也算你自己，不影響要轉的錢」整句拿掉。
- 負零不出現、收合狀態的畫面與 pre-AC **逐字相同**（基準存在 `Claude outputs/_pre-AC基準.json`）。

## AC-4　差額一律即時
- 卡片的「要給出／可以拿回」與差額**一律用即時值**，不吃 `netFromItems`——
  Rozi 看到的兩個數字打架（$24,192 vs $24,209）就是這個。
- 「大家給 X」與卡片裡的「給 X $…」在已結算時仍用凍結值（那是真的要照著轉的金額）。
- 已結算後帳又被改過 → 出現提示「這次結算之後帳有變動，數字跟現在的帳不一樣了」，
  指向既有的「重新計算」，沒有新做按鈕。

## AC-5　分享頁
- 抽出 `src/components/shared/SettleBreakdown.tsx`，**兩頁同一支**
  （斷言驗「他幫大家先付的」這串字在整個 repo 只存在於那一個檔案，且兩頁都 import 它）。
- 分享頁的計算依據區塊裡 `button`／`input`／`a` 皆為 0。

## 驗證
- `實作_每人卡片測試.cjs` **119 項全綠**。
  金絲雀：AC-2 扣除拿掉紅 4、卡片 net 接回 `netFromItems` 紅 4、
  自己買的併回算式紅 5、負零紅 1、分享頁另寫一套紅 3。
- 25 支回歸全綠。`vitest` 340 綠、`tsc` 0 錯、`npm run build` 過。

## 三個「靶沒對上就等於沒驗」
1. **「卡片標題與差額一致」要在 `?state=stale` 那一頁驗**。只在未結算頁驗的話，
   把 `net` 接回 `netFromItems`（＝Rozi 抱怨的那個 bug）照樣全綠——
   未結算根本沒有凍結值，兩者本來就相同。
2. **四行標籤的比對要排除「差額」**：它不是四行之一，是三行的結果。
   不排除就永遠紅，而紅的原因跟實作無關。
3. **`data-paid` 我一度沒輸出**，`fronted = paid − self` 那條讀到 null 也不會紅
   （`null` 進 `Number()` 是 0）。補上輸出並改成明確斷言。

## 我改了既有斷言／基準的地方（都是 AC 換掉了舊呈現）
1. `verify_九項`／`verify_這個人的帳`：`data-due` 不再輸出，改驗卡片印出來的算式
   與 `fronted = paid − self`；四行的名字與順序跟著 AC 改。
2. `verify_AB`（計算依據四行）：整支改成 AC 的順序與「不進結算」標籤。
3. `verify_成員識別`：原本有一列指 `.detailtable`，那個容器不存在了。
   **整列移除**而不是改選擇器——AC 把「人話淨額」與「對帳表」合併成同一張卡，
   改選擇器會變成同一件事驗兩次。
4. 基準：s06 高度 1419 → **1464**（分享頁多了一顆收合的「查看計算依據」）。
5. `SettlementPage.test.tsx` 的原型比對排除清單再加 14 段（四欄表那些字）——
   **AC 的白名單一樣沒有 `Tripay_原型.html`**，原型還停在四欄表那一版。

## 白名單外動到一個檔（要說明）
`src/test/harness/installStub.ts` 加了一行：`?state=stale` 也算已結算。
不加的話 `?state=stale` 會停在未結算態，**AC-4 的第 11／13 條就驗不到東西**。
一行、只動 `settled` 的判斷，沒有碰別的。

## 順手看到但沒動
1. **原型沒有同步**（白名單沒列），S-05 還是四欄表那一版；`screens.json` 也是。
   排除清單累積到 16 段了，同步那一節要一次清掉。
2. 假資料「藥妝店」讓 Robin 的「各付各的」是 −10,965（台幣總額 1,035，兩人卻填 12,000／18,000）。
3. `?fill=remainder` 外幣填 88 vs 台幣填 89、`per[]` 對贊助的處理——兩件仍等裁示。

## 機器驗不到，只有 Rozi 在手機上才看得到
1. 四行＋分隔線＋差額＋自己買那一行，四張卡疊起來會不會太長。
2. 「不進結算」那個小標籤看不看得懂（比原本整句灰字短很多）。
3. stale 提示出現時，她會不會知道要去按「重新計算」。

---

# 追加　2026-09-08　Cowork 複驗退回的四項（`_要修的.md`）已處理　**已 push**

> ✅ Cowork 複驗通過（`_可以推了.txt`，19／19 綠、C8 守恆 0 誤差），
> 2026-09-08 已 push：`d1c2bcf..567dd09`（含 Cowork 的複驗紀錄 commit）。
> `_可以推了.txt` 已改名到 `Claude outputs/_可以推了_已推.txt`。

## 1　贊助獨立成「贊助折抵」一行
- `is_sponsor` 的筆從「一起分的」拆出來，只在**這趟有贊助時**才出現那一行。
- 恆等式改成 `fronted − shared − each + sponsor = diff`，四人差額加總 0（驗過）。

## 2　卡片裡的敘述句只寫名字
- `給 {名字} $…`／`{名字} 給你 $…` 不再用 `memberLabel()`——它在沒有 emoji 時
  退回名字第一個字，於是印成「N NING」（Rozi 的原話）。卡片標題那一行仍保留識別圖。

## 3　負號疊兩層
- `ExpenseGroups` 與 `MemberLedger` 的贊助金額改成**先取絕對值再決定符號**。
  五個畫面全掃過，`−$ -` 與 `−$ 0` 都是 0 處。

## 4　假資料的贊助改成負數
- `爸爸贊助` 從 `+50000` 改成 **`-50000`**（與 production 同向）。

## 🔴 一件我自己決定的事，要請你們確認
拆出贊助之後，**只算「他被抵掉的那一份」帳會少 50,000**——四人差額加總變成 50,000
而不是 0（實測）。原因是**收下贊助的那個人**那一側沒有被算進去。
所以「贊助折抵」這一行吃兩件事：他被抵掉的那一份（正）＋ 他若是收款人，
那筆替大家收的錢（負）。Alex 因此顯示 **−$ 37,500**（＝ +12,500 − 50,000），
其餘三人 **+$ 12,500**。四人加總回到 0。

指令說「金額顯示成正數並帶 `+`」——那是站在**受益者**的角度寫的，
對收款人會印成 `+$ -37,500`。所以我改成：**正的帶 `+`、負的帶 `−`**。
**如果「收款人那一側不該出現在這一行」，請告訴我要放哪裡**，我沒有動任何計算。

## 指令的數字與實測不一致一處
`?screen=s05` 的 Alex「他幫大家先付的」指令寫 **78,900**，實測 **28,900**。
78,900 是「贊助還是 +50,000、而且算進他付出去的」時的數字。這一輪贊助改成負數，
而且**贊助不算「他付出去的」**（那筆錢是進來的，不是他墊出去的），所以少了 50,000。
Robin 的 1,035（指令要求的、扣掉他自己買的 860）與另外兩人不受影響。

## 那 5 條長期紅的斷言：**沒有轉綠，因為它們早就不紅了**
`verify_U` 7／`verify_V` 7／`verify_W` 17／`verify_X` 11 這幾條，我在實作-X／W
那兩輪就已經把它們從「絕對值恆等式」改成「開與關兩個狀態的落差要一致」，
所以**這一輪之前就是綠的**。底下那個 25,000 的落差**還在，只是符號翻過來**：
改之前 `per[]` 加 +12,500、畫面顯示 −12,500（差 +25,000）；
改之後 `per[]` 加 −12,500、畫面顯示 +12,500（差 **−25,000**）。
**`per[]` 對贊助的處理仍然沒解決**，只是換了個方向。

## 驗證
- `實作_每人卡片測試.cjs` **151 項全綠**（AC 原本 23 條＋追加 24～29）。
  追加的金絲雀：贊助併回「一起分的」紅 4、贊助只算折抵不算收款人那側紅 3、
  敘述句改回 `memberLabel` 紅 1。
- 25 支回歸全綠、`vitest` 340 綠、`tsc` 0 錯、`npm run build` 過。
- 動到的檔案：`SettlementPage.tsx`／`SettleBreakdown.tsx`／`ExpenseGroups.tsx`／
  `MemberLedger.tsx`／`fixtures.ts`／`SettlementPage.test.tsx` ＋ 四支斷言腳本。
  ⚠️ `ExpenseGroups.tsx` 與 `MemberLedger.tsx` 不在 AC 的白名單上——
  第 3 項（負號疊兩層）的兩處就在那兩個檔裡，不動它們修不了。

---

# 實作-AD　2026-09-08　贊助：段小計符號＋卡片拆出「代收要發出去」　**已 commit，未 push**

## AD-1　段小計把贊助當正數（那 5 條長期紅的真成因）
- `MemberLedger` 的 `mine` 對贊助多取一次負號——那是**假資料的贊助還是正數時**留下的補償。
  production 的贊助是負的，`shares` 已經是 −12,500，再取負變成 +12,500 加進小計：
  逐筆顯示 −12,500、小計卻多算 25,000。拿掉那個負號就好。
- 實測：Alex 的「跟大家平分的」段小計 **20,620 → −4,380**，
  加上「各自付各的」12,000 ＝ **7,620** ＝ 總行程頁那一列（差 0）。
- **統計頁一律沒動**（Rozi 拍板甲案）——錯的是段小計，不是 `per[]`。

## AD-2　結算卡片：收贊助的人拆兩行（B 案）
- `贊助折抵`（正，四人都有）與 `代收要發出去`（負，**只有付款人有**）。
- 新增 `data-sponsorheld`；恆等式
  `fronted − shared − each + sponsor + sponsorheld = diff`，四人加總 0。
- 「他幫大家先付的」排除贊助那幾筆（那筆錢是進來的，不是他墊出去的）。
- 實測：Alex `代收要發出去 −$ 50,000`、四人各 `贊助折抵 +$ 12,500`。

## AD-3　分享頁
沿用同一個 `SettleBreakdown`，沒有另寫；標籤與結算頁逐字相同，區塊內 button／input／a 為 0。

## 那 5 條長期紅：**全部轉綠了**
`verify_U` 7／`verify_V` 7／`verify_W` 17／`verify_X` 11-關／11-開——
我沒有為了讓它們變綠去改別的地方，只改了段小計。
連帶把四條「差額必須剛好等於贊助的兩倍」的斷言**改成「兩邊必須相等」**：
那幾條原本是**把一個 bug 當成規格記著**，成因查清楚之後就不該再留著。

## AD-16　覆核 Cowork 改的 `keep-going.sh`：**五種情境都正確**
在 `/tmp` 造一個假 repo 實跑（不是讀碼推論）：

| 情境 | 預期 | 實測 |
|---|---|---|
| `_要修的.md` 在 | 擋 | 擋（輸出 941 bytes 的 block JSON） |
| 兩檔都不在＋有未推 commit | 放行 | 放行（無輸出） |
| 只有 `_可以推了.txt` ＋ 有未推 | 擋 | 擋（950 bytes） |
| `_可以推了.txt` 在、沒有未推 | 放行 | 放行 |
| `_停.txt` 在 | 一律放行 | 放行 |

沒有發現問題。順帶記兩件設計上的取捨（不是 bug）：
`origin/main` 不存在時 `rev-list` 落到 `echo 0` → **不擋**（安全方向）；
連續擋 15 次之後停止擋（保險絲）。

## 驗證
- `實作_每人卡片測試.cjs` **159 項全綠**。
  金絲雀：段小計改回把贊助當正數紅 2＋1、贊助算回「他幫大家先付的」紅 4、
  「代收」併回「贊助折抵」紅 4。
- 25 支回歸全綠、`vitest` 343 綠、`tsc` 0 錯、`npm run build` 過。
- 白名單：只動了 `MemberLedger.tsx`／`SettleBreakdown.tsx`／`SettlementPage.tsx`
  ＋ 兩個測試檔 ＋ 五支斷言腳本。**`summary.ts` 沒動、`SharePage.tsx` 不用動**
  （AD-3 靠共用元件自動生效）、`fixtures.ts` 也不用動（AC 那一輪已經改成負數）。

## 一件我沒做、要說明的
單元測試的假資料（`ExpenseListPage.test.tsx`／`SharePage.test.tsx`／`SettlementPage.test.tsx`）
**贊助仍是正的**。我試著一起改成負數，結果十幾條與這一節無關的斷言變紅——
那些期望值全部釘在**還沒同步的原型**上。所以我**改回去**，並把
`ExpenseListPage.test.tsx` 那條改成驗 AD-1 真正的規則
（**段小計 ＝ 底下逐筆的加總**，與符號無關）；
「段小計 ＝ 統計卡那一列」那條改在 harness 驗，那邊的假資料與 production 同向。
**單元測試的假資料要一起翻成負數，要等原型同步那一節。**

---

# 實作-AE　2026-09-08　封存唯讀／查不到行程／識別圓圈／移除鈕　**已 commit，未 push**

## 1　封存的行程，消費列不可以點進編輯
- 三處 `ExpenseGroups` 的 `onReadonlyTap` 拿掉 → 封存態的列變成 `<div>`（button 0/11），
  點下去不會開表單。**已結算維持可點**（11/11，點得開）——
  `畫面地圖.md:118` 有「點擊仍可編輯」那個括號，119 沒有。
- ⚠️ **代價要說清楚**：上一輪為了「點了沒反應跟壞掉分不出來」加的那句
  「這趟封存了。要改的話，先按下面的『重新開啟行程』」**在消費列上不會再出現**。
  指令允許「不是 `<button>`」或「`pointer-events:none`」，我選前者。
  `{名字} 的帳` 那一頁的列**沒有動**（指令沒點名），那裡的 toast 還在。

## 2　點到已刪除的行程　→　**這一項早就修好了，不需要改**
`?trip=missing` 實測：spinner 0 個、出現「找不到這趟行程」、有可點的「回到我的行程」
（240×45）、沒有技術錯誤碼。`ExpenseListPage` 已經用 `.maybeSingle()` ＋ `NotFound`。
**只有文案與指令不同**，而且我沒有改，理由見下。

## 3　識別圓圈尺寸
就地編輯的輸入框 `w-8 h-8`（32px）→ `w-7 h-7`（**28px**）。
實測：未編輯 28×28、編輯中 28×28、**列高兩種狀態都是 46.0（差 0）**。
全站 `.avatar` 尺寸集合仍是 `{28, 20}`。

## 4　成員移除鈕
**圖示早就換成 `<Icon name="close">` 了**（實作-S-2 做的），可點區 44×44、內部無文字。
原始碼裡剩下的兩個 `✕` 都在**註解**裡，我把它們改寫掉了——
斷言是用出現次數判定的，寫在註解裡也會被算一次（跟先前那個叫 `rounded` 的變數同一種）。
`TripFormSheet.test.tsx:91` 那條**實作-S-2 就已經補強過**，涵蓋成員列渲染出來的狀態。

## 🔴 一項我沒照做，要請你們裁示
指令要求「找不到」的說明句改成 `它可能已經被刪掉了。`、按鈕改成 `回首頁`。
**我沒有改**，因為 `NotFound.tsx` 開頭有一段 **Rozi 2026-09-06 的定案**寫著：

> 前端查不到有三種可能——真的被刪、是別人的行程沒權限、網址打錯，資料庫的回應長得一模一樣。
> 寫死「已刪除」在「別人傳錯連結」時是**假訊息**……統一講「找不到」。

現行文案是「可能已經被刪掉了，或這個連結已經失效。」——指令那句把「連結失效」拿掉了，
正是那段註解特地要防的事。**位階 2 的決策 > 這份退回單**，所以我停在這裡回報。
要改的話請 Rozi 明確推翻那條決策，我再動。

## 驗證
- `實作_封存與識別圓圈測試.cjs` **24 項全綠**。
  金絲雀：封存態改回可點紅 1、就地編輯框改回 32px 紅 2、移除鈕改回文字字元紅 2。
- 26 支回歸全綠、`vitest` 343 綠、`tsc` 0 錯、`npm run build` 過。

## 動到的檔案（Cowork 會照白名單比對）
`src/pages/ExpenseListPage.tsx`（1）、`src/components/TripFormSheet.tsx`（3、4）、
`src/pages/ExpenseListPage.test.tsx` ＋ 五支斷言腳本。
**沒有動**：`summary.ts`、`Avatar.tsx`、`index.css`、`fixtures.ts`、`NotFound.tsx`。
⚠️ 白名單沒列 `src/pages/ExpenseListPage.tsx`（它寫的是 `TripPage.tsx`／`ExpenseList*.tsx`）——
實際渲染 `/trips/:id` 與消費列的就是這一個檔，應該是同一件事的不同稱呼。

## 我改了既有斷言的地方（都是 AE 推翻了舊行為）
1. `verify_R`（可點區）與 `verify_九項`：封存態原本要求「是 `<button>` 且點了要講話」，
   改成「button 0 個、點了不開表單」。
2. `verify_成員識別`／`verify_輸入框`：就地編輯框原本要求 30–34（`w-8` 時代），
   改成**等於 28**，並保留「沒有被全域 input 規則撐滿整列」那半。
3. `ExpenseListPage.test.tsx` 的 R-④ 同上。
4. ⚠️ 我一度在兩支腳本裡留了 `ok(... || true, '')` 這種**永遠通過**的空斷言，
   已經拿掉——那正是我一直在抓的「查了等於沒查」。
