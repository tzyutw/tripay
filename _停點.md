# 停點｜實作-B～K（**已上線**）　2026-09-06 14:30

## 實作-K　假資料換成 Rozi 真實資料的形狀

**根因**：原型與 harness 的假資料**都只走 happy path**（清一色有 emoji、八筆消費、金額填好），
而她每天打開的三趟是**無 emoji、0 筆消費、匯率只填一邊、有一趟沒設支付方式**。
她每天看的畫面，跟原型上驗收過的畫面，從第一天就不是同一個。

原型加 T4／T5（`store.realTrips`）＋ S-01 加「示範資料的形狀」切換鈕，現有三趟沒動｜
harness 補 `?expenses=none`／`?rate=half`／`?pays=none`，預設值維持現況｜
版面回歸跑四種組合 × 10 畫面 × 3 寬度：**293 → 1163 條**｜
`scrollIntoView` 補 `?.`，**Unhandled Errors 已清空**。
新增 `實作_原型真實形狀測試.cjs`（50）與 `實作_資料形狀測試.cjs`（9，驗參數真的有接上——
沒接上的話那三種組合等於跑三次預設值，全綠但什麼都沒驗到）。反向驗證六條全紅。

### 真名外洩處置：**沒有洩出去，已結案**
指令原本說「`Tripay_原型.html` 沒被 git 追蹤，可以放真實成員名」——**那句是錯的**，
它自 `750d857` 起就是 tracked 且在公開的 `origin/main` 上。
（Cowork 誤判的原因：`git ls-files | grep 原型` 抓不到，git 會把中文檔名轉義成
`"Tripay_\345\216\237..."`。）我照那個指令把真名寫進工作區，
Cowork 06:22 直接改回中性名，我 06:23 的 commit 因此帶的是中性名。

**逐條查證（這是重點，不是推測）**：
| 檢查 | 結果 |
|---|---|
| 工作區 `grep -nw Ziyu/Ning/Mei/Xiu Tripay_原型.html` | 無輸出 |
| `git log --all -S <各名> -- Tripay_原型.html` | **0 個 commit** |
| `750d857`／`f37c8ca`／`8677f73`／`b0f9f97` 各自的原型內容 | 真名 **0 處** |
| `origin/main:Tripay_原型.html` 第 994／998 行 | `Alex/Robin/Sam/Kai` |

→ **真名從未被 commit、也從未 push。** 不觸發「已推出去」那條五類例外，不需要改寫歷史。

`.gitignore` 裡我加的那幾行已移除——原型本來就在版控裡，Rozi 要它有版控備份；
留著只會製造「它沒被追蹤」的誤解，何況 `.gitignore` 對已追蹤檔案無效。

⚠️ **其餘不動**：那幾個真名早就散在公開 repo 的十幾個歷史文件裡
（`Excel匯入對應規則_濟州島案例.md`、`_指令佇列.md`、`專案狀態.md`…），
最早可追到專案初期。清理那些屬於改寫已推出的歷史＝不可逆，**已交 Rozi 決定，我沒有碰**。

## 實作-J（含第一輪複驗退回）　成員識別全站貫徹
`Avatar` 加 `size`（28／20）＋抽出 `MemberTag`，十一處識別位接上；
三處真正的句子維持純文字。「加一個人」的頭像列刪掉（🙂 不再出現）。
裸元素選擇器改 `:where()`（`input[type=text]{width:100%}` 壓過 `w-8 h-8`）。
分享頁「這趟旅程還沒結算。」橫向溢出 14px 修好。原型同步。

## 前幾輪（都已上線，細節在 `Claude outputs/_實作B進度.md`）
**B–C** 全站對齊原型＋引擎查證＋版面回歸測試｜**D／E** 複驗退回兩輪｜**F** 推上線｜
**G** 分享頁轉帳列重複 12 次｜**H** 日期欄位沒接上 `.datefield`、移除 G-09 預填成員｜
**I** 五個線上實測 bug：sheet 捲不動、沒選付款人靜默失敗、成員錯誤沒清、
名字沒按「加進來」就送出、`＋` 沒對齊（全域 `.ic` 沒被搬過來）。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`750d857` 實作-A｜`723466a` B＋C｜`8cba1ef` D｜`355a64e` E｜`3e01b02` 修 CI lockfile｜
`f8c00ca` G｜（本次）H。

**前兩次部署失敗是我造成的**：CI 走 `pnpm install --frozen-lockfile`，我卻整輪用 npm，
`pnpm-lock.yaml` 沒跟上。**往後這個 repo 一律用 pnpm，推之前先跑 CI 那兩道指令。**

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

## 已結案的舊事
`實作_212對帳表.cjs` 已由 Cowork 於 2026-09-06 作廢（判定規則會自動蓋章），
驗收判定改由 Cowork 寫、Cowork 跑（`_實作階段運作方式.md`「六之零」）。
S-06 hero 不帶成員 emoji、`➕` 維持原型——Rozi 2026-09-05 都已裁示。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
