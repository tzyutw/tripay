# 停點　2026-09-11 16:31　收尾-AH 收尾（commit／push／佇列／鎖檔逾時實測）

指令來源：`_開工.txt`（watcher 起的 `claude -p`，就是這個 session）

## 1. commit ＋ push　✅
- 開始時工作區**已經是乾淨的**：AH 的改動 16:26 就被前一個 session 提交成 `af9226b`，但還沒推。
- 我只把該 commit 的訊息改成指定文字（還沒推過，所以可以 amend）→ **`4313a7c`**。
  `git diff af9226b 4313a7c` 輸出為空＝**內容與 Cowork 複驗的版本逐位元組相同，只換了訊息**。
- `git push origin main` → `db18c52..4313a7c  main -> main`。
- 部署狀態**沒查到**：`gh run list` 需要核可，`-p` 模式沒人能按，被自動拒絕。
- rollback：`git revert 4313a7c && git push`

## 2. `_指令佇列.md`「## 收尾-AH」加 [已處理]　✅（前一個 session 已做）
`af9226b` 裡就有這一行 diff（第 10440 行），我沒再改。

## 3. AH-4 鎖檔逾時實測　❌ **沒跑，這個 session 裡跑不出真的結果**
**原因**：watcher 是單執行緒，它現在正卡在 `claude -p` 這一行等**我**結束。
- `_開工.txt` 的內容就是這份指令本身；`.claude/kickoff.lock` mtime 16:27＝它撿走這份指令的時間。
- 我在這裡 touch 舊鎖＋換一份 `_開工.txt`，watcher 在我結束前**根本不會回到迴圈去看鎖**；
  我一結束，它照 `kickoff.sh:78-79` 把 `_開工.txt` 歸檔、刪鎖——**不管那份是不是我換的**。
  結果會是「90 秒內歸檔了」的假綠燈，測到的是 watcher 在收拾我，不是逾時邏輯。
- 另外 `ps`／`date`／`cat` 那類指令在這個 session 要核可、被自動拒絕，連 watcher pid 都看不到。

**既有痕跡（不算驗過，只列出來）**：`Claude outputs/_開工_已處理_20260911-162029.txt`（一句話的測試工作），
`kickoff.log` 最後一行「I wrote `ok` to …ZZ_watcher_活著.txt」。這證明 16:20 有撿走一份工作，
**但 log 沒有記鎖的年紀**，證明不了是「逾時清鎖」那條路徑放行的。

**要真的驗，請在有人看著的終端機跑**（等這一輪結束、watcher 回到迴圈之後；期間不要按「收」）：
```
pgrep -fl kickoff.sh                                   # 應該 1 個
touch -t $(date -v-1H +%Y%m%d%H%M.%S) .claude/kickoff.lock && stat -f '%Sm' .claude/kickoff.lock
echo '只做一件事：把 ok 寫進 Claude outputs/ZZ_鎖檔逾時.txt，然後結束。' > _開工.txt; date +%T
# 等 90 秒後：
date +%T; ls -l _開工.txt .claude/kickoff.lock; ls -lt "Claude outputs"/_開工_已處理_* | head -1; tail -2 .claude/kickoff.log
```
通過＝`_開工.txt` 與鎖都不在、多一份剛歸檔的檔、log 尾巴是那句 ok。watcher 19:03 到期，要在那之前跑。

## 順手看到但沒動
- **AH-4 只救得了「watcher 死了、鎖留下來」這一種**。如果是 `claude -p` 本身掛住，
  watcher 會卡在前景那一行，**逾時檢查永遠輪不到**——跟 03:03～08:12 那次一樣安靜。
  那次的成因沒查出來，所以**不知道 AH-4 能不能擋住那一次**。要擋就得給 `claude -p` 本身加執行時限。
- 同時跑兩個 watcher 時，若一份工作做超過 30 分鐘，另一個會把**還在用的鎖**當殘骸清掉。
- `kickoff.log` 說寫了 `ZZ_watcher_活著.txt`，但 `Claude outputs/` 現在沒有這個檔（可能被移走了，沒追）。
