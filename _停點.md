# 停點報告

> 2026-09-04　狀態：**批一修正完成，等 Rozi 看過再做批二**

## 交付
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**（同一連結）

## 1. S-02-19 整項移除 ✅
連同我原本加的色調預覽色塊一起拿掉——那個色塊也是「顯示色調是什麼」。
編號在盤點表已標刪除線，號碼不回收。

## 2. 灰字 ✅ 砍 7 條、留 2 條
砍：封面自動色調說明、預設今天、最多 10 個字、色調＝預設盤、最後一個是自動、
點成員標記哪位是你、統計卡我的花費、長邊 1600px。
留：**可留空＝當天來回**、**只有你和拿到分享連結的人看得到**。
「最後一個是自動」改成讓那個選項自己標著「自動」，不用文字解釋。
測試有逐條斷言這 7 條字串不得再出現。

**順帶一提**：`S-02-17` 整塊就是被砍的那句灰字，所以它也標成已移除了。
「哪位成員是我」**功能本身完全沒動**（§3 註記的存廢批二再談）。

## 3. 「這是我」改互動 ✅
每一列右側**常駐**一個「這是我」標記：未選是描邊灰字、選了填實成工作色。
可點性由元件自己表達，左側原本那個圓形勾選框拿掉了——它讀起來像「要不要參與」，
和「哪個是我」是兩件事。

## 4. S-02b-12 這趟的現金匯率 ✅（新編號，已補進盤點表）
「台幣 [10000] 換到 [45000] KRW」與「1 KRW ≈ [0.2222] 台幣」**雙向連動**，
改一邊另一邊即時跟著動。灰字只留一句規則：**現金與儲值卡用它換算，刷卡直接填台幣不套用**。
記帳時的換算與捷徑入口屬 S-04，依指示**留到批二**。

## 5. 橫向滑動 ✅ 320／375／414 全過
`原型_版面回歸測試.cjs`，**用真實 Chrome 量測**——jsdom 沒有排版引擎，
`scrollWidth` 恆為 0，測了等於沒測，所以裝了 `puppeteer-core` 驅動本機 Chrome。

第一次跑就抓到 **3 處真的溢出**，都修了：
- 320px 整頁溢出 6px → S-00 登入鈕寫死 `width:250px`
- S-02b sheet 溢出 14px → 匯率第二列沒有 `flex-wrap`
- S-02 sheet → **原生 `<input type="date">` 有內建最小寬度**，兩欄並排在 320px 會撐開

## 6. 測試總覽（三組都全過）
| 測試 | 項數 | 結果 |
|---|--:|---|
| `原型_批一回歸測試.cjs`（jsdom，新存進 repo）| 45 | 全過 |
| `原型_輸入回歸測試.cjs`（#13 既有）| 34 | 全過 |
| `原型_版面回歸測試.cjs`（真實 Chrome，新增）| 12 | 全過 |

## 7. 盤點表差異
`S-02-17`、`S-02-19` 標刪除線；**新增 `S-02b-12`**。
另註記：編號實況自 2026-09-04 起以 `Tripay_原型.html` 為準，該檔改人工維護。

## 8. 沒做的
未動 `src/`、未跑 migration、未刪檔。舊稿橫幅維持「即將作廢」（四批完成才改）。
