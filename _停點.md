# 停點　2026-09-12 19:15　收尾-AJ　原型測試 15 支修復

15 支**全部跑到「通過 N 失敗 M」那一行，0 支中途丟例外**。紅合計 **2**（上限 3），兩條都有出處。

| 停止條件 | 實際 |
|---|---|
| 1　15 支都跑到底 | ✅ 0 崩潰（原本 5 支崩，指令寫 6 支——`原型_輸入回歸測試` 其實一直是綠的 34/0） |
| 2　三條 `.ic2` 斷言綠、原型的 `--ic2-`／`.ic2{` 沒被動 | ✅ `git diff` 裡這兩個字串**一行都沒有** |
| 3　紅 ≤3，每條寫明理由 | ✅ 2 條，見下 |
| 4　`no-sandbox` ≥15 | ⚠️ **11**——只有 11 支會開瀏覽器，另 4 支是 jsdom。11 已是全部 |
| 5　單元測試／tsc／build | ✅ 354 passed／3 skipped、tsc 0、build 0 |

各支：icon 79/0、倒序 38/0、可點區 44/0、字級 107/**1**、批一 61/0、批三回歸 54/0、批三回饋 45/0、
批二 132/**1**、焦點 37/0、版面回歸 18/0、版面與拖曳 64/0、稿註 54/0、貼邊 51/0、輸入 34/0、預設日期 32/0。

## 剩下的 2 條紅：都是**原型違反自己的稿註**，修法會動到版面

1. **S-02b-14「這趟叫什麼？」標籤自己佔一行**。它的稿註（原型 1738 行）寫著
   「與 S-02『去哪？』**同一款欄位**」，而 S-02-6 用的是 `.fieldrow`（標籤與輸入同列），
   S-02b-14 用 `.fld`＋`.lbl`。違反全站 UX 檢查第 1 條。**修法是包進 `.fieldrow`，會動版面。**
2. **S-06 分享頁的每人分擔列是可點的 `<button>`**。S-06-14 的稿註寫著
   「**分享頁是唯讀**——這裡的列**不可點**（⋯也不該讓外人進編輯視圖）」。
   但同一支還有一條斷言要求 S-06 與 S-03 的列**結構完全相同**（同一個元件），
   所以要修得先讓那個共用元件有唯讀變體——**是設計決定，不是腳本修得掉的**。

## 過程中查出來、原型真的壞掉的三件（都修了）

- **`currencyName()` 五處呼叫、零處定義**。三支崩潰的根因不是「腳本叫舊名」（指令的推測），
  是原型自己少了這個函式。照實作端 `src/lib/currencyTable.ts:30` 補回去。
- **`--h-tap` 從來沒有在原型裡定義過**，而三處 `::after` 在用它——那些透明可點區的寬高
  一直是 **0**，畫面上看不出來。**這是我在收尾-AI-6 把 `.ic2::after` 改用 token 時放大的**
  （原本只有 `.swchip`／`.clearbtn` 中招），這一輪量到 `tap=0×0` 才抓到。已補 `--h-tap:44px`。
- **`<section id="s03more">` 與 S-03 上那顆 `<button id="s03more">` 撞名**，
  於是「點 ⋯ 開選單」的處理器把該畫面內的**每一次點擊**（含返回鍵）都當成再開一次選單。
  按鈕改名 `s03morebtn`。

## 兩處指令的判斷與實測不符（用量到的做）

- **`原型_預設日期` 的「明確 height 只留一份」**：指令說合併原型 CSS。實測**不能合併**——
  拿掉 `.datefield input[type=date]` 的 height 之後 `.ui .fieldrow>input` 就贏了，
  **S-04 的日期欄從 40px 掉到 28px**（改前改後各量一次）。改成守原型註解真正寫的那句
  「**不要再寫高度字面值**」：兩條的值都必須是 `var(--h-*)`。
- 文件同步：`S-02b-14`／`S-03-34`／`S-03-35` 補進 `_盤點_畫面功能.md` 與 `_盤點_實作缺口.md`
  （統計表 全新 37→40、合計 212→215）；`S-01-2` 的稿註 kind 從 `st` 改 `del`
  （它的內文本來就寫著「移除」），盤點表那一列補上刪除線。

## 順手看到但沒動
`原型_批二` 的徽章掃描掃不到 **S-05-31**——甲案之下「筆數一樣就整句不顯示」，
而原型這趟假資料 direct 與 hub 都是 3 筆，**這份假資料到不了那個狀態**。
已在輸出印出「（不列入）」那一行讓它看得見；要真的掃到，原型得補一趟筆數會變的假行程。
