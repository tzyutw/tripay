# 停點　2026-09-10 22:15　修-3／修-4 做完

`_要修的.md`（晚間那份）兩項都做完，已歸檔到 `Claude outputs/_要修的_已處理_修3修4.md`。

## 修-3　幽靈卡文案「再開一趟」→「新增一趟」

改了四個檔，只動那一串字，幽靈卡的圓形加號、底色、尺寸、位置、可點區一個字都沒動：

| 檔 | 動了什麼 |
|---|---|
| `src/pages/TripListPage.tsx` | 按鈕文字（152 行）＋ 註解裡引到的同一串字（129 行） |
| `src/pages/TripListPage.test.tsx` | 兩條斷言的比對字串（97／104 行）＋ 註解（62 行） |
| `src/test/fixtures/screens.json` | `s01.list` 與 `s01.text` 兩處基準 |
| `Tripay_原型.html` | 幽靈卡本體（1351 行）＋ 三段註解 ＋ 盤點表 S-01-8 的標題與說明 |

### 停止條件
1. ✅ `git grep -c "再開一趟" -- src/ Tripay_原型.html` → 無命中（0）
2. ✅ `git grep -c "新增一趟" src/pages/TripListPage.tsx` → 2
3. ✅ `vitest` **343 綠 / 3 skip**（20 檔通過、1 檔 skip）、`tsc --noEmit` 0 錯、`npm run build` 過
4. ✅ 幽靈卡其他一切未動——`git diff src/pages/TripListPage.tsx` 只有那兩行

## 修-4　`Claude outputs/*.cjs` 移出版控

- `.gitignore` 補一條 `Claude outputs/*.cjs`，比照第 64 行 `*.png` 的寫法附四行註解說明「重現腳本是拋棄式的」。
- `git rm --cached "Claude outputs/_repro_修2.cjs"`。

### 停止條件
✅ `git ls-files "Claude outputs/*.cjs" | wc -l` → **0**，`ls` 確認本機檔案仍在（6509 bytes，18:23）。

## 本次動到的檔（`git diff --stat`）

`.gitignore`、`Tripay_原型.html`、`src/pages/TripListPage.tsx`、
`src/pages/TripListPage.test.tsx`、`src/test/fixtures/screens.json`。
另外三份文件（`_延後與不做清單.md`、`_需求台帳.md`、`專案狀態.md`）**不是本輪改的**——
是我接手前工作區裡就有的 Cowork 未提交編輯，一併帶進這次 commit。

## 順手看到但沒動（由 Rozi 決定要不要進下一節）

1. **`專案狀態.md:5` 還寫著舊文案**：那一段描述實作-AF 時說「幽靈卡⋯改成⋯＋「再開一趟」」。
   停止條件只點名 `src/` 與 `Tripay_原型.html`，所以沒動它。
2. **兩處註解的理由句在改字後變得不通**：`TripListPage.tsx:129` 與 `Tripay_原型.html:1349`
   寫「一趟都沒有的人沒有『新增一趟』」——原本的論證靠「再**開**」隱含「已經有過一趟」，
   換成「新增」之後這半句站不住（後半句「跟『建立第一趟』重複」還是成立的）。
   只改字、沒重寫論證。

## 機器驗不到、要人看的

這次只換四個字、版面寬度完全一樣（都是四個中文字），沒有原生表單控制項，
**沒有需要 Rozi 在手機上確認的項目**。
