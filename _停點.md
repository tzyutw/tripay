# 停點｜實作-O ＋ 實作-P（一次「收」跑完，**已上線**）　2026-09-06 22:10
歷輪完整內容在 `Claude outputs/_實作B進度.md`；需求↔斷言對照在 `_需求台帳.md`。

## 實作-O　外幣被靜默跳過／整筆算到付款人頭上（＋ 5～8 節）
**O-1** 只填外幣的消費，畫面用行程匯率顯示得好好的，但存進去是 `twd_amount=null`＋
`twd_pending=true`，引擎 `.eq(twd_pending,false)` **整筆跳過**，沒有任何一句話說它被跳過。
改成**存檔當下就換算**（取 `calc().twdTotal`，不另寫除法），並在「設定匯率」時
用一次 `upsert`（單一敘述＝要嘛全成要嘛全不做）補算既有的；已有台幣的一筆都不動。
**O-2** 「各自付各的」用外幣填時 `split_amount` 是 null 而 `split_pending` 卻是 false，
引擎兩個條件缺一 → 其他人 0 元、付款人扛整筆。改成存 `calc().shares[id]`（§2.2 比例回推，
差額已歸付款人），台幣算不出來時 `split_pending` 才是 true。
**O-3** 假資料補三條從沒被畫過的路徑：`?fill=for`／`?forOnly=1`／`?settlements=many`（12 筆結算）。
**O-4** 🔴 **新版部署成功但使用者拿不到**——SW 把舊版釘在裝置上，舊進入點去要已刪掉的
chunk → 整頁空白。Rozi 那兩輪「匯率沒帶 1」「刪除／分享沒反應」都是這個，不是功能壞掉。
改：`registerSW({immediate:true})`＋`onNeedRefresh` 立刻 `updateSW(true)`＋掛載後主動
`update()`；`vite:preloadError` 清 workbox precache 後重載一次（60 秒冷卻防無限轉）；
設定頁最下面顯示 commit 短碼。
**O-5／6** 建立頁與編輯頁**欄位完全一致**（各 8 個欄位標題，名稱兩頁都叫「這趟叫什麼？」）；
必填恰好四個（`.req`，`*` 走 `::after` 不進 textContent）；沒填就捲過去、聚焦、且不送出。
**O-7** 匯率改成「填了一邊，另一邊自動帶 1」；來源清空 1 跟著消失，動過就不再是自動的。
**O-8** 「分享／複製／刪除」按了沒反應：`/trips/:id` 與 `/trips/:id/more` 是兩個 sibling
`<Route>`，React Router 卸載一個掛另一個，`setXxxOpen(true)` 設在正要被卸載的實例上。
三個都改走 route（`/share`／`/copy`／`/delete`）。`onArchive` 走 mutation，沒動。

## 實作-P　高度兩階 ＋ App 圖示
五種高度（46／41／40／38／28）收斂成 **`--h-field:40` 與 `--h-inline:28`**，
token 定義在原型的 `.ui`、由 `實作_原型CSS移植.cjs` 搬過來；元件裡不再有輸入框高度字面值。
階 1 取 40 不取 44：日期欄已經是 40，而那一處在 iOS 上塌掉修過三次。
App 圖示底色換成 S-00 那組漸層（158deg 換算成 objectBoundingBox 座標，不是目測），
`theme-color` 兩處都改 `#1276C4`，`background_color` 沒動，全 repo `#7C2D12` 命中 0。

## 驗證
`tsc` 0 錯｜`vitest` **289 綠**無 Unhandled｜`pnpm run build` 過（bundle 內含 commit 短碼）。
版面回歸 1163｜寫入與更新 **61**｜高度階梯與圖示 **54**｜輸入框與選單 68｜查不到與把手 58｜
成員識別 79｜原型真實形狀 50｜資料形狀 9｜token 52｜型別對帳 29｜文案唯一性 13｜分享頁 2。
**九組反向驗證全部紅過**，逐條列在 `_需求台帳.md`。

**寫入路徑對 production 實跑再 rollback**（只動 `ZZ 測試行程`，四道全過、0 殘留）：
① 只填外幣 insert…returning → `twd_amount 23810`／`twd_pending false`
② 補算 upsert…returning → 只有那兩筆被改（藥妝店 14286、午餐 5714）
③ individual＋FOR 的 splits insert → `split_amount 276`／`split_amount_foreign 12000`
④ 建立行程 insert（這輪多寫了 `cash_rate_*`／`settlement_mode`／`payment_methods`）
> ⚠️ ③ 若寫成單一 CTE 會被 `expense_splits` 的 RLS 擋下——**前端本來就分兩次 request，
> 所以沒事**；但這證實了「RLS 看不到同一敘述剛插入的列」那條，不要把它併成一句。

## 要 Rozi 在手機上看過才算數
1. 匯率兩欄 38→40 之後，20px 的數字有沒有被切到
2. 行程名稱 46→40 之後，建立行程頁會不會太擠
3. iOS 日期欄位仍然正常（這一處修過三次，只有真機說了算）
4. **App 圖示要先把主畫面舊圖示刪掉、重新「加入主畫面」一次**，iOS 才會換圖
5. iOS Safari 上匯率欄位連打是否真的不收鍵盤

## 順手看到但沒動
- **原型的 S-02／S-02b 還沒補欄位**：O-5 只改了 App。原型要補得動五個有狀態的
  handler（`data-smode`／`data-hub`／`data-rmpay`／`data-paygrip`／付款輸入框）＋
  拖曳排序寫死 `#scr-s02b`，而且有 **15 支 `原型_*.cjs`** 綁著那些 id。
  佇列沒點名原型，這種規模不該順手做——**請 Rozi 決定要不要開一節**。
- 既有的紅（**與這一輪無關，已對 HEAD 版原型複核過，數字完全相同**）：
  `原型_字級與真機測試` 3 紅｜`原型_貼邊與結構測試` 5 紅｜`原型_預設日期與欄位列測試` 1 紅｜
  `原型_版面與拖曳測試` 2 紅｜`原型_焦點與圓角測試` 直接 crash。
- production 目前有 **19 筆**「有外幣沒台幣」的消費散在各趟。依裁示**不寫批次腳本**，
  等 Rozi 在該趟按下儲存匯率時由 App 自己補算。
- `exchange_rate` 存的是「1 外幣值多少台幣」（`twd/for`），與 `tripRate()` 的方向相反。
  這是既有慣例，換算時沒有用到它，這一輪沿用沒動。

## 檔案範圍
超出 O 第 5 條白名單的部分都是 4～8 節與 P 節必須動的：`App.tsx`、`ExpenseListPage.tsx`、
`main.tsx`、`vite.config.ts`、`index.html`、`SettingsPage.tsx`、`public/pwa-icon.svg`、
`src/lib/swUpdate.ts`、`src/index.css`、`vitest.config.ts`、`src/test/utils.tsx` 與各測試檔。

## 🚀 已上線　https://tzyutw.github.io/tripay/
Cowork 複驗通過（14/14 ＋ 7/7 ＋ 9/9 ＋ 圖示取樣 PASS，三支判定程式在舊版上先跑紅過）→ push。

**CI 掛了一次，是我造成的**：`virtual:pwa-register` 要 `workbox-window`，
我本機的 `node_modules` 有它（早期 npm 裝出來的扁平佈局），CI 的 pnpm 嚴格連結沒有 → build 失敗。
補成 devDependency，並且**砍掉 node_modules 重跑一次 `pnpm install --frozen-lockfile` ＋
`pnpm run build`** 才確認。這是同一類事故第二次（第一次是整輪用 npm）——
**往後新增任何 runtime import，都要用乾淨的 node_modules 跑一次 CI 那兩道指令。**

線上 bundle `index-Ddkb_X33.js` 已確認含本次 commit 短碼 `0327cc2`
——設定頁最下面那一行就是拿來做這件事的。
