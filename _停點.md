# 停點報告

> 2026-08-31 02:20（GMT+8）　狀態：**等第三批逐頁清單（S-03 起）**

## 一句話
佇列 #8 八項全數落地：封面 emoji 取消、Emoji 選擇器整個移除、首頁卡去成員列、
日期規則放寬、字體規則修正，四頁重新完整渲染。

## 一、做了什麼（對號 A–H）
- **A/B 封面**：S-02-5、S-02b-6 移除。S-02b 兩個動作＝更換照片／重選色調；
  色調是 `destinations.ts` 的 **8 種預設盤**（已畫出色票列），不做挑色器。
  `trips.emoji` **欄位保留不刪**，前端不再使用
- **C 成員識別**：S-02c 整段改寫。原選擇器 9 項全標移除，新增 4 項——
  三層 fallback、就地編輯、技術限制標註、放寬後的驗證規則。
  **`firstGrapheme()` 等驗證函式全部留用**，只拆 UI
- **D 首頁卡**：移除成員識別列。稿上新增 **S-01-13** 專門記錄適用範圍，
  白紙黑字寫明行程內頁／記帳表單／結算頁／分享頁照常顯示、**S-03E 空狀態不得誤刪**
- **E 幣別**：「52 列」確實是我誤讀（`max-h-52` 是高度不是列數）。已改標
  **實際 18 種（含泰銖）**，並在稿上標明出處
- **F 日期**：出發預設今天、回程可留空；新增 **S-02-20** 單日確認對話框，文案佔位
- **G 字體**：等寬只用於金額。v5 全稿與 **tokens 頁**同步——日期、天數、筆數、
  日期分組標頭、統計標籤、狀態章、chips、幣別切換全部改回系統字
- **H 重渲染**：S-01／S-02／S-02b／S-02c 四頁移除後版面已遞補，無殘洞

## 二、⚠️ 需要 Cowork 端動手的一件事
`supabase/migrations/009_member_emoji_default.sql` 已備好：
`trip_members.emoji` 預設改空字串（第二層 fallback 才會生效）。
**只改預設值，不動既有資料**——四趟的 🐵🐱🍋🐟 原樣保留，檔尾自檢會斷言筆數不得變少。
**待 Cowork 端套用 production。**

## 三、順帶回報一件事（不是這批的範圍，給第三批參考）
決策 D 說 S-03E 空狀態要顯示成員 emoji。實際讀碼：**現況的空狀態沒有成員 emoji**，
成員是顯示在行程頁 hero（S-03-8）上。這批我什麼都沒刪，所以不存在誤刪；
但要不要照決策 D 把成員 emoji 補進空狀態區塊本身，等第三批一起裁示。

## 四、artifact（兩個都更新了）
- v5 全稿：2facaecf-d131-41e2-8048-c58fe7a37e2c（label：v5-第二批）
- tokens：686e62e2-ea58-4ae2-a6f2-54be3deeeb37（label：v4-字體規則修正）

## 五、文件同步
`_盤點_畫面功能.md` 重出 245 列｜`專案狀態.md` 補 8 條決策｜
`決策符合性檢查表.md` v1.4 補 #46–53（累計 53 條，其中 4 條可 grep）｜
`_實作批次工作清單.md` 新增「v5 第二批裁定」一節｜
`CLAUDE.md` 完整性守則補「示意必標實際數量與出處」（100 行）
