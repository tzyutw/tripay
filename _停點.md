# 停點報告

> 2026-09-04　狀態：**#14 第 1 節查證完成，依「驗完先回報再往下」停下**

## 未改任何碼。V1–V4 結果如下，其中 V3、V4 與規則有出入。

## V1 消費列表頁有查 expense_splits ✅
`src/pages/ExpenseListPage.tsx:169` `.select('*, expense_splits(*)')`，
:240 已在用它算「我的花費」。**前端可直接計算本節狀態，不需新增快取欄位。**
護欄 12.2 未觸發。

## V2 「待填」3 處＋「待補填」2 處，合計 5 處（<10，護欄 12.3 未觸發）
**使用者看得到的 3 處：**
- `ExpenseListPage.tsx:370` —— S-03 統計卡「⚠️ 含 N 筆待填，數字僅供參考」（第 4.1 節要移除整條）
- `ExpenseListPage.tsx:467` —— S-03 消費列 badge「待補填」
- `SharePage.tsx:257` —— S-06 分享頁金額欄下方「待補填」

**程式碼註解、非文案的 2 處**（第 6 節文案替換不該動）：
- `ExpenseFormSheet.tsx:272` 註解、`SettlementPage.tsx:215` 註解

## V3 ⚠️ 每位成員只有一個輸入欄，但**這個欄位沒有幣別**
- 欄位：`form.individualAmts[m.id]`，`Record<string, string>`（`ExpenseFormSheet.tsx:45`），
  JSX 在 :450–460。**確實是一人一欄**，符合 2.2 的前提，判定規則可直接套用。
- **但幣別不是「由該筆消費決定」，而是根本不存在**：欄位沒有幣別標示、沒有切換，
  值在 :252 直接寫進 `split_amount`，而引擎把 `split_amount` 當台幣。
  在日幣／韓圜行程照當地金額填會**靜默算錯**（＝已列管的 P1-0）。
- 所以與規格「各自金額以該筆幣別統一填寫」**不一致**，依 V3 指示只回報、未改。
- **連帶影響 8.2**：該節寫「使用者在**外幣欄**輸入時寫入 `split_amount_foreign`」——
  但 splits 現在**沒有外幣欄**。這個寫入時機在現況無處可掛。

## V4 ⚠️ 結算完成 **不等於** 唯讀
- 兩者是不同狀態：`deriveStatus.ts:3-8` 分別由 `trips.status` 判為 `settled` / `archived`；
  `ExpenseListPage.tsx:277-279` 也分開處理。
- **唯讀只綁 `isArchived`**：:316 隱藏編輯行程、:483 隱藏底部操作列、:545 擋行程編輯 sheet。
- **消費列 :450 的 `openEdit` 完全沒有狀態守衛** → 已結算（甚至已封存）的行程，
  消費仍然點得進去改。
- 因此 V4 的但書「若結算完成即進入唯讀 → 本節不需處理已結算的帳被改動」**不成立**，
  而本節沒有規定這種情況要怎麼辦。

## 我建議這樣往下（等你一句話就開工）
1. **V3**：原型照規格做（一人一欄、幣別由該筆決定），src 不動——src 的修復屬 P1-0 實作批次。
   8.2 的 migration **照原文寫欄位**，只把「寫入時機」註記為「待 src 具備外幣欄後生效」。
2. **V4**：本輪**不處理**已結算被改動，只在原型與文件記為已知缺口，另案處理。

兩點都同意的話，我接著做 9.1 原型（S-03/S-04/S-05）＋9.2 七項測試＋8 節 migration。

## 順帶
`分帳模型原型.html` 已被加上作廢橫幅、指向 `Tripay_原型.html`。
依 #14「其他畫面與既有稿的併入屬 #15，不在本節範圍」，本輪不做併入。
