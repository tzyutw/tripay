# 停點｜實作-S 七項（一次「收」跑完，**已上線**）　2026-09-07 05:10
需求↔斷言對照在 `_需求台帳.md`；歷輪在 `Claude outputs/_實作B進度.md`。

## migration 016（Cowork 已套 production）
`expenses.note` ＋ 200 字 check 都已存在。套之前 `begin…rollback` 乾跑：
426 筆既有消費、0 筆有備註、constraint 掛得上。

## 1　匯率的說明「沒有案例顯示差別在哪」
兩個方向的第一行長得幾乎一模一樣（只有幣別名互換、數字還是同一個），
掃過去分辨不出來——而方向填反過一次，帳從 64,785 變成 1,469,048。
加第二行**她看得懂單位的換算範例**：

```
1 日圓 ＝ 0.21 台幣
花 10,000 日圓，記成台幣 2,100      〔換個方向〕   ← 按了變成 47,619
```

數量級差 22 倍，一眼看得出哪個對。示範金額依幣別量級決定（JPY/KRW/VND/IDR 用 10,000，
其餘用 100），換算走**既有的 `tripRate()`**——並且有一條**一致性斷言**：
範例算出來的台幣必須等於真的記一筆時算出來的，不然範例會騙人。
沒有用「約」字（那是「金額未定案」的專用標記）。

> ⚠️ 指令上寫的 `47,619,048` **多了三個 0**（10,000 ÷ 0.21 = 47,619），斷言照正確值寫。

## 2　成員移除鈕的 `✕` 換成 Feather icon
`tap44` 與 `w-6 h-6` 都保留——實作-R 剛把可點區撐到 44×44，看得見的仍是 24×24。
順手把 `TripFormSheet.test.tsx` 那條「不要用字元充當 icon」改成**真的會抓到**：
它原本只測「建立頁沒有成員」的狀態，所以成員列那顆 `✕` 從來沒被掃到過。

## 3　「記一筆」的備註欄
`note text`（migration 016），200 字上限，空的存 **null 不是空字串**。
**只在 S-04 看得到**——S-03／S-05／S-06 都不顯示（她明講「不用列在總行程頁」；
分享頁是給別人看的，更不該預設外流）。不參與任何計算（有一條斷言比對
「加了備註之後金額與分帳逐欄不變」）。

## 4　「記在」的預設日期：事後補記時跟著上一筆走
她在 9 月補記 2 月的濟州島，每一筆都預設回程日。加第三條規則：
今天晚於回程日 → **這趟最後建立的那一筆的日期**。
⚠️ 用 `created_at` 最大的，**不是 `expense_date` 最大的**——她照天數順序往下補，
用日期最大的話回頭補第 1 天之後又會跳回最後一天。有一條測試專門擋那個寫法。

## 5　金額的兩段說明同時顯示
教學句移到「金額」那一列**右側**，常駐小灰字；下面那一行只留警示。
兩句同時看得到，但不多佔一行；視覺重量差很多（警示是橘色帶 icon 的獨立一行）。
文案縮短成「填一邊就好，另一邊自動換算」。320px 塞不下時由 CSS 縮成「填一邊就好」。
**沒有塞進 placeholder**——那裡放的是自動換算出來的金額。

## 6　「記一筆」固定在畫面下方
`.btnrow.sticky`（`position:sticky; bottom:0`）＋清單底部 74px 留白。
改動前量到按鈕在 y=1457、視窗只有 844，要捲到底才看得到。
**留白跟著按鈕列存不存在走**——已結算態沒有主鈕，那時不留白（有反向斷言）。
S-04 彈層底部那一排沒碰。

## 7　外幣金額改灰
新增 `--tx2`（= `var(--md)`，不寫第二個字面值）。對白底 **7.03:1**（AA 要 4.5），
與 placeholder 的灰（4.51）差 **2.52**——不然「填了」跟「沒填」看起來一樣。
只在**台幣與外幣並列**的地方變灰：S-04 兩欄、各自金額、換算範例的外幣數字。
**整頁只有外幣的視角不准灰**（有反向斷言，而且先守門確認真的切過去了）。
字級、字重、`tabular-nums`、對齊、高度一個都沒動。

## 驗證
`tsc` 0 錯｜`vitest` **316 綠**無 Unhandled｜`pnpm run build` 過。
備註與說明 **64**｜可點區與圖示T 62｜寫入與更新 61｜版面回歸 1163｜輸入框與選單 53｜
查不到與把手 58｜高度階梯與圖示 52｜匯率與千分位 42｜成員識別 79｜原型真實形狀 50｜
資料形狀 9｜token 52｜型別 29｜文案唯一性 13。
**七組反向驗證全部紅過**，逐條列在 `_需求台帳.md`。

> ⚠️ **三支既有斷言的基準跟著這一輪動了，都是刻意的**：s04 的 28px 輸入框 3→4（備註欄）、
> s03 捲動高 1530→1604（+74 的 `.btnpad`）、s04 捲動高 725→783（備註那一列）。

## 要 Rozi 在手機上看過才算數
1. 換算範例那一行讀起來順不順、有沒有幫助判斷方向
2. 備註欄的位置（在最後）與 placeholder 文案
3. 「記一筆」貼在下緣之後，最後一筆會不會被蓋住（iOS 安全區已留）
4. 外幣的灰在真機螢幕上會不會太淡
5. 事後補記時日期有沒有跟著上一筆走

## 順手看到但沒動
- 原型的 S-02／S-02b 欄位一致（實作-O-5）仍未補。這一輪同步了原型的
  換算範例、標題列、sticky 按鈕列與外幣灰。
- 既有的紅（與這一輪無關）：`原型_字級與真機測試` 3｜`原型_貼邊與結構測試` 5｜
  `原型_預設日期與欄位列測試` 1｜`原型_版面與拖曳測試` 2｜`原型_焦點與圓角測試` crash。
- `note` 的寫入路徑已由 Cowork 在 production 實跑（`begin…rollback`，authenticated 身分，
  「建立行程 → 加成員 → 只填外幣記一筆 → 分帳 → 結算 → 轉帳明細」整條走完再回滾）。
  A3 量到 10,000 JPY ＠0.21 = **2,100 台幣**，與畫面上那行換算範例同一個數字。

## 🚀 已上線　https://tzyutw.github.io/tripay/
Cowork 複驗：verify_S 61/61、verify_R 28/28（常設回歸）、驗收案例核心區 12 條全過。
金絲雀打在 `5adbdf1`（實作-S 之前）→ **verify_S 24 紅**。

**Cowork 自己抓到一條會放行的斷言**（值得記著）：第 11 條（外幣灰）第一版只驗
「兩欄顏色不同」＋「外幣對比 ≥4.5」——**方向做反也會全綠**。
已改成「兩欄都填真值時，台幣必須比外幣深」。實作本身是對的
（外幣 7.03、台幣 17.21），但斷言差點放行。
**「有差別」不等於「差對方向」——這一類斷言以後都要驗方向。**
