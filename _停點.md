# 停點報告

> 2026-09-05　狀態：**#28 全部做完（含 28-6／28-7 追加），等 Rozi 看**

交付：**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**（同一連結）

## 你說的「文字頂到最左邊」——根因不是間距沒調，是兩個 class 從來沒定義過
全站搜 CSS，**`.sec` 與 `.stats` 都沒有任何樣式定義**，用到它們的地方全是裸 div。
這一次同時解釋了三件事：文字貼邊、字級比其他小標大、顏色不是灰的。
兩個都補上定義（左右各 14px，與卡片同一條 x 軸）。

**掃描方式**：量 `Range` 的**墨水位置**，不是元素外框；S-00～S-07 全狀態 ×３個寬度。
修前 14 處（`.sec` 12 處＋S-06 的兩個金額），**修後 0 處，沒有任何例外白名單**。

## S-03 的違和感：不只是按鈕太多，是**混了三種層級**
每天要用的只有「記一筆」；「複製行程」產出的是一趟新行程，屬於首頁；
「設定」是 App 層級的，不是這趟的設定。三種東西並排在右上角，這才是硬的原因。

頂列只剩 **‹** 與 **⋯**；消費／結算改**分段控制**（結算是檢視，不是一次性動作）；
底部只留一顆主鈕：旅途中「＋ 記一筆」／**已結算沒有主鈕**（那時要做的是逐筆標記付清）／
已封存「重新開啟行程」。設定移到首頁標題列（S-01-15）。

**S-05 一個字都沒動**——分頁只是把它的輸出借過來。測試逐字元比對五種狀態全部相同。
借過來會多一條它自己的導覽列（兩顆返回鍵疊著），用 CSS 收起來，沒有改它的輸出。

## 順手接住的一個規格漏洞
「結算行程」按鈕沒了，規格 §6 的**結算前檢查層**原本掛在那顆鈕上。
已把判斷搬到分頁切換：有未定案的帳，切到「結算」一樣先進檢查層，不是直接看結果。
批二測試改成點分頁後仍然通過。

## 其餘
- icon 底組定案 **Feather（MIT）**，對照條與另兩組移除，授權檔只留一組
- 返回全站改純 icon，`aria-label` 保留、可點區 44×44
- icon 容器 26×26 → **40×40 正圓＋一圈線**，icon 15px → 20px
- 登入頁標語與登入鈕共用 200px 欄，**實測兩者都是 200px**
- 登出確認框改描邊（#27 停下回報的那一項，#28-5 補上）

## 兩件要你裁示的
1. **編號**：指令說新號從 `S-03-30` 起，但 **30 已經被「已結算／已封存不提醒」占用**。
   依「不要重新編號」的護欄往後接，用了 **31／32／33**。
2. **有四個 icon 沒人用了**：`edit`／`copy`／`share`／`del`——
   它們的動作都變成 ⋯ 選單裡的**純文字**項。**⋯ 選單的項目要不要配 icon？**
   要就直接復用這四個；不要就依規則刪掉。先保留並記在 `_icon授權.md`，沒有自行決定。

## 測試（八組全過，新增一組 65 項）
60／132／54／34／18／45／80／**65**。盤點表與盤點檔都已同步（原型 210 項，無孤兒無缺漏）。
