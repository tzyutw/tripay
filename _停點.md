# 停點　2026-09-10　實作-AF（空行程頁改版／讀不到 ≠ 不存在／斷網不卡住）

## 做了什麼

- **AF-1** 頂部列那顆建立按鈕整顆移除；幽靈卡搬到列表**第一張**、拿掉模糊與呼吸動畫、
  改成 28×28 實心圓加號 ＋「再開一趟」；**零行程時不出現**。原型與 `screens.json` 同步。
- **AF-2** 結算頁讀不到時**不再說「大家剛好打平」**。根因不只是沒取 `isError`——
  那個 expenses query **整個把 `error` 丟掉**（`const { data } = await …`），
  讀不到就回 `[]`，於是拿一份空帳算出「不用付錢」。已改成 `throw`。
- **AF-3／AF-4** 行程詳情與分享頁：`isError` 先走錯誤區塊，`!trip` 才走 `NotFound`。
  `NotFound` 的文案一個字沒動——要修的是「什麼情況下顯示它」。
- **AF-5** 四條寫入路徑（存檔／刪除消費／刪除行程／結算）統一包 `guardedWrite`：
  送出前擋離線 ＋ 15 秒逾時。包成一個函式而不是各寫兩行——四份遲早漏一個。
- **AF-6** 五句 toast 縮到 7–9 字（原本 30 字，toast 只活 2200ms，讀不完就消失）。
- **AF-7** 新增 `usePullToRefresh`，掛在行程列表與行程詳情。離線時維持看得到舊資料。
- **AF-8** 樁補兩個洞：`single`／`maybeSingle` 現在吃 `?fail=read`；新增 `?fail=hang`。
- **AF-9** 開工小程式已啟動，**到期時間 2026-09-11 01:12**（8 小時後自己結束）。

## 動到的檔案
`src/pages/`：TripListPage／ExpenseListPage／SettlementPage／SharePage（＋三個 test）
`src/components/shared/ReadError.tsx`（新）、`src/hooks/usePullToRefresh.ts`（新）
`src/lib/messages.ts`、`src/index.css`、`src/test/harness/installStub.ts`
`src/test/fixtures/screens.json`（重新擷取）、`Tripay_原型.html`、`.claude/kickoff.sh`（新）、`.gitignore`

## AF-9 的三個停止條件我都實測過（不是讀碼推論）
在**不啟正式那一支**的前提下各起一次：已過期 → 0 個在跑；到期檔不存在 → 0；
`_停watcher.txt` 在 → 0。然後才用 8 小時到期啟動，並丟一個 `_開工.txt` 進去，
確認它**真的喚起 session、執行、把檔案歸檔到 `Claude outputs/`**，之後自己還活著。

## 我自己覺得有疑慮的地方

1. **`/privacy` 與 `/terms` 現在是 301 → 200，不是直接 200。**
   部-1 那一輪推上線後我量到的。Google 的爬蟲會跟隨 301，所以能用；
   但如果 Cowork 的巡檢斷言「直接 200」，它會判紅——那是斷言沒算到目錄轉址。
   **後台三個欄位填帶斜線的網址就直接 200**，不用再改 build。
2. **下拉重新整理只在真機（觸控）有效。** 我用 `touchstart/move/end` 實作，
   無頭 Chrome 沒有觸控事件，**這一項的行為機器驗不到**，只驗得到「hook 掛上去了」。
   桌機滑鼠下拉不會有反應，那是預期的。
3. **AF-1-4 的加號置中我只能量到 CSS 層面**（flex 置中 ＋ `svg{display:block}`）。
   「看起來有沒有正」要 Cowork 用像素量或 Rozi 用眼睛看。
4. **`?fail=hang` 目前沒有斷言在用。** 我把它做出來了（AF-8 要求），
   但「按了停在存檔中…」那個情境的斷言要能等待逾時 15 秒，
   我沒有把它加進常設套件——跑一次就多 15 秒。建議 Cowork 決定要不要單獨跑。
5. **這一節指令說「做完 push」，與方案 A（等 `_可以推了.txt` 才推）衝突。**
   我照這一節的指示 push，因為它比較新而且明確；但兩份規則現在不一致，
   建議擇一寫進 `CLAUDE.md`。

## 驗證
`實作_失敗路徑測試.cjs` 52 項全綠；28 支回歸全綠；`vitest` 343 綠、`tsc` 0 錯、`npm run build` 過。
自查那 12 條停止條件逐條對過（第 8 條的五句文案實測 7／9／8／7／9 字）。

## 這一輪第四次踩到同一種陷阱
停止條件用 `git grep` 出現次數判定時，**我自己的註解引用那個字串也會被算一次**。
這次是「移除的按鈕文案」「那句問句」「blur(0.8px)」「呼吸動畫的 class 名」四個都中。
前三次是 `rounded` 變數、`✕`、被刪掉的 hook 名字。**已經是常態，寫註解時要先想到。**
