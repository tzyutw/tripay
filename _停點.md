# 停點報告

> 2026-09-04　狀態：**等 D（分帳模型選版）與 E（資料層提案）的裁示**

## 一句話
#10 v3 六項全數落地；D、E 兩個決策節已交付，**只畫不改邏輯**，等你裁示。

## 一、P3 查證：引擎不用修
`calculate-settlement/index.ts:76` 已有 `.eq("twd_pending", false)`，**待填筆本來就跳過，不會 422**。
422 的真正觸發條件是 `twd_amount IS NULL` **且** `twd_pending = false` 的**孤兒狀態**
（第 90 行 `Number.isFinite(null)` 為假）——正是 B 的 ⚠️ 講的那個。
所以 toggle 可以拿掉，**但旗標必須照寫**，這已列為回歸測試項（檢查表 #62）。
個別金額同理：`split_pending=true` 時引擎讓該人 cost=0（`settlement-engine.ts:114-119`），
E 的提案與現有引擎相容，不用改引擎。

## 二、A–C 已落稿
- **A**：刪除確認改打「刪除」二字，S-03c 與 S-09 主卡一致
- **B**：S-04 兩個 toggle 移除，空欄即待填；新增 **S-04-29** 記錄 P3 查證
- **C**：S-02b-11「這趟的支付方式」（預設現金、信用卡，可新增與拖曳排序，
  已被使用的不可刪）；S-04-9 改為快選＋「＋ 管理」

## 三、D 分帳模型兩版對照（待裁示）
兩版都用五個情境走過一遍，附誰付／誰分攤／生不生欠款／進不進結算／badge。

**走查後看得出來的差別，集中在兩列：**
- **小美個人購物**：版本一要使用者**先認得「只算一個人」是一種型**再選人；
  版本二只是「挑選 → 勾一個」，沒有新概念
- **兩人汗蒸幕**：反過來——版本一「一起分再排除」比版本二少一次決策，因為**預設就是全員**

「只算我」在版本一已改名為**「只算一個人」＋選人**，理由寫進稿裡：
你是幫全家登記整趟的帳，**記帳者往往不是花錢的人**。

兩版都保留「當場各付」與「贊助／回饋」——它們是**正交旗標**，不屬於任何一版的型別。

## 四、E 各自金額幣別（待裁示，且已列 P1）
現行 `split_amount` **不帶幣別**，引擎當台幣加總，比對列寫 `$`，
但**表單從來沒說要填台幣**——日幣行程照當地金額填會**靜默算錯**。
兩版稿都畫出幣別切換，比對列同幣別比對。
已寫進 `Tripay_問題優先序清單.md` 的 **P1-0**（既有風險，非新功能）。

## 五、資料層提案四項（只提案，未出 migration）
- **C-①** 建議 `trips.payment_methods jsonb`，不另開表——這是「一趟的設定」不是共用實體，
  複製行程時自然跟著走，且**每張新表要付四條 RLS 的代價**（DELETE 那條已咬過五次）
- **C-②** `payment_label` 存名稱快照，`payment_method` enum 留著當分類；
  **新增時要選分類**，不能從名稱猜（「交通卡」是儲值卡、「聯名卡」是信用卡）
- **C-③** 建議**擋下刪除＋可停用**：擋下與成員規則一致，但要有出路，
  否則「這趟不用交通卡了」只能讓它永遠佔版位
- **E** 新增 `split_amount_foreign`，`split_amount` 語意維持台幣（動它要重跑四趟 verify）

## 六、artifact
2facaecf-d131-41e2-8048-c58fe7a37e2c（label：v5-第三批）
新增兩節：**分帳模型 · 兩版對照**、**資料層提案**

## 七、文件
`_盤點_畫面功能.md` 重出 255 列｜`專案狀態.md` 補 6 條決策｜
`決策符合性檢查表.md` v1.6 補 #60–65（累計 65 條）｜
`_實作批次工作清單.md` 補第三批裁定｜`Tripay_問題優先序清單.md` 補 P1-0
