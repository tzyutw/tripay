# 停點｜實作-B～N ＋ 類別 emoji 圓圈（**全部已上線**）　2026-09-06 18:30
（歷輪完整內容在 `Claude outputs/_實作B進度.md`，這裡只留最新一輪與常設事項）

## 追加：類別 emoji 的識別圓圈（Rozi 2026-09-06，接在實作-N 之後）
未編輯時是裸 emoji、看不出可以點；成員識別一直都有框。**兩種狀態統一成 28px `.avatar`**，
切換編輯時外框不變 → 整列不跳動。互動與 `➕` fallback 本來就一致，沒動。

量到的數字：**未編輯 28×28 列高 44｜編輯中 28×28 列高 44｜焦點落在 input**
（改動前列高 46，差 2px 在容許範圍內；28px 沒撐高那一列，不需退回 24px）。
原型同步，`data-av="exp:g"` 保留——打字即時換 emoji 的處理函式靠它。

驗證：`實作_輸入框與選單測試.cjs` 51→66 條（9 條圓圈斷言＋s04 三寬度不橫向捲動）。
**反向驗證兩條都紅**：拿掉 `.avatar` → 5 紅；編輯狀態改 20px → 1 紅。
界線遵守：`useInlineEdit`／`emojiForTitle`／成員 `<Avatar>` 都沒碰，
`git diff --stat` 只有三個檔。成員 `<Avatar>` 32／28 不一致寫進 `_延後與不做清單.md`。

## 實作-N（同一次「收」，已上線）
匯率的「1」沒自動帶值：原型 `tripToForm` 依 `oneSideOf()` 帶 1，實作漏了，
另一欄灰色 placeholder「1」看起來像已填好，Rozi 只填一欄 → `tripRate()` 回 null、
全程無警告、帳一直顯示「還沒填」。改 `defaultRates()`＋placeholder 依 `oneSideOf`
＋只填一欄出「還差一欄，兩邊都填才換算得出來」；`tripRate()`／`calc()` 一行沒動。
類別 emoji 字典 5→13 條，原型與實作同一份（走鐘就紅）；推不出來就 ➕，不硬猜。

> ⚠️ 反向驗證「placeholder 改回寫死 `isTwd`」第一次**沒紅**——fixture 是 KRW，
> `oneSideOf('KRW')==='twd'` 剛好同結果。加 `?cur=JPY` 才驗得出來。
> **對照組要挑得出差別的那一組。**

## 全套驗證現況
`tsc` 0 錯｜`vitest` **263 綠**無 Unhandled｜`pnpm run build` 過｜版面回歸 1163｜
輸入框與選單 **66**｜查不到與把手 58｜成員識別 79｜原型真實形狀 50｜資料形狀 9｜
token 52｜型別對帳 29｜文案唯一性 13。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome
量不準（#32）——中文日期會不會比框寬、空值與有值的框寬一不一樣、文字有沒有垂直置中。
**新增**：S-04 類別 emoji 圓圈在 iPhone 上大小是否合適、點下去鍵盤跳出時整列會不會跳動。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal）｜`.ic2::after` 在 `index.css` 有兩份定義｜
`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜寫死色碼依裁示不處理｜
成員 `<Avatar>` 編輯 32px／非編輯 28px（已入 `_延後與不做清單.md`）。

## 已結案的舊事
`實作_212對帳表.cjs` 已由 Cowork 2026-09-06 作廢（判定規則會自動蓋章），
驗收判定改由 Cowork 寫、Cowork 跑（`_實作階段運作方式.md`「六之零」）。
S-06 hero 不帶成員 emoji、`➕` 維持原型——Rozi 2026-09-05 都已裁示。
**真名**：`Tripay_原型.html` 的真名從未 commit 也從未 push（已查證）；
散在其他歷史文件裡的屬於改寫已推歷史＝不可逆，**已交 Rozi 決定**。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`750d857` A｜`723466a` B＋C｜`8cba1ef` D｜`355a64e` E｜`3e01b02` CI lockfile｜
`f8c00ca` G｜`03d36ca` N｜（本次）類別 emoji 圓圈。
**CI 走 `pnpm install --frozen-lockfile`，這個 repo 一律用 pnpm，推之前先跑 CI 那兩道指令。**
