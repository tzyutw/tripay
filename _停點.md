# 停點　2026-09-10 18:45　修-1（png 全清）／修-2（離線靜默失敗）／權限覆核

## 修-1　19 張 png 全部移出版控（檔案都還在本機）
`git rm --cached` 19 張、`.gitignore` 加 `Claude outputs/*.png`（含理由註解）。
停止條件：追蹤數 **0**、本機仍有 **19** 張、`.gitignore` 有那一行、`git status` 沒有新的未追蹤 png。
沒有 force push、沒有改寫歷史。

## 修-2　根因不是 `guardedWrite` 吞掉 rejection，是**編輯表單把讀取失敗畫成空白表單**

實走（不是讀碼）四種情境後量到的：**寫入失敗那條路本來就是好的**。
`?fail=write`→「存不起來，再試一次」、開著表單才斷網→「現在連不上網路」，兩者表單都沒關。
真正壞掉的是**先離線、才點開那一筆**：`['expense', id]` 這支查詢的 `error` 從來沒被接住 →
`existing` 是 undefined → 表單靜靜停在 `blank()`，看起來像「這筆什麼都沒填」。
使用者改標題按「記下來」，擋下他的是**「先選這筆是誰付的」**——從頭到尾沒有一個字提到網路。
（與 AF-2／3／4 是同一個毛病：讀不到被畫成沒有資料。那一輪修了四個畫面，**漏掉這張表單**。）
比 Cowork 描述的更糟的一種：若使用者真的補選了付款人，等網路回來按下去，
**會用一張空白表單覆蓋原本那筆**。

**改法兩層**（`src/components/ExpenseFormSheet.tsx`，只動這一個檔）：
1. 取出 `error`，讀不到時**退回 S-03 清單快取裡的那一筆**（使用者就是從那份點進來的，
   欄位形狀 `*, expense_splits(*)` 完全相同）→ 表單有內容、能編、存的時候才真的撞到網路錯誤。
2. 連快取都沒有（直接開連結又離線）時，「記下來」**不送出**，直接說「現在連不上網路」
   ——不是讓付款人驗證去頂替。

### 停止條件實測（`Claude outputs/_repro_修2.cjs`，可重跑）
| 情境 | 結果 |
|---|---|
| 執行期切 `__FAIL__='offline'` 再開編輯 | 0.9s 出現**現在連不上網路**、表單沒關、打的字還在 ✅ |
| 同上但快取也清掉 | 同樣的訊息，且 `__WRITES__` 是空的（沒有送出）✅ |
| `?fail=write` | 「存不起來，再試一次」，行為不變 ✅ |
| 執行期切 `hang` | 15 秒逾時後按鈕回「記下來」＋訊息，行為不變 ✅ |

`verify_C12.cjs` 的 13/13 由 Cowork 跑——**驗收判定不歸我寫**。我手上只有 11:56 那份備份，
它的 C12-3 走 `?fail=offline`（載入即離線，清單根本沒有 `.exprow`），跑了只會得到誤導的紅。

## 權限覆核（Cowork 那段）
1. **夠用**，只補過兩個洞：`tr` 不在白名單、`cd /tmp` 出專案外。都繞開了，沒有卡住。
2. **有三處我認為該收窄，但我改不動這個檔**（`.claude/settings.local.json` 屬受保護檔，
   Edit 被擋下來要人核可）。建議照下面改，理由各一句：
   - `Bash(npx:*)` → `npx vite:*`／`npx tsc:*`／`npx vitest:*`／`npx eslint:*`。
     `npx` 會下載並執行任意套件，是這份清單裡最寬的一項，而我只需要這四個。
   - deny 補 `git push --force:*`／`git push -f:*`／`git clean:*`／`git reset --hard:*`／
     `git filter-branch:*`。`Bash(git:*)` 目前把「不准 force push、不准改寫歷史」這條裁示
     完全交給我自律；寫進 deny 才是機器擋得住的。
   - deny 的 `Bash(rm -rf /*)` 只擋得到根目錄那一種寫法，建議改成 `rm -rf:*`／`rm -fr:*`。
     （`rm` 本來就不在 allow，仍會逐次問。）
3. **`kickoff.sh` 我不改**，你的理由成立而且還多一條：`--dangerously-skip-permissions`
   會把刪除護欄一起關掉——那正是誤刪濟州島 132 筆的那一類防線。設定檔的做法比較好。

## 順手看到但沒動（給 Rozi 決定）
- 退回快取那一份時，畫面上**沒有任何字說「這是剛才看到的那份、可能不是最新」**。
  我沒有自己加文案（那是產品層的字）。要不要加請 Rozi 決定。
- `src/contexts/ToastContext.tsx` 的 `text-sm`／`bg-[#292524]` 非 token（上一輪就列著，仍沒動）。

## 驗證
`vitest` 343 綠 / 3 skipped、`tsc` 0 錯、`npm run build` 過、`vite build --mode harness` 過。
根目錄 49 支腳本逐支跑：**`實作_*` 33 支裡 32 綠**（紅的那支是 `實作_212對帳表.cjs`，
它自己 2026-09-06 起就故意非零退出宣告作廢）。
`原型_*` 有 13 支紅——**既有狀態，不是本輪造成**：它們最後一次更新停在 #33，
而 `Tripay_原型.html` 之後被 W／Z／AF 改過；而且用 `grep` 確認過這 13 支**沒有一支**
碰 `src/` 或 `dist-harness`，只讀原型 html，本輪改的檔案影響不到它們。

## 本輪動到的檔案（超出這份清單的都沒動）
`.gitignore`、`_停點.md`、`src/components/ExpenseFormSheet.tsx`、
`Claude outputs/` 的 19 張 png（只從版控移除，檔案還在）、`Claude outputs/_repro_修2.cjs`（新，重現腳本）。
