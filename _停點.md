# 停點｜實作-B～M（**已上線**）　2026-09-06 16:40

## 實作-M　查不到行程的畫面 ＋ 把手可點區 ＋ 重複定義 ＋「自己的」

### 1. 查不到行程永遠卡在「載入中」
根因：`.single()` 查不到列時回 **406 並拋錯**，錯誤沒被 render，spinner 一直轉。
指令說 `ExpenseListPage:44`／`SettlementPage:88` 已是 `maybeSingle`——
**兩支其實都還是 `.single()`**（行號位移），那就是兩個 406 的來源。都改掉。

新增共用元件 `shared/NotFound.tsx`（文案一字不改）：
S-03 帶「回到我的行程」（`width: min(240px,100%)`）、**S-06 不放任何按鈕**。
版型沿用 `.empty`，**沒套紙飛機動畫**——那是邀請感，用在錯誤畫面上語氣不對。
harness 加 `?trip=missing`。

### 2. 拖曳把手可點區只有 20×30.5
與 `.rmbtn` 同一套 `::after` 撐到 44×44。量到：`::after` **44×44**、
box 仍 **20** 寬（沒撐大）、同列「還沒用到」仍 **1 行**、`touch-action: none` 保留。

### 3. `.rmbtn` 重複定義
第二份是**抽取器從原型搬來的**——直接刪下次重跑又回來。
改成抽取器排除 `.rmbtn`／`.grip`（`src/index.css` 有手寫的權威版，用 `var(--h-tap)`）。
`^\.rmbtn \{` 與 `^\.rmbtn::after` 各剩 **1 次**，可點區仍 44×44、色仍 `var(--gr)`。

### 4. 消費列的「自己的」標記
付款人＝被算的人 → **自己的**；付款人≠被算的人 → **只算 {名字}**；
歷史 `personal` → **自己的**。一律 `pill gr`（灰）。
**沒有新增分帳型態、沒有動結算邏輯**——`summary.ts` 只多帶一個 `personal` 顯示旗標。
原型與 harness 的示範資料各補兩筆，三種情況都畫得到。

> ⚠️ 停止條件 6 寫「三顆 badge 的底色與『各付各的』相同（灰，非藍）」——
> **「各付各的」實際上是藍的**（`pill ind`）。依括號裡的「灰，非藍」為準，
> 對照組改用同為 `pill gr` 的「當場就清了」，並加一條斷言確認藍灰真的不同色。

### 量到的數字
S-03 查不到：無 spinner、按鈕 **240px**、無紙飛機｜S-06 查不到：**0 顆** button/a（正常狀態 2 顆）｜
`.grip` ::after **44×44** / box 20 寬｜`.rmbtn` ::after 44×44、`rgb(106,121,128)`｜
三種 badge 底色 `rgb(244,245,241)`（灰），「各付各的」`rgb(238,245,251)`（藍）。

### 反向驗證（四條全紅）
拿掉查不到分支（10 紅）｜分享頁加一顆按鈕（3 紅）｜拿掉 `.grip::after`（1 紅）｜
拿掉付款人判斷（3 紅）。另有兩條結算不變性測試（personal 拿掉後淨額與轉帳完全相同）。

### 測試
vitest **246 綠**、無 Unhandled Errors｜版面回歸 1163｜查不到與把手 58｜輸入框與選單 32｜
成員識別 79｜原型真實形狀 50｜資料形狀 9｜token 52｜型別對帳 29｜文案唯一性 13。

## 實作-L（上一輪，已上線）
輸入框回歸（`:where()` 降權重連 preflight 都輸掉）｜匯率打字失焦（元件內部定義函式元件）｜
S-02b-14 編輯行程可改名｜⋯ 改成獨立頁面 `/trips/:id/more`。

## 前幾輪（都已上線，細節在 `Claude outputs/_實作B進度.md`）
**B–C** 全站對齊原型＋引擎查證＋版面回歸測試｜**D／E** 複驗退回兩輪｜**F** 推上線｜
**G** 分享頁轉帳列重複 12 次｜**H** 日期欄位沒接上 `.datefield`、移除 G-09 預填成員｜
**I** 五個線上實測 bug｜**J** 成員識別全站貫徹（＋第一輪複驗退回）｜
**K** 假資料換成真實形狀（`?members=noemoji`／`?expenses=none`／`?rate=half`／`?pays=none`）。

**真名外洩處置**：查證 `Tripay_原型.html` 的真名**從未被 commit、也從未 push**
（`git log --all -S` 0 個 commit、`origin/main` 上是 `Alex/Robin/Sam/Kai`）。
`.gitignore` 那幾行已移除——原型本來就在版控裡。
散在其他歷史文件裡的真名屬於改寫已推出的歷史＝不可逆，**已交 Rozi 決定**。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`750d857` 實作-A｜`723466a` B＋C｜`8cba1ef` D｜`355a64e` E｜`3e01b02` 修 CI lockfile｜
`f8c00ca` G｜（本次）H。

**前兩次部署失敗是我造成的**：CI 走 `pnpm install --frozen-lockfile`，我卻整輪用 npm，
`pnpm-lock.yaml` 沒跟上。**往後這個 repo 一律用 pnpm，推之前先跑 CI 那兩道指令。**

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

## 已結案的舊事
`實作_212對帳表.cjs` 已由 Cowork 於 2026-09-06 作廢（判定規則會自動蓋章），
驗收判定改由 Cowork 寫、Cowork 跑（`_實作階段運作方式.md`「六之零」）。
S-06 hero 不帶成員 emoji、`➕` 維持原型——Rozi 2026-09-05 都已裁示。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
