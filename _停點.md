# 停點報告

> 2026-09-04　狀態：**#21 完成，等 Rozi 看過再做批三**

## 你指出的漏洞成立
五情境測試斷言的 `c.debts.length` 是**那一筆內部的分攤關係**，
不是結算結果。`settleTrip()` 裡的 `if (e.onSpot) return;` 被刪掉，那組測試不會失敗。

## 補上四條結算層測試（驗 `settleTrip()`）
在同一個行程裡放多筆消費，比對**每人淨額的金額**（不是筆數）：

| 測試 | 內容 |
|---|---|
| **R10** | 加入一筆 `onSpot` 前後，四人淨額**逐一相同**（用 JSON 全等比對，不是比長度）|
| **R7 結算面** | 加入一筆台幣總額空白的消費前後，淨額**完全不變** |
| **Σ 淨額 = 0** | 基準、加 onSpot、加 pending、混合六筆（含各付各的／只算一人／贊助）各驗一次 |
| **結算恆為台幣** | 顯示幣別切到外幣後，淨額與轉帳明細**逐字元相同** |

## 反向驗證：測試真的抓得到（要求 4-2）
把 `settleTrip()` 的 `if (e.onSpot) return;` **暫時註解掉**後重跑：

```
加 onSpot 前：{"Ning":2481,"Ziyu":-207,"Xiu":-827,"Mei":-1447}
加 onSpot 後：{"Ning":3681,"Ziyu":-607,"Xiu":-1227,"Mei":-1847}   ← 淨額被汙染
❌ R10：當場各付各的不得影響結算淨額
通過 109　失敗 2
```

**新測試立刻失敗**；同一次執行中，舊的五情境測試（`c.debts.length === 3`）
**照樣全過**——正好證明你說的那個缺口是真的。

**已還原**：程式碼與備份 `diff` 一致，`git diff --stat` 對 `Tripay_原型.html` 無任何差異，
守衛回到原位（第 1615 行）。

## 改掉誤導的名稱
五情境日誌的「欠款」改為「**單筆分攤關係數**」，並在該行加註解說明
它不等於結算結果、結算由 `settleTrip()` 決定。

## 測試（四組全過，批二 103 → 111 項）
批二 **111**｜批一 60｜輸入 34｜版面 18

## 沒做的
**只加測試與改名稱，沒有動任何計算邏輯**（護欄要求）。
未改 `src/`、未跑 migration、未刪檔。
