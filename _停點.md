# 停點報告

> 2026-09-05　狀態：**#29 全部做完（29-1～29-10），等 Rozi 在手機上看**

## 用手機打開這個網址，會自動變成真機模式
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**

不必按任何東西。手機上打開就會：鋪滿整個螢幕 1:1（不再是縮小的模擬框）、
稿件元素（編號、右側清單、章節標題）全部藏起來、一次只看一個畫面，
底部有一條可以收起來的切換器換畫面。桌機另有「真機模式」開關可以強制進出。

## 你問「最小字級是不是 14／12」——方向對，數字不是
查了 Apple 官方文件：**iOS 內文預設 17pt、最小 11pt**（macOS 13／10，watchOS 16／12）。
網頁上 1pt ≈ 1px。另外有一條網頁特有的硬限制：**iOS Safari 在點進字級小於 16px 的
輸入框時會自動放大整頁**——記帳表單全是輸入框，這不是美感問題。

**量過之後發現不是某幾處特別小**：14px 以下有 **10 個不同字級、沒有階梯**，
內文只有 12.5px，**整體約原生的七成**。這才是你一路覺得字偏小的來源。
定成六階：**12／13／15／16／17／20**，全部用變數，`.ui` 內不再有寫死的字級。
你點名的五處：出遊／共記了／最大手筆／任何人免登入可看都是 **13px**（原 9～10.5），
S-03c-1 對話框標題 **17px**（原 13.5）；消費列的付款人 9.5 → **13**、名稱 12.5 → **15**。

**放大三成後版面沒有破**：三個寬度都沒有橫向捲動、沒有文字貼邊，逐一截圖看過。

## 你說「提示文字比選項還深」——是，而且是兩個獨立的錯
1. **placeholder 從來沒有被設定樣式**，用的是瀏覽器預設的 `#757575`，
   比「分帳方式」的未選項目還深，而且是純中性灰、跟 Tripay 帶青藍的灰不同色系。
2. `.seg` 未選用 `--gr`、`.chip` 未選用 `--md`——同一種東西兩個顏色。
訂成三階：內容／可點未選／提示。實測亮度 0.011 < 0.099 < 0.183，層級正過來了。

## `--gr` 那題，白話是這樣
灰字在白底上偏淡，無障礙標準要求深淺差距至少 4.5 倍，原本只有 3.7。
**實際後果**：戶外、車上、螢幕調暗時那些灰字會看不清楚——而 Tripay 就是旅遊時用的。
只降一階明度（`#78878F` → `#6A7980`）就到 **4.51**，並排幾乎看不出差別。已改。

## 其餘
- 日期移出欄位序列，改成標題下「記在 3/16（一）」的窄狀態列（一天只設一次的東西，
  不該擋在每筆都要填的「誰付的」前面）。回國後補帳**不自動修正、不警告、不擋**
- 「可留空」全站 0 次；金額兩欄 placeholder 改 `0`，「填一邊就好」整個區塊講一次
- **S-06 統計卡改成與 S-03 同一個元件**（`statCard()`），人均取消；分享頁唯讀，
  每人分擔列不可點、註腳不邀請點擊
- 分頁改 `.tabs`（iOS 淺灰底軌＋白色 pill），與幣別切換的 `.seg` 分成兩套
- **S-06 分享頁上漏印了「S-06-4」「S-06-6」兩個編號**（操作模式下就看得到，
  旅伴點連結會看到）。根因是隱藏規則寫成直接子選擇器，多包一層就蓋不到。已修，
  並加了永久斷言：操作模式下可見的編號徽章必須為 0
- ⋯ 選單每項配 16px icon；「封存行程」用 Feather 的 `archive`（不自己畫）

## 測試（九組全過，新增一組 114 項）
60／132／54／34／18／45／79／65／**114**。原型 212 項，盤點表與盤點檔都已同步。
