# 停點報告

> 2026-08-31 02:45（GMT+8）　狀態：**#9 草稿 v2 尚未核可，未執行；等核可或修正**

## 一句話
#9 標的是「草稿 v2（待 Rozi 核可）」，沒過核可閘門，所以**沒改任何稿、沒改任何碼**；
但把它依據的事實全部驗過並落檔 `_稿碼對照.md`，讓你核可時有正確行號可依。

## 一、為什麼停下
CLAUDE.md 協定寫明「佇列一律先經 Rozi 過目核可才寫入，終端機收到的每一節都視同已核可」。
這一節自己標了**待核可**，等於明說還沒過閘。所以我只做**唯讀掃描**——
掃描是核可的依據，做了不浪費；改稿改碼則等你一句話。

## 二、驗證結果：草稿有 2 處要修正、1 處要補

**❌ A「稿上 4 處 ✈️ 殘留清掉」是錯的——4 處都不該動**
- 2 處在盤點表裡，是「S-00-1 App icon ✈️」「S-02-5 預設 ✈️」的**移除紀錄敘述**，
  清掉就查不到當初移除了什麼
- 2 處是消費列「機票 ×4」的**類別 emoji（`category_emoji`）**，跟行程 emoji 無關，清掉＝誤刪

稿上真正的行程 emoji 殘留是**另外 4 處，而且不是 ✈️**：
S-01 三張卡的 🏝️🗻🍜（行 398／408／418）與 S-06-3 分享頁 hero 的 🏝️（行 1353）。

**⚠️ A 漏了 5 處——正好是 D 條要防的那種漏**
`trips.emoji` 全 src 共 **7 處**，草稿只列了顯示端 2 處，漏掉 `TripFormSheet` 的
`:46` 初值、`:158` 編輯寫入、`:216` 建立寫入、`:347` 按鈕 UI、`:559` picker 傳值。
只清顯示不清寫入，**新行程仍會存 `'✈️'`**，等於只退了一半。

**⚠️ E 的 G-05 行號**：141／285／294 正確；橫幅是 **:394 不是 393**，另外**漏了 :408**
的「之後再說」按鈕。

## 三、驗證通過的部分
- 幽靈卡 `LoginPage:69`（區塊 68–88）、`TripListPage:147` 保留 — 都正確
- `TripListPage:134` tabular-nums 套在日期上 — 正確
- **F 全站掃完**：`tabular-nums` 共 17 處，**16 處是金額（合規）**，
  只有 :134 那 1 處要清。所以 F 的實際工作量是 **1 行**，不是全站大改

## 四、順帶掃到、建議一併列管的同類問題
`tailwind.config.js:7–8` 與「不載中文 webfont、標題用 PingFang TC」決策不符：
`sans` 把 `Noto Sans TC` 排在 `PingFang TC` 前，`serif` 是 `Cormorant Garamond`。
`font-serif` 還有 **8 處**在用（EmojiPicker、兩個 FormSheet、LoginPage×2、
TripListPage、SettlementPage×2）。要不要納入 #9，你決定。

## 五、一個矛盾要你裁示
B 條要求保留「S-03E 空狀態成員列（決策 D）」，但**現況那裡沒有成員列**——
空狀態只有兩行文字，成員 emoji 在行程頁 hero 上。
所以是①決策 D 一直靠 hero 滿足（那 B 條等於確保 hero 不動，已確認不動），
還是②原意是空狀態本身要顯示成員（那是**要新增**、不是保留）？併第三批裁示。

## 六、產出
`_稿碼對照.md`（107 行，標為草稿）：完整稿↔碼對照，含上述修正後的正確行號、
7 處 emoji 清單、17 處 tabular-nums 逐處判定、以及打磨輪至今 18 項稿上決定的對應位置。

## 七、下一步
你核可 #9（含上述修正）後，我一次做完 A–G 並重渲染受影響頁面。
