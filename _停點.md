# 停點報告

> 2026-09-04　狀態：**批二修正完成，等 Rozi 看過再做批三**

## 交付
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**（同一連結）

## 1. Icon 全站換成 Remix Icon 的 SVG
用 fill 版統一風格，path 內嵌進單檔，**檔案註解保留 Apache 2.0 授權聲明**。
size 與顏色走 `currentColor` 與統一參數，不逐處寫死。
**手機框內已無任何文字符號充當 icon**（測試逐字元斷言 `‹ ✎ ⧉ ⚙ ▾ ▲ ▼`），
目前 25 個 SVG icon。成員識別與封面的 emoji 依既有決策不動。

## 2. 統計卡可展開
預設只顯示總花費。**有人被標「約」時，收合列上有一個「約」提示塊**（不是灰字），
點開才是每人分擔列。

## 3. 幣別切換真的能切
切到外幣時整頁金額用行程現金匯率換算，切回台幣還原。**結算恆為台幣不受影響**。
沒設匯率時該選項停用並說明原因。

## 4. 記帳不設匯率（規格變更已落地）
移除 S-04 的就地設定入口與**單筆覆寫**，改成一條與支付方式無關的規則：
有台幣就用台幣、只有外幣用行程匯率、兩者都有用台幣。沒設匯率時只給一個跳去 S-02b 的連結。
**全站匯率輸入框只剩 2 個，都在 S-02b**（測試有斷言）。

## 5–6. 選取語彙統一 ＋ 一起分回到定案版
全站「選取／未選取」只剩一套（未選描邊、選了填實）。
「一起分」改回定案的 **R2：預設不展開名單，只顯示「N 人均分 · 每人 $X」，點「要排除誰？」才展開**。
我照指示逐條對過 `分帳模型原型.html` 的 R1–R8，**除了這條，其餘沒有偏離**。

## 7. 動態 icon（只做兩處）
S-03 空狀態與 S-05 結算完成，內嵌 SVG＋CSS 動畫、沒有外部庫。
結算那個是「一疊帳摺起來、寄出去」的兩段敘事。**兩處都包在
`prefers-reduced-motion: no-preference` 裡**，關掉動態效果就不播。

## 10–11. 「大家各付各的」移位 ＋ 提示句減量
它移進「誰付的？」的最後一個選項，用分隔線隔開，**三型都能選**，灰字一併移除。
提示句 **9 句砍到 3 句**：只留「還沒設匯率算不出台幣」「補上外幣總額才算得出」「贊助會記成負數」。
被砍的資訊改用 placeholder 與「自動」標記做進欄位本身，不是丟掉。

## 測試（四組全過，批二那組 39 → 71 項）
| 測試 | 項數 | 結果 |
|---|--:|---|
| `原型_批二回歸測試.cjs` | 71 | 全過 |
| `原型_批一回歸測試.cjs` | 58 | 全過 |
| `原型_輸入回歸測試.cjs` | 34 | 全過 |
| `原型_版面回歸測試.cjs` | 18 | 全過 |

新增測項逐條對應你的停止條件。**其中一項抓到我原本的測試寫錯**：
舊測試斷言「刷卡不套用現金匯率」，但 #17-4 已把規則改成與支付方式無關——
是測試過期不是程式錯，已改成新規則並補齊三種支付方式的斷言。

## 沒做的
未改 `src/`、未跑 migration、未刪檔。字體維持現況等你選定。
「我的花費／哪位成員是我」的存廢照現況，未自行增刪。
