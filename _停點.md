# 停點報告

> 2026-09-05　狀態：**#33 全部做完（33-1～33-9）。有一項要你在手機上確認才算數**

## 手機打開這個網址（同一個連結）
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**

## ⚠️ 這一項要你在手機上看過才算數：日期欄位
**桌機量不出這個問題。** #32 我回報「兩框一樣大、文字已置中」，那是在 Chrome 上量的；
你的截圖顯示 iOS 完全不同——原生 date 欄位**讓內容決定寬度**，
中文日期比框還寬，空值時渲染「年/月/日」寬度又跟有值時不一樣。

已關掉系統外觀、改由欄位分配寬度、`min-width:0`，並**補回一個日曆 icon**。
點擊仍會叫出 iOS 的日期選擇器。**請在手機上確認三件事**：
① 兩框一樣寬 ② 中文日期不會撐爆 ③ 點了還叫得出選擇器。

**這件事已寫成常設規則**：牽涉原生表單控制項的問題，不得只靠機器斷言結案。

## 你問 placeholder 為什麼比提示句大——那是刻意的
輸入框裡的文字**必須 ≥16px**，否則 iOS Safari 一聚焦就會放大整頁；
而 placeholder 的字級**必須等於輸入值**，否則一開始打字文字會跳大。
**而且 placeholder 不是提示句**，它是「即將輸入的值」的預覽，佔的是輸入值的位置。

**但你這一問查出另一個真的不一致**：`.note`（「先記著了。補上台幣金額…」）
**從來沒有樣式定義**，繼承了 15px；`.hint` 是 13px。同樣是提示句、兩種大小，全部統一成 13。
**依指令沒有加粗或改色補償**——警示那幾條現在只剩 icon 在撐份量，要不要補回請你看過再說。

## 消費列倒序
分組由新到舊、「出發前」沉到最底；**同一天之內依記錄的先後由新到舊**——只倒轉分組
還不夠，今天剛記完的那一筆要出現在整頁最上面。S-03 與 S-06 共用同一段排序。

## S-04 的支付方式：兩處從來沒有接起來過
S-02b-11 讀的是這一趟的清單，S-04-9 讀的是**全域常數**（固定現金／信用卡／儲值卡）。
所以「儲值卡」才會冒出來。而且比看起來嚴重——**用清單裡沒有的方式記了一筆，
那筆在編輯頁看不到、刪不掉、統計歸不了類**。已改成一律讀行程清單、順序跟著走、
預設是第一項。全域常數刪掉了。**這不只是原型的問題，正式程式碼同樣是寫死的（漏做）。**

## 其餘
- **`⠿` 是 Unicode 字元不是 icon**（樣子由系統字體決定、大小跟著字級走）——換成 Feather 的
  `menu`。**順手掃到第二處**：「新增」動作全站有兩種畫法，四處全形「＋」改用 `add` icon，
  逐一看過視覺重量，**不需要微調**
- 分享選單的灰字原本與標題**在同一行**，讀起來黏成一句；改成放標題下方
- 「這趟怎麼結算？」的選取記號原本是**一個 chip 裡印著「選」字**（兩顆都寫「選」，
  要靠顏色才分得出來）——改成標準單選鈕，未選空心圓、選中圓內實心點
- 圓角定案 **C（10/20）**，切換器已移除

## 逐頁截圖（依 #32-10 的規則）
`…/scratchpad/shots33/` **24 張**（`33_桌機_s00…s07`、`33_真機_s00…s07`）

## 測試（十三組全過，新增一組 39 項）
60／132／54／34／18／45／79／65／114／54／32／64／**39**
