# 停點報告

> 2026-09-04　狀態：**批一完成，等 Rozi 看過再做批二**

## 交付
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098** ｜ `Tripay_原型.html`（871 行，單檔）

批一：S-00 登入、S-01 首頁、S-02 建立行程、S-02b 編輯行程。
右上角切換**標註／操作**（預設標註），徽章可點複製編號。四頁**真的連動**——
在 S-02 建一趟，S-01 立刻多一張卡；在 S-02b 改色調或成員，卡片跟著變。

## 1. 編號清單（完整版在原型右側，這裡只給總數與重點）

| 畫面 | 可操作 | 靜態 | 隱形 | 已移除 |
|---|--:|--:|--:|--:|
| S-00 | 1 | 3 | 1 | 2 |
| S-01 | 4 | 3 | 4 | 2 |
| S-02 | 12 | 6 | 0 | 1 |
| S-02b | 8 | 2 | 0 | 1 |

**值得先點的幾個**：`S-02-6` 打行程名會即時換下方色調預覽（試「沖繩」「京都」）；
`S-02-13` 點成員圓圈就地改識別（空著就用名字第一個字＋色底）；
`S-02-20` 回程留空按「出發！」才跳單日確認；
`S-02b-10` 8 種色調預設盤＋1 個「自動依行程名」；
`S-02b-11` 支付方式，刪「現金」會被擋（3 筆在用），新增的可以刪。

## 2. 與 `_盤點_畫面功能.md` 的差異

**沒有差異。** 批一四頁的編號全部沿用盤點表既有值，
**沒有新增編號，也沒有新標刪除線的編號**。已移除的 6 項
（S-00-1、S-00-6、S-01-9、S-01-10、S-02-5、S-02b-6）依規定不出現在原型上，
但仍列在右側清單標「已移除」，號碼不回收。

## 3. 要你裁示的兩件事

**(a) 隱形行為怎麼對停止條件 4。**
S-00-7、S-01-11、S-01-12、S-01-13、S-01-14 共 5 項是**看不見的行為**
（登入後跳轉、排序、列表過濾、成員適用範圍、emoji 退場），沒有區塊可以掛徽章。
我的做法是列在右側清單標「隱形」。若你要它們也有徽章，得先決定掛在哪。

**(b) 成員列的徽章只掛第一列。**
成員有 4 列，每列都掛 `S-02-13`／`S-02-14` 會把版面塞爆，所以只掛第一列代表整組。
要每列都掛也可以，說一聲。

## 4. 自我測試
jsdom 29 項全過：載入無 JS 錯誤、四頁都渲染、**41 個徽章逐一觸發各狀態後與清單完全對得起來
（無缺漏、無孤兒）**、模式切換、建立行程連動、色調判定、ZWJ 不切斷、
支付方式擋刪、成員擋刪、幣別搜尋。既有的 #13 輸入回歸測試 34 項也仍全過。

測試過程抓到我自己一個 bug 並修掉：**toast 掛在會被重繪的容器裡**，
先 toast 再 render 會被立刻沖掉，改成先 render 再 toast。

## 5. 沒做的
未動 `src/`、未跑 migration、未刪任何檔案。
兩份舊稿的橫幅**維持「即將作廢」**——依 §8 停止條件，四批全部併完才改「已作廢」。
