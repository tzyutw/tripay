# 停點　2026-09-12 18:55　修-8　「帳算清楚了」的分享鈕接上既有分享 sheet

Rozi 手機回報「『分享給大家』不能按」——實際是**按得到，但按了跳回行程頁、看不到分享**。
`SettlementPage.tsx` 那顆按鈕導的是 `/trips/:id`，而分享 sheet 的路由是 `/trips/:id/share`。
**只改那一行的目的地**，接 S-03 既有的那個 sheet，沒有另寫分享畫面。

## 五條停止條件的實際輸出

| # | 量到什麼 |
|---|---|
| 1 | 點下去 1.2 秒後：路由 `/trips/t1/settlement` → **`/trips/t1/share`**，畫面出現「複製文字摘要」 |
| 2 | 關掉 sheet → `/trips/t1`，只退一層，沒有多跳 |
| 3 | `navigate(\`/trips/${tripId}\`)` 在 `SettlementPage.tsx` → **0** |
| 4 | S-03 右上角入口還在（`aria-label=更多`），開得起來 |
| 5 | 單元測試 353 passed／3 skipped、`tsc` 0、`build` 0 |

驗證腳本 `Claude outputs/_verify_修8.cjs` **7/0**（拋棄式，不進版控）。

## 一個量測上的坑（記進腳本註解了）

停止條件 1 寫「`location.pathname` 以 `/share` 結尾」，**在量測靶上永遠驗不到**——
量測靶用 `MemoryRouter`，`window.location.pathname` 固定是 `/harness.html`。
路由要讀 `window.__ROUTE__`（`src/test/harness/main.tsx:50` 的 `RouteProbe` 就是為此而設）。
照字面寫會拿到假紅，改讀 `__ROUTE__` 之後量到的就是真的路由。

## 順帶發現、沒有動
原型寫「把結果傳給大家」、實作寫「分享給大家」，**兩邊文案不一致**。這一輪不改字
（改文案是產品決定，不是 bug 修復），已記進 `_延後與不做清單.md`。

## 進行中被插隊的工作
收尾-AJ（原型測試 15 支修復）做到一半，改動留在工作區；修-8 只 commit 它自己動到的檔。
