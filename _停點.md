# 停點報告

> 2026-09-04　狀態：**輸入 bug 已修，等 Rozi 續測**

## 一句話
是我的 bug，已治本修好；並補了會重現該症狀的回歸測試。原型連結不變。

## 一、根因（驗證過，不是猜的）
每次 `input` 事件我都重繪整塊 DOM，輸入框因此被重建。舊碼想用 `setSelectionRange`
還原游標——但 **`<input type="number">` 不支援 selection API**，呼叫會拋
`InvalidStateError`，而我用 `try/catch` 把它吞掉了。游標於是停在 0，
下一個字元就插到最前面：**2389 → 9832**。

用 jsdom 直接驗證：`type="number"` 呼叫 `setSelectionRange` 拋 `InvalidStateError`，
`type="text"` 則正常——所以標題欄沒事、金額欄全壞，症狀完全吻合。

## 二、修法：採你偏好的 (a)，治本
打字時改走新的 `refresh()`，**只更新衍生內容**（自動值、提示、比對列、即時結果、
存檔鈕狀態），**輸入框節點完全不動、也不寫它的 value**。
自動值改用 `placeholder` 呈現而非 `value`，所以重算永遠不可能蓋掉正在打的字。
`render()`（重建結構）只在點選造成結構變化時才跑。**`setSelectionRange` 整個移除。**

## 三、自我測試結果（要求 2）
`原型_輸入回歸測試.cjs`，jsdom 模擬逐字輸入，**34 項全過**：

| 測項 | 結果 |
|---|---|
| 台幣總額輸入 2389 | value `2389`、游標 4、節點重建 **0** 次 |
| 外幣總額輸入 45000 | value `45000`、游標 5、重建 **0** |
| 標題（text）輸入 | 正常，重建 0 |
| 各自金額四欄逐欄輸入 2389／45000／100／7 | 四欄全部正確，重建 **0** |
| Backspace 刪 2 次 | `2389` → `23`，游標 2 |
| 中間插入（`23`＋`55`＋`89`）| `235589`，游標 4 |
| 自動均分不覆蓋輸入（要求 3）| Ning 打 `1000` 時，其餘三人 **value 保持空字串**，自動值只出現在 placeholder（769／769／770）|
| 版本二同樣測一遍 | 全過 |

**測試本身也驗過**：把同一份測試跑在修正前的版本上，**重現出 `9832` 與 `00054`**、
節點每個字元重建一次。所以這不是「改完看起來好了」，是有能抓到它的測試。

## 四、範圍
只動 `分帳模型原型.html`。**src／production 完全未碰**（這是原型缺陷）。
順帶把這條教訓寫進 `CLAUDE.md` 環境備忘，避免日後在真的 S-04 表單重蹈覆轍。

## 五、續測建議
先試情境③（藥妝店）：切「各付各的」→ 用 ₩ 填 → 只填兩個人，看剩下兩人的自動值即時重算。
