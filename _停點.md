# 停點｜實作-B～H（**已上線**）　2026-09-06 00:20

## 實作-H　Rozi 手機實測回報的兩項
### ① S-02／S-02b 日期欄位跑版
**根因**：`.datefield`（`-webkit-appearance:none` ＋ 固定 40px ＋ 日曆 icon）
`index.css` 早就移植好了，**畫面卻沒接上**，日期列還是自己寫的 Tailwind。
iOS 沒關掉原生外觀時，寬度由 OS 決定、還用 zh-TW 長格式，比半欄還寬 → 整列撐出 sheet。
改成與原型 1195–1200 相同的結構（`.datefield` ＋ gap 9px ＋ `flex-1 min-w-0`），
高度交給 CSS、`lang="en"` 拿掉。S-02 與 S-02b 共用同一段 JSX。
> **順帶補上一個從沒被搬過來的東西**：原型的 `input[type=text|number|date]` 基礎樣式
> （border／背景／padding）是**無 class 的元素選擇器**，CSS 移植腳本的白名單只收有 class 的規則，
> 所以一直沒搬。`.datefield` 接上後才會發現欄位沒有邊框。抽取器已修，207 條。

### ② 新增行程不再預填上一趟成員（G-09，2026-09-04 已移除）
`TripListPage` 不再傳 `prefill`，`latestTripId` 一併移除；
`mode:'members'` 整條路從型別上拿掉，文案「已帶入上一趟的成員，可以改」刪除。
**`mode:'full'`（複製行程）保留**，測試確認它仍帶入四位成員與幣別。

### ③ 版面斷言補上「量會捲動的那個容器」
這次沒抓到就是因為只量 `document.documentElement`。現在改成：
固定清單（`.sheet`／`.dlg`／`.dlgwrap`／`.statcard`／`.detailtable`）＋
全域掃所有 `overflow-x` 非 `visible` 的元素。
**加了第二隻金絲雀**（把 900px 元素塞進 `.sheet`，document 不會溢出、只有容器那條抓得到）。
> 金絲雀自己也踩了兩個坑並修掉：`flex:none` 不加的話 900px 會被 flex 縮回去；
> 第一隻金絲雀不撤掉會把整頁撐寬、干擾第二隻。**「金絲雀沒響」也要當成紅燈看。**

`supabase/migrations/014_fix_trips_self_referential_rls.sql` 一併進版控
（Cowork 已套 production，這個檔只是記錄，**不要再對 production 執行**）。


## 實作-G　分享頁轉帳列重複（已修、已上線）
`get_shared_trip()` 回的是這趟**全部**結算，我改走 RPC 時把「挑 confirmed 那一次」
的篩選弄丟了 → 同樣三筆畫 12 遍。抽出 `pickConfirmed()` 修好，測試比列數不是比字串。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`750d857` 實作-A｜`723466a` B＋C｜`8cba1ef` D｜`355a64e` E｜`3e01b02` 修 CI lockfile｜
`f8c00ca` G｜（本次）H。

**前兩次部署失敗是我造成的**：CI 走 `pnpm install --frozen-lockfile`，我卻整輪用 npm，
`pnpm-lock.yaml` 沒跟上。**往後這個 repo 一律用 pnpm，推之前先跑 CI 那兩道指令。**

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

## ✅ 212 對帳表：**已完成 212／不適用 0／未做 0** —— 停止條件 4 過了

## 原型與佇列指令的兩處出入 —— **Rozi 2026-09-05 都已裁示，這兩項已結案**
1. **S-06 hero 的成員 emoji**：裁示「不要 emoji，維持現狀」（與 S-03 一致）。原型待日後同步。
2. **`➕` 當「還沒選 emoji」的預設值**：裁示維持 `➕`（原型是位階 1）。

**留給原型的待辦**（`Tripay_原型.html` 由終端機維護，但這兩處要不要同步請 Cowork 指示）：
S-06 的 `renderS06()` hero 仍接著 `members.map(...).join('')`。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
