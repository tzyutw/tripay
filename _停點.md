# 停點　2026-09-12 19:25　收尾-AJ　原型測試 15 支修復（含追加：AJ-4 第 3 小項）

主體在 commit `34f4603`。這一輪是**重跑複驗 ＋ 補做 AJ-4.3**（缺 md 的錯誤訊息）。

## 五條停止條件——這一輪全部重新實測，不是引用上次的紀錄

| 停止條件 | 實測 |
|---|---|
| 1　15 支都跑到「通過 N 失敗 M」 | ✅ 15/15 跑到底，0 支丟例外 |
| 2　三條 `.ic2` 斷言綠、原型 `--ic2-`／`.ic2{` 沒被動 | ✅ 實測 `alpha 0 / 0.22`、`border 0px`；`git show 34f4603 -- Tripay_原型.html` 沒有這兩個字串的變更行 |
| 3　紅 ≤3，每條寫明理由 | ✅ 合計 **2**，兩條都是原型違反自己的稿註（見下） |
| 4　`no-sandbox` ≥15 | ⚠️ **11**——只有 11 支開瀏覽器，另 4 支是 jsdom（`grep -ln puppeteer` 對那四支無命中）。11 就是全部，這條的數字訂錯 |
| 5　單元測試／tsc／build | ✅ 354 passed／3 skipped、tsc 無輸出、build ✓ 962ms |

各支：icon 79/0、倒序 38/0、可點區 44/0、字級 107/**1**、批一 61/0、批三回歸 54/0、批三回饋 45/0、
批二 132/**1**、焦點 37/0、版面回歸 18/0、版面與拖曳 64/0、稿註 54/0、貼邊 51/0、輸入 34/0、預設日期 32/0。

## 這一輪補做的：AJ-4 第 3 小項（上次沒落實）

上次只做了 AJ-4 的 1、2 兩項；第 3 項「缺同目錄 md 時印出缺哪個檔再退出」**九支都還是裸的
`fs.readFileSync('_盤點_畫面功能.md')`**，缺檔會直接丟 ENOENT 堆疊。已在 8 支讀 md 的腳本
各加一個 `needMd()`：相對 `__dirname` 解析（從別的 cwd 跑也找得到）、缺檔印
「缺少同目錄的 X——這支測試要拿它對稿註編號」後 `exit 2`。
`原型_icon與承諾測試` 的 `_icon授權.md` 那條斷言也改成相對 `__dirname`，免得換 cwd 就假紅。

**金絲雀**：把 `原型_批三回饋測試.cjs` 複製到一個沒有 md 的子目錄跑 → 印出上面那句、沒有堆疊。
改完 8 支重跑，通過數與改動前完全一致（79／38／44／107／45／54／51／64）。

## 剩下的 2 條紅：都是原型違反自己的稿註，修法會動版面

1. **S-02b-14「這趟叫什麼？」標籤自己佔一行**。稿註（原型 1738 行）寫「與 S-02『去哪？』同一款欄位」，
   而 S-02-6 用 `.fieldrow`（標籤與輸入同列）、S-02b-14 用 `.fld`＋`.lbl`。違反全站 UX 檢查第 1 條。
   修法是包進 `.fieldrow`——**會動版面，不在這一節範圍**。
2. **S-06 分享頁的每人分擔列是可點的 `<button>`**。S-06-14 稿註寫「分享頁是唯讀、這裡的列不可點」，
   但同一支另有一條斷言要求 S-06 與 S-03 的列結構完全相同（同一元件）。
   要修得先讓共用元件有唯讀變體——**是設計決定，不是腳本修得掉的**。

## 機器驗不到、要人看的
這一節只動測試腳本與文件，**沒有動產品程式、沒有動 `Tripay_原型.html`**，所以沒有要 Rozi 在手機上看的項目。

## 這次沒 push
CLAUDE.md 的 2026-09-08「方案 A」定案是 commit 後等 `_可以推了.txt` 才 push，
與佇列該節寫的「commit ＋ push」牴觸；照定案辦——**已 commit、未 push**，等放行檔。

## 順手看到但沒動
- 停止條件 4 的 `≥15` 訂錯（分母是 11）。要改的是條件，不是腳本。
- `原型_輸入回歸測試.cjs` 預設吃的是已作廢的 `分帳模型原型.html`（不給引數時）。
- `_需求台帳.md` 有一段未提交的 C13 內容（Cowork 端寫的），**沒碰、沒提交**。
- 本輪為了跑 jsdom 的四支，留下兩個暫用檔 `_tmp_runjsdom.cjs`、`_tmp_guard/canary.cjs`；
  這個 session 的刪除權限被拒，**沒有提交它們**，請手動刪。
