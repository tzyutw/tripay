# 停點報告

> 2026-09-05　狀態：**實作-A 做完 A-0a、A-3、A-4 的文案常數。
> A-0c 攔下一個真的會出事的問題，A-1／A-2／A-0b 卡在它後面。**

## 🛑 A-0c 對帳不一致——schema 工作全部停在這裡
migration 檔案 **12 個**（001～011b），`list_migrations` 只回報 **9 筆**：
**001／002／003 沒有登記在 `schema_migrations`。**
已查證它們其實都套過了（`trips` 表、`confirm_settlement` RPC、
`settled_on_spot`／`is_sponsor` 都在），只是當初沒走 migration 機制。

**不能硬上的原因**：`create_branch` 照「已登記的 migration」重建 schema。少了 001，
branch 會從 004 開始套在空資料庫上——不是失敗就是做出跟 production 不一樣的 schema，
**012／013 的每一條斷言都在錯的基準上跑**。我沒有建 branch、沒有碰 production。

**解法**（production 寫入，不由終端機做）：把 001／002／003 **補登記**
（`supabase migration repair --status applied`），**不要重跑 SQL**——物件已經在了。
補完按「收」，我接著跑 A-1／A-2／A-0b。

## ✅ A-0a　型別補齊
`Trip` 補五欄、`TripMember` 補 `person_id`，型別照 MCP 實查（`trip_kind` 是 enum，
`settlement_mode` 是 text＋check，**不是** enum）。`tsc` 零錯誤。
⚠️ 佇列預期「`TripListPage.tsx:37` 會報錯」**實際沒有**——那行是 `.eq('kind','trip')`，
`.eq()` 本來就收字串鍵，補欄位只是讓查詢變正確。

## ✅ A-3　全域 token
⚠️ **停止條件 1、2 照字面驗不出來**：條件假設 `src/` 是手寫 CSS，但這裡是 Tailwind class，
**一個 `font-size:` 都沒有**（改動前後都是 0）——照字面跑永遠 0/0，正是「查了但沒查到」
跟「沒有問題」長得一樣的坑。改成兩段驗：① `src/` 寫死的字級與圓角**各 0 處**
（改動前 200 處／122 處）；② **建置產物**的 CSS 裡 `font-size` 全是 `var(--fs-*)`、寫死 0 處。

九個字級／四個圓角／八個顏色逐一比對原型 `.ui`（158–173 行）搬入，深色殼沒搬；
字體走 `@fontsource/onest`（Google Fonts 0 命中、等寬禁字全 0）；
另定義輸入框高度階梯與 **icon 三個層級**（`.ic2` 薄膜／`.rmbtn` 裸 icon＋擴張區／`.chev` 不可點），
17 個 Feather icon 逐字搬進 `Icon.tsx`。

**踩到一個會靜靜壞掉的坑**：`@import` 放在 `@tailwind` 後面，PostCSS 會**整段丟掉、
只留一行警告、建置照樣成功**，字體就沒載到。已修，並加斷言守著。

## ✅ A-4（部分）　三句未定案文案
`src/lib/messages.ts` 逐字搬自原型 1884–1886 行。
⚠️「各 ≥2 處引用」**現在驗不了，不是漏做**：App 還沒畫任何未定案金額的狀態（在實作-B），
沒有第二處可引用；為湊數字製造引用就是假通過。改驗**唯一性**：三句各只出現在這一個檔案。

## ⏸ A-4 的六個共用元件沒做——不是做不到
A-3 護欄要求「五個畫面各 render 一次，`textContent` 與改動前 diff」。
**這個專案沒有 React 測試環境**（vitest 只跑 supabase function 測試，無 jsdom／testing-library），
五個畫面都要 mock supabase／react-query／router 才 render 得起來。
沒有這張網就重構 2,665 行、五個檔案，正是一路在防的「改 A 壞 B」。
改動前的 `src/` 存在 `scratchpad/src_beforeA3`。**先架測試環境還是先做 B 的畫面，請 Cowork 決定。**

## 測試
token 掃描 **52 項**、文案唯一性 **13 項**、原型既有 15 份 **871 項**——全綠；
`tsc` 零錯誤、`vite build` 通過。反向驗證四條都會變紅：@import 移位／改回寫死字級／
拿掉 `.rmbtn` 可點區／`.ic2` 薄膜改實色。
`git diff` 只動 `src/`(13 檔)、`package.json`＋lock、`tailwind.config.js`。
**沒有對 production 執行 DDL、沒有建 branch。**（原型顯示 modified 是 #34／#35 未 commit。）

## 順手看到但沒動
1. `src/` 有 **89 處寫死色碼**（#E4DFD9 ×29、#F5F4F2 ×15…）。顏色不在 A-3 的五條停止條件內，只換了舊色名（219 處）。其中 8 個警示用暖色底**原型沒有對應 token**，要新增才收得掉。
2. 金額落在 `--fs-strong`(17)／`--fs-money`(16) 兩階——原寫 17／18px 的金額被對到 strong；哪些該是 money 是語意判斷，留給實作-B。
3. `EmojiPicker.tsx` 還在（規矩是整檔刪除、不得複用），刪它是 B 的事，本節只換了它的 token。
