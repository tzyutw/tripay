# 停點　2026-09-08　實作-AB（未結算頁也要有計算依據；自己買給自己的獨立一行）

## AB-1　「查看計算依據」在還沒結算的狀態也有
- 把按鈕與展開區塊抽成變數（`DetailsToggle`／`DetailsBlock`），
  兩個狀態掛**同一顆、同一個區塊、同一個 `breakdownOf`**，沒有另寫一份。
- 位置：轉帳卡片下面、「結算行程」按鈕上面。已結算那一顆位置與行為不變。

## AB-2　四行，「自己買給自己的」獨立
- 判定**直接 import `summary.ts` 的 `isSelfPaid()`**（「只看共同的帳」用的同一個）。
  `breakdownOf` 裡沒有第二套判斷（斷言有掃）。
- 四行固定顯示，筆數 0 的照樣顯示；「自己買給自己的」下面一句灰字
  `這些你自己付、也算你自己，不影響要轉的錢`，每位成員各 1 句。
- 只換呈現不改計算：前三行相加 ＝ 應分攤（四位成員逐一驗過），
  `實際付出 − 應分攤 ＝ 差額`（±1）也仍然成立。
- 順手把 `breakdownFor()` 抽到模組層級當純函式，單元測試才打得到（4 條）。

## 驗證
- `實作_計算依據四行測試.cjs` **60 項全綠**。
  金絲雀：拿掉未結算那顆按鈕紅 15、`isSelfPaid` 停用紅 1、
  兩類判定順序對調紅 1、0 筆的行又被過濾紅 3、灰字拿掉紅 4。
- 24 支回歸全綠。`vitest` 337 綠、`tsc` 0 錯、`npm run build` 過。
- 條件 12：`git diff --name-only` 剛好就是白名單那三個原始碼檔。

## 兩個「靶沒對上就等於沒驗」
1. **只驗「四行都在」抓不到 AB-2 的核心**：把 `isSelfPaid` 停用（全部落到「各付各的」），
   四行照樣都在、加總也對，金絲雀不會紅。補了「紀念品 860 必須落在第二行、
   同一位的『各付各的』仍是 2 筆」才有靶。
2. **筆數要從畫面上讀**：我原本讀 `data-selfN`，但那個屬性根本沒輸出，
   `undefined > 0` 永遠是 false——那一組會靜靜地永遠通過。改讀渲染出來的「N 筆」。

## 我改了既有斷言的地方（都是 AB-2 推翻了舊呈現，不是為了讓它綠）
1. `verify_九項` 與 `verify_這個人的帳`：`data-named` 不存在了，改讀 `data-self`＋`data-each`，
   驗的恆等式一個字沒改（前三行相加 ＝ 應分攤）。
2. `verify_九項` 原本有一條「沒代墊的人**不該出現**『他先付出去的』那一段」——
   **Rozi 2026-09-08 拍板改成四行固定顯示**，所以反過來驗「那一段在，而且寫 0 筆」。
3. `SettlementPage.test.tsx` 的原型比對排除「指名算他的」與「−$ 9,605」兩段：
   **AB 的檔案白名單沒有 `Tripay_原型.html`**，原型與 `screens.json` 還停在拆之前。
   排除清單只有這兩段，原型同步之後要清空。

## 順手看到但沒動
1. **原型沒有同步**（白名單沒列），S-05 的計算依據還是拆之前的兩行。
2. 假資料「藥妝店」讓 Robin 的「各付各的」是 **−10,965**：那一筆台幣總額只有 1,035，
   兩人卻各填了 12,000／18,000 台幣，差額歸付款人就把付款人壓成大負數。
   計算是自洽的（R9 允許加總不等於總額），但**假資料本身不像真的帳**。
3. `?fill=remainder` 外幣填 88 vs 台幣填 89（差額歸屬只在引擎生效）、
   `per[]` 對贊助的處理——兩件都還等裁示。

## 機器驗不到，只有 Rozi 在手機上才看得到
1. 四行加一句灰字之後，未結算那一頁會不會變得太長、要捲很久才看到「結算行程」。
2. 「自己買給自己的 0 筆 $ 0」對沒有那類消費的人來說是幫助還是雜訊。
3. 那句灰字的說法夠不夠清楚（她讀到的是「所以我不用管這一行」嗎）。
