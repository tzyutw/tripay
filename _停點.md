# 停點報告

> 2026-09-05　狀態：**#27 全部做完（含 27-4／27-5／27-6 追加），等 Rozi 挑 icon**

交付：**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**（同一連結）

## 要你挑的：icon 三組候選
稿頭說明底下多了一條**對照條**：三組並排、17px 與 20px 各一排、13 個全列出，
線寬尺寸顏色完全相同，**只差造形**。按「用這組」會立刻換掉底下所有畫面，可以直接比。

| 組 | 授權 | 造形差在哪 |
|---|---|---|
| **Lucide**（目前預設）| ISC | 鉛筆帶方框，筆畫最平均 |
| Feather | MIT | 鉛筆是斜線一筆，整體最細瘦 |
| Heroicons | MIT | 圓角最大，複製是兩張文件不是兩個方框 |

**SF Symbols 不能用**：Apple 授權只涵蓋「執行在 Apple 作業系統上的軟體」，且不得修改內嵌
——Tripay 是網頁。以上是做出接近的風格，不是拿它的檔案。授權記在 `_icon授權.md`。
**選定後我改一行就換掉。**

## 你放進 icons_自備/ 的 6 個檔案，這一輪一個都沒生效
檔名都是下載時的雜湊亂碼，不在 13 個鍵名之內。**渲染出來看過了**，該改成的檔名：
`1190062063…`＝鉛筆（**實心**）→`edit.svg`；`1644942480…`＝分享→`share.svg`；
`1920496784…`＝向左迴轉箭頭（**實心**）→`back.svg`；`5412118401…`＝向右箭頭（**實心**）→`next.svg`；
`6579874521…`＝兩張疊起來的方框→`copy.svg`；`6952612681…`＝齒輪→`settings.svg`。
**三個是實心的**，跟其餘 10 個線稿混在一起會不一致；依規則不自動轉線稿，轉出來一定歪。
格式本身都可以收。詳表在 `_icon授權.md`。`請看這裡.md` 底部的**來源欄還是空的，上線前要補**。

## 徽章蓋住文字：你看到的字跟產品上的字不一樣
徽章原本壓在區塊左上角，S-03c-2 只看得到「救不回來」，S-07-5 幾乎整個沒了。
你這幾輪回饋大量集中在文案，等於在讀被截斷的句子。已修，三個寬度全站 0 筆重疊。
**操作模式一個 px 都沒動**——把標註樣式整組停用後重量，102 個區塊差異 0 筆。

## 其餘
- 分享頁不再說「下載 Tripay」（沒有 App）；「安裝」鈕移除（iOS Safari 不支援那個事件）
- 深色模式整段移除（`src/` 裡根本沒有深色樣式）。**Phase 1 的設定頁只剩帳號卡與登出**
- 回顧卡的 `$` 回到數字前；登出改中性色（`.delrow` 維持刪除紅）
- `filter`／`money` 兩個沒人用的 icon 刪掉，剩 13 個
- S-04-3 類別 emoji 改成可點的就地編輯，**與成員識別共用同一個函式**，改過就不再被標題蓋掉

## 停下回報、沒有自行處理
**S-07-8 登出確認框的「登出」鈕仍是實心刪除紅**（`.btn dg`）。27-4c 只寫了 S-07-6 的
文字色，但同一條理由（登出可復原）也適用這裡。指令沒寫到，所以只回報。要改請說一聲。

## 測試（七組全過，新增一組 86 項）
批一 60｜批二 132｜批三 54｜輸入 34｜版面 18｜批三回饋 45｜**icon 與承諾 86**。
盤點檔與盤點表已依 #27 同步，schema 缺口 6 → 7 項（多了類別 emoji 的手動指定標記）。
