# 停點報告

> 2026-09-04　狀態：**#18／#19 完成，等 Rozi 看過再做批三**

## 交付
原型 **https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**
tokens **https://claude.ai/code/artifact/686e62e2-ea58-4ae2-a6f2-54be3deeeb37**

## #18 字體換成 Onest
`Onest, PingFang TC, Noto Sans TC, -apple-system, sans-serif` 一條到位——
Onest 不含中文，中文自動 fallback，**不需要為金額另外指定字體**。
Google Fonts 只載 400／500／600 三個字重。

**等寬字全站移除**，`--mono` token 整個拿掉。金額的層級改靠
`tabular-nums` 對齊 ＋ 字重 600，一般文字 400。

**用真實 Chrome 量測驗過，不是只看原始碼**：Onest 確實載入（`document.fonts.check` 為 true）、
金額元素的 computed style 是 `tabular-nums` / `600`、**整份文件沒有任何元素還在用 mono 字族**。

順帶抓到一個原始碼看不出來的：`<code>` 元素會吃瀏覽器**預設的 monospace**，
原始碼裡搜不到但畫面上就是等寬字。已改成繼承主字體。

## #19「哪位成員是我」整組移除
`owner_member_id` 只被用來算「我的花費」，統計卡改版後那個數字不再需要，欄位就失去用途。
- S-02-12「這是我」標記移除，**建立與編輯行程各少一步**
- **寫入端一併清掉**（建立行程、編輯儲存、預填都不再寫 owner），不是只清顯示端
- 欄位保留不刪，作法同 `trips.emoji`
- Phase 1.5「切換成員視角」已從候選清單移除
- **S-02-10 成員區標題照指示保留**

測試新增斷言：表單與行程資料的欄位清單裡都不得再有 `owner`，
且 S-04 的選取語彙仍在（確認沒有把 selchip 一起誤刪）。

## 換字後重測版面（必要，字寬會變）
320／375／414 三個寬度、標註與操作兩模式、批一批二全狀態展開，**全部無溢出**。

## 測試（四組全過）
| 測試 | 項數 | 結果 |
|---|--:|---|
| `原型_批一回歸測試.cjs` | 60 | 全過 |
| `原型_批二回歸測試.cjs` | 71 | 全過 |
| `原型_輸入回歸測試.cjs` | 34 | 全過 |
| `原型_版面回歸測試.cjs` | 18 | 全過 |

## 文件
`_盤點_畫面功能.md` S-02-12 標刪除線｜`專案狀態.md` 補 3 條決策（含**誤記更正**：
成員視角與副卡視角本來就不共用元件）｜檢查表 v1.8 補 #72–74｜
`_實作批次工作清單.md` 的 Phase 1.5 劃掉切換成員視角

## 沒做的
未改 `src/`、未跑 migration、未刪檔。只換字體，沒有順手動任何版面或間距。
