# 停點報告

> 2026-09-04　狀態：**#22 完成，等 Rozi 看過再做批三**

## 交付
原型 **https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**
migration `supabase/migrations/010_settlement_mode.sql`（**只寫檔未執行**，待 Cowork 端套用）

## 1–5、9 已做
- **G-09 移除**：原型控制與預填提示都清掉，`專案狀態.md` 標記移除並註明理由
- **「約」移到金額前面**：統計卡與每人分擔列一致
- **贊助顯示負數＋收款綠**（`−$ 50,000`），badge 保留
- **「當場就清了」改名**：你點出的根因對了——與分帳型態「各付各的」同名不同義。
  改名後用**虛線方角**與成員 chip 的圓角實心區隔，S-03 badge 一併改
- **贊助改用統一 chip**，打勾框與 check icon 移除。**全站打勾框現在是 0 個**
- **S-04-32 人名與輸入框同列**——根因是我漏了 `.ui .amtrow` 的 CSS，
  div 退回 block 才會堆成兩行。這與第一次的金額欄是同一類疏漏

## 6 結算模式「都轉給同一個人」
設定**只在 S-02b**（S-02b-13），選第二項要選人、**預設帶代墊最多的成員**。
S-05 只在代墊比例 >70% 且目前是 direct 時給**引導連結**（S-05-31），**沒有第二個設定入口**。

演算法只改轉帳明細的產生方式，**淨額計算一行都沒動**。實測：
```
direct  淨額 {Rozi:5500, 小美:-500, 阿明:-2500, 小魚:-2500}
hub     淨額 {Rozi:5500, 小美:-500, 阿明:-2500, 小魚:-2500}   ← 完全相同
hub 下每個欠錢的人各轉 1 次；Σ 淨額兩種模式都是 0
```

## 10 全站 UX 檢查八條
| # | 檢查 | 結果 |
|---|---|---|
| 1 | 標籤與輸入框同列 | **發現 5 處已修**：S-02-6、S-02-7、S-04-2、S-04-10、S-04-32。群組標籤（清單／chips／並排日期）共 11 處**標明白名單理由**，測試會驗 |
| 2 | 灰字只留兩種 | 通過（#17 已砍到 3 句，本批未新增） |
| 3 | 選取語彙單一 | **發現 1 處已修**（S-04-19 的打勾框）。全站打勾框 0 個 |
| 4 | 當下不需要的不攤開 | 通過（統計卡、一起分、色調盤、匯率都已收合） |
| 5 | icon 風格統一 | 通過（全 SVG、Remix fill、無混用） |
| 6 | 一個設定一個入口 | 通過（匯率 2 個輸入框都在 S-02b；結算模式設定只在 S-02b，測試有驗） |
| 7 | 需要解釋才懂的互動 | 通過 |
| 8 | 同名不同義 | **發現 1 處已修**（「各付各的」，即第 4 項） |

## 11 改動範圍
本次動的檔案：`Tripay_原型.html`、四支測試中的 `原型_批二回歸測試.cjs`、
`_盤點_畫面功能.md`、`專案狀態.md`、新增 `010_settlement_mode.sql`。
**超出指令範圍的只有一項**：批一測試裡兩條 #17 的斷言用了舊名稱
（`.approxdot`、「大家各付各的」），改名後必然失敗，**只更新斷言字串，未動行為**。

## 測試（四組全過，批二 111 → 132 項）
批二 **132**｜批一 60｜輸入 34｜版面 18（320／375／414 無溢出）

## 沒做的
未改 `src/`、**未執行** migration、未刪檔。淨額計算方式未動。
