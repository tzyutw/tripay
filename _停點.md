# 停點｜實作-B ＋ C ＋ D ＋ E ＋ F（**已上線**）　2026-09-05 23:20

## 🚀 已 push：`c23dcf2..ff2a8fc`（5 個 commit）→ GitHub Actions 自動部署到
https://tzyutw.github.io/tripay/

| commit | 內容 |
|---|---|
| `750d857` | 實作-A 型別對齊、全域 token、schema 012／013、分享權限漏洞 |
| `723466a` | 實作-B（B-1～B-7）＋ 實作-C |
| `8cba1ef` | 實作-D Cowork 複驗退回三項 |
| `355a64e` | 實作-E 對帳表 4 列「未做」收掉 |
| `ff2a8fc` | 文件：Cowork 決策紀錄 |

| `46fa331` | 實作-F 收尾文件 |
| `3e01b02` | **修 CI：pnpm-lock.yaml** |

### ⚠️ 前兩次部署失敗，第三次才成功——我自己造成的
CI 走 `pnpm install --frozen-lockfile`，但我整輪都用 **npm** 裝套件，
只更新了 `package-lock.json`，`pnpm-lock.yaml` 沒跟上：
`[ERR_PNPM_OUTDATED_LOCKFILE]　6 dependencies were added`
（`@testing-library/*` ×3、`jsdom`、`puppeteer-core`、`@fontsource/onest`）。
用與 CI 同版的 **pnpm 11** 重新產生 lockfile，並在本機把 CI 的兩道指令完整跑過
（`pnpm install --frozen-lockfile` ✅、`pnpm run build` ✅），
換成 pnpm 的 `node_modules` 之後 vitest 211 綠、tsc 0 錯、212 對帳表未做 0 仍成立。

**線上已確認是新版**：`https://tzyutw.github.io/tripay/` 回 200，
`index-Bm0H7L_v.js`／`index-ClCpmVi1.css` 與本機 `dist/` 完全相同。

Cowork 第三輪複驗全過（`Claude outputs/_可以推了_已推.txt`）。
`Claude outputs/_cowork暫存_可刪/` 已刪除（裡面有一份含 anon key 的 `_e2e.env`，未進 commit）。

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

細節：`Claude outputs/_實作B進度.md`、`Claude outputs/_212對帳表.md`。
vitest **211 綠**（3 skip）｜版面回歸 172｜token 52｜型別對帳 29｜文案唯一性 13｜端到端 2｜
212 交叉檢查 13｜`tsc` 零錯誤｜`vite build` 通過。B 開始前是 92 綠。

## ✅ 212 對帳表：**已完成 212／不適用 0／未做 0** —— 停止條件 4 過了

## 各節摘要（細節在 `Claude outputs/_實作B進度.md`）
- **B-1～B-7**　S-00～S-07 全站對齊原型，每頁對 `fixtures/screens.json` 逐字比對。
- **C**　C-1 查證後**沒有改引擎**（→ 四趟 verify 不必跑）｜C-2 hub 淨額與 direct 完全相同｜
  C-3 新增真實 Chrome 版面回歸，抓到並修好 S-02 少 `min-w-0` 的 28px 溢出｜C-5 環境地雷修三處。
- **D**　修掉兩個刪除對話框疊在一起（B-3 重寫時「保留尾巴」留下的）、S-03b 三筆文案、
  212 對帳表改由程式產生。
- **E**　S-03c-2／3／4／7 逐字對齊原型；S-02c-10 三層 fallback 收進
  `shared/Avatar.tsx`（並修掉「預設 `'🙂'` 讓第二層永遠走不到」這個成因）。

## 原型與佇列指令的兩處出入 —— **Rozi 2026-09-05 都已裁示，這兩項已結案**
1. **S-06 hero 的成員 emoji**：裁示「不要 emoji，維持現狀」（與 S-03 一致）。原型待日後同步。
2. **`➕` 當「還沒選 emoji」的預設值**：裁示維持 `➕`（原型是位階 1）。

**留給原型的待辦**（`Tripay_原型.html` 由終端機維護，但這兩處要不要同步請 Cowork 指示）：
S-06 的 `renderS06()` hero 仍接著 `members.map(...).join('')`。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
