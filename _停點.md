# 停點｜實作-B～J（**已上線**）　2026-09-06 11:30

## 實作-K　假資料換成 Rozi 真實資料的形狀

**根因**：原型與 harness 的假資料**都只走 happy path**（清一色有 emoji、八筆消費、金額填好），
而她每天打開的三趟是**成員全部沒 emoji、0 筆消費、匯率只填一邊、有一趟沒設支付方式**。
她每天看的畫面，跟原型上驗收過的畫面，從第一天就不是同一個。

1. **原型加 T4／T5**（`store.realTrips`）＋ S-01 原型控制列加「示範資料的形狀」切換鈕。
   現有三趟一行沒動。切換會連批二／批三的畫面一起重畫。
2. **harness 補三個參數**：`?expenses=none`／`?rate=half`（外幣有值、台幣 null）／
   `?pays=none`。預設值全部維持現況，既有斷言不受影響。
3. **版面回歸跑四種資料組合** × 10 畫面 × 3 寬度：**293 → 1163 條**，全過。
4. `scrollIntoView` 補一個 `?.`——`vitest` 的 **Unhandled Errors 已清空**（jsdom 沒實作它）。

### 新增兩支量測
`實作_原型真實形狀測試.cjs`（50 條）｜`實作_資料形狀測試.cjs`（9 條，驗參數真的有接上——
沒接上的話那三種組合等於跑了三次預設值，全綠但什麼都沒驗到）。

### 反向驗證（六條全紅）
三個參數各不接上（3／1／2 紅）｜T4 補回 emoji｜T4 匯率兩邊都填｜T4／T5 補上消費。

### ⚠️ 停止條件 5 沒過，而且前提有誤——這一項要你裁示
指令說「`Tripay_原型.html` 目前沒有被 git 追蹤」。**實際上它是 tracked 的，
而且從 `750d857` 起就已經推上公開的 GitHub**（我這幾輪也一直在 commit 它）。
所以 `git check-ignore -v` 對它沒有輸出——`.gitignore` 對已追蹤檔案無效。

我**只加了 `.gitignore` 那一行（附說明註解），沒有 `git rm --cached`**：
把唯一真相的設計稿移出版控會失去它的歷史，而且**已經公開的版本不會因此消失**
（那要重寫歷史＋強推，屬不可逆）。這是隱私／曝露類的決定，等你指示。

順帶：T4／T5 的成員名用**中性英文短名**（`Alex`／`Robin`／`Sam`／`Kai`）而不是
指令舉例的真實名——理由同上，那個檔是公開的。字寬特性一樣（4–5 字母），
停止條件 1 驗的是形狀不是名字。（那幾個真實名在 repo 的文件與舊原型裡本來就有，
不是這一輪新增的曝露，但沒必要再多一處。）

## 實作-J（含第一輪複驗退回）　成員識別全站貫徹
`Avatar` 加 `size`（28／20）＋抽出 `MemberTag`，十一處識別位接上；
三處真正的句子維持純文字。「加一個人」的頭像列刪掉（🙂 不再出現）。
裸元素選擇器改 `:where()`（`input[type=text]{width:100%}` 壓過 `w-8 h-8`）。
分享頁「這趟旅程還沒結算。」橫向溢出 14px 修好。原型同步。

## 前幾輪（都已上線，細節在 `Claude outputs/_實作B進度.md`）
**B–C** 全站對齊原型＋引擎查證＋版面回歸測試｜**D／E** 複驗退回兩輪｜**F** 推上線｜
**G** 分享頁轉帳列重複 12 次｜**H** 日期欄位沒接上 `.datefield`、移除 G-09 預填成員｜
**I** 五個線上實測 bug：sheet 捲不動、沒選付款人靜默失敗、成員錯誤沒清、
名字沒按「加進來」就送出、`＋` 沒對齊（全域 `.ic` 沒被搬過來）。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`750d857` 實作-A｜`723466a` B＋C｜`8cba1ef` D｜`355a64e` E｜`3e01b02` 修 CI lockfile｜
`f8c00ca` G｜（本次）H。

**前兩次部署失敗是我造成的**：CI 走 `pnpm install --frozen-lockfile`，我卻整輪用 npm，
`pnpm-lock.yaml` 沒跟上。**往後這個 repo 一律用 pnpm，推之前先跑 CI 那兩道指令。**

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

## 212 對帳表
`實作_212對帳表.cjs` **已由 Cowork 於 2026-09-06 作廢**（判定規則會自動蓋章）。
依 `_實作階段運作方式.md`「六之零」，驗收判定改由 Cowork 寫、Cowork 跑，我不再產出對帳表。

## 原型與佇列指令的兩處出入 —— **Rozi 2026-09-05 都已裁示，這兩項已結案**
1. **S-06 hero 的成員 emoji**：裁示「不要 emoji，維持現狀」（與 S-03 一致）。原型待日後同步。
2. **`➕` 當「還沒選 emoji」的預設值**：裁示維持 `➕`（原型是位階 1）。

**留給原型的待辦**（`Tripay_原型.html` 由終端機維護，但這兩處要不要同步請 Cowork 指示）：
S-06 的 `renderS06()` hero 仍接著 `members.map(...).join('')`。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
