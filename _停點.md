# 停點報告

> 2026-09-04　狀態：**等 Rozi 試用原型後的選版與逐條修改意見**

## 一句話
分帳模型可點原型已交付，兩版都能真的操作、走完五個情境；資料層四項方向已入決策紀錄。

## 一、原型
**https://claude.ai/code/artifact/a5dc1321-bcaf-4a34-8108-407a1b1afa3c**

- 頂部切換兩版；390px 手機；成員 🍋Ning／🐟Ziyu／🐵Xiu／🐱Mei
- **記帳者視角明示**：標題列寫「記帳者：🐟 Ziyu（Rozi）」，
  **付款人沒有預設值、一定要選**——記帳者不等於付錢的人
- 真的可操作：填金額、選人、切幣別、逐人填各自金額都會即時反映
- 每一筆即時顯示：誰付／誰分攤／誰欠誰多少／進不進結算／badge
- **點選次數計數**：每筆記錄用掉幾次點選，兩版各自累計，走完五情境後自動比較
- 情境③用韓圜填各自金額，比對列同幣別比對，並標明結算換算成台幣

## 二、我先跑過一次驗證（不是只寫完就交）
兩版各走完五情境，結果：
- **Σnet = 0**（兩版皆是），最小轉帳 3 筆
- **兩版最終結算完全相同**——證明兩版是同一個模型的兩種問法，不是兩種算法
- 情境③ 版本一 parts=4、版本二 parts=3：這是**真實差異**不是 bug——
  版本一的「各付各的」照現行模型會替**全員**寫 split（沒填的標待填），
  版本二只替勾選的人寫。原型把這個差別如實呈現了

## 三、原型會主動指出兩個既有問題
- **只算一個人，但付款人不是他** → 跳警示：現行引擎會把 `personal` 整筆排除，
  **欠款不會產生**。這種情況若真的會發生，兩版都要補規則
- **外幣筆** → 跳警示：現行 `split_amount` 沒有幣別，填韓圜會被直接當台幣加（E 要修的）

## 四、好改
兩版規則全部集中在原始碼最上方的 `RULES` 區塊，逐條有中文註解
（一起分是否預設全員、要不要點開才排除、只算一個人要不要帶付款人、
誰要分是否預設全勾、結算納入規則）。你試用時想改哪條，直接指名該條即可。

## 五、資料層方向已核可入帳（#11-B）
C-① `trips.payment_methods jsonb`｜C-② `payment_label` 快照＋enum 當分類｜
C-③ 擋下刪除改提供停用｜E `split_amount_foreign`。
**正式 migration 仍另備提案待拍板**，本輪未出。

## 六、文件
`專案狀態.md` +2 條決策｜`_實作批次工作清單.md` 標註方向已核可｜`CLAUDE.md` 文件地圖補原型
