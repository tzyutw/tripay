# 停點報告

> 2026-09-05　狀態：**#32 全部做完（32-1～32-10），圓角切換器仍留著等你再挑一次**

## 手機打開這個網址（同一個連結）
**https://claude.ai/code/artifact/57aa2e36-a139-4fc6-8521-a7d1841c2098**

## 薄膜改回半透明黑，8 個色調全部過關
你說的沒錯——hero 那一套本來就是對的，要修的是淺色底那顆實心白。
改回 `rgba(0,0,0,.28)` 之後逐一實測（量按鈕實際落點的漸層色，不是把所有色停都當底色）：
最差是沖繩 **3.91:1**，其餘 4.45～11.05，**全部過 3:1**。

## 「日期框跑版」的成因不是日期框
兩個日期框實測**一樣大**（153.5×43），文字**已經垂直置中**（內容盒中心偏移 0px）。
你看到的錯位是**「當地幣別」標籤折行**把下面那一列往上擠造成的——
根因是 #29 把字級放大到 15px，但標籤欄還鎖在 58px（四個字要 60px）。
標籤欄加寬到 68 並禁止折行，錯位就消失了。
**不需要關掉原生外觀**，日期欄位該有的系統線索都還在。

## 登入頁「整體偏上」的根因是容器只有畫面一半高
`min-height:520px` 是寫死的——在 844px 的手機上根本沒撐滿，內容只能待在上半部。
改成跟著可用高度走，品牌落在 **39%**（光學中心，人眼看幾何置中會覺得偏低），
登入鈕貼底 32px。標語欄 200 → **240**，單行不折；四種尺寸都量過。

## 圓角變大之後的內距，你也說對了
內距原本剛好等於圓角（10 配 10），等於沒有餘裕。
`.rowb`／`.amtline`／`.amtrow`／`.fieldrow`／`.txrow`／`.tcard` **左右都改成 14px**，
並訂成規則：**有圓角的容器，左右內距至少要比圓角多 4px**。上下內距沒動。

**順掃時抓到一個同類的**：每人分擔列的 emoji 格寬 20px，但 #29 把 emoji 放大到 20px
之後實際需要 22px——已加寬到 24。

## 那個 `⠿` 本來就是假的
純文字、沒有任何拖曳程式，**看起來可以拖、按了完全沒反應**。
而排序本來就在 2026-09-04 的定案範圍內，是**漏做**。已做成真的：
只能從把手起拖（整列起拖會跟畫面捲動打架）、按下立刻浮起、跟手時其他列即時讓位、
放開回彈。觸控與滑鼠都能用，新增的也能拖。

## 其餘
- 分享頁的消費明細改成**與行程頁相同的日期分組**，列上只留付款人；
  **兩處共用 `expenseGroups()`／`expenseRow()`**，分享頁唯讀所以渲染成不可點的列
- 圓角預設 **C（10/20）**，六個版本的切換器**都留著**
- ⋯ 選單的「取消」補到 44px
- **順手發現並修好**：圓角切換器多了一排之後，真機模式的底部列變高，
  把登入鈕蓋住了。已改成量出實際高度再讓版面扣掉

## 逐頁截圖（依 #32-10 的新規則）
`/private/tmp/claude-501/-Users-ziyu/44be7cd8-.../scratchpad/shots32/`
**24 張**：`32_桌機_s00…s07.png`、`32_真機_s00…s07.png`（清單見同目錄 `32_截圖清單.txt`）

## 測試（十二組全過，新增一組 65 項）
60／132／54／34／18／45／79／65／114／54／46／**65**
