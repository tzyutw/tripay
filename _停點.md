# 停點｜實作-T 九項（一次「收」跑完，**已上線**）　2026-09-07 15:30
需求↔斷言對照在 `_需求台帳.md`；歷輪在 `Claude outputs/_實作B進度.md`。
**這一輪不需要 migration**，沒有 schema 變更。

## 1　「還沒算清楚」的列點不進去
那一頁把 `ExpenseGroups` 用 `readonly` 渲染，列是 `<div>`、`onClick` 是 undefined。
**這一頁本來就是要她去補資料的**，不給點等於這一頁沒有用。改成傳 `onEdit`；
封存態仍維持唯讀（點下去跳 toast，與 S-03 一致）。
⚠️ 這一頁**從來沒有被任何斷言掃過**——所以壞了沒人發現。已加進常設掃描。

## 2　金額欄的游標跑版
桌機上幾何是置中的，但 `height:28` 對 `line-height:24`——iOS 用自己的行盒擺游標，
所以偏下。`.fieldrow>input`／`.amtline input`／日期欄的 `line-height` 都改成跟自己的框一樣高。
字級與框高一個都沒動。

## 3　「花費」前那顆 icon 點完之後只有 26×26
`::after` **不能掛在 `<input>` 上**（replaced element 不渲染虛擬元素），
所以擴張區掛在外層 `.avatar.tap44`，並讓它把焦點交還給裡面那個框——
點 44×44 範圍內任何一處都會聚焦。看得見的圓圈沒放大。

## 4　結算頁看得到「怎麼算出來的」
每個人的「應分攤」拆成三段（一起分的／指名算他的／他先付出去的），各帶筆數與金額；
沒有那一段的人整段不顯示。**只是把既有計算攤開，一個算式都沒改。**
機器讀得到的 `data-shared`／`data-named`／`data-due`／`data-paid`／`data-diff` 都掛上了，
三條恆等式逐人比對全過、差額加總為 0。

> 🔴 **這一項讓兩個既有問題浮出來**：
> ① 「實際付出」原本把 `personal`（自己的）也算進去，而那幾筆沒有人分攤——
> 帳面自洽是因為「應分攤」是用 `實際付出 − 差額` 反推的，**兩欄都不誠實**。
> 改成排除 personal，三條等式才對得起來（實測差 300 的就是那一筆）。
> ② 假資料的結算金額是寫死的，與假消費算出來的淨額對不起來，
> **那三條等式在假資料上本來就不成立**。改成從 `settleTrip()` 真的算出來。

## 5　編輯行程的欄位寬度
**先做了前後對照：實作-S 一個數字都沒改**（成員列 350、日期 171、幣別鈕 350、匯率格 217）。
但她看到的不一致是真的：`.fld` 自己帶 14px 內距，放進**已經有 20px 內距的捲動區**
就變成雙重內距，同一頁出現 350 與 322 兩種寬度。在那個容器裡把 `.fld` 的左右內距歸零。

## 6　「記一筆」離底部太近
距底 16 → **30**，與上方返回鈕距頂的 30 對稱。
`.btnrow` 74→88，清單底部留白**跟著它走**（三個數字收成 CSS 變數，
不再像實作-S 那樣寫死第二個 74）。

## 7　外幣視角要顯示使用者自己填的數字
清單一律拿台幣 × 匯率回推（她截圖的「₩ 692,235」正好等於 15,383 × 45）。
改成**先看使用者填過的 `foreign_amount`**，有就原樣顯示；沒有才回推，
而且**回推的標「約」**。總花費同一套規則。台幣視角一個字都沒變。

## 8　返回後「變窄再變寬」的閃爍
根因量到了：`translateX(100%)` 讓文件在那 200ms 之內是 **780px 寬**（視窗 390），
手機瀏覽器為了塞下自動縮小顯示，動畫結束才彈回。

> ⚠️ **第一版我修錯了**：`overflow-x: clip` 加在 `.animate-slide-in` 自己身上——
> 位移的就是它，裁自己的內容沒有用，量出來還是 780。
> 改成「動畫掛子層、父層只當裁切容器」才有效。用 `clip` 不用 `hidden`：
> `hidden` 會建立捲動容器，S-03 的 sticky 按鈕就黏不住了。動畫本身沒動。

## 9　外幣灰再淡一階
`--tx2` 從 `--md`（對比 7.03）改成 `--gr`（**4.51**）。
**不能再更淡**：`#7E8B92` 只有 3.5，低於 WCAG AA。與 placeholder 的 2.54 相差 1.97，
填了跟沒填仍分得出來。

## 驗證
`tsc` 0 錯｜`vitest` **317 綠**無 Unhandled｜`pnpm run build` 過。
九項回饋 **77**｜可點區與圖示T 62｜備註與說明 64｜寫入與更新 61｜版面回歸 1163｜
輸入框與選單 53｜查不到與把手 58｜高度階梯與圖示 52｜匯率與千分位 42｜成員識別 79｜
原型真實形狀 50｜資料形狀 9｜token 52｜型別 29｜文案唯一性 13。
**六組反向驗證全部紅過**，逐條列在 `_需求台帳.md`。

**四個既有基準跟著這一輪動了，都有原因**：
`s01` 1688→**844**（先前那個 1688 是我這支腳本的異常值，Cowork 前後都量到 844；
現在實際渲染是一張行程卡、內容比視窗短）｜`s02`／`s02b` **−19**（T-5 拿掉雙重內距後
提示句不再折行）｜`s03` **+28**（T-6 的 74→88）｜S-04 段數 33→34、S-05 段數 77→104（新增的段落）。

## 超出白名單的兩個檔（都是 T-7 必須的）
`src/components/shared/types.ts`（`SharedCalc` 帶 `forTotalEff`／`forTotalAuto`、
`MoneyOpts` 帶 `raw`）與 `src/lib/format.ts`（`money()` 認得 `raw`）。
白名單只寫了 `summary.ts`「只加外幣原值的傳遞」，但那個傳遞跨這三個檔，
**沒有動任何算式**。

## 要 Rozi 在手機上看過才算數
1. 返回時的閃爍有沒有真的消失（無頭 Chrome 量得到寬度，量不到「視覺上的縮放」）
2. 金額欄的游標位置（iOS 的行盒與 Chrome 不同）
3. 「花費」前那顆 icon 點完之後好不好再點
4. 外幣灰淡了一階之後，在真機上分不分得出主從
5. 「記一筆」距底 30px 會不會太遠
6. 結算頁展開後那三段讀不讀得懂

## 順手看到但沒動
- 原型的 S-02／S-02b 欄位一致（實作-O-5）仍未補。這一輪同步了原型的
  結算三段、`.fld` 內距、底部距離、游標行盒、外幣灰。
- 假資料的「藥妝店」把 `indiv` 填成外幣數字但 `split_fill_currency` 是 `TWD`，
  所以某人的「指名算他的」會算出負數（−9,605）。**恆等式仍然成立**，
  是假資料本身不合理，這一輪沒動它。
- 既有的紅（與這一輪無關）：`原型_字級與真機測試` 3｜`原型_貼邊與結構測試` 5｜
  `原型_預設日期與欄位列測試` 1｜`原型_版面與拖曳測試` 2｜`原型_焦點與圓角測試` crash。

## 🚀 已上線　https://tzyutw.github.io/tripay/
Cowork 複驗：verify_T 41/41、verify_R 28/28、verify_S 62/62（pre-T 也全綠＝沒改壞 S）、
C6 掃 11 畫面 × 113 個互動元素、驗收案例 T1–T5 ＋ 核心區 A9／A11／A12 全過。
金絲雀打在 `220939e`（實作-T 之前）→ **16 紅，九項每項都紅過**。
production 走完整條龍的 `begin…rollback`，正式資料 0 筆變動。

## Cowork 點名下一輪要覆核的三件（不擋這次 push）
1. **`d489420` 是 Cowork 自己改的 src**（`harness/main.tsx`、`harness/fixtures.ts`），
   越界且沒有第二人檢查——下一節指令要覆核這兩個檔。
2. C6 掃出 **3 顆既有的矮按鈕**（31px）：「標記付清」×2、「開一趟自己的」。
   pre-T 量到一模一樣的三顆，**不是實作-T 造成的**，這一輪刻意不動。
3. 常設新增 **C7**（帳務三欄一致性，`acct_check.mjs`）；尾差顯示 Rozi 已裁示這一輪不動。
