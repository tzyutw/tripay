# 停點　2026-09-08　實作-AA（各自金額：切幣別要換算、編輯畫面看得到每人台幣）

## AA-1　切「$ 台幣填」⇄「₩ 外幣填」真的換算
- 兩個模式各自保有一份值＋一個「這一份是使用者打的還是系統換算填的」旗標
  （`indivAlt`／`indivOwn`／`indivAltOwn`）。使用者打的直接還原、不重算；
  空的或系統填的才用**當下的值**重新換算。公式只用比例，不用匯率（§2.2）。
- 換算與切換都抽成純函式 `convertIndiv()`／`switchFillCur()`，單元測試直接打。
- **多做了一件（規格沒寫，但條件 8 需要它才成立）**：算不出來時值照留，
  但那幾個數字在新模式下沒有意義——多一個 `indivStale` 旗標讓比對列不渲染。
  不擋的話畫面會拿 12,000＋40,000 去對台幣總額 1,140，印出「差 $ 50,860」
  ——**那正是 Rozi 手機上看到的「差 $ 52,882」**。使用者一動任何一格就清掉。

## AA-2　外幣填時，每一列看得到那個人的台幣
- 輸入框後面一個唯讀灰字 `$ 435`，`pointer-events:none`（不吃可點區）。
- 只在「填外幣且算得出來」時出現；填台幣時 0 個、外幣總額空白時 0 個。
- 320／390／430 三寬度、名字換成 20 字都不溢出——擠不下時先截名字。

## AA-3　假資料
- `?fill=twd`（台幣填 276／414）＋ **`?fill=oy`**（照她那筆 OY髮油：
  1,118／54,000、33,000／21,000，第三人沒填）。

## 驗證
- `實作_各自金額換算測試.cjs` **63 項全綠**。單元測試 7 條（條件 15 要 4 條）。
- 金絲雀五個都紅過：停用換算紅 5、拿掉列內台幣紅 3、不還原使用者的值紅 1、
  空白格填 0 紅 1、拿掉 `indivStale` 紅 1。
- 23 支回歸全綠共 2,360 項。`tsc` 0 錯、`vitest` 332 綠、`npm run build` 過。
- 條件 16：`git diff --name-only` **剛好就是白名單那四個檔**。
- 條件 17：新樣式走 `:where(.amttwd)`，沒有無前綴元素選擇器，沒有新的 `!important`
  （grep 會命中一處，那是註解裡的字，不是規則）。

## 兩個「靶沒對上就等於沒驗」
1. **`?fill=for` 驗不到「還原 vs 重算」**：12,000／18,000 來回換算剛好整除，
   兩種做法結果一模一樣，金絲雀不會紅。補了 `?fill=oy` 才有靶
   （重算會得到 32,989／21,011，還原才是 33,000／21,000）。
2. **`?fill=noforetotal` 驗不到「空白格維持空白」**：那個狀態根本換算不出來，
   空白那段程式跑不到。改成在 `?fill=oy` 把一格清空再切，才真的走過那條路。

## 兩個踩到的坑（都留了註解）
1. **新 fixture 不能定義在 `baseExpenses` 前面**：`mk()` 的 `seq` 是模組層級計數器，
   插在前面會把 base 那 11 筆的 id 整組往後推，`verify_V` 兩條當場紅。
2. **`實作_token掃描測試.cjs` 用正規表示式掃「寫死的圓角」，分不出 class 與 JS 識別字**
   ——我一個叫那個名字的區域變數讓它紅，改名後連**註解裡寫到那個字**也還是被算一處。

## 機器驗不到，只有 Rozi 在手機上才看得到
1. 「換算後台幣」那行灰字在真機上會不會太擠（320px 量得到寬度，量不到好不好看）。
2. 切幣別時兩格數字跳動的感覺——會不會讓人以為自己填的被改掉了。
3. 她那筆「OY髮油」實際打開後，683／435 是不是她預期的數字（Excel 用匯率算 460）。

## 順手看到但沒動
1. `實作_token掃描測試.cjs` 的圓角掃描會誤報 JS 識別字與註解，該加 class 上下文判斷。
2. `per[]` 對贊助的處理（固定 25,000 的落差）仍等她拍板。
3. 原型這一節**沒有同步**——AA 的檔案白名單沒有列 `Tripay_原型.html`。

---

# 追加　2026-09-08　Cowork 複驗退回的三項（`_要修的.md`）已處理

## 1　「換算不出來」改成**目標模式留空白**（原本是值照留）
- 值照留的話，`12,000`／`40,000`（韓元）會頂著「$ 台幣填」的標題待在格子裡，
  按「記下來」就當台幣存進去——**那正是 C8 守恆式抓到的「藥局灌水 50,860」**。
  我原本用 `indivStale` 把比對列藏起來，那只是把警訊藏掉，錢還是錯的。Cowork 說得對。
- 改完的實際行為（量到的）：`?fill=noforetotal` 切「$ 台幣填」→ 四格空白、
  四格都有「自動」、placeholder 皆 `285`、比對列綠 `$ 1,140 ／ $ 1,140`；
  切回去逐字還原成 `12,000／40,000／空／空`，「還沒填」警語也回來。
- `indivStale` 連同它擋比對列的邏輯整個拿掉，`src/` 命中 0 次。

## 2　「換算不出來」的判準沿用 `calc()`
- `switchFillCur` 現在收 `{ forTotalEff, noAuto }`，由元件從 `c` 傳進去。
  自己用 `f.forAmt` 判斷會把 R6（總額空白但全員都填了）誤判成算不出來。

## 3　補齊規格 §2 的三條分母路徑
- `?fill=remainder`（§47 外幣總額有值、剩餘不為 0）、
  `?fill=alltyped`（§48／R6 總額空白但全員都填）。
- `noForTotalExpense` 的標題從「藥局（外幣總額空白）」改成
  「各自付各的（總額空白＋有人沒填）」——production 那筆藥局四人都有值，走的是 R6，
  名字對不上形狀比沒有假資料更糟。`實作_這個人的帳測試.cjs` 兩處抓標題的字串跟著改。

## 指令上的數字與實測不一致（我照實測，並在此說明）
- `?fill=remainder` 在**外幣填**模式下，四人的列內台幣實測是 **88／542／265／542**
  （合計 1,437），指令寫的是 `89／541／542／265`。差別來自兩件事：
  ① 成員順序是 M0=4,050、M1=24,675、M2=12,060、M3 未填（自動 24,675）；
  ② 引擎的「差額歸付款人」把付款人 M0 從 89 調成 88。
  **合計 1,437 這條恆等式是對的**，所以我斷言合計與各項實測值，不套用指令的清單。
- 切到台幣填之後三格是 `89／542／265`、空白格維持空白、placeholder `541`
  ——這部分與指令一致（指令自己也已訂正過這一條）。
- ⚠️ 順帶量到：同一個人在**外幣填**看到 `$ 88`、切到**台幣填**格子裡是 `89`，
  差 1 元。原因是「差額歸付款人」只在引擎的 `shares` 生效，比例換算不套用它。
  兩邊的**合計都是 1,437**。要不要讓換算也吃差額歸屬，是帳務語意，等裁示。

## 驗證
- `實作_各自金額換算測試.cjs` **80 項全綠**。
  金絲雀：R6 停用紅 3、換算不出來時值照留紅 6、判準改回自己寫紅 1。
- 23 支回歸全綠、`vitest` 333 綠、`tsc` 0 錯、`npm run build` 過。
