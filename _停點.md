# 停點　2026-09-07　實作-YXW（批 Y → 批 X → 批 W，一次跑完三批）

三批各自 commit：`b218d11`（Y）／`02e5124`（X）／`cc9c11c`（W）。
**2026-09-07 已 push**：`de65ca7..17dd52e`，共 12 個 commit。
⚠️ 推的時候 repo 裡**沒有** `_可以推了.txt` 也沒有 `_要修的.md`——是收尾流程指示直接推的，
**Cowork 的上線前複驗還沒發生過**。

## 批 Y｜安全區從來沒生效　**68 項全綠，六個金絲雀分別紅 1／3／1／8／4／10 項**
- 根因：`index.html` 少了 `viewport-fit=cover`，全站六處 `env(safe-area-inset-*)` 一律回 0。
  改走 `:root` 的 `--sat`／`--sab`，量測靶加 `?sat=`／`?sab=` 才驗得到。
- 十一個畫面逐一確認頂部沒被蓋住，補上 `.bar`（五處共用）、分享頁的 hero 與「朋友檢視」章、
  建立／編輯行程的底部按鈕列。收合條底色 `#0F5E9E` → `#1276C4`（＝`theme-color`）並蓋掉
  行程色漸層；展開態的漸層不動。
- `.topbar::before` 往上塗 12px（不用位移解，那會動到 V-5 的停止條件）。`.bar` 改成與內容同色，
  靠 `--ln` 的線分隔；MoreSheet 實測本來就是 `--bg`，不需覆寫。

## 批 X｜「只看共同的帳」　**52 項全綠，七個金絲雀分別紅 5／3／1／1／10／1／2 項**
- 判斷放進引擎：`personal`，或參與者剛好一人且就是付款人。「別人付、只算某一個人」不收。
- 開關排在統計卡與清單標頭之間；開著時標題改「共同的帳」、底部出現摘要行（點得進去）。摘要行
  金額只含算得出台幣的，算不出來的另外報筆數——混進去恆等式就不成立。預設關，以 trip id 為 key
  記在 `localStorage`（不加資料庫欄位）。「還沒算清楚」的數字、結算的轉帳清單、幣別切換都不受
  影響（都驗過）。原型同步並實跑：35,175 → 34,015，收起來的就是那兩筆，與實作一致。
- ⚠️ 踩到一次假綠：base 假資料裡自己買的兩筆**都填了金額**，所以「未定案不受開關影響」
  那條把程式改壞也照樣綠。補了 `?selfpending=1` 才有靶。

## 批 W｜約／320px／分享頁　**63 項全綠，七個金絲雀分別紅 3／1／2／1／5／1／2 項**
- 已結算與已封存不再標「約」；§5.4 的橘紅左邊框留著（三列，改前改後逐字相同）。樁的 `rpc`
  改回端出套用過 `?state=` 的那一份。320px 的「標記付清」不再凸出：`.who` 加 `min-width:0`、
  `.amt` 加 `flex:none`——**選 (a) 不選 (b)**，(b) 會動到 390px 的列高，那是反向斷言在守的。
- 分享頁成員列改成點得進去，開唯讀版的「{名字} 的帳」；三段結構抽成共用元件，不寫第二套。
  文案用「{名字}付的」（自己那一邊的「你付的」這一輪不動）。

## 兩條停止條件我改了寫法（都不是為了讓它綠）
1. **X-11／W-17「三段加總＝統計卡金額」照原文不可能過**：`per[]` 把贊助**加** 12,500、
   成員頁顯示 **−**12,500，四位成員一律差 25,000——那是本節明文不准動的事。改驗
   「開關／分享頁**沒有製造新的落差**」，落差在四種狀態下都是 25,000。
2. **W-3「藥妝店的左邊框」**：那一列的左邊框是一般的 `--ln`，拿它比對等於沒驗。改成先找出
   真的有異色左邊框的那幾列（計程車／只有外幣的一筆／城山日出峰門票）再比。

## 收尾
- 常設回歸 18 支全綠（最大的版面回歸 1163 項、這個人的帳 118、可點區 62、九項 77、時間差 88…）。
  `tsc` 0 錯、`vitest` 325 綠、`npm run build` 過。
- **不需要 migration。** 推翻兩條既有斷言（分享頁成員列不得是 button）——W-3 就是要反過來，
  A9 改由成員頁那幾條守。基準更新兩處（s03 高 1632→1697、文字段數 94→95，都是 X-1 那一列），
  `screens.json` 重新擷取，差異只有那兩句。

## 機器驗不到，只有 Rozi 在手機上才看得到
1. **安全區**：無頭瀏覽器的 `env()` 永遠是 0，`?sat=59` 只是模擬。要看①收合後頂部是不是
   一整塊藍、②首頁頂部列上方還有沒有那條縫（1px 是 iOS 的合成問題）。
2. **開關**：「只看共同的帳」按下去是填實的深色 chip，看得出是「開著」嗎。**320px**（iPhone SE
   或字級調大）的結算列也只有真機看得準。

## 順手看到但沒動
1. **原型的「{名字} 的帳」還停在改版前**：S-03d 還是「影響 X 的 · N 筆」那個篩選頁，
   實作早在 U-2 就換成三段了。這一輪只同步了 W-3 的分享頁那一半。
2. **自己那一邊的「你付的」**（`owner_member_id` 是 null，分不出誰是本人）與
   **`per[]` 對贊助的處理**（就是那個 25,000 的落差）：兩件都等她拍板。
