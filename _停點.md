# 停點｜實作-B～N（**已上線**）　2026-09-06 17:40

## 實作-N　匯率的「1」沒有自動帶值 ＋ 類別 emoji 字典太小

### 1. 🔴 匯率填了卻不換算
根因如指令所述：原型的 `tripToForm` 會依 `oneSideOf()` 自動帶「1」，**實作漏了**。
兩欄都空，而另一欄的 placeholder 是灰色的「1」——**看起來像已經填好**。
Rozi 只填了 JPY 那欄的 0.19，`tripRate()` 拿 `Number(null)=0` 判 `<=0` 回 null，
**全程沒有任何警告**，記的帳一直顯示「還沒填」。

改了三處：`defaultRates()`（初始化／載入既有行程／切幣別都走它）、
placeholder 改依 `oneSideOf` 而不是寫死 `isTwd`、只填一欄時出
「還差一欄，兩邊都填才換算得出來」。
**`tripRate()` 的算式與 `calc()` 的換算方向一行都沒動。**

量到：KRW → 1 在台幣欄（排第一）、JPY → 1 在 JPY 欄（排第一），placeholder 跟著換邊；
端到端「外幣 5000、台幣空」＋ 匯率 0.21 → 消費列顯示 **$ 23,810**＝`round(5000/0.21)`，
沒有匯率時顯示「還沒填」。

### 2. 類別 emoji 字典從 5 條擴到 13 條
原型與實作**同一份**（測試會把原型的 `EMOJI_RULES` 解析出來逐條比對，走鐘就紅）。
順序由具體到通用：`機票` 不會被 `票` 搶走、`藥妝店` 走 💄 不走 🛍️。
指令指定的 11 組輸入全對，`大國` → ➕（推不出來就是 ➕，不硬猜）。
**已存在資料庫的 `category_emoji` 沒有回頭重算**，`emojiManual` 的判斷也還在。

### 反向驗證（五條全紅）
拿掉「1」的預設值（2 紅）｜placeholder 改回寫死 `isTwd`（2 紅）｜拿掉只填一欄的提示｜
拿掉「帽子→👕」那條規則｜拿掉 `emojiManual` 判斷。

> ⚠️ 「placeholder 改回寫死 `isTwd`」第一次**沒有紅**——fixture 是 KRW，
> `oneSideOf('KRW')==='twd'` 剛好與 `isTwd` 同結果。加了 `?cur=JPY` 模式才驗得出來。
> 這與先前「假資料只走 happy path」是同一類問題：**對照組要挑得出差別的那一組。**

### 測試
vitest **263 綠**、無 Unhandled Errors｜版面回歸 1163｜輸入框與選單 51｜查不到與把手 58｜
成員識別 79｜原型真實形狀 50｜資料形狀 9｜token 52｜型別對帳 29｜文案唯一性 13。

## 實作-M（上一輪，已上線）
查不到行程卡在「載入中」（`.single()` 回 406 沒被 render）→ 新增 `NotFound`，
S-03 有「回到我的行程」、S-06 不放按鈕｜拖曳把手可點區補到 44×44｜
`.rmbtn` 重複定義（抽取器排除手寫版）｜消費列的「自己的」標記三種情況。

## 實作-L（上一輪，已上線）
輸入框回歸（`:where()` 降權重連 preflight 都輸掉）｜匯率打字失焦（元件內部定義函式元件）｜
S-02b-14 編輯行程可改名｜⋯ 改成獨立頁面 `/trips/:id/more`。

## 前幾輪（都已上線，細節在 `Claude outputs/_實作B進度.md`）
**B–C** 全站對齊原型＋引擎查證＋版面回歸測試｜**D／E** 複驗退回兩輪｜**F** 推上線｜
**G** 分享頁轉帳列重複 12 次｜**H** 日期欄位沒接上 `.datefield`、移除 G-09 預填成員｜
**I** 五個線上實測 bug｜**J** 成員識別全站貫徹（＋第一輪複驗退回）｜
**K** 假資料換成真實形狀（`?members=noemoji`／`?expenses=none`／`?rate=half`／`?pays=none`）。

**真名外洩處置**：查證 `Tripay_原型.html` 的真名**從未被 commit、也從未 push**
（`git log --all -S` 0 個 commit、`origin/main` 上是 `Alex/Robin/Sam/Kai`）。
`.gitignore` 那幾行已移除——原型本來就在版控裡。
散在其他歷史文件裡的真名屬於改寫已推出的歷史＝不可逆，**已交 Rozi 決定**。

## 🚀 已上線　https://tzyutw.github.io/tripay/
`750d857` 實作-A｜`723466a` B＋C｜`8cba1ef` D｜`355a64e` E｜`3e01b02` 修 CI lockfile｜
`f8c00ca` G｜（本次）H。

**前兩次部署失敗是我造成的**：CI 走 `pnpm install --frozen-lockfile`，我卻整輪用 npm，
`pnpm-lock.yaml` 沒跟上。**往後這個 repo 一律用 pnpm，推之前先跑 CI 那兩道指令。**

**上線後的線上驗證由 Cowork 做**：線上頁面、anon 六類仍 0、owner 4／404／16、分享頁端到端。

## 已結案的舊事
`實作_212對帳表.cjs` 已由 Cowork 於 2026-09-06 作廢（判定規則會自動蓋章），
驗收判定改由 Cowork 寫、Cowork 跑（`_實作階段運作方式.md`「六之零」）。
S-06 hero 不帶成員 emoji、`➕` 維持原型——Rozi 2026-09-05 都已裁示。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
