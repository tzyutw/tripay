# 停點｜實作-B ＋ 實作-C ＋ 實作-D（Cowork 複驗退回三項）　2026-09-05 22:50

**已 commit，未 push**（等 `_可以推了.txt`）。細節：`Claude outputs/_實作B進度.md`、
`Claude outputs/_212對帳表.md`。
vitest **201 綠**（3 skip）｜版面回歸 172｜token 52｜型別對帳 29｜文案唯一性 13｜端到端 2｜
212 交叉檢查 10｜`tsc` 零錯誤｜`vite build` 通過。B 開始前是 92 綠。

## 實作-D　三項都修好
- **① 兩個刪除對話框同時渲染**（真的壞掉）：根因是 B-3 重寫 render 時「保留尾巴」，
  尾巴裡還有舊的 `{deleteOpen && (…)}`。兩個共用同一個 state，使用者怎麼打都有一顆是灰的。
  舊的 40 行已刪，`grep -c "{deleteOpen && ("` ＝ 1。
- **② S-03b 三筆文案**逐字對齊原型 2866–2868（標題改「複製文字摘要」、兩句講好處的灰字砍掉）。
- **③ 212 對帳表**：`實作_212對帳表.cjs` 從盤點檔**解析**產生，不手抄；
  宣告與證據衝突就紅（第一次跑就抓到我自己標錯一列）。

### ⚠️ 停止條件「未做＝0」**沒有達成**：未做 4 列
| 編號 | 名稱 | 差在哪 |
|---|---|---|
| S-02c-10 | 三層 fallback | `TripFormSheet` 直接印 `{m.emoji}`，缺「名字第一個字＋填色圓底」與 🙂 兩層 |
| S-03c-2 | 後果說明 | 原型是「刪掉就**救不回來**：」 |
| S-03c-3 | 影響清單 | 原型是三個 `<li>`，目前收成一句話 |
| S-03c-4 | 確認提示 | 原型是「請輸入「刪除」兩個字」 |

四列都在 212 母體裡標「要改」，是 B 本來就該做、被我漏掉的（沒有表所以沒發現）。
依 `_要修的.md` ③「先寫進表裡並回報，修不修等這份表出來再說」，
**這一輪沒有動它們**（S-02c-10 還會動到沒被點名的 `TripFormSheet.tsx`）。
要修的話四項都很小，等指示。

## 實作-C（上一輪，Cowork 已獨立驗過）
- **C-1 沒有改引擎**：`calculate-settlement` 在查詢階段就用 `.eq("twd_pending", false)`
  濾掉待填的筆，排在型別驗證之前。422 只來自「金額 null 但 pending false」，
  那正是 S-04-29 擋的。→ **C-4 四趟 verify 不需要跑。**
- **C-2 hub**：direct／hub 逐人淨額完全相同（Rozi +46,180、小美 −8,220、阿明 −17,740、
  小魚 −20,220），只有轉帳路徑不同。`TransferView` 兩個檔案共用。
- **C-3 版面回歸**：抓到並修好 S-02 日期列少 `min-w-0` 造成的 28px 溢出；斷言帶金絲雀。
- **C-5**：環境地雷修三處（`ShareSheet` 少 portal、`autoFocus`、DELETE 沒斷言列數）。
  `TC-STATUS-18～20` 仍 skip（本機沒 Docker／Supabase CLI，**沒有自行安裝**）。

## 要 Rozi 裁示（原型與佇列指令有出入）
1. **S-06 hero 的成員 emoji**：佇列說拿掉，**原型仍然有**。目前照佇列做，
   差異釘在 `SharePage.test.tsx` 的 `KNOWN_DIVERGENCE`（212 表列「不適用」那一列）。
2. **`➕` 當「還沒選 emoji」的預設值**：CLAUDE.md 說該走 Feather，原型仍輸出 `➕`。維持原型。

## 要 Rozi 在手機上看過才算數
S-04 日期欄位與 S-02 出發／回程兩欄：`input[type=date]` 排版由 iOS 決定，無頭 Chrome 量不準（#32）。
要確認：中文日期會不會比框寬、空值與有值的框寬度一不一樣、文字有沒有垂直置中。

## 順手看到但沒動
`ShareSheet` 仍是自寫版面（只補 portal，沒改長相）｜`.ic2::after` 在 `index.css` 有兩份定義
（效果相同但會漂移）｜`expense_type: 'personal'` 在原型的三種分帳方式裡沒有對應｜
寫死色碼依裁示這一輪不處理。
