# 停點｜實作-V 七項（一次「收」跑完，**未推——等 `_可以推了.txt`**）　2026-09-07 23:30
需求↔斷言對照在 `_需求台帳.md`。**這一輪不需要 migration**，沒有 schema 變更。

## V-1　🔴 改完一筆再打開看到的是舊資料
編輯表單自己有一支 `['expense', id]`（**單數**）的查詢，但存檔後只清了
`['expenses', tripId]`（**複數**，整份清單）。兩個 key 不同 → 單筆快取沒被清 →
全站 `staleTime: 30_000` 讓她 30 秒內重開同一筆會看到舊資料。
存檔與刪除兩處都補上清單筆快取，順帶清「事後補記的預設日期」那一支。
**沒有動 `staleTime`**（全站設定），**也沒有在關表單前 await refetch**
——她正在大量登錄，每一筆都慢一拍比偶爾看到舊資料更煩。

## V-0　🔴 量測靶自己是假的（四個問題，這一輪才挖出來）
1. **stub 的寫入完全不改動假資料**——「改 → 存 → 重開」永遠讀到原始值，
   **修好與沒修好量起來一模一樣**。
2. **寫入是 eager 執行的**：`.update(row).eq('id', x)` 的 `.eq()` 在 `.update()` 之後才呼叫，
   一收到就動手的話 `byId` 還是 null，**會把整張表都改掉**。改成延後到 `await` 才執行。
3. **讀出去回傳同一個陣列參考** → react-query 判定「沒變」→ 畫面不重繪，
   看起來像快取沒清。改成每次讀都複製一份。
4. **QueryClient 是 `retry:false, gcTime:0`**，快取等於不存在——
   **V-1 的金絲雀第一次跑是綠的**。改成與 `src/App.tsx` 相同的 `staleTime: 30_000` 才紅得出來。
5. **（H-1，Cowork 複驗時抓到）新插入的消費沒有 `expense_splits`**。
   真實的 PostgREST 對 `select('*, expense_splits(*)')` **一定回陣列**，空的就是 `[]`；
   樁少了它 → `e.expense_splits.filter(...)` 拿到 undefined → **整個 app 白屏**，
   而那是量測靶造出來的假故障。連帶補上「分帳列要掛回它所屬的那一筆」與
   「delete 時巢狀那份也要清掉」（不然改分帳方式會愈存愈多）。
   ⚠️ **沒有去產品那一行加 `?? []`**——那等於把量測靶的問題藏起來。

> 這四點合起來的意思是：**這一類「快取 key 不一致」的 bug，在量測靶上從來驗不到。**
> 現在可以了，代價是 `installStub` 從 20 行變成 90 行。

## V-2　贊助在「{名字} 的帳」符號相反
S-03 早就有正確處理（負號＋收入色），新頁面沒抄過去，而且「你付的 $ 50,000」是錯的敘述
（爸爸贊助 50,000，不是 Alex 付的）。改成接上同一套語彙。
⚠️ **`per[]`（每人金額）仍把贊助當正數累加，這一輪明文不動**——那是帳務語意，要 Rozi 拍板。
所以斷言驗的是「段小計與統計卡的差**剛好等於贊助的兩倍**」，不是「兩邊相等」。
**她拍板之後這條要改回相等。**

## V-3　警示行的圖示與文字被拆成兩行
根因是 `flex-wrap: wrap`：圖示與文字是兩個 flex item，文字放不下時**整個 item 換行**。
改成 `nowrap` ＋ `align-items:flex-start`，文字在自己內部斷行。
`.note.calm` 多一顆「去設定」要能自己換行，所以把內文包一層 `.notebody`。

## V-4／V-5／V-6
備註 placeholder → `補一句`。
首頁頂部列 sticky（底色不透明、不蓋住內容）。
行程頁 hero **151 → 56 隨捲動收合**，用 CSS `animation-timeline: scroll()`
（不用 JS 監聽捲動）；收合後行程名移到與返回鍵同一列，兩顆鍵仍 44×44 且沒被蓋住。
⚠️ `collapsing` 只掛行程頁——分享頁共用 `.hero`，沒有這個需求。
⚠️ 漸層改用 `backgroundImage`：`background` 簡寫會把 CSS 的 `background-color` 重設成
transparent，收合後就會透出捲過去的消費列。

## 驗證
`tsc` 0 錯｜`vitest` **325 綠**無 Unhandled｜`pnpm run build` 過。
時間差與收合 **88**（含 H-1 與新增路徑）｜這個人的帳 116｜九項回饋 77｜備註與說明 65｜可點區與圖示T 62｜
寫入與更新 61｜版面回歸 1163｜其餘全綠。**六組反向驗證全部紅過。**
> ⚠️ H-1 的金絲雀**第一次沒紅**：走畫面新增時，分帳列的 insert 會順手把
> `expense_splits` 陣列建起來，所以「有沒有預設 `[]`」看不出差別。
> 改成**直接對樁**插一筆沒有分帳的消費再驗，才紅得出來。
> ——這與 V-0 第 4 點是同一個教訓：**驗的路徑要能碰到那一行才算數。**
**九畫面的捲動高度與上一輪逐項相同（差 0）**——sticky 不改變文件高度。

## 超出白名單的一個檔
`src/pages/SettlementPage.tsx`：V-3 明文要求 `.note.calm` 一起改，
而它的內文要包一層才不會整塊換行（`.note.warn` 只有圖示＋文字，光靠 CSS 就夠）。

## 要 Rozi 在手機上看過才算數
1. 改完一筆立刻重開，看到的是不是新的（真機的網路延遲與 Chrome 不同）
2. hero 收合的過渡順不順、收合後那一條會不會太薄
3. 首頁頂部列固定之後，捲動時會不會遮到卡片
4. 警示行現在圖示與文字在同一行起頭

## 順手看到但沒動
- **`per[]` 要不要排除贊助**：`total` 已經排除、`per[]` 沒有。這是帳務語意，**等她拍板**。
- 已結算／已封存仍顯示「約」（§5.6），上一輪就記著了，等指令。
- 原型的 S-02／S-02b 欄位一致（實作-O-5）仍未補。

## 🚀 已上線　https://tzyutw.github.io/tripay/
Cowork 複驗：產品程式碼停止條件 0～20 除第 4（量測靶缺口＝H-1，已補）與
第 7（`per[]` 是否排除贊助，已列給 Rozi 拍板）之外全綠；
verify_T 41／verify_S 62／verify_R 28／verify_U7 28 全綠；
verify_U 的 19d 為已知紅（§5.6「約」，下一節處理）；
C6 掃 113 個互動元素全綠；**C8 守恆式在 Rozi 的正式帳（111 筆）差 0**。
