# 停點｜實作-U 七項（含 Rozi 追加的 U-7，一次「收」跑完，**未推**）　2026-09-07 19:40
需求↔斷言對照在 `_需求台帳.md`。**這一輪不需要 migration**，沒有 schema 變更。

## U-1　🔴 一筆真的算錯的帳：外幣總額空白時把外幣當台幣
`calc()` 的比例回推在 `forTotalEff` 為 null 時掉進 `Math.round(v)`，
**直接把 12,000／40,000 韓元當成台幣**。production 的「藥局」那筆台幣只有 1,140，
各人分擔卻加到 52,000（灌水 50,860）——**畫面上沒有任何一句話說它算錯了**。
改成：填的是外幣、外幣總額空白時，**每一個參與者都算不出台幣**（包含已經填了的人）。
§2.2 用比例回推、§2.5「不得由系統推斷」——**沒有拿行程匯率去補分母**。
（我用只讀查詢確認 production 現在剛好只有那一筆是這個形狀。）

## U-1-b　算不出來時要講得出「要補什麼」
編輯那一筆時出現 §2.3 的原句「還沒填外幣總額，先補上才算得出各自要付多少」。
只在這個狀態出現（有反向斷言）。

## U-2　成員金額列點進去 ＝「{名字} 的帳」
三段（跟大家平分的／各自付各的／只算我的），各帶筆數與小計，沒有那一段整段不顯示。
**三段小計加起來等於總行程頁那一列的金額**（有斷言）。
他同時是付款人的列多一行小字「你付的 $ N」；算不出來的列顯示「還沒算清楚」且不計入小計。
每一列點得進編輯；封存態維持唯讀＋toast。分享頁不受影響（反向斷言）。
註腳改成常駐的「點名字看這個人的帳是怎麼算出來的」。

## U-4　三顆 31px 的矮按鈕
`.clearbtn`（結算頁「標記付清」、分享頁「開一趟自己的」）用透明 `::after`
撐到 44×44。**看得見的仍是 31px**（有反向斷言），沒有動 height／padding／字級。

## U-5　hub 模式從來沒被驗過
harness 加 `?settle=hub`，中心人刻意挑**不是付最多錢的那個**。
三段恆等式在 hub 下重跑全過。
> ⚠️ 正確的恆等式是「**每一筆轉帳的其中一端是中心人**」，不是「所有人的收款人都是同一個」
> ——中心人也會轉出去給該拿回的人。我第一版寫成後者，紅了才發現。

## U-6　清單上看得出來哪幾筆還沒算清楚
§5.4 原本只定義兩種狀態，「藥局」是**第三種**：整筆台幣有填、但各自金額算不出來，
`calc()` 因為 `noAutoReason` 不標 `estimated`，**兩種樣式都套不上，看起來完全正常**。
沿用既有的橘紅左框；金額欄照常顯示台幣總額（那個數字是真的）。
**`規格_金額未定案與幣別.md` §5.4 的表已經補上這一列**——不補的話下一輪會再被拿掉一次。

## 覆核 Cowork 越界寫的程式（指令交辦）
**`d489420` 只動了 `_指令佇列.md`（+17 行），沒有碰任何 `src/` 檔案。**
harness 的 `unsettled`／`view=foreign` 參數與 `data-settle-row` 屬性
是終端機在實作-T（`39c4cc9`）做的，有經過那一輪複驗。**那次越界沒有發生。**

## U-7　四層提示都在誤導（Rozi 追加，同一次「收」接著做）
> 她的話：「這一筆金額不是一定不對，是他現在資訊不夠完整……
> 他還不知道說這一筆帳不清楚，是不清楚在另外兩個人到底是零，還是他們有要負擔的金額。
> 所以我覺得是在我們的標示不夠清楚。」

同一個狀態下四個地方在說話，兩個是錯的：
- **金額區灰字**「填一邊就好，另一邊自動換算」→ 改成 `還需要 KRW 總額`。
  ⚠️ 紅框的觸發條件**正是** `noAutoReason==='noForeignTotal'`——
  **系統知道卡在哪，只是嘴上不說**。這不是缺資訊，是缺把已知資訊講出來。
- **存檔後 toast**「先照均分算」→ **那句是假的**（根本沒有均分，是算不出來）。
  加第五種並**排在人數分支之前**；規格 §4 的表已同步補上這一列。
- **各自金額區下方**改成**點名是誰沒填**（1／2／3 人三種寫法，一律用「他們」）。
- **沒填的那幾格加紅框**，已填的不加；**只紅框、不加字**（欄位維持橫槓）。

⚠️ **不攔存檔**：規格 §4「一律允許存檔」維持不動——有一條反向斷言守著
（「記下來」不得被 disable）。文案集中在 `messages.ts`，元件裡沒有中文字面值。

## 驗證
`tsc` 0 錯｜`vitest` **325 綠**無 Unhandled｜`pnpm run build` 過。
這個人的帳 **116**（含 U-7 的 14 條停止條件）｜九項回饋 77｜備註與說明 65｜
可點區與圖示T 62｜寫入與更新 61｜
版面回歸 1163｜輸入框與選單 53｜查不到與把手 58｜高度階梯與圖示 52｜匯率與千分位 42｜
成員識別 79｜原型真實形狀 50｜資料形狀 9｜token 52｜型別 29｜文案唯一性 13。
**九組反向驗證全部紅過**（U-1～U-6 六組 ＋ U-7 三組：灰字不切換 → 4 紅、
紅框標到全部格子 → 3 紅、toast 落回「先照均分算」→ 單元 2 紅），
逐條列在 `_需求台帳.md`。

## 超出白名單的三個檔
- `src/test/harness/installStub.ts`：樁的 `.eq('id', …)` **沒有真的過濾**，
  「編輯第 N 筆」永遠拿到第一筆——U-1-b 的斷言一開始就驗在錯的資料上。非修不可。
- `src/test/fixtures/*.json`：原型基準重新擷取（註腳文案改了）。
- `規格_金額未定案與幣別.md`：U-6 明文要求同步 §5.4。

## 要 Rozi 在手機上看過才算數
1. 「藥局」那種筆現在在清單上有橘紅左框，看不看得出來要調整
2. 點成員金額列進去的三段讀不讀得懂、順序對不對
3. 「標記付清」現在按不按得到（看得見的大小沒變）
4. 她按下「結算行程」時 hub 模式的轉帳清單對不對——**這是第一次有人用 hub 結算**

## 順手看到但沒動
- 假資料的「藥妝店」把外幣數字填進 `split_amount` 卻標 `TWD`，
  所以某人的「各自付各的」小計是負的（−10,965）。恆等式仍成立，是假資料本身不合理。
- 「爸爸贊助」在新頁面上顯示 `$ 12,500` 而不是負數（S-03 有特別處理 `e.sponsor`，
  新頁面沒有）。指令沒點名，沒動。
- 原型的 S-02／S-02b 欄位一致（實作-O-5）仍未補。
- 既有的紅（與這一輪無關）：`原型_字級與真機測試` 3｜`原型_貼邊與結構測試` 5｜
  `原型_預設日期與欄位列測試` 1｜`原型_版面與拖曳測試` 2｜`原型_焦點與圓角測試` crash。

## 🚀 尚未 push——等 `_可以推了.txt`
