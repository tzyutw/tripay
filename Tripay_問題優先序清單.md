# Tripay 問題優先序清單

整理自過去四個 session（家庭旅遊分帳Dashboard、Trip split app analysis、HTML travel dashboard iOS deployment、Tripay QA smoke test）與濟州島四寶團實測回饋。依 PRD 慣用的 P1–P4 分級，P1 為必須先解決。

---

## P1：資料模型缺口（影響對帳正確性，優先於視覺）

**0. 各自金額沒有幣別，填外幣會靜默算錯**（2026-09-04 讀碼確認，既有風險）
`expense_splits.split_amount` **不帶幣別**，結算引擎一律當台幣加總
（`settlement-engine.ts:109-119`），表單的差額比對列也寫 `$`——
但 **UI 從來沒有標示這一欄必須填台幣**。在日幣／韓圜行程裡照著當地金額填，
數字會被當台幣算進結算，**不報錯、直接算錯**，而且錯在「各付各的」這種本來就容易出錯的情境。
修法提案：新增 `expense_splits.split_amount_foreign`，`split_amount` 語意維持台幣供結算，
既有資料不動；只有外幣而台幣待填時標 `split_pending`，引擎已支援跳過。
詳見 `wireframe_v5_完整版.html` 的「資料層提案 · E」。**列入實作批次。**


**1. 缺少「代墊」交易型態**
現有邏輯只有「均分」，無法記錄「某人現金代墊，其他人各欠一筆」。濟州島四寶團實測發現這是最容易出錯的環節（WOWPASS、國泰旅平險、炒年糕等個別結算項目都得手動算）。這是資料模型層級的缺口，不是 UI 問題，建議最先處理。

**2. 資料一重整就遺失**
GitHub Pages 版本上機測試後確認：重整頁面資料消失。沒有可靠的本地持久化（localStorage 疑似未正確寫入或讀取），這是會直接勸退使用者的 bug。

**3. 付款卡別／現金 vs 刷卡邏輯缺失**
App 目前強制輸入外幣＋匯率，但實際多數支出是台幣現金。沒有付款方式欄位，導致無法對帳「哪張卡的帳」。

---

## P2：核心流程與資訊架構（影響能不能順暢用）

**4. 首頁／總覽功能重疊**
「建立第一個旅遊」與「建立新旅遊」兩個入口重複，且總覽頁缺少你要的國家卡（列城市、放照片，不顯示金額）。需要拆出獨立首頁。

**5. 匯率強制輸入卡住建立流程**
目前必須先輸入匯率才能建立旅遊並開始記帳，但實務上常常匯率還沒確定。應改為選填。

**6. 贊助金顯示位置錯誤**
目前列在模式設定頁，應只在最終結算畫面顯示。

**7. QA 驗證缺口尚未關閉**
E2E 測試 TC-STATUS-18～20 未本地執行、Codex CR 補測 6 項狀態未核實、邊界案例 TC-DIFF-03／TC-COMBO-01 未確認是否關閉。這些是「已知但沒答案」的項目，不算已驗證通過。

---

## P3：視覺與互動細節

**8. 視覺系統重新設計**
目前被評「很醜」、「只有功能沒有質感」。需要先做競品分析（記帳/旅遊工具的視覺參考），再定義「現代但像日記」的敘事方向，wireframe 確認後才進實作。

**9. 成員 Icon／顏色不統一**
通用底色到下一頁變色；姓名首字是 Emoji 時會與預設頭像重複，造成兩個 Emoji 疊加。需決定「預設／自選／使用者自訂」三選一。

**10. 頁面 Icon 過多、文字顯示缺損**
「會員／編輯／完成旅遊／刪除」四個 icon 密度過高，部分文字被截斷，需要從 UX 角度精簡。

---

## P4：範疇外，但要有共識

**11. 多人即時協作**
純 HTML／localStorage 架構做不到多人同時編輯，目前定位是「你記帳、他人看結果」。若要多人共同輸入，需要換架構（後端資料庫），這是重大範疇決策，不建議現階段做。

---

## 建議下一步

先處理 P1 三項（代墊模式、資料遺失、付款方式），這三個不解決，其他做得再漂亮也是空中樓閣。P1 解完再回頭做競品分析與視覺重新設計，避免重工。

## P1-1 已封存的行程，消費仍點得進去編輯（2026-09-04 發現）

封存的定義是唯讀，但唯讀只綁在 `isArchived` 的部分位置，消費列的 `openEdit` 沒有狀態守衛。
已結算、甚至已封存的行程，消費仍然點得進去改。既有 bug，非本輪造成。
影響：已結算行程的消費被改動後，統計卡會與結算頁對不起來。
來源：終端機 V4 唯讀查證（ExpenseListPage:277-278、:450）。
處置：本輪不修（會讓原型輪範圍蔓延），另案排入實作批次。
