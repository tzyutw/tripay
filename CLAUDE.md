# Tripay

家庭／朋友旅遊分帳 PWA（Vite + React + TS + Tailwind + Supabase），
線上：https://tzyutw.github.io/tripay/ ｜ Phase 1 已上線，Phase 2（副卡記帳）進行中。

## 文件地圖

| 檔案 | 地位 |
|------|------|
| `專案狀態.md` | **進度總表＋已確認決策紀錄**。每輪開始先讀，收尾要更新 |
| `協作規則_精簡版_v2.md` | **流程權威**。決策模式、部署授權、刪除護欄的原文在這 |
| `決策符合性檢查表.md` | **每次 UI 改動或視覺提案交付前必跑**。決策表新增一列，這裡同步新增一列 |
| `打磨輪_tokens與before-after.html` | 現行視覺系統（日記精裝）的 token 定義 · 等寬只用於金額 |
| `Tripay_原型.html` | **唯一的視覺稿＋可點原型**（標註／操作雙模式，S-XX-NN 編號）。**由終端機 Claude Code 維護**。`wireframe_v5_完整版.html` 與 `分帳模型原型.html` 併入後作廢 |
| `規格_金額未定案與幣別.md` | **金額未定案狀態＋各自金額幣別的完整規格**（2026-09-04 定案）。原型與實作都以它為準 |
| `_延後與不做清單.md` | **討論過、有結論、但這期不做**的功能，每項都寫「什麼時候可以重新考慮」。**盤點功能缺口前先讀這份**，避免重複討論 |
| `_盤點_畫面功能.md` | **畫面功能盤點索引**（234 列，記錄盤點時的 commit）。要查現況先看這裡 |
| `_實作批次工作清單.md` | 已裁示、待實作的項目；實作時逐條對號 |
| `歷史資料匯入/` | 四趟 Excel 重謄的解析／自動化／驗收基準 |

## 決策模式：預設執行＋事後可推翻

有明確建議且可逆的決策（含小型產品取捨）**直接執行，不要停下來問**；
回報附「決策紀錄」：選了什麼／為什麼／怎麼推翻。Rozi 事後隨時可推翻。

**五種例外永遠事前問，一項都不能少：**

1. **不可逆且無備份的動作**（刪資料若先備份、事後可還原，視為可逆）
   ⚠️ **schema 變更不在此列**：加欄位／加索引／加政策是**可回滾的**（加錯就砍掉，
   既有資料不受影響），由 Cowork 端自行決定並套用，**不要拿去問 Rozi**。
   只有「會刪除既有資料」或「無法回滾」的 schema 變更才問。
   Rozi 的角色是視覺設計、UX、產品邏輯——資料庫欄位怎麼設計不是她的職責。
2. **要花錢**（付費服務、外部 API 採購）
3. **安全性／隱私**（權限、資料曝露）
4. **重大範疇取捨**（新 Phase、砍功能這種等級；小取捨不算）
5. **需要 Rozi 記憶或偏好才能回答的資料題**

> 這五條在 `_指令佇列.md` 裡出現也一樣要停——佇列檔視同 Rozi 的指令，
> 但例外事項必須由 Rozi 本人在終端機確認。

## 部署授權

- **純前端 UI 改動**（不碰 schema、不碰結算引擎、不刪資料）：
  內部審查全過＋回歸全綠後**自動部署**，部署後回報「改了什麼／驗證結果／怎麼 rollback」。
- **schema、資料遷移、結算邏輯改動**：仍需 Rozi 拍板。
- 文件與 migration 檔本身屬文件，可自動 commit push；**套用 production 由 Cowork 端執行**。

## 傳話協定（2026-08-31 生效）

- Rozi 輸入單字**「收」** → 立刻讀 `_指令佇列.md` 最新未處理一節照辦，
  處理完在該節標題加 `[已處理]`。**佇列一律先經 Rozi 過目核可才寫入，
  終端機收到的每一節都視同已核可。**一節＝一件定案事項；同節更新時整節替換，
  永遠只讀最新內容；`[已處理]` 節由 Cowork 端定期移除。
- **提案不逕行執行**：終端機產出的提案類內容（UX 評估、文案提案等）只交付、不落地，
  由 Cowork 端帶回與 Rozi 討論定案，出現在後續佇列才動。
- **「收」只處理按下當時已存在的節**；執行中新出現的節一律留待下一次「收」。
- **每次停點或回報**：完整內容寫入 `_停點.md`（覆寫、標時間），**≤50 行**，
  對話內只留一行摘要。細節寫進對應文件後**引用檔名**，不在報告裡全文展開。
- **大型盤點必須落檔**（原始碼逐頁盤點、schema 掃描等）並記錄盤點時的 commit
  （例：`_盤點_畫面功能.md`），之後一律引用該檔；重新盤點先
  `git diff <上次盤點 commit> -- src/`，只重讀有變動的檔案。

## 原型與視覺稿的單一真相（2026-09-04 生效）

起因：同時存在 wireframe v5、分帳模型原型、以及 Cowork 端零散做的對照稿，
Rozi 無法判斷「S-05 以後要以哪一份為準」。

**兩種東西要分清楚，混在一起就是這次亂掉的原因：**

| | 誰做 | 進 repo？ |
|---|---|---|
| **決策對照稿**（同一件事並排幾個方案，幫 Rozi 選一個）| Cowork 端 | **不進**。拋棄式，Rozi 選完、決策入表即作廢 |
| **產品原型／視覺稿**（唯一真相，一路累積到全畫面）| **終端機 Claude Code** | 進，就是 `Tripay_原型.html` |

- **唯一檔案**：`Tripay_原型.html`（repo 根目錄）。視覺稿與可點原型合而為一，
  **不帶版號**——版號會誘發分岔。所有畫面、所有批次都往這一份加，不另開檔。
- **唯一維護者**：**終端機 Claude Code**。它做的原型與它的實作出自同一套元件認知，
  中間沒有轉譯損失——這正是實作放在終端機的理由。
  **Cowork 端不產出、不修改產品原型**，只出決策對照稿與規格文字。
- **實作依據是規格文字，不是原型**。原型是「檢查規格有沒有漏掉 Rozi 要的東西」的工具。
  發現原型與佇列指令有出入 → 停下回報，不自行取捨。
- **已作廢、不再維護**：`wireframe_v5_完整版.html`、`分帳模型原型.html`
  （由 Claude Code 併入 `Tripay_原型.html`）。
  `_to_delete/Tripay_原型_COWORK廢稿.html` 是 Cowork 端越權做的版本，**不得參考**，
  Rozi 可自行刪除該資料夾。

## 版面守則：不得橫向滑動（2026-09-04 生效）

起因：Rozi 回報線上產品的彈出頁（建立行程、記一筆等 sheet／modal）**可以左右滑動**，
會跑版。原型上看不出來，但實作上線後很明顯。

- 任何 sheet／modal／頁面**都不得整頁橫向捲動**。
- 寬內容（表格、長字串、金額列）自己包一層 `overflow-x:auto` 的容器，
  **不要讓它把整頁撐開**。
- sheet 開啟時鎖住底層頁面捲動，關閉時還原。
- 元素一律 `max-width:100%`，不用會撐開容器的固定寬度。

**這一項要有自動測試，不靠肉眼**：在各常見寬度（320／375／414）逐頁斷言
`document.documentElement.scrollWidth <= clientWidth`，
以及每個 sheet 容器的 `scrollWidth <= clientWidth`。
新增畫面時測試要一併涵蓋。

## 視覺細節不准憑印象（2026-09-04 生效）

起因：挑金額字體時，Cowork 端連續三輪憑印象標註「這個字體的 0 是空心的」，
全部標錯（Spline Sans Mono、Source Code Pro、Anonymous Pro、Fragment Mono、
Overpass Mono、Noto Sans Mono 都被誤標為空心，實際上都有點或斜線），
浪費 Rozi 三輪來回。

**規則：字體、字形、色值、間距這類可驗證的視覺事實，一律實測，不得憑記憶或訓練印象斷言。**

字體的實測方法（已驗證可行，容器內可跑）：
1. `fonts.googleapis.com` 被 egress 政策擋住，改用 npm：
   `npm install @fontsource/<kebab-case-name>`，字體檔在 `files/*.woff2`
2. `pip install fonttools pillow brotli --break-system-packages`
3. 用 `fontTools` 開 woff2，取 `getBestCmap()[ord("0")]` 的 glyph，
   以 `RecordingPen` 數 `closePath` 次數：**2 = 空心，3 = 有點或有斜線**
4. `font.flavor=None; font.save(ttf)` 後用 PIL 渲染成圖，**親眼再確認一次**

同理適用於：色票對比度（實算 WCAG 比值，不用「應該有過」）、
字級與行高（實測，不用估）、元素是否溢出（真實瀏覽器量測，不用 jsdom）。

## 改 A 不准順手換 B（2026-09-04 生效）

起因：同一類事故已發生三次——
改灰字時把幣別切換器改成純文字顯示（造成餘額算不出來）、
做批二時把「一起分」從收合改成攤開（偏離已定案的 R2）、
移除 S-02-17 時連帶動到相鄰項目。

**規則：動 A 的過程中若發現需要動到 B，停下回報，不得順手改。**

- 每份指令的護欄段落要明列「不准動什麼」，不只寫「要做什麼」
- **既有測試就是「不准動」的具體化**：改動前先確認測試涵蓋了不該變的東西；
  若某個不該變的行為沒有測試守著，先補測試再改
- 交付時要回報「本次動了哪些檔案／哪些區塊」，超出指令範圍的一律說明原因

## 版位層級：東西要放在哪一段（2026-09-04 生效）

起因：結算模式設定（S-02b-13）被放在成員設定（S-02b-7）之前，
但**要先有成員才選得出中心人**。Rozi 指出層級不對，而且這類問題她已提醒多次。

**根因有兩層，兩邊都要修：**
- Cowork 端寫指令時**只說「放在哪一頁」，沒說「放在哪一段」**
- 終端機拿到沒指定位置的項目時**自己隨便找地方放**，沒有判斷依據

### Cowork 端的責任
指令中新增任何 UI 項目，**必須指定位置**（寫「排在 S-XX-N 之後」），
不得只寫「放在某某頁」。

### 終端機的責任
指令若未指定位置，**不得自己隨便放**。依下列順序判斷，
並在停點報告寫明「為什麼放這個位置」：

1. **依賴優先**：B 需要 A 才有意義 → B 一定排在 A 之後
   （選中心人需要先有成員；選支付方式需要先有這趟的支付方式清單）
2. **必填在前、選填在後**
3. **頻率高的在前**：每次都要改的排前面，一趟設一次的排後面
4. **同類相鄰**：性質相同的設定放在一起，不要被別的插開

判斷不了時**停下回報**，不要猜。

### 加進全站 UX 檢查（第 9 條）
| 9 | 版位層級 | 每個設定項的位置符合上述四條；有依賴關係的沒有排在被依賴者之前 |

## 每批交付前必跑的全站 UX 檢查（2026-09-04 生效）

起因：Rozi 已經第二次提醒同一類問題（標籤與輸入框沒有排在同一列）。
把她講過的原則變成清單，每批交付前自己跑一遍，**不要等她發現**。

| # | 檢查 | 判準 |
|---|---|---|
| 1 | 標籤與輸入框同列 | 表單欄位的標籤不得自己佔一行（除非該標籤真的需要獨立） |
| 2 | 灰字只留兩種 | 只留「使用者不做就不會知道的限制」與「隱私或後果告知」；解釋系統怎麼運作、推銷功能好處、重複欄位已顯示的資訊，一律砍 |
| 3 | 選取語彙單一 | 全站只有一套選取樣式（未選描邊、選了填實）。不得出現打勾框等第二套 |
| 4 | 當下不需要的不攤開 | 使用比例低或屬於進階的區塊預設收合，要用才展開 |
| 5 | icon 風格統一 | 全部 SVG、同一組來源、line 與 fill 不混用；不得用文字符號充當 icon |
| 6 | 一個設定只有一個入口 | 同一件事不得在兩個畫面都能設定（匯率、結算模式都踩過這條） |
| 7 | 需要解釋才懂的互動 | 代表該互動缺少可辨識性——**修互動，不要加說明文字** |
| 8 | 同名不同義 | 兩個功能不得共用同一個名稱（「各付各的」踩過這條） |
| 9 | **版位層級** | 每個設定項的位置符合依賴／必填／頻率／同類四條原則；**有依賴關係的不得排在被依賴者之前** |

檢查結果寫進停點報告，逐項標「通過／發現 N 處已修」。

## 灰色提示小字的兩條規則（2026-09-04 生效）

起因：Rozi 在批一原型看到大量灰字，指出「需要這麼多備註小字，就代表 UX 有問題」。

**規則一：原型上的稿註與產品文案必須看得出差別。**
給 Rozi 看的說明（為什麼移除了某個元件、這一版跟現況差在哪）只能出現在
**右側編號清單／稿註區**，絕不能混進手機框裡。
手機框裡出現的每一個字，都是產品上真的會出現的字。

**規則二：欄位下方的灰字只有兩種情況可以留。**
1. 使用者不做這個動作就不會知道的**限制或選項**（例：「留空＝當天來回」）
2. **隱私或後果**的告知（例：誰看得到我上傳的照片）

以下一律砍掉，改用介面本身表達：
- 解釋系統怎麼運作（「封面會依行程名自動套目的地色調」）
- 解釋功能有什麼好處（「標記哪位是你，統計卡就會算你這一份」）
- 重複欄位已經顯示的資訊（「預設今天」——欄位上就寫著今天的日期了）
- 字數／格式限制（「最多 10 個字」——超過再提示）
- 技術細節（「會自動壓成 webp、長邊 1600px」——使用者不在乎）

需要一句話解釋才懂的互動，代表那個互動缺少 affordance。
**修互動，不要加說明文字。**

## Rozi 的角色：產品決策者，不做技術決策（2026-09-04 生效）

Rozi 的專長是視覺設計與專案管理。她要參與的是**產品功能、邏輯定調、運算方式**
——「這筆帳該算誰頭上」「差額歸誰」「沒填完能不能結算」這一類。

**下列一律自行決定，不要寫進停點報告要她裁示：**
資料庫加哪個欄位、前端怎麼查資料、用哪個函式寫在哪個檔案、
migration 怎麼寫、測試寫幾項、原型的徽章掛在哪一列、版面怎麼排。
判斷不了時回報給 Cowork 端，由 Cowork 決定——**不要丟給 Rozi**。

**要回報給她的**：產品層的取捨（功能現在做還是之後做）、
畫面上要顯示什麼、文案怎麼說、帳怎麼算。

技術事實還是要講，但**只講對她的影響，不展開實作細節**：

    ✅「你在藥妝店填 4000，系統不知道那是日幣還是台幣，一律當台幣算，
        所以帳會錯而且不會警告你」
    ❌「split_amount 沒有幣別欄位、引擎當台幣，已列管 P1-0」

## 草稿交付前的自我審查（強制，2026-09-04 生效）

每份要寫入 `_指令佇列.md` 的草稿，交給 Rozi 過目之前，必須先跑完這兩道檢查，
並在交付時附上檢查結果。Rozi 不負責檢查草稿有沒有漏掉她講過的需求。

1. **需求覆蓋**：逐條列出本輪與 Rozi 討論過的需求，對照草稿的哪一節涵蓋它。
   有漏的直接補上，不是提醒她。特別注意她最初提問時的情境細節
   （例：她問的是「各自的**外幣**金額沒填」，草稿只寫「金額」就是漏）。
2. **收件人檢查**：草稿是寫給終端機 Claude Code 讀的，它沒有 Cowork 對話的上下文。
   任何依賴對話上下文的指涉（「先前拍板的 X」「#N 的做法」「方案 C」）
   都必須展開成自描述的敘述。草稿中不得出現只有 Rozi 和 Cowork 才懂的簡稱。

另外每份草稿都要有**可驗證的停止條件**（什麼叫做完），否則終端機不知道何時該停。

## 完整性守則（wireframe，常設）

每次 wireframe 修改後，**受影響頁面必須重新完整渲染**——移除區塊後的版面遞補、底部預留
空間都要如實呈現，**任何頁不得出現殘洞或截斷**。編號不重排：移除項留在盤點表標刪除線，號碼不回收。
**任何省略示意都必須標明實際數量與出處**。**移除某資料欄位的顯示時，須連同寫入端一起清**，不得只清顯示端。

## 實作階段自動連跑（2026-09-04 生效）

**原型階段**：每批做完停下等 Rozi 看——那是設計階段，她的眼睛就是驗收條件。

**實作階段相反：一次跑完整批，中途不要停下等指示。**
理由：實作有測試當客觀的對錯訊號，不需要人看每一步；
而且**分批停下反而更耗 token**——每次重啟都要重讀專案狀態、CLAUDE.md、
盤點表與相關原始碼，停五次就重讀五次。

實作節次的執行方式：
- 節內若分多批，**做完一批自己接下一批**，不要回報後等「收」
- 迴圈到底：改碼 → 跑測試 → 讀失敗 → 再改 → 全綠才停
- **純前端改動**（不碰 schema、不碰結算引擎、不刪資料）測試全綠後**自動部署**
- 全部跑完才寫 `_停點.md`，Rozi 回來一次看結果

**只有這兩種情況可以中途停：**
1. **schema 變更**：migration 檔寫好就停，由 Cowork 端套用 production，
   套完 Rozi 不必再按「收」——終端機在同一次執行中等待並繼續
2. **五類例外**（不可逆且無備份、花錢、安全隱私、重大範疇取捨、
   需 Rozi 記憶或偏好才能答的資料題）

**Rozi 可能不在電腦前**：按完「收」就離開是預期用法。
不要設計成需要她中途回應的流程；真的卡住就停下並在 `_停點.md` 寫清楚卡在哪、
需要什麼才能繼續。

## 刪除護欄（三條，任何自動化腳本都適用）

起因：QA 腳本誤刪正式的濟州島行程（132 筆消費）。根因是**刪除目標從 UI 狀態推導**。

1. 刪除目標只能是「同一次執行中自己建立、且明列 id」的測試資料。
   **嚴禁從 UI 狀態（URL、畫面文字、查詢結果）推導刪除目標。**
2. 測試資料名稱一律 **`ZZ` 前綴**。
3. 刪除前**必查正式行程保護清單**（`歷史資料匯入/checkpoint.json` 的 tripUuid），命中即中止。

實作：`歷史資料匯入/automation/guard.cjs`（登記制刪除器，三種違規直接 throw）。

## 帳務鐵律

- **每個 DELETE 都必須斷言實際影響列數**，不能只看 HTTP status——
  RLS 會把不符政策的 DELETE 靜默過濾成「影響 0 列」而仍回 200。
  這一類根因已咬過三次（expense_splits／settlements／expenses）。
  回歸：`歷史資料匯入/automation/delete-integrity-check.cjs`
- **結算邏輯改動必跑四趟 verify**（福岡／東京／北海道／濟州島），
  對照 `驗收基準表.md`（🔒 已凍結 v1.1，不得再動）。
  Excel 慣例「負＝應收」與 Tripay「正＝應收」方向相反，比對時看文字不要只看正負號。
- 結算恆為台幣；統計預設台幣、可切換外幣（決策 34）。

## 內部審查關卡（自己跑完，不需請示）

凱（前端實作）→ Aria（版位／badge 一致性／小螢幕，自行截圖自查）→ QA（驗收與回歸）
→ Tech Lead（資料對應）→ Data Analyst（帳務）→ Max（流程把關）。
結果寫成一行濃縮「審查軌跡」；重大發現才攤開各角色具名對話。

**順序紀律**：標示為「部署後收尾」的項目不得在部署前執行。

## 環境備忘

- Supabase 免費專案閒置逾 7 天自動暫停 → 全站白畫面。已由 `.github/workflows/keepalive.yml`（方案 b′）處理。
- PostgREST embed 歧義：`trips` 與 `trip_members` 有兩條 FK，
  查詢一律寫 `trip_members!trip_members_trip_id_fkey(*)`。
- 表單重繪不得重建使用者正在輸入的欄位：`<input type="number">` 不支援 selection API，`setSelectionRange` 拋錯後游標歸 0、字元倒序。回歸 `原型_輸入回歸測試.cjs`
- Sheet／彈窗一律 `createPortal` 到 `document.body`——
  頁面根層的 `animate-slide-in` 帶 transform 會讓 `position:fixed` 定位錯亂。

## 未定案狀態的文案：用「補上就會⋯」，不用「會被跳過」

起因：S-04 金額區寫「沒有台幣金額，結算會跳過這筆」。
Rozi 指出這句邏輯不通——**現金匯率很可能到旅程結束、要結算時才輸入**，
所以這是一個隨時會解除的暫時狀態，卻被寫成永久的判決。
她讀到的是：「那這樣到底有沒有記錄的必要？」

**規則**：凡是「資料還不完整」的提示，必須同時滿足兩件事——

1. **先說東西還在**：「先記著了」「已經存起來了」。
   使用者最怕的不是算不出來，是**以為自己記的東西不見了**。
2. **用可行動的語氣講解除條件**：「補上台幣金額就會算進結算」，
   不要寫「沒有 X 就不算／會跳過／無效」。

**同理，只描述系統處境的句子要改成描述使用者的後果。**
例：「這趟還沒設現金匯率，換算不了」→ 講的是系統算不出來，
但真正該讓使用者知道的是「**上面那個總花費是少算的，少了 N 筆**」。
一個看起來精確、實際上不完整的數字，比看不懂更危險。

## 圖示按鈕 vs 方向指示：不要「統一」

Rozi 2026-09-05：「你說的兩種選項的兩個層級不一樣，所以不應該『統一』。」

| 是什麼 | 樣式 |
|---|---|
| **一顆 icon 就是一個按鈕**（返回、⋯、關閉、設定）| `.ic2`：40×40 正圓＋**半透明薄膜**＋20px icon，可點區 ≥44×44 |
| **列尾的 `›` 方向指示** | **裸 icon、不加容器、16px** |

`›` 不是按鈕——可點的是整一列，44px 由整列滿足；加容器會讓它看起來像另一顆按鈕。
**往後不要有人「順手統一」把 `›` 也包進容器。**

`.ic2` 的底色**永遠是「底下顏色的薄膜」**：底暗就加亮（`rgba(255,255,255,.14)`）、
底亮就加暗（`rgba(0,0,0,.05)`）。兩邊都半透明，所以不管底下是漸層、表單底還是白卡片
都會透出底色。**不要寫成實色**——那會變成一顆貼在畫面上的貼紙。

## 手機上不得有「使用者沒要求的聚焦」

`autofocus` 綁的是「元素被放進 DOM」，不是「使用者要編輯」。
只要有任何一次重繪把輸入框插回畫面，瀏覽器就再聚焦一次——
**在手機上就是鍵盤關掉又跳出來。**

- **禁止使用 `autofocus`。** 要聚焦就在使用者真的觸發時呼叫 `.focus()`。
- 「重繪後還原焦點」只還給**本來就聚焦中的輸入框**；
  `document.activeElement` 是按鈕之類的東西時**一律不呼叫 `.focus()`**。

## 同一句文案只准寫一次

同一段提示若出現在兩個畫面（例如 S-04 的金額提示與 S-03 消費列的副標），
**必須引用同一個字串常數**，不得各寫一份。
理由與 `transferView()`／`statCard()` 相同：兩處分開寫，遲早會走鐘。
測試要比對兩處輸出的字串完全相同。

## 圖示的兩個層級：按鈕 vs 方向指示

- **一顆 icon 就是一個按鈕**（返回、更多、關閉、設定）→ 一律用 `.ic2`：
  40×40 正圓、**半透明薄膜**（底暗加亮、底亮加暗，永遠透出底色）、20px icon、可點區 ≥44×44。
- **列尾的 `›`** → **裸 icon、不加容器、16px**。它是方向指示，不是可點目標；
  可點的是整一列，44px 由整列滿足。加容器會讓它看起來像另一顆按鈕。

Rozi 2026-09-05 明確界定：「兩個層級不一樣，所以不應該統一。」
**不要再「順手統一」把 `›` 包進容器。**

## 容器的顏色是薄膜，不是實色

圖示容器、遮罩這類「疊在別的東西上面」的元件，
一律用**半透明**讓底色透出來，不要用實色填。
起因：`.ic2` 曾經在深底用半透明黑（會透）、淺底用實色白（不透），
同一顆按鈕在兩頁變成兩種材質，Rozi 一眼看出來。
**規則只有一句：容器是底下顏色的薄膜。底暗就加亮，底亮就加暗。**

## 全域改動之後要逐頁看過，不能只看測試數字

起因：#29 把字級放大三成之後，58px 的標籤欄爆掉（「當地幣別」折成兩行）、
200px 的標語折行、登入頁的區塊被推得更偏上。
**三個都是同一次改動的可預期後果，只要改完把每一頁看一遍就會發現**，
但測試全綠，所以沒人發現，是 Rozi 自己在手機上抓到的。

**根因**：既有測試守住「不橫向捲動」「文字不貼邊」「對比夠不夠」，
但沒有任何一條在問「這一頁看起來對不對」。
**機器斷言抓得到壞掉，抓不到難看。**

**規則**：凡是動到全域的節次——**字級、圓角、顏色、容器樣式、間距**——
停止條件必須包含：**輸出全部畫面的截圖**（S-00～S-07 每一個主要狀態，
桌機與真機模式各一組），檔名列在 `_停點.md`，**Cowork 端逐張看過才算通過**。

## 圓角與內距的關係

**有圓角的內容容器，左右內距至少要比圓角多 4px。**
圓角越大，角落的曲線越往內吃；內距沒跟著加，文字與 emoji 就會壓在曲線上。
起因：圓角改成 10px 之後，內距還是 10px，Rozi 一眼看出「元素太貼近邊框」。
往後調圓角時，內距要一起檢查。
