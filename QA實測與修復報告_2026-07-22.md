# Tripay QA 實測與修復報告

**日期**：2026-07-22　**執行**：QA Lead（線上實測）× Tech Lead（修復）　**決策者**：Rozi
**正式站**：https://tzyutw.github.io/tripay/　**後端**：Supabase（免費方案）

---

## 一、背景

Tripay 於 2026-07-06 標記「Phase 1 上線」。Rozi 觀察到「產品有問題」，並在思考是否需要補寫 SRS／SAD／SDD 三份規格文件。本輪先釐清「問題屬於實作、需求、還是架構」，再對症下藥，遂啟動一次針對正式站的端到端 QA 實測。

**結論先講**：產品的問題與「缺文件」無關，而是一次後端維運事故加上數個實作 bug。這些只有靠實測才揪得出來，補文件無法解決。同時發現「上線」里程碑當時被打勾，但核心功能「建立行程」其實從未在 production 成功過。

---

## 二、發現與處理（共 7 項）

| # | 問題 | 嚴重度 | 根因 | 處理 |
|---|------|--------|------|------|
| 1 | 全站無法登入 | Blocker | Supabase 免費專案閒置逾 7 天被自動暫停（DNS NXDOMAIN） | Rozi 於 Dashboard Restore；並新增 keep-alive 防復發 |
| 2 | 建立行程失敗（HTTP 409） | Blocker | 登入者在 `profiles` 無資料列，`handle_new_user` 觸發器未在 production 生效，`trips.owner_id` 外鍵違反 | `hotfix_profiles_backfill.sql`：補建 profiles + 重建觸發器 |
| 3 | 行程列表／詳情／結算／分享頁全部載不出 | Blocker | `trips` 與 `trip_members` 有兩條外鍵，PostgREST embed 歧義（PGRST201/HTTP 300） | 5 處查詢改用具名外鍵 `trip_members!trip_members_trip_id_fkey`，build + 部署 |
| 4 | 編輯消費造成分帳金額錯誤 | Major | `expense_splits` 缺 RLS DELETE 政策，編輯時「刪舊 splits」被靜默擋掉 → 重複累積 | `hotfix_2_expense_splits_delete_policy.sql`：補政策 + 清重複 |
| 5 | 可存入「無金額且未標記待填」的消費 | 驗證缺口 | 前端表單缺驗證（後端 Edge Function 本有 422 防禦） | ExpenseFormSheet 加驗證：台幣需填或標記待填 |
| 6 | console 反覆報錯、G-01 新用戶跳轉失效 | Bug | `usePostLoginRedirect` 查詢誤加 `.is('deleted_at', null)`，但 `trips` 無此欄位（42703） | 移除該條件，build + 部署 |
| 7 | 統計卡幣別符號 ¥ 與台幣數字不一致 | Minor | 統計卡數字固定為台幣值（totalTwd/myCost），符號卻誤用行程外幣符號 | 統計卡符號改為 `$`（台幣），build + 部署，線上驗證顯示 $ 1,500 / $ 500 ✅ |

---

## 三、驗證結果（修正上線後於正式站實測）

**四條使用者旅程 + 編輯**：全部通過
- 旅程零 建立行程（成員 emoji、owner_member_id「這是我」、幣別、日期）
- 旅程一 記帳（共同均分即時計算「每人 $200」、即時統計加總、依天分組、付款人切換、G-05 分享提示）
- 旅程二 結算（Minimum Transactions 正確：小華→小美 $400、小華→阿明 $100，Σnet=0，最小 2 筆）
- 旅程三 分享（`/share/:token` 唯讀頁、免登入、結算＋消費明細、G-06 下載 CTA、無編輯按鈕）
- 封存／重新開啟（`reopen-settlement` archived→settled，決策 B）

**Codex CR 高風險 6 項**：全部通過（API 實測）
1. archived 行程拒絕結算 → 409 `trip_archived`
2. 軟刪除消費不進結算 + Σnet_balance = 0
3. 多 draft 時舊 draft 無法 confirm（superseded → 拒絕）
4. `confirm_settlement` RPC 兩個 UPDATE 單一交易，原子性
5. 零參與人 → 422 `invalid_expense_splits`（非 500）
6. 負數金額 → 422 `invalid_amount`

---

## 四、預防措施

- **keep-alive**：新增 GitHub Actions workflow（`.github/workflows/keepalive.yml`），每 3 天 ping Supabase REST，避免免費專案再因閒置被暫停（直接防止問題 #1 復發）。
- **流程建議**：將「任何『上線』里程碑必須附一次真機端到端實測記錄才能打勾」寫進協作規則，避免再發生「上線但無人能用」。
- **協作模式**：本輪同步將舊的「多對話 + Rozi 手動往返」改為「單一團隊對話：Claude 內部自審與交叉審查，只在決策點找 Rozi」，降低 Rozi 的協調負擔。

---

## 五、遺留 / 後續（非阻斷）

- 7 個問題（含 Minor #7）已全部修復並上線，無未結項。
- 可選：評估升級 Supabase Pro（永不暫停，有 keep-alive 後非必要）。
- 測試資料：行程「QA測試團」已封存。

---

## 六、一句話總結

Tripay 從「看似上線、實則無人能用」，經一次端到端 QA 實測揪出 3 個 Blocker + 4 個其他問題（共 7 項），**全部修復、上線並驗證，無未結項**，四條旅程與 Codex CR 高風險守門全數通過，並加上 keep-alive 防護——**現在是真正可用、正確且有防護的狀態**。
