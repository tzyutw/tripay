# 決策符合性檢查表

> 把「已確認決策紀錄」逐條對照線上產品實況。
> **用途：每次 UI 改動的 QA 必跑回歸清單** —— 讓決策從「文件裡的字」變成「攔得住 code 的檢查」。
>
> 首次稽核：2026-08-30（Max × QA）　對照版本：commit `148f8d7`
> **v1.1 更新 2026-08-30**：補漏 9 條決策、加防漏機制。起因見「為什麼漏掉」一節。
> **v1.2 更新 2026-08-31**：依新規則同步打磨輪的 8 條新決策（#31–38）。累計 38 條。
> **v1.3 更新 2026-08-31**：同步 v5 裁定的 6 條（#39–44）＋S-05-29 文案（#45）。累計 45 條。
> **v1.4 更新 2026-08-31**：同步 v5 第二批 8 條（#46–53）。累計 53 條。

## 判定總覽

| 判定 | 數量 |
|------|-----:|
| ✅ 落實 | 18 |
| ⚠️ 部分落實 | 1（#5 切換成員視角）|
| ❌ 違反 | 0（首次稽核發現 2 項，已當場修正）|
| N/A 不適用 | 2 |
| **v1.1 補漏** | **9 條（見下）** |

---

## 🔴 為什麼漏掉「首頁不顯金額」——檢討

**事件**：2026-08-30 打磨輪的 tokens 提案，我在首頁行程卡加上「可拿回 3,633／要給出 7,951」，
違反路線 2 決策「首頁不顯金額」（Rozi 同日才確認維持）。**檢查表沒攔住。**

**根因三條，都是我的疏漏：**

| # | 根因 | 證據 |
|---|------|------|
| 1 | **檢查表是一次性快照，不會隨新決策增長** | 檢查表涵蓋 **21** 條，決策紀錄表現在有 **30** 條——**9 條新決策從未進入檢查表**，包含「首頁列表金額」（2026-08-30 才加入決策表）|
| 2 | **沒有流程規則要求同步** | 「新增一條決策」與「新增對應檢查項」之間沒有任何強制連結 |
| 3 | **觸發時機定義太窄** | 原文寫「每次 **UI 改動** 的 QA 必跑」。設計提案還不是 code 改動，於是整份提案沒有被掃過 |

**三條修正（已套用於本檔）：**
1. 補上 9 條缺漏決策（下方 v1.1 區）
2. **新規則**：決策紀錄表新增一列時，本檔必須同步新增一列（判定 N/A 也要寫）。寫入協作規則
3. **觸發時機擴大**：改為「每次 UI 改動**或視覺／wireframe 提案**交付前必跑」

---

## v1.1 補漏的 9 條決策

| # | 決策 | 落實狀態 | 證據／檢查方式 |
|---|------|---------|--------------|
| 22 | **首頁列表金額**：維持不顯示 | ✅ *(本次修正)* | 打磨稿一度違反，已移除行程卡的金錢方向色。<br>**自動檢查**：首頁 DOM 不得出現 `$`／金額樣式 |
| 23 | **行程列表排序（S-01）**：出發日新→舊 | ✅ | `TripListPage` `.order('start_date',…)`；`qa-round5.cjs` 已含測項 |
| 24 | **Supabase 保活**：方案 b′ GitHub 內部保活 | ✅ | `keepalive.yml` anti-disable job，手動 dispatch 驗證通過 |
| 25 | **決策模式**：預設執行＋事後可推翻，五種例外事前問 | N/A | 協作方式決策，非產品行為 |
| 26 | **Phase 2 D1**：帳單沿用 `trips`＋`kind` | ✅ | `007` 已套用；`TripListPage` 加 `.eq('kind','trip')`，`qa-kind-filter.cjs` 4/4 |
| 27 | **Phase 2 D2**：抽 `people` 通訊錄層 | ✅ | `006` 已套用；`people-layer-check.cjs` 10/10 |
| 28 | **Phase 2 D3／D4**：半自動先行、OCR 與影像隱私延後 | ✅ | 尚未實作 OCR，符合決策 |
| 29 | **Phase 2 D5**：分帳三型沿用，Phase 2 預設 `personal` | ⏳ 未到期 | Phase 2 記帳 UI 尚未實作，屆時檢查預設值 |
| 30 | **Phase 2 D6**：A→B 兩里程碑 | ⏳ 未到期 | A 里程碑其餘前端已依 Rozi 指示暫停，併入路線 3 |

---

## 逐條檢查

| # | 決策 | 落實狀態 | 證據 |
|---|------|---------|------|
| 1 | **Phase 規劃**（P1 分帳／P2 副卡／P3 商業化）| N/A | 專案管理決策，非產品行為，無可稽核的線上實況 |
| 2 | **商業模式時程**（P1+P2 全免費）| ✅ | 全 `src/` 無訂閱／付費／pricing／升級入口（唯一 `subscribe` 命中是 `AuthLayout.tsx:44` 的 Supabase auth listener）|
| 3 | **分帳演算法**（Minimum Transactions、差額歸付款人）| ✅ | `_shared/settlement-engine.ts`：`minimumTransactions()` 貪婪配對；shared 餘數歸付款人、individual 差額 `cost[payer] += twd - input` |
| 4 | **待填筆統計顯示**（方案 C：⚠️ N 筆待填，數字僅供參考）| ✅ *(本輪修正)* | 原為「⚠️ 含 N 筆待填」，漏了後半句，與決策及文案 v1.3 S-03 皆不符 → 已補為「⚠️ 含 N 筆待填，數字僅供參考」（`ExpenseListPage.tsx:334`）|
| 5 | **我的開支識別**（owner_member_id、支援切換成員視角）| ⚠️ **部分落實** | `owner_member_id` ✅ 有實作且統計卡「我的花費」依此計算；**「切換成員視角」未實作**。原表單文案承諾「之後可以切換視角看自己的花費」→ 本輪改為描述現況，不再承諾未建功能。功能本身列入 Phase 1.5 候選 |
| 6 | **消費瀏覽頁成員分佈**（Phase 1 不做）| ✅ | 未實作，符合決策 |
| 7 | **分享入口**（S-03 右上角常駐、全狀態皆可分享）| ✅ | `ExpenseListPage.tsx:300` 分享鈕在 `!isArchived` 守衛**之外**，archived 也看得到 |
| 8 | **payment_method**（三選一、Phase 1 必填）| ✅ | `ExpenseFormSheet.tsx:17-21` 三選項；`:268` 有必填驗證「這欄還沒填喔」|
| 9 | **individual 差額**（歸付款人、不提供選擇）| ✅ | `settlement-engine.ts` 差額一律 `cost[payerId] += difference`，UI 無選項 |
| 10 | **App 名稱 Tripay** | ✅ | `LoginPage`／`TripListPage` 標題、`manifest.webmanifest` |
| 11 | **Slogan「每一趟，都記得」** | ✅ | `LoginPage.tsx:44` |
| 12 | **文案語氣**（用「你」不用「您」；說「記一筆」不說「新增消費」；旅伴語氣、不裝可愛）| ✅ *(本輪修正)* | 全 `src/` **無「您」** ✅；**「＋ 新增消費」違反決策** → 已改為「＋ 記一筆」（`ExpenseListPage.tsx:462`）。表單標題本來就是「記一筆」 |
| 13 | **行程狀態（決策 A）**（出發前／旅途中）| ✅ | `deriveStatus.ts` `STATUS_LABEL`：planned=出發前、active=旅途中。<br>⚠️ 決策表原文寫「準備旅遊／旅遊中」，文案 v1.1（2026-06-26，Rozi 已確認）改定為「出發前／旅途中」，實作依文案定稿。**建議把決策表用語同步為文案定稿版**，避免日後誤判為違反 |
| 14 | **封存定義（決策 B）**（預設只讀、可重新開啟）| ✅ | archived 時記帳／編輯／結算入口全隱藏，僅留「重新開啟」（`ExpenseListPage.tsx:486`）；`calculate-settlement` 對 archived 回 409 |
| 15 | **消費列 badge（決策 C）** | ✅（以文案定稿為準）| 共同無 badge ✅；待填有橘紅左邊框 ✅。<br>⚠️ 決策原文寫 badge 文字為「各自」「自付」，實作為「各付各的」「只算我」，且待填另加了「待補填」badge —— 這些都出自後續文案 v1.1/v1.3（S-03「待填筆標籤：待補填」、S-04「各自付各自的／只算我自己」），屬**文案定稿覆蓋決策原文**，非違反。**建議同步決策表用語** |
| 16 | **S-03E 成員列（決策 D）**（空白行程不顯示 NT$0）| ✅ | 全 `src/` 無 `NT$`；列表卡片空值顯示 `$ —`，統計卡有記帳後才出數字 |
| 17 | **Growth 範疇**（G-01～G-08 Phase 1）| ✅ | G-01 `usePostLoginRedirect`／G-02 `LoginPage` 幽靈預覽卡／G-03 `LoginPage` 行動引導／G-05 `ExpenseListPage` `showG05`／G-06 `SharePage:271`「想自己記帳？下載 Tripay」／G-07 `SettlementPage:341`「＋ 建立新行程」／G-08 `SettlementPage:282` Highlights —— 八項全數在線 |
| 18 | **目標用戶**（泛旅遊、不限家庭）| ✅ | 文案 v1.3 已泛化；`src/` 無「家人／父母」等限定用語 |
| 19 | **部署授權下放**（2026-08-30）| ✅ | 本輪即依此授權自動部署 |
| 20 | **角色討論呈現方式**（2026-08-30）| ✅ | 本輪回報採濃縮審查軌跡 |
| 21 | **協作流程改版**（單一團隊對話）| N/A | 協作方式決策，非產品行為 |

---

## 本輪處置

**已當場修正（純前端，隨本輪部署）**

| 決策 | 違反內容 | 修正 |
|------|---------|------|
| #12 文案語氣 | 「＋ 新增消費」違反「說『記一筆』不說『新增消費』」 | → 「＋ 記一筆」 |
| #4 待填統計顯示 | 「⚠️ 含 N 筆待填」漏掉「，數字僅供參考」 | → 補齊 |
| #5 我的開支識別 | 文案承諾了未實作的「切換視角」 | → 改為描述現況；功能已列 **Phase 1.5 候選清單**，優先序與複製／刪除行程、匯入 UI 一起排（Rozi 2026-08-30 確認）|

**文件同步（已完成 2026-08-30，Rozi 拍板）**

決策 #13、#15 的決策表原文與文案定稿用語不一致，已把 `專案狀態.md` 決策表更新為**文案定稿版**
（出發前／旅途中；各付各的／只算我／待補填），並加註「**原用語**」保留歷史脈絡。
→ 這兩條自此判定為 **✅ 落實**，下次稽核不會再被誤判為違反。

---

## v1.2 新增的 8 條決策（2026-08-30 ～ 08-31 打磨輪）

> 依 v1.1 新規則：決策紀錄表新增一列，本檔同步新增一列（判定 N/A 也要寫）。

| # | 決策 | 落實狀態 | 證據／檢查方式 |
|---|------|---------|--------------|
| 31 | **視覺方向**：02 日記精裝＋等寬數字，六條反模板要求 | ⏳ 未到期 | 打磨輪實作批次尚未開始；驗收看 tokens v3 與 wireframe v4 |
| 32 | **工作色富士青 `#1276C4`**，金錢雙色 `#276E45`／`#B03A22`，紫留 Phase 3 | ⏳ 未到期 | 實作後 **自動檢查**：`grep -rn "#D2764A\|#F4F1EA\|teal" src/` 須為 0（AI 模板色與 Splitwise 皮）；`grep -rn "6B4FA8" src/` 在 Phase 3 前須為 0 |
| 33 | **中文字體用系統字**，唯一 webfont 是 IBM Plex Mono | ⏳ 未到期 | **自動檢查**：`grep -rn "fonts.googleapis" index.html src/` 只准出現 IBM Plex Mono 一筆 |
| 34 | **統計幣別預設台幣＋可切換**，結算恆為台幣 | ⏳ 未到期 | 實作後瀏覽器檢查：切到外幣時結算頁金額不得變動 |
| 35 | **摺紙動畫品質閘**：不達標退回乾淨 loading | ⏳ 未到期 | Aria 於實作批次判定，判定結果須寫入回報 |
| 36 | **封面照片不放非自有照片進 repo** | ✅ | `public/` 與 repo 內無任何旅遊照片；四趟 `cover_path` 全 NULL 走 gradient。<br>**自動檢查**：`find public src -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.webp"` 不得出現旅遊照 |
| 37 | **008 Storage**：五條政策＋刪行程同步刪封面檔 | ⏳ 待套用 | 套用後跑 008 檔尾三項自檢（bucket 非公開／5 條政策／4 趟 NULL）；刪行程流程須斷言 Storage remove 有被呼叫 |
| 38 | **Phase 1.5 三做兩不做** | ⏳ 未到期 | 三項進第二批；兩項不做，若日後出現在 backlog 需重新拍板 |

---

## v1.3 新增的 7 條決策（2026-08-31 打磨輪 v5 裁定）

| # | 決策 | 落實狀態 | 證據／檢查方式 |
|---|------|---------|--------------|
| 39 | **P-1 登入頁識別**：字標＋slogan，不補圖形 | ⏳ 未到期 | 實作後 **自動檢查**：`grep -n "76px\|rounded-\[22px\]" src/pages/LoginPage.tsx` 須為 0（App icon 方塊已移除）|
| 40 | **P-2 幽靈卡**：S-00 移除、S-01 保留 | ⏳ 未到期 | **自動檢查**：`grep -c "你的下一趟在哪" src/` 須為 **1**（只剩 TripListPage）|
| 41 | **P-3 分享動線**：G-05 拿掉、S-05 加分享 CTA、S-03 常駐不動 | ⏳ 未到期 | **自動檢查**：`grep -c "g05-dismissed" src/` 須為 0；SettlementPage 狀態 4 須有分享入口；S-03 的 ↑ 須仍在 |
| 42 | **首頁空狀態補 CTA** | ⏳ 未到期 | 瀏覽器檢查：無行程時首頁須有一顆可點的主要按鈕 |
| 43 | **wireframe 完整性守則** | ✅ | 每次改稿後受影響頁重新完整渲染；本輪 S-01／S-03／S-05 已重渲染並在稿上標出底部收束 |
| 44 | **「收」只處理當下已存在的節** | N/A | 協作方式決策，非產品行為 |
| 45 | **S-05-29 文案**：主「把結果傳給大家」／副「任何人免登入可看」 | ⏳ 未到期 | 實作後 **自動檢查**：`grep -c "把結果傳給大家" src/pages/SettlementPage.tsx` 須為 1 |

---

## v1.4 新增的 8 條決策（2026-08-31 v5 第二批）

| # | 決策 | 落實狀態 | 證據／檢查方式 |
|---|------|---------|--------------|
| 46 | **封面 emoji 取消**，一律先套目的地色調 | ⏳ 未到期 | **自動檢查**：`grep -c "kind: 'cover'" src/` 須為 0；`trips.emoji` 欄位須仍存在（不得刪欄）|
| 47 | **色調預設盤 8 種，不做挑色器** | ⏳ 未到期 | 實作後檢查：色調選項只能來自 `destinations.ts`，不得出現 `<input type="color">` |
| 48 | **EmojiPicker 整個移除**，改就地編輯 | ⏳ 未到期 | **自動檢查**：`ls src/components/EmojiPicker.tsx` 須不存在；`grep -c "firstGrapheme" src/` 須 ≥1（驗證邏輯保留）|
| 49 | **成員識別三層 fallback** | ⏳ 未到期 | 瀏覽器檢查：清空某成員 emoji 後須顯示名字首字圓底，不得變空白 |
| 50 | **trip_members.emoji 預設空字串，既有資料不動** | ⏳ 待套用 | 009 套用後跑檔尾自檢：預設為 `''`，且既有非空 emoji 筆數不得變少 |
| 51 | **首頁行程卡不顯示成員**（僅首頁）| ⏳ 未到期 | `qa-pkg3.cjs` 加測項：首頁卡片 DOM 不得出現成員 emoji；**同時斷言行程內頁／結算頁／分享頁仍有** |
| 52 | **日期：出發預設今天、回程可留空** | ⏳ 未到期 | 瀏覽器檢查：新行程表單出發日＝今天；回程空白送出須跳確認而非擋下 |
| 53 | **等寬只用於金額** | ⏳ 未到期 | **自動檢查**：`grep -rn "font-mono\|IBM Plex Mono" src/` 命中處須全部是金額；日期／計數不得使用 |

---

## 使用方式（回歸清單）

每次 UI 改動後至少跑這幾項自動檢查：

```bash
grep -rn "您" src/                      # 決策12：不得出現
grep -rn "新增消費" src/                 # 決策12：應為「記一筆」
grep -rn "NT\$" src/                    # 決策16：不得出現
grep -rn "數字僅供參考" src/              # 決策4：待填警示應含此句
grep -rniE "訂閱|付費|pricing|升級" src/  # 決策2：Phase 1+2 不得有付費入口
grep -n "kind', 'trip'" src/pages/TripListPage.tsx   # D1：帳單不得混入旅遊列表
```

**首頁不得顯示金額（決策 22）** —— 這條 grep 擋不住（金額是動態值），必須跑瀏覽器檢查：
`qa-pkg3.cjs` 的「首頁完全沒有金額」測項已涵蓋，正則為 `/\$\s*[\d,—]/`。
**視覺／wireframe 提案交付前也要用肉眼掃一次首頁稿。**

其餘（分享鈕常駐、payment_method 必填、封存只讀、Growth 八項、排序、金額幣別）
已納入 `歷史資料匯入/automation/` 的自動化檢查：
`qa-round5.cjs`／`qa-emoji-edit.cjs`／`qa-pkg2.cjs`／`qa-pkg3.cjs`／`qa-kind-filter.cjs`。

## 觸發時機（v1.1 擴大）

- 每次 **UI 改動**部署前
- 每次 **視覺／wireframe 提案**交付前 ← v1.1 新增，就是這次漏掉的那一類
- 每次 **決策紀錄表新增一列**時，本檔同步新增一列
