# 延後與不做清單

> 起因：Rozi 2026-09-04 提出——決定不做的東西如果沒記錄，之後只會重新討論一次。
> 這份文件收「**討論過、有結論、但這一期不做**」的功能，重點在最後一欄「什麼時候可以重新考慮」。
>
> **維護規則**：任何「不做／延後」的決定，除了寫進 `專案狀態.md` 的決策表，也要在這裡補一列。
> 之後盤點功能缺口時先讀這份，避免重複討論。

---

## A. 功能層級：延後或不做

| 項目 | 決定 | 日期 | 為什麼 | 什麼時候可以重新考慮 |
|---|---|---|---|---|
| **封面照片顯示**<br>（照片存得進去卻畫不出來）| 延後 | 09-05 | 原型複驗查出：`photo` 有寫回 store，但**首頁卡片、行程頁大圖、分享頁大圖三處都只畫漸層色調**，沒有一處讀照片。封面上傳本來就排在「第二批（新畫面）」，前端一行都沒寫；後端已備（migration 008：`covers` bucket、Storage RLS 四條、`trips.cover_path`，已套 production）| **封面上傳功能開發時一起做**。另有一題要先解：bucket 非公開走簽名網址，但分享頁免登入——要讓旅伴看到封面照，得先設計「拿分享連結的人怎麼取得簽名網址」，屬安全性設計 |
| **分享選單灰字的範圍**<br>（只提到消費明細）| 延後 | 09-05 | S-03b「不用登入就看得到消費明細」只涵蓋消費明細。目前封面照不會出現在分享頁，所以這句是對的 | **封面照片真的會出現在分享頁時**，這句要跟著加寬（連封面照也看得到） |
| **輸入框高度沒有階梯**<br>（全站三種：41／25／32px）| 延後 | 09-05 | 跟字級與圓角當初一樣：沒有階梯，是各處各自決定出來的。原型階段改它會牽動所有畫面，風險大於收益 | **實作時一次定義輸入框高度階梯**，與字級六階、圓角 C 放在同一份 token 表 |
| **消費列成員分佈 chips**<br>（每筆顯示「這筆算了誰」）| 不做 | 09-04 | 與「當下不需要的東西不該攤在畫面上」衝突。每列多一排 emoji 提高視覺密度，但「算了誰」只在對帳那一兩次才需要，點進去看即可 | **Phase 2 帳單頁要用同一個元件**（S-10-9 分佈條，顯示各持卡人佔比）。做 Phase 2 時順手一起做，成本低 |
| **歷史資料匯入 UI**<br>（Excel 匯入的使用者介面）| 不做 | 08-31 | 與 Phase 2「拍照掃帳單」共用同一套解析管線，現在做等於做兩次。目前四趟歷史帳是跑腳本匯的 | **Phase 2 半自動帳單管線做好之後**，兩者共用同一套解析，屆時匯入 UI 幾乎是順手 |
| **G-04 OG 分享預覽圖**<br>（貼到 LINE／FB 的縮圖）| 不做 | 08-31 | 那張圖的價值在顯示旅遊封面照，但封面上傳功能未到位，現在只能生成漸層色塊，沒有意義 | **封面照片上傳上線、Rozi 實際傳了照片之後**。技術方案已評估（Satori，工時 3–4 天） |
| **G-09 帶上一趟成員** | 移除 | 09-04 | 原本只是複製上一趟的 name／emoji 硬湊，而**複製行程（S-03-4）已經會帶成員**，功能重疊 | 若日後 people 層（通訊錄）做起來，可改為「從通訊錄挑人」，那才是真功能而非硬湊 |
| **切換成員視角** | 取消 | 09-04 | `owner_member_id` 只被用來算「我的花費」，統計卡改成收合／展開後該數字不再顯示，欄位失去用途 | 若日後統計卡又要顯示「某人要付多少」才有意義。**與 Phase 2 副卡視角不共用元件**（曾誤記為共用） |
| **統計卡「我的花費」** | 移除 | 09-04 | 統計卡改為「收合只顯示總花費、點開顯示每個人」。查看每人金額的頻率低，沒必要一進來就攤開 | 若使用後發現常常需要一眼看自己欠多少，可加回收合狀態 |
| **單筆覆寫現金匯率** | 拿掉 | 09-04 | 一趟若換兩次錢，匯率差通常 1–3%，而分帳是相對的、所有人用同一匯率，誤差相互抵消。為此多一層「單筆 vs 行程誰大」的心智負擔不划算 | 若實際使用中出現匯率差很大的情況（例如跨越兩國），再考慮 |
| **記帳時就地設定匯率** | 拿掉 | 09-04 | 匯率是「換錢」時的事，記帳只是消費紀錄。第二個入口會製造層級混淆 | 不打算恢復。匯率永遠只有 S-02b 一個入口 |
| **自動抓網路匯率** | 不做 | 09-04 | 使用者在當地換的匯率含價差，與市場牌價不同，自動帶入反而會算錯 | 若日後要做，只能當「參考值」提示，不可直接帶入 |
| **金額輸入的自訂數字鍵盤** | 不做 | 09-04 | 那是原生 App 才划算的投資；網頁用 `inputmode="decimal"` 叫系統鍵盤效果一樣 | 若日後包成原生 App |
| **色票自由挑色器**<br>（封面色調）| 不做 | 08-31 | 自由選色會讓卡片失去「一眼看出是哪一國」的功能。改用 8 種目的地色系預設盤 | 不打算恢復。除非目的地色系不夠用 |
| **分帳模型「版本二」**<br>（兩維度：誰要分／怎麼分）| 未選用 | 09-04 | 經可點原型試用後 Rozi 選定版本一（維持三型）。版本二的稿保留在 `分帳模型原型.html` 作紀錄 | 若三型在實際使用中仍讓人困惑，版本二是現成的備案 |
| **結算模式自動判斷**<br>（代墊超過 N 成就自動用 hub）| 未選用 | 09-04 | Rozi 選擇手動切換。自動判斷的問題是「你不知道它為什麼這樣算、也改不了」 | 若使用後發現每趟都要手動切很煩 |
| **Phase 2 OCR 辨識帳單** | 延後 | — | 先做半自動版本（手動輸入／表格貼上＋逐筆確認），OCR 等「月底儀式」的假設被驗證後再投入 | **半自動版上線、且月底對帳的使用習慣被驗證之後** |
| **批四 Phase 2 原型**<br>（S-08～S-11）| 延後 | 09-04 | 與 Phase 1 實作無依賴，且 Phase 2 規格只有初稿、細節未定，卡在前面只會拖延上線 | **Phase 1＋1.5 實作上線、Rozi 實際使用過之後**。那時對 Phase 2 的產品判斷會更有依據 |

---

## B. 設計決策：已移除的元件

> 這些是設計上定案拿掉的，不是「延後」。列在這裡是為了保留「推翻方式」，
> 之後若有人問「為什麼沒有 X」，可以查到當初的理由。

| 元件 | 日期 | 為什麼拿掉 | 推翻方式 |
|---|---|---|---|
| **S-00-1 App icon** | 08-31 | 尚未設計 logo，S-00 以字標 Tripay ＋ slogan 承擔識別 | 日後若做 logo，收據摺紙是現成語彙 |
| **S-00-6 幽靈卡** | 08-31 | 在登入頁不可點卻長得可點、稀釋唯一動作、未登入者沒有「上一趟」 | 不搬家、不恢復。S-01-8 的幽靈卡維持可點 |
| **G-05 分享橫幅**（S-03-13）| 08-31 | 只在消費恰好 1 筆時出現，觸發條件太窄。由 S-05-29 結算完成頁的分享 CTA 取代 | 恢復 G-05 的觸發條件即可，S-05-29 可獨立保留 |
| **封面 emoji**（S-02-5／S-02b-6）| 08-31 | 封面只保留「照片」與「色調」兩種。`trips.emoji` 欄位保留不刪，前端不用 | 恢復顯示即可 |
| **行程 emoji 全站退場** | 08-31 | 建立不設、卡片不顯、分享不顯。碼上 7 處一次清完（含寫入端）| 恢復顯示即可，欄位還在 |
| **EmojiPicker 元件** | 08-31 | 整個元件移除。成員識別改三層 fallback（emoji → 名字首字 → 🙂）＋就地編輯 | 驗證邏輯保留（限一個 grapheme），只移除 UI |
| **「之後再填」toggle**（S-04）| 09-04 | 兩個 toggle 拿掉，**空欄位即待填**，pending 旗標由系統自動寫入 DB | 旗標是欄位不是 UI，恢復 toggle 不影響資料 |
| **消費列「待補填」badge** | 09-04 | 金額欄已顯示「還沒填」，badge 再寫一次是同一件事講兩次 | — |
| **首頁行程卡的成員顯示** | 08-31 | 首頁定位為回憶架；行程命名本身已含人數；成員屬進入行程後的層級 | **僅限首頁**。內頁／記帳／結算／分享頁照常顯示 |
| **S-02-12「這是我」標記** | 09-04 | 隨「切換成員視角」取消一起移除。建立與編輯行程各少一步 | `trips.owner_member_id` 欄位保留不刪 |
| **S-02-19 建立行程顯示主色調** | 09-04 | 建立時不提供調整，就不該顯示。看到不能動的東西只會想「這能改嗎」| 色調調整維持在 S-02b |

---

## C. 已知缺口（不是不做，是還沒排）

詳見 `Tripay_問題優先序清單.md`，此處只列索引：

| 編號 | 缺口 | 狀態 |
|---|---|---|
| P1-0 | 各自金額無幣別，填外幣會靜默算錯 | ✅ **2026-09-11 實測已解**（見下） |
| P1-1 | 已封存的行程，消費仍點得進去編輯 | ✅ **2026-09-11 實測已解**（見下） |
| P1-2 | 彈出頁可左右滑動、會跑版 | ✅ **2026-09-11 實測已解**（見下） |

### 2026-09-11　收線前把這三條實際量一次（Cowork 跑，不是讀碼推論）

用 76dd035 的建置，在無頭瀏覽器實走：

- **P1-0**：記一筆 →「各付各的」，每個人的金額欄位上方有「$ 台幣填 ／ ₩ KRW 填」
  兩個分頁可切。**填外幣不會再被當台幣算。**
- **P1-1**：`s03&state=archived` 點第一筆消費 → **表單不開**（正確）。
  對照組 `s03&state=settled` 點下去**表單會開**——那是 `畫面地圖.md` 白紙黑字的規格
  「已結算 → 列表唯讀（點擊仍可編輯，重新計算）」，不是 bug，不要再「修」掉它。
- **P1-2**：`s05&state=settled` 在 320px 下 `scrollWidth = clientWidth = 320`，
  `.clearbtn` 右緣 291（門檻 320）。**不橫向捲動。**

另外三條「沒有人在守」的既有缺口也一併量掉，都已修：
已結算／已封存的消費列「約」標記 0 處（守門：一般狀態下有 2 處，證明假資料裡確實有未定案的帳，
不是因為沒東西可測而假通過）；結算頁沒有 NaN／undefined／負金額。

## 2026-09-06 Cowork 複驗發現，Rozi 尚未排程

- **點進已刪除／不存在的行程會永遠卡在「載入中」**：`/trips/:id` 用失效的 id 進入時，
  `.single()` 回 0 筆 → HTTP 406 → 沒有錯誤處理，spinner 不會停。
  → **2026-09-06 Rozi 已裁示要做，見 `專案狀態.md` 的決策紀錄，將寫成 `實作-M`。**

- **分享頁失效畫面的「開一趟自己的」入口 → 延到共編階段再做**
  （Rozi 2026-09-06 裁示）。理由是她的產品判斷，記在這裡免得日後有人「順手補上」：
  **旅伴撲空時看到那顆按鈕，可能會自己另開一趟、重複記同一趟旅行的帳，變成兩本帳。**
  在沒有共編、兩本帳併不回來之前，這個入口製造的混亂大於它帶來的新用戶。
  共編做出來、兩邊能合併之後再放。

- **成員識別圓圈的尺寸在編輯前後不一致（32px／28px）**：`<Avatar>` 在成員列
  未編輯時 28px、進入就地編輯時 32px，切換會讓那一列微微跳動。
  Rozi 沒抱怨過，2026-09-06 的類別 emoji 那一輪明文界線寫「不要順手改成員那邊」。
  → **什麼時候可以重新考慮**：下一次有人回報成員列跳動，或下一輪要統一
  `.avatar` 尺寸規則時一起處理（類別 emoji 已經統一成 28px，成員這邊照抄即可）。

## 2026-09-06（實作-R 掃可點區時看到，這一輪沒動）

> ⚠️ **2026-09-11 更新：下面這兩條關於 `✕` 的紀錄已經過時，事情早就做完了。**
> Cowork 實地量過：`src/` 裡的 `✕`／`＋`／`✓`／`⠿`／`×` 字元 **0 個**，
> 成員與支付方式的移除鈕都已經是畫出來的 icon，有效可點區 **43×44**（四邊對稱），
> 而且 `TripFormSheet.test.tsx:93` 已經有一條測試在守，測的正是「成員列有渲染出來」的狀態。
> **這兩條保留只為了留下紀錄，不要再排工。**

- **`TripFormSheet.tsx:752` 成員移除鈕用的是文字字元 `✕`**，
  違反 `Icon.tsx` 開頭那條「不要用文字字元充當 icon（樣子由系統字體決定）」，
  而且 `TripFormSheet.test.tsx:91` 有一條測試在禁這件事（目前沒抓到，因為它測的是另一個渲染狀態）。
  → 換成 `<Icon>`，連同那條測試一起補強。**留到下一輪，實作-R 只擴可點區。**
- **矮但寬的按鈕**（`.chip` 35px、分頁鈕 39px、`.rowb` 41px、`.btn` 40px、s01「新增行程」37px）
  低於 Apple 的 44pt 建議值，但寬度 52～322px，實際點得到。
  這一輪刻意不動（動了會改整個版面）。C6 的門檻放行它們——**要收緊是 Rozi 的畫面決策。**

- **`TripFormSheet.tsx` 成員移除鈕的 `✕` 是文字字元**：違反「不要用文字字元充當 icon」
  （樣子由系統字體決定、大小跟字級走），`TripFormSheet.test.tsx:91` 有一條測試在禁它。
  實作-R 明文寫「這一輪只擴可點區，`✕` 換成 `<Icon>` 留到下一輪」，所以沒動。
  → **什麼時候可以重新考慮**：下一次動到成員列時一起換成 `<Icon name="close" size={16} />`，
  換的時候要一併確認 44×44 的 `.tap44` 擴張還在。


---

## 2026-09-08　封面照片上傳：討論完整紀錄（Rozi 裁示：**保留，Phase 1 維持色調**）

### 為什麼會討論到它

盤延後清單時 Cowork 指出：**封面上傳整個沒做**，不是延後項目，是原本排在「第二批」沒做到。
Rozi 問「Phase 1 如果加入，要不要改視覺稿」。

### 量到的現況（不是推論）

- **視覺稿不用重畫。** `Tripay_原型.html` 的 S-02b 封面區**已經畫好**，
  被一個總開關關掉：`const COVER_UI = false;`（約第 1108 行）。
  改成 `true` 整區就回來：封面標題、104px 預覽、「目前：目的地色調」、
  **更換照片／重選色調**兩顆鈕、8 色預設盤與「自動」鈕。
  Cowork 2026-09-08 把開關打開、用無頭瀏覽器截了圖給 Rozi 看過（拋棄式對照稿，未進 repo）。
- **後端早就備好**：migration 008 的 `covers` bucket、Storage RLS 四條、`trips.cover_path`，
  都已套 production。
- **前端一行沒寫**：`cover_path` 只出現在型別與測試 fixture，
  **首頁卡片、行程頁大圖、分享頁大圖三處都只畫漸層色**，沒有一處讀照片。
- 原型那句灰字寫的是「**只有你和拿到分享連結的人看得到**」——
  它假設**分享頁會顯示照片**。若分享頁不顯示，這句話是錯的，要改成「只有你看得到」。

### 分享頁怎麼讓沒登入的人看到照片：三個解法

| | 做法 | 代價 |
|---|---|---|
| 甲 | 沒登入就看色調（維持現況） | 分享出去的畫面跟自己看到的不一樣；那句灰字要改 |
| 乙 | 分享時把封面另存一份到公開空間 | 有連結就看得到照片；要多一套同步／刪除邏輯 |
| 丙 | 發長效簽名網址，跟著分享連結一起給 | 不用複製檔案，但網址外流＝照片外流 |

**乙與丙的安全性其實同級**：拿到分享連結的人本來就看得到整趟帳目與每個人的金額，
多一張封面照沒有把風險升一級。真正的差別是**照片的性質**——帳目是數字，照片可能有人臉。

### Rozi 提出、目前還沒有解的風險

> 「我覺得後續還會延伸**文字看不清楚**的問題。」

現在封面是**可控的漸層色**，所以疊在上面的字（首頁卡片的狀態 badge／行程名稱／日期區間、
行程頁大圖的標題、分享頁大圖）一定讀得到。**換成使用者自己上傳的照片就不可控**——
亮色照片上白字會看不見。要做的話得先解決：遮罩層？文字陰影？依照片亮度自動切換深淺字？
**這是做封面上傳的前置設計，不是做完再補。**

### OG 分享預覽圖的相依

貼到 LINE／FB 的那張預覽圖（`G-04`，已列「不做」）**依賴封面照**。
沒有照片就只能生一塊漸層色，沒有意義。封面上傳不做，它就繼續不做。

### 決定

**Phase 1 維持目的地色調，封面上傳保留不做。**
什麼時候可以重新考慮：**照片上的文字對比方案想清楚之後**，
連同分享頁的甲／乙／丙一起決定，再打開 `COVER_UI`。

---

## 2026-09-10（實作-AF 複驗後）延後三項

1. **`Claude outputs/_repro_修2.cjs` 仍被版控追蹤**（1 個檔）。
   `.gitignore` 已擋 `*.png`，要補 `Claude outputs/*.cjs`。
   不影響功能、不含敏感資料，下一輪順手處理。
2. **`原型_*` 13 支腳本紅**：既有狀態（腳本停在 9/4–9/5，原型 html 之後被 W／Z／AF 改過）。
   要更新到現在的原型。⚠️ 同時要把 `原型_*.cjs` **加進 Cowork 的回歸清單**——
   這是「回歸範圍圈錯」的第七次，AF 那一輪改了原型但沒有人跑那些腳本。
3. **`src/contexts/ToastContext.tsx` 的 `text-sm` 與 `bg-[#292524]` 不是專案 token**
   （九階字級沒有 `text-sm`；`--ink` 是 #151C21）。token 掃描該擋而沒擋到。

## 2026-09-10（複驗後）

- **`.git` 裡有我造成的垃圾檔**：`.git/index.lock.stale`、`.git/objects/*/tmp_obj_*` 數個。
  Cowork 從遠端這一側沒有刪除權限（`rm` 被擋），檔案本身不影響 git 運作
  （`.stale` 不是 lock、tmp_obj 是孤兒物件）。下次終端機有動作時順手
  `rm -f .git/index.lock.stale && git gc --prune=now` 即可。
- **Cowork 的驗收腳本仍只存在雲端工作區**（`verify_AF*.cjs` 六支）。
  雲端環境會被回收，下一輪要重跑得重寫。`Claude outputs/_cowork暫存_可刪/_c12x/` 有落檔的先例，
  但這一輪傳輸成本高（base64 走 shell），先不做。
- **`原型_*.cjs` 13 支紅**，回歸清單從未涵蓋 `原型_*`（範圍圈錯第七次），沿用上一輪未處理。
- **`.gitignore` 缺 `Claude outputs/*.cjs`**（`_repro_修2.cjs` 仍被追蹤）。

## 2026-09-11　Rozi 拍板

- **斷網提示不補「這是上次看到的資料」** —— Rozi 2026-09-11 明確說不用。
  提出的理由是：斷網時畫面上還留著舊資料，使用者可能誤以為是剛抓回來的。
  她的決定是維持現狀，只說「現在連不上網路」。**這條就此關閉，不要再提。**

## 2026-09-11　開工小程式的兩個剩餘風險（終端機指出，這一輪不處理）

- **AH-4 的逾時只救得了「watcher 死了、鎖留下來」這一種。**
  如果是 `claude -p` 本身掛住，watcher 會卡在前景那一行，**逾時檢查永遠輪不到**——
  外面看起來會跟 2026-09-11 03:03～08:12 那次一樣安靜。
  要救得加「前景工作也設上限」（例如 `timeout 1800 claude -p …`），這一輪沒做。
- **反過來的風險**：若同時有兩個 watcher 在跑，而某份工作做超過 30 分鐘，
  另一個會把**還在用的鎖**當成殘骸刪掉 → 同一份工作被執行兩次。
  目前只跑一個 watcher，所以碰不到；真要防要在清鎖前加 `pgrep -f kickoff.sh` 數量檢查。


## 2026-09-12　`verify_Y` 與 `原型_*` 實跑結果（Cowork 自己的事，Rozi 問「現在不能做嗎」之後做的）

### `verify_Y`：**不是過時，12/12 全綠、金絲雀有效**
之前記「整支選擇器過時、6 條紅」是**假紅**——當時量測 server 掛了（`ERR_CONNECTION_REFUSED`）。
這一輪用 AH 的建置重跑：12 綠 0 紅；`BREAK=hero` 讓 7／9 轉紅、`BREAK=topbar` 讓 10／11 轉紅，
金絲雀擋得住。**這條從延後清單移除。**

→ 教訓：判紅之前先看是不是 `ERR_CONNECTION_REFUSED`。這一輪 server 被環境回收了至少四次，
已改成「同一個 shell 內起 server、跑、關」（`run_with_server.sh`），不再靠背景進程。

### `原型_*.cjs` 15 支：分三堆

**環境問題（cloud 才有，Mac 上沒有）**：root 要 `--no-sandbox`、4 支要 jsdom、
6 支要同目錄有 `_盤點_畫面功能.md`／`_icon授權.md`／`_盤點_實作缺口.md`。已用墊片與補檔解決。

| 結果 | 支數 | 哪幾支 |
|---|---|---|
| ✅ 全綠 | 2 | 批三回歸 54/0、版面回歸 18/0 |
| 🟡 能跑、有紅 | 7 | icon 78/1、倒序 36/2、可點區 42/2、批三回饋 44/1、版面與拖曳 57/6、貼邊 45/6、預設日期 30/1 |
| 🔴 腳本崩潰 | 6 | 字級／批二／稿註：`currencyName is not defined`（原型的全域函式改名了）；批一：`null.value`；焦點：`undefined.parentElement`；輸入：`undefined.length` |

**紅的裡面值得注意的**：
- `版面與拖曳` 那兩條紅（第 90–91 行）在守 `.ic2` 的**舊**樣式——跟收尾-AI 的 B 案直接衝突，已寫進 AI-6 要求同步改斷言
- `預設日期`「明確 height 應該只有一份，實際 2 份」→ 跟 `.ic2` 有兩份定義是同一類（CSS 移植殘留）
- `批三回饋`「孤兒編號 S-02b-14 S-03-34 S-03-35」與 `倒序`「編號對不起來、盤點檔對不起來」→ 原型的稿註編號跟 `_盤點_畫面功能.md` 脫節，文件同步問題

**6 支崩潰的是腳本過時，不是原型壞掉**——原型由終端機維護，這些測試也是它寫的，
修腳本是它的事。Cowork 的事是「納入回歸並回報」：已納入，這是第一次完整的報告。

→ 下一輪寫一節「原型測試腳本修復」給終端機，把 6 支崩潰的修好、3 個文件同步問題處理掉。
**不併進 AI**——AI 是產品變更，這是工具修復，混在一起會讓範圍失控。

## 2026-09-12　結算完成頁分享 CTA 的文案兩邊不一致（修-8 順帶發現）

原型 `Tripay_原型.html:3137` S-05-29 寫「**把結果傳給大家**」，實作寫「**分享給大家**」。
修-8 只改按鈕的去向（原本導回行程頁，改成開既有的分享 sheet），**文案一個字都沒動**。

**為什麼不順手統一**：「分享給大家」是 Rozi 已經在手機上看過、沒有抱怨的字；
改文案是產品決定，不是 bug 修復（`CLAUDE.md`「改 A 不准順手換 B」）。

**什麼時候可以重新考慮**：下一次她檢視結算完成頁的文案時一起定，
要嘛原型改成「分享給大家」，要嘛實作改成「把結果傳給大家」——**兩邊必須同一句**。
