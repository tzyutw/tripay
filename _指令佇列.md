# 指令佇列（Cowork 端代 Rozi 撰寫；每節皆經內部審查；處理完標 [已處理]，由 Cowork 端定期清掃）

---

## #14 全畫面可點原型（批一 ✓ ／批二 ✓ ／批三 ✓ ／批四＝延後）

> ⚠️ **本節尚未完成，不要當成已處理跳過。**
> 批一（S-00／S-01／S-02／S-02b）已交付、經 #15／#16 修正，**Rozi 已確認通過**。
> **下一次「收」從批二開始**：S-03 行程頁、S-04 記帳表單、S-05 結算。
>
> **批二的行為規格**見 `規格_金額未定案與幣別.md`，涵蓋：
> 金額未定案的判定（未輸入 0／1 人不標記、2 人以上標記）、四種存檔提示、
> 統計卡的「約」標記與點擊、消費列表頂端的未定案入口、篩選後列表（兩種進入方式同一畫面）、
> 消費列樣式、結算前檢查層、以及**記帳時用行程匯率自動換算＋「設定這趟的匯率」捷徑入口**。
>
> **批二額外要遵守的（批一之後才立的規則，`CLAUDE.md` 有全文）：**
> 1. **灰字規則**：手機框裡的每個字都是產品上真的會有的字，稿註只能放右側清單。
>    欄位下方的灰字只留兩種——使用者不做就不會知道的限制、隱私或後果告知。
>    解釋系統怎麼運作／推銷功能好處／重複欄位已顯示的資訊，一律砍。
>    需要一句話解釋才懂的互動＝缺少 affordance，**修互動不要加說明**。
> 2. **不得橫向滑動**：`原型_版面回歸測試.cjs` 要涵蓋批二的新畫面，320／375／414 全過。
> 3. **徽章三通則**：隱形行為不掛徽章（列進清單標「隱形」）、重複結構只掛第一項、
>    已移除項不畫但保留在清單。
>
> **字體待定，先維持現況**：Rozi 不喜歡 IBM Plex Mono 的斜線 0，正在挑替代的等寬字。
> 那只是一個 CSS 變數，選定後全站一次換掉，**不要為此暫停批二**。
>
> **「哪位成員是我」（S-02-10）的功能存廢由 Rozi 在批二期間決定**，
> 在她裁示之前照現況做，不要自行增刪。
>
> **執行順序調整（Rozi 2026-09-04）**：批三做完並經 Rozi 確認後，
> **直接進 Phase 1＋1.5 的實作節次，不等批四**。
> 批四（Phase 2 副卡記帳）延到實作上線、Rozi 實際使用過之後再做。
>
> 每批做完一樣停下等 Rozi 看過。

⚠️ **本輪不動 `src/`、不跑 migration、不改任何實作。只做原型。**
先前佇列 #14 的實作交付與 #15 全部由本節取代。
被取代的是**交付物**，不是規格——那些規格已完整移到 `規格_金額未定案與幣別.md`，內容有效。

### 1. 為什麼是原型優先

Rozi 驗證需求的方式是**在原型上點**，不是讀規格文字。
先前的排法（規格 → 實作 → 順便做原型）順序是反的，原型屬設計階段，在實作之前。
本輪把原型做完、Rozi 逐頁確認過，才會開實作節次。

### 2. 檔案

唯一檔案 `Tripay_原型.html`（repo 根目錄，**不帶版號**——版號會誘發分岔）。

把下列兩份的內容併入，併完把兩份原檔頂端橫幅由「即將作廢」改為「已作廢」：
- `wireframe_v5_完整版.html`（全畫面完整稿與逐頁功能盤點）
- `分帳模型原型.html`（分帳三型可點原型；版本二的稿保留作紀錄即可）

⚠️ `_to_delete/Tripay_原型_COWORK廢稿.html` **不得參考**，那是越權產出的版本。

**技術限制（不遵守 Rozi 就打不開）：**
- 單一 HTML 檔，不依賴任何 build 步驟，瀏覽器直接開就能跑
- 純前端 mock，不連 Supabase、不連任何 DB
- 外部資源只允許字型；其餘 CSS/JS 一律內嵌

### 3. 視覺

原型套用**已定案的新視覺**，不是複製線上現況的樣式。
tokens 見 `打磨輪_tokens與before-after.html`：
工作色富士青 `#1276C4`、紙 `#EDEEE8`、收 `#276E45`、付 `#B03A22`、墨 `#151C21`；
標題與內文一律 PingFang TC 系統字，不載中文 webfont；
**等寬（IBM Plex Mono）只用於金額**，日期／天數／筆數用 PingFang TC＋`tabular-nums`。

### 4. 格式

右上角切換兩種模式，**預設標註模式**：
- **標註模式**：每個區塊浮出編號徽章（S-XX-NN）＋虛線框標出範圍，
  畫面旁列出該畫面的完整編號清單。徽章可點，點擊複製該編號到剪貼簿。
- **操作模式**：徽章與虛線框全部隱藏，純粹操作。

編號沿用現行 S-XX-NN，**不得重編**。
已移除的項目（S-00-1、S-00-6、S-01-9、S-01-10、S-03-13 等）不出現在原型上，
編號留在盤點表標刪除線，號碼不回收。
S-08～S-11 等盤點表尚無編號的新畫面，**編號一併新增進 `_盤點_畫面功能.md`**，
否則停止條件第 4 項對不起來。

**徽章掛不掛的三條通則（已裁示，不用再問）：**
1. **隱形行為不掛徽章。** 登入後跳轉、排序規則、列表過濾、欄位退場這類
   「有規則但沒有畫面區塊」的項目，只列進編號清單並標「隱形」，不硬造視覺元素去掛。
2. **重複結構只掛第一項。** 成員列、消費列這種同構的多列，徽章掛第一列代表整組，
   不要每列都掛——那是同一個功能，不是四個不同的東西。
3. **已移除的項目不畫在原型上**，但仍列進編號清單標「已移除」，號碼不回收。

### 5. 範疇

S-00 到 S-11 全部畫面，一個都不少。

- **有互動邏輯的區塊** → 做成可實際操作
- **純視覺、無分支邏輯的區塊**（空狀態、純資訊頁）→ 靜態即可，但仍要有編號徽章
- 判斷不了算不算「有互動」時，做成可操作的那一版

**分四批，依使用者實際動線由前往後，每批做完停下等 Rozi 看過再做下一批：**

| 批次 | 畫面 |
|---|---|
| 批一 | S-00 登入、S-01 首頁、S-02 建立行程、S-02b 編輯行程 |
| 批二 | S-03 行程頁、S-04 記帳表單、S-05 結算 |
| 批三 | **S-02c 成員識別（正確是 13 個編號，不是 15——指令原本寫 15 是 Cowork 端 grep 誤算；其中 1～9 已隨 EmojiPicker 移除，有效項目為 10～13）**、S-06、S-07 及子畫面（S-03b 分享、S-03c 刪除、S-04 子彈窗等）。<br>⚠️ **成員分佈 chips 已由 Rozi 裁示不做**（2026-09-04），不要加進來 |
| 批四 | S-08～S-11（通訊錄、卡片管理、帳單週期、副卡視角）**＝ Phase 2 副卡記帳。Rozi 2026-09-04 裁示：批四延後到 Phase 1＋1.5 實作上線、她實際使用過之後再做**，因為 Phase 2 規格只有初稿、細節未定，且與 Phase 1 實作無依賴 |

### 6. 每批交付時要附的東西

1. 該批畫面的編號清單（編號＋一句話說明＋是可操作還是靜態）
2. 本批與 `_盤點_畫面功能.md` 的差異（新增的編號、標刪除線的編號）
3. 做不出來或規格自相矛盾的地方（見第 7 節）

### 7. 行為規格

原型的互動行為照 `規格_金額未定案與幣別.md` 做。該檔已在 repo 根目錄。
內容涵蓋：各自金額的幣別與換算、未定案判定、四種存檔提示、
統計卡「約」標記、未定案入口、篩選後列表、消費列樣式、結算前檢查、文案。

⚠️ 發現規格做不出來、或規格自相矛盾 → **停下回報，不自行取捨**。

### 8. 停止條件（全部成立才算完成）

1. 四批全部併入同一份 `Tripay_原型.html`
2. 每一批都經 Rozi 確認過（未確認的批次不算完成）
3. `wireframe_v5_完整版.html` 與 `分帳模型原型.html` 的橫幅已改為「已作廢」
4. `_盤點_畫面功能.md` 的編號與原型的**編號清單**逐一對得起來，無孤兒、無缺漏。
   （對的是清單，不是徽章——隱形項本來就沒有徽章可掛，見第 4 節）

### 9. 護欄（觸發任一項立刻停下回報）

1. **不改 `src/` 下任何檔案**——本節只做原型
2. 不跑任何 migration、不對 DB 做任何寫入
3. 不刪除任何檔案（含舊稿），只改橫幅文字
4. **每批做完停下回報，不要一口氣做完四批**
5. 規格與現況衝突、或規格本身矛盾 → 停下回報，不自行決定怎麼取捨

---

## #15 批一修正（做完再接批二） [已處理]

Rozi 已看過批一原型，以下是回饋與裁示。**先把批一修到位，再做批二。**

### 1. S-02-19 建立行程不顯示主色調

建立行程頁不提供調整色調，就不該顯示色調是什麼。**整項從 S-02 移除。**
理由：使用者當下關心的是「叫什麼、什麼時候、跟誰」，
看到一個不能動的東西只會想「這能改嗎、怎麼改」。
色調的調整維持在 S-02b 編輯行程頁，那裡不動。

編號 S-02-19 留在盤點表標刪除線，號碼不回收。

### 2. 清掉手機框裡的說明性灰字

**原則（已寫進 `CLAUDE.md`，此處只列本批要動的）：**
欄位下方的灰字只留兩種——①使用者不做就不會知道的限制或選項
②隱私或後果的告知。其餘一律砍，改用介面本身表達。

本批逐條處置：

| 現有灰字 | 處置 |
|---|---|
| 封面會依行程名自動套目的地色調，建立後可在「編輯行程」換成照片 | **砍**（在解釋系統怎麼運作）|
| 預設今天 | **砍**（欄位上就寫著今天的日期）|
| 最多 10 個字。 | **砍**（超過再提示）|
| 色調＝目的地色系預設盤（…） | **砍**（在解釋內部機制）|
| 最後一個是「自動依行程名判定」 | **砍**（讓那個選項自己標示，例如選項本身就叫「自動」）|
| 點成員，標記哪位是你 | **砍**，並見第 3 點 |
| 標記哪位是你，統計卡的「我的花費」就會算你這一份。 | **砍**（在推銷功能好處）|
| 可留空＝當天來回 | **留**（不講使用者不會知道可以留空）|
| 上傳後會自動壓成 webp、長邊 1600px。只有你和拿到分享連結的人看得到。 | **前半砍**（技術細節，使用者不在乎），**後半留**：「只有你和拿到分享連結的人看得到」 |

### 3. 「標記哪位是你」要修互動，不是留說明

需要一句話教使用者怎麼點，代表那個互動**沒有做出「可以點」的樣子**。
把說明砍掉之後，成員列本身要看得出可以標記——
做法自行決定（例如每列右側常駐一個未選取狀態的「這是我」標記，點了才填實），
**不要用灰字補救**。

註：S-02-10／S-02-17「哪位成員是我」這個功能本身要不要保留，
Rozi 要在批二時一併討論（關係到之後的成員視角切換）。
**本節只修互動呈現，不要動功能存廢。**

### 4. 現金匯率（Rozi 已拍板方案 C）

完整規格見 `規格_金額未定案與幣別.md` 第 2b 節（已更新）。摘要：
- 行程層級設一個「這趟的現金匯率」，單筆可覆寫
- 現金／儲值卡用它換算台幣；**刷卡不套用**，刷卡直接填台幣
- 輸入以「台幣 X 換到 Y 外幣」為主，也可直接填匯率，兩者連動
- 設定位置在 **S-02b 編輯行程頁**，與「這趟的支付方式」並列。
  **不放建立行程頁**——換錢發生在出發之後
- 修改匯率時，用行程匯率換算的消費即時重算；單筆覆寫過的不受影響；已結算的不重算
- 還沒設匯率時該筆標 `twd_pending`、結算跳過，並顯示可點的「設定這趟的匯率」入口。**存檔照樣成功**

**本批（批一）只做 S-02b 的匯率設定介面。**
記帳時的換算與捷徑入口屬 S-04，**留到批二做，不要提前**。

### 5. 版面：不得橫向滑動

Rozi 回報線上產品的 sheet／modal 可以左右滑動、會跑版。
常設守則已寫進 `CLAUDE.md`。原型上也要遵守：

- 任何 sheet／modal／頁面不得整頁橫向捲動
- 寬內容自己包一層 `overflow-x:auto`，不要把整頁撐開
- sheet 開啟時鎖底層捲動，關閉還原
- 元素一律 `max-width:100%`

**要做成自動測試**，在 320／375／414 三種寬度逐頁斷言
`documentElement.scrollWidth <= clientWidth`，以及每個 sheet 容器同樣的斷言。
之後每一批都要跑這組測試。

### 6. 停止條件

1. 第 1～5 項全部做完
2. 橫向滑動測試在 320／375／414 三種寬度全過
3. 既有的自我測試（批一 29 項、輸入回歸 34 項）沒有因本次改動而失敗
4. 編號清單與 `_盤點_畫面功能.md` 仍對得起來（S-02-19 標刪除線）

達成後停下回報，等 Rozi 看過再做批二。

### 7. 護欄

- 不改 `src/`、不跑 migration、不刪檔（同 #14）
- 第 3 點只修互動呈現，**不要動「哪位成員是我」的功能存廢**
- 第 4 點只做 S-02b 的設定介面，**S-04 的換算留到批二**
- 判斷不了的技術或版面問題回報給 Cowork 端決定，**不要丟給 Rozi**


---

## #16 現金匯率介面收斂（批一補修，做完再接批二） [已處理]

Rozi 看過批一的 `S-02b-12` 後指出兩件事，**第二件推翻了第一版的改法**：

1. 現在要填 4 個輸入框，太複雜
2. **更根本的**：參考截圖（WOWPASS 換匯）的邏輯與 Tripay 相反——
   WOWPASS 的匯率是銀行給的**已知值**，使用者填金額看能換多少；
   Tripay 的匯率是使用者要告訴系統的**未知值**。
   **換了多少錢對分帳完全沒有影響，只有匯率有影響**，所以兩個金額欄本身就是多餘的。

### 1. 改成（完整規格見 `規格_金額未定案與幣別.md` 第 2b.3 節）

    ┌──────────────────────────────┐
    │  這趟的現金匯率               │
    │  在當地換錢後填一次就好        │
    │                              │
    │  🇹🇼  [    1    ]  TWD        │  ← 預設 1，通常不用動
    │  🇰🇷  [   45    ]  KRW        │  ← 只填這個
    │                              │
    │  刷卡不用這個匯率，直接填台幣    │
    └──────────────────────────────┘

- **「1」擺哪一欄依幣別決定**（見下方 1a），另一欄空白待填，是唯一必填的數字。兩欄都可編輯
- 匯率 ＝ 外幣 ÷ 台幣，內部計算
- **移除**上一版那行「1 TWD ≈ 45.0 KRW」的唯讀灰字——
  畫面本身就是那個算式，再寫一次是重複
- 兩欄雙向即時運算
- **兩種情境同一個介面，不做模式切換**：
  知道匯率 → 台幣留 1、外幣填 45（**一個數字**）；
  只記得換了 10,000 拿到 450,000 → 台幣改 10000、外幣填 450000（兩個數字）
- 標籤用國旗 emoji ＋ 幣別代碼，移除「台幣總額」這類文字標籤
- 焦點狀態：編輯中的欄位用主文字色，另一欄轉次要文字色
- 不做幣別下拉（行程已設定幣別）
- 外幣小數位數依幣別：KRW／JPY 不給小數，USD／EUR 給兩位
- 不做自訂數字鍵盤，`inputmode="decimal"` 即可
- 金額字級放大（這是區塊主角），沿用等寬字

### 1a. 「1」擺哪一欄

台灣人講匯率會選數字好記的方向，而方向依幣別不同：

| 幣別 | 習慣講法 | 「1」放哪 |
|---|---|---|
| KRW 韓元 | 1 台幣 = 45 韓元 | **台幣欄** |
| JPY 日圓 | 1 日圓 = 0.22 台幣 | **外幣欄** |
| THB 泰銖 | 1 泰銖 = 0.9 台幣 | **外幣欄** |
| USD 美金 | 1 美金 = 32 台幣 | **外幣欄** |
| VND／IDR | 1 台幣 = N 外幣 | **台幣欄** |

**判斷規則**（`src/lib/currencies.ts` 的其餘幣別依此自行判定，不需回報逐一確認）：

    1 單位外幣換算成台幣後 < 0.1 → 「1」放台幣欄
    否則                        → 「1」放外幣欄

**「1」的那一欄排在上面**，讀起來永遠是「1 X = N Y」。
兩列的上下順序會依幣別改變，**這是刻意的，不是 bug**。

這只影響預設值擺哪邊，不影響計算——使用者把 1 移到另一欄照樣算出同一個匯率。
**兩欄都要允許小數輸入**（台幣欄可能是 0.22）。

### 2. 灰字精簡

該區塊的灰字只留兩句：
- 區塊副標：`在當地換錢後填一次就好`
- 底部：`刷卡不用這個匯率，直接填台幣`

原本的「現金與儲值卡用它換算，刷卡直接填台幣不套用」前半刪除——
區塊標題已寫著「現金匯率」，前半重複。

### 3. 停止條件

1. 第 1、2 項做完，該區塊的 `input` 元素數量為 **2**，且台幣欄預設值為 `1`
2. 該區塊**不存在**唯讀的匯率顯示列
3. 橫向滑動測試在 320／375／414 三種寬度全過
4. 既有三組測試（批一 45 項、輸入回歸 34 項、版面 12 項）沒有因本次改動失敗
5. 新增測試，至少涵蓋：
   - 輸入框數量為 2、匯率顯示列不存在
   - **KRW**：「1」在台幣欄且排在上面；外幣填 45 → 內部匯率 45
   - **JPY**：「1」在外幣欄且排在上面；台幣欄填 0.22 → 內部匯率等價於 1 JPY = 0.22 TWD
   - **THB**：「1」在外幣欄
   - 把 1 改成 10000、另一欄填 450000 → 匯率與「1 / 45」的情況相同
   - 兩欄都接受小數輸入

### 4. 護欄

- 不改 `src/`、不跑 migration、不刪檔
- 只動 `S-02b-12` 這個區塊，不要順手改 S-02b 其他部分
- 判斷不了的技術或版面問題回報給 Cowork 端決定，不要丟給 Rozi

---

## #17 批二修正（做完再接批三） [已處理]

Rozi 已看過批二原型，以下七項。**先把批二修到位，再做批三。**

### 1. Icon 全站改用真正的 SVG（不只 S-03）

現況：`‹ ✎ ⧉ ↑ ⚙` 等**都是文字符號**，不是 icon，粗細跟著字型走所以顯得細。
全份原型只有 1 個 `<svg>`。

**改法：全站 icon 一律改用 Remix Icon 的 SVG，內嵌 path，不載外部資源。**
- 取得方式：`npm install remixicon`，SVG 檔在 `node_modules/remixicon/icons/**`
- **把用到的 icon 的 path 資料內嵌進 `Tripay_原型.html`**（原型是單檔、不依賴 build）
- 授權 Apache 2.0，**在檔案註解保留授權聲明**
- 建議用 **fill 版本**（`-fill.svg`）或 line 版本加大 stroke——Rozi 的意見是「線條太細」，
  自行判斷哪個達到她要的份量，同一組 icon 風格必須統一，不可 line 與 fill 混用
- 尺寸與顏色用 `currentColor` 與統一的 size token，不要逐處寫死

**範圍是全站，不只 S-03**：返回、編輯、複製、分享、設定、新增、刪除、篩選、
勾選、展開收合、警示等，所有目前用文字符號或 emoji 充當 icon 的地方。
**成員識別與封面的 emoji 不算 icon，維持不動**（既有決策）。

### 2. S-03-9 ＋ S-03-27 改成可展開卡片

現況：統計卡直接把每個人的分擔金額全部列出來，軸長被拉長。

**改法：預設只顯示「這趟總花費」，每人分擔列收合起來，點開才展開。**
理由（Rozi 原話）：使用者一進來不一定要看每個人付了多少，
全部攤開會讓往下滑的軸長變得很長。

- 收合狀態：顯示總花費 ＋ 一個可點的展開控制
- **「約」標記的資訊不能因為收合就消失**——收合時若有人被標「約」，
  在收合列上要有可辨識的提示（自行設計，但不得用一句灰字補救）
- 展開後即現行的每人分擔列（含「約」標記與點擊進篩選）

### 3. S-03-12 幣別切換要能真的切

現況被標成靜態，點了沒反應。

**改法：改為可操作。** 切到外幣時整頁金額換算成外幣顯示，
切回台幣即還原。**結算恆為台幣，不受切換影響**（既有決策）。
換算用行程的現金匯率；沒設匯率時該切換不可用並說明原因。

### 4. 記帳時不設定匯率（規格變更，Rozi 裁示）

Rozi 的判斷：**單筆記帳的場合根本不需要設定匯率**。
匯率是「換錢」時的事，記帳只是消費紀錄。

**移除 S-04 的就地設定匯率入口。**（規格 §2b.3 的「捷徑」整段作廢。）

**改用更簡單的規則，與支付方式無關：**

| 這筆有什麼 | 台幣金額怎麼來 |
|---|---|
| 有台幣金額 | 直接用它。這是實付金額，最準 |
| 只有外幣 | 用**行程的現金匯率**換算 |
| 兩者都有 | 用台幣（實付）；外幣只當紀錄 |
| 只有外幣、且行程還沒設匯率 | 該筆標 `twd_pending`、結算跳過，**照樣可以存檔**。<br>顯示一個連結跳到 S-02b 設定，設完回來自動換算 |

這條規則自動涵蓋 Rozi 指出的三種情況：
- **信用卡**：帳單就是台幣，一定走第一列，不需要匯率
- **儲值卡**：有台幣就走第一列；只有外幣就走第二列，跟現金同一套
- **現金**：通常只有外幣，走第二列用行程匯率

**同時移除「單筆可覆寫匯率」**。理由：一趟旅程若換兩次錢，匯率差通常在 1–3%，
而分帳是相對的、所有人用同一個匯率，誤差會相互抵消；
為此多一層「單筆匯率 vs 行程匯率誰大」的心智負擔不划算。
真的需要時改行程匯率即可（會回溯重算）。

⚠️ **匯率只有一個地方能設定：S-02b 編輯行程頁。** 不得有第二個入口。

### 5. 選取樣式全站統一

Rozi 指出 S-04-12（分給誰）、S-04-18（當場各付各的）、S-04-19（贊助）
用打勾的方式，跟 S-02 設定「哪位成員是我」的呈現不一致。

**改法：沿用 S-02 成員識別的那一套**（未選為描邊、選了填實成工作色），
全站所有「選取／未選取」的呈現統一。不得同一個產品裡出現兩套選取語彙。

### 6. S-04-11「一起分」預設收合（回到已定案的版本一）

⚠️ **這一項偏離了 2026-09-04 已定案的分帳模型，必須改回。**

已定案的規則（見 `分帳模型原型.html` 的 R2，該原型即 Rozi 試用後定案的版本一）：

    R2  一起分：預設全員均分，要排除人才點開

也就是：
- 選「一起分」後，**預設只顯示「N 人均分 · 每人 $X」**，不展開任何名單
- 旁邊一顆「**要排除誰？**」，**點了才展開**成員清單
- 展開後的選取樣式依第 5 項統一

現況直接攤開一整排勾選清單，與定案不符，也與第 2 項「不要把當下不需要的東西
全部攤在畫面上」的原則衝突。

其餘兩型維持已定案的規則：
- R3 **「只算一個人」預設帶付款人、可改**（記帳者未必是受益人）
- R8 **各付各的：空白欄位即時顯示自動均分值、無按鈕；空白 ≠ 輸入 0**

**實作前先讀 `分帳模型原型.html` 的 `RULES` 區塊（R1–R8）逐條對照**，
確認批二的 S-04 沒有其他偏離定案之處，發現差異一併回報。

### 7. 動態 icon（空狀態與結算）

Rozi 希望空狀態、結算完成這類時刻有動態 icon，
參考 `https://www.iconanimator.app` 的動作風格。

**技術限制：原型是單檔、不依賴 build、外部資源只允許字型**，
所以**不得引入 Lottie 或任何外部動畫庫**。

做法自行判斷，兩條路：
- **優先**：內嵌 SVG ＋ CSS 動畫自己畫（可控、體積小、跟 Remix Icon 同一套語彙）
- **備案**：emoji ＋ CSS 動畫（結算可用 🧮，Rozi 指定可接受）

**範圍先做兩處就好**：S-03 空狀態、S-05 結算完成。不要全站鋪。
**必須遵守 `prefers-reduced-motion`**：使用者關閉動態效果時不播放。
既有決策：整個產品**只做一個 orchestrated moment**，結算就是那一個，
其餘動畫維持克制。

### 10. 「當場各付各的」移進「誰付的？」

現況：獨立佔一列，還配一句灰字「記錄但不算欠款」。Rozi 指出使用比例偏低卻很佔視覺。

**改法：它本來就是在回答「誰付的」——答案是「沒有人代墊」。**
把它變成「誰付的？」的最後一個選項：

    誰付的？
    🍋 Ning   🐟 Ziyu   🐵 Xiu   🐱 Mei   ｜  大家各付各的

- 不再獨立佔一列，灰字「記錄但不算欠款」一併移除
- **三型都適用**（現況只在「一起分」時出現，改後一起分／各付各的／只算一個人都能選）
- 選了它就是 `settled_on_spot = true`，記錄但不進結算，行為不變
- 排在成員之後、用分隔線區隔，使用比例低的選項不干擾主要動線

### 11. 提示句減量（記帳表單）

現況 S-04 有 8 種狀態提示句。Rozi 的原則：**需要提示，就代表 UX 沒做好**。

**逐條處置：**

| 現況提示 | 處置 |
|---|---|
| 沒填的先照均分算，之後可以再改 | **砍**。空欄位的 placeholder 已經顯示均分值了 |
| 剩下的平均分給還沒填的 N 人 | **砍**。同上，placeholder 就是答案 |
| 加總剛好等於總額 ✓ | **砍句子留狀態**。用顏色／符號表達，不寫一句話 |
| 外幣總額空著，先用各自金額加總 ⋯ | **砍句子**。直接把加總值填進外幣總額欄並標示為自動值（可覆寫），欄位自己說話 |
| 同一筆只能用一種幣別填，不混填 | **砍**。切換器本身就是單選，不需要解釋單選 |
| 用這趟的現金匯率換算成 ⋯ | **砍句子留數字**。台幣欄直接顯示換算結果並標為自動值 |
| 台幣金額還沒填，這筆⋯結算會跳過 | **留但縮短**。這是阻塞狀態，使用者不知道就會困惑 |
| 還沒填外幣總額，先補上才算得出各自要付多少 | **留但縮短**。同上，這是真的算不出來 |
| 金額會記成負數，由 N 人均分扣抵 | **留**。贊助是反直覺的行為，不講會誤解 |

**判準**：只有「使用者不做這個動作就不會知道、而且會影響他判斷」的才留；
凡是解釋介面自己就能表達的東西，一律砍。

⚠️ 砍掉句子不等於拿掉資訊——**要用 placeholder、顏色、狀態樣式把資訊做進介面本身**。

### 8. 停止條件

1. 第 1～7 項全部做完
2. 全站不再有文字符號充當 icon（可自動斷言：`‹ ✎ ⧉ ↑ ⚙` 等字元不得出現在 icon 位置）
3. `分帳模型原型.html` 的 R1–R8 逐條對照通過，無偏離
4. 全站不存在第二個匯率設定入口（可自動斷言）
5. 橫向滑動測試 320／375／414 全過
6. 既有四組測試沒有因本次改動失敗
7. 新增測試：統計卡預設收合、幣別切換可操作、一起分預設不展開名單、
   選取樣式全站單一套、記帳頁無匯率設定入口
8. 「大家各付各的」出現在「誰付的？」選項列，且三型都可選；不再獨立佔一列
9. S-04 的提示句數量從 9 條減到 3 條（可自動斷言被砍的 6 條字串不再出現）

### 9. 護欄

- 不改 `src/`、不跑 migration、不刪檔
- 字體仍維持現況（Rozi 尚在挑選，選定後另行一次換掉）
- 「我的花費／哪位成員是我」的存廢 Rozi 尚未裁示，**照現況做，不要自行增刪**
- 判斷不了的技術或版面問題回報給 Cowork 端決定，不要丟給 Rozi

---

## #18 全站字體換成 Onest（等 #17 做完再做） [已處理]

Rozi 已挑定：**Onest**（Google Fonts，非等寬 sans）。
起因是她不接受 IBM Plex Mono 的斜線 0；實測 55 個字體後確認：
等寬字大多在 0 中間加點或斜線（為了跟字母 O 區分，那是寫程式用的慣例），
真正空心 0 的等寬字只有 4 個無襯線候選；一般 sans 則全部是空心 0。

### 1. 新的字體規則（取代既有規則）

**作廢**：2026-08-30「全站唯一 webfont 是數字用的 IBM Plex Mono」、
2026-08-31「等寬（IBM Plex Mono）只用於金額；其他數字用 PingFang TC＋tabular-nums」。

**新規則：**

    font-family: "Onest", "PingFang TC", "Noto Sans TC", -apple-system, sans-serif;

- **Onest 不含中文字**，所以中文會自動 fallback 到 PingFang TC。
  結果是**西文與數字用 Onest、中文用 PingFang TC**，一條 font-family 就達成，
  不需要為金額另外指定字體。
- **IBM Plex Mono 完全移除**，全站不再有任何等寬字。
- **金額的層級改靠字重與字階**，不靠字體差異：
  金額用 `font-variant-numeric: tabular-nums` 對齊、字重 500–600；
  一般文字 400。這與 2026-08-31「標題靠字重與字階建立層級」一致。
- Google Fonts 只載 **400 / 500 / 600** 三個字重，不要載整個 variable 範圍。

### 2. 範圍

- `Tripay_原型.html`：`--mono` 這個 token 整個移除，
  所有引用它的地方（含右側稿註區、編號徽章、標籤）一併改用主字體
- `打磨輪_tokens與before-after.html`：token 定義同步更新
- **`src/` 本輪不動**（原型階段的護欄不變），實作階段另行處理

### 3. 停止條件

1. 全站搜尋不到 `IBM Plex Mono`、`--mono`、`monospace`（原型與 tokens 檔）
2. Google Fonts 的 link 只載 Onest 的 400/500/600
3. 金額仍有 `tabular-nums` 對齊（可自動斷言金額元素具備此屬性）
4. 既有四組測試沒有因本次改動失敗
5. 橫向滑動測試 320／375／414 全過（換字體會改變字寬，必須重測）

### 4. 護欄

- 不改 `src/`、不跑 migration、不刪檔
- 只換字體，不要順手調整任何版面或間距——
  若換字後有版面溢出，只修溢出的那一處並回報

---

## #19 「哪位成員是我」整組移除（與 #18 一起做完再接批三） [已處理]

Rozi 裁示：**「我的花費」不顯示、「切換成員視角」不做、「哪個成員是我」移除。**

### 1. 為什麼可以整組拿掉

`trips.owner_member_id` 在整個產品裡**只被用來算「我的花費」**，沒有第二個用途。
其他出現它的地方（S-02-12「這是我」、S-02b 儲存時連動）都只是在**維護這個欄位本身**，
不是在使用它。

統計卡已改為「收合只顯示總花費、點開顯示每個人」，
「我的花費」那個數字不再需要 → 這個欄位失去用途 → 整組收掉。

### 2. 要移除的三項

| 編號 | 項目 | 處置 |
|---|---|---|
| `S-02-12` | 成員列「這是我」標記 | **移除**。建立行程少一個步驟 |
| `S-03-11` | 我的花費 | 已於批二移除，維持 |
| — | Phase 1.5「切換成員視角」 | **從候選清單移除，不做** |

⚠️ **S-02-10 成員區標題**（「誰一起去？」）**保留**，只是不再有「這是我」的標記。

### 3. 資料層

`trips.owner_member_id` **欄位保留不刪**（既有資料相容），前端不再讀寫。
處理方式與先前的 `trips.emoji` 相同。
**本輪不出 migration**，只是前端不再使用。

⚠️ 若程式碼有任何地方**寫入** `owner_member_id`，寫入端要一併清掉——
不得只清顯示端（既有的完整性守則）。原型層先做到；`src/` 留待實作階段。

### 4. 一併釐清的誤記

決策表 2026-08-31 寫「切換成員視角**與 S-11 副卡視角共用同一套元件**」——
這是把兩件事誤當成一件：
副卡的「視角」是**哪張卡**，成員視角是**哪個人**，兩者不共用。
本節移除成員視角**不影響** Phase 2 副卡的規劃。

### 8. 停止條件

1. 原型中不存在「這是我」的標記或任何 `owner_member_id` 的讀寫
2. 建立行程（S-02）與編輯行程（S-02b）的步驟各少一項
3. `_盤點_畫面功能.md` 的 `S-02-12` 標刪除線，號碼不回收
4. 既有四組測試沒有因本次改動失敗
5. 新增測試：斷言原型無「這是我」相關的選取狀態與 `owner_member_id` 欄位

### 9. 護欄

- 不改 `src/`、不跑 migration、不刪檔
- **不要順手動 S-02-10 成員區標題**，那一項保留

---

## #20 批二補修（做完再接批三） [已處理]

Rozi 看過 #18／#19 後的四項，**四項都是 bug**。
第 3、4 項是同一個根因：填寫幣別的切換器被改成純文字顯示，
系統改用「有沒有外幣總額」去猜使用者要用哪種幣別填，猜錯就算不出餘額。

### 1. S-04-5／S-04-6 金額欄改成一行

現況：幣別標籤 `display:block` 自己佔一行，輸入框佔一行，兩個欄位共四行。

**改法：幣別標籤與輸入框同一行。**

    ₩ KRW   [        可留空        ]
    $ 台幣   [        可留空        ]

- 幣別標籤固定寬度、靠左，輸入框佔滿剩餘寬度
- **輸入框內的數字字級要小於區塊標題「金額」**。
  理由：表單裡的數字是輸入，不是展示；統計卡那個大數字才是展示。
  輸入框的數字比區塊標題大會搶錯重點。
- 兩列對齊，幣別標籤寬度一致

### 2. S-04-18 與 S-04-21 必須互斥（bug）

現況：付款人 chip 的高亮條件是 `!g.onSpot && g.payer === m.id`，
但**點成員時沒有清掉 `onSpot`**。結果是選了「大家各付各的」之後再點成員，
狀態改了畫面卻不亮，看起來像按不動。

**改法：兩者互斥。**
- 點任一成員 → 自動取消「大家各付各的」（`onSpot = false`）
- 點「大家各付各的」 → 取消成員選取的高亮
- 兩者永遠只有一個是選中狀態

### 3. S-04-31 填寫幣別切換器要加回來（bug 的根因）

**現況：** 各自金額的填寫幣別變成**純文字顯示**（`<p class="hint">$ TWD</p>`），
系統用「有沒有填外幣總額」去**推斷**使用者要用哪種幣別填：

    fillsAreForeign = Number.isFinite(forT)   // 有外幣總額 → 判定為填外幣

這是倒果為因。舊原型（`分帳模型原型.html`，Rozi 定案的版本一）**有兩顆切換按鈕**
讓使用者明確選擇「$ 台幣填 / ₩ KRW 填」。

**改法：把切換器加回來**，回到舊原型的做法：
- 兩顆按鈕明確二選一，預設台幣
- 這正是既有規則 R4「同一筆只能用一種幣別填，不混填」——
  要有切換器才表達得出「用的是哪一種」
- 切換時已填的數字保留、不清空（幣別只是決定基準，不改使用者輸入的值）

### 4. 餘額的計算基準要跟著填寫幣別走（修 bug）

**Rozi 實測到的錯誤畫面：**
台幣總額 2457、填寫幣別 TWD、Rozi 1000、小美 400、小魚未填
→ 小魚顯示 **0**，並提示「補上 ₩ 總額才算得出各自要付多少」。

**兩個都是錯的。** 那筆是用台幣填的，台幣總額就在上面，
餘額 = 2457 − 1400 = **1057**，根本用不到外幣總額。

**根因**：現況的判斷不分填寫幣別，一律觸發「沒有外幣總額就算不出來」：

    if (isEach && forTotalEff == null) {
      if (blanks.length === 0 && sumManual > 0) { forTotalEff = sumManual; forTotalAuto = true; }
      else if (blanks.length > 0 && sumManual > 0) noAutoReason = 'noForeignTotal';  // ← 錯在這
    }

**正確規則：基準跟著第 3 項的填寫幣別走。**

| 填寫幣別 | 餘額基準 | 要不要外幣總額 |
|---|---|---|
| **台幣** | 台幣總額 − Σ已填 | **不需要**。台幣總額空白才算不出來 |
| **外幣** | 外幣總額 − Σ已填 | 需要。全員填完可自動補為 Σ各人；有人沒填且外幣總額空白 → 不猜 |

「不猜」那條**只在填寫幣別為外幣時**成立。理由仍然是：
只有一人填 12000 時把總額補成 12000，那人就佔 100%，會嚴重算錯。
但那個風險**只存在於外幣情境**，台幣情境沒有這個問題。

**判定人數的規則不變**（已定案，本項不得改動）：
- 剩 **1 人**未填 → 該人 = 基準總額 − 已填合計，**唯一解，不標記估計**
- 剩 **2 人以上** → 餘額平分，**全部標記估計**
- 餘數塞進最後一個自動欄

### 4b. 算不出來時的介面表達

當「填外幣 + 外幣總額空白 + 有人沒填」成立而自動均分停用時：
- **「外幣總額」那個欄位本身要有視覺標示**（邊框／底色／狀態標記擇一），
  讓使用者一眼知道要填的是這一格，**不要靠加長灰字解決**
- 各人欄位的 placeholder 顯示 `—` 而非 `0`，
  讓「算不出來」與「均分後就是 0」在視覺上可分辨

### 5. R9 補漏：不強制加總相等（規格先前漏寫）

`分帳模型原型.html` 的 **R9** 在決策表中已定案，但先前**漏未寫進規格檔**，
`規格_金額未定案與幣別.md` 已補上第 2d 節。內容：

各自金額的加總**不必等於總額**，不得以此擋存檔。

| 差額 | 呈現 |
|---|---|
| 0 | 可用視覺標示「剛好」 |
| ≤ 總額的 1% | **淡色提示** |
| > 總額的 1% | **橘色警示** |

三種都可存檔。差額一律**歸付款人**；付款人未參與該筆時規則相同。

註：走自動均分時加總必然等於總額；差額只在**所有人都手動填了、加總卻 ≠ 總額**時出現。

### 6. 逐條重對 R1–R10（上一次只對到 R8，且 R4 判定有誤）

上一次回報「R1–R8 逐條對過，除了 R2 其餘沒有偏離」，但實際上：
- **R4 也偏離了**——「各自金額以該筆幣別統一填寫」的重點是**使用者選哪一種**，
  不是「有顯示幣別」。切換器被改成純文字就是偏離（見第 3 項）
- **R9、R10 根本沒被對照到**（那份原型有十條，不是八條）

**這次要對完 R1–R10 全部十條**，逐條回報「有做到／偏離／怎麼修的」。

### 7. 用那份原型的五個情境當驗收

`分帳模型原型.html` 的 `SCENARIOS` 是 Rozi 當初實際點過、確認過的五個情境，
比另外寫的測試更可靠。**逐一在 S-04 跑通並寫成測試**：

| 情境 | 內容 | 台幣 | 外幣 |
|---|---|---|---|
| ① | 四人吃飯，Ning 刷卡（韓式烤肉，四個人都吃）| 3308 | — |
| ② | 兩人汗蒸幕，Ziyu 付（只有 Ziyu 和 Mei 去）| 1240 | — |
| ③ | **藥妝店一起結帳、買各自的**（Xiu 結帳，**用韓圜填**）| 1035 | 45000 |
| ④ | Mei 個人購物，自己付（只算 Mei 一個人）| 860 | — |
| ⑤ | 大家當場各付各的（機場接送，當場就清了）| 1600 | — |

**情境③是 Rozi 最初提出的那個情境**，必須跑通：
用韓圜填各自金額、外幣總額 45000、台幣總額 1035，
各人台幣依 R5 比例回推（各人台幣 = 1035 × 各人韓圜 ÷ 45000）。

### 5. 停止條件

1. 第 1～4 項做完
2. 金額區塊從四行減為兩行（可自動斷言幣別標籤非 `display:block`）
3. 新增測試：點成員會清掉 `onSpot`、點 `onSpot` 會清掉成員高亮，兩者互斥
4. 新增測試：填寫幣別切換器存在且可切換，切換時已填數字不被清空
5. 新增測試（**Rozi 實測到的錯誤，逐字對照**）：
   台幣總額 2457、填寫幣別 TWD、三人填 1000／400／未填
   → 未填者算出 **1057**、**不**標記估計、**不**顯示任何要求補外幣總額的提示
6. 新增測試（回歸確認，防止再度走鐘）：
   - 填台幣、剩 1 人未填 → 該人 = 台幣總額 − 已填合計，**不**標記估計
   - 填台幣、剩 2 人未填 → 兩人各得餘額一半，**都**標記估計
   - **填台幣、外幣總額空白 → 照常算得出來**（不得觸發 noForeignTotal）
   - 填外幣、全員填完、外幣總額空白 → 自動補為 Σ各人，標「自動」
   - 填外幣、有人沒填、外幣總額空白 → 停用自動值，該欄位帶狀態標示、
     各人 placeholder 為 `—`
7. 既有四組測試沒有因本次改動失敗
8. 橫向滑動測試 320／375／414 全過（金額欄改一行會改變版面，必須重測）
9. R1–R10 **全十條**逐條對照完成並回報結果
10. 五個情境全部跑通並寫成測試，**情境③（用韓圜填、比例回推）為必測**
11. 新增測試：加總 ≠ 總額時不擋存檔；差額 ≤1% 為淡色、>1% 為橘色警示

### 6. 護欄

- 不改 `src/`、不跑 migration、不刪檔
- **第 4 項的「人數判定規則」（1 人唯一解／2 人以上均分）是已定案內容，不得改動**——
  只修「基準要跟著填寫幣別走」這一點
- 第 4b 項只改介面表達，不得改動計算邏輯
- 判斷不了的技術或版面問題回報給 Cowork 端決定，不要丟給 Rozi

---

## #21 帳務規則的測試補強（很短，做完接批三） [已處理]

#20 的實作與數學都正確，**但測試沒守住帳務規則**。

### 1. 問題

五情境測試只驗 `calc()` 的輸出，**沒有驗 `settleTrip()` 的行為**。
情境⑤「大家當場各付各的」斷言的 `c.debts.length === 3`
是那一筆內部的分攤關係，不是結算結果。

後果：`settleTrip()` 裡的 `if (e.onSpot) return;` 若被誤刪，
**這組測試不會失敗**——一筆不該進結算的帳會悄悄被算進去。

R10 是帳務規則，不能只靠人記得。

### 2. 要補的測試（針對 `settleTrip()`，不是 `calc()`）

在同一個行程裡放進多筆消費，然後斷言結算結果：

1. **R10**：加入一筆 `onSpot:true` 的消費前後，
   結算的每人淨額**完全不變**（不是「筆數不變」，是**金額不變**）
2. **twd_pending**：加入一筆台幣總額空白的消費前後，
   結算的每人淨額**完全不變**（R7 的結算面）
3. **Σ 淨額恆為 0**：任何組合下，所有人的淨額相加必須等於 0
   （這是分帳演算法的鐵律，任何改動都不得破壞）
4. **結算恆為台幣**：把幣別切換到外幣後再結算，
   結算結果仍是台幣、金額不變（既有決策）

### 3. 順便改掉誤導的名稱

五情境測試的日誌輸出把 `c.debts.length` 印成「欠款」，
容易被誤讀成結算欠款。改成能分辨的名稱
（例如「單筆分攤關係數」），並在該行加一句註解說明
它不等於結算結果。

### 4. 停止條件

1. 第 2 項四條測試全部加入並通過
2. **反向驗證**：暫時把 `settleTrip()` 的 `if (e.onSpot) return;` 註解掉，
   確認新測試**會失敗**；確認完再改回來。
   （測試要能抓到問題才有價值，這一步要在回報中說明結果）
3. 既有四組測試沒有因本次改動失敗

### 5. 護欄

- 不改 `src/`、不跑 migration、不刪檔
- **只加測試與改名稱，不得改動任何計算邏輯**
- 第 4-2 項的反向驗證做完務必把程式碼改回原狀

---

## #22 批二補修二（做完再接批三） [已處理]

Rozi 看過 #20 後的六項。前五項直接做，第六項是新功能。

### 1. G-09「帶上一趟成員」從 PRD 移除

Rozi 裁示移除。理由：該功能原本只是「複製上一趟的 name/emoji 硬湊」，
而**複製行程（S-03-4）已經會帶成員**，功能重疊。
日後若有需求再加。

- 從 PRD／MVP 功能清單移除該項，標註移除日期與理由
- 原型上若有相關殘留一併清掉

### 2. S-03-9「約」移到金額前面

現況：「約」在總金額**後面**。
**改法：移到金額前面**——「約 NT$76,796」。

理由：中文的「約」是修飾語，位置在被修飾的詞之前。放後面閱讀不順。
S-03-27 每人分擔列的「約」若也在後面，一併調整為前置。

### 3. S-03-21 贊助金額要顯示負數（漏做）

現況：只有「贊助回饋」badge，**金額仍顯示正數**，看不出是負額。

**改法**（決策表 2026-08-31 已定案，本次補做）：
- 贊助列的金額顯示為**負數**
- 用**收款綠**（`--get`）顯示，與一般支出的顏色區隔
- badge 保留

### 4. S-04-18 改名為「當場就清了」（混淆的根因是同名）

**根因**：S-04-11 的分帳型態叫「**各付各的**」，
S-04-18 叫「大家**各付各的**」，**同名不同義**：
- 分帳型態「各付各的」＝ 每個人買自己的，金額不同，**會產生欠款**
- S-04-18 ＝ 當場就結清了，**不進結算**

**改法：**
- S-04-18 改名為「**當場就清了**」，全站一致（含 S-03-20 消費列 badge）
- 視覺上與成員 chip 區隔：用分隔線隔開，形狀或樣式與成員 chip 不同，
  讓人看得出它不是「選一個人」

### 5. S-04-19 贊助改用全站統一的選取樣式（上次漏改）

現況：S-04-18 已改為 chip（描邊／填實），但 **S-04-19 仍是 `selchip` ＋ 打勾 icon**。
#17 第 5 項要求全站統一，這一處漏改。

**改法**：S-04-19 改成與 S-04-18／成員選取相同的樣式（未選描邊、選了填實），
**移除打勾框與 check icon**。全站不得再有第二套選取語彙。

### 6. 結算模式：新增「都轉給同一個人」（Rozi 拍板）

**背景**：淨額算法不變，兩種模式**淨額完全一樣，差別只在轉帳路徑**。

例：淨額 Ning +3,000、Ziyu +500、Xiu −2,000、Mei −1,500

    誰欠誰就轉給誰（現行）        都轉給同一個人（新增）
    Xiu  → Ning 2,000            Xiu  → Ning 2,000
    Mei  → Ning 1,000            Mei  → Ning 1,500
    Mei  → Ziyu    500           Ning → Ziyu    500
    （Mei 要轉兩次給兩個人）      （每個欠錢的人只轉一次）

**6a. 設定位置：只在 S-02b 編輯行程頁，不得有第二個入口。**
（與匯率同一條原則——一個設定只有一個地方，避免層級混淆。）

    這趟怎麼結算？
    ○ 誰欠誰就轉給誰          ← 預設
    ○ 都轉給同一個人  [ 🍋 Ning ▾ ]

- 選第二項時必須選人，下拉**預設帶目前代墊最多的成員**
- **文案不用「家庭／朋友」分類**——朋友出遊也可能有人代墊全部，
  家庭旅遊也可能各付各的。用「錢怎麼轉」描述才準確

**6b. S-05 結算頁：只顯示結果，不提供切換。**
但要有**引導**（不是第二個設定入口）：
若偵測到某成員代墊比例 **> 70%** 而目前是「誰欠誰」模式，
顯示一則提示並提供直接跳去 S-02b 設定的連結。

**6c. 演算法：不改結算引擎的淨額計算。**
`hub` 模式只改**轉帳明細的產生方式**：
- 淨額為負的人 → 全額轉給中心人
- 淨額為正的人（中心人以外）→ 由中心人轉給他
- 中心人自己不出現在「轉給自己」的列
- **Σ 淨額恆為 0 這條鐵律不得破壞**，兩種模式的每人淨額必須完全相同

**6d. 資料庫（寫 migration 檔，不執行）**

    alter table trips add column settlement_mode text not null default 'direct'
      check (settlement_mode in ('direct','hub'));
    alter table trips add column hub_member_id uuid null references trip_members(id);

- `direct` ＝ 誰欠誰就轉給誰（預設，現行行為）
- `hub` ＝ 都轉給同一個人，中心人存在 `hub_member_id`
- **既有行程一律 `direct`，行為不變**
- migration 檔寫好後**停下回報**，由 Cowork 端套用 production

### 6e. 資料庫由 Cowork 端決定並套用，不再問 Rozi

第 6d 的 migration **不需要 Rozi 拍板**。加欄位是可回滾的技術決定，
Rozi 的角色是視覺設計、UX、產品邏輯。
終端機一樣**只寫檔不執行**，由 Cowork 端檢查後套用 production。

### 9. S-04-32 人名與金額要同列

現況：各自金額區的成員名字與輸入框不在同一列。

**改法：同列。** `emoji 名字` 靠左、輸入框靠右，一列一人。

⚠️ **這是 Rozi 第二次提醒同一類問題**（第一次是 S-04-5／S-04-6 金額欄）。
見第 10 項。

### 10. 全站 UX 檢查（本批必做，往後每批都要）

Rozi 已就「標籤與輸入框沒排在同一列」提醒兩次。
`CLAUDE.md` 已新增常設的八條檢查清單，**本批要在全站跑第一次**：

| # | 檢查 | 判準 |
|---|---|---|
| 1 | 標籤與輸入框同列 | 表單欄位的標籤不得自己佔一行（除非真的需要獨立） |
| 2 | 灰字只留兩種 | 只留「不做就不會知道的限制」與「隱私或後果告知」 |
| 3 | 選取語彙單一 | 全站只有一套（未選描邊、選了填實），不得有打勾框 |
| 4 | 當下不需要的不攤開 | 使用比例低或屬進階的區塊預設收合 |
| 5 | icon 風格統一 | 全 SVG、同一組來源、line 與 fill 不混用 |
| 6 | 一個設定一個入口 | 同一件事不得在兩個畫面都能設定 |
| 7 | 需要解釋才懂的互動 | 修互動，不要加說明文字 |
| 8 | 同名不同義 | 兩個功能不得共用同一個名稱 |

**逐項掃過批一與批二全部畫面**，發現幾處就修幾處，
在停點報告逐項標「通過／發現 N 處已修」，並列出修了哪些編號。

### 11. 改動範圍紀律（常設，本批起適用）

`CLAUDE.md` 已新增「改 A 不准順手換 B」。本批起：
- **動 A 的過程中若發現需要動到 B，停下回報，不得順手改**
- 交付時回報「本次動了哪些檔案／哪些區塊」，**超出指令範圍的一律說明原因**
- 不該變的行為若沒有測試守著，**先補測試再改**

起因：同類事故已三次——改灰字時把幣別切換器改成純文字、
做批二時把「一起分」從收合改成攤開、移除 S-02-17 時連帶動到相鄰項目。

### 12. 停止條件

1. 第 1～6、9、10、11 項做完（6d 只寫 migration 檔，不執行）
2. 新增測試：兩種模式的**每人淨額完全相同**，只有轉帳明細不同
3. 新增測試：hub 模式下，**每個淨額為負的成員只出現在一筆轉帳**
4. 新增測試：兩種模式下 **Σ 淨額皆為 0**
5. 新增測試：全站不存在打勾框樣式的選取（S-04-19 已改）
6. 新增測試：贊助列金額為負數且使用收款綠
7. 既有四組測試沒有因本次改動失敗
8. 橫向滑動測試 320／375／414 全過
9. S-04-32 人名與輸入框同列（可自動斷言該列為單一 flex row）
10. 全站 UX 檢查八條逐項跑完並在停點報告列出結果
11. 新增測試：全站無「標籤獨佔一行」的表單欄位（除非白名單註明理由）

### 13. 護欄

- 不改 `src/`、**不執行** migration、不刪檔
- **不得修改淨額的計算方式**，hub 只改轉帳明細的產生
- 第 6a 項**不得**在 S-05 增加設定入口，只能有引導連結

---

## #23 S-00 Google 登入鈕（小修，可併進批三一起做） [已處理]

### 1. 問題

原型的登入鈕用 **`◉` 這個符號**當 icon：

    <span style="font-size:15px">◉</span> 用 Google 帳號繼續

`◉` 是「選中的單選鈕」圖示，所以整顆按鈕看起來像**被選取的狀態**，
而不是一個可以按的登入鈕。這是 #17 換 icon 時漏掉的一處。

註：`src/pages/LoginPage.tsx` **線上版本其實有正確的 `<GoogleIcon />`**，
只有原型偷懶用了符號。

### 2. icon：用 Google 官方四色 G

⚠️ **這一顆不能用 Remix Icon 的通用圖示。**
Google 的品牌規範要求使用官方 logo，**不得改色、變形或重繪**。

**⚠️ 去取得 Google 官方釋出的檔案，不准自己畫。**
Cowork 端這邊的網路擋住了 Google 網域（實測 `developers.google.com`、
`gstatic.com` 皆連不上），所以由終端機取得。依序嘗試，**回報用了哪一個來源**：

1. **Google 官方品牌資源**（首選）：
   `https://developers.google.com/identity/branding-guidelines`
   提供的官方 "G" mark SVG／PNG
2. **Google Identity Services 的資產**：`https://www.gstatic.com/` 下的官方 logo
3. 若上述都取不到 → **停下回報**，由 Rozi 自行從 Google 品牌頁下載後放進 repo

**三種都失敗也不准自己繪製 path**——重繪出來的比例、圓角、色塊分界都會與官方不同，
那正是品牌規範禁止的。

取得後內嵌成 SVG，維持原型單檔、不依賴外部資源的限制。
官方四色為藍 `#4285F4`／綠 `#34A853`／黃 `#FBBC05`／紅 `#EA4335`，
**用來驗證取得的檔案是否正確，不是拿來自己畫的依據**。
- 尺寸與文字基線對齊，logo 與文字之間留適當間距
- **不要套 `currentColor`**——那會把四色 logo 變成單色，違反品牌規範
  （這是全站 icon 規則的**唯一例外**，在程式碼加註解說明原因）

### 3. 文案：「用 Google 繼續」（Rozi 2026-09-04 拍板）

從「用 Google 帳號繼續」縮短為「**用 Google 繼續**」。
理由：搭配彩色 G logo 已經很清楚是 Google 帳號，「帳號」兩字重複了。

用「繼續」而非「登入」是刻意的——首次使用者還沒有帳號，
看到「登入」會遲疑「我沒有帳號啊」。這也對應 Google 官方的 Continue with Google。

**`src/pages/LoginPage.tsx` 的文案一併同步**（實作階段處理，本輪只改原型）。

### 4. 樣式

按鈕是**整頁唯一動作**，要看得出是可按的按鈕，不是被選取的狀態：
- 白底、深色文字（維持現況）
- 不得使用任何與「選取狀態」相同的視覺語彙
- Google logo 在左、文字在右，整體置中

### 7. S-02b-13 結算模式移到 S-02b-7 之後

現況位置太前面。**要先設定完成員，才選得出中心人**——
結算模式依賴成員，卻排在成員設定之前，層級不對。

**改法：S-02b-13 移到 S-02b-7（成員設定）之後。**
編號不變（不重編），只調整版面順序。

⚠️ 這是 Cowork 端的疏漏：#22 只寫「設定位置：只在 S-02b」，
沒指定在 S-02b 的哪一段。往後指令會指定到「排在 S-XX-N 之後」。

### 8. 版位層級規則（常設，本批起適用）

`CLAUDE.md` 已新增「版位層級」一節與全站 UX 檢查第 9 條。要點：

指令若未指定位置，**不得自己隨便放**。依序判斷並在停點報告寫明理由：
1. **依賴優先**：B 需要 A 才有意義 → B 排在 A 之後
2. **必填在前、選填在後**
3. **頻率高的在前**（每次要改的在前，一趟設一次的在後）
4. **同類相鄰**

判斷不了時停下回報，不要猜。

**本批要做的**：把批一到批三所有畫面的設定項順序**照這四條檢查一遍**，
發現層級不對的一併調整，在報告中列出調了哪些、為什麼。

### 5. 停止條件

1. 原型的 S-00-5 不再有 `◉` 或任何文字符號充當 icon
1b. **Google logo 來自官方釋出的檔案**（報告寫明來源網址），非自行繪製
2. 使用 Google 官方四色 G 的 SVG，**未被 `currentColor` 覆寫成單色**
3. 文案為「用 Google 繼續」
4. 既有四組測試沒有因本次改動失敗
5. 新增測試：S-00-5 內含四色 SVG（可斷言 fill 屬性有多個不同色值）、
   且不含 `◉` 等符號字元
6. S-02b-13 排在 S-02b-7 之後
7. 批一至批三所有設定項的順序照四條版位原則檢查完，調整的項目列在報告中

### 6. 護欄

- 不改 `src/`（文案同步留到實作階段）、不跑 migration、不刪檔
- **全站 icon 規則的唯一例外就是這一顆**，不要順手把別處的 icon 也改成彩色

---

## #24 S-05 結算頁整頁重新檢視（併進批三一起做） [已處理]

Rozi 指出 S-05 已與前面幾輪的討論脫節，整頁需要重新對齊。

### 1. 根本問題：只給數字，不給行動

未結算狀態只顯示「可以拿回／要給出」的淨額，**但那不能行動**——
使用者知道自己要付 1,500，卻不知道**要付給誰**。
加上 #22 新增了「都轉給同一個人」模式，轉帳對象會因模式而不同，
更不能只給一個數字。

**先釐清一個容易混淆的區別（不要弄錯）：**
- **統計卡展開**看到的是「每個人**分攤**多少」＝花了多少
- **結算頁**看到的是「每個人該收或該付多少」＝**淨額**

兩者是不同的數字。例：Ning 分攤 20,340、但代墊 50,000 → 淨額「可以拿回 29,660」。
統計卡不會告訴你這件事，所以 **S-05 的預覽有存在價值，不得因為統計卡能展開就移除**。

### 2. S-05-28 改為「完整轉帳預覽」

現況只有淨額，**改為直接顯示轉帳明細**：

    現在的狀況
    還會變 —— 之後記帳會影響這裡

    🐵 Xiu    要給  🍋 Ning    2,000
    🐱 Mei    要給  🍋 Ning    1,500
    🍋 Ning   要給  🐟 Ziyu      500

    ────────────────────
    [ 結算行程 ]
    結算後金額固定，可以逐筆標記付清

- **轉帳明細要跟著行程的結算模式走**（direct／hub，見 #22-6）
- 有未定案的帳時，**金額前加「約」**，與統計卡一致；不要另外加一句提示
- 即時計算、**不寫入 settlements**、不影響「結算行程」流程（此條不變）

**文案改動與理由：**

| 位置 | 原 | 新 | 為什麼 |
|---|---|---|---|
| 標題 | 暫時算一下 | **現在的狀況** | 「暫時算一下」像一個動作；使用者按它不代表行程結束 |
| 說明 | 記帳還在變，按「結算行程」才算數 | **還會變 —— 之後記帳會影響這裡** | 原文暗示「現在的數字不算數」，但**數字其實是準的**，只是之後記帳會改變它 |
| 按鈕下方 | （無）| **結算後金額固定，可以逐筆標記付清** | 說明結算會做什麼，而不是暗示非結算不可 |

**預覽與結算的差別要講清楚**：看得到的東西一樣，**差別在定不定案**。

### 2b. 兩種結算模式的呈現方式必須不同（Rozi 2026-09-05 補充）

⚠️ **不是只有明細內容不同，連組織方式和文案都要不同。**

**`direct`（誰欠誰就轉給誰）——平鋪清單**

    現在的狀況
    還會變 —— 之後記帳會影響這裡

    🐵 Xiu    給  🍋 Ning    2,000
    🐱 Mei    給  🍋 Ning    1,500
    🍋 Ning   給  🐟 Ziyu      500

**`hub`（都轉給同一個人）——分兩段，反映實際金流順序**

    現在的狀況
    還會變 —— 之後記帳會影響這裡

    都跟 🍋 Ning 結算

    大家給 Ning
      🐵 Xiu              2,000
      🐱 Mei              1,500

    Ning 再給
      🐟 Ziyu               500

**為什麼 hub 要分兩段**：那反映真實的金流順序——**先收進來，再撥出去**。
平鋪成一個清單會讓「Ning 給 Ziyu」看起來跟其他列是同一種事，
但它其實是第二階段。分兩段之後，每個人一眼看到自己在哪一段、要做什麼。

**邊界情況：**
- 中心人**只收不撥**（其他人都欠他）→ 只顯示第一段，不留空標題
- 中心人**只撥不收** → 只顯示第二段
- 全員打平 → 兩段都不顯示，顯示「大家剛好打平」
- **中心人自己不出現在任何一段**（不會有「Ning 給 Ning」）

**同樣的規則適用於已結算狀態的轉帳明細（S-05-7）**，
不是只有未結算的預覽要改——兩處用同一套呈現邏輯。

### 3. S-05-1 未結算引導：內容化之後移除

現況是「🧮＋一句說明」的空引導，整頁沒有任何金額。
第 2 項讓未結算態有了真實內容，**這個空引導失去作用，移除**。
編號留在盤點表標刪除線，號碼不回收。

⚠️ 但 **S-05-16 的摺紙動畫與 🧮 的視覺語彙不受影響**，那是結算完成時的 signature。

### 4. S-05-11 人話淨額要顯示對象

已結算狀態的「可以拿回 X／要給出 Y」同樣沒有說對象。
**加上對象**，格式與第 2 項的轉帳明細一致。
若某人的淨額由多筆轉帳組成（direct 模式可能發生），列出全部。

### 5. S-05-12 Excel 正負號提醒：移除（Rozi 2026-09-04 裁示）

「Excel 習慣用負數表示應收，Tripay 相反」是當初為了 Rozi 重謄四趟 Excel 而加的，
屬於她的特殊需求，**一般使用者不知道這句在講什麼**。
四趟重謄已完成，這句的任務結束。

**整條移除**，編號標刪除線。日後對帳需要時，`驗收基準表.md` 裡有寫。

### 6. 四種狀態各自要顯示什麼（重新對齊後的定案）

| 狀態 | 內容 |
|---|---|
| **1 還沒結算** | 轉帳預覽（第 2 項）／結算模式引導 S-05-31（代墊 >70% 時）／結算行程按鈕。**移除 S-05-1 空引導** |
| **2 結算前檢查** | 維持 #22 已定案的檢查層（S-05-4／5／30），不動 |
| **3 已結算未付清** | 進度條 S-05-6／轉帳明細 S-05-7（可標記付清）／人話淨額 S-05-11（**補對象**）／查看計算依據 S-05-10 折疊／重新計算或封存 S-05-15。**移除 S-05-12** |
| **4 全部付清** | 慶祝 S-05-16／回顧卡 S-05-17／分享 CTA S-05-29／建立新行程或封存 S-05-19 |

### 7. 停止條件

1. 第 2～6 項做完
2. 未結算態顯示完整轉帳明細（誰給誰多少），**且跟著行程的結算模式變動**
3. 新增測試：切換 direct／hub 時，未結算預覽的轉帳明細跟著改變，
   但**每人淨額完全相同**
3b. 新增測試：`hub` 模式呈現為**兩段**（大家給 X／X 再給），
   `direct` 為平鋪清單；兩種模式的標題文案不同
3c. 新增測試（邊界）：中心人只收不撥時只出現一段、不留空標題；
   中心人不出現在任何一段；全員打平時顯示「大家剛好打平」
3d. 新增測試：已結算狀態的 S-05-7 轉帳明細與未結算預覽**用同一套呈現邏輯**
4. 新增測試：有未定案的帳時，預覽金額前有「約」；沒有時不出現
5. 原型中不存在 S-05-1 空引導與 S-05-12 Excel 提醒
6. 既有四組測試沒有因本次改動失敗

### 8. 護欄

- 不改 `src/`、不跑 migration、不刪檔
- **不得因為統計卡能展開就移除結算頁的預覽**——那是兩個不同的數字
- 不動 S-05-16 摺紙動畫與狀態 2 的檢查層（#17／#22 已定案）
- 淨額計算方式不得改動

---

## #25 批三回饋修正（7 項＋1 項自查發現） [已處理]

Rozi 看完批三原型後的回饋。本節只改 `Tripay_原型.html` 與 `_盤點_畫面功能.md`，
**不改 `src/`、不跑 migration、不刪檔**。

---

### 25-1　S-02-20 單日行程確認：文案定稿，拿掉佔位符

**現況**：對話框標題寫著「〔文案佔位〕這是單日行程嗎？」，
說明「回程沒填。確認的話就以 8/31 當天來回建立。」，
按鈕「回去填回程」／「是，單日」。

**問題**：
① 佔位符沒清掉，直接出現在畫面上。
② 「單日行程」是系統講法，使用者腦中想的是「只有那一天」。
③ 說明句在解釋系統怎麼運作（「就以 X 當天來回建立」），
　 依既有規則「解釋系統怎麼運作的灰字一律砍」，這句不該留。
④ 「是，單日」是在回答問句，不是在描述動作。

**改成**（`f.start` 是使用者已填的出發日，用既有的 `md()` 格式化）：

| 位置 | 文字 |
|---|---|
| 標題 | `只有 ${md(f.start)} 這一天？` |
| 說明 | `回程還沒填。之後可以再補。` |
| 左鈕（qt） | `回去填回程` |
| 右鈕（主色） | `就這一天` |

說明句留下來的理由：「之後可以再補」是使用者不做就不會知道的選項，
符合灰字兩條規則的第一條；被刪掉的那句是機制解釋，屬於第二類。

---

### 25-2　S-05-16 結算完成動畫：改掉，並只播一次

**現況**：`.anim.fold` 有兩組 `<g>`——`.sheetline`（一張帳單）與 `.plane`（紙飛機）。
CSS 是 `fadeline`（帳單淡出）＋ `lift`（飛機位移旋轉後 opacity 歸零），
兩者都是 `2.6s ease-in-out infinite`。

**Rozi 的回饋是「不夠細致」。拆開來是四個具體問題**：

1. **沒有「摺」這個動作**。兩個圖形是疊著的，一個淡出、另一個滑走，
　 這是交叉淡入淡出，不是摺紙。圖說寫「一疊帳摺起來寄出去」，動畫沒有兌現。
2. **無限迴圈**。這是結算完成的慶祝時刻，只會發生一次；
　 做成 `infinite` 會讓它降格成裝飾，而且「寄出去」這件事每 2.6 秒重演一次。
3. **收尾是消失後硬跳回起點**。`lift` 的 100% 是 `opacity:0`，
　 迴圈重來時整個圖形瞬間閃回原位——「不細致」的手感主要來自這裡。
4. **緩動不對**。紙飛機飛走是加速離開，整段用 `ease-in-out` 會顯得軟。

**改成**（技術做法指定如下，不要自行換成外部動畫函式庫或現成 icon 素材，
理由：這是全站唯一的 signature 圖形，必須是自有資產；
Rozi 原本提供的參考站 `iconanimator.app` 目前是 Deployment Paused 狀態，取不到內容）：

- **播放次數改為 1**，`animation-fill-mode: forwards`，結束後停在最終畫面。
  最終畫面是「紙飛機停在右上、帳單已不在」，不是空白——不得讓圖形整個消失。
- **三拍，總長約 1.4s**：
  - `0 → 0.35s`　帳單上的三條字線（`#9FB0BA` 那三條 path）沿中央摺線收攏：
    `transform: scaleX(0)`，`transform-origin` 設在摺線的 x 位置。
  - `0.30 → 0.80s`　帳單本體沿同一條摺線對摺：`.sheetline` 做 `scaleX(1 → 0.06)`，
    同一個 `transform-origin`。這是讓「摺」真的看得到的關鍵，不要用 opacity 代替。
  - `0.70 → 1.40s`　紙飛機用 `stroke-dasharray` ＋ `stroke-dashoffset` 由 0 描出線條
    （描線需要先量出每條 path 的 `getTotalLength()`，或直接給一個大於路徑長的常數），
    同時 `translate(18px,-20px) rotate(-8deg)`，緩動 `cubic-bezier(.4,0,.9,.4)`（加速離開）。
- **那條紅色虛線**（`#B03A22` 的 `stroke-dasharray:3 3`）目前沒有語意，
  改成飛機飛行軌跡的殘影：跟著飛機一起描出、然後在最後 0.2s 淡到 `opacity:.35` 停住。
- **`prefers-reduced-motion: reduce` 時直接呈現最終畫面**（飛機在最終位置、帳單不在），
  完全不套 animation。現有的 `@media(prefers-reduced-motion:no-preference)` 包法保留。

`.anim.empty-anim`（S-03 空狀態的 drift）**不要動**，那個本來就該是持續的呼吸感。

---

### 25-3　S-02c-12 / S-02c-13：從手機框裡拿掉，規則本身不變

Rozi 的裁示：這兩張說明卡不需要。
「就是開放給使用者自己去做設定，看他們要用 emoji 還是只要直接抓單字。
如果他沒有做設定，我們就是直接判斷、抓命名的第一個字，預設就是這樣。
這只是我們為了使用者體驗做的一個小設計，但不一定每一個功能都要說明。」

**要改的**：把 S-02c-12（「網頁沒辦法主動叫出 emoji 鍵盤」那張卡）
與 S-02c-13（「驗證規則」那張卡）**從 `renderS02c()` 產生的 HTML 裡刪掉**。

**不要改的**：
- **驗證邏輯照舊**：仍然限一個 grapheme、emoji 或文字皆可、超過只取第一個。
  只是不在畫面上說明它。
- **編號照舊**：S-02c-12、S-02c-13 兩個編號保留在右側清單與盤點表，狀態改為
  「稿上不呈現（Rozi 2026-09-05：不需要向使用者說明）」，
  並把原本卡片上的那兩段文字搬進右側清單的說明欄——那是給看稿的人的資訊，
  本來就不該出現在手機框裡（既有規則：原型稿註與產品文案必須分開）。
  **不要重新編號。**

---

### 25-4　S-02c-10 第三層：名字空白不得存檔

Rozi 的裁示：「沒有名字就不應該存檔，這一個我覺得是避免有過多的操作失誤。
所以他如果沒有設定任何的名字，就沒有辦法讓他新增成員。」

**要改的**：

1. **S-02-15「加一個人」的「加進來」鈕**：名字欄為空（trim 後長度 0）時 `disabled`。
   現在的程式是 `if (n) { ...push... }`——空白時按鈕可按但什麼都不會發生，
   使用者得不到任何回饋，這是三種做法裡最差的一種。改成按鈕直接不可按。
   Enter 送出的路徑（`s02newname` 的 keydown）同樣要擋。
   **不要加錯誤訊息文字**，disabled 的按鈕本身就是回饋。
2. **S-02b 編輯行程頁的新增成員**若走同一段程式，一併適用。
3. **任何「成員名字」的輸入框**，清空後都不得存檔。

**S-02c-10 三層 fallback 的呈現要跟著改**：
第三層「名字也空 → 🙂」在產品上已經不可能發生，
所以 `renderS02c()` 的示範清單裡**移除那一列**（示範清單剩三列：
有 emoji ／ 沒 emoji 取首字 ／ ZWJ 組合不被切斷）。

但是——**程式裡的 🙂 fallback 保留不刪**，
理由：正式資料庫裡可能存在早期建立、名字為空的成員列，
拿掉防禦會讓那些列渲染成空白圓圈。
在右側清單的 S-02c-10 說明欄註明：「🙂 是防禦性 fallback，
產品流程已擋住空名字，稿上不再示範」。

---

### 25-5　S-03b 分享選單：砍兩條灰字，改一個標籤

Rozi 提的是疑問不是指令——「這一頁的文案有沒有必要縮短一點？確定大家都會看得完嗎？」
以下是討論後的結論與理由，她可以推翻。

**現況**：三個選項各配一行灰字。

| 標題 | 灰字 |
|---|---|
| 複製結算摘要 | 貼到 LINE 群組，讓大家知道誰付誰 |
| 複製分享連結 | 任何人打開都能看消費明細，不用登入 |
| 預覽分享頁面 | 看看對方收到連結會看到什麼 |

**判斷依據是既有的灰字兩條規則**：欄位下方灰字只留兩種——
①使用者不做就不會知道的限制或選項　②隱私或後果告知。
解釋功能好處、重複標題已說的資訊，一律砍。

- 第 1 條在講好處（「讓大家知道誰付誰」）→ **砍**。
- 第 2 條是隱私後果告知（連結不用登入就看得到明細）→ **留**，這三條裡唯一該留的。
- 第 3 條在解釋「預覽」是什麼意思，標題已經說了 → **砍**。

砍掉第 1 條之後，「複製結算摘要」與「複製分享連結」的差別會變模糊，
所以**把差別做進標題本身，而不是靠灰字補救**（既有規則：需要一句話解釋才懂的互動＝缺少
affordance，修互動不要加說明）：

| 編號 | 標題 | 灰字 |
|---|---|---|
| S-03b-2 | `複製文字摘要` | （無） |
| S-03b-3 | `複製分享連結` | `不用登入就看得到消費明細` |
| S-03b-4 | `預覽分享頁面` | （無） |

「文字摘要」對上「分享連結」，一個是貼得出去的字、一個是打得開的網址，
不用解釋就分得出來。

---

### 25-6　S-03c 刪除對話框：砍提示行，清單留著

**現況五段**：標題／「這會一併刪掉這趟的全部資料，而且救不回來：」／
三項清單（N 筆消費與分帳紀錄・M 位成員・結算結果與分享連結）／
「確定的話，請輸入「刪除」二字：」／輸入框／兩顆鈕／
S-03c-8「沒打對「刪除」二字之前，刪除鈕維持不可按」。

**結論**：要縮，但縮掉的跟 S-03b 不是同一種東西。

- **S-03c-8 整行刪掉**。它在解釋系統怎麼運作，而且解釋的是使用者眼睛已經看得到的事
  （鈕是灰的、輸入框有 placeholder）。編號保留、標為移除，不要重新編號。
- **S-03c-2 縮短**：「這會一併刪掉這趟的全部資料，而且救不回來：」→ `刪掉就救不回來：`
  （後面緊接著就是清單，「這趟的全部資料」由清單本身講得更具體）。
- **S-03c-4 縮短**：「確定的話，請輸入「刪除」二字：」→ `請輸入「刪除」兩個字`
  （「二字」是書面語，Tripay 語氣用「兩個字」）。
- **S-03c-3 的三項清單不要動也不要縮**。這是整個對話框裡唯一給她判斷依據的東西——
  「132 筆消費與分帳紀錄」比任何警語都有嚇阻力。刪除確認頁的閱讀成本應該花在這裡。

---

### 25-7　S-03c-6 / S-03c-7 按鈕寬度：這是 bug，不是樣式選擇

Rozi 的觀察正確，而且比她看到的更嚴重。**Cowork 端已用 headless Chrome 實測**
（`file://` 開啟原型、切到 `store.s03bView='delete'`、量 `getBoundingClientRect()`）：

| 位置 | 「算了，留著」 | 「刪除」 | 同層其他頁的按鈕 |
|---|---|---|---|
| 實測寬度 | **72px** | **28px** | 176.5px / 175.5px |

**根因**：`b(id, html)` 這個標註輔助函式會在每個項目外面包一層 `<div class="b">`。
S-03c 的按鈕列是 `<div style="display:flex;gap:8px">`，
裡面兩個直接子元素是 `div.b`，不是 `button`。
`.ui .btn{width:100%}` 是相對於 `div.b` 算的，而 `div.b` 本身沒有 `flex:1`，
於是收縮成內容寬度，按鈕跟著縮。
其他頁沒事是因為 `.btnrow` 是整列被 `b()` 包一次，按鈕是 flex 容器的直接子元素。

**修法**（兩者擇一，但要全站一致）：
- 首選：`b()` 包整列（`b('S-03c-67', '<div class="btnrow-inline">…兩顆鈕…</div>')`），
  但這樣會讓兩顆鈕共用一個編號，不可行——**所以用第二種**。
- **採用**：新增 CSS `.ui .flexrow > .b{flex:1;min-width:0}`，
  並把 S-03c 的按鈕列容器加上 `class="flexrow"`。
  這樣編號保持一顆鈕一個，版面也對。

**同一個根因還有第二處，Rozi 沒提到但一起修**：
S-02b-3「更換照片」與 S-02b-4「重選色調」，實測各 58px，同層平均應為 180px。
同樣加 `flexrow`。

**把這個檢查寫進 `原型_版面回歸測試.cjs`**（見下方停止條件）。

---

### 25-8　S-03c-7 的紅色：Design System 確實沒有分級，要分開

Rozi 問「那個紅色看起來不夠有警示的效果，是整個 Design System 的系統配色就是這樣嗎？」
**是的，而且比想像中直接**：原型第 105 行

```
--w:#1276C4;--wi:#0F5E9E;--in:#276E45;--out:#B03A22;--dg:#B03A22;
```

`--dg`（destructive，刪除）與 `--out`（警示／提醒，用在「還沒填」、差額、超支）
**是同一個色碼 `#B03A22`**。也就是說「刪掉整趟行程救不回來」
跟「這筆金額還沒填」在視覺上同一個重量級。

**改成**：

- `--out` 維持 `#B03A22`（提醒層級不變，避免動到已定案的「還沒填」呈現）。
- `--dg` 改為 **`#9B1B14`**——比 `--out` 更深、更飽和一階，
  在日記精裝的低彩度環境裡不會刺眼，但明顯重於提醒。
- 加一項檢查：白字對 `--dg` 的對比度需 ≥ 4.5:1（`#9B1B14` 約 7.7:1，通過）。
- **`--dg` 的使用範圍限縮**：只用於「按下去會刪掉資料且無法還原」的按鈕
  （S-03c-7 刪除、S-07-8 登出對話框的登出鈕）。
  現在用 `var(--dg)` 當文字色的地方（`.delrow`、S-07-6 登出鈕文字）一併換到新色碼即可，
  不需要改用法。

修好 25-7 的寬度之後，刪除鈕會變成滿版填色的深紅塊，
警示強度主要來自這個——顏色只是其中一半。

---

### 25-9　S-07：「批四」是內部術語外洩

Rozi：「這一頁的「批四」是不是文字不應該這樣子寫？…目前就只有我看得懂是什麼意思。
這一個產品到時候是要給不同的人使用，如果只這樣子寫的話，我覺得對方會看不懂。」

**現況**：S-07-4「我的資料」區塊有三列——通訊錄、卡片管理、帳單週期，
右側灰字都寫「批四」。「批四」是這次原型分批交付的內部批號，使用者看不懂。

**改成**：

1. 手機框裡三處「批四」→ **`即將推出`**。
2. **在右側清單的 S-07-4 說明欄註明**：
   「這三列屬於 Phase 2 副卡記帳。**Phase 1 上線時整段不渲染**——
   還沒做的功能不該在設定頁佔位。稿上保留是為了標示 Phase 2 的位置。」
3. **順手全站檢查**：手機框（`.ui` 內）不得出現「批一／批二／批三／批四」字樣。
   右側標註清單與稿頭的批次標記不受此限。

---

### 護欄：這一節不准動的東西

- **不改 `src/`、不跑 migration、不刪任何檔案。**
- **不要重新編號。** S-02c-12／13、S-03c-8 都是保留編號、改狀態。
- **驗證邏輯不動**：一個 grapheme、emoji 或文字皆可（25-3 只拿掉畫面上的說明）。
- **`.anim.empty-anim` 不動**（25-2 只改 `.anim.fold`）。
- **`--out` 的色碼與所有既有用法不動**（25-8 只動 `--dg`）。
- **S-03c-3 的三項清單不動、不縮**（25-6）。
- **S-05 結算頁的轉帳預覽、兩種結算模式的呈現、`transferView()` 共用結構一律不動**。
- **淨額計算方式不動。**
- 動 A 時若發現需要動 B，**停下回報，不得順手改**。

---

### 停止條件（全部要機器判斷得出來，人工目測的不算）

在 `原型_版面回歸測試.cjs`（或新增 `原型_批三回饋測試.cjs`）加入以下斷言，
並讓既有五組測試維持全綠（批一 60／批二 132／批三 54／輸入回歸 34／版面 18）：

1. `Tripay_原型.html` 全文搜尋 **`佔位`、`〔`、`〕` 出現次數為 0**。
2. `.ui` 範圍內的 `textContent` **不含「批一」「批二」「批三」「批四」**
   （在 320／375／414 三個寬度、且逐一渲染 S-00～S-07 各狀態後檢查）。
3. **flex 錯位全站掃描**：對每個 `display:flex` 的容器，
   若其直接子元素中有 `.b` 且該 `.b` 內含 `.btn`，
   則該 `.b` 的實際寬度不得小於「容器寬度 ÷ 子元素數」的 70%。
   **這一條全站掃描結果必須為 0 筆。**
   （Cowork 端修改前實測為 4 筆：S-02b-3、S-02b-4、S-03c-6、S-03c-7。）
4. `getComputedStyle` 取 `--dg` 與 `--out`，**兩者不得相等**；
   `--dg` 解析後的相對亮度低於 `--out`。
5. 白色 `#FFFFFF` 對 `--dg` 的對比度 **≥ 4.5**（自己算，不要用外部套件）。
6. S-02-15 展開後，名字欄為空字串（含只有空白）時，
   `#s02adddo` 的 `disabled` 為 `true`；輸入任一非空字元後為 `false`。
   對 `#s02newname` 送 Enter 時成員數不變。
7. `renderS02c()` 產生的 HTML 中，
   **不含字串「emoji 鍵盤」「驗證規則」「grapheme」**（說明卡已移除）；
   且示範列數為 **3**（原本 4，移除「名字也空」那列）。
8. `.anim.fold` 的 `.sheetline` 與 `.plane` 的 `animation-iteration-count`
   **皆為 `1`**，且 `animation-fill-mode` 含 `forwards`。
9. 模擬 `prefers-reduced-motion: reduce`（`page.emulateMediaFeatures`）時，
   `.anim.fold` 底下所有元素的 `animation-name` 皆為 `none`。
10. 動畫播完（等待 2s）後，`.plane` 的 `opacity` **> 0.9**——
    確認最終畫面不是空的。
11. `renderS03b()` 的分享視圖中，`.shopt .s`（灰字）**只出現 1 次**，
    且其文字為「不用登入就看得到消費明細」。
12. 刪除視圖中**不含字串「維持不可按」**；`.dlg` 內的清單 `<li>` 仍為 **3 項**。
13. 三個寬度下，**沒有任何畫面出現橫向捲動**（既有規則，照舊跑）。
14. 原型項目總數與 `_盤點_畫面功能.md` 逐一對得起來，**無孤兒、無缺漏**。

### 交付後要更新的檔案

- `_盤點_畫面功能.md`：S-02-20、S-02c-10／12／13、S-03b-2～4、S-03c-2／4／6／7／8、
  S-02b-3／4、S-07-4 的「改法」欄與狀態。
- 回報寫進 `_停點.md`（覆寫、標時間、≤50 行）。
- 回報中要明講：`--dg` 換色後有哪些畫面的顏色跟著變了。

---

## #26 實作前的現況盤點（唯讀，不改任何程式） [已處理]

**這一節不寫任何功能，只產出一份盤點檔。**
目的：接下來的 Phase 1＋1.5 實作節次要一次跑完、中途不打斷 Rozi，
所以必須先知道「哪些其實已經做好了」，避免重做已存在的功能。

已知至少有四處資訊過期，這就是要盤點的原因：
`_實作批次工作清單.md` 裡列為待實作的「複製行程／刪除行程」，
在 `ExpenseListPage.tsx` 第 319 行與第 558 行附近看起來線上已經有了；
「切換成員視角」已被 Rozi 取消（不做）；
「hub 結算顯示」已經在原型裡從「只顯示」升級成完整功能；
`P9_schema變更提案.md` 的前置 migration 早就套用過了。

### 硬性限制

- **完全不改 `src/`、不改 `supabase/`、不跑 migration、不部署。**
- 對正式資料庫**只能唯讀查詢**（SELECT／查 schema），不得寫入。
- 唯一允許新增的檔案是 `_盤點_實作缺口.md`。
- 這一節跑完後 `git status` 除了那一個新檔案之外**必須是乾淨的**。

### 要產出什麼：`_盤點_實作缺口.md`

**第一段：逐項對照表。**
以 `Tripay_原型.html` 的編號清單為母體（目前 206 項），
每一項一列，欄位如下：

| 欄位 | 內容 |
|---|---|
| 編號 | S-XX-N |
| 名稱 | 沿用原型清單的名稱 |
| 分類 | `已如此`／`要改`／`全新`／`要移除` 四選一 |
| 現況位置 | `src/檔名.tsx:行號`，或 `不存在` |
| 差在哪 | 只有 `要改` 需要填，一句話講現況與稿上的差別 |

四類的定義：

- **已如此**：現在的程式行為就是稿上的樣子，實作時不用動。
- **要改**：功能存在但行為／文案／版面與稿不同。
- **全新**：`src/` 裡沒有這個東西。
- **要移除**：`src/` 裡有，但稿上已經刪掉了（例如 `EmojiPicker.tsx` 整個元件）。

已經有 `_稿碼對照.md` 與 `_盤點_畫面功能.md` 兩份既有對照資料，
**先讀它們再去讀碼**，不要從零開始比對；
但兩份都可能過期，**以實際讀到的 `src/` 程式為準**，
發現既有對照表寫錯的地方，在盤點檔最後開一段「既有對照表的錯誤」列出來。

**第二段：schema 缺口。**
列出原型上需要、但正式資料庫目前沒有的欄位或表。
每一列要寫：需要它的原型編號、欄位名、型別、
以及「加了會不會影響既有資料」（加欄位／加索引不影響，就寫「不影響」）。
**這一段只列清單，不要寫 migration 檔，也不要套用。**
已套用的 migration 是 001～010，其中 009（`trip_members.emoji` 預設空字串）
與 010（`trips.settlement_mode` ＋ `hub_member_id`）是最近才套上去的。

**第三段：`_實作批次工作清單.md` 的回填。**
逐條檢查該檔裡「待實作」的每一項，標成三種狀態之一：
`其實已完成`（附 `src/檔名:行號`）／`仍待做`／`已作廢`（附作廢原因）。
已知會落在 `已作廢` 的至少有：切換成員視角（Rozi 取消）、
成員分佈 chips（Rozi 裁示不做）、匯入 UI、G-04 OG 圖。
**這一段只是在盤點檔裡記錄結論，不要去改 `_實作批次工作清單.md` 本身。**

**第四段：`Tripay_問題優先序清單.md` 的複驗。**
逐條標 `已修`（附行號）／`仍在`／`已不適用`。

**第五段：分類統計。**
四類各幾項、schema 缺口幾項、`仍待做` 幾項。
這一段要放在檔案最前面（讀的人先看到總量）。

### 停止條件（機器判斷）

1. `_盤點_實作缺口.md` 存在。
2. 第一段的資料列數 **等於原型編號清單的項目數**（自己從 `Tripay_原型.html`
   的 `INDEX` 物件算出母體數量，不要用寫死的 206）。
3. 第一段每一列的「分類」欄都是四個值之一，**沒有空白、沒有第五種值**。
4. 第一段每一列的「現況位置」欄**非空**（不是 `src/...:行號` 就是 `不存在`）。
5. 分類為 `要改` 的列，「差在哪」欄**非空**。
6. 第五段的統計數字與第一段實際列數**加總相符**。
7. `git status --porcelain` 的輸出**只有 `_盤點_實作缺口.md` 一行**。
8. 既有五組測試維持全綠（批一／批二／批三／輸入回歸／版面）——
   這一節不該讓任何測試變色，若變色代表誤動了東西。

### 回報

寫進 `_停點.md`（覆寫、標時間、≤50 行）。
回報裡**只要五段的統計數字＋最意外的三項發現**，不要把對照表貼進來。

---

## #27 承諾了產品沒有的東西（3 項）＋ 全站 icon 改為線性系統 [已處理]

> **這一節優先於 #26。** 如果 #26 還沒處理，先做完這一節再做 #26。
> 原因：Rozi 接下來要一次檢視批一～批三，icon 換過會影響每一個畫面，
> 應該在她檢視之前就位；#26 是唯讀盤點，晚做不影響任何人。

Rozi 檢視批三時提出的三項。前兩項有一個共同的毛病：
**畫面上承諾了產品其實沒有的東西。**

---

### 27-1　S-06-12 / S-06-13：沒有 App 就不要說「下載」

**現況**：分享頁（旅伴用連結打開的那一頁）頁尾有 G-06 區塊，
文案「想自己記帳？下載 Tripay」，旁邊有一顆 S-06-13「安裝」鈕。

**問題**：

1. **沒有 App 可以下載。** Tripay 是網頁，目前只有網址，沒有上架任何商店。
   「下載 Tripay」對點進來的旅伴是一句不成立的話。
2. **「安裝」鈕在 iPhone 上永遠不會出現。** 它綁在瀏覽器的
   `beforeinstallprompt` 事件上，**iOS Safari 不支援這個事件**。
   Tripay 的使用者以 iPhone 為主，等於這顆鈕大多數人根本看不到，
   卻要為它維護一條分支。而且「安裝」跟「下載 App」是兩件不同的事，
   並排放在一起只會讓人更不懂。

**改法**：

- **S-06-13「安裝」鈕整顆移除。** 編號保留、標為移除，不要重新編號。
  加到主畫面的引導留到真的要推 PWA 時再做，不在 Phase 1。
- **S-06-12 保留，但文案改成誠實的說法。** 這裡是 Phase 1 唯一的獲客點——
  旅伴點連結進來、看到帳算得清清楚楚，是他最可能想自己用一次的時刻，
  整塊拿掉等於把這個時機丟掉。所以留，只是不要說謊：

  | 位置 | 原文 | 改成 |
  |---|---|---|
  | 標題 | 想自己記帳？下載 Tripay | `這趟帳是用 Tripay 記的` |
  | 動作 | （複製連結） | `開一趟自己的` → 連到首頁網址 |

  「開一趟自己的」用的是產品自己的詞（一趟＝一個行程），
  而且描述的是按下去真的會發生的事：開一個新行程。

**Rozi 原話**：「這個構想很好，但現在沒有 App，目前就只有網站的網址連結才對。
所以我覺得這一部分其實沒有必要現在就放上去呢？」
——她傾向整塊拿掉。這裡選擇「留 CTA、殺安裝鈕、改文案」，
**推翻方式**：把 S-06-12 一起標為移除即可，不影響其他項目。

---

### 27-2　S-07-5 深色模式：查證結果是這個功能不存在

**先釐清一個編號**：S-07-5 是設定頁的「顯示」區塊，
底下目前只有一列「深色模式／跟隨系統」。它跟上架商店沒有關係。
Rozi 描述的「只是對應網站的網址連結」講的是 S-06-13 那顆安裝鈕，已在 27-1 處理。

**但 S-07-5 本身查出了另一個問題**。Cowork 端已對 `src/` 全文查證：

- `src/` 底下**沒有任何深色模式的樣式**（搜尋 `dark`、`prefers-color-scheme` 皆無結果）。
- `tailwind.config` **沒有設定 `darkMode`**。

也就是說這一列右邊寫著「跟隨系統」、右側還有一個 `›` 表示點得進去，
但**點進去後面什麼都沒有，系統也不會跟隨任何東西**。
這跟 S-06-12 是同一個毛病：畫面承諾了產品沒有的東西。

**改法**：**S-07-5 整段移除**（連「顯示」小標一起）。編號保留、標為移除。
深色模式不在 Phase 1 範圍，等真的要做的時候再把這一段加回來。
移除後 S-07 的內容是：登入帳號 → 我的資料（三列，見 #25-9，Phase 1 上線時不渲染）→ 登出。
**Phase 1 上線時 S-07 實際只剩帳號卡與登出**——
如果盤點後認為這樣的設定頁太空，在 `_停點.md` 提出來，不要自己加東西補位。

---

### 27-3　全站 icon 改為線性系統（SF Symbols 風格，但不能用 SF Symbols）

Rozi：「全站的功能 icon 我目前看起來還是略微粗糙…
我想要 icon 在使用的時候可以比較接近蘋果 iOS 的風格。」

#### 先講一個授權事實（Cowork 端已查證 Apple 官方授權條款）

**SF Symbols 與 SF 字體不能用在 Tripay。** Apple 的授權明文限制：
字體與符號僅授權用於「執行在 Apple 作業系統上的軟體」，
且**不得修改、不得內嵌到軟體產品中**。Tripay 是跑在瀏覽器裡的網頁，
不在授權範圍內。所以**不得下載、內嵌或轉描任何 SF Symbols 檔案**。

能做的是**做出接近的風格**——風格本身不受著作權保護，具體的字形檔案才受保護。

#### 為什麼現在看起來粗糙（已讀碼查證，不是印象）

原型的 `ICON` 物件定義了 **15** 個鍵，但 `filter` 與 `money` **全站沒有任何地方用到**，實際在用的是 **13** 個，問題有四層：

1. **全部是實心單一路徑**（`ic()` 用 `fill="currentColor"`，每個 icon 一條 `<path>`）。
   iOS 的系統圖示絕大多數是**等寬線條**，不是實心色塊。
   實心圖形縮到 15～18px 會糊成一團，這是「粗糙感」的主因。
2. **兩套不同來源混在一起**。`back` / `next` / `down` / `up` 是手寫的實心三角形
   （`M8 12L14 6V18L8 12Z` 這種四點封閉路徑），
   其餘 `edit` / `copy` / `settings` / `del` / `close` / `money` 明顯是圖示庫的路徑。
   同一組 icon 裡並存兩種造形邏輯，比單獨哪一個難看都更傷。
3. **箭頭方向錯了**。iOS 用的是細的 chevron（兩筆組成的 `›`），不是實心三角形。
   實心三角在 iOS 上的語意是「播放」或「排序」，不是「返回／進入」。
4. **沒有視覺尺寸調整**。全部以 24 格繪製、卻在 15～18px 之間任意縮放，
   線的粗細與細節密度每個 icon 都不一樣。

#### 要做的

**第一步：改成線性系統。**
`ic()` 改為輸出 `fill="none" stroke="currentColor" stroke-linecap="round"
stroke-linejoin="round"`，`stroke-width` 全站一致（建議 1.7，在 24 格上）。
實際在用的 13 個 icon 都要改成線稿路徑，**不得留下任何 `fill="currentColor"` 的圖示**。

**第二步：四個方向鍵改成 chevron。**
`back` / `next` / `down` / `up` 改成**開放路徑的兩筆折線**（例如 `next` 是
`M9 5 L16 12 L9 19`），不要封閉、不要填色。

**第三步：來源必須授權乾淨。**
造形基底可以用允許商用且允許修改的開源圖示集
（例如 MIT 或 ISC 授權者），或自己重畫。
**新增一份 `_icon授權.md`**，逐一記錄每個 icon 的來源與授權條款；
不確定授權的一律自己重畫，不要放進來。

**第四步：做一條對照，讓 Rozi 一眼選定。**
在原型的「標註模式」裡加一個獨立區塊（放在最上面的批次說明底下，
不要塞進任何一個畫面裡），並排呈現 **2～3 組候選**，
每組都要包含實際在用的 13 個 icon、**以實際使用尺寸渲染**（17px 與 20px 各一排），
每組下方標明來源與授權。組與組之間只能差在造形，
`stroke-width`、尺寸、顏色都要相同，否則比較不出來。

這一步是 Rozi 的專業範圍（視覺設計），她看一眼就能定案，
**不要自己挑一組就套下去、也不要在對話裡用文字描述差別**。

---

### 護欄：這一節不准動的東西

- **不改 `src/`、不跑 migration、不刪任何檔案**（新增 `_icon授權.md` 除外）。
- **不要重新編號。** S-06-13、S-07-5 都是保留編號、改狀態為移除。
- **不得下載、內嵌或轉描 SF Symbols、SF Pro、SF Compact、SF Mono 的任何檔案。**
- **只改 icon 的造形與繪製方式，不改任何 icon 的用途與位置**
  （哪個畫面用哪個 icon 維持原樣）。
- #25 已定案的內容（S-02-20 文案、S-05-16 動畫、S-02c 說明卡移除、
  成員名字必填、S-03b／S-03c 文案、`flexrow` 修法、`--dg` 換色、「即將推出」）
  一律不得回退。
- 動 A 時若發現需要動 B，**停下回報，不得順手改**。

---

### 停止條件（機器判斷）

1. `Tripay_原型.html` 全文中，`ic()` 產生的 SVG **不含 `fill="currentColor"`**；
   且含 `stroke="currentColor"`、`stroke-linecap="round"`、`stroke-linejoin="round"`。
2. `ICON` 物件的每一個值都**不是實心封閉造形**——
   具體判準：`back`、`next`、`down`、`up` 四個路徑字串**不含 `Z` 或 `z`**。
3. 全站搜尋 **`SF Pro`、`SF Compact`、`SF Mono`、`SFSymbols`、`sfsymbols`
   出現次數皆為 0**。
4. `_icon授權.md` 存在，且**每一個 `ICON` 的鍵名都在該檔中出現至少一次**。
5. 對照區塊存在，且每一組候選的 icon 數量**等於 `ICON` 的鍵數**（清掉 filter／money 之後應為 13）。
6. `.ui` 範圍內**找不到**字串「下載 Tripay」與「安裝」。
7. `renderS07()` 產生的 HTML **不含字串「深色模式」與「顯示」小標**。
8. 三個寬度（320／375／414）**沒有任何畫面出現橫向捲動**。
9. #25 加入的所有斷言**維持通過**（icon 換線稿不得讓任何一條變紅）。
10. 既有五組測試全綠。
11. 原型項目總數與 `_盤點_畫面功能.md` 逐一對得起來，**無孤兒、無缺漏**。

### 交付後要更新的檔案

- `_盤點_畫面功能.md`：S-06-12、S-06-13、S-07-5 的改法欄與狀態。
- 回報寫進 `_停點.md`（覆寫、標時間、≤50 行），
  並在回報中**明列對照區塊有哪幾組候選、各自的來源與授權**。

---

### 27-4　Cowork 端複驗 #25 的結果（追加三項，一併做完）

**#25 的 12 條停止條件，Cowork 端用 headless Chrome 全部重跑過，12 條全過。**
（佔位符清空、`.ui` 內無批次字樣、flex 錯位掃描 0 筆、`--dg`≠`--out` 且白字對比 8.2、
名字空白時「加進來」為 disabled、S-02c 說明卡已移除、
摺紙動畫四組皆 `1` 次＋`forwards`、reduced-motion 下無 animation、
播完 2.2 秒後 `.plane` opacity 仍為 1、S-03b 灰字剩 1 條、S-03c 提示行已刪且清單 3 項。）
摺紙動畫的重做特別好——把軌跡 `.trail` 放在 `.plane` 之外，飛機飛走後殘影留在原地，
這是原指令沒寫、但方向正確的判斷。

以下三項是複驗時**額外看出來**的，一併修。

#### 27-4a　標註徽章蓋住內容的第一行（全站通病，優先修）

`b()` 產生的 `<span class="bdg">` 是 `position:absolute; top:0; left:0; z-index:40`，
**直接壓在區塊左上角的內容上**。凡是區塊的第一行是靠左起頭的文字，開頭幾個字就看不見。

實際被蓋住的（截圖確認，不是推測）：

| 編號 | 真正的文字 | 標註模式下看到的 |
|---|---|---|
| S-03c-2 | 刪掉就救不回來： | 　　救不回來： |
| S-03c-3 | 8 筆消費與分帳紀錄 | 　　消費與分帳紀錄 |
| S-03c-4 | 請輸入「刪除」兩個字 | 　　「刪除」兩個字 |
| S-05-18 | 誰付給誰 | 　　給誰 |
| S-07-2 | 登入帳號 | 　　帳號 |
| S-07-4 | 我的資料 | 　　資料 |
| S-07-5 | 顯示 | （幾乎全被蓋住） |

**這件事會直接妨礙 Rozi 檢視**——她看到的字跟產品上的字不一樣，
而她這幾輪回饋大量集中在文案，等於在讀被截斷的句子做判斷。

**要求**（做法自己決定，但必須同時滿足這三條）：

1. **標註模式（`html.anno`）下，徽章不得覆蓋任何文字。**
2. **操作模式（`html:not(.anno)`）的版面完全不受影響**——
   所有元素的位置與尺寸必須與現在完全相同。標註是輔助層，不能改變被標註的東西。
3. 徽章仍要能一眼對應到它標的是哪一個區塊（不能移到離區塊很遠的地方）。

既有的 `.rt`（徽章靠右）是個別修補，不要靠它逐一處理——會漏，而且靠右也可能蓋到金額。
建議往「徽章移到區塊上緣之外、只在標註模式撐出那段空間」的方向做，但由你決定。

#### 27-4b　S-05-17 回顧卡的貨幣符號放錯位置

第三欄現在是：數字 `50,000`（`money()` 的 `$ ` 被 `.replace('$ ','')` 拿掉了），
標籤寫 `最大手筆 $`。所以畫面上讀起來是「50,000」配「最大手筆 $」，
**錢字符號跑到標籤尾巴**，而數字本身沒有符號。

前兩欄的結構是「數字＋單位（天／筆）」配標籤（出遊／共記了），第三欄卻不是。

**改成**：數字欄顯示 `$ 50,000`（沿用 `money()` 原本的輸出，不要 replace），
單位欄留空，標籤欄改為 `最大手筆`。三欄結構就一致了。

註：`_實作批次工作清單.md` 第 8 項「G-08 最大手筆標外幣符號、值是台幣」講的就是這裡，
可以一併結案——結算恆為台幣，所以 `$` 是對的，錯的只有位置。

#### 27-4c　登出鈕誤用了刪除色（這是 Cowork 指令寫錯造成的）

`S-07-6` 的登出鈕是 `style="color:var(--dg)"`。
#25-8 的指令寫著「現在用 `var(--dg)` 當文字色的地方（`.delrow`、S-07-6 登出鈕文字）
一併換到新色碼即可」——**這句話是錯的，是 Cowork 端的疏漏**：

`_盤點_畫面功能.md` 的 S-07-6 早就定調「**登出可復原（再登入即可）→ 用描邊不用實心填底，
與刪除區隔**」。而 #25-8 同時定調 `--dg` 只用於「按下去會刪掉資料且無法還原」。
兩條放在一起，登出就不該是 `--dg`。照 #25-8 做等於把登出染成最重的刪除紅。

**改成**：S-07-6 登出鈕的文字色改為 `var(--md)`（中性），維持 `.btn qt` 的描邊樣式。
`.delrow`（S-03-25 刪除這趟行程、S-04-23 刪除這筆）**維持 `--dg` 不變**，那兩個確實是刪除。

---

### 停止條件（27-4 追加）

12. 標註模式下，對每一個 `.bdg`：取其 `getBoundingClientRect()`，
    與**同一個 `.b` 內任何含文字的元素**的矩形**不得相交**。全站 0 筆。
13. 操作模式（移除 `html.anno`）下，逐一渲染 S-00～S-07 各狀態，
    每個 `.b` 的 `getBoundingClientRect()` **與修改前完全相同**
    （做法：先在修改前把各值輸出成基準檔，修改後比對；容差 0）。
14. `renderS05()` 在 `store.s05='done'` 時，
    回顧卡第三欄的數字文字**以 `$` 開頭**，標籤文字**等於 `最大手筆`**（不含 `$`）。
15. `renderS07()` 產生的 HTML 中，**不含 `var(--dg)`**。
16. 全站搜尋 `.delrow` 的 CSS **仍為 `color:var(--dg)`**（這一項不准被順手改掉）。
17. #25 的 12 條停止條件**全部維持通過**。

---

### 27-5　清掉沒用到的 icon ＋ 接受 Rozi 自備的 SVG

#### 27-5a　`filter` 與 `money` 是死條目

`ICON` 物件定義了 15 個鍵，但 `filter` 與 `money` **全站沒有任何 `ic()` 呼叫用到**
（已用 `grep -o "ic([^)]*)"` 逐一比對確認）。
**兩個都刪掉**，`ICON` 剩 13 個鍵。
往後新增 icon 才加鍵，不要先放著備用——沒人用的 icon 沒有人會維護，
換風格時它會變成唯一沒被換到的那一個。

#### 27-5b　Rozi 自備 icon 的接手方式

已在專案資料夾建立 `icons_自備/`，裡面有一份 `請看這裡.md` 說明檔案命名規則。
Rozi 如果對候選組都不滿意，會把 SVG 放進這個資料夾，**檔名等於 `ICON` 的鍵名**
（`back.svg`、`next.svg`、…、`close.svg`），只放她要換的，沒放的維持原樣。

**每次交付前都要檢查這個資料夾。** 有檔案就照下列規則接手：

1. **逐檔正規化**，不要原樣貼進去：
   - 取出路徑，換算到 24×24 的 `viewBox`（來源不是 24 格就等比換算，
     並置中對齊，不要拉伸）。
   - 移除所有 `fill`／`stroke` 的具體色值、`width`／`height` 屬性、
     `style` 屬性、`id`、`class`、註解、編輯器產生的 metadata。
   - 統一成全站的線稿參數（`fill="none"`、`stroke="currentColor"`、
     一致的 `stroke-width`、圓端圓角）。
   - 若來源是**填色圖示**（沒有 stroke，只有實心路徑），**不要自動轉線稿**——
     轉出來一定歪。改為：保留該檔為填色，但在 `_停點.md` 明講
     「`<檔名>` 是填色圖示，跟其餘 12 個線稿混在一起會不一致，
     建議 Rozi 換一個線稿版本」，**不要自己找一個替代品塞進去**。
2. **拒收的情況**（不要嘗試修，回報請 Rozi 重給）：
   含漸層、遮罩、`<image>` 內嵌點陣圖、`<text>`、外部參照、多色結構。
   回報時逐檔講明是哪一項。
3. **檔名不在 13 個鍵名之內的檔案一律忽略**，並在回報中列出被忽略的檔名。
4. **授權**：`icons_自備/請看這裡.md` 底部有一行來源欄。
   如果 Rozi 放了檔案但來源欄還是空的，**照樣做進去**（不要卡住她），
   但在 `_停點.md` 開一行提醒「`icons_自備/` 的來源欄還沒填，上線前要補」。
   這是提醒，不是阻擋。
5. 自備的 icon **同樣要進 `_icon授權.md`**，來源欄寫 Rozi 提供＋她填的來源。

**這一輪（#27）跑的時候 `icons_自備/` 大概還是空的**——
Rozi 要先看過候選組才會決定要不要自備。
所以這一節的行為是：**資料夾空的就跳過，只做 27-5a**，並在回報中寫明「`icons_自備/` 為空」。

#### 停止條件（27-5 追加）

18. `ICON` 的鍵數**等於 13**，且**不含 `filter`、`money`**。
19. 對 `ICON` 的每一個鍵 `k`，全站**至少有一處** `ic(` 呼叫會用到它
    （含 `ic(cond ? 'up' : 'down', n)` 這種動態寫法，
    做法：把原始碼裡所有 `ic(` 的參數字串抓出來比對，不要只找 `ic('k'`）。
20. `icons_自備/` 若有 `.svg`：每一個被採用的檔案，
    其鍵名都必須出現在 `_icon授權.md` 中。

---

### 27-6　#26 盤點之後的三件事

> 關於執行順序：#27 的開頭寫「優先於 #26」，但終端機先做了 #26，**判斷是對的**。
> **#26 的停止條件第 7 條要求 `git status --porcelain` 只有盤點檔一行**，
> 而 #27 會改原型檔——兩節的護欄互斥，先做會改檔的那個，#26 就永遠過不了。
> 這是 Cowork 端寫指令的疏漏：用散文寫優先序，卻同時下了一條互斥的機器條件，
> 機器條件贏是對的。往後唯讀節次的 git 斷言會改成
> 「`src/` 與 `supabase/` 沒有變動」，不再要求整個工作區乾淨。

#### 27-6a　#27 改完後要回頭同步盤點檔

`_盤點_實作缺口.md` 是照 **#27 之前**的原型盤的。
#27 會動到 S-06-12、S-06-13、S-07-5、S-05-17、S-07-6、S-04-3 與全站 icon，
這幾項在盤點檔第一段的「分類／現況位置／差在哪」會過期。

**#27 做完後，回頭更新盤點檔中受影響的列**，並重算第五段的分類統計。
不要重做整份盤點，只改受影響的列。

#### 27-6b　S-04-3 類別 emoji：裁示為「可點，用就地編輯」

盤點查出一組互相打架的裁示：`_實作批次工作清單.md` 第 13 項要
「S-04-3 的 emoji 改成可點，**複用既有的 EmojiPicker**」，
但同一份檔案的 v5 第二批裁示**刪掉 `EmojiPicker.tsx`**。兩條不可能同時成立。

**裁示：S-04-3 改成可點，做法用「就地編輯」，不重建任何選擇器。**

- 點一下類別 emoji → 變成單字元輸入框並自動聚焦 → 輸入後自動存檔收合，沒有確認鈕。
  **與 S-02c-11 成員識別完全同一套互動**，不要做第二種寫法。
- 驗證同樣是「限一個 grapheme，emoji 或文字皆可」。
- **手動指定優先於自動推斷**：使用者一旦自己改過，之後再改標題**不得**把它覆蓋回去。
  未曾手動改過的，維持依標題自動推斷（餐→🍜 交通→🚌 住→🏨 票→🎡 買→🛍️ 其他→➕）。
- 為此需要一個「這筆的 emoji 是使用者指定的」標記，**在盤點檔第二段的 schema 缺口
  補上這一項**（加欄位，不影響既有資料）。**不要寫 migration，也不要套用。**
- 盤點表 `_盤點_畫面功能.md` 的 S-04-3 從 `[議]` 改為 `[改]`，寫上這個做法。

理由：EmojiPicker 已經被拿掉了，再造一個彈層等於把剛移除的東西裝回來；
就地編輯已經是全站處理「一個字元」的統一手法，多一套只會多一種要維護的互動。

#### 27-6c　工作清單第 9 項標為已作廢

第 9 項要「沒標『這是我』時顯示『還沒標記你是誰 →』提示」，
但 S-03-11「我的花費」整格已經移除，這個提示沒有依附的位置。
**在 `_盤點_實作缺口.md` 第三段把第 9 項改標為「已作廢」**，
原因寫「S-03-11 已移除，提示沒有落點」。
**一樣只在盤點檔記錄，不要去改 `_實作批次工作清單.md` 本身。**

#### 停止條件（27-6 追加）

21. `_盤點_實作缺口.md` 第一段中，S-06-12、S-06-13、S-07-5、S-05-17、S-07-6、S-04-3
    六列的內容與 #27 完成後的原型一致；第五段統計數字與第一段實際列數相符。
22. 第三段的第 9 項標為「已作廢」。
23. 第二段 schema 缺口**多出一列**（類別 emoji 的手動指定標記），
    且該列的「加了會不會影響既有資料」欄為「不影響」。
24. `renderS04()` 中，S-04-3 的 emoji 是可互動元素（有 `id` 且可被點擊觸發輸入態），
    不再是純 `<span>`；且點擊後產生的輸入框與 S-02c-11 使用**同一個**處理函式
    （測試方式：兩處的事件處理走同一個函式名，不得各寫一份）。
25. 盤點表 `_盤點_畫面功能.md` 的 S-04-3 該列**不含 `[議]`**。

---

## #28 icon 底組定案 ＋ 文字貼邊（全站）＋ 三項小修 [已處理]

Rozi 看過 #27 的候選對照條後的裁示。**本節只改 `Tripay_原型.html` 與盤點文件，
不改 `src/`、不跑 migration、不刪檔。**

> **更新（Rozi 2026-09-05 追加）**：S-03 的動作結構已拍板，見 28-6；
> icon 尺寸與容器樣式見 28-7；設定移到首頁見 28-6c。這三項一併在本節做完。

---

### 28-1　icon 底組定案：Feather

Rozi 選 **Feather**。把它設為全站唯一的 icon 來源，
另外兩組候選（Lucide／Heroicons）**從對照條移除**，
`ICON` 的 13 個鍵全部改用 Feather 的造形。

**對照條本身也拿掉**——它的任務（讓 Rozi 挑）已經完成，留著只會讓稿頭變長。
`_icon授權.md` 只留 Feather（MIT）一組，其餘刪除。

**`icons_自備/` 的六個檔案不採用。** Rozi 原話：
「不要管我放進 `icons_自備/` 的 svg，因為我原本以為你做不到我要的，
但現在看 Feather 這組我覺得可以接受。」
Cowork 端已把六個檔案移到 `icons_自備/未採用/`，
**#27-5b 那條「每次交付前檢查 `icons_自備/`」的規則改成：只看該資料夾的第一層，
`未採用/` 子資料夾一律忽略。**

---

### 28-2　`.sec` 這個 class 根本沒有定義（全站文字貼邊的根因）

Rozi：「S-03 顯示的日期有『出發前』跟日期的文字排列，目前畫面上看起來是頂在頁面的
最左邊，並沒有保留一點間距……不要有文字的 X 軸直接貼近畫面的最邊邊，
不要有 X=0 這樣子的數值。這個文字的排列，請全站都要再詳細檢查一遍。」

**Cowork 端用 headless Chrome 量過文字的實際墨水位置**（不是元素外框，是
`Range.getBoundingClientRect()`），找到 **14 處**文字左緣落在 x=0：

| 畫面 | 文字 | class |
|---|---|---|
| S-03 | 出發前／第 1 天 · 3/14（六）／第 2 天／第 3 天／第 5 天（共 5 處）| `sec` |
| S-03d | 還沒算清楚 · 3 筆 | `sec` |
| S-02c | 顯示：三層 fallback／編輯：點識別圓圈，就地改 | `sec` |
| S-06 | 誰付給誰／消費明細 | `sec` |
| S-07 | 登入帳號／我的資料 | `sec` |
| S-06 | `$ 34,375`／`$ 8,594` | `money` |

**根因（已查證）**：全站搜尋 CSS，**`.sec` 這個 class 沒有任何樣式定義**。
有定義的是 `.txsec`（`padding:10px 14px 5px`，小字灰色）。
所以凡是用 `class="sec"` 的地方都是**裸 div**——沒有內距、沒有字級、沒有顏色，
只是繼承來的預設值剛好看起來像小標。這一次同時解釋了三件事：
文字貼邊、字級比其他小標大、顏色不是灰的。

**改法**：

1. **定義 `.ui .sec`**，與既有的 `.txsec` 對齊同一套語彙：
   `padding:10px 14px 5px; font-size:10.5px; font-weight:600; color:var(--gr)`。
   左內距 **14px**，與卡片、統計卡、`.fld` 同一條 x 軸——
   Rozi 明確要求「總花費的跟『總花費』是對齊同一個 x 軸」。
2. **S-03 的日期分組標頭（S-03-15）**要特別確認：改完之後
   「出發前」「第 1 天 · 3/14（六）」的左緣要和下方消費卡片的左緣相同。
3. **S-06 那兩個 `money`** 是另一件事，不是 `.sec` 造成的，
   查出它們為什麼撐到左緣並修掉（同樣要 ≥14px）。
4. **如果修完發現某些 `.sec` 其實想要的是別的樣式**（例如 S-02c 的兩個是稿註用的大標，
   不是產品小標），**停下回報，不要自己改成兩種**。

---

### 28-3　S-00-5 登入鈕寬度要與 S-00-4 標語一致

Rozi：「S-00-5 按鈕寬度請跟 S-00-4 寬度一致。」

**實測**：S-00-4 標語 189px（它有 `max-width:200px`，文字換行後實得 189）、
S-00-5 按鈕 126px（收縮成內容寬）。

**改法**：在登入頁的中央區塊建立一個**固定寬度 200px 的欄**，
S-00-4 與 S-00-5 都填滿它（標語 `width:200px` 置中、按鈕 `width:100%`）。
不要只把按鈕設成 200px 就算——那樣標語還是 189，兩者仍然不齊。
**停止條件會直接比對兩者的實測寬度必須相等。**

---

### 28-4　返回改成純 icon（全站）

Rozi：「S-03-2 是否不需要『返回』的文字？只要有 icon 就可以了？」

**採用，而且全站一起改**（S-03-2、S-05、S-07 及所有導覽列的返回）。
理由：返回永遠在左上角、永遠是同一顆，是使用者第一次點過就記住的位置；
文字在深色 hero 上還會跟行程標題爭視線。

**但兩件事不准省**：

1. **點擊區域維持至少 44×44**。icon 本身只有 16px，
   要靠 padding 撐出可點範圍，不能讓可點區跟著文字一起縮小。
2. **`aria-label="返回"` 必須保留**，讀螢幕的人不會因為拿掉文字就失去資訊。

---

### 28-5　登出確認框的登出鈕（#27 停下回報的那一項）

終端機在 #27 停下回報：S-07-8 登出確認框的「登出」鈕仍是實心刪除紅（`.btn dg`）。
**它判斷正確，指令確實漏了。**

**改法**：S-07-8 的登出鈕改為 `.btn gh`（白底、主色描邊、主色字），
與 S-07-6 的描邊語彙一致，並與「刪除」的實心深紅明確區隔。
理由：登出可復原（再登入即可），不屬於 `--dg` 的使用範圍。
取消鈕維持 `.btn qt`。

---

### 護欄

- **不改 `src/`、不跑 migration、不刪檔。**
- **S-05 的所有編號、內容、`transferView()` 共用結構、兩種結算模式的呈現一律不動**——
  28-6 只改它「從哪裡進去」，不改它本身。
- **結算引擎與淨額計算不動。**
- **不要重新編號。**
- `.txsec`、`.lbl`、`.txhead` 等既有樣式**不得順手一起改**（28-2 只新增 `.sec` 的定義）。
- `--dg`、`--out` 的色碼不動（28-5 只改一顆鈕用哪個 class）。
- #25／#27 既有的所有斷言**維持通過**。
- 動 A 時若發現需要動 B，**停下回報，不得順手改**。

---

### 停止條件（機器判斷）

1. **全站文字貼邊掃描 0 筆**：對每個 `.ui` 內的文字節點，
   用 `Range.getBoundingClientRect()`（**不是元素外框**）量墨水位置，
   左緣與右緣距畫面內緣**皆 ≥ 14px**。
   逐一渲染 S-00～S-07 的各狀態、在 320／375／414 三個寬度下都要過。
   **例外白名單只允許**：hero 內的全幅背景、遮罩層、分隔線這類非文字元素——
   若確實需要例外，在盤點檔列出並寫明理由，不得靜默放行。
2. `getComputedStyle(document.querySelector('.ui .sec')).paddingLeft` **等於 `14px`**。
3. S-00-4 與 S-00-5 的 `getBoundingClientRect().width` **完全相等**（容差 0）。
4. 全站所有返回控制項：`textContent.trim()` **不含「返回」二字**，
   且 `getAttribute('aria-label')` **等於「返回」**，
   且可點區域的 `width` 與 `height` **皆 ≥ 44**。
5. `ICON` 的 13 個鍵**全部來自 Feather**；`_icon授權.md` **只列 Feather 一組**；
   稿頭的候選對照條**已移除**（全站搜尋 `Lucide`、`Heroicons` 出現次數為 0）。
6. `renderS07()` 產生的 HTML 中，登出確認框的登出鈕 class **含 `gh`、不含 `dg`**。
7. 三個寬度下**沒有任何畫面出現橫向捲動**。
8. #25／#27 的全部斷言維持通過；既有七組測試全綠。
9. 原型項目總數與 `_盤點_畫面功能.md` 逐一對得起來，無孤兒、無缺漏；
   `_盤點_實作缺口.md` 受影響的列已同步。

### 交付

回報寫進 `_停點.md`（覆寫、標時間、≤50 行）。
回報中要明講：貼邊掃描修了幾處、有沒有例外白名單、`.sec` 定義後有沒有畫面看起來變了。

---

### 28-6　S-03 動作結構重整（Rozi 已拍板）

Rozi 原話：「S-03-3＆S-03-4＆S-03-5＆S-03-6＆S-03-25 這個區塊有個問題……
我對這幾塊功能擺放的位置，在新的體驗上感覺有點硬、有點違和……目前看起來按鈕太多了。」
**經討論後她拍板採用下列結構。**

**問題不只是數量，是這 7 個動作不屬於同一個層級**：每天要用的只有「記一筆」；
「複製行程」產出的是一趟新行程，屬於首頁；「設定」是 App 層級的，不是這趟的設定。
右上角那排 icon 混了三種層級，這是違和感的來源。

#### 28-6a　新結構

```
┌──────────────────────────┐
│  ‹                     ⋯  │  ← 頂列只剩兩顆
│    2026 濟州島四寶團        │
│    3/14–3/18 · 🐵🐱🍋🐟    │
├──────────────────────────┤
│   ［ 消費 ］    結算        │  ← 分段控制：切換檢視，不是動作
├──────────────────────────┤
│   總花費        $ 34,375   │
│   … 消費列 …               │
├──────────────────────────┤
│        ＋ 記一筆           │  ← 唯一的主鈕
└──────────────────────────┘
```

- **分段控制**沿用既有的 `.seg` 樣式，兩格：`消費` ／ `結算`。
  放在 hero 下方、統計卡上方。
- **「結算」分頁的內容就是現在的 S-05 整頁**。
  **S-05 的所有編號、內容與行為一律不動**——只改「從哪裡進去」。
  原型上 S-05 仍保留為獨立區塊供檢視，但要在該區塊的稿註寫明
  「產品上這是 S-03 的第二個分頁，不是另一頁」。
- **不做底部導覽列。** 理由記在 `專案狀態.md`，這裡只要照做。

#### 28-6b　⋯ 選單的內容（依行程狀態變化）

| 行程狀態 | 底部主鈕 | ⋯ 選單 |
|---|---|---|
| 準備旅遊／旅遊中 | `＋ 記一筆` | 編輯行程 ／ 分享 ／──／ 複製成新的一趟 ／──／ 刪除行程（紅） |
| 已結算 | **沒有底部主鈕** | 編輯行程 ／ 分享 ／──／ 複製成新的一趟 ／──／ 封存行程 ／ 刪除行程（紅） |
| 已封存 | `重新開啟行程` | 分享 ／──／ 複製成新的一趟 ／──／ 刪除行程（紅） |

判斷依據：
- **已結算態沒有底部主鈕**，因為那時的主要動作是「逐筆標記付清」，那在結算分頁裡做。
- **已封存態把「重新開啟行程」留在底部**，因為唯讀狀態下那是唯一能做的事，值得佔主位。
- **封存時「編輯行程」不出現**（沿用既有規則：封存＝預設只讀）。
- ⋯ 選單的樣式沿用既有的 `.sheet`（底部彈出）或 `.dlg`，**擇一即可，不要新造第三種彈層**；
  「刪除行程」用 `--dg` 的紅字，是選單裡唯一著色的一項。

**編號怎麼給**（不准重新編號既有的）：
- `S-03-2` 返回：保留，改為純 icon（見 28-4）＋新容器（見 28-7）
- `S-03-3` 編輯行程、`S-03-4` 複製行程、`S-03-5` 分享、`S-03-6` 設定：
  **四個編號都保留**，但 3／4／5 移進 ⋯ 選單、`S-03-6` 設定**從 S-03 移除**（見 28-6c）。
  盤點表逐一標明「移進 ⋯ 選單」或「移到 S-01」。
- `S-03-25` 刪除這趟行程：**從列表底部移進 ⋯ 選單**，編號保留。
- `S-03-22`／`S-03-23`／`S-03-24` 三種狀態的底部列：依上表改內容，編號保留。
- **新增的編號從 `S-03-30` 開始接**：`S-03-30` ⋯ 選單觸發鈕、`S-03-31` ⋯ 選單本體、
  `S-03-32` 分段控制。

#### 28-6c　設定移到首頁

`S-03-6` 是全站唯一的設定入口，但那是 **App 設定**（登入帳號、我的資料），
不是這趟行程的設定。掛在行程頁是層級錯位。

- **S-03 移除設定 icon。**
- **S-01 首頁標題列右上角加設定入口**（齒輪，同 28-7 的容器樣式）。
  首頁目前的標題列在 `TripListPage` 對應的 S-01-1；
  **排在 S-01-1 標題列內、靠右**，與既有的「＋ 新增」並列時設定放最右。
- 這一項就是 `_實作批次工作清單.md` 第 10 項「設定頁只能從行程內頁 ⚙ 進去」，
  **在 `_盤點_實作缺口.md` 第三段把它標為「稿上已解，待實作」**（不要改工作清單本身）。
- 新編號：首頁的設定入口用 `S-01-14`（接在既有最大號之後，**先查證目前最大號再接**）。

---

### 28-7　icon 太小：尺寸與容器一起改

Rozi：「icon 太小也是一個問題，icon 大小與視覺顯示可以參考我給你的附圖，
例如 icon 外面還有一圈灰線。」

**現況（已讀碼查證）**：`.ui .hero .ic2` 是 `26×26`、`border-radius:4px`、
`background:rgba(255,255,255,.2)`、**沒有邊框**，裡面的 icon 只有 **15px**。
26px 的可點區遠低於 44px 的最小觸控標準。

**改成**（兩套，深底與淺底各一，其餘規格相同）：

| | 深色 hero 上（S-03 的 ‹ 與 ⋯） | 淺色導覽列上（S-05／S-07 等的 ‹） |
|---|---|---|
| 容器尺寸 | 40×40 | 40×40 |
| 形狀 | 正圓（`border-radius:50%`） | 正圓 |
| 底色 | `rgba(0,0,0,.28)` | `#fff` |
| **邊框** | `1px solid rgba(255,255,255,.35)` | `1px solid var(--ln)` |
| icon 尺寸 | **20px** | **20px** |
| icon 顏色 | `#fff` | `var(--md)` |
| 可點區 | **≥44×44**（靠外距撐，容器本身維持 40） | 同左 |

- `.ic2` 這個 class 直接改掉即可，**不要新增第二個並存的樣式**。
- 「一圈灰線」就是上表的邊框那一列——深底上用半透明白線（灰線在彩色 hero 上會髒），
  淺底上用既有的 `--ln`。
- 首頁（S-01）新增的設定入口用**淺底那一套**。
- **只改容器與尺寸，不改任何 icon 用在哪裡**（Feather 造形已在 28-1 定案）。

---

### 停止條件（28-6／28-7 追加）

10. S-03 的 hero 導覽列裡**只有兩個可點元素**（返回、⋯）。
11. `.ui .hero .ic2` 的實測尺寸為 **40×40**，`border-radius` 解析為圓形，
    `border-width` **不為 0**；其內 `svg` 的 `width` 為 **20px**；
    可點區域（含外距）的 `width` 與 `height` **皆 ≥44**。
    淺色導覽列的返回容器同樣通過這一組檢查。
12. S-03 存在分段控制，恰好 **2 格**，文字為 `消費` 與 `結算`；
    切到「結算」時渲染的內容包含 S-05 的編號。
13. 三種行程狀態下，S-03 底部主鈕分別為
    `＋ 記一筆`／**不存在**／`重新開啟行程`。
14. ⋯ 選單在三種狀態下的項目數與內容符合 28-6b 的表；
    「刪除行程」是選單中**唯一**使用 `--dg` 的項目。
15. `renderS03()` 產生的 HTML **不含設定 icon**；
    `renderS01()` **含設定入口**且其 `aria-label` 為「設定」。
16. 新增編號為 `S-03-30`／`S-03-31`／`S-03-32` 與首頁的一個新號；
    **既有編號一個都沒有被重新指派**（與 `_盤點_畫面功能.md` 逐一比對）。
17. `renderS05()` 的輸出與本節修改前**逐字元相同**
    （做法：修改前先把輸出存成基準檔，修改後比對）——證明只改了入口、沒動 S-05。
18. 三個寬度下沒有橫向捲動；28-1～28-5 與 #25／#27 的全部斷言維持通過。

---

## #29 S-04 日期位置 ＋「可留空」文案 ＋ 提示文字的視覺層級顛倒 [已處理]

**本節只改 `Tripay_原型.html` 與盤點文件，不改 `src/`、不跑 migration、不刪檔。**
**#28 的所有內容一律不得回退**（Feather、`.sec` 定義、S-00-5 等寬、返回純 icon、
登出鈕描邊、S-03 動作結構、icon 容器 40×40）。

---

### 29-1　S-04-10 日期改成標題下的一條窄列（Rozi 選方案 C）

Rozi：「日期每一天大概只會設定一次，其他時候都是設定那一天的其他子配置。
它現在放在這個位置，以順序來講，我覺得比重有點太重了。」

**問題定性**：日期不是「這一筆的屬性」，是「**這一批帳的共同前提**」。
使用者在一天結束前連記三筆，日期只是背景資訊，
現在卻是第 5 個完整欄位、擋在每筆都要填的「誰付的」前面，等於要跳過它三次。

**改法**：

- **從欄位序列中移出**，改成**標題列（S-04-1）正下方的一條窄列**，
  排在 S-04-1 之後、S-04-2（花費名稱）之前。
- 樣式：淺底（比表單底色略深一階）、灰字、**整條可點**、右側一個 `›`。
  高度明顯低於一個欄位列——它不是輸入欄，是狀態列。
- **文案：`記在 3/16（一）`**，日期本身用深字、「記在」用灰字。
  不要用「日期」當標籤——那是欄位的講法，這裡描述的是狀態。
- 點整條 → 開日期選擇（沿用既有的原生 date input 或既有選擇方式，**不要新造第三種**）。
- **編號 `S-04-10` 保留**，不要重新編號；盤點表的「改法」欄註明位置與型態都變了。
- **為什麼不放進標題列**（Rozi 也問過這個方案）：日期會緊鄰 `✕`，
  手指誤觸就整張表單關掉；而且標題列會同時放「說明用的標題」和「可點的東西」，
  兩種角色混在一起。這一條記在 `專案狀態.md`，這裡照做即可。

**預設值不變**，維持現行邏輯。Rozi 對「回國後補帳、日期落在行程結束之後」的裁示是
**「我可以自己手動調整日期」**——**不做任何自動修正、不加警告、不擋**。

---

### 29-2　S-04-5／S-04-6 的「可留空」

Rozi 問這個詞是不是該換掉。以下是 UX／Copywriter／PM 討論後的結論。

**現況**：外幣總額與台幣總額兩個輸入框，placeholder 都是「可留空」。

**三個問題**：

1. **它講的是表單的規則（你被允許不填），不是這一格要放什麼。**
   placeholder 的位置最適合放「這裡長什麼樣子」的示範，不是放許可。
2. **兩欄寫同一個詞。** 使用者看到兩個空格、兩個一模一樣的灰字，
   卻沒有任何線索分辨哪一格是外幣、哪一格是台幣。
3. **它沒有講出真正有用的那件事**——「只要填一邊，另一邊會自己算出來」。
   而那句話不是每一格都要重複的話，是整個金額區塊講一次的話。

**改法**：

- **兩欄的 placeholder 都改成 `0`**（與既有匯率欄的 placeholder 一致，
  純粹表示「這裡放數字」）。哪一欄是哪種幣別，由左側既有的 `.cur` 幣別標籤負責。
- **金額區塊下方放一行 `.hint`**，且**兩種狀態只會出現其中一種，不得同時出現**：
  - 這趟**有設匯率** → `填一邊就好，另一邊會自動換算`
  - 這趟**沒設匯率** → 維持既有的 S-04-38 警告（`還沒設這趟的匯率，算不出台幣`），
    **不要再多加上面那句**。
- **順帶改一處同樣毛病**：S-02-9 回程欄下方的 `.hint`
  「可留空＝當天來回」→ **`不填就是當天來回`**。
  「可留空」是系統用語，Tripay 的語氣不用它。
- **全站搜尋「可留空」，改完後出現次數必須為 0。**

---

### 29-3　提示文字比可點選項還深（視覺層級顛倒）

Rozi：「為什麼全站的提示文字，顏色會比其他功能選項（例如 S-04-21＆S-04-11）的
字體顏色還要深？以視覺層級來講，提示文字應該是灰階才對，現在看起來有點顛倒了。」

**她說對了。Cowork 端已用 `getComputedStyle` 逐一量過**（含 `::placeholder` 偽元素）：

| 元素 | 實際色值 | 相對亮度（越小越深）|
|---|---|---|
| 欄位標籤 `.lbl` | `#4C5B64`（`--md`）| 0.099 |
| chip 未選（S-04-21 誰付的）| `#4C5B64`（`--md`）| 0.099 |
| **placeholder「可留空」** | **`#757575`**（瀏覽器預設）| **0.178** |
| 提示 `.hint` | `#78878F`（`--gr`）| 0.233 |
| **seg 未選（S-04-11 分帳方式）** | **`#78878F`（`--gr`）** | **0.233** |

**兩個獨立的錯**：

1. **placeholder 從來沒有被設定樣式**，用的是瀏覽器預設的中性灰 `#757575`。
   它比「分帳方式」的未選項目（0.233）**還深**——這就是 Rozi 看到的顛倒。
   而且 `#757575` 是純中性灰，Tripay 的 `--gr` 是帶青藍調的 `#78878F`，
   兩者不同色系，placeholder 等於整個脫離設計系統。
2. **`.seg button` 未選用了 `--gr`，但 `.chip` 未選用 `--md`。**
   兩者是同一種東西（可點、未選的選項），卻是兩個顏色。
   S-04-11 用 seg、S-04-21 用 chip，並排看就明顯不一致。

**改法——把層級訂死成三階，全站遵守**：

| 階 | 用途 | 色 |
|---|---|---|
| 一 | 內容本身（已輸入的值、金額、標題）| `#151C21` |
| 二 | 欄位標籤、**可點但未選的選項**（chip 未選、**seg 未選**）| `var(--md)` `#4C5B64` |
| 三 | 提示文字、**placeholder**、次要說明 | `var(--gr)` `#78878F` |

具體兩處：

- **新增全站 `::placeholder{color:var(--gr)}`**（含 `::-webkit-input-placeholder`
  等必要的相容寫法），不要逐個欄位設。
- **`.ui .seg button` 未選態的 `color` 由 `var(--gr)` 改為 `var(--md)`**，
  與 `.chip` 未選一致。**已選態（`.on`）維持白字藍底，不動。**

**不要順手做的事**：
- **`--gr` 與 `--md` 的色碼本身不准改。** 這一節只改「誰用哪一個」。
- 若改完發現某處原本刻意用 `--gr` 來壓低權重（例如 `.autotag`），
  **停下回報，不要一起改成 `--md`**。

**另外記一筆、這一節不處理**：`--gr` `#78878F` 對白底的對比度約 **3.7:1**，
低於 WCAG AA 對一般文字要求的 4.5:1，而它被用在 10.5px 的提示小字上。
這是全站性的可讀性風險，**改它會動到每一個畫面，不在這一節做**——
在 `_盤點_實作缺口.md` 開一列記著即可。

---

### 護欄

- 不改 `src/`、不跑 migration、不刪檔。
- 不要重新編號（`S-04-10` 保留）。
- **`--gr`、`--md`、`--dg`、`--out` 的色碼一律不動。**
- **S-05 的輸出不得改變**（沿用 #28 的基準檔比對）。
- #25／#27／#28 的全部斷言維持通過。
- 動 A 時若發現需要動 B，**停下回報，不得順手改**。

### 停止條件（機器判斷）

1. `renderS04()` 產生的 HTML 中，`S-04-10` 出現在 `S-04-1` 之後、`S-04-2` 之前
   （用字串位置比對）。
2. `S-04-10` 那一條的 `textContent` **含「記在」、不含「日期」**；
   其高度**小於**任何一個 `.fld` 欄位列的高度；整條為可點元素。
3. 全站搜尋 **`可留空` 出現次數為 0**。
4. `#scr-s04` 兩個金額輸入框的 `placeholder` **皆為 `0`**（自動帶值的狀態除外，
   該狀態沿用既有行為）。
5. 「填一邊就好，另一邊會自動換算」與 S-04-38 的匯率警告
   **在任何一種狀態下都不會同時出現**（兩種狀態各渲染一次驗證）。
6. `getComputedStyle(input,'::placeholder').color` 解析後**等於 `--gr` 的值**，
   全站所有 `input[placeholder]` 皆然。
7. `.ui .seg button`（未選）的 `color` **等於 `.ui .chip`（未選）的 `color`**。
8. 三階層級成立：**內容色亮度 < 二階亮度 < 三階亮度**，
   用實測值比對，不得有任何「提示比選項深」的情況。
9. `--gr`、`--md`、`--dg`、`--out` 的色碼與本節修改前**完全相同**。
10. `renderS05()` 的輸出與本節修改前逐字元相同。
11. 三個寬度（320／375／414）沒有橫向捲動；文字左右緣皆 ≥14px（#28 的斷言）。
12. #25／#27／#28 的全部斷言維持通過；既有各組測試全綠。
13. `_盤點_實作缺口.md` 多一列記錄 `--gr` 對比度 3.7:1 的可讀性風險。

### 交付

回報寫進 `_停點.md`（覆寫、標時間、≤50 行）。
回報中要明講：改了 `::placeholder` 與 seg 未選色之後，**有哪些畫面的觀感變了**。

---

### 29-4　全站字級：沒有階梯，而且整體比 iOS 原生小三成

Rozi：「S-05-17＆S-05-29 這個區塊的『出遊』『共記了』『最大手筆』『任何人免登入可看』
文字為什麼要特別小？請放大，並檢查全站有沒有一樣的字級大小，請一併調整。」
「S-03c-1 這裡的文字字體好像都偏小？」
「我記得 UX 體驗來講，在手機操作上最小字級只能在 14／12 而已？請你確認。」

#### 查證結果（Apple 官方文件，2026-09-05 讀取）

Rozi 記的方向對，**但數字不是 14／12**。Apple Human Interface Guidelines 的
Typography 頁面明載：

| 平台 | 內文預設 | **最小** |
|---|---|---|
| **iOS／iPadOS** | **17 pt** | **11 pt** |
| macOS | 13 pt | 10 pt |
| watchOS | 16 pt | 12 pt |

**網頁上 1pt ≈ 1 CSS px**（標準 viewport meta 之下），所以這兩個數字可以直接當 px 用。

**另外有一條網頁特有的硬性限制**：**iOS Safari 在使用者點進字級小於 16px 的輸入框時，
會自動把整頁放大**。Tripay 的記帳表單全是輸入框——這不是美感問題，
是「每次點金額欄畫面就跳一下」的體驗災難。

#### 現況：全站量過的結果

**Cowork 端量過每一個文字節點的 computed `font-size`**，
14px 以下有 **10 個不同字級**，沒有階梯：

| 字級 | 筆數 | 有哪些 |
|---|---:|---|
| **9px** | 7 | S-01 狀態徽章、S-06「朋友檢視」、**S-05「出遊／共記了／最大手筆」** |
| **9.5px** | 23 | **S-03 消費列的付款人「🐵 Rozi」**、各種 badge、S-06 日期、S-07 footer |
| 10px | 8 | S-07 email、「即將推出」 |
| 10.5px | 15 | 多數 `.hint`，含 **S-05「任何人免登入可看」** |
| 11 / 11.5 / 12 / 12.5 / 13 / 13.3px | 169 | 內文、欄位標籤、chip、消費名稱、成員名字 |

**對照 Apple 的數字**：Tripay 的內文是 **12.5px**，iOS 的內文預設是 **17pt**。
**整體大約只有原生的七成**——這就是 Rozi 一路上反覆覺得「字好像偏小」的來源，
不是某幾個地方特別小，是整套都小。

#### 改法：六階，向 iOS 原生對齊

| 角色 | 字級 | 對應的 iOS text style |
|---|---|---|
| 標記類（不成句）：狀態徽章、各付各的／只算 X／當場就清了、「自動」標、單位（天／筆）、「朋友檢視」 | **12** | Caption |
| 輔助資訊與提示句：**付款人**、日期、所有 `.hint`、**「出遊／共記了／最大手筆」**、**「任何人免登入可看」**、email | **13** | Footnote |
| 內文（預設）：消費名稱、chip／seg 選項、欄位標籤、按鈕文字、清單項目 | **15** | Subhead |
| **所有輸入框內的文字（`input`／`textarea`／`select`）** | **16（硬性下限）** | 避免 iOS Safari 自動放大 |
| 區塊強調：成員名字、**對話框標題（S-03c-1）**、區塊主標 | **17** | Body |
| 頁面標題、行程名 | **20** | Title 3 |

**金額也要一起放大**，否則內文變 15 之後金額會比它旁邊的文字還小：

| 金額出現的地方 | 字級 |
|---|---|
| 消費列、轉帳列的金額 | **16** |
| 統計卡主數字 | **26** |
| 結算頁的大數字 | 維持它與統計卡主數字的相對比例，**但不得小於 16** |

**硬規則**：
1. `.ui` 之內不得有任何文字小於 **12px**。
2. 任何輸入框的字級不得小於 **16px**。
3. **同一列裡，金額的字級不得小於該列文字的字級。**

**不動的**：
- 金額仍用等寬字，**只改大小、不改字體**。
- 原型的稿註區與右側清單不受此表約束（那是給看稿的人的，不是產品文字）。

**這是一次大改，做法上要注意**：
- 用 CSS 變數定義這些階（`--fs-tag/--fs-sub/--fs-body/--fs-input/--fs-strong/--fs-title`
  與金額的三階），各處引用變數，**原始碼裡不得再出現寫死的 `font-size:9.5px` 這類值**。
- **字級整體放大三成，版面一定會擠**。既有的版面回歸測試就是用來擋這個的，
  三個寬度（320／375／414）都要重跑。
- **若某一處放大後明顯破版、換行變醜、或一頁放不下幾筆消費，
  停下回報並附上是哪一處，不要自己縮回去、也不要順手改間距或改欄位排列。**
  密度的取捨要由 Rozi 看過再決定。


### 29-5　S-03b／S-03c／S-07 會不會被 icon 調整影響（查證結果）

Rozi 問的。**已逐一讀碼確認**：

| 畫面 | 有沒有用 icon | 會不會因 #28 的 icon 調整而變 |
|---|---|---|
| **S-03b 分享選單** | **完全沒有** | **不會**。三個選項都是純文字 |
| **S-03c 刪除對話框** | **完全沒有** | **不會**。純文字＋輸入框＋兩顆鈕 |
| **S-06 分享頁** | **完全沒有** | **不會** |
| **S-07 設定頁** | 有 2 種 | **會**：返回 `back`（→ 40×40 圓容器＋20px icon）、列尾 `›` `next` ×3（→ Feather 造形）|

**所以這三個畫面這一輪只會因為字級（29-4）而變，不會因為 icon 而變**——
S-07 除外。

**S-07 列尾的 `›` 箭頭**：**不加圓形容器**，維持裸 icon，
但尺寸由 15px 調為 **16px**。理由：它是方向指示，不是可點目標——
可點的是整一列，44px 的觸控標準由整列滿足。加容器會讓它看起來像另一顆按鈕。

---

### 29-6　S-06 的文字貼邊

Rozi：「S-06 這個區塊的文字也有 X=0 的問題，請照前面的處理方式調整這個畫面。」

**這一項 #28-2 已經涵蓋**：#28 的貼邊清單裡就列了 S-06 的
「誰付給誰」「消費明細」（`.sec`，由新增的 `.sec` 定義解決）
與兩個 `$ 34,375`／`$ 8,594`（`money`，#28-2 第 3 點要求另外查因並修）。
#28 的停止條件第 1 條是**全站掃描**，S-06 本來就在範圍內。

**這一節要做的只有一件事：複驗。** 如果 #28 跑完 S-06 仍有貼邊，
在 `_停點.md` 明講是哪一處、為什麼 #28 沒修到。

---

### 29-7　`--gr` 調深到 `#6A7980`

Rozi：「『--gr 對比度 3.7:1 的可讀性風險，之後單獨處理』這題我不知道我要處理什麼？」

**白話**：Tripay 的灰字（提示、付款人、日期）在白底上偏淡。
無障礙標準要求一般文字的深淺差距至少 4.5 倍，現在這個灰只有 **3.7 倍**。
實際後果是——**在戶外、車上、或把螢幕亮度調低的時候，那些灰字會看不清楚**。
而 Tripay 的使用情境正好是旅遊，多半就在戶外。

原本說「之後單獨處理」是因為擔心動到全站。**但實測之後發現代價很小**：
保持同一個色相與飽和度，只把明度降一階，
**`#78878F` → `#6A7980` 就達到 4.51:1**，剛好過標準。
兩者並排幾乎看不出差別，灰字與深字（`--md` `#4C5B64`，7.8:1）的層級照樣分得開。

**改法**：`--gr` 由 `#78878F` 改為 **`#6A7980`**。
`--md`／`--dg`／`--out`／`--w` 一律不動。
**推翻方式**：把 `--gr` 改回 `#78878F` 即可，沒有其他地方依賴這個值。

---

### 停止條件（29-4～29-7 追加）

14. `.ui` 之內**沒有任何文字節點的 computed `font-size` 小於 12px**
    （逐一渲染各畫面各狀態、三個寬度）。原型稿註區與右側清單不列入。
15. `.ui` 之內、**非金額**的文字，其 computed `font-size` 只能落在
    **{12, 13, 15, 16, 17, 20}** 這六個值（容差 0）。
16. **所有 `input`／`textarea`／`select` 的 computed `font-size` ≥ 16px**
    ——這一條是 iOS Safari 的硬性限制，破了就會自動放大整頁，不得例外。
17. 金額的字級只能落在 **{16, 26}**，或（結算頁大數字）**≥16**；
    **同一列中，金額的字級 ≥ 該列其他文字的字級**。
18. Rozi 點名的五處實測字級：
    「出遊」「共記了」「最大手筆」「任何人免登入可看」**皆為 13px**；
    S-03c-1 的對話框標題**為 17px**。
19. 消費列的付款人（「🐵 Rozi」那一行）**為 13px**（原 9.5px）；
    消費名稱**為 15px**（原 12.5px）。
20. 原始碼中**不再出現寫死的 `font-size:` 數值**（`.ui` 範圍內一律引用字級變數；
    稿註區不限）。
21. `renderS03b()` 的輸出**不含任何 `<svg>`**（確認該畫面沒有 icon）。
22. S-07 列尾 `›` 的 `svg` 寬度為 **16px** 且**沒有圓形容器**；
    S-07 返回鈕仍符合 #28-7 的容器規格。
23. `--gr` 的值為 `#6A7980`；白底對它的對比度 **≥4.5**；
    `--md`／`--dg`／`--out`／`--w` 與修改前完全相同。
24. **全站文字左右緣皆 ≥14px**（#28 的斷言重跑，S-06 必須在內且為 0 筆）。
25. 三個寬度（320／375／414）**沒有橫向捲動**——字級整體放大三成後這一條最容易破，
    破了要停下回報是哪一處，不得自行縮字級或改間距。
26. `renderS05()` 的輸出除了字級之外沒有結構改變
    （比對 HTML 的標籤結構與文字內容，允許 class／style 的字級差異）。
27. #25／#27／#28 與 29-1～29-3 的全部斷言維持通過。

---

### 29-8　Cowork 端複驗 #28 的結果（追加三項）

**#28 的斷言 Cowork 端全部重跑過，主要項目都通過**：
文字貼邊（Range 墨水位置、S-00～S-07 全狀態）0 筆、S-00-4 與 S-00-5 實測**都是 200px**、
icon 容器 40×40 正圓＋1px 邊框＋20px icon、返回純 icon 且 `aria-label` 保留
（可點區靠 `::after` 撐到 44×44，做法正確）、S-03 頂列只剩返回與更多、
分段 2 格「消費／結算」、⋯ 選單三種狀態的項目與 28-6b 的表相符、
Lucide／Heroicons 已清乾淨、登出鈕改描邊且 S-07 不再出現 `var(--dg)`。

**S-03 的重整方向對了**，違和感確實消掉。以下三項是複驗時看出來的。

#### 29-8a　兩個標註徽章漏進產品畫面（優先修）

**分享頁 S-06 的統計卡上，直接印著「S-06-4」與「S-06-6」兩個編號**——
不是在標註模式，是**操作模式下就看得到**。
全站 99 個徽章裡有這 2 個沒被隱藏。

**根因**：隱藏規則寫成 `.b>.bdg{display:none}`，用的是**直接子選擇器**。
這兩處的徽章中間多包了一層，選擇器沒中。

**改法**：把 `.b>.bdg` 與 `html.anno .b>.bdg` 的**直接子選擇器改成後代選擇器**
（`.ui .bdg` / `html.anno .ui .bdg`），讓巢狀幾層都蓋得到。
改完要確認標註模式的徽章位置沒有跑掉（#28-4a 的不遮字斷言必須維持通過）。

**這件事特別嚴重的地方**：S-06 是**分享頁**，是旅伴點連結看到的畫面。
內部編號漏在那裡，等於把稿件編號給外人看。

**加一條永久斷言**：操作模式（`html` 沒有 `anno`）下，
`.ui` 之內 computed `display !== 'none'` 的 `.bdg` **必須為 0 個**。

#### 29-8b　分頁與幣別切換器長得一模一樣（層級混淆）

S-03-33（消費／結算**分頁**）與 S-03-12（台幣／KRW **幣別切換**）
**用的是同一個 `.seg` 樣式**——都是滿版主色實心。

**兩個問題**：

1. **層級混淆。** 一個是「切換整頁看什麼」（導覽層級），
   一個是「選一個值」（欄位層級）。長得一樣，使用者無法從外觀判斷按下去會發生什麼。
2. **兩個大藍塊上下相鄰，畫面很重。** hero 下方連續兩條滿版主色，
   視覺重量壓過了它們之間的統計卡（那才是這一頁的主角）。

**改法——把兩種控制項分成兩套，訂成全站規則**：

| 用途 | 樣式 | 用在哪 |
|---|---|---|
| **分頁（切換整頁看什麼）** | 新增 `.tabs`：**iOS 原生風格**——淺灰色底軌、選中的那格是**白色凸起 pill**（深字＋極輕陰影）、未選為灰字。**不使用主色填滿。** | S-03-33 消費／結算 |
| **欄位內的二選一（選一個值）** | 沿用既有 `.seg`：實心主色填滿 | S-03-12 幣別、S-02b 結算模式、S-04-31 填哪一邊 |

淺灰底軌用既有的 `#EDEEE8` 一階，pill 用 `#fff`＋`1px solid var(--ln)`，
**不要新造陰影變數**。這也與 Rozi 提供的 iOS App 參考圖一致。

**只改樣式，不改行為**：分頁仍是兩格、文字仍是「消費／結算」、
切換邏輯與 §6 的結算前檢查層一律不動。

#### 29-8c　⋯ 選單的項目配 icon（終端機停下來問的）

終端機回報：`edit`／`copy`／`share`／`del` 四個 icon 因為動作都變成 ⋯ 選單裡的
純文字項而**沒人用了**，問要不要配 icon。

**裁示：要配。** 三個理由：
1. Rozi 提供的 iOS App 參考圖，`⋯` 選單每一項左邊都有 icon。
2. **「刪除」配上垃圾桶，危險訊號會提前出現**——在使用者點下去之前就看得到，
   不是等對話框跳出來才知道。
3. 那四個 icon 本來就是為這些動作畫的，刪掉再補回來是白工。

**規格**：
- 每項左側一個 **16px** 的 Feather icon，與文字之間 10px。
- 對應：編輯行程 `edit`／分享 `share`／複製成新的一趟 `copy`／
  刪除行程 `del`／封存行程（**Feather 沒有現成的封存 icon 就用 `check` 以外的合適者，
  找不到就停下回報，不要自己畫**）。
- **只有「刪除行程」那一項的 icon 與文字同時用 `--dg`**，其餘用 `--md`。
- `_icon授權.md` 維持記錄這四個仍在使用。

#### 29-8d　編號用 31／32／33（終端機停下來問的）

指令原本寫新號從 `S-03-30` 起，但 30 已被「已結算／已封存不提醒」占用。
終端機依「不要重新編號」的護欄改用 **31／32／33**。**判斷正確，照這樣定案。**

---

### 停止條件（29-8 追加）

28. **操作模式（`html` 無 `anno`）下，`.ui` 之內 computed `display !== 'none'` 的
    `.bdg` 數量為 0**（逐一渲染 S-00～S-07 各狀態）。
29. 標註模式下，#28-4a 的「徽章不遮字」斷言**維持 0 筆**。
30. `S-03-33` 的容器 class **不是 `seg`**；其選中格的背景色**不是主色 `--w`**；
    `S-03-12` 的容器 class **仍是 `seg`** 且選中格背景**仍是 `--w`**。
    兩者的 computed 樣式**必須不同**。
31. `⋯` 選單的每一個動作項目**都含一個 `<svg>`**，寬度為 **16px**；
    「刪除行程」那一項的 icon 與文字 color **皆為 `--dg`**，
    其餘項目的 icon 與文字 color **皆為 `--md`**。
32. `ICON` 的每一個鍵在全站**至少有一處使用**（`edit`／`copy`／`share`／`del`
    因 ⋯ 選單而重新有用）。
33. 新編號為 `S-03-31`／`S-03-32`／`S-03-33`，且既有編號一個都沒有被重新指派。

---

### 29-9　原型要能在真的手機上看（**這一節最重要的一項**）

Rozi：「你現在畫給我的視覺原型，因為都只能在電腦上面操作，我沒有辦法實際在手機上面預覽確認。
這樣會導致我沒有辦法判斷你調整的字體大小到底會不會破版，或是有太大、太小的問題。」

**她說的是對的，而且這件事會讓 29-4 的字級調整無法驗收。**
在桌機上看，`.ph` 是一個 390px 的模擬框顯示在一片大螢幕上，**物理尺寸遠大於真的手機**；
在手機上打開現在這份稿，`.ph` 是 `min(390px,100%)` 再扣掉 `.wrap` 的左右 22px 內距，
**實際只有約 349px**，比真機的 393pt 又小了一成，還多了外框與圓角吃掉空間。
**兩種看法都量不準。**

**要做一個「真機模式」**：

- **自動進入**：`@media (max-width:600px) and (pointer:coarse)` 時自動套用。
  Rozi 用手機打開連結就直接是這個模式，不需要按任何東西。
- **桌機上另外提供一個開關**可以強制進出，方便你自己測。
- 真機模式下：
  - `.wrap` 左右內距 **0**；`.ph` 去掉邊框、圓角、陰影，`width:100%`；
    `.ui` 寬度等於視窗寬度——**畫面必須真的鋪滿螢幕，1:1**。
  - **隱藏所有稿件元素**：徽章 `.bdg`、右側清單 `.rig`／`.idx`、
    稿頭、每個 `section` 的 `.scrhd` 標題與說明。
  - **一次只顯示一個畫面**，底部放一條極簡的畫面切換器
    （S-00／S-01／S-02／S-02b／S-03／S-04／S-05／S-06／S-07…），
    **切換器本身要能收起來**，否則它會擋住被檢視畫面的底部按鈕。
  - **所有互動照常運作**，跟操作模式一致。
- 確認 `<meta name="viewport" content="width=device-width, initial-scale=1">` 存在且正確。

**在 `_停點.md` 的回報最上面，直接給 Rozi 一句話：用手機打開哪一個網址就會看到真機模式。**

---

### 29-10　S-06 統計區改成與 S-03-9 同一套

Rozi：「分享行程的『S-06-4』『S-06-6』我不要這樣子的顯示方式，
我要跟前面 S-03-9 一樣的顯示方式：上面先顯示總花費，然後做卡片展開看個人，
大家可以個別看個人的花費是多少。因為我們沒辦法設定分享出去的連結只指定分享哪些項目
給該成員看，所以就是分享全部。目前把所有都做跟 S-03-9 一樣的模式就可以了。」

**現況**：S-06 是左右兩格並排——`S-06-4` 總花費、`S-06-6` 人均。

**改成**：

- **上方一列「總花費」＋金額，可點展開**（＝ S-03-9 的 `.tot` 那一列）。
- **展開後是每位成員一列**（＝ S-03-27 的 `.perlist`），顯示每個人分擔多少。
- **「人均」那一格取消**——每人分擔列已經把它取代了，而且人均只在均分時才有意義。
  `S-06-6` 編號保留、標為移除，不要重新編號。
- `S-06-4` 編號保留，改為新的「總花費（可展開）」那一列。
- 每人分擔列需要的新編號，**先查證 S-06 目前的最大號再往後接**。

**必須共用同一個元件，不要各寫一份。**
理由與 S-05 的 `transferView()` 相同——兩處分開寫，遲早會走鐘。
測試要比對 S-03 與 S-06 兩處產生的每人分擔列**結構相同**。

**分享頁是唯讀，這三點要跟著調整**：

1. **每人分擔列在 S-06 不可點**（S-03 上點了會進未定案明細 S-03d，
   分享頁沒有那一頁，也不該讓外人進編輯視圖）。
2. **不顯示 S-03-28 的「點名字看是哪幾筆」註腳**——點不了就不要邀請人點。
   若該趟有「約」的金額，改成不邀請點擊的說法，
   例如「標『約』的金額還沒算清楚」，**句尾不要有動作指示**。
3. **「約」的顯示規則沿用既有決策**（已結算／已封存不顯示「約」）。

**若唯讀變體與既有元件衝突到必須拆成兩個元件，停下回報，不要自己拆。**

---

### 停止條件（29-9／29-10 追加）

34. 模擬 `max-width:600px` ＋ `pointer:coarse` 時：
    `.wrap` 的左右 padding 為 **0**；`.ph` 的 `border-width` 為 0、`border-radius` 為 0；
    `.ui` 的寬度**等於視窗寬度**（容差 0）。
35. 同上條件下，`.bdg`／`.rig`／`.idx`／`.scrhd` 的 computed `display` **皆為 `none`**；
    且同一時間只有一個 `section` 可見。
36. 同上條件下，在 320／375／414 三個寬度**沒有橫向捲動**，
    且**沒有任何文字左右緣 <14px**。
37. 桌機模式（`pointer:fine`）的版面**與本節修改前完全相同**
    （沿用基準檔比對）——真機模式不得影響桌機的檢視。
38. `renderS06()` 的輸出**不含 `人均`**；含一列可展開的「總花費」；
    展開後每位成員一列。
39. S-03 與 S-06 的每人分擔列**由同一個函式產生**
    （測試方式：兩處輸出的 HTML 標籤結構相同，只有值不同；
    不得各寫一份）。
40. S-06 的每人分擔列**不是可點元素**，且該畫面**不含字串「點名字看是哪幾筆」**。
41. 29-8 的斷言（操作模式下可見徽章為 0）在 S-06 上**必須通過**。
---

## #30 稿註漏在產品畫面 ＋ 圖示按鈕統一 ＋ hero 成員 emoji ＋ S-07-4 [已處理]

> 這五項原本誤寫在 #29 底下，但 #29 已經處理完（終端機做的是 29-1～29-10），
> 所以獨立成 #30。**#29 的所有成果一律不得回退。**

**本節只改 `Tripay_原型.html` 與盤點文件，不改 `src/`、不跑 migration、不刪檔。**

---

### 30-1　兩處稿註漏在產品畫面裡（S-00、S-01）

Rozi 指出這兩句：

- **S-00**：「移除了 App icon 與幽靈卡／識別由字標與 slogan 承擔」（原型第 923 行）
- **S-01**：「列表到此結束。首頁沒有固定底部操作列」（原型第 963 行）

**兩句都在 `.ui` 之內，也就是手機框裡面——產品上真的會顯示。**
它們是寫給看稿的人的說明，屬於既有規則「**原型稿註與產品文案必須分開**」明文禁止的那一類。

**兩句都刪掉。** 若這兩段說明對看稿有價值，搬到右側標註清單的對應項目說明欄，
**不要留在手機框裡**。

**加一條永久斷言**：`.ui` 之內的文字**不得出現**
「移除了」「稿上」「這裡示意」「原型」「操作列」這類描述稿件本身的字眼。
清單由你依實際內容決定，但至少要涵蓋上面這兩句。
**如果掃出第三句、第四句，一併刪掉並在回報中列出。**

---

### 30-2　所有圖示按鈕統一用 S-01-15 的形式

Rozi：「S-02b 全站的 icon 視覺圖都應該要參考 S-01-15 的『Ｘ關閉鍵』形式。」

S-01-15 是 `<button class="ic2" aria-label="設定">`——就是 #28-7 定的
**40×40 正圓＋一圈線＋20px icon**。

**現況查證**：全站還有 **3 個 `class="x"` 的關閉鍵**沒有跟上，
它們是 `24×24`、`background:#E2E5DE`、內含 **13px** 的 icon：

| 編號 | 在哪 |
|---|---|
| S-02-4 | 建立行程的 ✕ |
| S-02b-1 | 編輯行程的 ✕ |
| S-04-1 | 記一筆的 ✕ |

**改法**：這三個一律改用 `.ic2`（淺底那一套：白底＋`1px solid var(--ln)`＋
`var(--md)` 的 20px icon），可點區維持 ≥44×44。
**`.x` 這個 class 改完之後應該沒有人用了，一併刪除。**

原則寫死：**全站只要是「一顆 icon 就是一個按鈕」的東西，一律用 `.ic2`。**
不含列尾的 `›` 方向指示（那不是按鈕，見 #29-5）。

---

### 30-3　S-03-8 hero 的成員 emoji：拿掉

Rozi：「S-03-8 這裡為什麼會新增成員的 emoji？我不記得我在前面的討論有過這個需求…
而且如果在設定的時候沒有做 emoji 的話，這裡會呈現什麼樣子的狀態？應該會變得更奇怪。
所以我覺得這裡應該不用放成員的 emoji 才對。」

**先回答「為什麼會有」**——它不是新增的，有來歷：

- **2026-06-26 決策 D**：「空白行程只顯示成員 emoji，不顯示 NT$0」。
- **2026-08-31 裁示**：決策 D **由行程頁 hero 的成員 emoji 列來滿足**，
  空狀態不另外新增成員列。

**她的第二個疑慮是對的，而且是真的 bug**。現在的程式是
`t.members.map(m => m.emoji || firstGrapheme(m.name)).join('')`——
沒有 emoji 的成員會退回「名字第一個字」，而且**四個字直接串在一起、沒有間距、沒有圓底**。
四位成員都沒設 emoji 時，hero 上會出現「小阿美魚」這種看起來像亂碼的字串。

**改法：把成員那一段從 S-03-8 拿掉，hero 副標只留日期範圍。**

**這會連帶讓 2026-06-26 的決策 D 失效**（空白行程不再顯示成員）。
判斷是：**決策 D 的初衷是「空白行程看起來太荒涼」，而那個問題現在已經由
S-03-14 空狀態的引導文案解決了**（「第一筆從哪裡開始？早餐、計程車、門票，都可以記」
＋一顆記一筆）。有記帳之後，每一列本來就寫了誰付的，hero 上那排 emoji 沒有任何資訊價值。

**推翻方式**：如果 Rozi 仍要「空白行程看得到這趟有誰」，
就在 S-03-14 空狀態裡加回一行成員，**不要加回 hero**。

`_盤點_畫面功能.md` 的 S-03-8 與 S-03E 相關列都要同步，
並註明決策 D 已由空狀態引導取代。

---

### 30-4　S-07-4「我的資料」整段從稿上拿掉

Rozi：「S-07-4 前面的討論不是說這一個區塊先拿掉嗎？還是我們可以再確認一遍？」

**#25-9 的裁示是「稿上保留、標『即將推出』，Phase 1 上線時整段不渲染」**——
保留的理由只是「標示 Phase 2 的位置」。

**重新裁示：稿上也拿掉。** 理由：Phase 1 不會出這一段，
留著只會讓 Rozi 每次檢視都要重新想一次「這個到底要不要」——
已經發生兩次了。Phase 2 的位置等批四畫 S-08～S-11 時再標。

- **S-07-4 整段移除**（含通訊錄／卡片管理／帳單週期三列與「我的資料」小標）。
  編號保留、標為移除，不要重新編號。
- 移除後 **S-07 只剩：登入帳號（S-07-2／S-07-3）→ 登出（S-07-6）→ footer（S-07-7）**。
- **如果這樣的設定頁看起來太空，停下回報，不要自己加東西補位。**

---

### 護欄

- 不改 `src/`、不跑 migration、不刪檔。
- **不要重新編號**（S-07-4、`.x` 相關編號一律保留、只改狀態）。
- **#29 的十項成果全部不得回退**：真機模式、六階字級、三階顏色、`--gr` `#6A7980`、
  S-04 日期窄列、「可留空」退場、`statCard()` 共用、`.tabs` 與 `.seg` 分家、
  ⋯ 選單的 icon、操作模式下可見徽章為 0。
- 動 A 時若發現需要動 B，**停下回報，不得順手改**。

### 停止條件（機器判斷）

1. `.ui` 之內的文字**不含**「移除了 App icon」「識別由字標」「列表到此結束」
    「首頁沒有固定底部操作列」；且不含描述稿件本身的字眼（清單見 30-1）。
2. 全站**沒有 `class="x"` 的按鈕**；原本那三處（S-02-4、S-02b-1、S-04-1）
    改用 `.ic2`，實測 40×40、`border-width` 不為 0、內含 20px icon、可點區 ≥44×44。
    `.x` 的 CSS 定義已刪除。
3. `renderS03()` 的 hero 副標（S-03-8）**只含日期範圍**，
    **不含任何成員 emoji 或成員名字的字元**。
4. `renderS07()` 的輸出**不含**「我的資料」「通訊錄」「卡片管理」「帳單週期」
    「即將推出」。
5. #25／#27／#28／#29 的全部斷言維持通過；既有九組測試全綠。
6. 三個寬度（320／375／414）沒有橫向捲動、沒有文字左右緣 <14px；真機模式同樣通過。
7. 原型項目數與 `_盤點_畫面功能.md`、`_盤點_實作缺口.md` 逐一對得起來，無孤兒無缺漏。

### 交付

回報寫進 `_停點.md`（覆寫、標時間、≤50 行）。**最上面一樣要放手機預覽的網址。**

---

### 30-5　S-04 金額區：文案講反了因果，輸入框也太小

Rozi 在手機上實際操作後提出。**字級與密度她看過說 OK，不用調**，以下三項才要改。

#### 30-5a　「沒有台幣金額，結算會跳過這筆」——因果講反了

Rozi 原話：「現金匯率不一定會在建立行程的時候就輸入進去，
有可能到旅程都結束、要做結算的時候才輸入現金匯率。
這樣子的話，『我沒有台幣金額』這句話聽起來會有點讓人疑惑：那這樣到底有沒有記錄的必要？」

**她指出的是一個真正的邏輯錯誤**：這句話把一個**暫時的、隨時可以解除的狀態**，
寫成了**永久的判決**。使用者讀到「會跳過這筆」，合理的反應就是懷疑記它有沒有意義——
但事實正好相反：**這筆已經被完整記下來了，只是在補上台幣金額之前不列入結算計算，
補上就會進去。**

**文案原則（寫進 `CLAUDE.md`）**：
**未定案狀態的提示，一律用「補上就會⋯」的可行動語氣，不用「會被跳過／不算」的判決語氣；
而且要先讓使用者知道「這筆已經記下來了」。**
使用者最怕的不是算不出來，是**以為自己記的東西不見了**。

**改成**：

| 狀態 | 現在 | 改成 |
|---|---|---|
| 這趟完全沒設匯率（`needRateLink`）| 還沒設這趟的匯率，算不出台幣。去設定 › | **先記著了。設好這趟的現金匯率就會自動換算。去設定 ›** |
| 這筆沒有台幣金額（`twdPending`）| 沒有台幣金額，結算會跳過這筆 | **先記著了。補上台幣金額就會算進結算。** |
| 有匯率、正常 | 填一邊就好，另一邊會自動換算 | **不變** |

**同一句字串在 S-03 消費列的 `sub2`（原型約第 2053 行）也出現一次，一併改成同一句。**
兩處**必須引用同一個字串常數**，不要各寫一份——這是「改 A 不准換 B」的結構性做法。

**如果某個狀態組合套上新文案會變得不通順（例如同時沒匯率又沒外幣金額），
停下回報是哪一種組合，不要自己再造第四句。**

#### 30-5b　金額輸入框太小、兩列太近

**實測**：`.amtline` 高 **39px**、內距 `7px 10px`、兩列間距 **7px**。
**39px 低於 44px 的最小觸控標準**，而金額是整張表單最重要的輸入。

**改成**：`.amtline` 內距加到 `12px`（上下），實測高度 **≥48px**；
`.amtstack` 的 `gap` 由 7px 加到 **10px**。
**只改這兩個值，不要順手動 `.fld`、`.chip`、`.seg` 或其他欄位的間距。**

#### 30-5c　整列都要能點，不是只有右邊的數字

Rozi：「我希望它不是只有點擊右側的金額才會跳出輸入框，而是點整欄的時候就可以跳出。
例如我手指按到『台幣』的時候，就可以直接輸入數字，這樣才能放大點擊範圍。」

**做法：把 `.amtline` 從 `<div>` 改成 `<label for="e-for">`／`<label for="e-twd">`。**
原生的 label 會把點擊轉給對應的 input，**不需要寫任何 JavaScript**，
也不會有「點了沒反應」的邊界情況。左側的幣別標籤（`.cur`）自然一起變成可點區。
`.amtline` 加 `cursor:text`。

**同樣的做法要套到「各自多少？」的逐人金額列**（S-04-15／S-04-32 那一區）——
那裡是同一種「標籤在左、輸入框在右」的結構，同一個問題。
**如果那一區的結構不同、套 label 會壞掉，停下回報，不要硬套。**

---

### 30-6　S-03-12 統計卡的幣別提示：講的是「換算不了」，其實是「總花費不完整」

Rozi：「『這趟還沒設現金匯率，換算不了』這句的意思應該不是『還沒設定現金匯率就換算不了』，
而是說它的統計會不夠完整。因為如果我還沒有設定現金匯率的話，
有可能我在付外幣現金的時候，沒有辦法轉換成台幣金額。」

**她說對了。** 現在這句只描述了系統的處境（我算不出來），
**沒有講出對使用者的後果**——上面那個「總花費 $34,375」**是少算的**，
而使用者不會知道少算了多少、少算了哪幾筆。這比看不懂還危險：
**一個看起來精確、實際上不完整的數字。**

**改成——把後果、規模、解法一次講清楚**：

| 檢視 | 現在 | 改成 |
|---|---|---|
| 台幣（預設）| 這趟還沒設現金匯率，換算不了 | **有 N 筆還沒換算成台幣，上面的總花費不含它們。設現金匯率 ›** |
| 外幣（KRW 等）| （目前看不到）| **有 N 筆只有台幣金額，上面的總花費不含它們。** |
| 沒有缺口時 | — | **整條不顯示**（不要顯示「都換算好了」這種沒有作用的話）|

- **N 要是真的筆數**，不是寫死的字。
- 台幣檢視的那條**帶「設現金匯率 ›」的連結**，指向 S-02b 的匯率設定；
  外幣檢視**不帶連結**（設了匯率也補不出外幣金額——那些消費本來就只有台幣）。
- **這兩句是對稱的**：沒有匯率時，台幣檢視少算「只填外幣的」、
  外幣檢視少算「只填台幣的」；**有匯率時兩邊都完整，兩條都不顯示。**
  這個對稱關係寫進 `規格_金額未定案與幣別.md`。

**Rozi 另外指出：「我現在這一個區塊看不到點擊外幣的時候會長什麼樣子。」**
——原型的外幣分頁**要真的能切**，切過去之後：
總花費以外幣呈現、每人分擔列以外幣呈現、上表的外幣提示照規則出現。
**目前是不是點了沒反應，先查證再改；如果是刻意沒做，在回報中說明原因。**

---

### 停止條件（30-5／30-6 追加）

8. 全站搜尋 **「結算會跳過這筆」出現次數為 0**、**「換算不了」出現次數為 0**。
9. S-04 的三種金額提示與 S-03 消費列的 `sub2` **引用同一個字串常數**
   （測試方式：兩處輸出的字串完全相同，且原始碼中該句只寫了一次）。
10. `.amtline` 實測高度 **≥48px**；`.amtstack` 的 `gap` 為 **10px**；
    `.fld`／`.chip`／`.seg` 的間距與本節修改前**完全相同**。
11. `.amtline` 的標籤是 `<label>` 且 `for` 指向該列的 input；
    **模擬點擊 `.cur`（左側幣別文字）之後，`document.activeElement` 是該列的 input**。
    「各自多少？」的逐人金額列同樣通過這一條（或已回報無法套用的理由）。
12. 統計卡幣別提示：沒有缺口時**整條不存在**；有缺口時字串含真實筆數，
    且台幣檢視含連結、外幣檢視不含連結。
13. 原型可以實際切到外幣檢視，切換後總花費與每人分擔列**都以外幣呈現**。

---

### 30-7　Cowork 端補驗 #29 的結果

**#28 我有自己重跑，#29 沒有——這是漏掉的一步，現在補上。結論：#29 乾淨，13 項全過。**

實測（無頭 Chrome，操作模式）：

| 項目 | 結果 |
|---|---|
| 「可留空」全站 | **0 次** |
| S-04 日期窄列 | 在 S-04-1 之後、S-04-2 之前；含「記在」、不含「日期」；高 18px，遠低於欄位列的 60px |
| `--gr` | **`#6A7980`** |
| placeholder 顏色 | 7 個 placeholder **全部是 `rgb(106,121,128)`＝`--gr`** |
| seg 未選＝chip 未選 | 兩者皆 `rgb(76,91,100)` |
| 三階層級 | 內容 0.011 ＜ 可點未選 0.099 ＜ 提示 0.183 ✓ |
| 字級 | `.ui` 內實際只出現 **12／13／15／16／17／20／26／44**，沒有任何 <12px |
| 輸入框 | 全部 **≥16px** |
| 操作模式可見徽章 | **0** |
| `.tabs` vs `.seg` | class 不同、選中背景一白一藍 |
| S-06 | 無「人均」、有可展開的總花費、無「點名字看是哪幾筆」、每人列不可點 |
| 真機模式（iPhone 390×844 模擬）| `.ui` 寬 **390＝視窗寬**、外框與圓角皆 0、稿件元素 0 個、一次一個畫面、無橫向捲動 |

**一項要補記的例外**：S-00 的字標「Tripay」是 **44px**，不在六階之內。
**這是對的，不要改**——字標是圖形識別，不是內文，本來就不受字級階梯約束。
**把這一條寫進字級規則的例外**：`.ui` 內唯一允許超出六階的是 S-00 的品牌字標。
往後的斷言要把它排除，不要有人「順手修正」把它縮到 20px。

**另外提醒終端機**：`.top`／`.scrhd`／`.rig`／`.idx`／`#devtoggle`／`.modesw`／`.bdg`
這些**稿件外殼**的 CSS 裡仍有 8.5～14.5px 的寫死字級，
**那是對的、不要動**——它們不在 `.ui` 之內，是給看稿的人用的。

### 停止條件（30-7 追加）

14. 字級斷言把 **S-00 字標**列為明確例外；除它之外 `.ui` 內非金額文字仍只落在
    {12,13,15,16,17,20}。
15. 稿件外殼（`.top`／`.scrhd`／`.rig`／`.idx`／`#devtoggle`／`.modesw`／`.bdg`）
    的字級**與本節修改前完全相同**。

---

## #31 手機上鍵盤彈出且關不掉 ＋ 真機模式的返回鍵沒有去處 [已處理]

Rozi 用手機開真機模式時回報：**點了 hero 左上角的圓形返回鍵，鍵盤就跳出來，而且關不掉。**

**本節只改 `Tripay_原型.html`，不改 `src/`、不跑 migration、不刪檔。
#25／#27／#28／#29／#30 的成果一律不得回退。**

---

### 31-1　Cowork 端查證的結果：重現不出來，但找到一個真的地雷

**重現嘗試**（無頭 Chrome、iPhone 390×844 模擬、真機模式）：

- S-03 在真機模式下**沒有任何可見的 `input`／`textarea`／`select`**（實測 0 個）。
- 點 hero 的返回鍵後，`document.activeElement` 是那顆 `<button>`，**沒有任何輸入框取得焦點**。
- 底部畫面切換器是一排 `<button>`，不是輸入元件。

**所以在這個環境重現不出來。** 無頭瀏覽器也沒有真的軟體鍵盤，
本來就測不出 iOS 會不會彈鍵盤——**這一條不要當成「沒問題」的結論**。

**但查碼時找到一個真的地雷，不管是不是這次的原因都要修**：

**原型第 874 行**，就地編輯用的輸入框帶著 `autofocus` 屬性：

```
return `<input type="text" data-avin="${key}" value="" maxlength="4" autofocus>`;
```

**`autofocus` 綁的是「元素被放進 DOM」，不是「使用者要編輯」。**
只要有任何一次重繪把這個輸入框重新插回畫面，瀏覽器就會再聚焦一次，
在手機上就是**鍵盤再彈一次**——使用者關掉、它又跳出來，正好是「關不掉」的形狀。

而且**下一行本來就有明確的 `.focus()`**（`beginInlineEdit()` 裡的
`const inp = document.querySelector(...); if (inp) inp.focus();`），
所以 `autofocus` 這個屬性是**多餘的**，只帶來風險。

**第二個地雷在第 2565 行**：

```
const keep = document.activeElement;
...
if (keep && document.contains(keep)) keep.focus();
```

這是「重繪後把焦點還回去」。在桌機上是好事，
**在手機上，只要重繪的時機不對，就會把已經收起來的鍵盤再叫出來。**

---

### 31-2　要做的三件事

1. **拿掉第 874 行的 `autofocus` 屬性。** 聚焦一律由 `beginInlineEdit()` 裡的
   `.focus()` 負責——那才是「使用者真的要編輯」的時機。
2. **`keep.focus()` 加條件**：只有在**重繪前後都是同一個輸入框、而且它原本就是聚焦狀態**時
   才還原焦點；`keep` 不是輸入元件時（例如是 `<button>`）**一律不呼叫 `.focus()`**。
3. **全站掃一遍還有沒有別的地方會在使用者沒要求的情況下聚焦輸入框**
   （`autofocus`、`.focus()`、`.select()`、`click()` 觸發的間接聚焦），
   找到就列在回報裡，逐一說明是不是使用者主動觸發的。

---

### 31-3　真機模式的返回鍵沒有去處（可能是她一直點的原因）

**實測**：真機模式下，點 hero 的返回鍵 **`‹`，畫面完全沒有變化**——
還是停在 S-03。因為真機模式一次只顯示一個畫面，而返回鍵沒有接上任何導覽。

**使用者點了沒反應，合理的反應就是再點幾次**——這很可能就是 Rozi 反覆點擊的原因。

**改法（擇一，你判斷哪個對看稿的人比較好用，並在回報說明理由）**：

- **A**：真機模式下，返回鍵接上「回到上一個看過的畫面」（維持它看起來是產品的一部分）。
- **B**：真機模式下，返回鍵改為**視覺上仍在、但明確不可點**（`pointer-events:none`
  ＋降低不透明度），並在底部切換器旁註明「返回鍵在稿上不作用」。

**不要兩個都做，也不要讓它繼續「看起來可點、點了沒反應」**——
那是最糟的一種，既給了承諾又不兌現。

---

### 31-4　回答 Rozi 的問題：「實際上線時也會變這樣嗎？」

**在回報裡用白話回答她這一句**，內容如下（可自行潤飾，但意思不要跑掉）：

原型和正式產品是兩份不同的程式，**這個原型上的行為不會直接跑到產品上**。
但「就地編輯要自動聚焦」這件事，產品上一定會做，
而原型現在用的做法（`autofocus` 屬性）是危險的——
它綁在「元素被放進畫面」而不是「使用者要編輯」，**重繪一次就再彈一次鍵盤**。
所以現在就把它改掉，免得實作時照抄過去。

---

### 護欄

- 不改 `src/`、不跑 migration、不刪檔。
- **不要為了避免鍵盤問題就拿掉就地編輯的自動聚焦**——
  使用者點了圓圈就是要打字，那個聚焦是對的，要改的是**觸發時機**。
- **不要改任何文案、版面、字級、顏色。** 這一節只處理焦點行為與返回鍵。
- 動 A 時若發現需要動 B，**停下回報，不得順手改**。

### 停止條件（機器判斷）

1. 原始碼中 **`autofocus` 出現次數為 0**。
2. 模擬 iPhone（390×844、`pointer:coarse`）載入後，
   **`document.activeElement` 是 `body`，不是任何 `input`／`textarea`**；
   逐一切過真機模式的每一個畫面之後仍然如此。
3. 點擊 hero 的返回鍵、`⋯`、分頁、底部主鈕之後，
   **`document.activeElement` 都不是 `input`／`textarea`**。
4. 主動觸發就地編輯（點成員識別圓圈、點 S-04-3 類別 emoji）之後，
   **`document.activeElement` 確實是該列的輸入框**——自動聚焦的正常行為不得被改壞。
5. 就地編輯開啟後**呼叫一次重繪**，焦點仍在同一個輸入框（不得跳走、也不得重複聚焦）。
6. 真機模式下點返回鍵：**要嘛畫面真的變了（方案 A），要嘛該元素的
   `pointer-events` 為 `none`（方案 B）**——不得「可點但沒反應」。
7. #25／#27／#28／#29／#30 的全部斷言維持通過；既有十組測試全綠。

### 交付

回報寫進 `_停點.md`（覆寫、標時間、≤50 行），最上面照舊放手機預覽網址，
並且**要有一段白話回答 31-4 那個問題**。

---

### 31-5　圖示按鈕的底色要透，不要實色（全站統一）

Rozi 對照行程頁與記一筆兩頁後指出：
「為什麼我要的是像行程頁那樣，icon 可以透出底下的底色，
但是消費記錄頁這一個 icon 看起來底色卻是白色的？
請幫我把全站的 icon 都做統一，全部都要像行程頁那樣可以透出底色。」

**這是 Cowork 端 #28-7 的規格寫錯**。當時我定了兩套：
深色 hero 上用半透明黑（會透出漸層，就是她要的），
**淺色底上卻寫成「白底 `#fff`」——那是實色，不會透。**
所以 `記一筆`／`建立行程`／`編輯行程` 的 ✕ 變成一顆貼在畫面上的白色貼紙，
跟 hero 上那兩顆是兩種材質。

**改成——一條規則、兩個值**（不是「兩套樣式」）。
規則只有一句：**容器永遠是「底下顏色的薄膜」**；底暗就加亮、底亮就加暗。
之所以必須有兩個值，是因為底下的明暗相反，同一個半透明值在其中一邊一定會消失：

| | 深色底（hero 的 `‹`／`⋯`）| 淺色底（表單的 ✕、S-07 的 `‹`、S-01 的設定）|
|---|---|---|
| 填色 | `rgba(255,255,255,.14)` | `rgba(0,0,0,.05)` |
| 邊框 | `1px solid rgba(255,255,255,.38)` | `1px solid rgba(0,0,0,.14)` |
| icon 顏色 | `#fff` | `var(--md)` |
| 尺寸 | 40×40 正圓、icon 20px、可點區 ≥44×44（不變）| 同左 |

兩者都是**半透明**，所以不管底下是漸層、`#EDEEE8` 的表單底、還是白色卡片，
**都會透出底下的顏色**——這就是她要的一致。

**深色那一套現在是 `rgba(0,0,0,.28)`（黑色薄膜）**。
改成白色薄膜之後如果在某些目的地色調上對比不夠（icon 看不清楚），
**停下回報是哪一個色調，不要自己改回黑色或加陰影**。

**要確認的**：淺色那一套疊在**白色卡片**上時仍然看得出邊界
（`rgba(0,0,0,.05)` 疊在 `#fff` 上是 `#F2F2F2`，靠 `rgba(0,0,0,.14)` 的邊框撐）。
若實測某一處看不出來，回報，不要自己加深。

**只改這兩套容器的填色與邊框，不改尺寸、不改 icon、不改任何其他元件。**

#### 停止條件（31-5 追加）

8. 全站所有 `.ic2` 的 `background-color` **都是半透明**
   （computed 值的 alpha < 1，不得是 `rgb(...)` 或 `#fff`）。
9. 深色底與淺色底兩套的 `background-color` 與 `border-color` 各自一致，
   且**全站只有這兩種組合**。
10. 白字／`--md` icon 對各自實際底色的對比度 **≥ 3:1**
    （圖形元素的下限），逐一在 hero 的 8 種目的地色調上量過。
11. `.ic2` 的尺寸仍為 40×40、icon 20px、可點區 ≥44×44。

---

### 31-6　⋯ 選單的項目沒有撐滿寬度（分隔線長短不一、可點區只有文字寬）

截圖給 Rozi 看紅色對照時發現的，**她沒提，但這是真的 bug**。

**實測**：選單面板寬 **388px**，但每一項的 `<button>` 只有內容寬度——
`編輯行程` **86px**、`分享` **56px**、`複製成新的一趟` **131px**、`刪除行程` **86px**。
每顆按鈕自己帶 `border-bottom:1px`，所以畫面上就出現**四條長度不一的短線**，
看起來像沒對齊的破版。

**這是 #25-7 那個 bug 的同一個家族**（`b()` 的包層讓按鈕收縮成內容寬），
但後果多了一層：**可點區只有文字那麼寬**。
選單項目在手機上應該**整列可點**，現在點到「分享」右邊的空白處是沒反應的。

**改法**：

1. 選單項目的按鈕 **`width:100%`**（或整列 `display:block`），讓它撐滿面板。
2. **分隔線不要掛在按鈕上**——改由面板負責：
   依 28-6b 的分組，只在 **編輯／分享 之後**、**複製 之後** 各畫一條**滿版**分隔線，
   其餘項目之間不畫線。現在這種「每一項都有一條自己寬度的線」是錯的。
3. **可點區整列**：高度 ≥44px，左右滿版。

**順便全站再掃一次同一種毛病**（按鈕或可點列沒有撐滿容器、導致可點區小於視覺區）：
用 #25-7 已經寫進測試的那條掃描，**把 `.sheet`／`.dlg` 內的項目也納入範圍**。

#### 停止條件（31-6 追加）

12. ⋯ 選單內每一個動作項目的 `getBoundingClientRect().width`
    **等於面板內容區寬度**（容差 2px）；高度 **≥44px**。
13. 選單內帶 `border-bottom` 的元素**只有分組分隔線**，且其寬度為滿版；
    分隔線數量符合 28-6b 的分組（旅遊中 2 條、已結算 2 條、已封存 2 條——
    依實際分組計算，回報寫明）。
14. 全站掃描：`.sheet`／`.dlg` 內的可點元素**沒有任何一個寬度小於其容器的 70%**
    （沿用 #25-7 的掃描，擴大範圍）。

---

### 31-7　列尾 `›` 維持裸 icon（Rozi 已確認）

Rozi 對「全站 icon 統一」的範圍給了明確界線：
**「你說的兩種選項的兩個層級不一樣，所以不應該『統一』。」**

**定案**：
- **「一顆 icon 就是一個按鈕」的東西** → 一律 `.ic2`（40×40 正圓＋半透明薄膜＋20px icon）。
- **列尾的 `›` 方向指示** → **維持裸 icon、不加容器、16px**。
  可點的是整一列，44px 觸控標準由整列滿足；加容器會讓它看起來像另一顆按鈕。

這一條寫進 `CLAUDE.md` 的視覺規則，往後不要再有人「順手統一」把 `›` 也包進容器。

---

### 31-8　`.tabs` 與 `.seg` 合併成一套（Rozi 裁示）

Rozi：「分頁與幣別切換的樣式，統一改成『消費／結算』的顯示。」

**#29-8b 把它們拆成兩套是 Cowork 的判斷，Rozi 沒有要求過，現在收回。**

- **`.seg` 全部改成 `.tabs` 現在的樣子**：淺灰底軌（`#E2E4DD`）＋選中格為白色凸起 pill＋深字，
  未選為 `--md`。**不再有實心主色的切換器。**
- **兩個 class 合併成一個**，不要留下兩個長得一樣的 class。
  保留哪一個名字你決定，另一個從 CSS 與所有用到的地方移除。
- 影響到的地方至少有：S-03-33 消費／結算、S-03-12 幣別切換、
  S-02b 結算模式（誰欠誰就轉給誰／都轉給同一個人）、S-04-31 填哪一邊。
  **全站掃一遍，不要漏。**
- **停用態要照顧到**：幣別切換在「還沒設匯率」時 KRW 那格是停用的，
  換成新樣式後**仍然要一眼看得出不能按**（不要只靠淡一點的字）。
  做法你決定，但要在回報中說明。

---

### 31-9　圓角統一：做一個切換器讓 Rozi 挑

Rozi：「可以確認所有的圓角矩形按鈕的圓角都統一一個樣式嗎？請給我幾個版本做挑選。
電腦用跟手機用的原型版本都要修改，我會用電腦跟手機各看一次。」

**現況（Cowork 端實測，`.ui` 之內）**：圓角有 **8 個不同的值**——
`2px`（img／stamp／grab，29 處）、**`3px`（279 處，主力：按鈕、卡片、輸入列、欄位列）**、
`4px`（Google 登入鈕、對話框）、`6px`（分頁的 pill）、`8px`（分頁底軌）、
`14px`（底部彈出面板上緣）、`50%`（`.ic2` 圓形）、`999px`（chip 膠囊）。

跟字級當初一樣：**沒有階梯，是各自決定出來的。**

**要做的是一個「圓角版本切換器」**，做法完全比照 #27-3 的 icon 對照條——
按一下就整站換掉，讓 Rozi 直接在真的畫面上比，不是看色塊。

**六個版本**。A～D 之中，`.ic2` 的 `50%` 與 chip 的 `999px` 維持不變（那是形狀不是圓角）；
**E 與 F 是 Rozi 特別要求要看的極端值**，會把 `--r-base` 本身推到膠囊與橢圓：

| 版本 | 按鈕／卡片／輸入框／列 | 面板（底部彈出、對話框）| 個性 |
|---|---|---|---|
| **A · 現況收斂** | **3px** | **14px** | 幾乎是直角，像紙、像票根，最貼近「日記精裝」 |
| **B · 微圓** | **6px** | **16px** | 柔一點，仍然克制 |
| **C · iOS 原生感** | **10px** | **20px** | 與 iOS 系統元件同一個手感，最像原生 App |
| **D · 圓潤** | **14px** | **24px** | 明顯圓、親和，但會削弱「紙」的質感 |
| **E · 膠囊** | **999px** | **28px** | 按鈕與輸入框變成兩端全圓的膠囊，最軟、最像近年的消費型 App |
| **F · 全圓 50%** | **50%** | **50%** | Rozi 指定要看。**注意：`50%` 用在非正方形元素上會變成橢圓，不是圓角矩形**——寬 360、高 49 的按鈕會變成一顆扁橢圓 |

**F 的橢圓是 `border-radius:50%` 的正常行為，不是 bug。**
`50%` 之所以只用在 `.ic2`，正是因為那個元素是 40×40 的正方形。
**不要為了讓 F「看起來正常」而自作主張改成 `999px` 或加限制**——
Rozi 要看的就是它真實的樣子。若某些元素在 F 之下高度被壓垮或文字溢出，
**照樣呈現、在回報中列出是哪幾處**，不要偷偷修。

**E 與 F 之下，`.ic2` 與 chip 也跟著 `--r-base` 走**
（否則就看不出「全部都圓」是什麼樣子）——這一點與 A～D 不同，要在切換器上標明。

**實作要求**：

1. **全站圓角一律改用 CSS 變數**（`--r-base` 與 `--r-panel` 兩個就夠），
   切換器只換這兩個變數的值。**原始碼裡不得再有寫死的 `border-radius: Npx`**
   （`50%` 與 `999px` 除外；稿件外殼 `.top`／`.scrhd`／`.rig`／`.idx` 不受此限）。
2. **上面列的 8 個值要全部收斂進去**：`2px` 與 `4px` 併入 `--r-base`；
   `6px`／`8px`（分頁）也用 `--r-base`（底軌與 pill 可差一點，
   用 `calc(var(--r-base) + 2px)` 之類，**不要再寫死**）。
3. **切換器在桌機與真機模式都要看得到、按得到**：
   - 桌機：放在稿頭，與既有的模式切換並列。
   - **真機模式：放進底部那條「真機模式」列裡**（它已經有「收起清單」，
     旁邊加一排 A／B／C／D 即可）。**不要讓它擋住被檢視畫面的底部按鈕。**
4. **記住選擇**：切了版本再換畫面，版本不能跑掉。
5. 預設仍為 **A（現況）**，Rozi 挑完再定案。
6. **切換器上要標出每個版本的實際數值**（例如「C · 10 / 20」），
   Rozi 挑完才知道自己選的是什麼，也方便她說「C 再小一點」。

**這是拋棄式的工具**，Rozi 選定之後把切換器移除、把選中的值寫死成變數預設值。
**在回報裡提醒她這件事**，不要留著長期存在。

#### 停止條件（31-8／31-9 追加）

15. 全站**只有一種切換器樣式**：`.tabs` 與 `.seg` 已合併，
    另一個 class 在 CSS 與所有用法中都不存在；
    合併後所有切換器的選中格背景**皆為白色**、**沒有任何一個是主色實心**。
16. 幣別切換的停用態在新樣式下仍可辨識（回報說明做法，並附實測的對比度）。
17. A～D 之下，`.ui` 之內的 `border-radius` computed 值**只會出現四種**：
    `--r-base` 當前值、`--r-panel` 當前值、`50%`（`.ic2`）、`999px`（chip）。
    **E 與 F 之下，`.ic2` 與 chip 也跟著 `--r-base`**，因此只會出現兩種值。
18. 原始碼中 `.ui` 範圍內**沒有寫死的 `border-radius: Npx`**（`50%`／`999px` 除外）。
19. **六個版本各切一次**，每次都：三個寬度無橫向捲動、無文字貼邊、
    既有全部斷言維持通過。**F 之下若有元素破版，允許破版但必須在回報中逐一列出**
    ——那是 Rozi 判斷的依據，不是要你修掉的缺陷。
20. 真機模式下切換器可見且可按，**不遮住畫面底部的主要按鈕**；
    切換版本後再切畫面，版本設定仍保留。

---

## #32 深色薄膜改回半透明黑（Cowork 又改錯了一次）＋ 一個 1px [已處理]

**本節只改 `Tripay_原型.html`，不改 `src/`。#25～#31 的成果一律不得回退。**

---

### 32-1　hero 的 icon 底色改回半透明黑

終端機回報：改成白色薄膜之後，**深色 hero 那一套在 5 個目的地色調上對比不足 3:1**
（沖繩 1.88／台灣 2.12／東京 2.66／福岡 2.73／沖繩另一處 2.93），
它依指令只回報、沒有自己改。**回報是對的，但這個問題不該由調色調來解——是 Cowork 改錯了。**

**回頭看 Rozi 的原話**：

> 「為什麼**我要的是像行程頁那樣，icon 可以透出底下的底色**，
> 但是消費記錄頁這一個 icon 看起來底色卻是白色的？」

**她說行程頁（hero）那一套是對的。** hero 原本就是 `rgba(0,0,0,.28)`——
**半透明黑一樣是薄膜，一樣透得出底下的漸層。**
她要修的是**淺色底那顆實心白**，不是 hero。

**#31-5 我把深色那套從半透明黑改成半透明白，是多做的、而且做反了**：
白色薄膜會把底色提亮，而 icon 本身就是白的——**兩個都變亮，對比只會更差**。
5 個色調不足就是這樣來的。

**改法**：

| | 深色底（hero 的 `‹`／`⋯`）| 淺色底（表單 ✕、S-07 返回、S-01 設定）|
|---|---|---|
| 填色 | **改回 `rgba(0, 0, 0, .28)`** | **維持 `rgba(0, 0, 0, .05)`** |
| 邊框 | **改回 `1px solid rgba(255, 255, 255, .35)`** | 維持 `1px solid rgba(0, 0, 0, .14)` |
| icon | `#fff` | `var(--md)` |

**規則本身不變，而且現在更純粹**：兩套都是**半透明黑薄膜**，
差別只在底暗時用白線描邊、底亮時用黑線描邊。**填色的邏輯統一成同一個方向了。**

**改完要重量一次那 8 個色調的對比**，回報實測值。
若仍有色調不足 3:1，**這時候才回來討論色調本身**——但預期不會，
因為這就是 Rozi 原本看過並認可的那一套。

---

### 32-2　⋯ 選單的「取消」鈕高 43px

Cowork 端複驗實測：選單四個動作項目都是 **44px**，只有最下面的「取消」是 **43px**。
差 1px，但 44 是觸控下限，補齊即可。**只改這一顆的高度，不要動其他間距。**

---

### 護欄

- 不改 `src/`、不跑 migration、不刪檔。
- **圓角切換器維持不動**（Rozi 還在挑，六個版本都要留著）。
- **淺色底那一套不要動**——她要修的就是那顆，已經修對了。
- #25～#31 的全部斷言維持通過。

### 停止條件（機器判斷）

1. `.ic2` 在深色 hero 上的 `background-color` 為 `rgba(0, 0, 0, 0.28)`、
   邊框為 `rgba(255, 255, 255, 0.35)`；淺色底那一套維持 `rgba(0, 0, 0, 0.05)` /
   `rgba(0, 0, 0, 0.14)`。**兩者的 alpha 皆 < 1（仍是薄膜）。**
2. 8 個目的地色調下，白色 icon 對按鈕實際落點底色的對比度**皆 ≥3:1**，
   回報附實測值。
3. ⋯ 選單內**每一個**按鈕（含「取消」）的高度 **≥44px**。
4. 圓角切換器仍有 6 個版本，桌機與真機模式都可用，切換後換畫面不跑掉。
5. #25～#31 的全部斷言維持通過；既有十一組測試全綠。

---

### 32-3　圓角定案 C，但切換器**先留著**

Rozi 在真機上比過六個版本，**選 C**：`--r-base:10px`／`--r-panel:20px`。

- 把 **C 設為預設值**（`:root` 的 `--r-base:10px; --r-panel:20px`）。
- **切換器不要移除。** Rozi：「切換器先不要移除，我想等這輪修改再挑選一次。」
  六個版本全部保留，等她這一輪看完再定。

---

### 32-4　圓角變大之後，左右內距太窄

Rozi：「如果選擇圓角 C，目前會覺得左右的元素（文字或 emoji）都太貼近邊框了，
這部分可以再全站微調 X 軸嗎？或許可以往中間移動 2-4px。」

**她說的是對的，而且有明確的原因**：**圓角越大，角落的曲線越往內吃**，
內距沒有跟著加，文字與 emoji 就會貼在曲線上。
實測目前主要的內容容器全部是 **左右內距 10px 配 10px 圓角**——
內距剛好等於圓角，等於完全沒有餘裕：

| 元件 | 現在 | 圓角 |
|---|---|---|
| `.rowb`（成員列、消費列、設定列，18 處）| 10px / 10px | 10px |
| `.amtline`（金額輸入列）| 10px / 10px | 10px |
| `.fieldrow`（欄位列）| 10px / 10px | 10px |
| `.txrow`（轉帳列）| 10px / 10px | 10px |
| `.tcard`（小卡）| 4px / 4px | 10px |

**改法**：上表這些**有圓角的內容容器，左右內距一律改為 14px**
（`.tcard` 由 4 改為 14）。取她區間的上限，而且 14 正好與全站既有的模組尺寸一致
（卡片距畫面邊緣、`.sec` 的內距都是 14）。

**同時寫成一條規則，往後改圓角不必再回頭調一次**：
> **有圓角的內容容器，左右內距至少要比圓角多 4px。**
> 圓角 10 → 內距至少 14。這是為了讓內容不要壓在角落的曲線上。

**不要順手改的**：垂直內距（上下）不動；卡片距畫面左右邊緣的 14px 不動；
`.seg`（2px，那是軌道與 pill 的間隙，不是內容內距）不動。

**內距加寬後每列可用寬度變窄 4px**，可能讓某些長文字換行——
**三個寬度都要重跑版面測試；若有換行變醜的，停下回報是哪一處，不要自己縮字級。**

---

### 32-5　S-00 登入頁：標語折行 ＋ 整個區塊偏上

Rozi：「這一句文案目前變成兩行，請讓它在一行，同時要注意下一個按鈕的寬度應該要做成多寬？
然後這一頁有一個最大的問題：整體從產品文案到按鈕，整個區塊在視覺上都是偏上的，
應該要讓這個區塊在視覺的中間。」

#### 實測

- 標語字級 **15px**（#29 從 13.5 放大），**單行需要 211px**，但共用欄寬鎖在 **200px**
  （#28-3 定的）——差 11px，所以折行。
- **S-00 的畫面容器高度是寫死的 `min-height:520px`**，在 844px 的手機上根本沒撐滿。
  **這才是「整體偏上」的根因**——不是對齊沒設好，是容器只有畫面的一半高，
  內容只能待在上半部。

#### 改法

**① 標語一行、欄寬 240**

- 共用欄寬 **200px → 240px**（單行需要 211，留餘裕；不同機型字寬有差，不能剛好卡住）。
- **標語與登入鈕維持等寬**（#28-3 的規則不變），兩者都是 240px。
- 加上限 `max-width: calc(100% - 48px)`，320px 小螢幕不會溢出（240+48=288 < 320）。
- 標語加 `white-space:nowrap` 保險：寬度不足時寧可縮，不要折行。

**② 版面：品牌在上、動作貼底**（Cowork 判斷，Rozi 可推翻）

- 容器高度**跟著可用高度走**，不要寫死 520。
  用 `100dvh`（動態視口高度，避開 iOS 網址列伸縮造成的跳動）；
  真機模式與桌機框內都要成立。
- 內容分三段用 flex 分配：
  **上留白（flex 1.2）／品牌區（字標＋slogan＋標語）／下留白（flex 1）／登入鈕（貼底）**。
  登入鈕距底部 **32px ＋ 安全區**（`env(safe-area-inset-bottom)`）。
- 這樣品牌會落在畫面約 **40%** 的高度——**光學中心，比幾何中心略高**。
  人眼看幾何置中會覺得偏低，這是排版的常識，不是隨便挑的數字。
- **為什麼不用單純垂直置中**：那會讓整頁唯一的動作（登入鈕）落在畫面正中，
  手機上拇指要往上伸；而且下方那片空白只是換個位置，沒有變成構圖的一部分。
- **塞不下時**（橫向、小螢幕、放大字級）：退回自然排列並可捲動，**不要硬撐或裁切**。

---

### 32-6　S-02-7「當地幣別」折成兩行

**實測**：`.fieldrow>.lbl` 固定 `width:58px`，但字級 15px 之下「當地幣別」四個字需要約 60px，
所以斷行（該標籤的高度 38px，其他標籤都是 23px）。**這是 #29 字級放大的後遺症。**

**改法**：

- `.fieldrow>.lbl` 的寬度改為 **68px**（容得下四個 15px 的中文字＋餘裕），
  **並加 `white-space:nowrap`**——寬度不足時寧可擠壓輸入框，也不要把標籤折斷。
- 所有 `.fieldrow` 的標籤欄仍要**等寬對齊**，不要改成自適應（那會讓各列左緣參差）。
- **順便掃一次全站**：還有沒有別的固定寬度容器因為 #29 的字級放大而裝不下內容。
  找到就一起修，並在回報中列出。

---

### 32-7　S-02-9 日期輸入框的文字對齊

**先講一件事**：Rozi 看到的「回程日期框跑版」，**成因是 32-6 的標籤折行**——
折行把下面「出發」那一列往上擠，看起來才像框歪了。
**實測兩個日期框其實一樣大（177×40）**，修好 32-6 這個錯位就會消失。

**但她說的「文字沒有水平垂直置中」是另一回事，那是真的**：
兩個日期框都是 `text-align:start`、`padding:9px 10px`。

**改法與限制**：

- 規則訂清楚：**一般文字欄位靠左、金額欄位靠右、日期比照一般文字靠左**，
  **垂直一律置中**。
- **技術限制要先知道**：iOS 的原生 `<input type="date">`，
  內部的文字排版**不吃一般的 CSS**，要 `-webkit-appearance:none` 才控制得住，
  但關掉之後會失去「這是日期欄位」的系統外觀線索。
- **請試出兩全的做法**（例如保留原生行為但用容器對齊、或自繪但補回外觀線索）。
  **試不出來就停下回報**，讓 Rozi 在「文字對齊」與「看起來像日期欄位」之間選，
  **不要自己選一邊做掉**。

---

### 32-8　S-02b-11 支付方式的 `⠿` 是假的

**實測**：那個 `⠿` 是一個**純文字字元**，沒有 `draggable`、沒有任何指標事件、
沒有任何拖曳程式。**它看起來可以拖，按了完全沒反應——這是假的操作暗示，比沒有更糟。**

而**排序本來就在範圍內**：2026-09-04 定案寫的是「這趟的支付方式（預設現金、信用卡，
**可自訂新增與排序**）」。所以這不是「刻意固定」，是**漏做**。

**要做的**：

- **只能從 `⠿` 起拖。** 整列起拖會跟畫面的垂直捲動打架——把手存在的理由就是這個。
- **按下立刻浮起**：陰影加深、約 `scale(1.03)` 微放大，讓人知道「抓住了」。
- **跟手移動**，其他列**即時讓位**（不是放開才重排）。
- **放開落位要有回饋**（短促的回彈或高亮），並更新順序。
- **觸控與滑鼠都要支援。**
- **新增的支付方式一樣可以拖**——這是 Rozi 特別問的。
- 順序要能存下來（原型用前端狀態即可；資料層是 `trips.payment_methods` 的陣列順序，
  **這一節不要寫 migration**）。

**若某些瀏覽器的觸控行為導致拖曳與捲動衝突解不掉，停下回報**，不要留一個半殘的版本。

---

### 32-9　S-06 分享頁的消費明細比照行程頁排版

Rozi：「在這個分享頁狀態，我不要日期寫在付款人的後面，這會讓我覺得這樣子的呈現方式
跟前面行程頁不一樣。分享頁的模式也請比照行程頁的排版。」

**現況**：分享頁每一列是「🐵 Rozi · 2026-02-10」——付款人後面接日期。
**行程頁**是用**日期分組標頭**（出發前／第 1 天 · 3/14（六）），列上只有付款人。

**改法**：分享頁的消費明細**改用與行程頁相同的日期分組排版**——
分組標頭照 S-03-15 的樣子，**列上只留付款人、不寫日期**。

**必須共用同一個元件／同一套產生邏輯**，理由與 `statCard()`、`transferView()` 相同：
兩處分開寫，遲早會走鐘。測試要比對兩處的分組標頭與列結構相同。

**分享頁仍是唯讀**：列不可點（沿用 #29-10 的既有規則），不得因為改排版而變成可點。

---

### 32-10　全域改動之後要逐頁看過（流程規則，寫進 `CLAUDE.md`）

Rozi：「我已經講過很多次，UX 都沒有在注重使用者體驗。」

**這幾輪她抓到的問題有一個共同點：都是全域改動之後沒有回頭逐頁看。**
#29 把字級放大三成之後，58px 的標籤欄會爆、200px 的標語會折行、
登入頁的區塊會被推得更偏上——**這三個都是同一次改動的可預期後果**，
只要改完把每一頁看一遍就會發現。

**根因**：既有測試守住了「不橫向捲動」「文字不貼邊」「對比夠不夠」，
但**沒有任何一條在問「這一頁看起來對不對」**。
**機器斷言抓得到壞掉，抓不到難看。**

**往後的規則（寫進 `CLAUDE.md`）**：

> 凡是動到全域的節次——**字級、圓角、顏色、容器樣式、間距**——
> 停止條件必須包含：**輸出全部畫面的截圖**
> （S-00～S-07 的每一個主要狀態，桌機與真機模式各一組），
> 放在 repo 外的暫存路徑並在 `_停點.md` 列出檔名。
> **Cowork 端要逐張看過才算通過**，不能只看測試數字。

**這一節（#32）就要照做**——它同時動到圓角、內距、版面，是典型的全域改動。

---

### 護欄（#32 全節）

- 不改 `src/`、不跑 migration、不刪檔。
- **圓角切換器不得移除**（Rozi 要再挑一次）。
- **不要動**：垂直內距、卡片距畫面邊緣的 14px、`.seg` 的 2px 間隙、
  淺色底那一套 icon 薄膜（已修對）、S-05 的內容與結構。
- #25～#31 的全部斷言維持通過。
- 動 A 時若發現需要動 B，**停下回報，不得順手改**。

### 停止條件（32-3～32-10 追加）

6. `--r-base` 預設為 `10px`、`--r-panel` 預設為 `20px`；**圓角切換器仍有 6 個版本且可用**。
7. `.rowb`／`.amtline`／`.fieldrow`／`.txrow`／`.tcard` 的左右內距**皆為 14px**；
   垂直內距與修改前相同。
8. 對每一個有圓角的內容容器：**左右內距 ≥ 圓角 + 4px**（圓角取 `--r-base` 的當前值；
   `--r-base` 為 `999px`／`50%` 的版本不列入此檢查）。
9. S-00：標語**單行不折**（其 `Range` 高度等於單行行高）；
   標語與登入鈕**寬度相等且為 240px**；
   兩者在 320px 寬時仍不溢出。
10. S-00 容器高度**跟隨可用高度**（模擬 844 與 667 兩種高度，容器高度不同且皆填滿）；
    登入鈕底緣距容器底 **32px ± 2**（不含安全區）；
    品牌區的垂直中心落在容器高度的 **35%～45%** 之間。
11. `.fieldrow>.lbl` 寬度為 **68px** 且 `white-space` 為 `nowrap`；
    **S-02 的五個標籤沒有任何一個折行**（`Range` 高度等於單行行高）。
12. 兩個日期輸入框：文字**垂直置中**（文字盒的中心與輸入框中心相差 ≤2px）、
    水平靠左；或已在 `_停點.md` 回報做不到的理由與取捨選項。
13. `⠿` 是**可拖曳元素**：具備指標事件處理；模擬從把手拖曳可改變順序；
    整列的其他區域**不會**觸發拖曳。新增一個支付方式後同樣可拖。
14. `renderS06()` 的消費明細：**含日期分組標頭**、**列上不含日期字串**；
    與 S-03 的分組標頭與列結構**由同一段邏輯產生**；列**不可點**。
15. **逐頁截圖已輸出**（S-00～S-07 各主要狀態 × 桌機／真機模式兩組），
    檔名列在 `_停點.md`。
16. 三個寬度無橫向捲動、無文字貼邊；#25～#31 的全部斷言維持通過；既有測試全綠。

---

## #33 Rozi 手機實測回饋（4 項）＋ Cowork 複驗發現（2 項）＋ 圓角定案 [已處理]

**本節只改 `Tripay_原型.html` 與盤點文件，不改 `src/`、不跑 migration、不刪檔。
#25～#32 的成果一律不得回退。**

> **這一節動到全域（提示句字級、圓角寫死），依 #32-10 的規則，
> 停止條件包含「輸出全部畫面截圖」，Cowork 端會逐張看過。**

---

### 33-1　S-03 與 S-06 的日期分組改成倒序

Rozi：「行程頁跟分享頁的排序應該要顛倒過來。以出發前來說，它應該會變成最後一頁。
再來是第一天、第二天由此往上推，所以最上面會是今天的日期。不然就會變成我今天填的，
如果今天是第三天，我填了今天的款項，今天的帳反而要滑到最下面才會出現。」

- **日期分組由新到舊**：最後一天在最上、「出發前」沉到最底。
- **同一天之內的多筆消費，依「記錄的時間」（`created_at`）由新到舊排。**
  這一條是 Cowork 補的——只倒轉分組還不夠，
  **要讓剛記完的那一筆出現在整頁最上面**，才真的解決她說的「要滑到最下面」。
- **S-03 與 S-06 兩處都改，而且共用同一段排序邏輯**（`expenseGroups()` 已經是共用的）。
- 「還沒算清楚」的橫幅（S-03-29）不屬於任何日期組，**維持在列表最上方**。
- **只改顯示順序，不動任何計算。**

---

### 33-2　S-02-9 日期欄位在 iOS 上跑版（Chrome 上正常）

**這一題連同一個流程問題一起講**：#32 的回報說「兩個日期框一樣大、文字已垂直置中」，
Cowork 端複驗也量出 176/176——**兩邊都是用無頭 Chrome 量的**。
但 Rozi 的手機截圖顯示 iOS 上完全不同：
「2026年9月5日」比框還寬（中文日期格式更長）、空的回程框寬度又跟有值的不一樣。

**根因**：iOS 的原生 `<input type="date">` **讓內容決定寬度**，不照我們給的欄寬；
而且空值時渲染「年/月/日」佔位，寬度與有值時不同。

**改法**：

- 把日期欄位的盒子正規化：**`-webkit-appearance:none`**、
  **寬度由欄位分配（各半，不讓內容決定）**、**`min-width:0`**（flex 子元素預設
  `min-width:auto` 會拒絕被壓縮，這是溢出的直接原因）、
  高度／內距／字級與其他輸入框一致。
- **關掉系統外觀會失去右邊那個日曆小圖示**——
  **要自己補一個日曆 icon 回去**（Feather 的 `calendar`，加進 `ICON`，
  並更新 `_icon授權.md`），否則使用者看不出這是日期欄位。
- **點擊仍要能叫出 iOS 的日期選擇器**——`-webkit-appearance:none` 只改外觀，
  不影響 `input[type=date]` 的原生選擇行為。**改完要確認這一點沒壞。**

**⚠️ 這一項無頭 Chrome 驗不出來。** 停止條件只能寫到「桌機上兩框等寬、
有值與空值寬度相同」；**iOS 的實際結果必須由 Rozi 在手機上確認**，
在 `_停點.md` 明寫「這一項要 Rozi 在手機上看過才算數」。

---

### 33-3　提示句有兩種字級，統一成 13px

Rozi 問：輸入框的提示文字為什麼比「不填就是當天來回」還大？

**先回答她的問題：placeholder 比提示句大是刻意的，不能統一。**
理由要寫進回報讓她看得懂：
① 輸入框裡的文字**必須 ≥16px**，否則 iOS Safari 一聚焦就會放大整頁；
② **placeholder 的字級必須等於輸入值的字級**，否則一開始打字文字會跳大。
所以 placeholder 是 16px 是被夾出來的，不是挑的。
**而且 placeholder 不是提示句，它是「即將輸入的值」的預覽**，佔的是輸入值的位置。

**但她這一問查出另一個真的不一致**（Cowork 端實測）：

| 字級 | class | 例子 |
|---|---|---|
| **13px** | `.hint` | 不填就是當天來回／只有你和拿到分享連結的人看得到／在當地換錢後填一次就好／有 N 筆還沒換算成台幣… |
| **15px** | `.note warn`／`.txsub` | 先記著了。補上台幣金額就會算進結算。／還會變 —— 之後記帳會影響這裡 |

**同樣是提示句，兩種大小。全部收斂成 13px。**
警示那幾條的份量本來就有 icon、有色底、有顏色在撐，**不需要再靠字級**——
一件事用一種手段表達就好。

**改完要回報有沒有哪一條變得太弱**，有就停下來讓 Rozi 看，不要自己加粗或改色補償。

---

### 33-4　S-04-9 沒有連動 S-02b-11 的支付方式清單

Rozi：「在 S-02b-11 有設定當地付款方式，但在 S-04-9 這裡並沒有連動到……
而且不應該顯示「儲值卡」的項目，因為前面在 S-02b-11 並沒有「儲值卡」的設定選項。」

**兩件都是真的錯，不是原型的權宜。** 讀碼確認：

- **S-02b-11** 讀的是 `t.pays`——**這一趟自己的清單**，可新增／刪除／排序。
- **S-04-9** 讀的是**全域常數 `PAY`**——固定就是「現金／信用卡／儲值卡」。

**兩處從來沒有接起來過。**

**而且比看起來嚴重**：若使用者用「儲值卡」記了一筆，那筆的支付方式在行程清單裡不存在，
**資料就對不起來**——編輯行程頁看不到它、刪不掉它、統計歸不了類。

這也**不只是原型的問題**：2026-09-04 的定案寫的是「S-04 從該清單快選，
**不再固定三選一**」，而正式程式碼的 `ExpenseFormSheet.tsx:18` 同樣是寫死常數
（#26 盤點時已記錄）。**是漏做。**

**改法**：

- S-04-9 改讀**這一趟的支付方式清單**，**順序跟著 S-02b 的排序走**。
- **全域常數 `PAY` 刪掉**（若別處還在用，一併改掉；改不掉就停下回報）。
- **預設值**：清單的第一項，不要寫死「現金」——
  使用者可能把清單改成只有信用卡。
- **邊界要確認**：某筆消費用的支付方式後來被刪掉會怎樣。
  既有規則是「已經有消費在用的不能刪」，**要確認這條擋得徹底**
  （包含改名、排序後索引位移等情況）。擋不住就停下回報，不要自己設計補救。

---

### 33-5　S-03b 分享選單的說明文字跟標題擠在同一行

Cowork 端複驗時看出來的，Rozi 沒提。

**實測**：`.shopt` 是 `display:flex`，標題與灰字的頂端只差 3px——**在同一行**。
所以讀起來是「複製分享連結　不用登入就看得到消費明細」黏成一句。
而且另外兩個選項沒有灰字，變成三列裡**一列有兩段文字並排、兩列只有一段**，結構不一致。

**改法**：`.shopt` 改為直向排列，**灰字放在標題下方**。三列的高度不必一致
（只有一列有灰字是正常的），但標題的左緣與垂直節奏要對齊。

---

### 33-6　S-02b「這趟怎麼結算？」用「選」字當單選鈕

Cowork 端複驗時看出來的，Rozi 沒提。

**實測**：兩個選項各有一個 `32×25` 的 chip，**裡面印著一個「選」字**
（`.selchip`，選中的藍底白字、未選的灰框灰字）。

**這跟那個假的 `⠿` 是同一類問題：用文字假裝控制項。**
一個圓圈裡放一個「選」字，既不是單選鈕該有的樣子，也沒有傳達任何資訊——
**兩顆都寫「選」**，要靠顏色才分得出哪個被選中。

**改法**：改成**標準單選鈕的形狀**——未選是空心圓框，選中是圓框內一個實心圓點。
**不要放任何文字在裡面。** 選項的意思由旁邊那行字負責
（「誰欠誰就轉給誰」／「都轉給同一個人」）。
選中列維持既有的淺藍底＋主色外框，那個是對的。

**順手掃一次全站**：還有沒有別的地方用文字假裝控制項
（`⠿`、「選」這一類），找到就列在回報裡。

---

### 33-7　圓角定案 C，移除切換器

Rozi 二次確認：**「圓角我還是選擇 C」**。

- `--r-base:10px`／`--r-panel:20px` **寫死為預設值**。
- **移除圓角切換器**（稿頭與真機模式底部列兩處），`RADIUS_SETS` 一併刪除。
- 移除後**真機模式底部列會變矮**——#32 有一段是量出實際高度再讓版面扣掉，
  **確認那段仍然正確**，不要留下多餘的空間。

---

### 33-8　原生表單控制項必須真機確認（流程規則，寫進 `CLAUDE.md`）

33-2 暴露的問題：**iOS 的原生日期欄位在無頭 Chrome 上量不出真實行為**，
所以 #32 的「已修好」是在錯的平台上得到的結論。

**規則**：凡是牽涉**原生表單控制項**（`input[type=date]`／`select`／`file`／
`input[type=time]` 等，排版由作業系統決定的元件）的問題——

> **無頭 Chrome 驗不出來。** 這類項目的停止條件不得只寫機器斷言，
> 必須在 `_停點.md` 明確標記「**這一項要 Rozi 在手機上看過才算數**」，
> 並列出她要確認什麼。

---

### 護欄

- 不改 `src/`、不跑 migration、不刪檔。
- **不要動**：placeholder 的 16px（那是 iOS 硬性下限）、輸入框字級、
  金額字級、`--gr`／`--md`／`--dg`／`--out` 的色碼、S-05 的內容與結構、
  卡片距畫面邊緣的 14px、內容容器的 14px 內距。
- **不要改任何計算**（33-1 只改顯示順序）。
- 動 A 時若發現需要動 B，**停下回報，不得順手改**。

### 停止條件（機器判斷）

1. `renderS03()` 與 `renderS06()` 的日期分組**由新到舊**（最後一天在最上、
   「出發前」在最底）；同一組內依 `created_at` 由新到舊；兩處由**同一段邏輯**產生。
2. 「還沒算清楚」橫幅仍在列表最上方。
3. 兩個日期輸入框：桌機上**等寬**，且**有值與空值的寬度相同**；
   `-webkit-appearance` 為 `none`；`min-width` 為 `0`；右側有一個日曆 `<svg>`；
   `ICON` 含 `calendar` 且 `_icon授權.md` 已更新。
4. `.ui` 之內所有提示句（`.hint`／`.note`／`.txsub`／`.sub2`／`.foot`）
   的 computed `font-size` **皆為 13px**；輸入框與 placeholder **仍為 16px**。
5. `renderS04()` 的「怎麼付的？」chips **由該行程的 `pays` 產生**，
   數量與內容與 S-02b-11 相同、**順序相同**；
   全站搜尋**不存在全域常數 `PAY`**；預設選中的是清單第一項。
6. `renderS04()` 的輸出**不含「儲值卡」**（除非該行程的清單裡真的有）。
7. `.shopt` 的標題與灰字**不在同一行**（兩者 `top` 相差 ≥ 一個行高）。
8. `.selchip` **不含任何文字**；未選為空心圓、選中為圓內實心點；
   全站搜尋**不存在** `>選<` 這類用文字當控制項的寫法。
9. `--r-base` 為 `10px`、`--r-panel` 為 `20px`；
   **全站搜尋 `data-radius` 出現次數為 0**；`RADIUS_SETS` 已刪除。
10. 真機模式底部列變矮之後，**畫面底部的主要按鈕沒有被遮住**
    （逐一切過每個畫面驗證）。
11. 三個寬度（320／375／414）無橫向捲動、無文字左右緣 <14px。
12. **逐頁截圖已輸出**（S-00～S-07 各主要狀態 × 桌機／真機模式兩組），
    檔名列在 `_停點.md`。
13. #25～#32 的全部斷言維持通過；既有各組測試全綠。

### 交付

回報寫進 `_停點.md`（覆寫、標時間、≤50 行），最上面放手機預覽網址，
並且**明確標出「33-2 的 iOS 日期欄位要 Rozi 在手機上看過才算數」**。

---

### 33-9　用文字字元充當 icon（Rozi 追問後查出的第二層）

Rozi 對「`⠿` 是純文字」提出質疑。**釐清一下，那句話其實包含兩個不同的問題，
Cowork 前面沒有分開講**：

| | 狀態 |
|---|---|
| **功能**：沒有拖曳程式，按了沒反應 | **#32 已修**（現在有 `data-paygrip`、`role="button"`、`aria-label="拖曳排序"`，真的拖得動）|
| **長相**：它是 Unicode 字元 `⠿`（U+283F 點字符號），**不是 icon** | **還沒修，#33 原本也漏掉** |

**為什麼字元不能當 icon**：
① 它的樣子**由系統字體決定**，不同裝置、不同字體會長得不一樣，我們控制不了；
② 大小跟著**字級**走，不跟 icon 尺寸走，換字級就會失衡；
③ 違反「全站 icon 一律用 Feather」的定案（#28-1）。

**改法**：

- **`⠿` 換成 Feather 的 `menu`**（三條橫線）——**那正好就是 iOS 清單重新排序把手的樣子**。
  加進 `ICON`，尺寸 16px，更新 `_icon授權.md`。

**順手掃出第二處同類問題**（Cowork 端全站掃過字元）：
**「新增」這個動作全站有兩種畫法**——

| 畫法 | 在哪 |
|---|---|
| **全形「＋」字元** | S-01-2 新增行程／S-01 空狀態「建立第一趟」／S-02-16 新增成員／S-05-19 建立新行程 |
| **Feather 的 `add` icon** | S-03-22「記一筆」 |

**同一個動作兩種長相。四處全形「＋」一律改用 `ic('add', 16)`。**

**⚠️ 改完要檢查視覺重量，這一項不是純替換**：
全形「＋」在中文字體裡的粗細與寬度，本來就是跟中文字配好的；
**16px 的 SVG 加號放在 15px 的中文旁邊，重量與間距不會自動對得起來**。
逐一看過那四顆按鈕（新增行程／建立第一趟／新增成員／建立新行程），
必要時微調 icon 尺寸或與文字的間距。
**調不出來就停下回報，附上截圖，讓 Rozi 決定要不要維持全形「＋」**——
一致性的代價如果是四顆按鈕都變醜，那就不值得。

**另外一個判斷，寫在這裡讓 Rozi 可以推翻**：
現金匯率欄位旁邊的 **🇹🇼 國旗 emoji 維持不動**。
它在標示幣別，屬於**內容**而不是圖示（與成員 emoji、消費類別 emoji 同類），
不在「不要用字元當 icon」的範圍內。

**掃描結果補充**：`.ui` 之內的其他符號（`✕`／`›`／`⋯`／`✓`／`⚙`）
**全部只出現在稿註區與註解裡，產品畫面沒有**——這幾個不用動。

#### 停止條件（33-9 追加）

14. `.ui` 之內**不存在** `⠿` 字元；拖曳把手是 `<svg>`（Feather `menu`），寬 16px。
15. `.ui` 之內**不存在**全形 `＋` 字元；四處新增動作都用 `ic('add', 16)`。
16. `ICON` 含 `menu` 與 `calendar`；`_icon授權.md` 已更新；
    `ICON` 的每一個鍵**都至少有一處使用**。
17. 四顆「新增」按鈕改用 icon 之後，**已逐一看過視覺重量**，
    並在 `_停點.md` 說明有沒有微調、或回報調不出來。
18. **全站掃描**：`.ui` 產生的 HTML 中，不得出現被當成圖示使用的
    Unicode 符號字元（掃描清單至少涵蓋 `⠿ ✕ ✖ × ＋ › ‹ ▸ ▾ ↑ ↓ → ← ⋯ ☰ ≡ ✓ ⚙ ⌄`）。
    **emoji（成員識別、消費類別、國旗）不在此限**——那是內容，不是圖示。

---

## #34 移除鈕可點區 ＋ 單選鈕 ＋「N 人均分」併列 ＋ 回程日期框（iOS）＋ 封面區隱藏 [已處理]

> **Rozi 已裁示：這一節只做這三項，原本 Cowork 提的其餘四項全部撤掉，不要做。**
> （34-3 是她後來追加的，不是 Cowork 自己找的。）
> 撤掉的是：`➕` 換成 icon、S-06 hero 的成員 emoji、S-03b 分享選單加 icon、
> `.ic2` 註解與實測不符。**這四項在本節之外，不得順手處理。**

**本節只改 `Tripay_原型.html`，不改 `src/`、不跑 migration、不刪檔。
#25～#33 的成果一律不得回退。**

---

### ⛔ 這一節的作業範圍（最重要的一條，先讀）

**只能動下面 34-1 ～ 34-6 明確點名的元素。其他一律不准碰。**

過去幾輪反覆發生「順手把看起來不對的地方一起改掉」，
結果是改 A 壞 B、每一輪都在長，實作一直往後推。
**從這一節開始的規矩**：

- 看到別的問題 → **寫進 `_停點.md` 的「順手看到但沒動」清單，不要動它**。
- 不確定算不算範圍內 → **不做，寫進那份清單**。
- 只有指令明寫「全站」兩個字的項目，才可以跨畫面掃描與修改。
  **本節沒有任何一項是全站範圍。**

---

### 34-1　六顆移除鈕的可點區只有 20×12（不要圓形容器）

**在哪**（就這六顆，不要多也不要少）：

1. S-02b「誰一起去？」四位成員列右側的移除鈕 —— 四顆
2. S-02b「這趟的支付方式」兩列右側的移除鈕 —— 兩顆

**實測**（iPhone 390×844、`pointer:coarse` 真機模式，量 border box ＋ margin）：
這六顆是 **20 × 12 px**，`class` 是空字串；對照組 `.ic2` 是 40 × 40。
**12px 高的可點區在手機上按不準，而按錯的後果是刪掉一個人。**

#### ⚠️ Rozi 已裁示：**不要加圓形容器**

> 「可以改成員列的高度，但是成員列右邊的刪除 icon 我不要有圓形容器。」

**所以這一項不是套 `.ic2`**（本節初稿寫的是套 `.ic2`，已被推翻，照下面做）：

- **裸 icon，沒有容器、沒有底色、沒有外框。**
- **可點區用看不見的方式撐到 44 × 44**
  （例如 `position:relative` ＋ `::after{content:'';position:absolute;inset:-N}`），
  **不要把看得見的圖形放大，也不要把按鈕的 border box 撐成 44** ——
  border box 撐大會把「3 筆在用」那段文字擠掉。
- **列高可以變高**（Rozi 已同意）。目前列內距是 7px，加到約 11px 讓 44 的可點區有地方站。
- **icon 尺寸 12px → 16px**，理由：與**同一列左邊的拖曳把手（`.grip`，16px）同尺寸**，
  兩顆 icon 在同一列上份量一致；也不會蓋過成員名字。
  **Rozi 已確認 16px**，不要改成別的尺寸。
- 顏色維持現況：可刪的用 `var(--gr)`，已被使用而不可刪的用 `#CDD3D0`。
  **不要改成 `--dg` 深紅**——移除一個成員不是刪除整趟。

#### 這件事要寫成規則（`CLAUDE.md`），否則下一輪又會被「統一」回去

icon 按鈕現在有**三個層級**，不是兩個：

| 層級 | 長相 | 例子 |
|---|---|---|
| 畫面層級的動作（標題列） | `.ic2`：40×40 正圓＋一圈線＋20px icon | 返回／關閉／更多／設定 |
| **列內的動作** | **裸 icon，無容器**，可點區 44×44（透明撐出來） | 成員／支付方式的移除鈕 |
| 方向指示（不可點） | 裸 icon，可點的是整列 | 列尾的 `›` |

**不要順手處理的**（看到也不要動，寫進「順手看到但沒動」清單就好）：
拖曳把手 `.grip` 只有 16×16、`.ic2` 本身的可點區實測是 40 不是註解說的 44。

### 34-2　`.selchip` 有三處，#33-6 只改到兩處，而且第三處的形狀本來就用錯

**#33-6 已完成的兩處**（正確，不要再動）：
S-02b「這趟怎麼結算？」的「誰欠誰就轉給誰」與「都轉給同一個人」，
已改成未選空心圓、選中圓內實心點，標籤內無文字並帶 `aria-hidden="true"`。

**沒改到的第三處**：**S-04「分帳方式」選「一起分」之後，
按下「要排除誰？」展開的成員列**，每一列右側仍是
`<span class="selchip ${on?'on':''}">分</span>`。

**為什麼機器斷言沒抓到**：那段要把面板展開才會渲染，
斷言只掃到當下畫面上看得見的兩顆。

**為什麼形狀用錯**：那一列是**複選**——每位成員各自可開可關（`data-epart` 是 toggle）。
**圓形單選鈕的意思是「只能挑一個」，跟實際行為相反。**
兩顆結算模式是二選一，用圓的對；成員排除是多選，要用打勾。

**改法**：

- 另立一個 class（例如 `.chkchip`），**不要沿用 `.selchip`**——
  形狀不同就是兩個元件，共用只會讓下一次改動又漏一處。
- 未選：20×20 空心圓角方框（圓角 4px，邊框 `#B9C1BB`，白底）。
- 選中：填 `var(--w)`，中間一個白色的 Feather `check`。
- 標籤內不得有文字，加 `aria-hidden="true"`。
- 尺寸與 `.selchip` 相同（20×20），**只有形狀不同、份量相同**。

---

---

### 34-3　S-04「N 人均分」與「每人 ⋯」併成同一列

**在哪**：S-04「分帳方式」選「一起分」之後，下面那一格（`.sharedhead` 裡面）。

**現況**：左邊直排兩行——第一行「4 人均分」，第二行「每人 —」；
右邊是「要排除誰？」按鈕。

**Rozi 要求**：「每人 —」跟「4 人均分」**放在同一列**。

**改法**：

- 兩段文字改成橫排，同一行，中間用一個明確的間隔
  （建議 `·` 或 8–10px 的間距，你決定；選了在 `_停點.md` 說明）。
- **「每人 ⋯」的金額維持等寬字（`.money`）與現在的字級**——
  它是金額，字型與字級不要因為換行而改掉。
- 「4 人均分」維持現在的字級與字重。
- **右邊的「要排除誰？」按鈕位置與樣式不動。**
- 併成一列之後這一格會變矮，**上下內距要重新配平**，
  不要讓文字貼著上緣或下緣（有圓角的容器，左右內距至少要比圓角多 4px 這條照舊）。
- 台幣金額還沒填時顯示的是「每人 —」，**這個文案不變**。

**這一格的可點行為不變**（現在整格不可點，只有「要排除誰？」可點，維持原狀）。

---

### 34-4　S-02-9「回程」欄位在 iOS 上比「出發」矮一截（第三次修同一個地方）

**Rozi 的手機截圖**：「出發」是正常高度的白框，裡面「2026年9月5日」；
**「回程」（空值）是一個明顯矮很多的細長白框**，裡面完全沒有字。
她的話：「回程的欄位高度請跟出發日期的欄位一樣，現在的欄位完全不同。」

**⚠️ 這是同一個地方第三次修**（#32 一次、#33-2 一次）。
前兩次都是在無頭 Chrome 上量出「兩框一樣」就結案，**iOS 上根本不是那樣**。
這一次不要再用 Chrome 的量測當作修好了。

#### 根因（可以從現在的 CSS 直接讀出來，不需要 iOS）

日期輸入框**目前沒有明確高度**，高度是「上下內距 9px ＋ 裡面那行字的行高」撐出來的：

```
.ui input[type=text],.ui input[type=number],.ui input[type=date]{
  ...padding:9px 10px;font-size:var(--fs-input)...}   ← 沒有 height
```

- **Chrome**：空的 date 欄位仍然會畫出 `mm/dd/yyyy` 佔位文字，行高在，高度是 40px。
- **iOS Safari**：關掉系統外觀（`-webkit-appearance:none`）之後，
  **空值的 date 欄位裡面什麼都不畫**，那一行的高度塌成 0，
  整個框就只剩 9＋9 的內距 ── 這就是她看到的細長框。

**所以這不是「兩個框寬度不一樣」的延續，是另一個原因造成的另一個症狀。**
前兩次修的是寬度，高度從頭到尾沒有被固定過。

#### 改法

1. **給日期欄位明確高度**，不要讓內容決定。
   數值取「同一張表單裡文字輸入框的實測高度」（目前是 **40px**），
   並在那條 CSS 規則旁邊註明：**這個數字必須與 `.ui input[type=text]` 的高度相同，
   哪一天改了字級或內距，兩邊要一起改**。
2. **處理 iOS 專屬的內層盒子** ── `::-webkit-date-and-time-value`。
   那是 Safari 畫日期值用的內部元素，**Chrome 上不存在，所以永遠測不到**。
   要給它 `text-align:left`、`margin:0`、以及能撐住一行的高度／行高，
   否則就算外框固定了，裡面的字仍然會偏上或偏下。
3. 空值時外框看起來仍是一個完整的輸入框（有邊框、有圓角、有日曆圖示），
   **不要因為裡面沒字就塌掉**。
4. 「出發」與「回程」兩框的**寬度維持現在的做法**（各佔一半、`min-width:0`）——
   #33-2 那一段不要動，這次只處理高度。

#### 如果這次還是修不好

**不要再修第四次。** 改成回報，並在 `_停點.md` 寫出你試了什麼、
iOS 上還剩什麼症狀，然後提出**換掉原生日期欄位**這個方案讓 Rozi 決定：
用 S-04「記在 3/14（六）」那種「一整列，點下去叫出日期選擇器」的做法
（那個樣式她已經核可過），畫面完全由我們控制，不受 iOS 內部渲染影響。
**這個替代方案這一輪不要動手做，只寫進回報。**

#### 停止條件（這一項特別註明）

- **機器可判斷的部分**：`.ui` 的 CSS 中，`input[type=date]` 有明確的 `height`
  （或 `min-height`），且該數值與 `.ui input[type=text]` 在 Chrome 的實測高度相同；
  Chrome 上「出發」與「回程」兩框的 `getBoundingClientRect()` 高度與寬度都相同
  （有值與空值兩種狀態都要量）；CSS 中存在 `::-webkit-date-and-time-value` 規則。
- **⚠️ 這一項要 Rozi 在手機上看過才算數**（依 #33-8 的常設規則）。
  Chrome 全過**不等於**修好了。`_停點.md` 要明白寫出「這一項尚未確認」。

---

### 34-5　拿掉 S-02b-5「只有你和拿到分享連結的人看得到」

**Rozi 問**：「這句文案有什麼意義嗎？如果會讓使用者更混亂，也不會影響功能，就可以拿掉。」

**查證結果：這句話講的事情，現在沒有發生，Phase 1 也不會發生。**

1. **封面照片在原型的任何一頁都沒有被顯示。**
   `renderS01()`（首頁卡片）、`renderS03()`（行程頁 hero）、`renderS06()`（分享頁 hero）
   **三處都只讀 `toneFor()` 的漸層色調，沒有一處讀 `t.photo`**。
   照片存得進去（`s02bsave` 有寫回 `photo`），但沒有任何畫面把它畫出來——
   **包含分享頁**。所以「拿到分享連結的人看得到」目前是不成立的。
2. **封面上傳排在第二批。** `_實作批次工作清單.md`「第二批（新畫面）」
   明列「S-02b 封面上傳」。**Phase 1 上線時沒有這個功能。**
3. 就算之後接起來，這句話現在的位置也不對——它掛在
   「更換照片／重選色調」兩顆按鈕下面，而**目前封面是一塊漸層色，漸層色沒有隱私可言**。
   真正需要這句話的時刻，是使用者**正要上傳一張自己的照片**的那一秒，
   不是編輯頁上一直掛在那裡。
4. 「拿到連結的人不用登入就看得到」這個隱私後果，
   **已經在該講的地方講了**——S-03b 分享選單的「不用登入就看得到消費明細」，
   那才是使用者真正製造曝險的時刻。

**改法**：把 `b('S-02b-5', ...)` 那一行**整段移除**（連同它的編號）。

- **編號 S-02b-5 不回收**，在右側稿註清單標為 `del`，
  說明「已移除：封面照片上線前這句話不成立，且掛在色調上沒有隱私可言」。
- **不要順手改「更換照片」「重選色調」兩顆按鈕**，它們維持原狀。
- **不要順手去改 S-03b 分享選單那句話**（見下方「順手看到但沒動」）。

#### 停止條件

- `.ui` 之內不存在「只有你和拿到分享連結的人看得到」這串文字。
- 右側稿註清單中 `S-02b-5` 標為 `del` 且有說明。
- 移除後 S-02b「封面」區塊與下方「誰一起去？」之間的間距沒有塌掉
  （量 `.fld` 的下緣到下一個 `.fld` 上緣，與其他相鄰 `.fld` 的間距相同）。

---

### 34-6　S-02b 整個「封面」區隱藏，色調改為自動（Rozi 裁示）

**背景（給你的判斷依據，不要自己延伸）**：
封面照片上傳**這一階段沒有**——8/31 定案「上線維持目的地 gradient；
封面改由產品內上傳（打磨輪第二批）」，而打磨輪第二批就是
`_實作批次工作清單.md`「第二批（新畫面）」那份清單（S-08～S-11 副卡記帳 ＋ S-02b 封面上傳）。
後端已備好（migration 008 已套 production：`covers` bucket、Storage RLS 四條、
`trips.cover_path`），**前端一行都沒寫**。

**Rozi 裁示**：既然這階段沒有這個功能，
「更換照片」與「重選色調」**都隱藏**，色調改為自動；
等 Phase 2 有封面上傳功能時，**再把這個畫面放回來**。

#### 要隱藏什麼

S-02b **整個「封面」區塊**，包含：
「封面」標題、104px 的色調預覽、「目前：目的地色調」那行字、
「更換照片」與「重選色調」兩顆按鈕、8 色預設盤與「自動」鈕，
以及 **34-5 那句灰字**（所以 34-5 被這一項吸收，兩項一起做，不要重複處理）。

隱藏後 S-02b 從「誰一起去？」開始。

**「隱藏」不是「刪掉」**：CSS 與產生這一區的程式碼**留在原地**，
用一個開關關掉（例如一個常數 `COVER_UI = false`，或整段包在條件裡）。
Phase 2 要放回來時把開關打開就好。
**在那段程式碼旁邊寫清楚**：為什麼關、什麼時候開、開的時候還要一併恢復什麼
（34-5 那句灰字要不要回來，屆時由 Rozi 決定，不要自動帶回）。

編號 S-02b-2／S-02b-3／S-02b-4／S-02b-5／S-02b-10 在右側稿註清單標為
**「暫時隱藏（Phase 2 封面上傳上線後恢復）」**，**編號一律不回收**。

#### 自動色調的規則（Rozi 選了「先認關鍵字，認不出來才依序循環」）

**兩段式，順序不能顛倒：**

1. **先比對關鍵字**。行程名含 8 組關鍵字之一（北海道／濟州／首爾／東京／福岡／沖繩／京都／台灣，
   各自的同義詞照現在的 `TONES` 表），**就用那一國的色系**。
   這一段維持現況，理由是 8/31 的既有決策：
   **卡片要能「一眼看出是哪一國」**，這也是當初否決「色票自由挑色器」的原因。
2. **比對不到，才依序循環**取 8 色其中一個，
   **順序就是 `TONES` 陣列現在的順序**（北海道 → 濟州 → 首爾 → 東京 → 福岡 → 沖繩 → 京都 → 台灣，
   也就是預設盤畫面上由左到右的順序），第 9 趟回到第 1 色。
   **原本那個單一 fallback 色（`FALLBACK_TONE`）不再使用**——
   它就是「所有認不出目的地的行程長得一模一樣」的原因，這一項要解決的正是它。

**循環的序號怎麼取（這一點很重要，不要用「目前列表的第幾筆」）**：
序號必須在**建立行程的當下決定並存起來**，之後不再重算。
理由：如果每次渲染都用「這是清單裡的第幾趟」，
**使用者刪掉一趟，後面所有行程的顏色會集體位移**——那是明顯的錯。
欄位怎麼存、叫什麼名字你自己決定。

**注意兩段的先後**：關鍵字比對是**在顯示時**做（所以使用者把
「四寶團」改名成「濟州島四寶團」，顏色會立刻跟著對）；
循環色是**建立時就定好**的備胎，只有關鍵字比不到時才拿出來用。

#### 停止條件

1. `.ui` 之內不存在「封面」「更換照片」「重選色調」「目前：目的地色調」
   「只有你和拿到分享連結的人看得到」這幾串文字；
   S-02b 第一個區塊是「誰一起去？」。
2. 產生封面區的程式碼**仍然存在**，由一個開關關閉；把開關打開時該區完整重現
   （測試要實際把開關打開跑一次，確認沒有壞掉，跑完改回關閉）。
3. `FALLBACK_TONE` **不再被任何顯示路徑使用**。
4. 建一個名稱不含任何關鍵字的行程（例如「阿明生日」），
   **刪掉它前面的一趟行程之後，它的顏色不變**。
5. 連續建立 9 個名稱不含關鍵字的行程，取得的色號依序是
   `TONES` 的 0,1,2,3,4,5,6,7,0。
6. 名稱含關鍵字的行程，顏色仍是該關鍵字對應的色系
   （用現有的「2026 濟州島四寶團」驗，它必須維持濟州的青綠色）。
7. 右側稿註清單中 S-02b-2／3／4／5／10 標為「暫時隱藏」且說明恢復條件。

### 停止條件（機器判斷得出來）

在 iPhone 390×844、`pointer:coarse` 的真機模式下量測：

1. S-02b 那六顆移除鈕**都沒有可見容器**（`background` 為透明、`border-width` 為 0、
   `className` **不含** `ic2`），且**有效可點區（含透明擴張區）都 ≥ 44×44**。
   斷言要**列出這六顆各自的可見尺寸與可點尺寸兩組數字**，不要只回一個布林值。
   驗可點區用 `document.elementFromPoint()` 打四個角，不要只量 border box。
2. 原始碼中 `class="selchip"` 的每一處，標籤內都是空的、都帶 `aria-hidden="true"`。
3. S-04「要排除誰？」的成員列用的 class **不是 `selchip`**；
   展開後每一列含一個空方框（未選）或含 Feather `check` 的填色方框（選中）。
4. **斷言必須先把可展開的面板展開再掃**——
   至少展開 S-04「要排除誰？」，並在 `_停點.md` 寫出這一輪展開了哪幾個。
   （**這一條是因為 34-2 漏網才加的**：只掃當下可見的元素等於沒掃。）
5. S-04「一起分」的那一格，「N 人均分」與「每人 ⋯」兩段文字的
   `getBoundingClientRect().top` **相同**（同一列），
   且「每人 ⋯」仍套用 `.money`（等寬字）。
6. `git diff --stat` 只顯示 `Tripay_原型.html` 一個檔案有變動
   （`_停點.md`、`專案狀態.md` 除外）。
7. **`git diff` 的變動行數不得超過 260 行。**
   超過就表示動到範圍外的東西了，停下來回報。
8. 既有的十三組測試全部仍然全綠。

9. **改 A 壞 B 的攔截網（這一節新增，之後每一節都要做）**
   Rozi 這一輪明講：「希望不要再改 A 壞 B 了。」所以這一節多一道機器關卡。

   **做法**：動手改之前，先在真機模式（iPhone 390×844、`pointer:coarse`）
   把十二個畫面各存一張截圖當**基準**；改完之後用同樣的方式再存一份，
   **逐張做像素比對**，輸出「每一張有幾成的像素改變、改變的區域在哪」。

   **只有這幾個地方允許有差異**：
   - S-02b：成員列與支付方式列（34-1）、整個封面區消失（34-6）
   - S-04：「要排除誰？」展開後的成員列（34-2）、「N 人均分」那一格（34-3）
   - S-02：兩個日期欄位（34-4）
   - 所有畫面：因為上述區塊高度改變而**整體往上或往下位移**——這種算合理

   **任何一張畫面在上述範圍以外出現像素差異，就是改 A 壞 B**：
   停下來，在 `_停點.md` 寫清楚是哪一張、哪個位置、為什麼會動到，**不要自行判斷「這樣比較好看」**。

   基準圖與比對後的圖都留著，路徑寫進 `_停點.md`，Cowork 端會自己再比一次。
10. 逐頁截圖重出一份（依 #32-10 的規則），路徑寫進 `_停點.md`。

### `_停點.md` 必須包含

- 六顆移除鈕的實測尺寸（改前／改後）
- 這一輪展開了哪幾個摺疊面板
- **本輪必須寫進「順手看到但沒動」清單的兩項**（查 34-5 時發現，**不要動手修**）：
  ① 封面照片存得進 `store` 卻沒有任何畫面顯示它（首頁卡片／行程頁 hero／分享頁 hero
  三處都只畫漸層色調）——要不要接起來由 Rozi 決定。
  ② S-03b 分享選單的「不用登入就看得到消費明細」只提到消費明細；
  若日後封面照片會出現在分享頁，這句話的範圍要跟著補。
- **其他「順手看到但沒動」清單**——這一輪看到、覺得不對、但因為不在範圍內所以沒碰的東西。
  這份清單是給 Rozi 看的，由她決定要不要進下一節，**不要自己動手**。

### 交付前檢查

**① 需求覆蓋**：Rozi 這一輪的裁示是「#34 只做 1、3 兩項」——
她說的「1」是六顆移除鈕的可點區（本節 34-1），
「3」是「要排除誰？」那處單選鈕沒改到、而且複選用了單選形狀（本節 34-2）。
兩項都在。**她隨後追加第三項**：S-04「每人 ⋯」要跟「N 人均分」放在同一列（本節 34-3）。
**她隨後又追加第四項**：S-02-9「回程」欄位在 iOS 上比「出發」矮一截（本節 34-4）。
**第六項**：S-02b 整個封面區隱藏、色調改自動（本節 34-6）——
Rozi 裁示「這階段沒有上傳功能就先隱藏，Phase 2 有了再把畫面放回來」，
自動規則她選了「先認關鍵字，認不出來才依序循環」。**34-5 被 34-6 吸收，一起做。**

**第五項**：S-02b-5 那句灰字（本節 34-5）——她問「有什麼意義嗎」，
查證後確認那句話描述的事情現在沒有發生、Phase 1 也不會發生，依她給的條件拿掉。
她同時裁示 33-5、33-6 **確認採用，不退回**。

**她另外問的一題不在本節**：在 S-02b 加了 Linepay 之後，S-04-9 沒有跟著出現。
**已查明是原型的問題，不是連動邏輯壞掉**——①「加入」只寫進編輯草稿 `fb`，
要按「儲存」才寫回 `store` ②`#s02bsave` 只呼叫 `renderS01()`，
真機模式下 S-04 是先渲染好的區塊，切過去不會重畫，所以按了儲存也要重新整理才看得到。
`renderS04()` 本身讀的是 `t.pays`，邏輯是對的。
**Rozi 已表示只要實作會連動就不用管**，所以**本節不要修這個**。

**② 收件人檢查**：本節提到的先前決策都已展開成自描述敘述——
#30-2 的 `.ic2` 定義（40×40 正圓＋一圈線＋20px icon）、
#33-6 對 `.selchip` 的要求、以及「要排除誰？」是複選而非單選的行為事實，
都在內文寫清楚，不需要回頭讀前面的節。

**③ 五類例外**：本節沒有觸及不可逆動作、花錢、安全性／隱私、重大範疇取捨，
也沒有需要 Rozi 記憶或偏好才能回答的問題。**可以一路做完。**

---

## #35 「記一筆」的日期：預設今天 ＋ 改成同列欄位（**原型最後一節**） [已處理]

> **這是原型檢視的最後一節。** Rozi 明講：「不要再有 #36 了，可以趕快實作。」
> 所以這一節**只做她點名的一件事**，而且要一次做對。
> 做完之後下一節就是 Phase 1＋1.5 的實作節次，不再有原型輪次。

**本節只改 `Tripay_原型.html`，不改 `src/`、不跑 migration、不刪檔。
#25～#34 的成果一律不得回退。**

---

### ⛔ 作業範圍

**只能動 S-04 的日期列（`S-04-10`／`.daterow`）與它的預設值。其他一律不准碰。**

`_停點.md` 上那六項「順手看到但沒動」，**Rozi 裁示全部不做，只做紀錄**——
紀錄已由 Cowork 寫進 `_延後與不做清單.md`（封面照片顯示、分享選單灰字範圍、輸入框高度階梯）
與 `Tripay_問題優先序清單.md` P4（拖曳把手可點區、`.ic2` 註解與實測不符，
以及「S-04 日期 iOS 塌陷風險」**經查證排除、不是問題**）。
**這一節不要碰那六項中的任何一項。**

---

### 35-1　預設日期改成「今天」

**現況**：`blankExp()` 寫的是 `date: t.start`——**行程的出發日**。
所以不管今天幾號，點開「記一筆」看到的永遠是出發日。

**改成**：預設今天。

**邊界規則（Cowork 判斷，已告知 Rozi，她沒有推翻）**：

- 今天 **在回程日之後** → 取**回程日**。
  理由：行程結束後才補記的帳，若記在今天，`dayLabel()` 會算出「第 200 天」這種分組。
- 今天 **在出發日之前** → **就用今天，不要夾**。
  理由：那會落進「出發前」分組，機票就是這樣記的（示範資料的機票是 2026-02-10）。
- 今天在行程區間內 → 就是今天。

**只影響新增**：`blankExp()` 的預設值。
**編輯既有消費（`loadExp()`）一律沿用那筆自己的日期，不得被改掉。**

---

### 35-2　日期列改成「標籤與欄位同一列」的樣式

**Rozi 裁示**：選 B（做成跟出發／回程一樣的欄位），
**但「記在」兩個字要跟日期在同一列**，不是標籤在上、欄位在下。

**所以要做的是 `.fieldrow` 那個形狀，不是 S-02 出發／回程那個形狀。**
畫面上已經有兩個現成的同款：S-02-6「去哪？」與 S-02-7「當地幣別」——
白底、`--r-base` 圓角、一圈 `--ln` 細線，標籤靠左固定寬，值填滿剩下的寬度。

**改法**：

- S-04-10 從 `.daterow`（灰底窄列、貼齊左右邊、底部一條分隔線）
  改成與 `.fieldrow` 同款的欄位，放在 `.fld` 裡（左右內距 14）。
- 一列之內：**「記在」靠左**（沿用 `.fieldrow>.lbl` 的 68px 固定寬與 `white-space:nowrap`）、
  **日期值填滿中間**、**右邊一個方向指示**。
- 日期值的字級用 `--fs-input`（16px），與其他欄位的值一致；
  「記在」用 `--fs-body`（15px）、`--md`、600，與其他欄位標籤一致。
- **欄高與其他輸入框一致**（目前 40px），並沿用 #34-4 已經修好的做法：
  **明確 `height`、`-webkit-appearance:none`、`min-width:0`、
  以及 Safari 專用的 `::-webkit-date-and-time-value`**。
  **這三件事不要重寫一份**——與 S-02 的日期欄位共用同一組 CSS 規則，
  兩處分開寫遲早會走鐘（理由同 `statCard()`／`expenseGroups()`）。
- 位置**維持在標題正下方**，不要移進欄位序列的中間。
- **原生日期選擇器要照常叫得出來**：現在的做法是把 `input[type=date]` 透明疊在整列上，
  這個做法可以留著，也可以改成讓 input 本身就是那個欄位——
  你自己決定哪一種比較不會壞，但**點整列的任何位置都要能開選擇器**。

**右邊那個方向指示**：現在是 `ic('next',16)`。
改成 `ic('calendar',16)`，與 S-02 的日期欄位一致——
它現在長得像「點進去會換一頁」，但實際上是叫出日期選擇器。
（**這一項可被 Rozi 推翻**，但兩處日期欄位的右邊圖示必須一樣，不要一邊日曆一邊箭頭。）

---

### 停止條件（機器判斷得出來）

在 iPhone 390×844、`pointer:coarse` 的真機模式下量測：

1. 把系統日期分別設在**出發日之前／行程區間內／回程日之後**三種情形
   （用注入的方式模擬即可，不必真的改系統時間），
   `blankExp().date` 依序等於 **今天／今天／回程日**。
2. `loadExp()` 載入既有消費後，`g.date` 等於該筆原本的日期（沒有被改成今天）。
3. S-04 日期列中，「記在」與日期值的 `getBoundingClientRect().top` **相同**（同一列）。
4. 該列的高度與 S-02 出發欄位的高度**相同**；
   日期值的 `font-size` 等於 `--fs-input`。
5. 產生 iOS 修正的那三件事（明確 `height`、`-webkit-appearance:none`、
   `::-webkit-date-and-time-value`）**只有一份 CSS 規則**，S-02 與 S-04 共用；
   斷言要檢查「規則的選擇器同時涵蓋兩處」，不是兩處各有一份。
6. 點該列的**左端、中央、右端三個位置**，都會觸發同一個 `input[type=date]`
   （用 `document.elementFromPoint()` 驗，三點打到的元素是同一顆或它的子孫）。
7. `.ui` 之內不再有 `.daterow` 這個 class（或它已無任何樣式殘留）。
8. **改 A 壞 B 攔截網**（沿用 #34 的做法）：
   動手前先把十二個畫面在真機模式各存一張基準截圖，改完再存一份，逐張像素比對。
   **只允許 S-04 有差異，而且只在標題下方那一列與因它而生的整體位移**。
   **其他十一張必須 0.00%。** 任何範圍外的差異就是改壞了，停下來回報。
9. 既有的十四份測試全部仍然全綠。
10. `git diff --stat` 只顯示 `Tripay_原型.html`（`_停點.md`、`專案狀態.md` 除外）；
    `git diff` 變動行數不得超過 90 行。
11. 逐頁截圖重出一份，路徑寫進 `_停點.md`。

### 如果做不出來

**不要修第二次、更不要開 #36。** 停下來，在 `_停點.md` 寫清楚
卡在哪一條停止條件、試過什麼、iOS 上還剩什麼症狀，由 Rozi 決定要不要帶進實作階段處理。
原型檢視在這一節之後結束。

### 交付前檢查

**① 需求覆蓋**：Rozi 這一輪只講兩件事——
「記一筆的日期欄位預設會抓當天日期嗎？」（本節 35-1，答案是不會，現在抓的是出發日）
與「選 B，但『記在』跟日期欄位要在同一列」（本節 35-2）。兩項都在。
她同時裁示六項「看到但沒動」**全部不做、只做紀錄**，紀錄已完成。

**② 收件人檢查**：本節提到的先前決策都已展開成自描述敘述——
`.fieldrow` 的形狀（S-02-6「去哪？」與 S-02-7「當地幣別」那一款：白底、圓角、
一圈細線、標籤靠左 68px 固定寬）、#34-4 已修好的 iOS 日期欄位三件事、
#32-10 的逐頁截圖規則、#34 的像素比對攔截網，都在內文寫清楚，不需回頭讀前面的節。

**③ 五類例外**：本節沒有觸及不可逆動作、花錢、安全性／隱私、重大範疇取捨，
也沒有需要 Rozi 記憶或偏好才能回答的問題。**可以一路做完。**

---
---

# ═══ 原型檢視結束，以下進入實作 ═══

## 🔁 執行順序與規則（**先讀這個，它管住下面全部節次**）

### ⛔ 按下「收」之後，**從「實作-A2」開始，不要跳到最後一節**

原型階段的規則是「讀最新未處理的一節」。**實作階段不適用**——
這個區塊的四節是**有順序的同一批工作**，最後一節（實作-C）依賴前面兩節的產出。

**所以：「收」一律從本區塊第一個還沒做的節次開始，也就是「實作-A2」。**
A2 做完自己接 B，B 做完自己接 C。**不要直接開 B 或 C。**

**檔案裡的節次順序就是執行順序：**

| 步驟 | 節次 | 狀態 |
|---|---|---|
| — | 實作-A | **已完成**（A-0a 型別、A-3 token、A-4 文案常數由終端機做；A-0c／A-1／A-2 由 Cowork 做）**不要重跑** |
| 1 | **實作-A2** | **已完成 2026-09-05**（型別 12 欄、比對表差異 0、vitest 77 過、六個共用元件）**不要重跑** |
| 2 | **實作-B** | 待做 —— 畫面 212 項 ← **「收」從這裡開始** |
| 3 | **實作-C** | 待做 —— 已併入「實作-B」那一節的後半段，同一次做完 |

### ⚡ 這三步**連續跑完，中間不要停下來等「收」**

Rozi 2026-09-05 明確要求：**她只按一次「收」，之後由終端機自己一路做到完。**

所以：**A2 的停止條件全綠 → 直接開始 B；B 全綠 → 直接開始 C。**
每一步做完照樣把結果寫進 `_停點.md`（**用附加，不要覆蓋前一步的內容**），
但**不要停下來等指令**。

### 🔁 如果你這一輪講完話停住了

你的一輪結束時會回到提示字元等輸入，佇列檔寫「不要停」也叫不動你。
Rozi 會再打一次「收」。**你收到「收」時：**

1. 先看 `_停點.md` 最後一段，確認自己上次做到哪裡。
2. **從沒做完的地方接著做，不要重跑已經全綠的部分。**
3. 已標 `[已處理]` 的節次一律不重跑。

### 只有這五種情況才停下來（其餘一律自己判斷、自己做完）

1. 不可逆且無備份的動作
2. 要花錢
3. 安全性／隱私（**注意：production 的 DDL 與權限已由 Cowork 做完，你不需要碰**）
4. 重大範疇取捨（新 Phase、砍功能這種等級；小取捨不算）
5. 需要 Rozi 的記憶或偏好才答得出來的資料題

**技術問題一律自己決定。**「要不要架測試環境」那種**不要再問**——
A2 已經幫你裁定了，往後同類的自己判斷，把選了什麼與為什麼寫進 `_停點.md`。

**卡住的時候**：不要空轉也不要硬猜。把「卡在哪一條停止條件、試過什麼、
你認為的兩三種解法」寫進 `_停點.md` 然後停，Cowork 會來處理。

### 全部做完之後：**先 commit，但不要 push——等 Cowork 複驗過才推**

Rozi 2026-09-05 裁示：**上線前要先經過 Cowork 複驗。**

三步全綠之後：

1. **`git commit`**（不要 push）。commit 訊息格式見下。
2. 把三步的結果寫齊在 `_停點.md`。
3. **等 Cowork 放行**：檢查 repo 根目錄有沒有 `_可以推了.txt` 這個檔案。
   - **沒有** → `sleep 60` 再檢查一次。**最多等 2 小時**。
   - **有** → `git push origin main`，然後把 `_可以推了.txt` 的內容
     （Cowork 會在裡面寫複驗結論）附進 `_停點.md`，收工。
   - **等超過 2 小時還沒出現** → 停下來，在 `_停點.md` 寫「已完成、等待放行逾時」。
4. **Cowork 若改放 `_要修的.md`**（而不是 `_可以推了.txt`）→
   照那份檔案修，修完回到第 1 步。**只修那份檔案點名的東西，不要順手改別的。**

**commit 之前必須全部滿足**：`tsc --noEmit` 零錯誤、`pnpm run build` 通過、
所有測試全綠、三步各自的停止條件全過、`_停點.md` 三步的紀錄都寫齊。
**任何一項沒過就不要 commit**，停下回報。

commit 訊息結尾照既有格式加：
```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_016dEEpsvjvXneWBi8oFXfsv
```

---


> #25～#35 是原型輪次，**已全部結束**。以下的節次是 Phase 1＋1.5 的實作。
> 實作分三節（A／B／C），每一節按一次「收」。分節不是為了讓 Rozi 判斷，
> 是為了每一段結束在一個可驗證、可部署的狀態。**三節都不需要 Rozi 看內容。**

## 實作-A　（已完成，不要重跑）地基：型別對齊 ＋ schema ＋ 權限 ＋ 全域 token ＋ 共用元件 [已處理｜A-0c 卡住，A-1／A-2／A-0b 未做]

> **本節已經過 QA Lead 交叉審查並重寫過一次。** 初稿有六處會讓你卡死或做錯
> （套不套 production 自相矛盾、掃描範圍與護欄互斥、A-0 順序衝突、
> 用 `hashtext` 比對 token 會製造真漏洞、禁 `auth.uid()` 字面量會逼壞 INSERT 政策、
> 欄位名稱與權威規格衝突）。下面是修正後的版本，**不要再參考任何更早的說法**。

### 文件效力順序（衝突時照這個判斷，不要「兩邊都做一點」）

| 位階 | 文件 |
|---|---|
| **0** | **本節（`_指令佇列.md` 的這一節）**——與其他文件衝突時以本節為準，**但衝突本身要寫進 `_停點.md`** |
| 1 | `Tripay_原型.html` —— 畫面、文案、互動、版面的唯一真相 |
| 2 | `專案狀態.md` 決策表 —— 新的一列推翻舊的一列 |
| 3 | `規格_金額未定案與幣別.md` —— S-04 金額與幣別判定 |
| 4 | **實際的 Supabase schema（用 MCP 查）** —— 資料庫結構的真相 |
| 5 | `CLAUDE.md` |

**四份已過期、不得當依據**（都已加頂部警語）：`_實作批次工作清單.md`、
`使用者旅程地圖.md`、`MVP功能清單.md`、`資料模型.md`。
**三個反向陷阱**：設定頁不要補「我的資料／顯示」兩段；不要複用 `EmojiPicker`（整檔刪）；
字體走 Onest、全站移除等寬。

### ⛔ production 與 branch 的分界（最重要，違反就是事故）

- **終端機不得對 production 執行任何 DDL。**
- 所有 migration 用 Supabase MCP **開 branch**（`create_branch`）套用與驗證。
- **反向驗證（把判定函式暫時改成 `true`）只能在 branch 跑**——
  在 production 跑那段時間等於全體使用者可讀全部行程。
- branch 用完**不要刪**，Cowork 要複驗。
- production 由 **Cowork** 套。

---

### A-0c　先對帳：migration 檔案 vs 已套清單

`supabase/migrations/` 的檔案清單，與 `list_migrations` 回報的已套清單**必須一致**。
不一致就**停下回報，不要繼續**——branch 重建出來的 schema 會跟 production 不一樣，
後面所有斷言都是在錯的基準上跑。

**已知現況**：Cowork 已於 2026-09-05 套用並留檔
`011_close_unverified_share_read.sql`（關掉六條沒驗 token 的 share 政策）與
`011b_close_unverified_cover_share_read.sql`（關掉 storage 上同一形態的 `covers: share read`）。

---

### A-0a　補齊「已上線四個 migration」的型別

`src/types/database.ts` 落後：
`Trip` 缺 `cover_path`(008)、`kind`／`card_id`(007)、`settlement_mode`／`hub_member_id`(010)；
`TripMember` 缺 `person_id`(006)。

⚠️ 補完之後 **`TripListPage.tsx:37` 會報錯**——它已經在查 `kind`，現在靠型別沒擋才過。
報錯就修，那正是補型別的目的。

---

### A-1　migration 012：純欄位／約束／索引

**七項缺口**（用途見各列，缺了就是那個功能做不出來）：

| # | 欄位 | 定義 | 為了什麼 |
|---|---|---|---|
| 1 | `expense_splits.split_amount_foreign` | numeric null | **P1-0：唯一一條會靜默算錯帳的。** 各自金額目前只有台幣語意，填外幣會被當台幣加總，不報錯 |
| 2 | `expenses.split_fill_currency` | **`text not null default 'TWD' check (split_fill_currency in ('TWD','FOR'))`** | 各自金額是用哪種幣別填的。**一定要有 default**，否則既有 404 筆全是 NULL，實作-B 就多一個「NULL 當成什麼」的未定義分支——那正是 P1-0 的同一個坑 |
| 3 | `expenses.individual_member_id` | uuid null → `trip_members(id)` **`on delete restrict`** | 「只算一個人」要能選人（S-04-17）。**Rozi 已裁示：有帳款紀錄的成員不能刪**，所以是 restrict 不是 set null |
| 4 | `trips.payment_methods` | jsonb null | 支付方式清單（S-02b-11） |
| 5 | `expenses.payment_label` | text null | 行程自訂的支付方式（Linepay），enum 塞不下（S-04-9） |
| 6 | **`trips.cash_rate_twd` ／ `trips.cash_rate_foreign`** | numeric null ×2 | 行程現金匯率。**⚠️ 用這兩個名字，不要用 `rate_twd`／`rate_for`**——後者是 `_盤點_實作缺口.md:257` 的舊寫法，權威規格 `規格_金額未定案與幣別.md` §2A.5 寫的是 `cash_rate_twd`／`cash_rate_foreign` |
| 7 | `expenses.category_emoji_manual` | boolean not null default false | 手動改過的類別 emoji 不被標題覆蓋（S-04-3） |

**色調序號**：`trips` 加一欄（例如 `tone_seq smallint`）存建立當下算出的循環色號 0–7。
**建立時決定並存起來，不可用「清單第幾筆」**——否則刪掉一趟，後面所有行程顏色集體位移。

**四項共編前置**（Phase 2 才用，現在加最便宜）：
`trip_members.user_id uuid null` ＋ `unique (trip_id, user_id)`；
`expenses.updated_by uuid null`（之後才加補不回歷史）；
`trip_members.role text`（可編輯／只能看）。

**索引**：`create index on trips(share_token) where share_token is not null`
——A-2 的 RPC 每次分享頁載入都要查它，加欄位這一節是最便宜的時機。

**✅ 這一題 Rozi 已經裁示了（2026-09-05）**：「如果已經有記錄他的帳款，就不能刪除。」

所以**不是** `set null`，是**擋住不讓刪**：

- 資料庫層：`on delete restrict`。
- **這與既有規則一致，不是新規則**——S-02b-7 本來就是「已有紀錄的成員不能移除」，
  現在只是把「有紀錄」的定義補上一種：**除了有消費、有分帳，
  還包含「有一筆消費是只算他一個人的」**。
- UI 層（實作-B 做）：三種情形共用同一句擋下訊息，
  沿用原型現有的「`{名字}` 已經有消費或分帳紀錄，不能移除。要拿掉的話，請先刪掉相關消費。」
- **停止條件**：試著刪一位有 `individual_member_id` 指向他的成員 → 資料庫必須拒絕；
  刪一位完全沒有任何紀錄的成員 → 必須成功。兩個方向都要驗。

---

### A-2　migration 013：權限收斂 ＋ 分享改成後端驗 token

**背景**：現存政策每條都各自寫死 `owner_id = auth.uid()`。共編時要一條條改，
**漏一條就是別人的帳被看到**。另外六條沒驗 token 的 share 政策已由 011／011b 關閉，
**所以線上的分享頁現在讀不到資料，要靠這一節補回來。**

**執行前先傾印一份還原點**：把當下 public 與 storage 的政策全部傾印成
`supabase/backups/rls_policies_<date>_before_013.sql`。
（既有的 `rls_policies_2026-09-05_before_refactor.sql` 是 **011 之前**的狀態，不是 013 之前的。）

**執行前先查一條前置條件**：
`select relforcerowsecurity from pg_class where relname in (...)` 五張表**必須全為 false**。
若有 true，`security definer` 函式在自己的政策裡查同一張表會**無限遞迴**——停下回報，不要硬上。
（Cowork 已於 2026-09-05 查證：九張表全部 false。）

#### ① 收斂成一個判定函式

建 `can_access_trip(p_trip_id uuid) returns boolean`，內容**先維持現狀語意**：

```
security definer
set search_path = public, pg_temp      -- pg_temp 不可省，否則留著 temp schema 的遮蔽面
stable
body: exists(select 1 from trips where id = p_trip_id and owner_id = (select auth.uid()))
```

- `auth.uid()` 寫成 **`(select auth.uid())`**——逐列呼叫會變成每列一次子查詢。
- **撤 PUBLIC 的 EXECUTE**：`revoke execute ... from public; grant execute ... to authenticated;`
  （預設 grant to public，等於 anon 拿到一個存在性探測器。）
- 政策改成呼叫它。**這一步不改變任何人看得到什麼**，只是同一句話從寫 N 遍變成寫 1 遍。
  共編上線時只改這個函式（加「或我是這趟的成員」），全部政策同時生效。

**⚠️ INSERT 政策改不了，不要硬改。** `trips` 的 `WITH CHECK (owner_id = auth.uid())`
在那一列還不存在時無法用 `can_access_trip(id)` 表達。
**INSERT 政策維持原樣，這是正確的，不是漏做。**

#### ② 分享改成後端驗 token

建 `get_shared_trip(p_token text)`：`security definer`、`stable`、
`set search_path = public, pg_temp`、**`grant execute to anon`**（不加的話分享頁直接 403）。

- 條件：`t.share_token is not null and t.share_token = p_token`。
  `is not null` 那半條不可省——否則 `p_token` 傳 null 時若有人寫成
  `is not distinct from` 就會全開。
- **回傳欄位一律逐欄列出，禁止 `select *`。**
  **明文排除**：`owner_id`、`user_id`、`created_by`、`updated_by`、`share_token`、`card_id`。
  （A-1 剛加的 `trip_members.user_id` 若跟著回傳，匿名訪客會拿到每位成員的帳號 UUID。）
- 一次回傳一包：行程、成員、消費、分帳、結算、結算明細。
- `SharePage.tsx` 改成呼叫這個 RPC，**不再直接查表；不要恢復 anon 對這些表的 SELECT**。

**token 比對規則（整段照做，不要自己發揮）**：

- 一律用**完全相等**比較（`=`）。**禁止** `like`／`ilike`／`position`／`starts_with`／
  `substring`／任何前綴比對。
- **禁止用 `hashtext()` 比對**——那是 32-bit 非密碼學雜湊，碰撞可在幾秒內構造，
  等於「隨便找一個雜湊值相同的字串就能進」。
- **不要做「固定時間比較」**——Postgres 沒有這個原語，索引查找本身就會洩漏時間。
  防線是熵。**已查證：現有 4 個 token 全部是 UUIDv4（36 字元、122 bit 熵），足夠，
  不需要重新產生。**（重新產生會讓 Rozi 已分享出去的連結全部失效，那要先問她，本節不做。）

#### 停止條件（權限，全部在 **branch** 上跑）

1. **政策數量不變**：套用前後各跑一次
   `select count(*) from pg_policies where schemaname='public'`，
   `N_after = N_before`。**兩個數字都寫進 `_停點.md`。**
   （不要用任何寫死的數字——舊文件寫 22、實際查是 41，011 關掉六條後是 35，各說各話。）
2. **只剩 INSERT 政策可以含 `auth.uid()`**：
   `select policyname, cmd from pg_policies where schemaname='public'
    and (qual || coalesce(with_check,'')) like '%auth.uid()%'`
   → 回傳的每一列 `cmd` **都必須是 INSERT**。任何 SELECT／UPDATE／DELETE 仍含它即為紅。
   （**不要**寫成「完全不得出現 `auth.uid()`」——那會逼你把 INSERT 政策改成永遠 true，
   那是權限繞過。）
3. **anon 讀不到**：以 `role anon` 查 `trips`／`expenses`／`trip_members`／`settlements`／
   `expense_splits` **全部 0 筆**；`storage.objects` 中 `covers/` 前綴也 0 筆。
4. **owner 不受影響**：以 `role authenticated` ＋ owner 的 `sub` 查，
   得到 **4 趟／404 筆消費／16 位成員**（與改動前相同）。
5. **RPC 行為**（**必須以 `role anon` 呼叫**，用 owner 身分測會假通過）：
   - 正確 token → 回傳該趟完整資料；
   - **四種錯誤輸入都要測**：`null`、`''`、隨機字串、**正確 token 的前 8 碼**
     （最後這種才驗得出有沒有用前綴比對）→ 全部回空，
     且**不得回傳任何其他行程的任何一列**。
   - **回傳 JSON 的鍵集合 == 白名單**，出現白名單外的鍵（尤其 `owner_id`／`user_id`／
     `created_by`／`share_token`）即為紅。
6. **反向驗證（只在 branch）**：把 `can_access_trip` 內容暫時改成 `true`，第 3 條要變紅；
   改回來要變綠。`_停點.md` 記錄「拿掉什麼 → 哪一條紅了」。
7. **效能留痕**：改動前後各跑一次
   `explain analyze select * from expense_splits;`，兩個時間都寫進 `_停點.md`。
   （逐列呼叫函式是真實成本，404 筆不會出事，但要留下量測而不是默默劣化。）

---

### A-0b　在 branch 上重新產生型別

012／013 套到 branch 之後，用 `generate_typescript_types` 重新產生，
把新欄位補進 `database.ts`，再跑一次 `tsc`。

**停止條件**：`database.ts` 的 `Trip`／`TripMember`／`Expense`／`ExpenseSplit` 欄位集合
**等於** branch 實際 schema 的欄位集合。`_停點.md` 附三欄比對表
（型別有／schema 有／是否相符），**差異列數必須為 0**。

---

### A-3　全域 token（排在所有畫面改動之前，否則每個畫面改兩次）

#### 從原型搬什麼、不搬什麼（**這一段照字面做，不要自己判斷**）

原型的 `:root`（第 9–25 行）是**檢視器外框的深色主題**，不是 App 的 token。
App 的 token 全部在 **`.ui`（第 158–173 行）**。

**從 `:root` 只取兩樣**：
`--sans:"Onest","PingFang TC","Noto Sans TC",-apple-system,sans-serif`
與 `--r-base:10px` `--r-panel:20px` `--r-icon:50%` `--r-chip:999px`。

**`:root` 其餘一律不搬**：`--pg*`／`--pgink*`／`--pgrule`／`--mark`／`--okd`／`--nod`，
以及 `[data-theme]` 與 `prefers-color-scheme` 兩個區塊。
**本專案沒有深色模式的決策，不要把深色殼帶進 App。**

**原型的字體是用 Google Fonts CDN 載的（第 3–6 行），那部分不要照抄**——
原型不經 build，App 經 build 且 **Google Fonts 被 egress 擋**，
App 要用 `@fontsource/onest`。

**從 `.ui` 取全部九個字級與八個顏色**，原封搬進 App 的 `:root`：

```
--fs-tag:12  --fs-sub:13  --fs-body:15  --fs-input:16  --fs-strong:17
--fs-title:20  --fs-money:16  --fs-money-lg:26  --fs-logo:44
--w:#1276C4  --wi:#0F5E9E  --in:#276E45  --out:#B03A22  --dg:#9B1B14
--ln:#DFE1DA --gr:#6A7980  --md:#4C5B64
```
內容色 `#151C21` 目前是字面量，一併收成變數。
**是九個字級，不是六個**（六階是指「成句文字」那六階，16money／26／44 是另外三個）。

#### 其他

- **輸入框硬性 ≥16px**（iOS 一聚焦就放大整頁）。輸入框高度階梯一併定義
  （目前全站三種 41／25／32px，沒有階梯）。
- icon：Feather，`fill:none`＋`stroke:currentColor`＋線寬 1.7，只搬原型 `ICON` 表裡有的鍵。
- **icon 按鈕三個層級**（不要「統一」成一種）：畫面層級動作＝40×40 正圓＋描邊；
  **列內動作＝裸 icon 無容器、可點區用透明擴張區撐到 44×44**；方向指示＝裸 icon 不可點。
- **全站移除等寬**，金額靠字重 500–600 ＋ `tabular-nums`。
- 有圓角的容器，左右內距至少比圓角多 4px。

#### 停止條件（**掃描範圍＝`src/` ＋ `index.html` ＋ `package.json`；不含 `Tripay_原型.html`、不含 `supabase/`**）

1. `src/` 內 `font-size:` 出現次數 **≥30**（下限，防「目標不存在所以通過」），
   且**每一處**都是 `var(--fs-…)`；`--fs-*` 的定義集合與上面九個名稱與數值**逐一相符**（附表）。
2. `src/` 內 `border-radius:` 出現次數 **≥20**，且每一處是 `var(--r-…)`
   或 `calc(...var(--r-…)...)`（原型自己就有三處合法 calc）。**禁止裸數字與裸 `50%`／`999px`。**
3. 等寬字體禁字清單全部 0 命中：`monospace`、`IBM Plex Mono`、`ui-monospace`、
   `SFMono`、`Menlo`、`Consolas`。（只搜 "mono" 會漏 `Consolas`。）
4. `rg -n "fonts\.googleapis\.com|fonts\.gstatic\.com" src index.html` → **0 命中**。
5. `src/` 內不存在 `[data-theme]` 或 `prefers-color-scheme`（沒有深色模式）。

---

### A-4　六個共用元件（排在畫面之前，否則兩處各寫一份）

原型已經抽好，**照它的邊界，不要重新切**。名稱都已查證存在：

| 元件 | 原型裡的名字 | 必須被這兩個檔案引用 |
|---|---|---|
| 統計卡 | `statCard()` | `ExpenseListPage.tsx` ＋ `SharePage.tsx` |
| 轉帳明細 | `transferView()` | `SettlementPage.tsx` ＋ `SharePage.tsx` |
| 消費列＋日期分組（含倒序） | `expenseGroups()`／`expenseRow()` | `ExpenseListPage.tsx` ＋ `SharePage.tsx` |
| emoji 就地編輯 | `beginInlineEdit()`／`commitInlineEdit()` | `TripFormSheet.tsx` ＋ `ExpenseFormSheet.tsx` |
| ⋯ 選單 | `s03MoreSheet()` | `ExpenseListPage.tsx`（沿用既有 `.sheet`，不新造第三種彈層） |
| 分段控制 | `.seg` | `ExpenseListPage.tsx` ＋ `ExpenseFormSheet.tsx` |

**未定案文案只准寫一次**：`MSG_NO_RATE`／`MSG_TWD_PENDING`／`MSG_FILL_ONE`
三個常數照搬，各 **≥2 處引用同一個常數**。

**停止條件**：不是驗「存在」，是驗**上表點名的那兩個檔案都真的 import 它**
（只驗「引用檔案數 ≥2」不夠——barrel 檔各 import 一次也會過）。

---

### 護欄：本節允許變什麼

**允許的外觀變化（預期，不必回報）**：字體、字級、字重、行高、圓角、顏色、間距
因換 token 而變；輸入框高度統一；列內動作可點區撐到 44×44。

**允許的版面變化——只有這三項，照做不必問**（抽共用元件的必然結果）：

- S-06 統計卡改用共用元件後，**外幣格與人均消失**（盤點 S-06-5／S-06-6「要移除」）
- S-06 消費明細改成日期分組排版，**列上不寫日期**（盤點 S-06-9）
- S-06 待填列補上左邊框，與 S-03 一致（盤點 S-06-10）

**禁止**：新增或刪除上面三項以外的任何畫面資訊、改任何文案字串、改欄位順序、
改導覽結構、改 hero 漸層。**這些全部留到實作-B。**

**斷言**：對 S-01／S-03／S-04／S-05／S-06 各跑一次 render，取 `textContent` 的字串集合
與改動前 diff；差異必須全部落在上面三項之內，**多一項即紅**。

看到別的問題**不要順手處理**，寫進 `_停點.md` 的「順手看到但沒動」清單。
（唯一例外：A-0a 補型別後冒出的編譯錯誤，那是本節造成的，要修。）
**不要碰 Rozi 已裁示「只做紀錄」的六項**（見 `_延後與不做清單.md` 與問題清單 P4）。

---

### 全節停止條件

1. `tsc --noEmit` 零錯誤（獨立一步，不要讓 vite 吞掉型別錯）；`pnpm run build` 通過。
2. A-0c 的對帳結果一致（不一致就是停下回報，不是通過）。
3. 012／013 在 **branch** 上 `apply_migration` 成功，且事後 `list_tables` 查出的欄位集合
   **包含本節列的全部欄位**（逐欄斷言，缺一即紅）。**不要對 production 執行 DDL。**
4. A-2 的七條停止條件全過，含反向驗證與效能留痕。
5. A-0b 的比對表差異列數為 0。
6. A-3 的五條掃描全過。
7. A-4 的檔案對引用斷言全過。
8. 既有測試全綠。**每一條新斷言都要有反向驗證並留下痕跡**——
   `_停點.md` 逐條列「拿掉什麼 → 哪一條紅了」。只寫「已做反向驗證」不算。
9. `git diff --stat` 只動：`src/`、`supabase/migrations/`、`supabase/backups/`、
   `package.json`／lock、`_停點.md`、`專案狀態.md`。**不動 `Tripay_原型.html`。**

### `_停點.md` 必須包含

- A-0c 的對帳結果
- A-0b 的三欄欄位比對表
- A-2：`N_before`／`N_after`、anon 五張表與 storage 的實際筆數、owner 的 4／404／16、
  RPC 五種輸入的實際回傳、回傳鍵集合、反向驗證哪一條變紅、`explain analyze` 前後時間
- 012／013 的檔名（Cowork 要據此套 production）
- 「順手看到但沒動」清單
- 與其他文件發生的任何衝突

### 交付前檢查

**① 需求覆蓋**：本節＝Tech Lead 稽核的階段 0～3，加 Rozi 2026-09-05 裁示的兩件權限工作。
畫面層 212 項在實作-B，引擎與測試在實作-C。

**② 收件人檢查**：文件效力順序、production/branch 分界、七項缺口各自的用途、
政策現況與已關閉的兩個漏洞、六個共用元件的邊界、token 的實際數值與來源行號，
都已在本節展開，不需要回頭讀前面的節。

**③ 五類例外**：012 全是加欄位／加約束／加索引（可回滾，Rozi 已同意）。
013 屬安全性，**Rozi 已於 2026-09-05 明確同意**（「可以」＋「關」），備份與還原點就位。
`individual_member_id` 的刪除行為 **Rozi 已於 2026-09-05 裁示**（有帳款紀錄就不能刪 → `restrict`），
**本節沒有任何待決策項目**。無不可逆動作、無花費、無重大範疇取捨——**可以一路做完。**

---

## 實作-A2　（第 1 步）A-0c 已解、012／013 已套 production，剩下三件事 [已處理]

### 🔧 步驟 0：先修好 git 狀態（**動任何程式碼之前先做這個**）

Cowork 端在你的 repo 裡留下兩個爛攤子，**不修的話你第一次 `git add` 就會失敗**：

1. **`.git/index.lock` 還在**（Cowork 那邊的掛載不允許刪檔，刪不掉）。
   你在 macOS 上有權限，直接刪：
   ```
   rm -f .git/index.lock .git/_stale_index.lock.bak
   ```
2. **`.git/index` 是舊的**。Cowork 用 `GIT_INDEX_FILE` 指到別處 commit（為了繞過那個 lock），
   所以預設的 index 還停在 commit 之前，`git status` 會顯示 60 行**假的**改動，
   其中還有幾個 `D`（刪除）——**照著 commit 下去會誤刪檔案**。
   HEAD 現在在 `750d857`（Cowork 的實作-A commit，**內容是對的，不要 reset 掉它**）。
   只要把 index 對回 HEAD：
   ```
   git reset
   git status --short          # 應該只剩真正還沒 commit 的東西
   ```
   `.git/objects/**/tmp_obj_*` 那些暫存檔也刪一刪（同樣是刪不掉留下的）：
   ```
   find .git/objects -name 'tmp_obj_*' -delete
   ```

**確認 `git status` 乾淨、`git log --oneline -1` 是 `750d857` 之後**，再往下做。
如果 HEAD 不是 750d857，停下來回報，不要自己猜。


**你 A-0c 的判斷是對的，攔得好。** 少登記的三筆會讓 branch 從 004 開始套在空資料庫上，
斷言全都會跑在錯的基準上。Cowork 端已處理完，往下走。

### ✅ Cowork 端已完成，**你不要重做**

**1. A-0c 已解**：`001`／`002`／`003` 已補登記進 `supabase_migrations.schema_migrations`
（version `20260703000001`／`2`／`3`），**沒有重跑 SQL**，物件本來就在。
現在檔案 12 個、`list_migrations` 12 筆，對得起來。

**2. `012_phase1_columns.sql` 已寫好並套 production。** 檔案在 repo。
12 個欄位逐欄查證全部 present：
`expense_splits.split_amount_foreign`／`expenses.split_fill_currency`（`not null default 'TWD'`
＋check）／`expenses.individual_member_id`（FK **`on delete restrict`**，Rozi 裁示有帳款紀錄不能刪）／
`trips.payment_methods`／`expenses.payment_label`／**`trips.cash_rate_twd`＋`cash_rate_foreign`**
（依規格 §2A.5 命名，不是 `rate_twd`／`rate_for`）／`expenses.category_emoji_manual`／
`trips.tone_seq`（0–7 check）／`trip_members.user_id`＋`(trip_id,user_id)` unique／
`expenses.updated_by`／`trip_members.role`／`trips_share_token_idx`。

**3. `013_rls_consolidation_and_share_rpc.sql` 已寫好並套 production。**
檔案與驗證數字在 repo（`013_*.sql` ＋ `README_013.md`）。摘要：

- 三個 `security definer` 判定函式 `can_access_trip`／`can_access_expense`／
  `can_access_settlement`，都是 `stable`、`search_path = public, pg_temp`、
  內部 `(select auth.uid())`、`revoke execute from public` ＋ `grant to authenticated`。
- **六張「以行程為範圍」的表**的政策改成呼叫函式。
  **cards／people／profiles 不動**（不是行程範圍的資料，共編不影響）。
  **`trips: owner insert` 維持 `auth.uid() = owner_id`**——那一列還不存在，
  無法用 `can_access_trip(id)` 表達，這是正確的不是漏做。
- `get_shared_trip(text)` RPC，`grant execute to anon`，逐欄白名單，
  排除 `owner_id`／`share_token`／`card_id`／`created_by`／`updated_by`／`user_id`／
  `person_id`／`linked_profile_id`，且不回傳 `deleted_at` 非空的消費。

**實測驗證（已通過，不必重驗）**：
anon 讀 trips／expenses／trip_members／settlements／expense_splits／storage covers
**全部 0 筆**；owner 讀 **4／404／16**（不變）；政策數 **35 → 35**；
RPC 正確 token 回該趟（132 筆消費），`null`／`''`／隨機字串／**正確 token 前 8 碼**
**四種全部回 null**。

**4. A 的成果已 commit**（`750d857`，含你做的 A-0a／A-3／A-4 文案常數）。**尚未 push。**

---

### 你要做的三件（做完這節，A 才算完成）

#### ① A-0b　重新產生型別

12 個新欄位還沒進 `src/types/database.ts`。用 Supabase MCP 的
`generate_typescript_types` 重新產生後補進去，跑 `tsc --noEmit` 零錯誤。

**停止條件**：`Trip`／`TripMember`／`Expense`／`ExpenseSplit` 的欄位集合
**等於**實際 schema 的欄位集合，`_停點.md` 附三欄比對表（型別有／schema 有／相符），
**差異列數為 0**。

#### ② 架前端測試環境（**你問的那題，Cowork 裁定：要架**）

理由：實作-B 的驗收方式本來就是「把畫面跑起來跟原型比對文字集合與關鍵量測」，
沒有 render 環境根本驗不了；實作-C 的五組版面回歸測試也要它。
**沒有這張網就重構 2,665 行五個檔案，正是一路在防的「改 A 壞 B」。**

裝 `jsdom` ＋ `@testing-library/react` ＋ `@testing-library/jest-dom`，
`vitest.config.ts` 加一個 jsdom 環境的 project（**不要動現有跑 supabase function 的那個**）。
supabase client／react-query／router 需要的 mock 一併備好，做成可重複用的 test util。

**停止條件**：能把 S-01 render 出來並斷言畫面上有「Tripay」字樣；
`pnpm test` 既有測試仍全綠（新舊兩個環境都要跑到）。

#### ③ A-4　六個共用元件

原型已經抽好，照它的邊界，名稱都已查證存在：
`statCard()`／`transferView()`／`expenseGroups()`＋`expenseRow()`／
`beginInlineEdit()`＋`commitInlineEdit()`／`s03MoreSheet()`／`.seg`。

**這一步只建立元件本身、不改任何畫面**——畫面接上去是實作-B 的事。
所以「必須被兩個檔案引用」的斷言**這一節先不驗**（還沒有第二處），
改驗：每個元件**只有一份實作**，且**行為與原型一致**
（用①架好的環境，餵原型同一組資料，比對 render 出來的文字集合）。

---

### 護欄

- **不要重寫 012／013**，也**不要對 production 執行任何 DDL**。那兩個已經套好了。
- 本節仍**不改任何畫面版面**。
- 看到別的問題寫進「順手看到但沒動」，不要順手處理。
- `_停點.md` 要包含：型別三欄比對表、測試環境跑起來的證明、六個元件的行為比對結果、
  反向驗證逐條記錄（拿掉什麼 → 哪一條紅了）。

### 你上一節回報的三項「順手看到但沒動」，Cowork 的處置

1. **89 處寫死色碼**——留到實作-B。八個警示暖色底原型沒有對應 token，B 做到那幾個畫面時再新增。
2. **金額落在兩階（17／16）**——語意判斷，留到實作-B 逐處決定。
3. **`EmojiPicker.tsx` 還在**——正確，刪它是實作-B 的 B-7，要等就地編輯真的接上畫面之後。

### 交付前檢查

**① 需求覆蓋**：本節＝實作-A 剩下的 A-0b、測試環境（你問的裁定）、A-4 六個元件。
A-0c／A-1／A-2 已由 Cowork 完成。

**② 收件人檢查**：012／013 的內容與驗證數字、Rozi 對成員刪除的裁示、
測試環境要裝什麼與為什麼，都已在本節展開。

**③ 五類例外**：無。**production 的 DDL 已經做完，你不要碰。可以一路做完。**

---

## 實作-B　（第 2 步）畫面：212 項 [已處理]

> ✅ **實作-A2 已於 2026-09-05 完成（標了 [已處理]），這一節就是「收」要做的起點。**
>
> ## 🚨 一輪要做完，不要做完一個子節就停下來寫報告
>
> **B-1～B-7 加上後面的「實作-C」，是同一輪的工作。**
>
> 你在 2026-09-05 12:54 只做完 B-1 就寫了 `_停點.md` 並結束該輪。
> **這樣做讓 Rozi 必須反覆手動輸入「收」，而她明確要求不要這樣。**
> macOS 不允許 Cowork 代打字，所以你每停一次，就是佔用她一次時間。
>
> **規則：**
> 1. 一個子節（B-1、B-2…）完成、測試綠 → **立刻接著做下一個子節，不要寫報告、不要總結、不要停。**
> 2. **只有這三種情況才寫 `_停點.md` 並結束該輪：**
>    - B-1～B-7 **全部**做完，而且「實作-C」也做完
>    - 遇到五類例外（不可逆無備份／花錢／安全性隱私／重大範疇取捨／需 Rozi 記憶偏好）
>    - 同一個問題修三次還是紅
> 3. 子節之間的進度不要寫進 `_停點.md`，改寫進 `Claude outputs/_實作B進度.md`（想寫多少都可以，不限行數），
>    這樣中途被打斷也接得回來。`_停點.md` 保留給最後那一次總報告（≤50 行）。
> 4. 如果你這一輪還是停了，Rozi 會再打「收」——你**先讀 `Claude outputs/_實作B進度.md`**，
>    從沒做完的子節接著做，**不要重跑已經全綠的**。
>
> **2026-09-05 12:54 的進度：B-1 已完成全綠（TripFormSheet 15 項）。下一個是 B-2。**

> **前置**：實作-A 必須全部完成且 migration 已套 production。
> A 給了這一節：對齊的型別、12 個新欄位、收斂後的權限、全域 token、六個共用元件。
> **B 只做畫面。引擎、hub 結算路徑、測試移植在實作-C。**

### ⚠️ 這一節明寫「全站」

Rozi 2026-09-05 的規則是「指令只能動明確點名的元素，只有明寫『全站』才可跨畫面掃改」。
**本節就是那個例外：這是全站換視覺的批次，範圍是 S-00～S-07 共 212 項。**
但「全站」不等於「什麼都可以動」——見下方〈允許與禁止〉。

### 唯一真相：`Tripay_原型.html`

畫面長什麼樣、文案怎麼寫、點了會怎樣，**一律看原型**，不要回頭查別的文件。
原型的**操作模式**（不是標註模式）就是產品該有的樣子。
`_盤點_實作缺口.md` 只用來查「這一項對應哪個檔案哪一行」，**不用來查它該長什麼樣**。

四份已過期文件不得當依據（都已加頂部警語）。三個反向陷阱：
設定頁不要補「我的資料／顯示」兩段；`EmojiPicker.tsx` 整檔刪除；字體 Onest、無等寬。

---

### 節次順序（有硬相依，不可調換）

```
B-1  S-02b 行程層設定    ← 支付方式必須早於 S-04-9；匯率必須早於 S-04-38/39
B-2  S-01 首頁 → S-00 登入 ← 設定入口先做，S-07 才進得去
B-3  S-03 行程頁 → S-03d 未定案清單
B-4  S-04 記一筆         ← 最大一項，且有一條「必須同一個 commit」的硬相依
B-5  S-05 結算
B-6  S-06 分享頁         ← 前面共用元件都做完才有得共用
B-7  S-07 設定 ＋ 刪除 EmojiPicker.tsx
```

---

### B-1　S-02b 編輯行程（16 項）

`src/components/TripFormSheet.tsx`（與 S-02 同一個元件）。

三段**全新**區塊，順序照原型：
① **這趟的支付方式**（S-02b-11）——清單管理，可新增、**可拖曳排序**（只能從把手起拖，
`touch-action:none` 只給把手，列本身仍要捲得動）、**已被使用的不可刪**（點了出中性色訊息）。
存進 A-1 新增的 `trips.payment_methods`。
② **這趟的現金匯率**（S-02b-12）——**只有兩個輸入框**，
「1」擺哪一欄依幣別決定（規格 §2b.3a），「1」的那欄排在上面。
存進 `trips.cash_rate_twd`／`cash_rate_foreign`。
③ **這趟怎麼結算**（S-02b-13）——誰欠誰就轉給誰（預設）／都轉給同一個人；
選第二項要選人，預設帶代墊最多的成員。存進 `trips.settlement_mode`／`hub_member_id`。

其餘：關閉鍵改 `.ic2`；標籤欄 68px 且 `white-space:nowrap`；
回程可留空＋單日確認（S-02-20 文案照原型定稿）；砍「這是我」勾選與三段灰字；
成員 emoji 改就地編輯（用 A-4 的共用元件）；名字空白時「加進來」disabled；
編輯模式成員列呈唯讀態；刪除行程移進本頁最下方。

**封面區**：**整區隱藏，不是刪掉**（`COVER_UI = false` 之類的開關），
Phase 2 封面上傳上線時打開就回來。編號 S-02b-2／3／4／5／10 在盤點表標「暫時隱藏」。
色調改自動：**先比對 8 組關鍵字，比不到才用 A-1 存好的 `tone_seq` 循環色**，
**停用單一 fallback 色**。

**成員刪除守衛**：三種情形共用同一句話——有消費、有分帳、
**有一筆消費的 `individual_member_id` 指向他**。沿用原型現有文案。

---

### B-2　S-01 首頁（8 項）→ S-00 登入（4 項）

`TripListPage.tsx`：標題列右上加**設定入口**（S-01-15，**四個新畫面的必要前提，先做**）；
全形＋改 `ic('add',16)`；空狀態補主要按鈕；**卡片下緣移除成員 emoji（只有首頁）**；
G-06 分享橫幅與複製連結鈕移除。

`LoginPage.tsx`：移除 App icon；底色改工作色深夜版；
**容器高度跟著可用高度走（`100dvh`），不要寫死 `min-height:520px`**——
那是「整體偏上」的根因；tagline 與登入鈕共用 240px 欄、單行不折；
文案「用 Google 繼續」。

---

### B-3　S-03 行程頁（33 項）→ S-03d（4 項）

`ExpenseListPage.tsx`，**改動量最大的檔案之一**。

- hero 用 `destinationOf()`；**hero 副標只有日期區間，沒有成員 emoji**。
- **動作重組**：hero 右上只留「返回」與「⋯」兩顆。編輯／複製／分享／封存／刪除
  全部進 ⋯ 選單（A-4 的共用元件），內容依狀態變（準備／旅途中 4 項、已結算 5 項、
  已封存 3 項），**刪除是唯一著色的一項**。
- **消費／結算分段控制**（S-03-33）：切換檢視不是動作。「結算」分頁的內容就是 S-05 整頁。
- 統計卡用 A-4 的共用元件：總花費可展開 → 每人分擔列 → 註腳。
- **日期分組由新到舊，同一天之內依記錄先後由新到舊**（共用元件已含此邏輯）。
- G-05 分享橫幅五處全清，含 `sessionStorage` 的 `g05-dismissed`。
- 未定案入口（S-03-29）：「有 N 筆還沒算清楚 ›」，N＝0 整條不顯示。
- 底部主鈕依狀態三種（旅途中「記一筆」／已封存「重新開啟行程」／已結算沒有）。
- 刪除確認改成打「刪除」二字才 enable，只有這裡用 `--dg` 實心。
- **既有 bug 一併修**：`openEdit` 沒有狀態守衛，封存／已結算的行程仍點得進去編輯
  （`ExpenseListPage.tsx:450` 附近）。封存＝預設只讀，這是既有決策。
- ⚠️ `ExpenseListPage.tsx:553` 的註解寫「複製行程」但下面接的是**刪除**對話框
  （複製在 :594）。改 S-03-25 時很容易改錯地方。

`S-03d` 未定案清單：**規格就是原型的 `renderS03d()`**，照它做。
兩種進入方式（點統計卡的人／點列表上方入口）走同一個畫面，只有標題與範圍不同。

---

### B-4　S-04 記一筆（39 項，最大一項）

`ExpenseFormSheet.tsx`。**金額與幣別的判定行為一律以
`規格_金額未定案與幣別.md` 為準**（R1–R10 都在那裡），本節不重述規則。

#### 🔴 一條硬相依：這兩項必須在同一個 commit

- **S-04-8**：拿掉「之後再填」toggle，空欄一律放行存檔。
- **S-04-29**：空欄時**自動寫 pending 旗標**。

**分開做的中間狀態是 `twd_amount = null` 且 `twd_pending = false`
→ 結算整趟回 422。** 現在是靠 `:273` 擋下輸入才沒事，拿掉 toggle 就必須同時補上。

#### 其餘

- 日期列：**「記在」與日期同一列的欄位**（`.fieldrow` 形狀，白底圓角一圈細線，
  標籤靠左 68px，右邊日曆 icon，高度與其他輸入框一致），**位置在標題正下方**。
  **預設今天**；今天超過回程日則取回程日；今天在出發日之前不夾（會落進「出發前」）；
  回程留空則無上界。**編輯既有消費沿用該筆原本的日期。**
- 金額區：外幣／台幣兩欄；**「填的是哪種幣別」切換**（存 `split_fill_currency`）；
  外幣總額自動補入並標「自動」、可覆寫；自動均分值走 **placeholder**（空白 ≠ 0）；
  差額三級（剛好／≤1% 淡色／>1% 橘色）**三種都可存檔，移除任何加總阻擋**。
- 各自金額：**存進 `split_amount_foreign`，不要塞進 `split_amount`**——
  那是 P1-0 靜默算錯帳的根因。逐人列用 `<label for>`、列高 ≥48。
- 支付方式（S-04-9）：**讀該行程的 `payment_methods`**，順序跟著清單，預設第一項。
  **不要有任何寫死的常數。**
- 「只算一個人」要能選人，存 `individual_member_id`。
- 類別 emoji：就地編輯（A-4 共用元件），**手動改過就寫 `category_emoji_manual=true`，
  之後改標題不再覆蓋**。
- 「當場就清了」與「贊助／回饋」用全站統一的 chip 樣式。
- 存檔四種 toast（整筆未填／1 人／2 人／3 人以上），文案照原型。
- 刪除文案改誠實說法（是軟刪，不要寫「無法復原」）。

---

### B-5　S-05 結算（30 項）

`SettlementPage.tsx`。

- 未結算態＝**完整轉帳預覽**（S-05-28），與 S-05-7 共用 A-4 的 `transferView()`。
  標題「現在的狀況」、說明「還會變 —— 之後記帳會影響這裡」。
- 結算前檢查層（S-05-4／30）：標題「有 N 筆還沒算清楚」＋**逐筆可點的清單**，
  「就這樣結算」**一定要能真的結算**（這是提醒不是禁止）。
- 人話淨額補上**對象**（由多筆轉帳組成時全部列出）。
- 摺紙 signature（S-05-16）取代舊的 popIn：三拍、總長 1.4s、**只播一次**、
  `forwards` 停在最終畫面，`prefers-reduced-motion:reduce` 直接給最終畫面。
- G-08 回顧卡（S-05-17）：數字帶符號、標籤純文字。
- 分享 CTA（S-05-29）升為主要動作，「建立新行程／封存行程」降為次級。
- 移除：待填警示（S-05-2）、Excel 正負號提醒（S-05-12）。
- **hub 模式的兩段式呈現（S-05-26）留到實作-C**，本節只做 direct。

---

### B-6　S-06 分享頁（15 項）

`SharePage.tsx`。**這一頁在實作-A 已經改成呼叫 `get_shared_trip()` RPC**，
本節只改畫面，**不要改回直接查表**。

- hero 用 `destinationOf()`；**拿掉成員 emoji**（與 S-03 一致，同一段 `join('')` 的問題）。
- 統計卡與消費列表**用 A-4 的共用元件**——結果是外幣格與人均消失（S-06-5／6「要移除」）、
  消費明細改日期分組、列上不寫日期、待填列補左邊框。**這是預期的，不要補回去。**
- 總花費**排除贊助**，與行程頁同一套算法（兩頁數字不得不同）。
- 全頁**唯讀**：列渲染成 `<div>` 不是 `<button>`，每人分擔列不可點，註腳不邀請點擊。
- 頁尾（S-06-12）改文案「這趟帳是用 Tripay 記的／開一趟自己的」，
  **PWA 安裝鈕（S-06-13）整顆移除**，連 state 與 listener 一起。

---

### B-7　S-07 設定（8 項）＋ 刪除 EmojiPicker

- 登出：描邊、文字色 `--md` 中性色（**不是 `--dg`**，登出可復原）；確認框改描邊。
- 版本 footer：**照原型是「Tripay · 每一趟，都記得」，沒有版本號**，不要自己加。
- **不要補做「我的資料」（S-07-4）與「顯示設定」（S-07-5）**，兩段都已裁示整段拿掉。
- **`src/components/EmojiPicker.tsx` 整檔刪除**，連同呼叫端
  （`TripFormSheet.tsx:539` 附近）。**必須在 B-1 的就地編輯接上之後才刪。**

---

### 允許與禁止

**允許**：S-00～S-07 的版面、文案、互動全面對齊原型。這是全站批次。

**禁止**：
- 動 `Tripay_原型.html`（它是規格，不是產出）。
- 補做原型上沒有的東西。**原型沒畫的就是不做**，想加寫進「順手看到但沒動」。
- 碰 Rozi 已裁示「只做紀錄」的六項（見 `_延後與不做清單.md` 與問題清單 P4）。
- 改實作-A 定下的 token 值與共用元件邊界。要改就停下回報。

---

### 停止條件（機器判斷得出來）

**核心方法：拿建置後的 App 跟原型逐畫面比對。原型是規格，所以比對對象是原型，
不是「上一版的自己」。**

1. `tsc --noEmit` 零錯誤；`pnpm run build` 通過。
2. **逐畫面文字比對**：在 390×844 把 App 的 S-00／S-01／S-02／S-02b／S-03／S-03d／
   S-04／S-05／S-06／S-07 各 render 一次，取 `textContent` 的字串集合，
   與原型**操作模式**同一畫面的字串集合比對。
   **差異必須為空集合**（原型的稿註區、編號徽章、原型控制列不列入比對）。
   斷言要**列出兩邊各自多出來的字串**，不是只回布林值。
3. **關鍵量測比對**（App vs 原型，同一畫面同一元素，容差 1px）：
   S-02 出發／回程欄位高度與寬度、S-04 日期列高度、S-03 消費列高度、
   `.ic2` 直徑、列內移除鈕的可點區（**≥44×44**）。
4. **212 項對帳表**：逐項標「已完成／不適用／未做」，
   `_停點.md` 附完整清單。**「未做」不得為 0 以外的數字**（全部要做完）。
   不適用要寫理由。
5. **P1-0 專項**：新增一筆「各付各的」且填外幣的消費 →
   `expense_splits.split_amount_foreign` 有值、`split_amount` 不得被塞進外幣數字；
   `expenses.split_fill_currency = 'FOR'`。
6. **S-04-8／29 專項**：存一筆完全沒填金額的消費 →
   `twd_amount is null` **且 pending 旗標為 true**；
   接著對該行程跑結算 → **不得回 422**，且那一筆被跳過。
7. **成員刪除守衛**：三種情形（有消費／有分帳／有 `individual_member_id` 指向他）
   都被擋下且出現同一句訊息；完全沒有紀錄的成員可以刪。
8. **支付方式連動**：在 S-02b 新增一種支付方式並儲存 → S-04 的「怎麼付的？」
   立刻出現它，順序與清單一致，預設第一項；**程式碼中不存在任何支付方式的寫死常數**。
9. **橫向不捲動**：320／375／414 三個寬度下，
   十個畫面全部 `document.documentElement.scrollWidth <= clientWidth`。
10. `EmojiPicker.tsx` 不存在，且全專案零 import。
11. 既有測試全綠；新斷言要有**反向驗證並留下痕跡**（`_停點.md` 逐條列
    「拿掉什麼 → 哪一條紅了」）。
12. `git diff --stat` 不含 `Tripay_原型.html`。

### `_停點.md` 必須包含

- 212 項對帳表（已完成／不適用＋理由／未做）
- 逐畫面文字比對的差異清單（應為空；不為空要逐條解釋）
- 第 5～8 條專項驗證的實際數值
- 反向驗證逐條記錄
- 「順手看到但沒動」清單

### 交付前檢查

**① 需求覆蓋**：本節＝`_盤點_實作缺口.md` 的 212 項畫面工作，
按「行程層設定 → 首頁 → 行程頁 → 記帳 → 結算 → 分享 → 設定」的相依順序切成 B-1～B-7。
引擎、hub 結算路徑、測試移植不在本節（實作-C）。

**② 收件人檢查**：本節的所有指涉都已展開——
硬相依（S-04-8＋S-04-29 同一個 commit 的理由與後果）、
三個反向陷阱、成員刪除守衛的三種情形、S-06 因共用元件而必然消失的三塊、
以及「原型的操作模式就是產品該有的樣子」這個比對基準。

**③ 五類例外**：本節不動 schema、不跑 migration、不刪任何使用者資料
（`EmojiPicker.tsx` 是程式碼不是資料）、無花費、無安全性變更。
`individual_member_id` 的刪除行為 Rozi 已裁示。**可以一路做完。**

---

### 實作-C　（第 3 步，接在實作-B 之後，**同一次「收」裡一起做完**）引擎、hub 結算、測試移植、上線前把關

> ⛔ 這一節不是獨立節次，是「實作-B」那一節的後半段。B 全綠之後直接接著做，不要等指令。

> **前置**：實作-B 全部完成。這一節做完就可以上線。
> 這一節不改任何畫面版面（除了 hub 模式那一塊，見 C-2）。

### C-1　結算引擎：pending 的行為（先確認，不要先改）

**先查證再動手。** `_盤點_實作缺口.md:151` 說「引擎遇 pending 的行為：已查證，
待填筆本來就跳過，不會 422」，但 `:296` 又說「pending 旗標必寫，否則結算整趟回 422」。
兩句話講的是**不同的東西**：引擎會跳過**有標記 pending** 的；
沒標記卻沒有台幣金額的那種，才是 422 的來源。

**要做的**：讀 `supabase/functions/settlement-engine`，確認
「`twd_amount is null` 且 pending 旗標為 true」→ 跳過該筆、不影響整趟。
**確認沒問題就不要改引擎**，把查證結果寫進 `_停點.md`。
有問題才改，改了就必須跑 C-4 的四趟 verify。

---

### C-2　hub 式結算（S-05-26）：**只做在呈現層，不動引擎**

**這一題的規格在文件裡兩處說法不一致**（一處說純顯示層、一處說已升級為完整功能），
**以原型為準，原型已經定調了**：

原型的 `settleTrip()` 裡，兩種模式算出來的 **`net`（每個人的淨額）完全相同**，
差別只在 `tx`（轉帳路徑）怎麼生成——
`direct` 走 minimum-transactions 配對，`hub` 則是「欠錢的人全額轉給中心人、
該拿回的人由中心人轉給他」。

**所以：淨額由引擎算，轉帳路徑在前端組。引擎一行都不用改。**
（這也表示 **hub 不需要跑四趟 verify**——淨額沒動。）

呈現：`transferView()`（實作-A 的共用元件）在 hub 模式下分兩段，
反映真實金流順序：先「大家給 {中心人}」，再「{中心人} 再給」。
S-05-7 與 S-05-28 用**同一份**，不得各寫一份。

代墊比例 > 70% 且目前是 direct 時出現引導（S-05-31）：**只給連結不給設定**，
設定的唯一入口是 S-02b-13。

---

### C-3　版面回歸測試移植進 `src/`

**現況**：版面回歸測試只有原型有（`原型_*.cjs` 那幾份），`src/` 一份都沒有。
橫向滑動、文字貼邊這類問題**改一次就會回來一次**，因為沒有東西守著。

移植成 `src/` 的測試，至少涵蓋：

1. **三個寬度不橫向捲動**：320／375／414，十個畫面
   `document.documentElement.scrollWidth <= clientWidth`。
2. **文字不貼邊**：`.ui` 之內每一個可見文字節點，
   用 `Range.getBoundingClientRect()`（**不是元素的盒子**）量，`left >= 8`。
   （量元素盒子會漏掉「容器有內距但文字被絕對定位推出去」那一類。）
3. **可點區**：每一個「只有 svg、沒有文字」的按鈕，有效可點區 ≥44×44。
4. **字級與圓角**：不存在九階以外的 `font-size`、四個變數以外的 `border-radius`
   （寫法照實作-A 的 A-3 停止條件，含出現次數下限）。
5. **兩處共用文案**：`MSG_NO_RATE`／`MSG_TWD_PENDING`／`MSG_FILL_ONE`
   各被 ≥2 處引用同一個常數，不得有第二份字面量。

**每一條都要有反向驗證**：拿掉被測的東西必須變紅。
`_停點.md` 逐條列「拿掉什麼 → 哪一條紅了」。

⚠️ **斷言的兩個既有陷阱，寫的時候要避開**：
- **「查了但沒查到」與「沒有問題」在輸出裡長得一樣**——斷言要輸出「掃到幾個、哪幾個」，
  而且要有數量下限，不能只回布林值。
- **比較式在目標消失時會假通過**（`null < 54` 在 JS 裡是 true，#29 那條斷言就這樣
  假通過了一輪）。先斷言目標存在，再比數值。
- 掃描前**必須先展開所有可收合面板**（S-04「要排除誰？」、S-03 統計卡、
  S-05 計算依據、⋯ 選單、各種 sheet），並在 `_停點.md` 列出展開了哪幾個。

---

### C-4　四趟 verify（**只有動到結算邏輯才需要**）

`CLAUDE.md:313`：結算邏輯改動必跑四趟 verify（福岡／東京／北海道／濟州島），
對照 `歷史資料匯入/驗收基準表.md`（🔒 已凍結 v1.1，不得再動）。

- **C-1 查證後沒改引擎、C-2 只動呈現層 → 不需要跑。**
  把「為什麼不需要」寫進 `_停點.md`。
- **只要動了引擎或任何影響淨額的計算，就必須跑，四趟全對才算過。**

⚠️ **腳本的刪除守則（既有鐵律，不得放寬）**：
任何自動化腳本的刪除目標，只能是同一次執行中自己建立且明列 id 的測試資料（ZZ 前綴）；
**嚴禁從 UI 狀態推導刪除目標**；執行刪除前必查
`歷史資料匯入/checkpoint.json` 的正式行程保護清單，命中即中止。

---

### C-5　上線前把關

1. **`TC-STATUS-18～20` 從來沒有任何檔案記錄執行過**（問題清單 P2-7）。
   跑一次，把結果寫進 `_停點.md`。
2. **環境地雷複查**（`CLAUDE.md` 既有清單，逐條確認沒有復發）：
   `trips`↔`trip_members` 兩條 FK 必須寫成
   `trip_members!trip_members_trip_id_fkey(*)`；
   `<input type="number">` 不支援 selection API（重繪會讓游標歸 0、字元倒序）——
   金額欄位一律 `type="text"` ＋ `inputmode="decimal"`；
   sheet 一律 `createPortal` 到 `document.body`。
3. **分享頁端到端**：用真實的分享網址（未登入的瀏覽器）打開 → 看得到那一趟；
   把網址的 token 改掉一個字元 → 看不到任何東西。
   **這是實作-A 的權限改動與實作-B 的畫面改動合起來才成立的，要在這裡驗一次。**
4. **不要自己 push、不要自己部署。** 全部跑完停下回報，
   **由 Cowork 複驗後推上線**（推 `main` 會觸發 GitHub Actions 自動部署到 Pages）。

---

### 停止條件（機器判斷得出來）

1. `tsc --noEmit` 零錯誤；`pnpm run build` 通過。
2. C-1 的查證結果寫進 `_停點.md`，並附「引擎有沒有改」的明確答案。
3. hub 模式：同一趟行程切換 `direct` ↔ `hub`，
   **每個人的淨額完全相同**（逐人比對，差一元即紅），只有轉帳路徑不同。
   `transferView()` 被 `SettlementPage.tsx` 與 `SharePage.tsx` **兩個檔案**都引用。
4. C-3 五組測試全綠，且**每一條都有反向驗證的痕跡**。
5. 若動了引擎 → 四趟 verify 全對；若沒動 → `_停點.md` 寫明理由。
6. C-5 第 3 條端到端：正確 token 看得到、改一個字元看不到，兩個方向都要有實際輸出。
7. 既有全部測試全綠。
8. **沒有執行 `git push`，沒有觸發任何部署。**

### `_停點.md` 必須包含

- C-1 的引擎查證結論（改了沒、為什麼）
- hub 兩種模式的逐人淨額對照表
- C-3 五組測試的反向驗證逐條記錄，以及掃描前展開了哪幾個面板
- 四趟 verify 跑了沒、為什麼
- `TC-STATUS-18～20` 的結果
- 分享頁端到端的兩個方向的實際輸出
- 「順手看到但沒動」清單

### 交付前檢查

**① 需求覆蓋**：本節＝Tech Lead 稽核的階段 7（引擎與測試），
加上文件裡兩處說法不一致的 hub 結算（本節以原型定調：呈現層，不動引擎），
加上 `src/` 從來沒有版面回歸測試這個缺口，加上上線前的環境地雷複查。

**② 收件人檢查**：hub 為什麼不動引擎（`net` 相同、只有 `tx` 不同）、
四趟 verify 的觸發條件與豁免理由、三個斷言陷阱的具體形態、
腳本刪除守則的完整內容，都已在本節展開。

**③ 五類例外**：本節**有一條硬規定**——
**腳本的刪除目標只能是自己建立且明列 id 的 ZZ 前綴測試資料，
嚴禁從 UI 狀態推導，執行前必查 `checkpoint.json` 保護清單，命中即中止。**
除此之外無不可逆動作、無花費、無安全性變更、無重大範疇取捨。
**不得自行 push 或部署**——那是 Cowork 的步驟。

---

---

## 實作-D　（第 4 步）Cowork 複驗退回：三項 [已處理]

> **完整內容在專案根目錄的 `_要修的.md`，那份才是指令本體，這一節只是入口。**

Cowork 於 2026-09-05 22:35 對 `723466a` 做了獨立複驗（自己跑 `tsc`／`vitest`／
型別對帳／token 掃描／文案唯一性／真實 Chromium 版面回歸／對 production 的分享頁端到端），
**大部分都過**，數字與你回報的一致（vitest 196 綠、版面回歸 172 綠、anon 六張表全 0 列）。

**退回三項**：

1. `src/pages/ExpenseListPage.tsx` 有**兩個 `{deleteOpen && (…)}` 區塊**（422 與 463），
   舊的那個忘了刪，按刪除時兩個對話框會疊在一起。**只刪 463–501，不要動 422–452。**
2. **S-03b 分享 sheet 三項**（`ExpenseListPage.tsx:80–84` 的 `opts`）沒對到原型
   `Tripay_原型.html:2866–2868`：標題「複製結算摘要」應為「複製文字摘要」，
   S-03b-2／S-03b-4 的灰字要拿掉，S-03b-3 的灰字要逐字改成「不用登入就看得到消費明細」。
   **只改文字，版面不要動。**
3. **停止條件 4 沒過**：212 項對帳表不存在。以 `_盤點_實作缺口.md`「第一段（212 列）」為母體，
   **用程式產生**（不要手抄）`Claude outputs/_212對帳表.md`，逐項標
   已完成／不適用（＋理由）／未做，三個數字相加＝212，**未做必須是 0**。
   第 ② 項就是因為少了這張表才漏掉的（B 的七個子節沒有涵蓋 S-02c／S-03b／S-03c）。

### ⛔ 界線

**這三項是逐條指名的。一個症狀只改造成它的那一處。**
你自己列在「順手看到但沒動」的其它項目（`.ic2::after` 兩份定義、`expense_type:'personal'`、
寫死色碼、`ShareSheet` 版面）**這一輪一律不要碰**。

### 停止條件（機器判斷得出來）

1. `grep -c "{deleteOpen && (" src/pages/ExpenseListPage.tsx` **等於 1**。
2. 新增斷言：開啟刪除對話框後，「刪除「…」？」這句只出現 **1** 次，
   且「請輸入行程名稱」**不存在**；反向驗證（把舊區塊貼回去）**要紅**。
3. 分享 sheet 三筆文字納入文字比對，與原型 2866–2868 **逐字相同**；反向驗證要紅。
4. `Claude outputs/_212對帳表.md` 存在，輸出**掃了幾列／已完成／不適用／未做**四個數字，
   相加＝212 且**未做＝0**。
5. `tsc --noEmit` 零錯誤；`vitest run` 全綠（項數 ≥ 196）。
6. 已 commit，**沒有 push**。`_要修的.md` 改名為 `Claude outputs/_要修的_已處理.md`。

### 五類例外

無。這三項都是既有程式的修正與一份對帳表，不刪資料、不花錢、不碰權限、不改範疇。
**可以一路做完。**

---

## 實作-E　（第 5 步）Cowork 第二輪複驗退回：對帳表的 4 列「未做」 [已處理]

> **完整內容在專案根目錄的 `_要修的.md`，那份才是指令本體。**

第一輪退回的三項**都過了**（`{deleteOpen && (` 只剩 1 個、分享 sheet 文字逐字相同、對帳表產出）。
Cowork 自己重跑：`tsc` 0 錯、`vitest` **201 綠 / 3 skip**。

**但停止條件 4 沒過**：對帳表自己列出 **未做 4 列**（要求 0）。這一輪收掉那 4 列：

1. **S-03c-2／3／4（＋按鈕字 S-03c-7）**：`ExpenseListPage.tsx:426–452` 刪除對話框的文案
   逐字對齊原型 `Tripay_原型.html:2873–2882`。版面不動。
2. **S-02c-10**：成員頭像三層 fallback（有 emoji → 名字第一個 grapheme ＋填色圓底 → 🙂），
   照抄原型 `993–1002` 的 `avatar()`、`LETTER_COLORS`、`firstGrapheme()`。
3. 重跑 `node 實作_212對帳表.cjs`，**未做＝0**。

### ⛔ 界線

只做這兩處修正加重跑對帳表。標「已完成／不適用」的列不要動，
「順手看到但沒動」清單不要碰。**S-06-3 已由 Rozi 2026-09-05 裁示：不要 emoji、維持現狀**（與 S-03 一致），
只要把對帳表那一列改標「已完成」並註明裁示，程式不用動；`➕` 一題已裁定維持原型。

### 停止條件（機器判斷得出來）

1. 刪除對話框的 `textContent` 與原型 `renderS03c()` 的字串集合**差異為空**；反向驗證要紅。
2. 三層 fallback 三個案例各一條斷言全綠；把第二層拿掉 → 中間那條要紅。
3. `node 實作_212對帳表.cjs` 輸出**未做＝0**，四個數字相加＝212。
4. `tsc --noEmit` 零錯誤；`vitest run` 全綠且項數 **≥ 201**。
5. 已 commit，**沒有 push**；`_要修的.md` 改名為 `Claude outputs/_要修的_第二輪_已處理.md`。

### 五類例外

無。**可以一路做完。**

---

## 實作-F　（第 6 步）Cowork 已放行 → 推上線 [已處理]

**Cowork 第三輪複驗全過**，結論與實際數字在專案根目錄的 `_可以推了.txt`。

摘要：`tsc` 0 錯｜`vitest` **211 綠/3 skip**｜212 對帳表 **已完成 212／不適用 0／未做 0**｜
型別對帳 29｜token 52｜文案 13｜真實 Chromium 版面回歸 **172 綠**（金絲雀有效）｜
anon 六張表全 0 列｜`get_shared_trip` 正確 token→74 筆、改一碼／前 8 碼／空字串→null｜
commit 無暫存檔、原型檔未被動。

### 你要做的（只有這一件）

```
git fetch origin
git push origin main
```

本機比 `origin/main` 超前 **4 個 commit**（`750d857` 實作-A、`723466a` B＋C、
`8cba1ef` 實作-D、`355a64e` 實作-E）。
push 之後 GitHub Actions 會自己建置並部署到 https://tzyutw.github.io/tripay/ 。

**順手把 Cowork 改過的 `專案狀態.md` 一起 commit**（決策紀錄：S-06-3 不帶成員 emoji、
實作階段交付模式），commit 訊息寫「文件：Cowork 決策紀錄與實作階段運作方式」。

`Claude outputs/_cowork暫存_可刪/` 整個資料夾是 Cowork 複驗時的暫存，**可以直接刪掉**
（裡面有一份含 anon key 的 `_e2e.env`，**不要 commit**，刪掉即可）。

### 停止條件

1. `git rev-list --count origin/main..HEAD` **等於 0**。
2. `_可以推了.txt` 改名為 `Claude outputs/_可以推了_已推.txt`。
3. `_停點.md` 記下推上去的 commit 與時間。
4. `git status --porcelain` 不含 `Claude outputs/_cowork暫存_可刪/`（已刪除或已忽略）。

push 失敗（認證、被拒、衝突都算）→ **把完整錯誤訊息寫進 `_停.txt` 後結束**，不要自己想辦法繞。

### 五類例外

無。這是把已複驗過的程式推到既有的部署流程，資料庫不動、不花錢。**可以直接做。**

---

## 實作-G　（第 7 步）上線後複驗：分享頁的轉帳列重複 [已處理]

> **完整內容在專案根目錄的 `_要修的.md`。**

`3e01b02` 已上線（Actions #91 綠）。Cowork 在**線上**實測分享頁，發現一個這一批做出來的退步：

**`src/pages/SharePage.tsx:69–70` 直接用 `data.settlement_items`（整趟全部結算的 item），
沒有挑「哪一次結算」**，那一趟有 12 筆結算（10 superseded／1 confirmed／1 draft），
於是「誰付給誰」重複列了 36 列。

改版前的 `SharePage`（`750d857^:85–88`）本來就有 `.eq('status','confirmed')` ＋ 倒序挑最新，
`SettlementPage.tsx:83–84` 現在也還有——**分享頁要跟它一致**。

**只改 `SharePage.tsx` 那一處**：從 `data.settlements` 挑 `status === 'confirmed'`
（多筆取 `settled_at` 最新，null 視為最舊），只取該 `settlement_id` 的 items；
挑不到就走原本的 `settleTrip(...)` 前端預覽。

**不要動 `get_shared_trip()`、不要動 `SettlementPage`、不要動版面、不要碰「順手看到但沒動」。**

### 停止條件（機器判斷得出來）

1. 新測試：fixture 3 筆結算（superseded／confirmed／draft 各 1，每筆 3 個 item）→
   分享頁只畫 confirmed 那 **3 列**，金額正確。**斷言要比對列數**，不能只看字串有無。
2. 反向驗證：拿掉篩選 → 該條要紅。
3. `tsc --noEmit` 0 錯；`vitest run` 全綠且 **≥ 212 項**。
4. **這次做完直接 push**（build 與測試都綠才推），並在 `_停點.md` 記下 commit。
   `_要修的.md` 改名為 `Claude outputs/_要修的_第三輪_已處理.md`。

### 五類例外

無。單一檔案的顯示邏輯修正，不動資料庫、不花錢。**可以一路做完。**

---

## 實作-H　（第 8 步）Rozi 手機實測回報：日期欄位跑版 ＋ 新增行程不該預填成員 [已處理]

> **完整內容在專案根目錄的 `_要修的.md`。**

Rozi 在 iPhone 上實測上線版，回報三件事。其中**「按出發！沒有存檔」已由 Cowork 修好並套上
production**（`supabase/migrations/014_fix_trips_self_referential_rls.sql`，根因是 013 的
`trips` SELECT 政策繞 STABLE 函式回查自己，害 `insert … returning` 被自己的 RLS 擋成 403）。
**那個檔已寫進 repo，請一起 commit，但不要再對 production 執行。**

這一節做兩項：

1. **日期欄位**：`TripFormSheet.tsx:483–508` 自己寫了 Tailwind 的原生 date 欄位，
   沒用已經移植好的 `.datefield`（`src/index.css:226–230`）。iOS 上系統外觀沒關掉，
   寬度與格式由 OS 決定 → sheet 左右跑版、兩欄看起來沒間距、高度不一致。
   改成與原型 `Tripay_原型.html:1195–1200` 同結構（gap 9px、`flex:1;min-width:0`、
   `<span class="datefield">` 包 input ＋ 日曆 icon、高度交給 `.datefield` 的 40px）。
2. **新增行程不要預填上一趟成員**：`TripListPage.tsx:179` 傳了 `prefill={{mode:'members'}}`。
   這是 `專案狀態.md:422` 記載 **2026-09-04 已移除的 G-09**，原型裡也沒有這句文案。
   拿掉那條路與「已帶入上一趟的成員，可以改」文案。**`mode:'full'`（複製行程）要保留。**

### ⛔ 界線

Google 登入頁顯示 `…supabase.co` **不要動**（需要付費自訂網域，Cowork 已問 Rozi）。
「順手看到但沒動」清單不要碰。

### 停止條件（機器判斷得出來）

1. `grep -c "h-\[46px\]" src/components/TripFormSheet.tsx` ＝ 0；日期兩欄 gap ＝ 9px、
   都在 `.datefield` 內、父層有 `flex-1 min-w-0`；兩欄高度相等（容差 1px，空值與有值都量）。
2. **版面斷言要量 sheet 容器自己的 `scrollWidth <= clientWidth`**（320／375／414），
   不是只量 `document.documentElement`——現有的 `實作_版面回歸測試.cjs` 只量後者，
   所以這次沒抓到。**補上這一條**，反向驗證（拿掉 `min-w-0`）要紅。
3. `grep -c "已帶入上一趟" src/` ＝ 0；`grep -n "mode: 'members'" src/` 無結果。
4. 新測試：無 prefill 的建立行程 sheet → 成員列數等於原型 S-02 起始值、畫面無「已帶入」；
   反向驗證要紅。另一條確認 `mode:'full'` 複製行程仍正常。
5. `tsc --noEmit` 0 錯；`vitest run` 全綠且 **≥ 215 項**。
6. `supabase/migrations/014_*.sql` 有進 commit；已 commit **並 push**。
7. `_要修的.md` 改名為 `Claude outputs/_要修的_第四輪_已處理.md`。

### 五類例外

無。**可以一路做完。**
