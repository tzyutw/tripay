# 指令佇列（Cowork 端代 Rozi 撰寫；每節皆經 Rozi 核可；處理完標 [已處理]，由 Cowork 端定期清掃）

---

## #14 金額還沒算清楚的狀態

本節說明一個新的資料狀態與對應的介面行為。所有規則以本節文字為準，
不需要參照其他節次或既有決策文件。

### 0. 這件事要解決什麼

使用者在旅途中記帳時，常常「總金額知道，但每個人各自要分擔多少還不知道」。
例如：三個人在藥妝店一起結帳共 NT$3,000，但誰買了什麼還沒對。

目前產品的行為是：沒填的欄位自動帶入均分值，直接存檔，使用者無從得知
這些數字是系統猜的。本節要補上這個狀態的顯示與提醒，同時維持
「記帳當下不打斷使用者」的原則——存檔一律成功，不擋。

### 1. 開工先驗（唯讀，不改任何碼，驗完先回報再往下）

V1. 消費列表頁是否已經查詢 expense_splits？回報檔名:行號。
    有 → 前端可直接計算本節的狀態；
    沒有 → 不要自行新增快取欄位，把方案寫成提案回報，停下等指示。

V2. 搜尋**兩組**字串，不只一組：「待填」與「待補填」。
    （既有消費列的 badge 字串是「待補填」三個字，只搜「待填」會漏掉它。）
    逐項列出檔名:行號與所在畫面。這決定第 6 節文案替換的改動範圍。
    兩組合計超過 10 處 → 先回報清單再動手。

V3. 記帳表單「各付各的」的各人分擔金額輸入欄位：
    每位成員是幾個輸入欄？該欄的幣別是怎麼決定的？
    回報實際欄位、其 state 名稱、以及幣別的決定方式。
    既有規格為「各自金額以該筆幣別統一填寫，同一筆不得混填」，
    請確認程式碼現況是否與此一致；**不一致就回報差異，不要自行改**。

V4. 「結算完成」與「封存唯讀」是不是同一個狀態？回報判斷依據與檔名:行號。
    若結算完成即進入唯讀 → 本節不需要處理「已結算的帳被改動」的情況。

### 2. 判定規則

**2.1 名詞定義**

「未輸入」= 該成員的分擔金額欄位，使用者沒有實際打字輸入。
介面上以 placeholder 顯示的自動均分值不算輸入。
技術判準：該欄位的 state 值為空字串或 null，而非 placeholder 顯示的數字。

「金額未定案的消費」= 符合下列任一條件的消費：
- (a) 整筆消費的金額尚未確定（現行的 twd_pending 狀態）
- (b) 該筆消費有 2 位以上成員的分擔金額未輸入（本節新增）

**2.2 各人分擔金額的判定**

每位成員的分擔金額**只有一個輸入欄**，其幣別由該筆消費決定
（同一筆不混填，見既有規格「各自金額以該筆幣別統一填寫」）。判定就是看這一欄：
- state 值為空字串或 null → 未輸入
- 有值 → 已輸入

⚠️ 若 V3 回報的程式碼現況與上述不符（例如實際有台幣、外幣兩欄），
**停下回報，不要自行決定看哪一欄**。

**2.3 依未輸入人數判定**

- 0 人未輸入 → 不標記
- 1 人未輸入 → 不標記。
  理由：該人金額 = 總額 − 其他人合計，這是唯一解，系統算得出來，不是猜的。
- 2 人以上未輸入 → 該筆消費中所有「未輸入」的 expense_splits 列標記
  `is_estimated = true`

**2.4 這條規則的優先權**

先前規則中有一條「分擔金額欄位空白即視為待填」。本節取代該條，正確行為是：
- 整筆總額有值 → 空白欄位自動均分，依 2.3 決定是否標記
- 整筆總額也沒有值（台幣外幣皆無）→ 整筆消費標為 twd_pending

### 3. 存檔行為

3.1 一律允許存檔。不得擋下、不得跳確認對話框、不得 disable 儲存鍵。

3.2 存檔後顯示 toast，共四種：
- **整筆金額未填**（台幣、外幣皆空）：
  `已存。這筆金額還沒填，之後補上就會算進總花費。`
  此時**不再**依人數顯示其他提示，只顯示這一句。
- 1 人未輸入：`已存。{名字} 的金額由總額推算。`
- 2 人未輸入：`已存。{名字A} 和 {名字B} 的金額還沒填，先照均分算。`
- 3 人以上未輸入：`已存。還有 {N} 人的金額還沒填，先照均分算。`

### 4. 顯示規則

**4.1 行程統計卡（畫面 S-03 上方）**

每人分擔那一列，若該成員符合下列**任一**條件，則標記：
- (a) 該成員參與了某筆「整筆金額尚未確定」（twd_pending）的消費。
  該筆總額未知，他的分擔金額自然也未知。
- (b) 該成員自己在某筆消費中的 expense_splits 列，`is_estimated = true`。

⚠️ **不得**用「參與了任何一筆金額未定案的消費」這種寬鬆判定。
在「各付各的」型態下各人金額彼此獨立：某成員已自行輸入金額時，
即使同一筆消費有其他人未輸入，他自己的金額仍然是確定的，不得標「約」。

符合上述條件者：
- 金額前加一個「約」字，次要文字色，字級小一階
- 金額本身加虛線底線，顏色用工作色
- 整列可點，點擊後跳到消費列表並套用「只看影響這個人的未定案消費」

卡片底部加一行註腳，次要文字色：
`標「約」的金額還沒算清楚，點名字看是哪幾筆`
若沒有任何成員被標「約」→ 註腳整行不顯示。

⚠️ 移除現行統計卡上的「N 筆待填，數字僅供參考」整條提示。
它的功能由 4.2 的入口取代。

**4.2 消費列表頂端（畫面 S-03 下方）**

在「消費紀錄 · N 筆」標題下方，加一條可點的入口列：
`有 {N} 筆還沒算清楚 ›`

N 的計算：符合 2.1 定義的消費筆數。
⚠️ 同一筆消費若同時符合 (a) 與 (b)，只計算一次，不得重複計數。

點擊後：消費列表套用篩選，只顯示這 N 筆，
列表標題改為 `還沒算清楚 · {N} 筆`，並提供返回全部列表的方式。

N = 0 時，整條入口列不顯示。

⚠️ **允許**出現「入口顯示有 N 筆，但統計卡沒有任何人被標『約』」的狀態。
例：一筆整筆金額未定、且尚未選任何參與成員的消費——它確實未定案（計入 N），
但沒有成員受影響（不標「約」、4.1 的註腳隱藏）。
**這不是 bug，不要為了讓兩處數字看起來一致而改動任一邊。**

**4.3 消費列（畫面 S-03 列表中的每一列）**

- 情況 (a) 整筆金額未定：維持現行的橘紅左邊框，金額欄顯示「還沒填」
- 情況 (b) 各自金額有 is_estimated：金額前加「約」字，次要文字色。
  不加邊框、不使用警示色。
- 兩者同時成立：橘紅左邊框優先，金額欄顯示「還沒填」，不再加「約」

**4.4 記帳表單（畫面 S-04）**

- 未輸入的欄位以 placeholder 顯示自動均分值。
  **重要**：placeholder 是唯一的呈現方式，不得把自動值寫進 input 的 value，
  也不得在使用者輸入過程中重新建立或改寫 input 節點——
  這會導致游標位置錯亂、輸入的數字順序顛倒。
- 分擔金額欄位區下方加一行灰字：`沒填的先照均分算，之後可以再改`

**4.5 篩選後列表**

4.1 點成員、4.2 點入口，兩者進入的是**同一個畫面**，只有標題與資料範圍不同：
- 從 4.2 進入 → 標題 `還沒算清楚 · {N} 筆`，內容為全部未定案消費
- 從 4.1 進入 → 標題 `影響 {名字} 的 · {N} 筆`，
  內容為該成員符合 4.1 (a)(b) 任一條件的消費

兩者都要有返回鍵，**返回到行程頁（S-03）頂部，不是瀏覽器上一頁**。
列內每筆可點，進入該筆的編輯畫面。

### 5. 結算前檢查（畫面 S-05）

5.1 檢查的時機是「使用者按下『結算行程』按鈕的當下」，在進入結算頁之前。
不得先進結算頁再檢查。

5.2 流程

```
按下「結算行程」
  → 檢查本行程是否有「金額未定案的消費」（定義見 2.1）
  → 沒有：直接進入結算頁，行為與現行相同
  → 有：先顯示檢查層
```

5.3 檢查層內容
- 標題：`有 {N} 筆還沒算清楚`
- 內文：`結算之後金額就固定了。要先去看一下嗎？`
- 清單：逐筆列出這 N 筆，每列顯示 emoji、品項名稱，右側小字說明原因：
  - 整筆未定 → `金額還沒填`
  - 各自未輸入 → `{N} 人還沒填`
  - 每列可點，點擊後直接進入該筆消費的編輯畫面
- 按鈕：主要 `先去看一下` ／ 次要 `就這樣結算`

5.4 ⚠️「就這樣結算」必須真的能完成結算。
這是提醒，不是禁止。任何情況下都不得阻止使用者結算。

5.5 若行程曾經結算後又被重新開啟，再次按下「結算行程」時，本檢查照常執行。

### 6. 文案替換

依 V2 回報的清單，將全站的「待填」改為「還沒填」。
包含但不限於消費列金額欄、篩選器選項、任何提示文字。
改動範圍以 V2 的實際結果為準，不要憑推測擴大或縮小。

### 7. 結算演算法

完全不修改。is_estimated 純粹是顯示用的旗標，不進入任何計算。
自動均分的數值本身會寫入 split_amount，因此
Σ 各人分擔 = 消費總額 這條恆等式不受影響，
結算的 net_balance 總和為零的性質不變。

### 8. 資料庫欄位（寫 migration 檔，不執行）

新增兩欄，寫在同一支 migration：

**8.1** `expense_splits.is_estimated boolean not null default false`
- 寫入時機：儲存消費時，依第 2 節規則計算後寫入。
- 讀取用途：第 4 節與第 5 節的所有顯示判斷。
- 不參與任何金額計算。

**8.2** `expense_splits.split_amount_foreign numeric null`
- 用途：保存使用者實際輸入的外幣分擔金額原始值。
- 寫入時機：使用者在外幣欄輸入時寫入。未輸入則為 null。
- 與 split_amount 的關係：split_amount 仍是台幣金額，仍是結算的唯一依據，
  計算邏輯不變。split_amount_foreign 只是原始輸入的保存，供編輯時回填，
  以及第 2.2 節判定「使用者是否實際輸入過」之用。
- 不得讓結算演算法讀取此欄位。

migration 檔寫好後停下回報，由 Cowork 端套用到 production。
終端機在任何情況下都不得對 production 執行寫入操作。

### 9. 交付物

**9.1 可點原型（由終端機產出）**

本節涉及的互動都要做進可點原型：
- 記帳表單填寫與存檔（含**四種**存檔提示）
- 統計卡的「約」標記與點擊
- 消費列表入口與篩選（含 4.5 的兩種進入方式）
- 結算前檢查層與兩個按鈕

原型需同時支援「標註模式」與「操作模式」，規格見第 11 節。

**技術限制（不遵守 Rozi 就打不開）：**
- **單一 HTML 檔**，不依賴任何 build 步驟，瀏覽器直接開就能跑
- **純前端 mock**，不連 Supabase、不連任何 DB
- 外部資源只允許字型；其餘 CSS/JS 一律內嵌
- 檔名 `Tripay_原型.html`，放 repo 根目錄

本節只需涵蓋 **S-03、S-04、S-05** 三個畫面。
其他畫面與既有稿的併入屬 #15，**不在本節範圍，不要提前做**。

**9.2 回歸測試**

在既有的輸入回歸測試中補上這 7 項：
- T1 全員未輸入 → 全部標記
- T2 剩 1 人未輸入 → 不標記
- T3 填了值又刪掉 → 回到標記狀態，旗標同步更新
- T4 手動填入的值剛好等於均分值 → 不標記
- T5 外幣總額空白但各人外幣都有輸入 → 不標記
- T6 編輯既有消費、清空某人金額 → 從未標記轉為標記
- T7 N = 0 時，4.2 的入口列整條消失

其中 T3、T4、T6 為必測，這三項是最容易做錯的。

**9.3** 停點回報，50 行以內

### 10. 停止條件（達成才算完成，未達成繼續迴圈）

以下全部成立才停下回報：
1. V1～V4 四項查證都有明確答案並已回報
2. 9.2 的 7 項回歸測試全部通過
3. 既有的回歸測試沒有因本次改動而失敗
4. 原型的判定邏輯有測試覆蓋：以第 2 節規則為準，針對「未輸入 0／1／2／3 人」
   與「整筆未填」五種輸入，斷言原型算出的 is_estimated 與「約」標記結果符合規格。
   （原型的**可操作性由 Rozi 目視確認**，不列入自動判定；
    做完停下等 Rozi 看過，不要自行認定通過。）
5. migration 檔已寫好但未執行

任何一項未達成 → 定位原因、修正、重測，不要中途回報。
例外：觸發第 12 節任一護欄時，立刻停下回報。

### 11. 原型格式規格（終端機維護）

11.1 視覺稿與可點原型合併為同一份文件 `Tripay_原型.html`，不再分開交付，也不再開新版號檔。

11.2 右上角提供模式切換，兩種模式：
- **標註模式**：每個區塊浮出編號徽章（例如 S-04-11），區塊範圍以虛線框標示，
  畫面旁列出該畫面的完整編號清單。
- **操作模式**：徽章與虛線框全部隱藏，純粹操作。

預設進入標註模式。

11.3 編號徽章可點擊，點擊後將該編號複製到剪貼簿。

11.4 有互動邏輯的區塊一律做成可實際操作的原型。
純顯示、無分支邏輯的區塊（例如空狀態、純資訊頁）維持靜態，但仍要有編號徽章。

11.5 編號規則沿用現行的 S-XX-NN 格式，不得重新編號。

### 12. 護欄（觸發任一項立刻停下回報，不要自行處理）

12.1 不對 production 執行任何寫入操作，包含 migration。
12.2 V1 若發現列表頁沒有查詢 expense_splits → 停下回報，
     不得自行新增快取欄位或改變查詢結構。
12.3 V2 若「待填」出現超過 10 處 → 先回報清單，等指示再動手。
12.4 不刪除或修改任何既有的正式行程資料。
12.5 若實作過程發現本節規則與程式碼現況有衝突、或規則本身有矛盾
     → 停下回報，不要自行決定怎麼取捨。

---

## #15 原型合併與全畫面補齊

**前置**：#14 完成、且 Rozi 已確認 #14 產出的原型格式沒問題。
**Rozi 沒確認格式前不要開始**——格式若要改，改一次比全做完再改便宜得多。

### 1. 合併

把下列兩份的內容併入 `Tripay_原型.html`，併完在兩份原檔頂端的警示橫幅
把「即將作廢」改為「已作廢」：
- `wireframe_v5_完整版.html`（全畫面完整稿與逐頁功能盤點）
- `分帳模型原型.html`（分帳三型的可點原型；版本二的稿保留作紀錄即可）

⚠️ `_to_delete/Tripay_原型_COWORK廢稿.html` **不得參考**，那是越權產出的版本。

### 2. 範疇

全部畫面都要進這一份，分四批交付，**每批停下等 Rozi 看過再做下一批**：
- 批一（#14 已完成）S-03、S-04、S-05
- 批二 S-00、S-01、S-02、S-02b
- 批三 S-06、S-07 及既有子畫面（S-03b、S-03c、S-04 子彈窗等）
- 批四 S-08～S-11（通訊錄、卡片管理、帳單週期、副卡視角）

有互動邏輯的區塊做成可實際操作；
純顯示、無分支邏輯的區塊（空狀態、純資訊頁）維持靜態，但仍要有編號徽章。
**判斷不了算不算「有互動」時，做成可操作的那一版。**

技術限制同 #14 第 9.1：單一 HTML 檔、不依賴 build、純前端 mock 不連 DB、
外部資源只允許字型。

### 3. 編號

沿用現行 S-XX-NN，**不得重編**。
已移除的項目（如 S-00-1、S-00-6、S-01-9、S-01-10、S-03-13）
留在盤點表標刪除線，號碼不回收，原型上不再出現。

### 4. 停止條件

1. 四批全部併入同一份 `Tripay_原型.html`
2. 每一批都經 Rozi 確認過（未確認的批次不算完成）
3. 兩份舊檔的橫幅已改為「已作廢」
4. `_盤點_畫面功能.md` 的編號與原型上的徽章逐一對得起來，無孤兒、無缺漏

### 5. 護欄

- 不改任何 `src/` 下的實作程式碼，本節只做原型
- 不刪除任何檔案（含舊稿），只加橫幅
- **每批做完停下回報**，不要一口氣做完四批
