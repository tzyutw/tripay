# 指令佇列（Cowork 端代 Rozi 撰寫；每節皆經 Rozi 核可；處理完標 [已處理]，由 Cowork 端定期清掃）

（目前無待辦節。#1–#9 已全數處理並清掃，結果見 _停點.md 與 專案狀態.md。）

## 10 v3（2026-09-04）（Rozi 已核可）v5 第三批：S-03c／S-04 [已處理]
A. S-03c-5 刪除行程二次確認改為輸入「刪除」二字。
   一致性：S-09 卡片管理稿上的刪除主卡同步改為「刪除」二字
   （此為稿上規範，卡片 UI 尚未實作）。

B. S-04-4 移除「之後再填」toggle，空欄位即待填。與分帳模型無關，兩版皆適用。
   - 台幣／外幣任一有值即可送出，未填那欄由系統自動寫入對應 pending 旗標
   - 兩欄都空亦可送出，系統自動寫入 twd_pending＋foreign_pending 皆為 true
   - ⚠️ pending 旗標是欄位、不是 UI：toggle 移除但旗標照常寫入 DB，
     否則會踩到讀碼發現 P3（twd_amount=null 讓整趟結算回 422 而非跳過該筆）
   - P3 一併確認：結算引擎遇到 pending 應跳過該筆而非 422；若現況不符，
     列入實作批次同批修，並補回歸測試
   - 取代既有 Bug #5 的「未填且未標記待填就擋下」防呆

C. S-04-9 支付方式（Rozi 已拍板：行程層級清單）——本輪只畫稿與定規格，不出 migration：
   - S-02b 新增「這趟的支付方式」：預設現金、信用卡；可自訂新增、排序
   - S-04 記一筆改為從該清單快選，不再固定三選一
   - 資料層需求（提案，待模型定案後一併備齊）：
     ① 行程層級清單的儲存位置（trips 新欄位 jsonb 或另開表，比較利弊後建議）
     ② expenses 記具體名稱的欄位（payment_label），enum 保留當分類
     ③ 邊界：刪除「已被使用」的支付方式如何處理——擋下刪除／保留文字快照，
        給建議供裁示

D. S-04-11~18 分帳模型：畫【兩版對照稿】，本輪只畫不改邏輯——
   版本一（維持三型）：一起分／各付各的／只算我 三塊各自完整獨立呈現；
     「只算我」示範「改名＋選人」的可能做法（記帳者未必是受益人：Rozi 負責
     登記整趟的帳，所以不能預設算在記帳者頭上）；
     並在此版畫出互動提案：一起分預設顯示「N 人均分 · 每人 $X」，需排除成員
     時點開才展開勾選（此提案僅適用版本一）
   版本二（兩維度）：只問「這筆誰要分？」（全員／挑選）＋「怎麼分？」
     （平均／各自金額）；三型退居系統自動判定，僅供 badge 與統計
   兩版都用這五個真實情境走一遍並附結算說明（誰付／誰分攤／會不會生欠款／
   進不進結算／badge）：四人吃飯 Ning 刷卡；兩人汗蒸幕；藥妝店一起結帳買
   各自的；小美個人購物自己付；大家當場各付各的。

E. 各自金額的幣別（讀碼確認為實際錯誤風險）：現行 split_amount 無幣別、
   引擎當台幣加總、比對列寫 $，但 UI 從未標示須填台幣 → 填外幣會靜默算錯。
   - 兩版稿都要畫出外幣情境：各自金額跟隨該筆幣別切換、比對列同幣別比對
   - 資料層需求（提案，待模型定案後一併備齊）：新增
     expense_splits.split_amount_foreign；split_amount 語意維持台幣供結算，
     既有資料不動；只有外幣而台幣待填時標 split_pending 由引擎跳過
   - 列入產品問題清單為既有風險項，實作批次修復

F. 受影響頁面完整重渲染，決策紀錄與檢查表同步，停下等 Rozi 對 D／E 的裁示。

## 11（2026-09-04）（Rozi 已核可）分帳模型：做成可點原型供試用 [已處理]
A. 建互動原型 artifact 供 Rozi 實際試用後決定 D（純前端 mock，不連 DB、
   不碰 production、不改 v5 主稿的分帳段落）：
   - 一個 artifact，頂部可切換【版本一 · 維持三型】與【版本二 · 兩個維度】
   - 手機尺寸 390px；成員用真實的 🍋Ning／🐟Ziyu／🐵Xiu／🐱Mei
   - 明示記帳者視角：Rozi 幫全家登記，付款人可指定任何人（不預設記帳者）
   - 必須真的可操作（可點、可填金額、可選人、可切幣別），不是靜態圖
   - 兩版都要能走完這五個情境：
     ① 四人吃飯 Ning 刷卡 ② 兩人汗蒸幕 Ziyu 付 ③ 藥妝店一起結帳買各自的
     ④ 小美個人購物自己付 ⑤ 大家當場各付各的
   - 每完成一筆即時顯示：誰付／誰分攤／誰欠誰多少／進不進結算／badge，
     以及「本次用了幾次點選」的計數，供兩版客觀比較
   - 情境③必須用韓圜填各自金額，示範幣別切換與同幣別比對列（E 的情境）
   - 版本二的「這筆誰要分」採建議預設：成員列常駐、預設全勾、點一下切換
   - Rozi 會邊試邊發現要修的細節，所以原型要好改：兩版的規則集中在
     可讀的設定區塊，方便後續逐條調整
B. 資料層方向核可（正式 migration 仍另備提案待拍板）：
   C-① trips.payment_methods jsonb，不另開表
   C-② expenses.payment_label 存名稱快照；enum 保留當分類，新增時必選分類
   C-③ 已被使用的支付方式擋下刪除，改提供「停用」
   E   expense_splits.split_amount_foreign；split_amount 語意維持台幣供結算
C. 原型完成後停下，等 Rozi 試用結果與逐條修改意見。

## 12 v3（2026-09-04）（Rozi 已核可）分帳定案：版本一＋幣別換算＋填寫便利性 [已處理]
A. 分帳模型定案：採【版本一 · 維持三型】。「只算我」改名「只算一個人」＋選人
   （記帳者未必是受益人）。一起分：預設全員均分，顯示「N 人均分 · 每人 $X」，
   需排除成員時點開展開。v5 主稿依此更新；版本二的稿保留在原型中作為紀錄。

B. 各自金額幣別換算規則：
   - 各自金額以該筆幣別填寫（幣別切換統一控制，同一筆不得混填）
   - 換算：各人台幣 = 台幣總額 ×（各人外幣 ÷ 外幣總額）
   - 外幣總額為空時，系統自動以 Σ各自外幣 補入並在 UI 明示，可覆寫
   - 台幣總額為空時該筆 twd_pending，結算跳過；補上後自動換算

C. 資料層：split_amount 維持台幣供結算（引擎不改）；新增 split_amount_foreign
   保存原始外幣輸入，供回填顯示與編輯。（正式 migration 另備提案待拍板）

D. 比對列同幣別顯示，下方小字顯示換算後台幣。

E. 各自金額的自動填寫（無按鈕）：
   - 所有空白欄位即時顯示「剩餘平均分給未填者」的自動值，淡一階＋「自動」小標
   - 填入任一欄後，剩餘在其餘未填者之間即時重算；填滿即無自動值
   - 手動輸入的欄位轉為一般樣式；清空則回到自動值
   - 空白 ≠ 輸入 0：空白＝自動均分；輸入 0＝該成員此筆確實不分攤
   - 與 B 條「空欄即待填」的關係（此模式下優先）：總額有值時空白＝自動均分；
     只有整筆總額待填（台幣外幣皆無）時，各自金額才一併標 split_pending
   - 已填金額超過總額時：未填者自動值顯示 0，提示「已超出 ₩X，超出部分由
     付款人吸收」，仍可存檔（沿用既有差額歸付款人規則）
   - 均分模式不適用

F. 不強制加總相等即可存檔：
   - 沿用引擎既有規則：差額（總額 − Σ各自）歸付款人，Σ淨額恆為 0
   - 分級提示且皆可存檔：差額 ≤ 總額 1% → 淡色「差 ₩30，由 🍋 Ning 承擔」；
     > 1% → 橘色警示同句型，提醒可能填錯但不擋存檔
   - 移除表單中任何「必須加總相等才能送出」的阻擋（若現況有）
   - 付款人未參與該筆時規則相同，由 1% 警示攔大額異常，不另開特例

G. 【交付方式】同時更新兩者，並以原型為主要驗收物：
   - 更新既有的互動原型 artifact（同一連結）：版本一依 A–F 全部可實際操作——
     自動均分即時重算、清空回自動值、幣別切換與換算、填爆總額的提示、
     差額分級提示都要能用手點出來
   - 同步更新 v5 主稿的 S-04 段落（靜態稿）
   - 受影響頁面完整重渲染；決策紀錄與檢查表同步
   - 停下等 Rozi 試用原型後的意見，再進第四批（S-05 起）
