# 指令佇列（Cowork 端代 Rozi 撰寫；處理完請在節標題加 [已處理]）

## 1（2026-08-31）[已處理]
- 008 已由 Cowork 端以 migration「cover_photos_storage」套用 production，
  自檢全中：bucket 私有／512KB／5 條政策（含 share read）／四趟 cover_path
  全 NULL 走 gradient。第二批（封面上傳）不再被阻擋。
- 傳話協定自此生效：停點報告寫 _停點.md（覆寫、標時間），對話內一行摘要即可。

## 2（2026-08-31）建立專案 CLAUDE.md [已處理]
建立 repo 根目錄 CLAUDE.md（Claude Code 每輪自動在場、不被 compact 壓掉），
內容精簡（100 行內），包含：
- 專案一句話定位＋文件地圖（專案狀態.md 為進度總表、協作規則_精簡版_v2.md
  為流程權威、決策符合性檢查表.md 每次 UI 改動必跑）
- 決策模式摘要：預設執行＋事後可推翻；五種例外事項全文
- 部署授權摘要：純前端內部審查全過可自動部署；schema/資料/結算邏輯須 Rozi
- 傳話協定：「收」＝讀 _指令佇列.md；停點寫 _停點.md
- 刪除護欄三條（ZZ 前綴／禁止 UI 推導目標／保護清單必查）
- 帳務鐵律：DELETE 影響列數必須斷言；結算改動必跑四趟 verify
寫完 commit push（屬文件，照授權自動處理），並回報 _停點.md。

## 3（2026-08-31）token 節約與佇列衛生（納入 CLAUDE.md 協定段）[已處理]
- 停點報告 ≤50 行；細節寫入對應文件後在報告中引用檔名，不全文展開。
- 大型盤點（原始碼逐頁盤點、schema 掃描等）結果必須落檔
  （例：_盤點_畫面功能.md，記錄盤點時的 commit hash）；之後一律引用該檔。
  需要重新盤點時，先 git diff 上次盤點的 commit，只重讀有變動的檔案。
- _指令佇列.md 衛生：一節＝一件定案事項；同節更新時 Cowork 端整節替換，
  永遠只讀最新內容即可；[已處理] 節由 Cowork 端定期移除。
- 本節與協定一併精簡寫入 CLAUDE.md（總量仍守 100 行內）。

## 4（2026-08-31）v5 第一批調整＋14 條裁示 [已處理]
A. 14 條裁示：#1–#13 照你報告中的建議處理，其中——
   - #2 分享連結漏 BASE_URL：線上死鏈，【立即 hotfix 並部署】（照部署授權），
     不等實作批次。#3 贊助「4 人」寫死：同一個 hotfix 順手修。
   - #1 G-06 無 onClick：先掛起，依下方 S-01 的 UX 重新評估結果決定修或拿掉。
   - 其餘 #4–#13：納入實作批次工作清單，實作時逐條處理並在回報中對號。
   - #14：Rozi 尚未裁示，保留待下一批。
B. v5 wireframe 調整（本批只動 S-00 與 S-01，其他頁等下一批）：
   - S-00-1 移除（App 未設計 logo）。移除後若頂部需要識別，UX 可「提案」
     以純文字 Tripay＋slogan 替代——標註為提案供 Rozi 下批裁示，不得逕行定案。
   - S-00-6 幽靈行程預覽卡（G-02）移除。UX 重新評估：此元件放在 S-00 是否
     合理？給結論與理由；若認為值得保留，提出更合理的位置與形式供裁示。
   - S-01-9 與 S-01-4 移除（分享入口常駐但對象不明，令人困惑）。UX 重新
     評估整體「分享」動線：分享入口該在何時、何處出現才合理？給提案供裁示。
   - S-01-11 維持不動。
   - S-01-12：Rozi 在稿上找不到此區塊——回覆其確切位置（是否為互斥狀態或
     摺疊內容），並在稿上加醒目指引方便核對。
C. 修改後更新同一個 v5 artifact；停點報告 ≤50 行；停下等下一批。

## 5（2026-08-31）協定修訂＋#14 定案（Rozi 已核可）[已處理]
A. 協定修訂（寫入 CLAUDE.md 協定段）：佇列指令一律先經 Rozi 過目核可後
   才由 Cowork 端寫入；終端機收到的每一節都視為已經 Rozi 核可。
   終端機帶回的提案類內容（UX 評估、文案提案等）不逕行執行，
   由 Cowork 端帶回與 Rozi 討論定案後才會出現在後續佇列。
B. #14 定案：S-05 未結算態新增唯讀預覽區塊——
   標題「暫時算一下」；警語「記帳還在變，按『算清楚』才算數」；
   每人一行照結算頁既有格式（🐟 可以拿回 $3,633／🐵 要給出 $1,929）；
   即時計算、不寫入 settlements、不影響「算清楚」流程與其後畫面。
   更新 v5 的 S-05 段落，並列入實作批次工作清單。
