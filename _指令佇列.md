# 指令佇列（Cowork 端代 Rozi 撰寫；每節皆經 Rozi 核可；處理完標 [已處理]，由 Cowork 端定期清掃）

（目前無待辦節。#1–#7 已全數處理並清掃，結果見 _停點.md 與 專案狀態.md。）

## 8（2026-08-31）（Rozi 已核可）v5 第二批：S-01／S-02 系列調整 [已處理]
A. S-02-5 移除封面 emoji：建立行程不設封面，一律先套目的地色調（依行程名
   自動判定）。trips.emoji 欄位保留不刪，前端不再使用。
B. S-02b-6 同步移除；S-02b 封面區兩個動作：更換照片、重選色調（色調＝目的地
   色系預設盤，不做色票挑色器）。行程卡顯示：有照片用照片、無照片用色調。
C. S-02c Emoji 選擇器【整個移除】。成員識別新規則：
   1) 顯示三層：有 emoji → emoji；無 emoji → 名字第一個字（grapheme）放在
      填色圓底（色從成員索引取目的地色系）；名字也空 → 🙂。
   2) 就地編輯：點擊識別圓圈 → 變單字元輸入框並自動聚焦（系統鍵盤彈出，
      使用者自行切到 emoji 鍵盤）→ 輸入後自動存檔收合。
   3) 驗證放寬為「限一個 grapheme，emoji 或文字皆可」；沿用現有 grapheme
      函式，EmojiPicker 的 UI 移除、驗證邏輯保留。
   4) trip_members.emoji 預設改空字串讓 fallback 生效；既有資料不追溯修改。
   5) 稿上標明技術限制：網頁無法主動喚起系統 emoji 面板。
D. 首頁行程卡移除成員識別列：卡片只留照片／色調、行程名、日期、狀態章。
   理由：路線 2 首頁定位為回憶架（已移除金額）；行程命名本身已含人數資訊；
   成員屬進入行程後的層級。
   適用範圍限定：行程內頁、記帳表單、結算頁、分享頁照常顯示成員；
   S-03E 空狀態依決策 D 仍顯示成員 emoji（不得誤刪）。
E. S-02-8 幣別：稿上「52 列」為誤標，實際 18 種（含泰銖）。改為完整列出或
   標「示意，實際 18 種可搜尋」。完整性守則補充：任何省略示意都必須標明
   實際數量與出處。
F. S-02-9 日期：預設出發日＝今天、回程留空。送出時若回程為空，跳提醒確認
   「這是單日行程嗎？」，確認後以單日建立、不強制填。行程狀態依出發日判定。
   提醒文案由 Copywriter 提案後經 Rozi 定案，先放佔位。
G. 字體規則修正：等寬（IBM Plex Mono）只用於金額；日期、天數等其他數字改用
   PingFang TC。tokens 頁與 v5 全稿同步。
H. 以上更新後受影響頁面完整重渲染（S-01／S-02／S-02b／S-02c 移除後版面遞補），
   決策紀錄與檢查表同步，停下等 Rozi 第三批（S-03 起）。
