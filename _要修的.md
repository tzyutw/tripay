# 要修的（Cowork 寫，2026-09-08 實作-AC 追加）

> Rozi 在正式環境開了一趟「測試贊助」實測，看到三個問題。
> **這四項都在你正在改的同一批檔案裡**，一起做完，不要另開一節。
> 其餘 AC 的內容照原本的做，不要動。

## 1　贊助要獨立成一行，不要混在「一起分的」裡

Rozi 原話：「贊助的項目應該獨立出來，不要放在一起分的名目裡面，
應該要把它列出來說這筆是贊助的，或是要折抵這樣子。」

**已查證的事實**（不要再假設）：
- 贊助的 `twd_amount` 在資料庫是**負數**。她那筆「導遊贊助」是 `-50000`。
- 現在 `is_sponsor` 的筆被當成一般 `shared` 併進「一起分的」，
  所以她的「一起分的 1 筆 −$12,500」其實是贊助，畫面上看不出來。
- 行為本身是對的（**折抵整趟、參與者平分抵**），Rozi 確認要維持，**不要改計算**。

**做法**：在四行之外多一行，**只在這趟有贊助時才出現**（0 筆時整行不顯示，
跟另外四行的規則不同，這是刻意的——大部分行程沒有贊助，多一行 0 是雜訊）。

```
他幫大家先付的     0 筆     $      0
一起分的           0 筆    −$      0
各付各的           0 筆    −$      0
贊助折抵           1 筆    +$ 12,500      ← 新增，只在有贊助時出現
─────────────────────────
差額                       +$ 12,500

自己買給自己的     0 筆     $      0   不進結算
```

- 標籤逐字：**`贊助折抵`**
- 金額**顯示成正數並帶 `+`**（它是替你抵掉的錢，不是你要付的）
- 新增資料屬性 **`data-sponsor`**（該成員被抵掉的金額，正數）
- 「一起分的」要把 `is_sponsor` 的筆**扣掉**，`data-shared` 跟著變
- 恆等式改成：`data-fronted − data-shared − data-each + data-sponsor === data-diff`

## 2　卡片裡的敘述句只顯示名字，不要識別圖

Rozi 原話：「現在成員的名字呈現在說明文字的地方，會變成例如「N NING」的顯示方式。
但我不要這樣，我只要它出現「NING」就可以了。」

- 卡片內**每一行轉帳敘述**（`給 {某人} $ …`／`{某人} 給你 $ …`）**只顯示名字**，
  不得出現識別圖（`Avatar`）。
- **卡片標題那一行維持識別圖**（那裡是在標示「這張卡是誰的」，不是句子）。
- ⚠️ 這是對常設 C1（成員識別全站一致）的**縮限**，由 Rozi 2026-09-08 拍板：
  **句子裡的成員只寫名字；獨立標示身分的位置才放識別圖。**
  已同步寫進 `_需求台帳.md` 常設區，不要當成違規改回去。

## 3　負號疊了兩層

她的截圖：Ning 那行顯示「他先付出去的 1 筆 **−$ -50,000**」。
金額本身是負數，格式化又加了一個負號。

- 規則：**先取絕對值再決定要不要加符號**，任何一格都不得出現 `−$ -` 或 `-$ -`。
- 同時沿用 AC 停止條件 7（不得出現 `−$ 0`）。

## 4　假資料的贊助符號跟正式資料相反

`src/test/harness/fixtures.ts` 第 209～210 行：
`mk({ title: '爸爸贊助', twd_amount: 50000, is_sponsor: true })` ——**正數**。
正式資料是 **負數**。這是常設 C10 說的「假資料借用正式資料的名字，形狀卻不同」。

- 改成 `twd_amount: -50000`。
- ⚠️ 改完之後，`verify_U` 7、`verify_V` 7、`verify_W` 17、`verify_X` 11-關／11-開
  這 5 條長期紅著的斷言**很可能會轉綠**（它們都是「三段加總 ≠ 列上金額」）。
  **不要**為了讓它們變綠去動程式——只改假資料，然後回報改完之後那 5 條的實際結果，
  紅的就照實說紅。判定由 Cowork 跑。

## 停止條件（追加，接在 AC 原本的 23 條後面）

24. `?screen=s05` 展開後，**沒有贊助的成員卡片不得出現「贊助折抵」這一行**；
    有贊助的假資料（`?sponsor=1`，若現有假資料每個人都被贊助抵到，則四張卡都要有）
    出現該行，標籤逐字 `贊助折抵`，金額帶 `+`。
25. 恆等式（逐人）：`data-fronted − data-shared − data-each + data-sponsor === data-diff`，
    其中沒有贊助時 `data-sponsor` 為 `0`。
26. `data-shared` **不含** `is_sponsor` 的筆：反向金絲雀——把贊助併回「一起分的」，
    第 24／25 條必須變紅。
27. 卡片內的轉帳敘述行**不得包含 `.avatar`**（數量 0）；
    卡片標題那一行**必須**有 `.avatar`（每張卡恰好 1 個）。
28. 畫面全文不得出現 `−$ -`、`-$ -`、`−$ 0`、`-$ 0`。
29. `fixtures.ts` 的贊助筆 `twd_amount` 為負數；且 `?screen=s03` 消費清單上
    該筆顯示的金額與正式資料同向（負的）。
