# 規格：對帳正確性 — 「當場結清不拆帳」與「外部贊助/折抵」

> 路線 1（Rozi 2026-07-22 定序 1→2→4→3 的第一項）。
> 背景：濟州島匯入發現 Tripay 無法表達「共同但當場各自結清、不生欠款」，導致要用 personal 迂迴才對得上 Excel。本規格補上這個資料模型缺口，並處理外部贊助。

---

## Part A — 共同「當場結清、不拆帳」

**問題**：一筆團體消費，大家當場各付各的（現金/各刷各的），對帳上**不該產生欠款**，但仍要留紀錄（旅遊日記 + 各人知道自己花了多少）。目前 shared 一定會拆帳生欠款。

**設計**
- 資料模型：`expenses` 新增 `settled_on_spot boolean not null default false`
- UI（凱 × Aria）：shared 模式的表單加一個 toggle **「大家當場各付各的，不用結算」**（預設關）。開啟時，「誰請客/付款人」欄位可淡化（因無單一代墊人）。
- 結算引擎（Data Analyst × Tech Lead）：`settled_on_spot = true` 的消費**完全不進 payout / cost / net 計算**（等同每人各付各的、淨額 0），也不進「N 筆待填」影響。
- 消費列 badge（Aria）：加一個「當場分」badge，與現有 各自/自付 badge 並列規則一致。

**決策點 A（待 Rozi）**：`settled_on_spot` 的消費，要不要算進「我的花費」統計？
- 建議：**算**（你確實花了你那份，日記/總花費才完整）；只是不進「誰欠誰」。
- 若選「不算」，則此筆完全不影響任何統計，只當純紀錄。

**濟州島效果**：有了 A，之前的「結算版用 personal 迂迴」就不需要了 —— 共同項只要標記 `settled_on_spot`，Tripay 結算自然對上 Excel 的殘差（Ziyu +$3,632…），且明細仍正確顯示為「共同（當場分）」而非「自付」。

---

## Part B — 外部贊助 / 折抵

先拆成兩種，別混為一談：

**B-1 折抵（帳單折扣、優惠券）**：這是**帳單本身變便宜**。
- 結論：**不需新功能**，直接記折扣後的實付金額即可。（教學/文案提醒使用者記淨額）

**B-2 贊助（外部免費資源，如金主/導遊贊助 $50,000）** ← Rozi 定案：中間點方案
- **做法：贊助 = 一筆「負金額的共同項」，需指定一位「代收贊助的人」。**
- 資料：沿用 `expenses`，允許 `twd_amount / foreign_amount 為負`；`payer_member_id` = 代收人；`expense_type = shared`（4 人均分負額）。可加分類旗標 `is_sponsor boolean default false` 供 UI 標示與統計排除於「總花費」。
- 效果：每人應分攤各少 (贊助額÷人數)，**真的扣進應付**；代收人在結算中「付了負額」＝收到再分給大家；**Σ 淨額恆為 0，結算自動平衡**，最終轉帳自動算對。
- UI（Aria/凱）：記一筆時可切「這是贊助/回饋（負額）」→ 金額輸入負數、選代收人；消費列以特殊 badge 標示；「總花費」統計建議排除贊助額（否則總花費被灌低），但「我的花費」會反映各人少分攤的部分。
- 若贊助是「金主直接付給店家、無成員經手」→ 不用本功能，改成把該筆被贊助的消費金額直接記少即可。

**折抵（帳單折扣）** 同 B-1：記淨額，不需功能。

**Rozi 決策紀錄（2026-07-22）**：
- 決策 A：settled_on_spot 消費**算進「我的花費」**（要知道當天花多少）。
- 決策 B：採**負金額共同項 + 代收人**（真的扣進應付、可結帳、平衡）。

---

## 邊界與相容（QA Lead）
- `settled_on_spot` 僅適用 shared；與 individual/personal 互斥（type 決定，不衝突）。
- 與「金額待填」可共存：當場分但金額還沒填 → 仍可標記，待填不影響（因不進結算）。
- 分享唯讀頁：`settled_on_spot` 消費照常顯示於明細，結算摘要不含它。
- 既有資料：新欄位 default false / 0，不影響已匯入的兩個濟州島行程與任何現有行程。
- 需補測：settled_on_spot 混合一般共同時 Σnet_balance 仍恆為 0；archived/soft-delete 行為不受影響。

## 遷移（Tech Lead）
- 一支 migration：`alter table expenses add column settled_on_spot boolean not null default false;`、`alter table trips add column sponsor_amount int not null default 0, add column sponsor_note text;`
- Edge Function `calculate-settlement`：查詢加 `settled_on_spot` 過濾；member_balances 計算跳過該類。
- 前端：ExpenseFormSheet 加 toggle + 送出欄位；SettlementPage 顯示贊助行；stats 依決策點 A 調整。

## 內部審查軌跡
- Tech Lead 產出資料模型 + Edge Function 影響點；Data Analyst 確認「settled_on_spot 不進 net、Σ 仍為 0」、贊助「純顯示不破壞平衡」；QA 補 5 項邊界；Aria 定 badge 與贊助行版位；Max 把關：兩個決策點屬產品取捨，需 Rozi 拍板後才進實作，未跳步。

## 待 Rozi 決策（進實作前關閉）
- **A**：settled_on_spot 消費算不算進「我的花費」？（建議：算）
- **B**：贊助純顯示，還是真的扣進每人應付？（建議：純顯示）
