# 規格：金額未定案的狀態＋各自金額幣別

> 定案日 2026-09-04。原型與日後實作都以本檔為準。
> 本檔只描述「該有什麼行為」，不規定實作方式。

## 0. 要解決的兩件事

**事情一：總金額知道，但每個人各自要分擔多少還不知道。**
例：三個人在藥妝店一起結帳共 NT$3,000，誰買了什麼還沒對。
現行行為是沒填的欄位自動帶入均分值直接存檔，使用者無從得知這些數字是系統猜的。

**事情二：刷卡只知道台幣總額，各自卻只知道外幣金額。**
例：日幣 15,000 的帳刷卡 NT$3,400，Ziyu 只知道自己買了日幣 4,000。
現行 `split_amount` 沒有幣別、引擎當台幣 → 填外幣會靜默算錯（已列管 P1-0）。

兩件事發生在同一個輸入欄上，所以一起處理。

---

## 1. 名詞

- **未輸入**：該成員的分擔金額欄位，使用者沒有實際打字輸入。
  以 placeholder 顯示的自動均分值不算輸入。
- **金額未定案的消費**：符合任一條件的消費
  - (a) 整筆消費的金額尚未確定（`twd_pending`）
  - (b) 該筆消費有 2 位以上成員的分擔金額未輸入

---

## 2. 各自金額的幣別與換算

### 2.1 幣別

每位成員的分擔金額**只有一個輸入欄**，幣別由該筆消費決定，**同一筆不得混填**。
欄位上必須明示現在填的是什麼幣別（現行 UI 沒有標示，這是 P1-0 的根因）。

### 2.2 換算公式

    各人台幣 ＝ 台幣總額 ×（各人外幣 ÷ 外幣總額）

用比例回推，**不需要匯率**。這正是為了解決「刷卡只知道台幣總額、各自只知道外幣」的情境。

### 2.3 外幣總額空白時

| 情況 | 行為 |
|---|---|
| 外幣總額**有值** | 未填者自動均分剩餘外幣；可換算台幣。正常路徑 |
| 外幣總額空白，**全員都填了** | 外幣總額 ＝ Σ各人外幣，並在 UI 明示是自動補的、可覆寫 |
| 外幣總額空白，**且有人沒填** | **不顯示自動均分值**（猜不出來）。提示：「還沒填外幣總額，先補上才算得出各自要付多少」。**仍可存檔，不擋** |

第三種情況若強行自動補總額會嚴重算錯：
例台幣 3,400、外幣總額空白、只有 Ziyu 填了 4,000 → 總額被補成 4,000 → Ziyu 佔 100%、其他人 0。

### 2.4 台幣總額空白時

整筆標 `twd_pending`，結算跳過該筆。台幣總額補上後自動換算各人台幣。

### 2.5 資料

- `split_amount`：語意維持**台幣**，是結算的唯一依據。
- `split_amount_foreign`：保存使用者輸入的外幣原始值，供編輯時回填、
  以及判定「使用者是否實際輸入過」。**結算演算法不得讀取此欄位。**
- 既有資料不動。

### 2.6 比對列

比對列以**該筆幣別**顯示（已填 ¥X ／ 總額 ¥Y），下方小字顯示換算後的台幣。

---

## 2A. 行程的現金匯率

### 2A.1 為什麼需要

現況沒有任何地方可以設匯率——`expenses.exchange_rate` 是「台幣÷外幣」倒推出來的唯讀顯示值，
不能設定、不會套用到其他筆。後果：現金消費只填外幣不填台幣時，那筆沒有台幣金額，
結算直接跳過它。一趟幾十筆現金消費，等於要使用者拿計算機手動換算幾十次。

### 2A.2 設定方式

行程層級設一個現金匯率，**單筆可覆寫**。

**輸入用換錢的實際動作，不要叫使用者填匯率數字**：
- 兩個欄位：「你用多少台幣」→「換到多少 {該趟幣別}」
- 匯率由系統算出，唯讀顯示
- 顯示格式：`10,000 台幣 → 45,000 日圓（1 日圓 ≈ 0.22 台幣）`

**放的位置**：
- **S-02 建立行程：不放。** 出發前通常還沒換錢，放了只會多一個填不出來的欄位
  （與「建立時只放必要欄位」的原則一致，見 S-02-19 的裁示）
- **S-02b 編輯行程：放**，可留空
- **S-04 記帳表單**：記現金消費而該趟還沒設匯率時，就地提供設定入口，
  不要把使用者踢去編輯行程頁

### 2A.3 套用範圍

| 支付方式 | 行為 |
|---|---|
| **現金** | 填外幣 → 用行程現金匯率自動換算台幣。單筆可覆寫匯率 |
| **信用卡** | 直接填台幣（帳單金額）。外幣選填，**只當紀錄，不套用行程匯率**——銀行匯率浮動，套用會算錯 |
| **儲值卡** | 比照現金，套用行程現金匯率，單筆可覆寫（儲值當下的匯率可能不同） |

### 2A.4 補算與改動（重要，做錯會動到已算好的帳）

**設定匯率時**：把該行程「有外幣金額、但沒有台幣金額」的現金／儲值卡消費，
自動補上換算後的台幣。**已經有台幣金額的消費一律不動。**

**事後修改行程匯率時**（例如中途又換了一次錢）：
只影響「之後新增的消費」與「目前仍沒有台幣金額的消費」。
**已經算出台幣金額的消費不追溯、不重算。**
理由：追溯重算會讓前面已經對過的帳整批變動，使用者無從察覺。

單筆要用不同匯率 → 在那一筆覆寫，不要去改行程匯率。

### 2A.5 資料

`trips` 新增兩欄，存**原始輸入**而非只存匯率，這樣使用者回頭看得懂當初怎麼換的：
- `cash_rate_twd numeric null` — 換出去的台幣
- `cash_rate_foreign numeric null` — 換到的外幣

匯率 = `cash_rate_twd ÷ cash_rate_foreign`，是衍生值不另存。
兩欄皆為 null＝這趟還沒設現金匯率。

單筆覆寫沿用既有的 `expenses.exchange_rate`，該筆有值時優先於行程匯率。

### 2A.6 與各自金額的關係

各自金額仍照 2.2 的公式換算（台幣總額 × 各人外幣 ÷ 外幣總額），**不使用匯率**。
現金匯率只負責「整筆的外幣 → 整筆的台幣」這一步；
整筆台幣算出來之後，各人分擔照原本的比例公式走，兩者不衝突。

---

## 2b. 現金匯率（2026-09-04 定案：方案 C）

### 2b.1 為什麼要有

現況沒有這個功能：`trips` 只有幣別、沒有匯率，`expenses.exchange_rate` 是由
「台幣 ÷ 外幣」回推的唯讀顯示值，不會套用到別筆。
後果是現金消費只填外幣時，該筆沒有台幣金額 → 結算跳過它 → 那筆錢憑空消失。
一趟旅程幾十筆現金，使用者得每筆自己按計算機。

### 2b.2 規則

**行程層級設一個「這趟的現金匯率」，單筆可覆寫。**

| 支付方式 | 填什麼 | 台幣怎麼來 |
|---|---|---|
| **現金** | 填當地幣別金額 | 用行程的現金匯率自動換算 |
| **刷卡** | 直接填台幣（帳單金額） | 就是使用者填的值。外幣選填，只當紀錄 |
| **儲值卡等** | 同現金 | 用行程的現金匯率 |

刷卡不套用現金匯率——銀行匯率浮動，用現金匯率算刷卡會錯。
這與既有規則「現金照當地貨幣記、刷卡以台幣記」一致。

### 2b.3 怎麼設定

**主要情況只填一個數字。**

（2026-09-04 兩次修正。第一版做成「換了多少」與「匯率」兩組並列輸入共 4 個框，
過於複雜。第二版參考 WOWPASS 換匯介面收斂成 2 個金額欄，但那個介面的邏輯是反的——
WOWPASS 的匯率是銀行給的已知值，使用者填金額看能換多少；
Tripay 的匯率是使用者要告訴系統的未知值，**換了多少錢對分帳完全沒有影響，只有匯率有影響**。
第三版即本版。）

    ┌──────────────────────────────┐
    │  這趟的現金匯率               │
    │  在當地換錢後填一次就好        │
    │                              │
    │  🇹🇼  [    1    ]  TWD        │  ← 預設 1，通常不用動
    │  🇰🇷  [   45    ]  KRW        │  ← 只填這個
    │                              │
    │  刷卡不用這個匯率，直接填台幣    │
    └──────────────────────────────┘

規則：
- **「1」擺哪一欄依幣別決定**（見下方 2b.3a），另一欄空白待填，
  那是唯一必填的數字。兩欄都可編輯
- 匯率 ＝ 外幣 ÷ 台幣，內部計算，**不另外顯示一行「1 TWD ≈ N 外幣」的灰字**
  ——畫面本身就是那個算式，再寫一次是重複
- 兩欄雙向即時運算：改任一欄，匯率跟著變
- **兩種情境同一個介面，不做模式切換**：

  | 使用者手上有什麼 | 要填幾個數字 |
  |---|---|
  | 知道匯率 | 「1」那欄不動、另一欄填數字 → **1 個** |
  | 只記得換了 10,000 拿到 450,000 | 把 1 改成 10000、另一欄填 450000 → 2 個 |

### 2b.3a 「1」擺哪一欄

台灣人講匯率會選**數字好記的那個方向**，而方向依幣別不同：

| 幣別 | 習慣講法 | 「1」放哪 |
|---|---|---|
| KRW 韓元 | 1 台幣 = 45 韓元（反過來 0.022 太小不好記）| **台幣欄** |
| JPY 日圓 | 1 日圓 = 0.22 台幣 | **外幣欄** |
| THB 泰銖 | 1 泰銖 = 0.9 台幣 | **外幣欄** |
| USD 美金 | 1 美金 = 32 台幣 | **外幣欄** |
| VND 越南盾／IDR 印尼盾 | 1 台幣 = N 外幣 | **台幣欄** |

**判斷規則**（其餘幣別依此自行判定，不需逐一列表）：

    1 單位外幣換算成台幣後 < 0.1 → 「1」放台幣欄
    否則                        → 「1」放外幣欄

**「1」的那一欄排在上面**，讀起來永遠是「1 X = N Y」，最自然。
所以兩列的上下順序會依幣別改變，這是刻意的。

這只影響**預設值擺哪邊**，不影響計算正確性——
使用者把 1 移到另一欄照樣算得出同一個匯率。

- **標籤用國旗 emoji ＋ 幣別代碼**，不要「台幣總額」這類文字標籤
- **焦點狀態**：正在編輯的欄位用主文字色，另一欄轉次要文字色
- **不做幣別下拉選單**：兩列幣別由行程設定決定
- **外幣小數位數依幣別**：KRW／JPY 不給小數，USD／EUR 給兩位
- **不做自訂數字鍵盤**：`inputmode="decimal"` 即可
- **不自動抓網路匯率**：當地換匯含價差，與市場牌價不同，自動帶入會算錯

**設定位置**：`S-02b` 編輯行程頁，與「這趟的支付方式」並列。
**不放在建立行程（S-02）**——換錢通常發生在出發之後，建立行程當下還沒換，
問了只會讓建立流程變長（同 S-02-19 砍色調的理由）。

**捷徑**：記第一筆現金消費時，若這趟還沒設現金匯率，
在金額欄旁提供一個就地設定的入口，設完直接回到記帳流程，不跳出去。

### 2b.4 修改匯率時的回溯

修改行程的現金匯率時，**所有「使用行程匯率換算」的消費即時重算台幣金額**。
單筆覆寫過匯率的消費不受影響。

已結算的行程不重算——結算是快照，已經定案。

### 2b.5 還沒設匯率時

現金消費只填了外幣、行程又還沒設匯率 → 該筆**沒有台幣金額**，
標 `twd_pending`，結算跳過（維持既有行為），並在該筆上顯示可點的
「設定這趟的匯率」入口。**存檔照樣成功，不擋。**

### 2b.6 與各自金額的關係

各自金額仍以該筆幣別填寫（見 2.1）。
各人台幣的算法**優先用第 2.2 節的比例回推**（台幣總額 × 各人外幣 ÷ 外幣總額），
因為那反映的是實際付款金額；只有在台幣總額不存在時，才用現金匯率換算各人金額。

---

## 2c. 各自金額的填寫幣別與餘額基準（2026-09-04 補，修正實測 bug）

### 2c.1 填寫幣別由使用者明確選擇

各自金額區有一個**兩選一的切換器**：`$ 台幣填` / `外幣填`，預設台幣。
**不得由系統推斷**（例如用「有沒有填外幣總額」去猜），那會猜錯。
切換時已填的數字保留、不清空。

這對應既有規則「同一筆只能用一種幣別填，不混填」——
要有切換器才表達得出「用的是哪一種」。

### 2c.2 餘額基準跟著填寫幣別走

| 填寫幣別 | 餘額基準 | 要不要外幣總額 |
|---|---|---|
| **台幣** | 台幣總額 − Σ已填 | **不需要**。台幣總額空白才算不出來 |
| **外幣** | 外幣總額 − Σ已填 | 需要 |

**「外幣總額空白且有人沒填就不猜」這條，只在填寫幣別為外幣時成立。**
理由（只有一人填 12000 時把總額補成 12000、那人就佔 100%）是外幣情境專屬的風險，
台幣情境沒有這個問題。

⚠️ **曾發生的 bug**（Rozi 2026-09-04 實測）：
台幣總額 2457、填寫幣別 TWD、三人填 1000／400／未填 →
未填者顯示 0，並提示「補上外幣總額才算得出各自要付多少」。
兩者都錯：該筆用台幣填，餘額 = 2457 − 1400 = **1057**，用不到外幣總額。
根因是判斷不分填寫幣別，一律觸發 `noForeignTotal`。

---

## 2d. 不強制加總相等（R9，2026-09-04 補漏）

**此條在決策表中已定案，但先前漏未寫進本規格。**

各自金額的加總**不必等於總額**，不得以此擋下存檔。

| 差額 | 呈現 |
|---|---|
| 0 | 正常，可用視覺標示「剛好」 |
| ≤ 總額的 1% | **淡色提示**，不警示 |
| > 總額的 1% | **橘色警示** |

三種情況**都可以存檔**。差額一律**歸付款人**（引擎既有規則，Σ淨額恆為 0）。
付款人未參與該筆時規則相同。

註：走自動均分時加總必然等於總額（餘額分給未填者）；
差額只在**所有人都手動填了、但加總 ≠ 總額**時出現。
例：總額 3000、三人各填 900 → 加總 2700、差 300 歸付款人，
300/3000 = 10% > 1% → 橘色警示，仍可存檔。

---

## 3. 未定案的判定

依「未輸入人數」判定：

| 未輸入人數 | 判定 | 理由 |
|---|---|---|
| 0 人 | 不標記 | 全部使用者輸入 |
| **1 人** | **不標記** | 該人金額 ＝ 總額 − 其他人合計，是**唯一解**，系統算得出來，不是猜的 |
| 2 人以上 | 標記 `is_estimated = true` | 均分只是眾多解之一 |

標記加在該筆消費中所有「未輸入」的 `expense_splits` 列上。

**優先權**：本規格取代先前「分擔欄空白即視為待填」一條。正確行為是
總額有值 → 空白欄位自動均分並依上表判定；總額也沒有值 → 整筆標 `twd_pending`。

---

## 4. 存檔行為

**一律允許存檔。** 不得擋下、不得跳確認對話框、不得 disable 儲存鍵。

存檔後提示，四種：

| 情況 | 文案 |
|---|---|
| 整筆金額未填（台幣外幣皆空） | 已存。這筆金額還沒填，之後補上就會算進總花費。 |
| 1 人未輸入 | 已存。{名字} 的金額由總額推算。 |
| 2 人未輸入 | 已存。{名字A} 和 {名字B} 的金額還沒填，先照均分算。 |
| 3 人以上未輸入 | 已存。還有 {N} 人的金額還沒填，先照均分算。 |

整筆金額未填時只顯示第一句，不再依人數顯示其他提示。

---

## 5. 顯示

### 5.1 行程統計卡（S-03 上方）

每人分擔那一列，若該成員符合**任一**條件，標記：
- (a) 參與了某筆整筆金額未確定（`twd_pending`）的消費——總額未知，他的分擔自然也未知
- (b) 自己的某個 `expense_splits` 列 `is_estimated = true`

**不得**用「參與了任何一筆未定案消費」這種寬鬆判定。「各付各的」型態下各人金額彼此獨立：
某成員已自行輸入金額時，即使同筆有其他人未輸入，他自己的金額仍是確定的，不標記。

標記樣式：
- 金額前加「約」字，次要文字色，字級小一階
- 金額加虛線底線，工作色
- 整列可點 → 進篩選後列表

卡底註腳（次要文字色）：`標「約」的金額還沒算清楚，點名字看是哪幾筆`
沒有任何成員被標「約」→ 註腳整行不顯示。

**移除**現行統計卡上的「N 筆待填，數字僅供參考」整條提示，功能由 5.2 取代。

### 5.2 消費列表頂端入口（S-03 下方）

「消費紀錄 · N 筆」標題下方一條可點入口：`有 {N} 筆還沒算清楚 ›`

N ＝ 符合第 1 節定義的消費筆數。**同一筆同時符合 (a)(b) 只算一次，不得重複計數。**
N ＝ 0 時整條不顯示。

允許出現「入口顯示有 N 筆，但沒有任何人被標『約』」的狀態。
例：一筆整筆金額未定、且尚未選任何參與成員的消費——它確實未定案（計入 N），
但沒有成員受影響（不標「約」、註腳隱藏）。**這不是 bug，不要為了讓兩處數字一致而改動任一邊。**

### 5.3 篩選後列表

5.1 點成員、5.2 點入口，進入的是**同一個畫面**，只有標題與資料範圍不同：
- 從 5.2 進入 → `還沒算清楚 · {N} 筆`，內容為全部未定案消費
- 從 5.1 進入 → `影響 {名字} 的 · {N} 筆`，內容為該成員符合 5.1 (a)(b) 任一條件的消費

兩者都要有返回鍵，**返回行程頁（S-03）頂部，不是瀏覽器上一頁**。
列內每筆可點，進入該筆編輯畫面。

### 5.4 消費列

| 情況 | 樣式 |
|---|---|
| 整筆金額未定 | 維持橘紅左邊框，金額欄顯示「還沒填」。**移除現行的「待補填」badge**——金額欄已經說了，badge 是重複 |
| 各自金額 `is_estimated` | 金額前加「約」字，次要文字色。不加邊框、不用警示色 |
| 兩者同時成立 | 橘紅左邊框優先，金額欄顯示「還沒填」，不再加「約」 |

### 5.5 記帳表單（S-04）

- 未輸入的欄位以 **placeholder** 顯示自動均分值。
  **placeholder 是唯一的呈現方式**，不得把自動值寫進 input 的 value，
  也不得在使用者輸入過程中重新建立或改寫 input 節點——
  這會導致游標錯亂、輸入的數字順序顛倒（已咬過一次）。
- 分擔金額欄位區下方一行灰字：`沒填的先照均分算，之後可以再改`

### 5.6 已結算／已封存的行程

**不顯示「約」標記、不顯示未定案入口、不做結算前檢查。**
結算已經定案，提醒改變不了任何事，只會製造焦慮。

---

## 6. 結算前檢查（S-05）

檢查時機是**按下「結算行程」按鈕的當下**，在進入結算頁之前。不得先進結算頁再檢查。

    按下「結算行程」
      → 檢查本行程是否有金額未定案的消費
      → 沒有：直接進結算頁，行為與現行相同
      → 有：先顯示檢查層

檢查層：
- 標題：`有 {N} 筆還沒算清楚`
- 內文：`結算之後金額就固定了。要先去看一下嗎？`
- 清單：逐筆列出，顯示 emoji、品項名稱，右側小字說明原因
  （整筆未定 → `金額還沒填`；各自未輸入 → `{N} 人還沒填`）。每列可點，直接進該筆編輯
- 按鈕：主要 `先去看一下` ／ 次要 `就這樣結算`

**「就這樣結算」必須真的能完成結算。這是提醒，不是禁止，任何情況下都不得阻止使用者結算。**

行程曾結算後又重新開啟，再次按下「結算行程」時本檢查照常執行。

---

## 7. 文案

上位詞用「**還沒算清楚**」，估計值一律標「**約**」。

全站「**待填**」與「**待補填**」一律改為「**還沒填**」（含程式碼註解，只改文字不動語意）。
註：統計卡的「N 筆待填，數字僅供參考」是**整條刪除**，不是改字（見 5.1）。

否決過的說法與理由（不要再提案）：
- 「待確認」：公文名詞，與「記一筆」不同語域，有罐頭感
- 「還會變」：讀起來像數字自己會動，但實際上是使用者之後會改

---

## 8. 結算演算法

**完全不修改。** `is_estimated` 純粹是顯示旗標，不進入任何計算。
自動均分的數值本身會寫入 `split_amount`，因此 Σ各人分擔 ＝ 消費總額 恆成立，
結算 net_balance 總和為零的性質不變。

---

## 9. 已知缺口（不在本規格範圍，另案處理）

- **已封存的行程，消費仍點得進去編輯**：唯讀只綁在 `isArchived` 的部分位置，
  消費列的 openEdit 沒有狀態守衛。既有 bug，非本規格造成。已記入問題清單。
