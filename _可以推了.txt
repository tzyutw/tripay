Cowork 複驗通過 —— 實作-U 七項可以 push。　2026-09-07 16:35

涵蓋到 commit **3c4bc24**（實作-U 六項 37aab0f ＋ U-7 3c4bc24）。

判定程式全部由 Cowork 撰寫並執行，量渲染結果不量原始碼字串：
  conserve.mjs      守恆式（停止條件 1-4）全過。**「驗收：濟州島」守恆差 50,860 → 0**
                    四趟已結算行程的三欄與改前基準檔（baseline_preU.json）**逐項相同**
  verify_U.cjs      29 條，2 紅（19d，既有問題，見下）
  verify_U7.cjs     28 條全綠（U-7 的 21-34）
  hub.cjs           10 條全綠｜direct 與 hub 兩模式的 5d/5e/5f 都過
  verify_T.cjs      41 條全綠
  verify_S.cjs      62 條全綠
  verify_R.cjs      28 條全綠（版面基準未動）
  standing_C6.cjs   11 畫面 × 6 種元素 = 113 個互動元素全過
                    （U-4 之前紅的 3 顆 31px 矮按鈕現在都過了）

這一輪不需要 migration（沒有 schema 變更）。

── 已知未修，不是這一輪造成的 ───────────────────────────────
**已結算／已封存態下，消費列仍顯示「約」標記**（`藥妝店 約 $ 1,035`）。
`規格_金額未定案與幣別.md` §5.6 明寫「不顯示「約」標記、不顯示未定案入口、
不做結算前檢查」——未定案入口已正確不顯示，只有「約」還在。
**pre-U（35f1bef）量到一模一樣的值，是既有缺口。**
斷言（verify_U 19d）**刻意留紅不放寬**，等下一節處理。

── Cowork 這一輪自己的斷言錯了六處（都不是實作的問題）──────────
1. verify_S 5e：用「fixture 第幾筆」點「第幾個 .exprow」，但清單按日期分組排序。
2. standing_C6：我為了省時間把「無條件 scrollIntoView」改成「看不到才捲」，
   結果元素被 sticky 底部按鈕列蓋住 → 7 個假紅。已改回並註明不要再動。
3. verify_U 7／12：`.perrow` 要先點開「總花費」才出現，我沒展開就量。
4. verify_U 19：用 `?unsettled=mix`——`unsettled` 是**進未定案頁**的路由參數，
   結果整組在錯的畫面上綠（假通過）。正確開關是 `?fill=noforetotal`。
5. verify_U 19d：原本寫「已結算不得出現警示左框」是**引用錯規格**，
   §5.6 沒有提到左框。已改成驗它真正寫的三件事。
6. verify_U7：用「藥妝店」找列，但那一筆的標題是「藥局（外幣總額空白）」。
   另外漏了停止條件 24（1／2／3 人三種文案），已用 `?blanks=N` 補上。
