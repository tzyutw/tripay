Cowork 複驗通過 —— 實作-T 九項可以 push。　2026-09-07 15:20

跑了什麼（全部由 Cowork 撰寫並執行，量渲染結果不量原始碼字串）：
  verify_T.cjs        41 條全綠｜金絲雀打 pre-T(220939e) 紅 16 條，九項每項都紅過
  verify_R.cjs        28 條全綠｜基準以 pre-T 現場重量，s03 1604→1632（+28＝T-6 的 16→30 與 74→88）
  verify_S.cjs        62 條全綠｜pre-T 也全綠 → 沒有把實作-S 改壞
  standing_C6.cjs     11 畫面 × 6 種元素 = 113 個互動元素
  _驗收案例.md         T1–T5 全 ✅｜核心區 A9／A11／A12 重跑通過
  production 寫入      begin…rollback 走完整條龍，rollback 乾淨、正式資料 0 筆變動

這一輪不需要 migration（沒有 schema 變更）。

── 下一輪要覆核的（不是擋 push 的理由）─────────────────────────
1. commit d489420（harness 的 unsettled/view 參數與 data-settle-row 屬性）是 Cowork
   自己寫的 src 變更，**越界、沒有經過第二人檢查**。請在下一節指令覆核這兩個檔案：
   src/test/harness/main.tsx、src/test/harness/fixtures.ts。
2. C6 掃出 3 顆既有的矮按鈕（31px，門檻 44）：「標記付清」×2、「開一趟自己的」。
   pre-T 量到一模一樣的三顆，**不是實作-T 造成的**，這一輪刻意不動（不擴大工項）。
3. 常設新增 C7（帳務三欄一致性，`acct_check.mjs`）。Rozi 已拍板「這一輪先不動」尾差顯示。
