Cowork 複驗通過（2026-09-07 04:5x）——實作-S 可以 push。

migration 016（expenses.note ＋ 200 字 check）**已套上 production**
（套之前 begin…rollback 乾跑：426 筆既有消費、0 筆有備註、constraint 掛得上）。

verify_S.cjs   61/61 綠（現行版建置，390×844，含 320/414 兩個寬度）
verify_R.cjs   28/28 綠（常設回歸：可點區門檻、中心點命中自己、B 類未被拉高）
case_A11_A12   5/5 綠　　case_A9 7/7 綠

金絲雀：同一套斷言打在實作-S 前一個 commit（5adbdf1）現場建置的 harness
→ verify_S **24 紅**（換算範例、USD 示範金額、移除鈕字元、備註欄六條、
預設日期三條、金額兩句同時顯示、按鈕 sticky、外幣灰的方向）。斷言會咬。

版面：九畫面前後對照，五個 0 差異；四個「刻意」變高且逐項對得上——
s02/s02b +19（換算範例那一行）、s03 +74（sticky 按鈕列的底部留白，
正好是指令指定的 74px）、s04 +58（備註列）。`.chip`／`.rowb` 高度集合完全相同。
verify_R.cjs 的 BASELINE 已更新成這一輪的值，下一輪拿它當基準。

驗收案例（`_驗收案例.md`）：核心區 12 條 **全部跑過並通過**。
A1／A3／A4／A6 是用一筆 begin…rollback 交易，以 authenticated 身分把
「建立行程 → 加成員 → 只填外幣記一筆 → 分帳 → 結算 → 轉帳明細」整條走完再回滾。
A3 量到 10,000 JPY ＠0.21 = **2,100 台幣**，跟畫面上那行換算範例同一個數字。

---

你在 _停點.md 抓到的那個錯，是我的：指令上把 10,000 ÷ 0.21 寫成 47,619,048
（多了三個 0），正確是 47,619。斷言照正確值寫，謝謝抓到。

我自己這一輪也寫錯四條斷言，都已修正（記在檔案的註解裡）：
① 2g 一致性硬寫 2,100，但 s04 的假資料匯率是 0.22 → 2,200，改成從該畫面自己的匯率算；
② 4e 要求所有「移除」鈕都 24×24，但支付方式的 `.rmbtn` 一直是 16×16，改成分開驗；
③ 5e 拿 `?screen=s04`（新增表單）當「讀得回備註」的守門，那裡本來就沒有既有備註，
   改成從 s03 點列進編輯；
④ 9d 用 line-height 當換行門檻，但那個元素量到的 line-height 是 0px，改成量單行高度。

**最重要的一條**：11（外幣灰）我第一版只驗「兩欄顏色不同」＋「外幣對比 >= 4.5」，
**方向做反也會全綠**。已改成「兩欄都填真值時，台幣必須比外幣深」，
金絲雀證實這條在 pre-S 是紅的。實作本身做對了（外幣 rgb(76,91,100) 對比 7.03、
台幣 rgb(21,28,33) 對比 17.21），是我的斷言差點放行。

push 完這一輪結束。
