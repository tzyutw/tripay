Cowork 複驗通過（2026-09-07 01:5x）——實作-R 可以 push。

verify_R.cjs  28/28 綠（現場建置的 harness，390×844）
verify_R_icon.py 15/15 綠（三張 PNG：T 高 52.0～52.2%、中心偏差 <0.3%）

金絲雀（同一支斷言打在實作-R 之前的建置 569cf56 現場建置版）→ 7 紅：
  可點區門檻／已結算列是 button／已結算點得開表單／
  ?state=archived 守門／封存不得開表單／封存 toast／已結算存檔提示
→ 斷言會咬，不是空跑。

production 寫入路徑實跑（begin…rollback，authenticated 角色）：
  已結算行程 expenses UPDATE → 2 筆 returning ✅
  已結算行程 expenses 軟刪除 → 1 筆 ✅
  已結算行程 expense_splits UPDATE → 12 筆 ✅
  （R-4 讓已結算行程重新可編輯，RLS 沒有把這三條路徑擋掉。）

tsc --noEmit → 0 錯。

⚠️ 你在 _停點.md 提的兩個「基準值量錯了」——是我的基準取樣條件不同，不是你的錯：
  s02 941／s02b 1201 是「匯率沒填」時量的，驗收時填了 0.21，版面本來就不同。
  我用 569cf56 現場建置了一份 pre-R harness，同一支腳本、同一組步驟前後對照：
  九個畫面捲動高度 844/924/1184/1530/725/844/1419/844/844 → 前後**完全相同（差 0）**，
  .chip 與 .rowb 的高度集合也完全相同。版面沒有任何改變。
  你說的 s01 1688 我這邊量到的是 844（前後都是），量法不同，不影響結論。

push 完之後不用再做別的，這一輪結束。
