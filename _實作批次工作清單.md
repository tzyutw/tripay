# 實作批次工作清單

> 已裁示、待實作的項目。**實作時逐條處理，回報中對號。**
> 來源：讀碼發現 14 條（`wireframe_v5_完整版.html` 最後一節）＋ Rozi 2026-08-31 裁示。
> 現況盤點索引見 `_盤點_畫面功能.md`。

## 已完成（不必再做）

> #9 v3（2026-08-31）已部署：行程 emoji 全站退場（7 處）、字體規則落地
> （webfont 移除／font-serif 全清／tabular-nums 只留金額）、登入頁幽靈卡移除。
> 勾稽見 `_稿碼對照.md`。

| # | 項目 | 處理 |
|---|------|------|
| 2 | 分享連結漏 BASE_URL（線上死鏈）| ✅ hotfix `0d2fd34` 已部署 |
| 3 | 贊助說明「4 人」寫死 | ✅ 同上 |
| 1 | 首頁 G-06「複製連結」無 onClick | ✅ 結案為**拿掉**：隨 S-01-9 移除 |

## 第一批（既有畫面換視覺）時一併處理

| # | 項目 | 建議做法 | 對應編號 |
|---|------|---------|---------|
| 4 | 行程內頁與分享頁 hero 寫死漸層，沒用 `destinationOf()` | 三處統一用 `destinationOf(trip.name, trip.id)`；封面照上線後共用同一套 fallback | S-03-1、S-06-1 |
| 5 | 行程頁總花費排除贊助、分享頁沒有 → 同趟兩頁數字不同 | 以行程頁為準（贊助是負額，計入會灌低總支出） | S-03-9、S-06-4 |
| 6 | 分享頁待填列沒有左邊框 | 補上，與 S-03 一致（決策 C） | S-06-10 |
| 7 | 「刪除後無法復原」其實是軟刪 | 文案改成誠實說法（不做復原 UI） | S-04-24 |
| 8 | G-08「最大手筆」標外幣符號、值是台幣 | 連同決策 34 幣別切換一起處理 | S-05-17 |
| 9 | 沒標「這是我」時「我的花費」恆為 $0 且無提示 | 改顯示「還沒標記你是誰 →」，點了開 S-02b 成員區 | S-03-11 |
| 10 | 設定頁只能從行程內頁 ⚙ 進去 | 首頁標題列右上加入口（四個新畫面的必要前提） | S-01-1、S-07-1 |
| 11 | 首頁空狀態沒有任何按鈕 | 空狀態補一顆主要按鈕（Rozi 2026-08-31 確認 S-01-4 保留並補 CTA）| S-01-4 |
| 12 | 「刪除這趟行程」封存時也顯示、被固定底部列蓋住 | 移進 S-02b 編輯行程最下方，列表頁不放 | S-03-25 |
| 13 | 消費類別 emoji 只能自動推斷 | S-04-3 改成可點，複用既有 EmojiPicker | S-04-3 |
| 14 | 結算前看不到任何金額 | **已定案**：未結算態新增唯讀預覽「暫時算一下」，警語「記帳還在變，按『算清楚』才算數」，每人一行沿用結算頁格式；即時計算、**不寫入 settlements**、不影響「算清楚」流程與其後畫面 | S-05-28 |

## P-3 裁定的分享動線改動（同批）

- **移除 G-05 分享橫幅**（S-03-13）：連同 sessionStorage 的 `g05-dismissed` 邏輯一起拿掉
- **S-05 結算完成頁新增分享 CTA**（S-05-29）：升為該頁主要動作，
  「建立新行程／封存行程」降為次級。**文案已定案**：主「把結果傳給大家」／副「任何人免登入可看」
- S-03 右上分享常駐 ↑ **不動**（6/26 既有決策）
- **推翻方式**：恢復 G-05 的觸發條件即可；S-05-29 可獨立保留或一併移除

## v5 第二批裁定（2026-08-31，同批實作）

| 項目 | 做法 | 對應編號 |
|------|------|---------|
| 封面 emoji 取消 | 移除 cover 模式呼叫；建立行程一律套 `destinationOf()` 色調。**`trips.emoji` 欄位保留不刪** | S-02-5、S-02b-6 |
| 色調預設盤 | S-02b 兩個動作：更換照片／重選色調；色調來源限 `destinations.ts` 8 種，**不做挑色器** | S-02b-4、S-02b-10 |
| **刪掉 `EmojiPicker.tsx`** | 三層 fallback＋就地編輯；驗證放寬為一個 grapheme（`firstGrapheme()` 保留）；`trip_members.emoji` 寫入改空字串 | S-02c-10～13 |
| 首頁卡移除成員列 | **只有首頁**；行程內頁／記帳表單／結算頁／分享頁照常，S-03E 空狀態依決策 D 仍顯示 | S-01-5、S-01-13 |
| 日期規則 | 出發預設今天、回程可留空；空回程送出跳確認後以單日建立 | S-02-9、S-02-20 |
| 字體規則 | 等寬只用於金額；日期／天數／筆數改 PingFang TC | 全站 |
| **009 migration** | `trip_members.emoji` 預設改 `''`。**待 Cowork 端套用** | — |
| G-05 全清（#9-D）| `ExpenseListPage:141／285／294／394／408` ＋ sessionStorage key `g05-dismissed` | S-03-13 |

## 同批的視覺／行為變更（來自 v5 盤點表）

- 幣別切換改為**整頁一起切**（決策 34）；結算恆為台幣不受影響 — S-03-12
- 補 `settled_on_spot`／`is_sponsor` 的列表 badge（目前列表完全看不出來） — S-03-20、S-03-21
- 設定頁升為樞紐：新增「我的資料」「顯示」兩段 — S-07-4、S-07-5
- 危險動作用色分級：金錢方向只出現在文字、危險動作才實心填底 — S-03c、S-09-12/13
- 摺紙 signature（品質門檻：不達標退回乾淨 loading） — S-05-27

## v5 第三批裁定（2026-09-04）

| 項目 | 做法 | 對應編號 |
|------|------|---------|
| 刪除確認改「刪除」二字 | `ExpenseListPage` 的 `deleteConfirm !== trip.name` 改比對「刪除」 | S-03c-4/5/7 |
| 移除「之後再填」toggle | `AmountRow` 拿掉 toggle；空欄自動寫 `twd_pending`／`foreign_pending`；**移除 Bug #5 的擋下邏輯** | S-04-4/5/6/8 |
| ⚠️ pending 旗標必寫 | 空欄若沒寫旗標 → `twd_amount=null` 且 `twd_pending=false` → **結算整趟回 422**。需補回歸測試 | S-04-29 |
| 支付方式行程層級 | `PAYMENT_OPTIONS` 常數改為讀行程清單；S-02b 加清單管理 UI | S-02b-11、S-04-9 |
| **P1-0 各自金額幣別** | 新增 `split_amount_foreign`；`split_amount` 語意維持台幣 | 資料層提案 E |

> 分帳模型（版本一／版本二）**尚未裁示**，S-04-11～18 的實作要等選版後才動。

## 第二批（新畫面）

S-08 通訊錄 ／ S-09 卡片管理 ／ S-10 帳單週期 ／ S-11 副卡視角 ／ S-02b 封面上傳
（008 已套用 production，不再被阻擋）

## 待 Rozi

| 項目 | 狀態 |
|------|------|
| v5 第二批逐頁調整清單（S-02 起） | 等清單 |

> P-1／P-2／P-3 與 S-01-4 皆已於 2026-08-31 裁定，不再是待辦。
