# 需求台帳（Rozi 講過的每一條需求 → 哪一條斷言在守）

> **這份檔案的用途**：交付前第 4 道檢查「需求 ↔ 斷言對照」的落檔處。
> 對照表不能只出現在對話裡——對話會捲掉，下一輪就沒人知道這條需求有沒有人在守。
>
> **寫指令之前先讀這份**，特別是「常設」那一區：那些是「全站一致」類的需求，
> 一次做完不代表以後不會壞，**每一輪都要跑**，否則就是改 A 壞 B。
>
> **一條需求都不准不在表上。** 每一列只有三種結局：
> ①有斷言 ②已列進「機器驗不到」清單 ③已明確記錄為「這一輪不做」。
>
> 狀態：`守著` 有斷言且在跑｜`人工` 只有 Rozi 在真機看得到｜`不做` 已裁示不做｜`待補` 還沒有斷言（不准留過夜）

---


## 實作-U（2026-09-07　Rozi 一邊登錄「驗收：濟州島」一邊回報）

| 她講的 | 指令 | 守它的斷言 | 狀態 |
|---|---|---|---|
| 點成員金額列進去沒資料，要看該成員的消費紀錄 | U-2 | `verify_U.cjs` 5、6、6b | ✅ |
| 分成均分／各自付各的／只算自己三段 | U-2-b | `verify_U.cjs` 6 | ✅ |
| 三段加起來要等於列上的數字 | U-2-b | `verify_U.cjs` 7 | ✅ |
| 明細點進去看「實際上他付了多少」 | U-2-c/e | `verify_U.cjs` 8a、8b、10a | ✅ |
| 列上的數字維持「算我頭上的」 | 不動 | `conserve.mjs` 全趟守恆 | ✅ |
| 還沒算清楚的那筆要列出來、點得進去補 | U-2-d | `verify_U.cjs` 9、9b、10 | ✅ |
| 「藥局」那筆算錯（韓元被當台幣） | U-1 | `conserve.mjs` 逐筆＋全趟守恆（**常設 C8**） | ✅ 50,860 → 0 |
| 三顆矮按鈕要修 | U-4 | `verify_U.cjs` 14a、15、16；`standing_C6` | ✅ |
| 在一長串消費裡看不出哪些要調整 → 紅線標示 | U-6 | `verify_U.cjs` 19a、19b、19c | ✅ |
| 「填一邊就好」在這情境是騙人的 | U-7-a | `verify_U7.cjs` 21a、21b、22a、26 | ✅ |
| 提示要講出「是那兩個人沒填」 | U-7-b | `verify_U7.cjs` 23、23b、24-1/2/3 | ✅ |
| 不是「錯了」是資訊不完整（語氣） | U-7-b | `verify_U7.cjs` 25（不含「錯／無效／請修正」） | ✅ |
| 沒填的欄位只加紅框、不加字 | U-7-d | `verify_U7.cjs` 31、31a、32、33 | ✅ |
| **不攔存檔**（規格 §4 維持不動，她看過設計意圖後保留） | U-7 拍板 | `verify_U7.cjs` 34、34a（反向） | ✅ |
| 手機震動 | — | **做不到**：iOS Safari 到 26.6 不支援 `navigator.vibrate`，已取消 | — |
| hub 結算沒人走過 | U-5 | `hub.cjs` 10 條 | ✅ |

**機器驗不到、只有 Rozi 看得到**：三段名稱讀不讀得懂、「你付的」小字的位置與大小、
註腳文案長度、新頁面在真機上的版面、紅框在真機上會不會被讀成「你填錯了」。

**這一輪未修（既有缺口）**：已結算態仍顯示「約」（違反 §5.6），
`verify_U.cjs` 19d 刻意留紅。

## 常設（全站一致類，每一輪都要跑，不是一次性）

| # | Rozi 的需求（她的話） | 定案 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| C1 | 「如果沒有輸入 emoji 就會抓第一個字加底色」 | S-02c-10 全站一致，一律走 `Avatar.tsx`，獨立 28px／chip 內 20px | `?members=noemoji` 逐畫面量 `.avatar.letter` 數量與尺寸；全站 `🙂` 出現次數 = 0 | 守著（**2026-09-08 Rozi 縮限**：識別圖只出現在「獨立標示這是誰」的位置；**句子裡的成員只寫名字**，不放識別圖。由來：結算卡片的轉帳敘述變成「N Ning 給你 $12,500」，她要的是「Ning 給你 $12,500」。） |
| C2 | 「不要改 A 壞 B」 | 元件宣告的尺寸必須等於畫面渲染的尺寸 | 每輪跑「宣告 vs 渲染」對照，不一致即紅（CSS 特異度陷阱已發生四次） | 守著 |
| C3 | 「沒點名的東西不要碰」 | 指令只能動明確點名的元素 | 每節都有 `git diff --stat` 檔案白名單 | 守著 |
| C4 | 手機上不能橫向捲動 | 390／320／414 三寬度都不得橫向捲動，**含進場動畫進行中的每一幀** | 量真正會捲動的那個容器；**動畫期間 0/20/50/80/120/160ms 各取樣一次**（2026-09-07 修訂：原本只在動畫結束後量，漏掉 `translateX(100%)` 讓文件變 780px 的那 200ms） | 守著 |
| C5 | iOS 上輸入框聚焦不能讓整頁放大 | `input{font-size:16px}` 全域維持 | 反向斷言：所有輸入框 computed `font-size` ≥ 16px | 守著 |
| C6 | 「按『換個方向』沒有反應」（＝點不到） | **每一個互動元素**（`button`／`a`／`input`／`select`／`textarea`／`[role=button]`）的有效可點區（含 `::after`／`::before`）須 `h≥44` 或（`h≥34` 且面積 `≥1600px²`）；且中心點的 `elementFromPoint` 必須是它自己或子孫 | 九畫面全掃（量之前先讓條件式元素出現——包含**點下去才會出現的就地編輯輸入框**——並先斷言目標數量下限） | 守著（2026-09-06 新增；**2026-09-07 從「只掃 button」擴大到所有互動元素**，由來：emoji 就地編輯框 26×26 沒被掃到）<br>判定程式：`standing_C6.cjs`，掃 **11 個畫面**（含 `?unsettled=all`「還沒算清楚」、`?state=settled` 已結算結算頁）× 6 種元素 = 113 個互動元素。<br>⚠️ **2026-09-07 掃出 3 顆既有的矮按鈕未過門檻**（「標記付清」×2、「開一趟自己的」，都是 31px），pre-T 量到同樣三顆，**不是實作-T 造成的**，這一輪未修，見 `_驗收案例.md`「沒有人在守的」。 |
| C7 | 「這筆帳到底怎麼算出來的」 | 結算頁展開的三欄 **實際付出 − 應分攤 = 差額** 必須對得起來，**而且要用正式資料驗，不能只用假資料** | `acct_check.mjs`：把 production 的行程餵進**真的 `calc()`**（不是 SQL 近似，SQL 複製不了「差額歸付款人」與 individual 的比例回推）逐人比對。<br>⚠️ 假資料筆數少、剛好整除，`verify_T` 5d–5f 在假資料上必綠——**假資料驗不到多筆累積取整**。 | 守著（2026-09-07 新增。由來：實作-T-4 把「應分攤」從 `實際付出 − 差額` 反推改成誠實計算，反推必然自洽、誠實計算不保證。實測四趟正式行程：濟州島／北海道全員自洽，福岡 1 人差 2 元，東京 3 人差 2～4 元。**轉帳金額未變、帳沒算錯**，是每筆均分各自 `Math.round` 的累積。顯示方式待 Rozi 拍板。） |
| C8 | 「總花費 97,622，四個人加起來卻是 148,482」 | **守恆式**：①每一筆的各人 `shares` 加總 ＝ 該筆台幣總額（±1，算不出來的筆整筆跳過）；②全趟 Σ各成員金額 ＋ Σ算不出來的筆金額 ＝ 總花費 | `conserve.mjs`：把 production 的行程餵進真的 `calc()` 逐筆與全趟比對。<br>⚠️ 只驗畫面數字看不出來，要逐筆比對才抓得到是哪一筆灌水。 | 守著（2026-09-07 新增。由來：`驗收：濟州島` 的「藥局」——各自金額填韓元、外幣總額空白，`calc()` 把 12,000／40,000 韓元當台幣塞進 shares，該筆灌水 50,860。**追溯：先前沒有任何一條斷言在驗守恆式**，所以壞了沒人發現，是 Rozi 在手機上看到「加起來對不上」才抓到。pre-T 量到一樣的值 → 既有 bug，非實作-T 造成。修在實作-U-1。） |
| C10 | 「假資料必須涵蓋 fallback 與空值路徑」（她 2026-09-06 提過，**當時只當成一次性項目，沒進常設 → 退化了**） | 規格裡**每一條分岔**都要有一筆假資料走過，而且假資料的**形狀**要跟它宣稱模仿的正式資料一致 | 每輪跑「規格分岔 ↔ harness 參數」對照：規格 `規格_金額未定案與幣別.md` §47／§48／§49 三種分母來源各要有一個參數；**假資料的 title 若引用正式資料的名稱，形狀必須逐欄相同** | 守著（2026-09-08 新增為常設。由來：§47「總額有值＋有人沒填 → 剩餘歸未填者」與 §48「總額空白＋全員都填 → 用已填加總當分母」**兩條都沒有靶**，只有 §49 有；而且 `noForTotalExpense` 取名叫「藥局」卻是 §49 的形狀，production 真正的藥局是 §48——**假資料掛著正式資料的名字、形狀卻不同，比沒有假資料更糟**，會讓人以為驗過了。補在 `_要修的.md` 第 3 節：`?fill=remainder`／`?fill=alltyped`。） |
| C10b | （2026-09-08 下午補）**新立常設條款時，要回頭掃一次既有畫面有沒有同類缺口**，不能只管新做的功能 | 立 C10 的同一天下午，Rozi 就在結算頁撞到同一類缺口（假資料沒有「已結算後又改帳」的狀態，所以「差額＝實際付出−應分攤」那條斷言在假資料上永遠成立） | 每次新增常設條款時，列出既有畫面逐一對照的結果 | 守著（由來：實作-AC） |

---

## 2026-09-06 這一輪（實作-O ／ 實作-P）

| # | Rozi 的需求（她的話） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「匯率還是沒有帶 1。我要的是，台幣或外幣其中一欄有輸入匯率，另外一欄就會自動帶 1」 | O-7 | 停止 13a～13d：兩欄一開始都空／填台幣外幣變 1／清空 1 消失／改填外幣台幣變 1 | 守著 |
| 2 | 「現在沒辦法刪除行程了，也沒辦法分享行程」 | O-8 | 停止 14（三個入口網址對＋畫面非空）、15（金絲雀：改回舊寫法必須紅） | 守著 |
| 3 | 「編輯行程頁應該跟建立新行程的欄位一致」 | O-5 | 停止 9（兩頁欄位集合完全相同、名稱都叫「這趟叫什麼？」） | 守著 |
| 4 | 「支付方式和現金匯率在建立頁就可以顯示，只是不設定必填」 | O-5b | 停止 9（集合相同就涵蓋）＋ 停止 10（這兩塊不得帶 `.req`） | 守著 |
| 5 | 「必填的欄位後面標 `*`」 | O-6a | 停止 10（`.req` 恰好 4 個，且就是指定那四個） | 守著 |
| 6 | 「必填欄位未填，要自動跳到未填的欄位提示使用者」 | O-6d | 停止 11（`document.activeElement` 是那個 input，且沒發出寫入請求） | 守著 |
| 7 | 「回程不標 `*` 也不擋」 | O-6b | 停止 10 反向斷言（「回程」不得帶 `.req`，且要在「剛好 4 個」成立時才算過） | 守著 |
| 8 | 「『這趟怎麼結算？』預設『誰欠誰就轉給誰』，但不鎖死必填」 | O-6c | 停止 10 反向斷言（不得帶 `.req`） | 守著 |
| 9 | 「這一欄位只要有調整，就改變結算方弍」 | O-6c | **停止 18**（`settlement_mode`／`hub_member_id` 真的寫進去＋反向斷言） | 守著（**原本漏掉，2026-09-06 補**） |
| 10 | 「『去哪』改『這趟叫什麼』」 | O-5c | 停止 9（`s02` 不得再出現「去哪？」） | 守著 |
| 11 | 「底色你還沒改成現在跟 S-00 的底色一樣」（App 圖示） | P-5 | 停止 9／10／11：`#7C2D12` 命中 0、SVG 渲染後左上右下取像素比色、theme-color 兩處一致 | 守著 |
| 12 | 「跑完實作-O 就接續跑實作-P，不用等我打收」 | 佇列總控區塊 | — 這是流程指示，不是產品需求 | 不適用 |
| 13 | Cowork 發現：只填外幣的消費結算會整筆跳過 | O-1 | 停止 2（存出的 row 逐欄斷言，6 條單元測試） | 守著 |
| 14 | Cowork 發現：各自付各的用外幣填會算到付款人頭上 | O-2 | 停止 2（`split_amount === calc().shares[id]`、Σ = `twd_amount`）＋停止 3 金絲雀 | 守著 |
| 15 | Cowork 發現：新版部署了但使用者拿不到 | O-4 | 停止 7（build 版本可辨識）、8（SW 立即更新＋chunk 失敗自救） | 守著 |
| 16 | 假資料要涵蓋 fallback 與空值路徑 | O-3 | **停止 17**（`?fill=for`／`?forOnly=1` 真的改變畫面，含反向斷言）、16（`__HARNESS_FIXTURE__`） | 守著（**原本漏掉，2026-09-06 補**） |
| 17 | 輸入框高度沒有階梯（C-2） | P-1～P-4 | 停止 2（集合恰好 `{40px,28px}`）、3（數量下限）、4（token 化）、5（金絲雀） | 守著 |

### 實作-Q 的斷言實際落在哪一支（終端機 2026-09-06 交付時填）

| 停止條件 | 檔案 | 反向驗證（拿掉什麼 → 紅） |
|---|---|---|
| 7／8 方向判定與存進去的組合 | `src/components/TripFormSheet.test.tsx`（`rateDirection`／`rateColumns`／`rateFromColumns`） | 判定改回「跟 1 比」→ 2 紅 |
| 2 千分位與剝離 | `src/lib/amount.test.ts`（8 條）＋ `實作_匯率與千分位測試.cjs` | 存檔前改回 `Number()` → 1 紅 |
| 9／10／11 一格・白話・換個方向・端到端 | `實作_匯率與千分位測試.cjs` | 舊的兩格復活 → `實作_輸入框與選單測試.cjs` 紅 |
| 12 重算範圍 | `src/components/save-rows.test.ts`（`twd_from_rate` 三筆情境） | 重算不看 `twd_from_rate` → 2 紅 |
| 3 先去看一下 | `實作_匯率與千分位測試.cjs`（驗畫面不驗網址） | 那一列改回 `navigate('/trips/:id')` → 1 紅 |
| 4 圖示 | `實作_匯率與千分位測試.cjs` | icon 不帶 base → 1 紅｜拿掉 apple-touch-icon → 4 紅 |

> ⚠️ **端到端那條的期望值改成「由這一趟真正的兩欄算出來」，不寫死 64,785。**
> 原本 `?rate=full` 的假資料是 `twd:1, for:0.21`——那對 KRW 是對的、**對 JPY 是反的**，
> 寫死數字會連同錯的方向一起被當成期望值。假資料反了，測試就守不到東西。

### 這一輪機器驗不到、只有 Rozi 在手機上看得到的

| # | 項目 | 為什麼機器驗不到 |
|---|---|---|
| H1 | iOS Safari 上匯率欄位連打是否真的不收鍵盤 | 無頭 Chrome 沒有軟鍵盤 |
| H2 | 匯率兩欄從 38 拉到 40 之後，20px 數字有沒有被切到 | 真機字體與 Chrome 不同 |
| H3 | 行程名稱從 46 降到 40 之後，建立行程頁會不會太擠 | 觀感 |
| H4 | iOS 日期欄位仍然正常 | 這一處修過三次，只有真機說了띗 |
| H5 | App 圖示換色後的實際樣子 | 要先刪掉主畫面舊圖示、重新加入一次，iOS 才會換圖 |

---

## 實際跑起來的斷言在哪一支（終端機 2026-09-06 實作-O／P 交付時填）

| 停止條件 | 檔案 | 條數 | 反向驗證（拿掉什麼 → 哪一條紅） |
|---|---|---|---|
| O-2（存出的 row 形狀，6 條起跳）｜O-1b 補算 | `src/components/save-rows.test.ts` | 11 | 2-b 改回 `v == null` → 1 紅｜拿掉存檔換算 → 2 紅｜補算不排除「已有台幣」 → 1 紅 |
| O-8 三個入口｜O-9 兩頁欄位一致｜O-10 必填｜O-11 未填會跳｜O-12 日期欄 CSS｜O-4 分享頁挑 confirmed｜O-16 harness 出口｜**O-17 假資料開關實效** | `實作_寫入與更新測試.cjs` | 61 | 分享改回 `setShareSheetOpen` → 紅｜回程加 `.req` → 3 紅｜`settlements=many` 不生效 → 3 紅｜`fillFor` 永遠 false → 5 紅 |
| O-4a／b／c SW 更新與 chunk 自救 | `src/lib/swUpdate.test.ts` | 8 | — |
| O-7 匯率動態帶 1 | `src/components/TripFormSheet.test.tsx`（`nextRate` 純函式 ＋ 畫面）／`實作_輸入框與選單測試.cjs` | 4 ＋ 12 | — |
| **O-18 結算方式改了要真的寫進去** | `src/components/TripFormSheet.test.tsx` | 4 | 拿掉建立頁 `settlement_mode` → 2 紅｜`hub_member_id` 永遠 null → 1 紅 |
| P-2／3／4 高度兩階｜P-5 圖示 | `實作_高度階梯與圖示測試.cjs` | 54 | `--h-field` 改 44 → 3 紅｜`.rateinput` 寫回 38px → 2 紅｜漸層方向對調 → 4 紅｜`theme_color` 只改一處 → 1 紅 |

**C2（宣告尺寸 vs 渲染尺寸）這一輪的落點**：`實作_高度階梯與圖示測試.cjs` 的
「高度集合恰好 {40px, 28px}」＋「CSS 與元件裡都不得再有輸入框高度字面值」兩條——
字面值散在元件裡就是「宣告與渲染各走各的」的起點。

---

## 歷史：曾經沒人守、後來被 Rozi 在手機上抓到的（不要再犯）

| 需求 | 當時的狀況 | 現在誰在守 |
|---|---|---|
| 沒 emoji → 名字第一個字＋填色圓底 | 版面回歸 **263 條全綠**，Rozi 連三批回報同一件事。11 處從頭到尾沒有斷言 | C1 |
| 記一筆彈出頁被裁 191px、「記下來」按不到 | `.sheet{overflow:hidden}` 壓掉 `overflow-y-auto`，字串比對全綠 | C2＋量捲動容器 |
| 就地編輯框撐滿整列、名字被擠出框外 | 全域 `input[type=text]{width:100%}` 壓掉 `w-8` | C2 |
| 支付方式框從 39px 塌成 26px | **修上一條的方法本身造成的**（`:where()` 降到 (0,0,0)） | C2 |
| 212 項對帳表 | 判定規則是「有測試檔＝完成」，212 項裡只有 12 項真的被量過 | 對帳表已作廢；驗收判定改由 Cowork 寫 |

---

## 2026-09-06 深夜（實作-Q）

| # | Rozi 的需求（她的話） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「日幣匯率是 0.21，308500×0.21=64785，你現在換算的台幣金額是錯誤的」 | Q-1 | 停止 7（方向判定 7 組值）、8（存進去的組合）、9（端到端 = 64,785） | 守著 |
| 2 | 「0 以下的數字匯率用乘號，0 以上的數字用除號」 | Q-1b | 停止 7 —— **她的規則是特例**，判準改成「跟幣別量級比」，測試含 USD 32／CNY 4.4 兩個她的規則會反的案例 | 守著 |
| 3 | 「我做這個功能要幹嘛？有做等於沒做」（否決「使用者自己選欄位」） | Q-1a | 停止 11（`.rateinput` 恰好 1 個，反向斷言） | 守著 |
| 4 | 「『記一筆』的數字欄要有千分位分隔符號」 | Q-2 | 停止 2（顯示值 `'308,500'`、`parseAmount` 三組值、金絲雀） | 守著 |
| 5 | 「『先去看一下』按下去沒有任何反應」 | Q-3 | 停止 3（點那一列出現編輯表單、點按鈕出現「還沒算清楚 · N 筆」；驗畫面不驗網址） | 守著 |
| 6 | 「刪掉主畫面舊圖示重新加入，顏色並沒有被改到」 | Q-4 | 停止 4（manifest 每個 icon fetch 回 200、PNG 實際 180×180、四角取樣藍調、反向斷言防紅調） | 守著 |
| 7 | 「改完跑一次重算」（同意動系統推算出來的金額） | Q-1d | 停止 12（三筆情境＋金絲雀：手填那筆被改就必須紅） | 守著 |
| 8 | 「我不要你改那筆 1,469,048」（要邏輯連動，不要手改） | Q-1d | — 由 migration 一次性標記＋重算，Cowork 套 production 前先 `begin…rollback` 報影響筆數 | 守著 |

### 這一輪機器驗不到、只有 Rozi 在手機上看得到的

| # | 項目 | 為什麼機器驗不到 |
|---|---|---|
| H6 | 那行白話（「1 日圓 ＝ 0.21 台幣」）讀起來會不會怪 | 語感 |
| H7 | 千分位在打字過程中游標會不會亂跳 | 真機軟鍵盤的輸入節奏與無頭瀏覽器不同 |
| H8 | App 圖示換色後的實際樣子 | 要刪掉主畫面舊圖示、重新加入一次 |

---

## 2026-09-06 深夜二（實作-R）— ✅ 2026-09-07 已上線並複驗通過（28+15 綠、金絲雀 7 紅）

| # | Rozi 的需求（她的話） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「按『換個方向』沒有反應」 | R-1a | 停止 2（`.ratelink` 可點區過門檻）＋停止 5（真的 `.click()`，白話那行要跟著變） | 守著 |
| 2 | 追溯 R-1 找到的同類：成員移除鈕 24px、可點的 avatar 28px | R-1b／R-1c | 停止 2（同一條門檻，含目標數量下限守門） | 守著 |
| 3 | 「不要改 A 壞 B」（撐大可點區不可以偷到鄰居的點擊） | R-1d | 停止 3（`elementFromPoint` 逐顆比對） | 守著 |
| 4 | 「沒點名的東西不要碰」（B 類矮但寬的按鈕不動） | R-0 B 類 | 停止 6（反向斷言：`.chip` 仍 35px、`.rowb` 仍 41px、分頁鈕仍 39px）＋停止 8 白名單 | 守著 |
| 5 | 版面不可以被撐開 | R-1a 負外距 | 停止 4（九畫面捲動高度基準值，差距 ≤ 4px） | 守著 |
| 6 | `TripFormSheet.tsx:752` 的 `✕` 是文字字元，違反 `Icon.tsx` 的規則 | — | **這一輪不做**（已寫進「不要做的」，避免擴大範圍）；記在 `_延後與不做清單.md` | 不做 |
| 7 | 「T 太小而且漂浮在上方的感覺沒有置中」 | R-3 | 停止 9（三張 PNG 各自量 `#FEF9EE` 的 ink 邊界框：高度佔 48～56%、垂直／水平中心偏差 ≤ 2%＋反向斷言） | 守著 |
| 8 | 「『2155測試 實作OP』的消費紀錄沒辦法點，沒有反應」 | R-4a | 停止 10（已結算態 `.exprow` 是 button 且點下去**開得了**編輯表單——**反過來驗**） | 守著 |
| 9 | 「已結算不代表有按封存吧？這兩個功能應該是分開的？」 | R-4a／4c | 停止 10（已結算＝可編輯；封存＝不可編輯但點下去有 toast；兩者行為必須不同） | 守著 |
| 10 | 已結算改完帳之後，結算數字會過期 | R-4b | 停止 10（存檔後出現「重新計算」提示；正常態該字串 0 次） | 守著 |

### 實作-R 的斷言實際落在哪一支（終端機 2026-09-07 交付時填）

`實作_可點區與圖示T測試.cjs`（62 條）涵蓋停止條件 2／3／4／5／6／9／10；
`src/pages/ExpenseListPage.test.tsx` 的 `R-④` 三條涵蓋封存／已結算／§5.6 不得被動到。

| 反向驗證 | 結果 |
|---|---|
| 拿掉 `.ratelink` 的可點區擴張 | 低於門檻 **4 顆** → 紅 |
| 拿掉 `.tap44::after` | 低於門檻 **9 顆** → 紅 |
| SVG 的 `y` 改回 320 | 三張 PNG 的 T 垂直偏 **−12.5%／−12.8%／−12.8%** → 3 紅 |
| 已結算改回不可編輯 | 單元測試 1 紅、瀏覽器斷言 3 紅 |

> ⚠️ **兩個基準值指令上寫錯了，這裡用實測值**（同一支腳本、390×844、改動前的建置量的）：
> ① s01 的捲動高度指令寫 844，**實測 1688**；
> ② B 類指令寫 `.chip` 35／分頁鈕 39／`.rowb` 41，實測 **34.5／38.5／46**
> （41 那個量到的是 `[data-payrow]` 46.5，不是 `.rowb`）。
> 用寫錯的數字當基準，這兩條會**一開始就紅**，然後被當成雜訊調鬆——那才是真正的風險。

### 這一輪機器驗不到、只有 Rozi 在手機上看得到的

| # | 項目 | 為什麼機器驗不到 |
|---|---|---|
| H9 | 「換個方向」變好按之後，實際用拇指按會不會誤觸旁邊那行字 | 無頭瀏覽器沒有手指接觸面積 |
| H10 | 成員列 `✕` 擴大後，滑動清單時會不會誤刪 | 真機的滑動／點擊判定 |
| H11 | 圖示改大置中後在 iOS 主畫面上的實際觀感 | 要真機看；機器只驗得到像素比例與置中 |
| H12 | 「帳改了。記得到『結算』分頁按『重新計算』」這句讀起來會不會太囉嗦 | 語感 |

### 追溯：這個 bug 本來該被哪一條斷言擋住？

#### 第 1 項（換個方向）
**答案是「沒有規則、也沒有斷言」。** 專案裡原有的 44px 規則只寫給圖示鈕
（`.ic2`、拖曳把手），**文字鈕從來沒有人管**。
依「驗收怎麼做才算數」第 5a 條，這是指示要改的訊號 → 已提出 C6 並寫進常設區。
根因是 Cowork 自己：實作-Q 1-a 指定「沿用既有的 `.ratelink` 樣式」，
而 `.ratelink` 是 19px 高的純文字樣式。

#### 第 4 項（已結算被鎖成唯讀）
**答案更糟：有斷言，但那條斷言守的是錯的行為。**
`ExpenseListPage.test.tsx:267`「已結算的行程也一樣：列不可點」是終端機自己寫的單元測試，
它**照著實作的認知寫**，沒有回頭對 `畫面地圖.md:118`
（「已結算 → 列表唯讀（**點擊仍可編輯，重新計算**）」）。
`ExpenseListPage.tsx:288` 的註解甚至把正確行為標成「既有 bug」。
→ 指示新增：**任何標成「既有 bug」而要移除的行為，動手前必須引用一份文件說明它為什麼是 bug；
引不出來就是規格，不准當 bug 修掉。**（已寫進交付前檢查第 6 條。）

---

## 2026-09-07（實作-S）

| # | Rozi 的需求（她的話） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「說明變得不太清楚，沒有案例顯示差別在哪」 | S-1 | 停止 2（逐字比對兩個方向的換算範例、USD 示範金額、**一致性斷言**：範例的台幣金額必須等於記一筆算出來的） | 守著 |
| 2 | 「這算是使用者體驗的問題，自己的工作要自己主動發現」 | 指示第 7／8／9 條 | 交付前 UX 三題＋`_驗收案例.md`＋審查軌跡要寫具體發現 | 守著（流程，非產品需求） |
| 3 | 「可以開始順手處理成員移除鈕」 | S-2 | 停止 4（sheet 文字不得含 `✕×＋✓⠿`、可點區仍 44×44、看得見仍 24×24、金絲雀、數量下限守門） | 守著 |
| 4 | 「『記一筆』要多一個備注欄」 | S-3 | 停止 5（標籤恰好 1 個、高度∈{40,28}、200 字上限、空值存 null） | 守著 |
| 5 | 「這個備注不用列在總行程頁」 | S-3c | 停止 5 反向斷言（s03／s06 不得出現備註內容，且先斷言 s04 讀得到——防假通過） | 守著 |
| 6 | 「2155 那趟我自己測試完自己會刪」 | — | **這一輪不做**，Cowork 不碰那趟資料 | 不做 |
| 7 | 「日期改抓上一筆的消費日期…我現在要填寫舊的行程，要自己手動每一筆改日期」 | S-4 | 停止 8（六條單元測試，含「用 `expense_date` 最大值就會紅」那條、軟刪除跳過、編輯不受影響的反向斷言） | 守著 |
| 8 | 「說明文字有兩段…應該讓他們要同時顯示」 | S-5 | 停止 9（未定案狀態下兩句**同時**在，缺一即紅；舊字串出現 0 次；該句全站只出現 1 次；320/390/414 不換行；placeholder 仍是自動換算金額） | 守著 |
| 9 | 「使用者會看這麼多文字嗎？」（她自己的疑慮） | S-5 | 改法回應：教學句移到區塊標題右側，**不多佔一行**，視覺重量低於警示句 | 守著（設計回應，非斷言） |
| 10 | 「『記一筆』按鈕…會被排擠到後面，請讓按鈕維持在固定位置」 | S-6 | 停止 10（捲到最上／最下都在視窗內、`position:sticky`、最後一筆不被蓋住、已結算態反向斷言、S-04 沒被動到） | 守著 |
| 11 | 「這一輪實作-S 跑完就做總驗收」 | — | 複驗時要跑完 `_驗收案例.md` 全部案例，不是只跑核心區 | 守著（流程） |
| 12 | 「金額那題照 🟢 走」 | S-5 | 拍板：教學句移到區塊標題右側 | 已定案 |
| 13 | 「外幣金額都有灰色呈現…才看得出來是用哪個金額結算」 | S-7 | 停止 11（兩欄 color 不同、外幣對白底對比 ≥4.5、與 placeholder 灰相差 ≥1.8、台幣色未動、外幣視角反向斷言、字級字重未動） | 守著 |

### 實作-S 的斷言實際落在哪一支（終端機 2026-09-07 交付時填）

`實作_備註與說明測試.cjs`（64 條）涵蓋停止條件 2／4／5／9／10／11；
`src/components/save-rows.test.ts` 的 `S-③`／`S-④` 九條涵蓋停止條件 5 的 row 形狀與 8 的預設日期。

| 反向驗證 | 結果 |
|---|---|
| `✕` 放回去 | 2 紅（含 svg 數量那條） |
| 拿掉存 `note` | 單元 2 紅 |
| `note` 存空字串而不是 null | 單元 1 紅 |
| 預設日期改用 `expense_date` 最大值 | 單元 1 紅（就是這條在擋錯寫法） |
| 拿掉 `.btnrow.sticky` | 2 紅（`bottom:auto`、一進畫面 bottom 1604 > 844） |
| 外幣欄不掛 `forcur` | 2 紅 |
| 拿掉常駐教學句 | 2 紅 |

> ⚠️ **指令上的 `47,619,048` 多了三個 0**：「1 台幣 = 0.21 日圓」→
> 10,000 日圓 ÷ 0.21 = **47,619** 台幣。用同一支 `tripRate()` 算出來就是 47,619，
> 斷言照正確值寫，並另加一條「兩個方向的範例數量級要差 10 倍以上」——
> 那才是這一行存在的理由。

> ⚠️ **三支既有斷言的基準跟著這一輪動了，都是刻意的**：
> `s04` 的 28px 輸入框 3→**4**（多了備註欄）；
> `s03` 捲動高 1530→**1604**（+74 是 `.btnpad`，sticky 按鈕列的留白，不留最後一筆會被蓋住）；
> `s04` 捲動高 725→**783**（+58 是備註那一列）。
> `實作_文案唯一性測試.cjs` 的第三句也跟著縮短（拿掉「會」），原型與 src 兩邊一起改。

### 這一輪機器驗不到、只有 Rozi 在手機上看得到的

| # | 項目 | 為什麼機器驗不到 |
|---|---|---|
| H13 | 「花 10,000 日圓，記成台幣 2,100」這句讀起來夠不夠一眼看懂 | 這正是上一輪機器沒守到的那一類——語意，只有人判斷得出來 |
| H14 | 移除鈕換成圖示之後，在真機上看起來會不會太小或太粗 | 真機字重與螢幕不同 |
| H15 | 備註欄放在表單最後，記帳時會不會找不到 | 使用流暢度 |
| H16 | 「填一邊就好，另一邊自動換算」放在標題右邊，實際掃視時會不會被忽略 | 觀感／視線 |
| H17 | 記一筆按鈕固定之後，在 iPhone 有 Home Indicator 的機型會不會太貼底 | 真機安全區 |
| H18 | 事後補記時「跟著上一筆」是不是真的比較順手 | 要她實際補完一趟才知道 |
| H19 | 外幣灰在真機（不同螢幕亮度／深色模式）看起來夠不夠清楚 | 對比度算得出來，觀感算不出來 |

### 追溯：這一輪 Rozi 回報的問題，哪一條斷言本來該擋住？

**「說明看不懂」——沒有規則、也沒有斷言。**
`verify_Q.cjs` 驗了「白話那行有沒有跟著變」，`verify_R.cjs` 驗了「按不按得到」，
**沒有一條在問「看不看得懂」**。依第 5a 條 → 指示新增第 7、8、9 條（見 `_規則由來.md`）。

---

## 2026-09-07（實作-T）

| # | Rozi 的需求（她的話） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「『還沒算清楚』，這個消費紀錄點不進去」 | T-1 | 停止 2（列是 button、點下去開表單、守門筆數、封存態反向） | 守著 |
| 2 | 「『花費金額』的游標會有點跑版」 | T-2 | 停止 3（`line-height === height`，逐一比對） | 守著 |
| 3 | 「『花費』前面的 icon 的游標很難點出來」 | T-3 | 停止 4（就地編輯框可點區 ≥44、看得見仍 26、自動聚焦、空 emoji 時有 svg） | 守著 |
| 4 | 「結算頁應該要看得到怎麼算出來的細節」 | T-4 | 停止 5（三段筆數與金額、三條一致性等式、無代墊者反向、金額不得變） | 守著 |
| 5 | 「編輯行程頁有好幾個欄位的寬度被你改窄了」 | T-5 | 停止 6（那幾列 ===350、全頁不再有 322、其餘寬度與改動前相同）。**前後對照證實實作-S 一個數字都沒改，是既有的不一致** | 守著 |
| 6 | 「記一筆按鈕太貼近底部，要跟上方返回鍵的距離一樣」 | T-6 | 停止 7（距底 ===30、btnrow ===88、留白跟著實際高度、不得再寫死 74） | 守著 |
| 7 | 「有填外幣數字就要顯示使用者填的，不要顯示回推的」 | T-7 | 停止 8（填過的原值逐字比對且不帶「約」、沒填的帶「約」、台幣視角不變、守門） | 守著 |
| 8 | 「返回後畫面會變窄再變寬，有閃爍感」 | T-8 | 停止 9（動畫期間六個時間點都不得橫向捲動＋金絲雀＋三寬度）。**根因已量到：`translateX(100%)` 讓文件在那 200ms 變成 780px 寬** | 守著 |
| 9 | 「外幣灰在手機上還是有點深，跟台幣沒辦法馬上分辨」 | T-9 | 停止 10（對比度落在 4.4～4.7、台幣仍 ≥17、與 placeholder 相差 ≥1.8、字級字重不變） | 守著 |

### 實作-T 的斷言實際落在哪一支（終端機 2026-09-07 交付時填）

`實作_九項回饋測試.cjs`（77 條）涵蓋全部九項的停止條件 2～10。

| 反向驗證 | 結果 |
|---|---|
| 動畫改回舊寫法（`.animate-slide-in` 自己帶 transform） | 三寬度 × 六個取樣點全紅 |
| 拿掉 `.tripform .fld` 的內距歸零 | 4 紅（322 又回來了） |
| 底部距離改回 16 | 2 紅（距底 16、btnrow 74） |
| 外幣不用原值（回推） | 1 紅（顯示 106,640 而不是使用者填的 108,000） |
| 「還沒算清楚」改回唯讀 | 3 紅 |
| 代墊把 personal 也算進去 | 1 紅（`實際付出 − 應分攤 ≠ 差額`，差 300） |

> ⚠️ **三個在實作過程中發現、指令上沒寫對的地方**：
> ① **`overflow-x: clip` 必須加在「被位移的元素的父層」**。第一版我加在
> `.animate-slide-in` 自己身上——位移的就是它，裁自己的內容沒有用，量出來還是 780。
> 改成「動畫掛子層、父層只當裁切容器」才有效。
> ② 指令寫「應分攤 − 實際付出 = 差額」，**App 的正負號是反過來的**
> （`v > 0` 顯示「可以拿回」＝付得比分攤多）。斷言照 App 的慣例寫。
> ③ 假資料的 `settlementItems` 原本寫死 20220/17740/8220，**與這些假消費算出來的
> 淨額對不起來**——那三條一致性等式在假資料上本來就不成立，驗不了。
> 改成從 `settleTrip()` 真的算出來。

### 這一輪機器驗不到、只有 Rozi 在手機上看得到的

| # | 項目 | 為什麼機器驗不到 |
|---|---|---|
| H20 | 游標在 iOS 上到底正不正 | 無頭 Chrome 不畫游標；只能保證行盒與框對齊 |
| H21 | 外幣灰改成 `#6A7980` 之後在真機夠不夠淡 | 對比度算得出來，觀感算不出來。**這已是不低於 AA 的下限，再淡就讀不清楚** |
| H22 | 結算明細三段的名稱讀不讀得懂 | 語意 |
| H23 | 返回動畫改完之後順不順 | 動畫觀感 |

### 追溯：這九項本來該被哪一條斷言擋住？

- **第 1 項**：`verify_S`／`verify_R` 的畫面清單**從來沒有包含「還沒算清楚」那一頁**
  （`?unsettled=all`）。→ 這一輪加進停止條件 2，**並列入常設掃描的畫面清單**。
- **第 3 項**：C6 **只掃 `<button>`**，那個 26×26 的就地編輯 `<input>` 不在範圍內。
  → C6 擴大到所有互動元素（已改常設區）。
- **第 8 項**：C4 只在**動畫結束後**量橫向捲動，漏掉 `translateX(100%)` 撐寬文件的那 200ms。
  → C4 改成動畫期間也要取樣（已改常設區）。
- **第 9 項**：是 Cowork 自己訂的門檻訂錯——只要求 `≥ 4.5`，沒有訂上限，
  7.03 也過。→ 改成**區間** 4.4～4.7。
- 第 2／4／5／6／7 項是新需求，先前沒有人提過，不算逃掉的 bug。


---

## 2026-09-07（實作-U）

| # | Rozi 的需求（她的話） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | Cowork 查證時抓到的：外幣總額空白時把外幣數字當成台幣 | U-1 | 單元 4 條（`shares` 全 null、存出去的 split 也 null、有填外幣總額時照常比例回推、填台幣不受影響）＋瀏覽器 3 條 | 守著 |
| 2 | 算不出來時要講得出「要補什麼」 | U-1-b | 瀏覽器：編輯那一筆出現 §2.3 的原句；外幣總額有值時不得出現（反向） | 守著 |
| 3 | 「點成員金額列進去看到『影響 Ning 的 · 0 筆』」 | U-2 | 停止 5～10（標題、三段名稱、小計加總 = 那一列金額、付款小字正反、算不出來的列、可點、封存反向） | 守著 |
| 4 | 註腳要跟著改成常駐 | U-2-f | 兩種狀態下都出現「點名字看這個人的帳是怎麼算出來的」 | 守著 |
| 5 | 分享頁不受影響 | U-2-g | `.perrow` 一顆 button 都沒有（反向） | 守著 |
| 6 | 三顆 31px 的矮按鈕 | U-4 | 可點區 ≥44×44、**看得見仍 31**（反向）、中心點命中自己 | 守著 |
| 7 | hub 模式從來沒被驗過 | U-5 | `?settle=hub` 真的切過去、每一筆轉帳都經過中心人、中心人有轉出去（＝不是付最多的那個）、三段恆等式在 hub 下重跑 | 守著 |
| 8 | 「這麼多筆消費紀錄裡看不出來哪一些還需要調整」 | U-6 | 那一列的 `border-left-color` ≠ 正常列的 `rgb(223,225,218)`；正常列不得被誤標（反向） | 守著 |

### 實作-U 的斷言實際落在哪一支

`實作_這個人的帳測試.cjs`（66 條）＋ `src/components/save-rows.test.ts` 的 `U-①`（4 條）。

| 反向驗證 | 結果 |
|---|---|
| U-1 改回把外幣當台幣 | 單元 2 紅＋瀏覽器 2 紅（那一筆算出 12,000） |
| 拿掉 `.clearbtn` 的擴張 | 2 紅（78×31、108×31） |
| 拿掉常駐註腳 | 2 紅 |
| 拿掉 U-1-b 的提示 | 1 紅 |
| `?settle=hub` 不生效 | 4 紅 |
| U-6 不標第三種狀態 | 2 紅 |

> ⚠️ **`d489420` 覆核結果：那一支 commit 只動了 `_指令佇列.md`（+17 行），
> 沒有碰任何 `src/` 檔案**（`git show --numstat` 與 `git log -- src/test/harness/main.tsx`
> 兩種方式都查過）。harness 的 `unsettled`／`view=foreign` 參數與 `data-settle-row`
> 屬性是**終端機在實作-T（`39c4cc9`）做的**，有經過那一輪的複驗。
> Cowork 記成自己越界寫了 src，這件事沒有發生。

> ⚠️ **三個在實作過程中發現的**：
> ① 樁的 `.eq('id', …)` 沒有真的過濾，「編輯第 N 筆」永遠拿到第一筆——
> U-1-b 的斷言一開始因此驗在錯的資料上。已修。
> ② hub 模式的正確恆等式是「**每一筆轉帳的其中一端是中心人**」，
> 不是「所有人的收款人都是同一個」（中心人也會轉出去給該拿回的人）。
> ③ 假資料的「藥妝店」把外幣數字填進 `split_amount` 卻標 `TWD`，
> 所以某人的「各自付各的」小計是負的。恆等式仍成立，是假資料本身不合理，這一輪沒動。

---

## 2026-09-07（實作-V）

| # | Rozi 的需求（她的話） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「修改消費紀錄再重新進入，更新的時間好像有時間差」 | V-1 | 停止 1／2（改完 1 秒內重開讀到新標題）、3（`staleTime` 未動）、4（存檔與刪除兩處都清） | 守著 |
| 2 | 贊助在「{名字} 的帳」符號相反、被算進小計 | V-2 | 停止 5（兩畫面都以 `−` 開頭、顏色相同）、6（不顯示「你付的」）、7（小計差額＝贊助兩倍） | 守著 |
| 3 | 警示行的圖示與文字被拆成兩行 | V-3 | 停止 8／9／10（圖示與文字 top 差 ≤4、320 下真的換行、`.note.calm` 一起） | 守著 |
| 4 | 備註提示文字縮短 | V-4 | 停止 11（`placeholder === '補一句'`） | 守著 |
| 5 | 首頁頂部列固定 | V-5 | 停止 12／13（捲 300px 後 top ≤4、底色不透明、不蓋住內容） | 守著 |
| 6 | 行程頁頂部捲動時收合成一條 | V-6 | 停止 14～20（展開 ≥140 有日期／收合 ≤64 無日期／黏頂／兩顆鍵仍 44×44 且沒被蓋／標題還在／底色不透明／一進畫面看得到 2 筆／五個捲動位置都不撐寬） | 守著 |

### 實作-V 的斷言實際落在哪一支

`實作_時間差與收合測試.cjs`（84 條）。

| 反向驗證 | 結果 |
|---|---|
| 不清單筆快取（V-1 改回去） | 1 紅（讀到舊標題「阿明的計程車」） |
| 警示行改回 `flex-wrap: wrap` | 2 紅（圖示與文字 top 差 20） |
| 首頁頂部列不固定 | 1 紅 |
| hero 不收合 | 6 紅（底色透明＋兩顆鍵被蓋住） |
| 贊助不加負號 | 2 紅 |

> 🔴 **三個「量測靶自己是假的」的問題，這一輪才被挖出來**（都在 `src/test/harness/`，不進 production）：
> ① **stub 的寫入完全不改動假資料**（V-0 指令已指出）——「改 → 存 → 重開」永遠讀到原始值。
> ② **寫入是 eager 執行的**：PostgREST 的寫法是 `.update(row).eq('id', x)`，
> `.eq()` 在 `.update()` **之後**才呼叫。一收到 `.update()` 就動手，`byId` 還是 null，
> **會把整張表都改掉**。改成延後到真的 `await` 才執行。
> ③ **讀出去回傳同一個陣列參考** → react-query 的 structural sharing 判定「沒變」→
> 畫面不重繪，看起來像快取沒清。改成每次讀都複製一份（真實網路回應本來就是新物件）。
> ④ **量測靶的 QueryClient 是 `retry:false, gcTime:0`**，快取等於不存在——
> **V-1 的金絲雀第一次跑是綠的**（＝拿掉修法也不會紅）。
> 改成與 `src/App.tsx` 相同的 `staleTime: 30_000` 之後才紅得出來。
> **量測靶的設定與 production 不一致，等於那一類 bug 永遠驗不到。**

### 實作-V 的 Cowork 複驗（2026-09-07　`verify_V.cjs` 39 條，pre-V 對照組現場建置）

對照組怎麼來的：`9bc7269`（V 之前的最後一個程式碼狀態）開 worktree，
**把改後的 `src/test/harness/` 整份複製過去**再 build——不複製就量不到差別，
因為 V-0 改的正是量測靶本身。兩份分別掛在 8851（V）／8852（pre-V）。

| 停止條件 | 結果 | 量到什麼 |
|---|---|---|
| 0 守門（兩份都要） | ✅ ×4 | stub 寫入後再查拿到新值，而且**只改到那一列**（沒有波及整張表） |
| 1 反向（pre-V 必須紅） | ✅ 紅過 | pre-V 重開讀到舊標題「機場接送」，而**清單已經是新的**——正是 Rozi 描述的症狀 |
| 2 正向 | ✅ | V 上 1 秒內重開讀到「ZZ改過的標題」 |
| 3 `staleTime` 未動 | ✅ | `src/App.tsx:14` 仍是 `retry: 1, staleTime: 30_000`，7b30af1 沒有碰這個檔 |
| **4 新增路徑** | ❌ **量不到** | 見 `_要修的.md` H-1：量測靶的新列沒有 `expense_splits`，一點開就白屏 |
| 5／5a／5b／6 贊助 | ✅ | 兩畫面都 `−$ 12,500`、顏色都是 `rgb(39,110,69)`（＝`--in`，不寫死色碼）、不含「你付的」 |
| **7 小計語意一致** | ❌ **等 Rozi 拍板** | Alex 段小計 7,620 vs 總行程頁 32,620，差 25,000 ＝ 贊助 12,500 的兩倍（7b ✅ 驗到就是這個數） |
| 8／9／9b／9c 警示行 | ✅ | 圖示與文字第一行 top 差 ≤4；320px 下文字佔 2 行且左邊界對齊；不橫向捲動 |
| 10 `.note.calm` 未被波及 | ✅ ×4 | s05／s05+settled 各 390／320 都同高 |
| 11 placeholder | ✅ | 完全等於「補一句」，且全表單只有一個 |
| 12／12b／13 首頁 | ✅ | 捲 300px 後 top 0、底色 alpha 1、捲到最上面不蓋住第一張卡 |
| 14～20＋15b hero | ✅ | 0→151 有日期／400→56 黏頂無日期／兩顆鍵 44×44 且中心命中自己／標題還在／alpha 1／一進畫面 9 筆完整可見／五個捲動位置都不撐寬／中段有過渡 |

**常設回歸**：verify_T 41 ✅／verify_S 62 ✅／verify_R 28 ✅／verify_U7 28 ✅／
standing_C6 **113 個互動元素** ✅／verify_U 29 中 3 紅（19d ×2 為已知的 §5.6「約」；
第 7 條與上表同因）／**C8 守恆式**在 Rozi 的正式帳（`驗收：濟州島` 111 筆）**差 0**。

> `verify_R` 的版面基準**沒有變**（28 條全綠）。原因不是沒量到：
> sticky 與 scroll-driven 的收合**都不改變文件高度**，
> 九個畫面的 `scrollHeight` 與上一輪逐項相同。基準沒有被改成「現在量到的值」。

### 這一輪機器驗不到、只有 Rozi 在手機上才看得到的

1. 改完一筆立刻重開看到的是不是新的——**真機的網路來回比 Chrome 慢**，
   斷言量的是「快取有沒有被清」，量不到「網路回來要多久」
2. hero 收合的**過渡順不順**、收合後那一條會不會太薄（56px）
3. 首頁頂部列固定之後，捲動時**觀感**上會不會覺得擋到卡片
4. iOS 的安全區（瀏海／狀態列）——headless Chromium 沒有 `env(safe-area-inset-top)`
5. `animation-timeline: scroll()` 在 **iOS Safari** 上的實際表現
   （Chromium 支援，iOS 17.4 起才支援；更舊的 iOS 會直接不收合＝退回現況，不會壞）

### 追溯：這一輪的問題，哪一條斷言本來該擋住？

| 問題 | 有斷言嗎 | 為什麼沒擋住 | 補在哪 |
|---|---|---|---|
| 改完一筆重開看到舊資料 | **沒有** | 從來沒有一條斷言走過「寫入 → 再讀同一筆」這條路 | 新增停止 0／1／2，並把量測靶的 QueryClient 對齊 production |
| 贊助在新頁面符號相反 | **沒有** | S-03 有斷言、新頁面是實作-U 才生出來的，沒有人把同一條套過去 | 停止 5／6，驗的是**兩個畫面的 computed 值相等**，不是各驗各的 |
| `s05&state=settled` @320 有 2px 橫向捲動 | **有（C4）但範圍圈錯** | C4 的畫面清單裡**沒有 `state=settled`**，320 寬也沒掃到那一頁 | 已加進常設清單（下方），肇因是 `.clearbtn` 78px 撞到左右邊距；**pre-V 量起來一模一樣，是既有缺口** |

### 常設清單新增（2026-09-07）

- **C4 的畫面清單補 `screen=s05&state=settled`，且三個寬度都要掃**（320／390／430）
- **C8 守恆式**每一輪對 Rozi 的正式帳跑一次（不寫死金額，只驗恆等式）

### 實作-V 收尾（2026-09-07 10:2x）

H-1 修好後重建 harness 重跑：**停止條件 4 轉綠**
（記一筆 → 存 → 立刻點開，1.2 秒內標題欄讀到剛打的字，且沒有 pageerror）。
`verify_V` 39 條剩 2 紅：第 7 條（per[] 待拍板）與 10b（既有的 320px 2px 溢出）。
常設全部重跑仍全綠。**上線 commit `de65ca7`。**

---

## 2026-09-07（實作-W）　指令已寫入，等 Rozi 按「收」

| # | 需求（出處） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 已結算／已封存不顯示「約」（規格 §5.6 白紙黑字） | W-1 | 停止 1（settled／archived 各 0 顆）、2（進行中維持 2 顆，反向） | 守著 |
| 2 | 拿掉「約」時不得順手拿掉橘紅左邊框（§5.6 沒有列它） | W-1 | 停止 3（左邊框 computed 值與進行中逐字相同，反向） | 守著 |
| 3 | 未定案入口在已結算態本來就正確不顯示，不得改壞 | W-1 | 停止 4（反向） | 守著 |
| 4 | 分享頁看到已結算行程時也不得顯示「約」 | W-1b | 停止 6；**先要停止 5**（量測靶的 rpc 回的狀態要是對的，不然量不到） | 守著 |
| 5 | 320px 下結算列不得凸出畫面 | W-2 | 停止 7（三寬度都不橫向捲動、沒有元素凸出右緣） | 守著 |
| 6 | 修版面不得犧牲可點區（C6）或識別圖（S-02c-10） | W-2 | 停止 8（可點區＋中心點命中）、9（`.avatar` 寬 ≥20） | 守著 |
| 7 | 390px 的現況不得被順手改掉 | W-2 | 停止 10（`.txrow` 高度不變，反向） | 守著 |

**這一輪機器驗不到的**：320px 真機（iPhone SE 一代）上的觀感；
名字截斷（若選方案 a）在真實長名字下好不好看。

**追溯**：這兩項都是既有缺口，`9bc7269` 量起來一模一樣。
①「約」逃掉的原因是 `StatCard` 做對了但 `ExpenseGroups` 沒有，
而斷言只驗了統計卡那一顆——**同一條規則有兩個實作點，只守了一個**。
②「320px 凸出」逃掉的原因是常設 A11／C4 的畫面清單沒有狀態變體
（`state=settled`）。兩個都已補進常設清單。

---

## 2026-09-07（實作-X）　總行程頁「只看共同的帳」開關　指令已寫入

| # | Rozi 的需求（她的話／她的拍板） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「總行程頁要設定一個開關，看共同的帳有哪些，把自己幫自己付的帳關閉」 | X-1 | 停止 1（少的剛好是那兩筆）、2（不誤傷三種）、3（`personal` 也要被抓到） | 守著 |
| 2 | 拍板：數字也一起變（總花費與每個人的金額） | X-1 | 停止 4（標題跟著改）、8（至少一人不同、沒有人變大） | 守著 |
| 3 | 拍板：留一行「另有 N 筆自己買的 $X」 | X-1 | 停止 5（恆等式：關 − 開 ＝ 摘要行）、6（筆數相符）、7（點得進去） | 守著 |
| 4 | 拍板：預設看全部，按過之後記住這一趟的選擇 | X-1 | 停止 13（重新載入仍是開的；換一趟是預設的關） | 守著 |
| 5 | 開關不得把「還沒算清楚」的警告一起藏掉（Cowork 加的安全條件） | X-1 | 停止 9（兩個狀態 N 相同，反向） | 守著 |
| 6 | 兩個畫面不得對不起來（「{名字} 的帳」要跟著篩） | X-2 | 停止 11（**兩個狀態都要驗**恆等式） | 守著 |
| 7 | 結算結果不得因為一個顯示開關而改變 | X-3 | 停止 12（轉帳清單逐字相同，反向） | 守著 |
| 8 | 開關與摘要行按得到、三寬度不橫向捲動 | 全節 | 停止 14（C6 門檻）、15（C4 三寬度） | 守著 |
| 9 | 全部都是自己買的時候不能給人一片空白 | 全節 | 停止 16（空清單要有一句話） | 守著 |

**這一輪機器驗不到的**：開關放在統計卡與清單之間好不好按、
「共同的帳」這個標題她看得順不順、摘要行的語氣。

**寫規格前實查到的兩個事實（不是推論，是對 production 與原型各量一次）**：
① `personal` 的筆**沒有任何 `expense_splits` 參與列**（原型「阿明的計程車」量到 0 列）
→ 只用「參與者剛好一人」會整批漏掉。
② Rozi 的正式帳有 **1 筆**是 `shared`、`individual_member_id` 是 null，
但實際只勾了自己 → 用 `individual_member_id` 判斷會漏掉它。
**規則必須看實際參與列，而且要 OR 上 `expense_type='personal'`。**

---

## 2026-09-07（實作-Y）　真機三項視覺問題　指令已寫入

| # | Rozi 的原話 | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「底色不要分兩塊，請改成藍色」 | Y-1／Y-2 | 停止 5／6（安全區與下面那條是同一個元素畫的，收合與展開都要）、7（收合態底色＝`theme-color`）、8（不得再出現 `#0F5E9E`，反向） | 守著 |
| 2 | 「圈選的位置好像有 1px 的間隔，卡片上滑會透出底色」 | Y-3 | 停止 10（五個分數捲動位置 × 每 0.25px 取樣全部命中頂部列）、11（不得靠負位移解，反向）、12（`::before` 不吃點擊，反向） | 守著 |
| 3 | 「『ziyu 的帳』區塊的底色，要跟下方消費紀錄區塊的底色一樣」 | Y-4 | 停止 13（四個畫面逐一比對，不寫死色碼）、14（第五個使用者 MoreSheet 沒被改壞，反向） | 守著 |
| 4 | 根因：安全區從來沒生效（Cowork 查到的，她沒講） | Y-1 | 停止 1（守門：`--sat` 吃得到）、2（viewport 有 `viewport-fit=cover`）、3（CSS 不得再直接寫 `env()`，反向）、4（十一個畫面都沒被瀏海蓋住） | 守著 |

### 這一輪機器驗不到、只有 Rozi 在手機上才看得到的

1. **Y-3 的 1px 縫本身**——我在 DPR 2 與 DPR 3、五個分數捲動位置、
   視窗頂端 0～3px 每 0.25px 取樣，**全部命中 `.topbar`，重現不了**。
   那是 iOS 的合成層次像素問題。斷言只能守「版面上沒有縫」，
   守不了「iOS 合成時沒有縫」。
2. 真機瀏海高度不是 59px（各機型不同），`?sat=59` 只是模擬。
3. 加了 `viewport-fit=cover` 之後，home indicator 那一條在真機上的實際觀感。
4. 收合條改成 `#1276C4` 之後，跟行程色漸層在捲動中途的過渡好不好看。

### 追溯：這三項本來該被哪一條斷言擋住？

| 她抓到的 | 有斷言嗎 | 為什麼沒擋住 |
|---|---|---|
| 頂部分兩塊 | **有一半**（`verify_V` 12b／18 驗了「底色 alpha ＝ 1」） | 斷言問的是「**透不透明**」，沒有問「**安全區那一塊是誰畫的**」。而且無頭瀏覽器的 `env()` 永遠是 0，**這件事在量測靶上本來就看不見** |
| 首頁有縫 | **有一半**（`verify_V` 12 驗了 `top ≤ 4`） | 量的是元素的位置，沒有量「元素上方 0～安全區這一段是誰在畫」 |
| 標題列底色不一致 | **沒有** | 從來沒有一條斷言在管「相鄰兩塊的底色關係」 |

**共同的根因：斷言都在問「這個元素對不對」，沒有一條在問「兩塊拼起來對不對」。**
Y 這一節新增的 5／6／13 三條就是這一類，**要進常設**。

### 常設清單新增（2026-09-07 第二批）

- **C9 相鄰兩塊的底色關係**：頁面標題列（`.bar`）／固定頂部列（`.topbar`）／
  收合條（`.hero.collapsing`）的底色，必須與它緊鄰的那一塊一致
  （或有可見的分隔線）。每輪掃 `s01`／`s03`（展開＋收合）／`s05`／`s07`／
  `s03&member=0`／`s03&unsettled=all`。
- **C10 安全區**：`?sat=59` 下每個畫面的第一個可見元素不得被瀏海蓋住；
  頂部固定元素與安全區必須是同一個元素畫的。
  ⚠️ 這條**必須用 `--sat` 變數**才驗得到；CSS 直接寫 `env()` 就永遠是 0。

### 實作-W 追加（2026-09-07 19:34　Rozi 真機回報）

| # | 她的原話 | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 8 | 「分享連結頁，沒辦法點各自成員看他個人的消費紀錄」 | W-3 | 停止 12（成員列是 BUTTON 不是 DIV）、13（點得到、進得去）、16（三段結構與自己那邊同一套）、17（金額對得起來） | 守著 |
| 9 | 唯讀頁不得有編輯入口（既有規則 A9，這一頁是新的表面） | W-3 | 停止 14（消費列全是 DIV、0 個輸入欄、不含五個編輯字樣）、15（連點三列都不會開表單） | 守著 |
| 10 | 分享頁的「你」指不到人 | W-3 | 停止 18（不含「你付的」、含「{名字}付的」；自己那邊維持原樣的反向對照） | 守著 |
| 11 | 分享頁要知道成員列點得進去 | W-3 | 停止 19（提示句） | 守著 |

**追溯**：分享頁的成員列是 `div.perrow.ro`，自己那邊是 `button.perrow`——
**刻意降級的**，推測是為了守「唯讀頁沒有編輯入口」（A9）。
但 A9 的斷言問的是「有沒有編輯入口」，**沒有人問「該看得到的東西看不看得到」**。
把「唯讀」做成「不能點」是把兩件事混在一起了。
新增的停止條件 12～17 正好把兩邊都守住：**點得進去（12/13），而且進去之後改不了（14/15）**。

**驗收案例 A9 要擴充**：現在只掃分享頁本身，要加上
`?screen=s06&member=N` 這個新表面（否則新畫面永遠不會被 A9 掃到）。

### 2026-09-07　三節併成一節（Rozi 要求「一次跑三節」）

`實作-Y`／`實作-X`／`實作-W` 已併成 **`實作-YXW`** 一個節次，順序 Y → X → W。
原因：傳話協定是「一次『收』＝做**最新的一節**」，三個節次就要按三次「收」。
併成一節之後按一次就會全部跑完。

**併節帶來的風險與對策**：
- 風險：三批疊在一起才跑斷言的話，紅了分不出是哪一批弄壞的。
  → 指令要求**每一批做完當下就跑自己那批的停止條件**，常設回歸留到最後跑一次。
- 風險：中途 push 會讓半成品上線。
  → 指令要求**每批 commit、三批都完成才 push**。
- 風險：批 Y 改了安全區與頂部固定，會動到 `verify_V` 的 12～20。
  → 收尾那一段明寫「要逐條確認是**還是綠的**，不是改了斷言讓它綠」。

---

## 2026-09-07　實作-YXW 三批　Cowork 複驗結果（已上線 `6378b0e`）

| 批 | 判定程式 | 條數 | 紅 |
|---|---|---:|---|
| Y | `verify_Y.cjs` ＋ 2 條讀檔判定 | 12＋2 | 0 |
| X | `verify_X.cjs` | 21 | 2（同一件事） |
| W | `verify_W.cjs` | 25 | 1（同一件事） |
| 常設 | T 41／S 62／R 28／U 29／U7 28／V 39 | 227 | 1（同一件事） |
| 常設 | `standing_C6` **114 個互動元素**（多的一個是新開關）／`case_A11_A12` 5／`case_A9` **15** | — | 0 |

**4 條紅全部是同一件事**：`per[]` 是否排除贊助（Rozi：等實作-W 做完再談）。
落差固定 25,000 ＝ 贊助分攤額 12,500 的兩倍。新增 `verify_X` 11b 專門守
「**開關與分享頁沒有製造新的落差**」——那一條綠了，代表這一輪沒有把帳弄得更亂。

### 「有沒有用同一套」——量渲染結果，不是看 import

W-3 要求分享頁與自己那邊共用 `MemberLedger`。驗的方式是**並排量渲染出來的骨架**：
段落 `data-seg-key` 一致 ✅｜段標題一致 ✅｜列的 class 骨架（去掉唯讀差異後）一致 ✅｜
唯一差別是列的標籤：自己那邊 `BUTTON`、分享頁 `DIV` ✅（正是唯讀該有的差別）。

### 這一輪更新／推翻的斷言（每一條都附依據）

| 斷言 | 怎麼改 | 依據 |
|---|---|---|
| `verify_R` s03 基準 1632 → 1697 | 更新 | 量到肇因＝新開關那一列 `.swrow` 65px（10＋44＋10＋1），**與差額逐字相同**；其餘八畫面沒動 |
| `verify_U` 12a／12b 反向 → 正向 | 推翻 | 原本守 S-06-14「分享頁的列不可點」。該條寫的兩個理由：①「沒有頁面可進」——U-2 之後已存在；②「不該讓外人進編輯視圖」——由 `verify_W` 14／15 接手。規格同日已同步更新 |
| `case_A9` 畫面清單 ＋`s06&member=0` | 擴大範圍 | W-3 生出的新唯讀表面；不加就永遠掃不到 |
| `verify_X` 新增 11b | 補斷言 | 11 在拍板前必紅，但「有沒有變更糟」必須有人守 |

### Cowork 自己這一輪寫錯的四條（都是假紅，已修，註解留在程式裡）

1. 從 `harness.html` 讀 `<meta theme-color>`——**量測靶沒有那個 meta**，拿到 null。
2. 掃描上界用 `SAT+56` **猜**，落在頂部列下緣外一格，命中底下的卡片。
3. 拿「網址變了」當入口條件——**既有的「還沒算清楚」與成員列點下去也不改網址**，
   等於我把新頁面要求得比既有畫面嚴。
4. 用「上緣同高」判斷單行——按鈕 31px、文字 17px，在 `align-items:center` 的列裡
   同一行本來就差 7px。**要比垂直中心。**

> 共同的毛病：**斷言的門檻或範圍是「猜」出來的，沒有先量既有畫面當基準。**
> 前三條只要先量一次同類的既有元素就不會發生。

---

## 2026-09-08（實作-AA）　指令已寫入，等 Rozi 按「收」

| # | 需求（Rozi 的話 / 出處） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「使用者在介面上看不到有被換算成台幣金額」 | AA-2 | 停止 10（外幣填模式下恰好 2 個唯讀顯示，值逐字 `$ 276`／`$ 414`，相加 = 690） | 守著 |
| 2 | 「如果不對，他可以做修改」 | AA-1 | 停止 2（切「$ 台幣填」後兩格被換算成 276／414，可直接改）＋停止 3 反向金絲雀 | 守著 |
| 3 | Cowork 實測發現：切幣別不換算，只是改單位解讀，出現 `差 $ 52,882` | AA-1 | 停止 2、3、5、6 | 守著 |
| 4 | 切回去不准掉精度（不能 12,000 → 11,999） | AA-1 兩份值＋旗標 | 停止 4、6 | 守著 |
| 5 | 算不出比例（外幣總額空白）時不准亂換算 | AA-1 | 停止 7、8、9（值不變、無 `NaN`、空白仍空白） | 守著 |
| 6 | 空白不等於 0（全站一致） | AA-1 | 停止 9 | 守著 |
| 7 | 新顯示不得吃掉輸入框可點區（C6） | AA-2 | 停止 12 | 守著（常設 C6） |
| 8 | 320 不得橫向捲動（C4） | AA-2 | 停止 13、14（含 20 字長名字） | 守著（常設 C4） |
| 9 | 只用比例不用匯率（規格 §2.2 第 41 行） | AA-1 | 停止 2、5、6 的數字就是比例值；用匯率會算出別的數 | 守著 |
| 10 | 不得順手改壞既有的「加總不必等於總額」規則 | AA「不要做的事」 | 停止 8（`noAutoReason` 時 `.cmp` 仍不出現）、停止 16 檔案白名單 | 守著 |
| 11 | 「$ 台幣填」模式本身要有假資料可驗 | AA-3 | 停止 6（`?fill=twd`） | 守著 |

**這一輪機器驗不到、只有 Rozi 在手機上看得到**：

- 灰字台幣放在輸入框後面，在真機上會不會被誤讀成「這格也可以打字」
- 兩個數字（韓元格 33,000 ＋ 灰字 $683）並排時，會不會反而更難一眼看懂
- 切幣別時數字整批跳動，在真機上是「幫我換好了」還是「它把我打的改掉了」的觀感
- 320px 真機（iPhone SE 一代）上長名字＋數字＋灰字三段並排的擁擠程度

**追溯（第 5 條：哪一條斷言本來該擋住它）**

| 逃掉的問題 | 本來該有的斷言 | 為什麼沒擋住 |
|---|---|---|
| 切幣別不換算，出現 `差 $52,882` | **沒有**。既有斷言只驗過「外幣填」單一模式，從來沒有人切過模式 | 依「驗收怎麼做才算數」第 5a 條，這是指示要改的訊號 → 已補：**凡是畫面上有兩段以上的切換（Seg／Tab／模式鈕），斷言必須切過去再切回來，不能只驗預設那一段**。加進常設 C9。 |
| 編輯畫面看不到每人台幣 | **沒有**。第 7 題「使用者看得懂嗎」2026-09-07 才立，這個畫面是在那之前做的 | 這一輪補 AA-2；C7「這筆帳到底怎麼算出來的」原本只涵蓋結算頁三欄，未涵蓋編輯畫面 → C7 範圍擴大（見常設區） |

### 常設清單新增（2026-09-08）

- **C9　凡是有兩段以上的切換（`Seg`／分頁／篩選），斷言必須「切過去 → 量 → 切回來 → 再量」**，
  不准只驗預設那一段。由來：`fillCur` 切換不換算，兩天沒人發現。
- **C7 範圍擴大**：「這筆帳怎麼算出來的」不只結算頁，
  **凡是使用者會去修正那個數字的畫面，都要看得到那個數字**（本輪先補編輯消費的各自金額）。

---

## 2026-09-08（實作-AA 複驗結果）

`verify_AA.cjs` **23／23 綠**（commit `e7e8f6a`）。常設：C9 36 綠、C6 4 綠（114 個互動元素）、
verify_T 41／0、verify_S 62／0、verify_R 28／0、verify_U 29／1、verify_U7 28／0、
verify_W 25／1、verify_X 21／2、verify_Z 13／0、case_A9 15／0、case_A11_A12 5／0。
**所有紅都是同一個贊助議題**，等 Rozi 拍板折抵。
C8 守恆式對她改過之後的正式資料：132 筆、逐筆不守恆 0、Σ成員 = 總花費 169,978、差 0。

### Cowork 這一輪自己寫錯的斷言（五個，都已修，註解留在程式裡）

| 錯在哪 | 後果 |
|---|---|
| `?fill=noforetotal` 一律點第一列（那是「機場接送」，不是各自付各的） | 7／8 **假通過** |
| 量可點區沒先把欄位捲進畫面（y=772／834 在 844 視窗外） | 假紅 |
| 指令規則本身寫錯：「換算不出來就維持原值」＝ 同一串數字換幣別解讀，正是要修的 bug | 會做出錯的東西（已在實作階段攔下） |
| 逐個**文字節點**比對金額（React 把「$ 」與「276」拆成兩個節點） | 灰字做好了卻報紅 |
| 把「取整少的 1 元落在誰頭上」寫死成正式資料的付款人 | 假資料付款人不同 → 假紅 |

### 常設清單新增（2026-09-08，第二批）

- **C10 補充**：**改假資料的 `title` 要同步 grep 全部判定程式**。
  由來：`?fill=noforetotal` 那筆依 C10 改名之後，`verify_U7`（3 處）與 `verify_V`（1 處）
  用「藥局」當選擇器，一起瞎掉，共 6 條假紅。
- **C11　對照組要用「同一版關掉某段程式」，不要用「保存的舊版建置」。**
  由來：`verify_V` 的 0／0b／1b 是拿 pre-V 建置當對照組，
  但假資料這一輪又加了 3 個 fixture，舊建置的靶已經對不上，金絲雀跑不動了。
  舊建置會隨假資料演進而失效；「停用這一段程式，斷言必須變紅」不會。
  **verify_V 第 0／0b／1b 這一輪標記為「跑不動」，不是綠也不是紅**，
  下一輪改寫成同版停用式金絲雀。

---

## 2026-09-08（實作-AB）　指令已寫入，等 Rozi 按「收」

| # | 需求（Rozi 的話 / 出處） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「你把 app 現在怎麼結算的邏輯寫在這頁上」（截圖＝還沒結算的結算頁） | AB-1 | 停止 1（未結算頁恰好 1 顆「查看計算依據」）、3（展開後四行） | 守著 |
| 2 | 「我先前有提修改說要在這裡顯示每個人是怎麼結算的」 | AB-1 | 停止 3、5 | 守著 |
| 3 | 「自己買給自己的帳要獨立出來」（2026-09-08 拍板） | AB-2 | 停止 3（四行標籤逐字）、6（必須 import 既有 `isSelfPaid`，不得另寫） | 守著 |
| 4 | 不准改到金額（她要的是看得懂，不是換算法） | AB「不要做的事」 | 停止 5（三行相加＝應分攤、應分攤−先付出去＝畫面差額 ±1） | 守著 |
| 5 | 筆數 0 的行也要顯示（讓人看懂這一欄在講什麼） | AB-2 | 停止 4 | 守著 |
| 6 | 同一個概念在兩個畫面不能有兩種說法 | AB-2 | 停止 6 反向（`breakdownOf` 內不得出現第二套判斷） | 守著（常設 C2 精神） |
| 7 | 已結算頁不得被改壞 | AB-1 | 停止 2（反向） | 守著 |
| 8 | 可點區（C6）／不橫向捲動（C4）／切換（C9） | — | 停止 8、9、10、11 | 守著（常設） |

**這一輪機器驗不到、只有 Rozi 在手機上看得到**：

- 四行在真機上會不會太長、要不要捲
- 「這些你自己付、也算你自己，不影響要轉的錢」這句話讀不讀得懂
- 「自己買給自己的 0 筆 $ 0」對沒有這種帳的人（Xiu）是有幫助還是多餘
- 未結算頁多一顆按鈕之後，「結算行程」還是不是最顯眼的那一顆

**使用者看得懂嗎（UX ＋ Copywriter ＋ Aria，交付前第 7 條）**

1. **什麼情境會看到它？** 結算前想確認「這個數字對不對」時，點開計算依據。
2. **要幫使用者做什麼決定？** 判斷該不該按「結算行程」——數字合理就結，不合理就回去改帳。
3. **有沒有給出所需資訊？** 拆成四行之前**沒有**：Ning 的「指名算他的 $41,131」
   裡有 $39,278 是他自己的購物，使用者無從得知那筆不影響結算。
   拆開之後看得到金額，但**抵消關係仍然看不出來**（$39,278 同時出現在「自己買給自己的」
   與「他先付出去的」裡，畫面沒說它們是同一批錢）。
   → 這就是為什麼那一行要加灰字「這些你自己付、也算你自己，不影響要轉的錢」。
   **沒有這句話，第 3 題就是答不出來，等於沒做完。**

---

## 2026-09-08（實作-AB 複驗結果）　commit `c4c8e28`，已推

`verify_AB.cjs` **13／13 綠**。四行在畫面上實際渲染出來的樣子（`?screen=s05`，還沒結算）：

```
Alex    一起分的 3 筆 $20,220｜自己買給自己的 0 筆 $0｜各付各的 1 筆 $12,000｜他先付出去的 3 筆 −$78,900
Robin   一起分的 3 筆 $20,220｜自己買給自己的 1 筆 $860｜各付各的 2 筆 −$10,465｜他先付出去的 2 筆 −$1,895
Sam     一起分的 3 筆 $20,220｜自己買給自己的 0 筆 $0｜各付各的 0 筆 $0｜他先付出去的 1 筆 −$2,480
Kai     一起分的 3 筆 $20,220｜自己買給自己的 0 筆 $0｜各付各的 0 筆 $0｜他先付出去的 0 筆 −$0
```

**常設**：C9 36／0、C6 4／0（114 個互動元素）、T 41／0、S 62／0、R 28／0、U7 28／0、
Z 13／0、A9 15／0、A11-A12 5／0、hub 10／0、AA 23／0。
**紅的共 5 條**：U-7、V-7、W-17、X-11關、X-11開——**全部是同一個贊助議題**（假資料的「爸爸贊助」，
正式資料 0 筆使用，見下）。
**跑不動 3 條**：V-0／0b／1b，pre-V 對照組（C11，下一輪改寫）。

**C8 守恆式**（正式資料）：132 筆、逐筆不守恆 0、總花費 169,978 ＝ Σ成員金額、差 0。

**白名單**：`src/` 只動了 `SettlementPage.tsx`、`index.css`、`SettlementPage.test.tsx`。
`src/lib/summary.ts` **一個字都沒動**（算錢的檔案沒被碰）。

### 這一輪「改 A 壞 B」被常設套件抓到一次（這就是常設存在的理由）

終端機把資料屬性 `data-named` 改名成 `data-each`（語意更清楚，四行相加仍守恆，**接受**）。
連帶三個地方一起瞎掉，**都是常設套件抓到的，不是 Rozi 在手機上發現的**：

| 壞掉的 | 為什麼 | 怎麼修 |
|---|---|---|
| `verify_T` 5b | 它在找「指名算他的」這四個字，那個標籤已經不存在了 | 改成找四段：一起分的／自己買給自己的／各付各的／他先付出去的 |
| `verify_T` 5d | 恆等式讀 `data-named`，讀到 null | 改讀 `data-each ＋ data-self`（回不到舊名字時的相容寫法） |
| `hub.cjs` | 同上 | 同上 |

**追溯**：這一條本來就該被擋住，而且**確實被擋住了**——`data-named` 是實作-T-4 加的量測屬性，
常設套件每輪都在讀它，改名當下就紅。沒有新的斷言要補。

### 贊助議題的優先序（2026-09-08 查證後下修）

查全站資料庫：`is_sponsor = true` 的消費 **0 筆**。四趟正式行程都沒用過這個功能。
所以那 5 條紅**只存在於假資料**，Rozi 的帳從來沒被它算錯。
真要修時的兩個產品問題：贊助抵**整趟**還是抵**特定幾筆**；贊助**超過花費**時多的算誰的。

### 順手看到但沒動（給 Rozi 決定，這一輪不改）

- Kai 沒有代墊任何一筆，那一行顯示「他先付出去的 0 筆 **−$ 0**」。
  「負的零」讀起來怪，但它不影響任何金額。要不要改成 `$ 0`（不帶負號）由 Rozi 決定。

### Stop hook 的閘門（第三次發生）

終端機這一輪又是**自己 push 之後才停**，Cowork 沒機會在推之前複驗。
這一輪結果是好的，但那是運氣，不是流程。建議仍是：
hook 改成「有未推 commit」只有在 `_可以推了.txt` 存在時才擋停。**待 Rozi 拍板。**

---

## 2026-09-08（實作-AC）　指令已寫入，等 Rozi 按「收」

| # | 需求（Rozi 的話 / 出處） | 落在哪一節 | 守它的斷言 | 狀態 |
|---|---|---|---|---|
| 1 | 「這個結算區塊直接整理在各自成員的底下」 | AC-1 | 停止 1（四欄表消失）、2（每人一張卡、4 行）、3（順序） | 守著 |
| 2 | 「不能把自己買給自己的放進算式內，不然很容易被理解這筆帳也要被扣掉」 | AC-1 | 停止 4（三行相加＝差額）、5（反向：加上 self 就不等於） | 守著 |
| 3 | Cowork 發現：Mei 的「他先付出去的 2,299」整筆是她自己買的，她沒幫大家墊 | AC-2 | 停止 6（Robin 1,895 → 1,035）＋反向金絲雀 | 守著 |
| 4 | 「這兩句有需要放嗎？會不會太冗長」 | AC-3 | 停止 8（「不影響要轉的錢」0 次、「總共付出」0 次、「不進結算」每人 1 個） | 守著 |
| 5 | 「bug 是因為我更新款項才有的嗎？以後會不會算錯？」 | AC-4 | 停止 11（未結算用即時＋反向金絲雀）、12（已結算維持凍結，反向）、13（有變動要提示，反向） | 守著 |
| 6 | 「要連同分享連結頁的結算畫面一起調整」 | AC-5 | 停止 14（分享頁有同樣四行）、15（同一支元件，字串合計 1 次）、16（唯讀，反向） | 守著 |
| 7 | 負零（「他先付出去的 0 筆 −$0」） | AC-1 | 停止 7（全文不得出現 `−$ 0`） | 守著 |
| 8 | 收合時的畫面不准被改壞 | AC-1 | 停止 10（逐字比對 pre-AC 的 innerText） | 守著 |
| 9 | 已結算頁不准被改壞 | AC-4 | 停止 12、20 | 守著 |
| 10 | 可點區 C6／不橫向捲動 C4／長名字 | — | 停止 17、18、19 | 守著（常設） |
| 11 | 假資料缺「已結算後又改帳」這個狀態 | AC-6 | 停止 11、13 用得到它；本身就是 C10 的補洞 | 守著 |

**這一輪機器驗不到、只有 Rozi 在手機上看得到**：

- 四個人的卡片疊起來會不會太長、要捲很久
- 「不進結算」四個字在灰字縮排的位置讀不讀得出來是「這行不算」
- 「他幫大家先付的」比「他先付出去的」是不是真的比較好懂
- 分享頁給別人看時，展開計算依據會不會資訊量過大（朋友只想知道自己要給多少）

**追溯（第 5 條：哪一條斷言本來該擋住「差額吃到舊結算」）**

有斷言（AB 停止 5b「應分攤 − 實際付出 = 差額」），**但假資料裡沒有「已經結算過、之後又改帳」
這個狀態**，所以它在假資料上永遠成立。這正是 2026-09-08 上午才立的常設 **C10**
（規格每一條分岔都要有假資料走過）講的同一種漏洞——**C10 立了，但沒有回頭盤點既有畫面**。
→ AC-6 補這筆假資料；並在 C10 加一句：**新立常設條款時，要回頭掃一次既有畫面有沒有同類缺口，
不能只管新做的功能。**
