# 稿 ↔ 碼 對照表

> ⚠️ **狀態：草稿。對應指令佇列 #9 草稿 v2，尚未經 Rozi 核可，因此本輪只做掃描落檔，未改任何稿或碼。**
> 掃描基準 commit：`a82a526`　掃描時間：2026-08-31 02:40
> 用途：打磨輪至今「稿上已決定」的每一項，對到實際要改的檔案與行號，實作批次逐條勾稽。

## 一、掃描結論摘要

| 項目 | 草稿 v2 的說法 | 實測 |
|---|---|---|
| `trip.emoji` 顯示點 | 2 處（TripListPage:119、SharePage:159）| ✅ 兩處正確，但**漏了 TripFormSheet 的 5 處寫入／UI**，見 §2 |
| 稿上 ✈️ 殘留 4 處 | 「清掉」 | ❌ **4 處都不該動**，見 §3 |
| 幽靈卡 LoginPage:69 | 仍在線上 | ✅ 正確（區塊 68–88，文案在 :80）|
| G-05 四處 | 141／285／294／393 | ⚠️ 前三處正確；橫幅是 **:394 不是 393**，且**漏列 :408** 的「之後再說」按鈕 |
| TripListPage:134 tabular-nums | 違反字體規則 | ✅ 正確 |
| tabular-nums 全站 | 逐處確認 | ✅ 已掃：**共 17 處，16 處是金額（合規），只有 1 處要清**（就是 :134）|

## 二、`trips.emoji` 的完整顯示／寫入點（草稿 A 只列了 2 處）

D 條要求「全 src 掃描一次清完」。實際有 **7 處**：

| # | 檔案:行 | 是什麼 | 動作 |
|---|---|---|---|
| 1 | `TripListPage.tsx:119` | 首頁卡右上角顯示 | 刪除（草稿有列）|
| 2 | `SharePage.tsx:159` | 分享頁 hero 顯示 | 刪除（草稿有列）|
| 3 | `TripFormSheet.tsx:46` | `coverEmoji` 初值 `?? '✈️'` | **刪除 state** |
| 4 | `TripFormSheet.tsx:158` | 編輯時 `update({ emoji: coverEmoji })` | **停止寫入** |
| 5 | `TripFormSheet.tsx:216` | 建立時 `insert({ emoji: coverEmoji })` | **停止寫入** |
| 6 | `TripFormSheet.tsx:347` | 封面 emoji 按鈕 UI | 刪除（#8-A 已定）|
| 7 | `TripFormSheet.tsx:559` | 傳給 EmojiPicker 的 cover 值 | 隨 EmojiPicker 一起刪 |

> **只清 1、2 不夠**：3–5 還在寫入，新行程仍會存 `'✈️'`。欄位保留供既有資料相容沒問題，
> 但**不該再產生新的**——否則「行程 emoji 全站退場」只退了一半。

## 三、稿上的 ✈️：4 處都不該清（草稿此項有誤）

| 行 | 內容 | 為什麼不能清 |
|---|---|---|
| 355 | 盤點表 S-00-1「App icon ✈️」 | **移除紀錄的歷史敘述**。清掉的話，「當初移除了什麼」就查不到了 |
| 641 | 盤點表 S-02-5「預設 ✈️」 | 同上 |
| 875 | S-03 消費列「機票 ×4」的 ✈️ | **類別 emoji（`category_emoji`）**，與行程 emoji 無關。清掉＝誤刪消費圖示 |
| 1376 | S-06 消費列同一筆 | 同上 |

**稿上真正的行程 emoji 殘留是另外 4 處，而且不是 ✈️：**

| 行 | 內容 | 對應 |
|---|---|---|
| 398 | S-01 濟州島卡 `class="em"` 🏝️ | TripListPage:119 |
| 408 | S-01 東京卡 `class="em"` 🗻 | 同上 |
| 418 | S-01 福岡卡 `class="em"` 🍜 | 同上 |
| 1353 | S-06-3 分享頁 hero 🏝️ | SharePage:159 |

## 四、B 條「必須保留」的，實測位置

| 保留項 | 實際位置 | 現況 |
|---|---|---|
| 行程內頁 hero 成員 emoji | `ExpenseListPage.tsx:283`（memberEmojis 組字串）→ 顯示於 :351 | 在，不動 |
| 分享頁成員與付款人 | `SharePage.tsx:122`（memberEmojis）、:246（payer）| 在，不動 |
| S-03E 空狀態成員列（決策 D）| **現況沒有**——空狀態（:437）只有兩行文字，成員 emoji 在 hero 上 | 見 §7 |

## 五、其他「稿上已決定、碼還沒動」的落差

| 稿上編號 | 決定 | 檔案:行 |
|---|---|---|
| S-00-1 | App icon 移除 | `LoginPage.tsx:20–31` |
| S-00-6 | 幽靈卡移除 | `LoginPage.tsx:68–88`（含 `animate-ghost-pulse`）|
| S-00 底色 | 暖棕漸層 → 富士青深夜版 | `LoginPage.tsx:14` |
| S-01-8 | 幽靈卡**保留**（P-2 裁定）| `TripListPage.tsx:147–166` **不動** |
| S-01-9/10 | G-06 橫幅移除 | `TripListPage.tsx:169–183` |
| S-01-5 | 成員識別列移除 | `TripListPage.tsx:131–137` |
| S-02c | EmojiPicker 整個移除 | 刪 `src/components/EmojiPicker.tsx`；呼叫端 `TripFormSheet.tsx:447, 555–573` |
| S-02c-13 | 驗證邏輯**保留** | `src/lib/emoji.ts` 的 `firstGrapheme()` **不動** |
| S-02-9 | 回程可留空 | `TripFormSheet.tsx:392–418`；必填驗證在 `:247` |
| S-03-13 | G-05 全清 | `ExpenseListPage.tsx:141, 285, 294, 394–411`（**含 :408**）＋ sessionStorage key `g05-dismissed` |
| S-03-1 | hero 改用 `destinationOf()` | `ExpenseListPage.tsx:292` |
| S-03-12 | 幣別整頁切換 | `ExpenseListPage.tsx:139, 363–383, 470` |
| S-05-16 | ✨ → 收據摺紙 | `SettlementPage.tsx:293, 295` |
| S-05-28 | 「暫時算一下」預覽 | `SettlementPage.tsx:255–275`（新增）|
| S-05-29 | 分享 CTA「把結果傳給大家」 | `SettlementPage.tsx:370–386` |
| S-06-1 | hero 改用 `destinationOf()` | `SharePage.tsx:150` |
| S-06-5 | 外幣格永遠「—」 | `SharePage.tsx:177–182` |
| 009 | `trip_members.emoji` 預設空字串 | **待 Cowork 端套用** |

## 六、F 條：`tabular-nums` 逐處判定（17 處）

**合規 16 處（都是金額）**：`ExpenseFormSheet` 460／634；`SettlementPage` 340／411／453／478／479／480；
`ExpenseListPage` 365／374／472；`SharePage` 175／179／185／209／252。

**不合規 1 處**：`TripListPage.tsx:134` —— 套在**日期**上。清掉即可。

> 所以 F 的實際工作量是 **1 行**，不是全站大改。

**順帶掃到的同類問題（草稿沒提，建議一併列管）**：
`tailwind.config.js:7–8` 的字體設定與「不載中文 webfont、標題用 PingFang TC」決策不符——
`sans` 把 `Noto Sans TC` 排在 `PingFang TC` 前面，`serif` 是 `Cormorant Garamond`（拉丁襯線，中文會落到 Georgia）。
`font-serif` 目前還有 **8 處**在用：`EmojiPicker:81`、`TripFormSheet:325`、`ExpenseFormSheet:309`、
`LoginPage:41,77`、`TripListPage:158`、`SettlementPage:295,296`。

## 七、一個要 Rozi 裁示的矛盾

B 條說「S-03E 空狀態成員列（決策 D）」必須保留，但**現況那裡沒有成員列**——
空狀態只有兩行文字，成員 emoji 顯示在行程頁 hero。

兩種可能：① 決策 D 一直是靠 hero 滿足的，那 B 條只要確保 hero 不被動到（已確認不動）；
② 決策 D 的原意是空狀態區塊本身要顯示成員，那是**要新增**、不是「保留」。

**這一項我不自行決定。** 併入第三批（S-03 起）一起裁示。
