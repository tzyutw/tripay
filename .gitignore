# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
# 實作-C-3 的版面量測靶建置產物（只在跑測試時產生）
dist-harness/

# Environment variables (NEVER commit secrets)
.env
.env.local
.env.production
.env.test

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Supabase local dev
supabase/.temp/

# PWA dev artifacts
dev-dist/

# Claude Code local settings
.claude/settings.local.json
MEMORY_*.md

# Playwright 登入 session（歷史資料匯入 Loop 任務）
.auth/
.claude/hooks/
.claude/.keepgoing_count
Claude outputs/_cowork暫存_可刪/

# Cowork 複驗用的 harness 建置壓縮檔（暫存，可刪）
Claude outputs/*.tgz

# 🔴 實作-私-1　Cowork 會把後台截圖暫存在 `Claude outputs/`，
# 那些圖裡有 Google Cloud 專案 ID、測試使用者名單與 email。
# **這個 repo 是公開的**（github.com/tzyutw/tripay，private: false），不要掃進來。
Claude outputs/screenshot-*

# 🔴 修-1　Cowork 做的決策對照稿與量測截圖是**拋棄式**的：
# Rozi 選完方案就作廢，不是產品的一部分，不該留在公開 repo 裡。
# 上一行的 `screenshot-*` 擋不到中文檔名（例：`幽靈卡_三案.png`），
# 所以這裡直接擋整個資料夾的 png。要進版控的圖請放在 `docs/` 或 `public/`。
Claude outputs/*.png

# 🔴 實作-AF-9　開工小程式的執行期檔案，一律不進版控
# （歸檔後的那幾份也算——`git add -A` 會把它們一起掃進來，這是修-1 同一個毛病）
Claude outputs/_開工_已處理_*.txt
_開工.txt
_停watcher.txt
.claude/kickoff.lock
.claude/kickoff_until
.claude/kickoff.log
